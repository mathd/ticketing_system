server {
    listen 8080;
    listen [::]:8080;
    root /usr/share/nginx/html;

    location = /healthz {
        default_type application/json;
        return 200 '{"status":"ok","service":"storefront"}';
    }

    location / {
        try_files $uri /index.html;
    }
}
