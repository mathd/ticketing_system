{
  "tracker": "local",
  "gates": "autonomous",
  "confluence": null,
  "code": {
    "repo": "ticketing_system",
    "defaultBranch": "main",
    "localGate": "make check",
    "gateVerdict": ".gate.verdict"
  },
  "models": {
    "plan": "main-agent",
    "planReview": "main-agent",
    "implement": "main-agent",
    "aiReview": "main-agent"
  },
  "registry": {
    "bindingPath": "docs/adr/",
    "referenceLocation": "local"
  },
  "modelWaiver": "Owner, 2026-09-25: run every stage on Claude Opus 5.5 (main-agent) only. codex (gpt-6-sol/gpt-6-luna) answered 401 on every call. This WAIVES the skill's cross-model invariant (plan != planReview, implement != aiReview); record it on every ticket (kind=decision) and in each closeout's overrides. Restore the gpt-* values and delete this key when codex is back."
}
