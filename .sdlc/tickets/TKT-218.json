{
  "key": "TKT-218",
  "type": "Task",
  "status": "Building",
  "labels": [
    "agent:ai-review"
  ],
  "assignee": "agent",
  "pr": {
    "number": 318,
    "url": "https://github.com/mathd/ticketing_system/pull/318",
    "state": "open"
  },
  "links": [],
  "summary": "Flaky: storefront call-budget tests fail under load, and one fails OPEN (TKT-216 gate incidental)",
  "description": "**Flaky under load, found while gating TKT-216.**\n\n`web/storefront/test/page-call-budget.test.ts` (TKT-208's call-budget proof) fails three of its six\ncases when the machine is busy:\n\n```\n× event list renders on exactly one upstream call      (timeout at 5000ms)\n× event detail renders on exactly one upstream call    (timeout at 5000ms)\n× festival detail renders on exactly one upstream call (expected length 1, got 2)\n```\n\n**It is not a regression.** Verified by checking out the parent commit (`a05d5cc6`, a docs-only\nchange sitting on a gate that was green) and reproducing all three failures there. Verified further\nby re-running with `--testTimeout=30000`: **6 passed, twice.** The transform step alone takes ~9.7s\nin these runs, against a 5s per-test default.\n\n**Why it matters beyond the noise.** The third failure is the interesting one: it does not time out,\nit counts **two** upstream calls where the budget allows one. That is the assertion TKT-208 exists to\nmake, and if load can make it report two calls, then the test cannot distinguish \"the page made a\nsecond call\" from \"the machine was busy\". A call-budget proof that fails open under load is not\nproving the budget.\n\n**Suggested direction (not a decision):**\n- Raise the per-test timeout for this file so a slow transform cannot be read as a budget violation.\n- Separately, work out whether the 2-call observation is a genuine second fetch triggered by a\n  timeout/retry path, or an artefact of the stub counting a cancelled request. Those need different\n  fixes, and only the second is harmless.\n\nDo not simply raise the timeout and close it: that hides the second question.\n\n---\n\n**Update (TKT-216 gate, same session).** The per-test timeout in that file was raised to 30s, because\nthe gate must be green and the 5s default was turning machine load into a reported budget violation.\nThe three failures stop reproducing.\n\n**That change closes the noise, not this ticket.** The substantive question is untouched and is the\nreason this ticket stays open:\n\n> one of the three observed failures did NOT time out — it counted **two** upstream calls where the\n> budget allows one.\n\nSo the assertion can still fail **open**. What remains to determine is whether a second fetch is ever\ngenuine (a retry path, a cancelled request the stub double-counts) or purely an artefact. Those need\ndifferent fixes and only one of them is harmless. A comment in the test now says the same thing, so\nwhoever raises the timeout again does not read it as resolved.",
  "context": {
    "capturedAt": "2026-08-24",
    "governingAdrs": [
      {
        "id": "ADR-004-frontend-read-model.md",
        "why": "Rule 3 (frontends are call-frugal: one call per page view, not per widget) is the invariant this test proves. VERIFY exact filename at brief time.",
        "constrains": "The budget itself is not re-litigated by this ticket — only the test's ability to measure it honestly."
      },
      {
        "id": "ADR-006",
        "why": "The SSR cache owns the minutes tier, and client islands hydrate browser-side where this test does not observe them. The test's own header comment (:13-16) records that a green result does NOT mean the page makes one request in production.",
        "constrains": "A fix must not extend the assertion to client-island reads — that is TKT-177's, explicitly."
      }
    ],
    "governingTests": [
      {
        "path": "web/storefront/test/page-call-budget.test.ts:124-133",
        "invariant": "An in-test comment left by TKT-216 warning that raising the timeout does NOT close this ticket, and naming the fail-open question. It is a live tripwire against the obvious wrong fix; it must be UPDATED (not deleted) when the fail-open path is actually closed, or it will keep warning about a question that has been answered."
      },
      {
        "path": "web/storefront/test/page-call-budget.test.ts:76-84",
        "invariant": "Records that the first version of this test called the api.ts wrappers directly and proved nothing about the page — an ai-review finding, explicitly warned against in the ticket's own plan-review. Any refactor that injects a clock must keep rendering the REAL page module through Astro's container."
      },
      {
        "path": "web/storefront/test/page-call-budget.test.ts:113-119",
        "invariant": "The render must return 200 AND every repeated child must appear in the HTML. Added in ai-review because a count alone is a weak oracle — a page that short-circuits to 503 spends exactly one call too. A fix must not weaken these."
      },
      {
        "path": "web/storefront/src/lib/cache.ts (PageDataCache constructor)",
        "invariant": "Already accepts `fetchImpl` and `now` for injection — the seam this fix needs exists. The class also documents a KNOWN unclosed gap: after a backward wall-clock step an entry can outlive its TTL in real time, and closing that needs a monotonic clock source, which would change the injected-clock contract shared with the middleware and the cache's own tests. This ticket must not silently take that on."
      }
    ],
    "notes": "EXCLUDED from the TKT-231/242 grouping deliberately (owner decision, 2026-08-24). Those two are Go/PostgreSQL smoke tests failing in one shared database at the same helper; this is a Vitest storefront test with no database. Different runtime, different mechanism — bundling would have put a false shared-cause claim in a closeout. What the three DO share is only the symptom class (load-sensitive), which is not a cause."
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Implementation readiness — CONCERNS (shaped 2026-08-08)\n\nFiled with a good description and never shaped, so Gate 1 has been hard-blocking it. Shaped now.\n\n| Criterion | Status | Note |\n|---|---|---|\n| Objective | OK | The assertion must fail closed, not open |\n| Scope | OK | The budget test and the fetch path behind it |\n| Uncertainties | **WARN** | Genuine second fetch vs stub artefact — unanswered, and it IS the ticket |\n| Dependencies | OK | None; the TKT-216 timeout bump already isolated the signal |\n| Approach | **WARN** | Follows from the uncertainty; cannot be settled in shaping |\n| AC verifiable | OK | Must demonstrate the test can still fail |\n| Context-mémo | OK | Re-resolved today |\n\n**Concern worth stating plainly:** two `open` items would normally block Gate 1, and they are left\nopen deliberately — this is an *investigation* ticket whose product is an answer, not a diff. If the\nowner prefers, it should be re-typed as a **Spike** with the fix as a follow-up; that is a cleaner\nfit for the pipeline than a Story carrying two open uncertainties.\n\n**The reason to prioritize it anyway:** this is the only known assertion in the repo that can pass\nwhile the property it proves is false. Per `AGENTS.md`, a fixture that cannot show the negative is a\nrecurring defect class here (TKT-61, and the \"fixture too small\" learning). This is that class,\ncaught in the act."
    },
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\n### Fresh reproduction of BOTH halves, from TKT-140's gate run (2026-08-09)\n\nNot a new ticket - adding evidence here rather than filing a duplicate, because this ticket already\nnames the exact shape and `web/storefront/test/page-call-budget.test.ts:129-134` already records that\nit is **not closed**.\n\nObserved while running `make check` for TKT-140 (a Go smoke-test-only change that cannot affect this\nsuite - `test-ts` is `pnpm -r test` and touches no Go). One gate run produced **both** documented\nfailure modes in the same file, in one invocation:\n\n```\nFAIL  test/page-call-budget.test.ts > ... > event list renders on exactly one upstream call\n  Error: Test timed out in 30000ms.\n\nFAIL  test/page-call-budget.test.ts > ... > event detail renders on exactly one upstream call\n  AssertionError: expected [ ...(2) ] to have a length of 1 but got 2\n    at test/page-call-budget.test.ts:122:37\n```\n\n**The second one is the half this ticket says is still open** - it did not time out, it counted TWO\nupstream calls where the budget allows one. The in-file comment predicted exactly this (\"That\nassertion can therefore still fail OPEN, and whether a second fetch is ever genuine - a retry path,\nor a cancelled request the stub counts twice - is an open question\").\n\n#### The condition that produced it, measured\n\nThe machine was at **load average ~30 on 16 cores** (~2x oversubscribed), caused by 24 orphaned\n`codex-companion` shells from an unrelated session burning ~64% CPU each for 13 hours. That is the\nstrongest hint yet about the open half's mechanism: **the double count appeared only under heavy\nCPU oversubscription**, never when unloaded.\n\nControlled runs on the same file, same commit, same machine:\n\n| Run | Tree | Load | Result |\n|---|---|---|---|\n| inside `make check` | TKT-140 change present | ~30 | 2 failed (1 timeout, 1 double-count) |\n| standalone | change **stashed**, tree == `origin/main` | idle | 6 passed |\n| standalone | change present | idle | 6 passed |\n| inside `make check`, after killing the orphans | change present | ~5 | **179/179 passed** |\n\nSo: reproducible under load, not reproducible idle, and independent of the tree under test.\n\n#### What this suggests for whoever picks this up\n\nThe timeout half is understood (Astro's vite transform is slow on a busy machine; TKT-218's 30s\nalready mitigates it). For the **open** half, the load-correlation points at the stub's counting\nrather than at a genuine second fetch: under CPU starvation a request that is cancelled or retried\nat the transport layer could plausibly be observed twice by `stubFetch` while the page still renders\none logical read. Worth testing that hypothesis directly - assert on distinct URLs plus a\nrender-once marker, rather than on raw call count - before concluding the storefront has a real\ndouble-read.\n\nRecording the load figure specifically because \"flaky under load\" was previously qualitative; there\nis now a number and a reproduction recipe."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n**Re-shaped 2026-08-24. Verdict: READY — 8/8 `met`.** The two items that were `open` are both closed, one by execution and one by an owner decision.\n\n### `uncertainties` — answered by running it, not by reasoning\n\nThe ticket's central question was whether the 2-call observation is a genuine second fetch or an artefact of the stub counting a cancelled request. **It is genuine.**\n\n`PageDataCache` measures freshness against a **wall clock**. `ageOf()` is `upstreamAgeSeconds + elapsed`, the stub serves `max-age=300` with no `Age` header, so an entry stays fresh for 300 seconds of *wall* time. A starved SSR process that loses more than `max-age` between two reads of one URL misses and re-fetches.\n\nReproduced directly, with the cache's own injected-clock seam:\n\n```\ncalls for two gets, normal clock: 1\ncalls after a 301s stall:         2\n```\n\nSo the stub counts correctly and the page really does fetch twice. The harmless reading is ruled out — which matters, because it was the reading that would have justified closing this with a widened assertion.\n\n### `approach` — Gate 1 decision, now sharpened by the diagnosis\n\nThe direction you chose was *\"make the assertion fail closed either way\"*. With the mechanism known it becomes concrete: give the cache a **deterministic clock** for this test — the constructor already takes `now`, so the seam exists — and let the fixture decide elapsed time instead of machine load. Same treatment TKT-233 and TKT-232 applied: assert the dependency edge, not a wall clock.\n\nTwo fixes explicitly rejected, both of which look right:\n\n- **Raise the timeout again.** The test's own comment at `:124-133` warns against exactly this. It addresses the three *timeout* failures and is silent on the fail-open one.\n- **Widen the assertion to `<= 2`.** That *is* the fail-open defect.\n\n### One trap carried into the mémo\n\n`cache.ts` documents a gap it deliberately did not close: after a *backward* wall-clock step, an entry can outlive its TTL in real time, and fixing it needs a monotonic clock source — which would change the injected-clock contract the cache shares with the middleware and its own tests. This ticket must not quietly take that on. It is a different ticket.\n\n### Why this is not grouped with TKT-231/242\n\nThose two are Go smoke tests failing at one helper in one shared PostgreSQL database. This is a Vitest test with no database. They share a *symptom class* — load-sensitive — which is not a cause. Grouping them would have put a shared-mechanism claim in a closeout that the code contradicts.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n**Claimed.** Running gateless per `config.gates: autonomous`; local tracker backend. Gate 1 taken by the owner (2026-08-24), including the decision to run this **separately** from TKT-231/242 rather than bundled — it is a Vitest test with no database and cannot share their PostgreSQL batch-limit cause.\n\n**Selection reason:** second and last of the two approved tickets. TKT-242 closed first because its shared-cause merge closed two board items; this one was left until its mechanism could be attacked with a clean context.\n\n**Blockers:** none. `links` relate to TKT-242 (explicitly *not* a shared cause).\n\n**Pre-claim check:** `git log origin/main..main` empty — nothing stranded on the local default branch. (This check is the retro item from TKT-242, where an unpushed TKT-249 commit surfaced only at merge time. Applied here rather than filed.)\n\n### Risk verdict: `risk:low` NOT claimed\n\nBlast radius is small — the expected diff is `web/storefront/test/page-call-budget.test.ts` plus possibly a test-only seam. But the plan gate is kept for the same reason as TKT-242, and more sharply:\n\nThe ticket's central question **is** *\"how do I construct a test that can actually fail?\"* — the shape the invariant names explicitly. This test's whole defect is that it **fails open**: it counted two upstream calls where the budget allows one, and could not distinguish \"the page made a second call\" from \"the machine was busy\". A fix that makes it stop reporting the failure without making it able to *detect* the real one is the obvious wrong answer, and the file already carries a comment warning the next person against exactly that.\n\nThere is a second reason specific to this ticket. `cache.ts` documents a gap it deliberately left open — after a **backward** wall-clock step an entry can outlive its TTL, and closing it needs a monotonic clock source that would change the injected-clock contract shared with the middleware and the cache's own tests. A plan is where that boundary gets stated and held, not discovered mid-implementation.\n\nSo: plan gate kept, cross-model critique runs, `agent:planning` set.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n**Plan draft — by `gpt-5.6-luna` via Codex** (`config.models.plan`), read-only, session `01a0373a-5aac-76c3-becb-c95f28b4cc97`.\n\n### Recommendation\n**Test-only fix.** Freeze `Date.now()` before the real `.astro` module is imported. No production setter, no change to `PageDataCache`.\n\nThe seam already exists: `cache.ts:96` accepts an injected clock, `api.ts:25` constructs the singleton with the default `Date.now`, and the page test dynamically imports the page module *after* its `beforeEach` — so a spy installed first reaches the singleton's constructor.\n\n### Planned change\n`page-call-budget.test.ts` only. Per test: reset modules → spy `Date.now` returning one fixed timestamp → only then render. After: restore mocks, unstub `fetch`.\n\nA date-only spy rather than fake timers, deliberately: real timers must keep governing Astro transforms, fetch promises and the 30s timeout.\n\nExplicitly rejected — adding a mutable test hook to `api.ts`: it would expose singleton reconfiguration and risk cross-request contamination for a test defect.\n\n### Assertions — unchanged\n```ts\nexpect(res.status).toBe(200);\nexpect(html.includes(child)).toBe(true);\nexpect(calls.map((c) => c.url)).toHaveLength(1);\nexpect(calls[0].url.startsWith(`${GATEWAY}/api/catalog/`)).toBe(true);\n```\nA real second request adds an entry, so `toHaveLength(1)` fails. A starved process cannot cause a miss because every read sees the same frozen timestamp.\n\n**Scope kept honest:** it remains an *upstream-call* budget — not a count of `PageDataCache.get()` calls, not browser-island requests. A duplicate read of the same URL inside its window is still absorbed by the cache; that is a different invariant.\n\n### Mutations proposed\n1. Add a direct `fetch()` to the real event-list page or a child `.astro` → 200 and children still render, but the budget sees 2 and fails. Proves the test still measures the **page boundary**, not an `api.ts` wrapper.\n2. Make `PageDataCache` bypass fresh entries → the repeat-render test sees >1 call. Proves the frozen clock has not made the assertion inert.\n3. Make the page return 503 after its read → the status assertion fails. Preserves \"one call plus no rendered page is not enough\".\n\nThe mutation that counts must fail **in `page-call-budget.test.ts` itself**, not only in the lower-level cache tests.\n\n### Risks named\nSpy must be installed before import or `new PageDataCache()` captures the real clock; spy must be restored after each test; do not replace `max-age=300` with a huge TTL (hides expiry behaviour); do not switch to a monotonic clock here (separate change, affects the middleware contract); and the test will deliberately stop treating a 301-second stall as a page-call violation, because that is cache-clock behaviour, not evidence the page issued an extra request.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n**Plan critique + final — critique by `main-agent`** (`config.models.planReview`; ≠ `plan`).\n\n**The `kind=plan` draft as amended below is the plan.**\n\nCritiqued by **executing** the draft's central claim rather than reading it.\n\n### ACCEPTED — the seam works, and it was verified, not assumed\n\nThe draft asserts a `Date.now` spy installed before the dynamic import reaches the singleton's constructor. Ran it:\n\n| recipe | result |\n|---|---|\n| frozen `Date.now` spy, then `new PageDataCache()`, two reads of one URL | **1 call** |\n| same, but a genuine second URL | **2 calls** |\n\nSo the fix absorbs the stall **and** still counts a real second fetch. That second row is the one that matters — freezing a clock is exactly the kind of change that can turn an assertion inert, and it does not here.\n\n### AMENDED — the mechanism is import-order-sensitive in a way worth writing down\n\nEstablishing the above cost one wrong attempt, and the reason belongs in the code. `cache.ts:96` captures the clock as a **default parameter value**:\n\n```ts\nconstructor(fetchImpl = fetch, now: () => number = Date.now) { this.#now = now; }\n```\n\n`this.#now` holds the function reference resolved **at construction**. So `vi.spyOn(Date, 'now')` installed *after* `api.ts` is first imported does nothing — the singleton already bound the original. My first reproduction attempt failed exactly this way and reported a false negative (`first=1 second=1`, looking like no defect).\n\n**The implementation must carry a comment saying the spy must precede the import**, because the failure mode is a *silently passing* test, not an error. The draft names this as a risk; the plan promotes it to a required comment.\n\n### AMENDED — mutation 2 as written cannot run, replace it\n\nThe draft's mutation 2 is \"make `PageDataCache` bypass fresh entries\". That mutates **production** code to prove a **test** is live, and its expected observation (\"the repeat-render test sees >1 call\") is about the *cache*, not the page budget. Replacing it with a mutation that is both runnable and about the thing under test:\n\n> **2′. Remove the frozen-clock setup from the test and stall the clock past `max-age` between the two renders of the repeat-render case.** The test must go red. This proves the fix is what makes the case immune, rather than the case having been immune anyway.\n\nMutations 1 and 3 are accepted as drafted.\n\n### REJECTED — the draft's last line\n\nIt recommends preserving `web/storefront/test/zz-repro-failopen.test.ts`. That was my throwaway reproduction scratch file, visible to the drafter mid-run; it is deleted and must not reach the diff.\n\n### CONFIRMED — the boundary holds\n\nThe draft correctly refuses the monotonic-clock change and says so. `cache.ts` documents that a backward wall-clock step can let an entry outlive its TTL, and that closing it changes the injected-clock contract shared with the middleware. **This ticket does not touch that**, and the closeout must not imply it did.\n\n### Introduced at plan-review, reviewed by no second model\n\n1. The **required comment** about spy-before-import (above).\n2. **Mutation 2′** replacing the draft's unrunnable mutation 2.\n3. The judgement that freezing does not make the assertion inert — supported by an executed both-directions check, but the *conclusion* is mine.\n\nAll three go into the ai-review focus so the adversarial pass attacks them directly.\n\n### Definition of done\n\n- The three page cases render real `.astro` modules through the container, return 200, and show every repeated child — unchanged.\n- Each spends exactly one upstream call, and that is immune to process starvation.\n- A genuine second fetch still fails the budget (mutation 1).\n- Mutation 2′ reddens the test without the fix.\n- The TKT-216 comment at `:124-133` is **updated, not deleted** — it currently says the fail-open question is open.\n- `cache.test.ts` and `middleware-page-tier.test.ts` untouched and green.\n- `make check` green.\n\n### Files\n`web/storefront/test/page-call-budget.test.ts` — expected to be the only file. No production code.\n\n### Self-made decisions (autonomous, no human at Gate 2)\n- **Test-only, no production seam** — the draft's reasoning is sound and the existing constructor seam suffices. Most reversible option.\n- **Frozen clock over fake timers** — accepted from the draft; real timers must keep governing Astro's transform and the 30s timeout.\n- **Keep `max-age=300`** — raising it would hide expiry behaviour rather than fix the measurement.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n**Implemented by `main-agent`** (`config.models.implement`). PR [#318](https://github.com/mathd/ticketing_system/pull/318), commit `f2b49963`. `make check` green (sentinel `EXIT=0`, log body grepped separately).\n\n### What shipped\n`Date.now` frozen for every case in `page-call-budget.test.ts`, plus the TKT-216 comment updated. **No production code.** +41/−7, one file.\n\n### The false negative worth recording\n\nThe first reproduction attempt used `vi.spyOn(Date, 'now')` installed *after* the module graph was imported. It reported `first=1 second=1` — i.e. **no defect**. That was wrong: `cache.ts` takes the clock as a default parameter and stores the resolved reference, so the singleton had already bound the real `Date.now`.\n\nThe failure mode is a test that **passes while proving nothing**, which is the exact shape this repo's rules warn about. It is now a comment in `beforeEach`, because the next person to touch this will hit it.\n\n### Mutations — all three observed\n\n| mutation | expected | observed |\n|---|---|---|\n| remove the freeze, advance the clock past `max-age` between the repeat-render's two renders | test reddens | ✅ **3 calls** where 1 allowed — the fail-open itself |\n| per-widget `fetch` in `PerformanceCard` (repeated child) | budget reddens | ✅ **4 calls**, failing both pages that render it |\n| festival page 503s after its read | status assertion reddens | ✅ `expected 503 to be 200` |\n\nMutation 2 (plan-review's replacement for the draft's unrunnable one) is the load-bearing evidence: **freezing a clock is exactly the change that can turn an assertion inert**, and mutation 1 proves it did not — a genuine second fetch still fails the budget, at the real page boundary.\n\n### Neighbours\n`cache.test.ts` and `middleware-page-tier.test.ts` untouched; 31 tests green across the three files. The frozen instant (2026-05-28) precedes the fixture's performance date (2026-09-01), so nothing date-dependent in rendering shifts.\n\n### Boundary held\nThe monotonic-clock gap `cache.ts` documents is untouched, as the plan required.\n"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "The call-budget assertion cannot report a violation that machine load caused, and cannot pass while a genuine second fetch happens. It must fail CLOSED."
    },
    "scope": {
      "state": "met",
      "note": "web/storefront/test/page-call-budget.test.ts and whatever in the storefront fetch path explains a second call. Not a re-litigation of TKT-208's budget itself."
    },
    "uncertainties": {
      "state": "met",
      "note": "ANSWERED at shaping, by execution rather than inference. The 2-call observation is a GENUINE second fetch, not a stub artefact. Mechanism: PageDataCache measures freshness against a WALL clock (Date.now, cache.ts). ageOf() = upstreamAgeSeconds + elapsed; the stub serves max-age=300 with no Age header, so an entry is fresh for 300s of WALL time. A starved SSR process that loses more than max-age of wall time between two reads of the same URL misses and re-fetches. Reproduced directly: constructing PageDataCache with an injected clock and stepping it 301s between two get() calls on one URL yields calls=2 (fresh clock yields 1). So the stub counts correctly and the page really does fetch twice."
    },
    "dependencies": {
      "state": "met",
      "note": "None. The 30s timeout raised during TKT-216's gate already removed the load-induced noise, so the remaining signal can be studied in isolation."
    },
    "approach": {
      "state": "met",
      "note": "Owner decision at Gate 1 (2026-08-24): make the assertion fail CLOSED regardless of which diagnosis won — and the diagnosis is now known, which sharpens it. The budget assertion must not be able to read wall-clock starvation as a budget violation, while still failing if the page genuinely adds a second read. Direction: give the cache a deterministic clock for this test (the constructor already accepts `now`), so elapsed time is decided by the fixture rather than by machine load. That is the same treatment TKT-233/TKT-232 applied — assert the dependency edge, not a wall clock. Explicitly REJECTED: raising the timeout again (the test's own comment at :124-133 warns against exactly that, and it addresses the timeout failures, not the fail-open one); and widening the assertion to `<=2` (that is the fail-open defect being fixed). NOTE the injected clock must NOT be threaded so far that the test stops rendering real .astro modules through the container — the file's own comment at :76-84 records that calling the api.ts wrappers directly was a defect found in ai-review."
    },
    "first_slice": {
      "state": "met",
      "note": "Reproduce the 2-call observation deliberately (load the machine, or drive the stub directly) so the question can be answered from evidence rather than inference."
    },
    "success": {
      "state": "met",
      "note": "A test that fails when a second upstream call genuinely happens, and passes under load. Prove the failing direction works - a budget proof that cannot fail is the defect being fixed."
    },
    "context_memo": {
      "state": "met",
      "note": "Re-resolved 2026-08-24. governingAdrs and governingTests below; ADR files verified to resolve at HEAD."
    }
  }
}
