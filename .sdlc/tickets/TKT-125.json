{
  "key": "TKT-125",
  "type": "Task",
  "status": "PO Review",
  "labels": [
    "needs:human"
  ],
  "assignee": "agent",
  "pr": {
    "number": 115,
    "url": "https://github.com/mathd/ticketing_system/pull/115",
    "state": "open"
  },
  "classification": "complex",
  "summary": "OpenAPI response validation runs unconditionally on every response, hot path included — gate it and amend ADR-028",
  "description": "### Finding (architecture review R1, 2026-07-25)\n\n`shared/go/contract.responseValidated` wraps **every** handler in all five services. For each request it:\n\n1. buffers the whole response into an `httptest.ResponseRecorder`,\n2. re-routes the request against the OpenAPI document (`router.FindRoute`),\n3. re-parses the recorded body and walks it against the response schema (`openapi3filter.ValidateResponse`),\n4. copies headers and body out to the real `ResponseWriter`.\n\nThere is no toggle. This runs on `POST /holds` and the three transitions under on-sale load, i.e. exactly the path `smoke/onsale_load_test.go` holds to a p99 ≤ 1s SLO. It is the largest self-inflicted cost standing against the project's stated scalability assumption, and it is one config knob away from being paid only where it earns its keep.\n\nTwo smaller defects in the same function:\n\n- it calls `context.Background()` for validation, so a drift error is logged **outside** the request trace — the one place you would want `trace_id`;\n- the recorder discards `http.Flusher`/`http.Hijacker`, so no handler behind it can ever stream.\n\n### Not in scope\n\n**Request** validation stays unconditional. It is a trust boundary and its cost is bounded by the request body, which is small on these routes. Only the response side moves.\n\n### Acceptance Criteria\n\n- [ ] Response validation is switchable via `runtimecfg` (with the rest of the bounded-resource policy), defaulted **on** for dev/CI/smoke.\n- [ ] ADR-028 is amended to state where fail-closed applies and why the placement changed. Semantics where enabled are untouched.\n- [ ] `smoke/onsale_load_test.go` is run with validation on and off; the p99 delta for hold/finalize/confirm is **recorded as a number** in `docs/evidence/`. If the delta is negligible, say so and keep validation everywhere — that outcome closes the ticket honestly.\n- [ ] With validation enabled, TKT-108's mask-body detection still passes.\n- [ ] The validator runs on the request context, so a drift log carries `trace_id`.\n- [ ] `make check` green.",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "OpenAPI response validation currently runs unconditionally on every response in every service, including inventory's claim path under on-sale load. Make it configurable so the contract guarantee is paid for where it has value (dev/CI/smoke) and not on the production hot path."
    },
    "scope": {
      "state": "met",
      "note": "IN: an explicit enable/sample switch for response validation in shared/go/contract, defaulted on for dev/CI/smoke; the ADR-028 amendment saying where fail-closed applies; passing the request context into the validator instead of context.Background(); a measured before/after on the on-sale load test. OUT: request validation (stays unconditional — it is a trust boundary); removing the guarantee anywhere it is currently proven; any change to the drift semantics where validation IS enabled."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Whether production runs it fully off or sampled at a low rate. Sampling keeps a live drift signal at a fraction of the cost but complicates the fail-closed contract (a sampled request fails closed, an unsampled one does not — two behaviours for one endpoint). The plan decides against the measured delta; both satisfy the COS."
    },
    "dependencies": {
      "state": "met",
      "note": "None. shared/go/contract is consumed by all five services but the change is additive (a config knob with the current behaviour as the dev/CI default)."
    },
    "approach": {
      "state": "met",
      "note": "Config through runtimecfg, alongside the other bounded-resource policy, not a bespoke env read per service. Keep ADR-028's fail-closed semantics EXACTLY as they are wherever validation is enabled — this ticket changes where it runs, not what it does. The evidence for the change is the load test, not reasoning: measure with it on and off."
    },
    "first_slice": {
      "state": "met",
      "note": "The knob + the ADR-028 amendment + the measurement. If the measured delta turns out to be negligible, that is a valid outcome — record it in the ADR and keep validation on everywhere; the ticket is then a documentation and context-propagation fix. Do not force the change against the measurement."
    },
    "success": {
      "state": "met",
      "note": "COS: (1) response validation is switchable, defaulted on in dev/CI/smoke; (2) ADR-028 states where fail-closed applies and why the placement changed; (3) smoke/onsale_load_test.go is run in both configurations and the p99 delta is recorded in the ticket and in docs/evidence — a number, not an estimate; (4) with validation enabled, TKT-108's mask-body detection still passes (the laundered-status learning still holds); (5) the validator runs on the request context so drift errors carry trace_id; (6) make check green."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked: ADR-028 (the decision being amended — placement, not semantics), ADR-009 (contract-first is the reason the guarantee exists at all), ADR-004 (the read path this sits in front of). Learning: docs/learnings/2026-07-21-test-validator-downstream-of-production-wrap-sees-laundered-statuses.md — the mask body is the detectable artifact, not the status; and docs/learnings/2026-07-25-coarse-observables-pass-the-broken-build.md — a coarse observable will not show this delta, the load test's p99 will."
    }
  },
  "context": {
    "binding": [
      {
        "id": "ADR-028",
        "date": "2026-07-12",
        "rule": "A response that drifts from the served OpenAPI contract fails closed: the drifted payload never reaches the client, and the violation is logged with the operation coordinates."
      },
      {
        "id": "ADR-009",
        "date": "2026-07-12",
        "rule": "APIs are contract-first: the OpenAPI spec is the source of truth, served by the service at /openapi.yaml and checked for drift in the gate; domain events use a versioned platform envelope (id, type==subject, occurred_at, schema, data) published to JetStream with acks (never fire-and-forget), with a deterministic envelope id so retried/raced emissions de-duplicate."
      },
      {
        "id": "ADR-004",
        "date": "2026-07-12",
        "rule": "Public reads declare volatility-tiered TTLs; the read path is cache-first, and each tier has exactly one cache owner so staleness never stacks."
      },
      {
        "id": "ADR-010",
        "date": "2026-07-12",
        "rule": "Reservation claims are decided inside a single Postgres transaction under a row lock; no external lock manager. Oversell is bounded by the database, not by application coordination."
      }
    ],
    "reference": [
      {
        "summary": "Architecture review R1 — the finding and the call sites",
        "link": "docs/reviews/2026-07-25-architecture.md"
      },
      {
        "summary": "The function under change",
        "link": "shared/go/contract/http.go"
      },
      {
        "summary": "The measurement harness and the p99 SLOs this must not regress",
        "link": "smoke/onsale_load_test.go"
      },
      {
        "summary": "A test validator downstream of the production wrap sees laundered statuses (TKT-108)",
        "link": "docs/learnings/2026-07-21-test-validator-downstream-of-production-wrap-sees-laundered-statuses.md"
      }
    ],
    "capturedAt": "2026-07-25"
  },
  "links": [],
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\nBorn from the 2026-07-25 architecture review, recommendation **R1** ([docs/reviews/2026-07-25-architecture.md](docs/reviews/2026-07-25-architecture.md)). The review's largest performance finding. Note the honest-outcome clause in the COS: the load test decides, and \"measured, negligible, kept on\" is a passing result — this ticket must not be implemented as a foregone conclusion. Code pointers: shared/go/contract/http.go (responseValidated, requestValidator), the five services/*/internal/api/server.go call sites, docs/adr/ADR-028-response-drift-fail-closed.md."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ DoR shaped — TKT-125 — all 8 items met/deferred (Gate 1 unblocked)\n\nShaped from the architecture review's code-seam survey; `shared/go/contract/http.go` was read in full. **No `open owner:human` items remain.**\n\n| DoR item | State |\n|---|---|\n| `objective` | met |\n| `scope` | met |\n| `uncertainties` | deferred |\n| `dependencies` | met |\n| `approach` | met |\n| `first_slice` | met |\n| `success` | met |\n| `context_memo` | met |\n\n**Deferred (safely resolved in Planning, no major redesign):**\n- `uncertainties` — Whether production runs it fully off or sampled at a low rate. Sampling keeps a live drift signal at a fraction of the cost but complicates the fail-closed contract (a sampled request fails closed, an unsampled one does not — two behaviours for one endpoint). The plan decides against the measured delta; both satisfy the COS.\n\n**Gate 1 granted by the owner on 2026-07-25** — the architecture review ([docs/reviews/2026-07-25-architecture.md](docs/reviews/2026-07-25-architecture.md)) was the prioritization pass; owner instruction was \"create new tickets and move them directly to Ready\".\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-125 — branch `TKT-125-gate-response-validation`\n\n**Selection reason:** highest-impact item in the Ready column and the architecture review's largest\nperformance finding (R1). No blockers (`links: []`), all 8 DoR items met/deferred, Gate 1 granted by\nthe owner on 2026-07-25. The single deferred item (`uncertainties` — full-off vs sampled) is a\nPlanning decision, not a redesign.\n\n### ⚠️ Process experiment — this ticket runs GATELESS\n\nThe repo owner has **deliberately waived all four human gates** for this run as a process\nexperiment, and asked that the waiver be recorded here so the board history shows it. The\nreplacements, in the owner's words:\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human moves Backlog → Ready | already granted (ticket was in Ready) |\n| 2 — plan approval | human moves Planning → Building | agent resolves open decisions itself after the cross-model critique: **prefer the option the critique recommends; absent a recommendation, pick the most reversible option.** Every self-made decision + rationale recorded in `kind=plan-final`. |\n| 3 — review + merge | human reviews the PR and squash-merges | agent squash-merges and deletes the branch **after** the adversarial ai-review converges (same triage, second-pass and churn-cap rules). The review-guide is still posted on the PR first — it becomes the audit record. |\n| 4 — PO acceptance | PO validates COS, moves to Done | agent self-validates each COS against test evidence on the merged code, posts the validation note, closes with the full metrics comment. |\n\n**Everything else in the skill applies unchanged**, in particular: `config.models` routing with no\nsubstitutions, cross-model review for both plan and code, TDD with tests observed red first,\n`make check` green before every push and after the merge, all marker comments at each stage, and no\nAI attribution in commits or PRs.\n\n**Stop conditions the owner set** (report instead of proceeding): gate/CI failing twice on the same\ncause with no clear fix; work touching auth, money paths, data migrations or CI/deploy config in a\nway the ticket and plan did not anticipate; a blocking review finding surviving the churn cap; a\nconfigured model unreachable after the documented escalation ladder; a destructive or ambiguous\nirreversible action.\n\n### Routing for this run (from `.claude/sdlc.config.json`)\n\n| Stage | Model |\n|---|---|\n| `plan` | `gpt-5.6-sol` (codex) |\n| `planReview` | `main-agent` |\n| `implement` | `main-agent` |\n| `aiReview` | `gpt-5.6-sol` (codex) |\n\nCross-model invariant checked: `plan` ≠ `planReview` ✅, `implement` ≠ `aiReview` ✅.\n\n**`risk:low` is not set and must not be** — this changes a middleware wrapping all five services'\nhandlers, on the path a p99 SLO is held against.\n\nBranch cut from `origin/main` @ `56b8984`.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — TKT-125 — drafted by **gpt-5.6-sol** (codex `task`, effort high, read-only)\n\nRouting: `config.models.plan = gpt-5.6-sol`. Run `task-ms114l5p-7o5rdy`, codex thread\n`019f9bb6-d38e-7640-be95-7d0e775a8ce4`. The drafter read the real code (contract, runtimecfg, all\nfive call sites, the catalog coverage gate, the load harness, compose/Makefile/smoke.sh) before\nwriting. Verbatim below; the critique follows in `kind=plan-final`.\n\n---\n\n# TKT-125 execution plan\n\n## 1. Configuration and middleware design\n\nUse one boolean switch; do not introduce sampling machinery.\n\n- Environment variable: `OPENAPI_RESPONSE_VALIDATION_ENABLED`\n- Type: boolean parsed with `strconv.ParseBool`\n- Default: `true`\n- Invalid values: fail service startup with an error naming the variable but not its value\n- Development, CI, and smoke: remain enabled through the default\n- Production recommendation: disable completely if the measurement shows a material cost; if the delta is negligible, leave it enabled everywhere as required by the ticket’s decision rule\n\nAdd an unexported `boolean(name string, fallback bool) (bool, error)` helper in `shared/go/runtimecfg/runtimecfg.go`, following the existing `duration` and `integer` helpers.\n\nExtend the existing HTTP policy rather than adding a separate configuration read:\n\n```go\ntype HTTP struct {\n    ReadHeaderTimeout       time.Duration\n    ReadTimeout             time.Duration\n    WriteTimeout            time.Duration\n    IdleTimeout             time.Duration\n    ValidateOpenAPIResponses bool\n}\n```\n\n`HTTPFromEnv()` reads `OPENAPI_RESPONSE_VALIDATION_ENABLED`, defaulting it to `true`. `HTTP.Apply` continues applying only `http.Server` fields; each service consumes `ValidateOpenAPIResponses` while constructing its API router.\n\nThread the value explicitly through these signatures:\n\n```go\n// shared/go/contract/http.go\nfunc RequestValidator(\n    spec []byte,\n    next http.Handler,\n    log *slog.Logger,\n    validateResponses bool,\n) (http.Handler, error)\n\nfunc RequestValidatorWithErrorHandler(\n    spec []byte,\n    next http.Handler,\n    log *slog.Logger,\n    validateResponses bool,\n    errorHandler func(http.ResponseWriter, string, int),\n) (http.Handler, error)\n\nfunc ResponseValidator(\n    spec []byte,\n    next http.Handler,\n    log *slog.Logger,\n    validateResponses bool,\n) (http.Handler, error)\n\nfunc requestValidator(\n    spec []byte,\n    next http.Handler,\n    log *slog.Logger,\n    validateResponses bool,\n    errorHandler func(http.ResponseWriter, string, int),\n) (http.Handler, error)\n```\n\nChange the service router seams to:\n\n```go\n// inventory, commerce, payments, access\nfunc (s *Server) Router(log *slog.Logger, validateResponses bool) http.Handler\n\n// catalog\nfunc NewRouter(s *Server, validateResponses bool) (http.Handler, error)\n```\n\nUpdate the five production call sites:\n\n- `services/inventory/cmd/inventory/main.go`: `Router(log, httpConfig.ValidateOpenAPIResponses)`\n- `services/commerce/cmd/commerce/main.go`: same\n- `services/payments/cmd/payments/main.go`: same\n- `services/access/cmd/access/main.go`: same\n- `services/catalog/cmd/catalog/main.go`: `NewRouter(..., httpConfig.ValidateOpenAPIResponses)`\n\nAll unit and smoke-test router construction passes `true` explicitly. This is intentional: it keeps ADR-030’s catalog unit coverage gate behind the real response validator and makes disabling it in a test a visible choice.\n\n`load(spec)` should still run at router construction when response validation is disabled. This preserves startup-time OpenAPI loading and validation; only the per-response buffering, route lookup, and schema walk are removed.\n\nWithin `requestValidator`:\n\n1. Always construct and install the request validator.\n2. If response validation is enabled, wrap `next` with `responseValidated`.\n3. If disabled, give `next` directly to the request validator.\n\nWithin `ResponseValidator`, choose between `responseValidated` and direct `next` after loading the spec.\n\nWhen the switch is off:\n\n- Request validation remains unconditional.\n- Responses, including drifting bodies and undocumented statuses, pass through unchanged.\n- No generic contract 500 is substituted.\n- No response-drift log is emitted.\n- There is no runtime response-conformance or fail-closed guarantee for that request.\n\nWhen enabled, status, headers, body, structured log fields, undocumented-status handling, and the generic 500 remain byte-for-byte equivalent to HEAD.\n\n## 2. Sampling decision\n\nRecommend fully off in production rather than sampled validation, provided the measured delta is material.\n\nFully off has the simplest contract: production responses are never buffered or runtime-validated, while dev/CI/smoke retain deterministic fail-closed validation. Its cost is the loss of live drift detection; a regression missed by the exercised gate coverage can reach clients.\n\nSampling retains a partial live signal and pays roughly the sampling fraction of the validation cost, but introduces two observable behaviours for the same endpoint:\n\n- Sampled requests fail closed.\n- Unsampled requests return the same drifting payload.\n\nIt also adds rate configuration, selection logic, deterministic-testing concerns, and incomplete diagnostics. A rare production drift could alternate between 500 and a nominal success, complicating client behaviour and incident analysis. That complexity is not justified for this testbed.\n\nDo not add a rate, RNG, hash sampler, or enum in this ticket. The knob remains boolean.\n\nIf the measured delta is negligible, follow the ticket’s explicit branch: keep validation fully on in production as well. Record that result rather than forcing either full-off or sampling.\n\n## 3. Strict TDD sequence\n\n### Slice A: runtime configuration\n\nAdd tests first in `shared/go/runtimecfg/runtimecfg_test.go`:\n\n- `TestHTTPDefaultsResponseValidationOn`\n  - Removes the variable from the test environment.\n  - Calls `HTTPFromEnv`.\n  - Asserts `ValidateOpenAPIResponses == true`.\n\n- `TestHTTPCanDisableResponseValidation`\n  - Sets `OPENAPI_RESPONSE_VALIDATION_ENABLED=false`.\n  - Asserts the returned field is false.\n\n- Extend `TestInvalidConfiguration` with a boolean case.\n  - Sets a non-boolean value.\n  - Asserts `HTTPFromEnv` fails and identifies `OPENAPI_RESPONSE_VALIDATION_ENABLED`.\n\nRun and observe red:\n\n```sh\n(cd shared/go && go test ./runtimecfg)\n```\n\nThe initial red should be the missing field/helper or the unimplemented environment parsing. Implement only this slice, then rerun green.\n\n### Slice B: shared contract behaviour\n\nAdd tests first in `shared/go/contract/http_test.go` against the new explicit boolean parameter:\n\n- `TestResponseValidationDisabledPassesDriftUnmodified`\n  - Builds a documented route whose handler returns a drifting body.\n  - Disables response validation.\n  - Asserts the handler’s original status, headers, and exact body reach the client.\n  - Asserts no drift log is emitted.\n\n- `TestRequestValidationRemainsEnabledWhenResponseValidationIsDisabled`\n  - Disables response validation but sends an invalid request body.\n  - Asserts the request is rejected and the handler is never called.\n\n- `TestResponseValidationEnabledPreservesFailClosedResponse`\n  - Enables validation and emits the same drift.\n  - Asserts exactly:\n    - status `500`;\n    - `Content-Type: application/json`;\n    - `Cache-Control: no-store`;\n    - body `{\"error\":\"response violates OpenAPI contract\"}\\n`;\n    - the existing structured `method`, `path`, original `status`, and `error` fields.\n  - This pins ADR-028 semantics against accidental changes while moving the placement.\n\n- Retain and adapt `TestUndocumentedResponseStatusIsRejected`.\n  - Pass `true`.\n  - Continue proving `IncludeResponseStatus` behaviour.\n\n- Retain and adapt both `ResponseValidator` tests.\n  - Test enabled drift rejection and enabled valid/undocumented-route passthrough.\n  - Add the disabled drifting-response case to the response-only catalog seam.\n\n- `TestResponseValidationUsesRequestContext`\n  - Add an unexported function type accepted by `responseValidated`, equivalent to:\n\n    ```go\n    type validateResponseFunc func(context.Context, *openapi3filter.ResponseValidationInput) error\n    ```\n\n  - Production callers pass `openapi3filter.ValidateResponse`.\n  - The test supplies a fake validator, puts a sentinel and an active OpenTelemetry span in the request context, returns a validation error, and asserts:\n    - the fake received the request context rather than `context.Background()`;\n    - the emitted log contains the span’s `trace_id`.\n\nThis injection is preferable to a mutable package-level test hook and remains concurrency-safe.\n\nHEAD already calls `logger.ErrorContext(r.Context(), ...)`, so a trace-only test would pass without detecting the `ValidateResponse(context.Background(), ...)` defect. The fake validator assertion is what makes the context test meaningful and red before the implementation change.\n\nRun and observe red:\n\n```sh\n(cd shared/go && go test ./contract)\n```\n\nImplement the boolean branch and change:\n\n```go\nopenapi3filter.ValidateResponse(context.Background(), input)\n```\n\nto validation with the request context. Rerun the package green.\n\n### Slice C: service wiring and ADR-030 protection\n\nChange the router signatures and first update all tests to pass `true`. Compilation failures identify any missed call site.\n\nFor catalog, add a focused wiring test beside `TestRuntimeResponseDriftFailsClosed` in `services/catalog/internal/api/coverage_test.go`:\n\n- Construct the same deliberately invalid negative-price fixture through `NewRouter(..., false)`.\n- Assert the original response passes through rather than becoming the ADR-028 500.\n- Keep `TestRuntimeResponseDriftFailsClosed` using `true`.\n\nRefactor `newEnv` in `services/catalog/internal/api/server_test.go` only as far as necessary to provide an explicit enabled/disabled constructor; its normal path must remain enabled.\n\nRun:\n\n```sh\n(cd services/catalog && go test ./internal/api)\n(cd services/inventory && go test ./internal/api)\n(cd services/commerce && go test ./internal/api)\n(cd services/payments && go test ./internal/api)\n(cd services/access && go test ./internal/api)\n```\n\nThe unfiltered catalog run is required because its `TestMain` coverage gate is intentionally skipped under `-run`.\n\nThe catalog run also exercises TKT-108’s mask-body detection in `env.validateResponse`: any normal enabled test that receives `response violates OpenAPI contract` must still fail hard.\n\n## 4. Context and streaming defects\n\nReplace the validation call’s `context.Background()` with the request context through the injected validation function. Keep `logger.ErrorContext(r.Context(), ...)`.\n\nThe streaming/Hijacker defect should be split into a separate ticket.\n\nReason: fail-closed response validation requires retaining the entire response until its status, headers, and body have been accepted. A real flush sends bytes before validation can decide to replace the response with a 500, and a hijacked connection is no longer an HTTP response the middleware can validate. Preserving true streaming semantics while retaining ADR-028 fail-closed behaviour is therefore a policy decision, not an adapter fix.\n\nFor TKT-125:\n\n- Enabled mode remains buffered and does not promise streaming or hijacking.\n- Disabled mode passes the original `ResponseWriter` directly, naturally restoring its optional interfaces.\n- Amend ADR-028’s buffering consequence to state this explicitly.\n- Open a follow-up only when a streaming/hijacking route exists, with a route-level exclusion or a different validation contract decided then.\n\n## 5. Measurement\n\nUse the existing full NFR profile, not the advisory gate profile. The relevant values are the `nfr-3000pm` stage’s hold, finalize, and confirm p99s.\n\nBuild once:\n\n```sh\nmake build-gate-linux build-ts\nmkdir -p docs/evidence/TKT-125\n```\n\nRun enabled:\n\n```sh\nOPENAPI_RESPONSE_VALIDATION_ENABLED=true \\\nONSALE_PROFILE=full \\\nONSALE_REPORT=\"$PWD/docs/evidence/TKT-125/response-validation-on.json\" \\\nSMOKE_TEST_TIMEOUT=40m \\\n./scripts/smoke.sh\n```\n\nRun disabled on the same idle host and commit:\n\n```sh\nOPENAPI_RESPONSE_VALIDATION_ENABLED=false \\\nONSALE_PROFILE=full \\\nONSALE_REPORT=\"$PWD/docs/evidence/TKT-125/response-validation-off.json\" \\\nSMOKE_TEST_TIMEOUT=40m \\\n./scripts/smoke.sh\n```\n\nEach invocation receives a clean isolated Compose stack through `scripts/smoke.sh`. `compose.yaml` must expose `OPENAPI_RESPONSE_VALIDATION_ENABLED` through the shared `x-go-service` environment so the shell override reaches all five services.\n\nExtract from each report:\n\n```sh\njq '.stages[] | select(.name == \"nfr-3000pm\") |\n    {hold_p99_ms, finalize_p99_ms, confirm_p99_ms}' \\\n    docs/evidence/TKT-125/response-validation-on.json \\\n    docs/evidence/TKT-125/response-validation-off.json\n```\n\nCreate `docs/evidence/TKT-125/README.md` containing:\n\n- commit SHA and host details;\n- exact commands;\n- confirmation that both runs completed with zero drops/client errors/server errors in the NFR window;\n- a table for hold, finalize, and confirm with:\n  - validation-on p99 in milliseconds;\n  - validation-off p99 in milliseconds;\n  - absolute delta `on - off` in milliseconds;\n  - percentage delta `(on - off) / off × 100`;\n- the operational conclusion.\n\nIf the first pair is close enough that host noise could determine the decision, run two more alternating pairs, retain the additional JSON reports with numbered suffixes, and compare medians. Do not describe a one-pair near-zero result as causal evidence.\n\nIf the delta is negligible:\n\n- Keep `OPENAPI_RESPONSE_VALIDATION_ENABLED=true` as the production policy.\n- Retain the switch, default-on behaviour, request-context fix, tests, and evidence.\n- Amend ADR-028 to say the runtime placement was measured and deliberately retained everywhere.\n- Do not introduce sampling merely to exercise the knob.\n\nIf the delta is material:\n\n- Recommend production set the variable to `false`.\n- Keep dev, CI, `make check`, and normal smoke runs at the default `true`.\n- Record that production has no runtime response fail-closed guarantee.\n\nThe validation-off measurement is performance evidence only; it does not replace the normal validation-on `make check`.\n\n## 6. ADR-028 amendment\n\nAmend `docs/adr/ADR-028-response-drift-fail-closed.md` after measurement so its conclusion contains the actual numbers.\n\nUpdate these sections:\n\n- **Status**\n  - Record the TKT-125 amendment and date.\n\n- **Context**\n  - Replace the unconditional “every service response” claim.\n  - Describe the recorder, per-request `FindRoute`, body decode/schema walk, and on-sale hold-transition placement.\n  - Reference the TKT-125 evidence.\n\n- **Possible Solutions**\n  - Preserve the historical fail-closed versus pass-through decision.\n  - Add placement alternatives: fully enabled, production-off with gate-on, and sampled.\n  - Record why sampling was rejected.\n\n- **Decision**\n  - Request validation remains unconditional.\n  - `OPENAPI_RESPONSE_VALIDATION_ENABLED` controls only response validation.\n  - Default is enabled.\n  - Wherever enabled, existing fail-closed semantics are unchanged.\n  - State the measured production setting: off if material, otherwise retained on.\n\n- **Consequences**\n  - Enabled mode still buffers and cannot promise streaming/hijacking.\n  - Disabled mode has no runtime response-schema rejection or drift log.\n  - Gate-time checks remain enabled and deterministic.\n  - Record the availability/diagnostic trade-off supported by the measured p99 deltas.\n\nApply ADR-021’s claim discipline explicitly:\n\n- This is an honest-implementation consistency control, not tamper-evidence.\n- When enabled, it guarantees only that a response which reaches this middleware and fails the committed schema/status validation is replaced before reaching the client.\n- It does not guarantee semantic correctness of schema-valid data.\n- It does not constrain an adversary who can change service code or configuration, disable the switch, replace the OpenAPI document, or produce malicious but schema-valid data.\n- When disabled, there is no runtime response-contract guarantee.\n- The gate guarantee covers exercised test paths only:\n  - catalog’s per-operation obligation is unit-level with a fake store under ADR-030;\n  - inventory, commerce, payments, and access use the smoke coverage gate;\n  - the coverage gates do not prove every documented status or every production data state.\n\n## 7. Final verification\n\nRun the focused tests during each TDD slice, then the normal enabled gate:\n\n```sh\nmake check\n```\n\nConfirm:\n\n- response validation is on in the normal smoke stack;\n- TKT-108 mask-body detection still passes in the unfiltered catalog suite;\n- `smoke/contract_validation_test.go` continues validating exercised real-stack responses;\n- the validation-off load run is not used as the gate result;\n- no OpenAPI document changed and no generated file drift was introduced.\n\nNo browser form verification is required because this ticket changes only Go HTTP middleware and configuration.\n\n## 8. Files touched\n\n| File | Reason |\n|---|---|\n| `shared/go/runtimecfg/runtimecfg.go` | Add the default-on HTTP policy field and boolean environment parser. |\n| `shared/go/runtimecfg/runtimecfg_test.go` | Pin default-on, explicit-off, and malformed-value behaviour. |\n| `shared/go/contract/http.go` | Gate response wrapping, preserve unconditional request validation, and pass request context into response validation. |\n| `shared/go/contract/http_test.go` | Pin disabled passthrough, enabled byte identity, unconditional request validation, and request-context/trace behaviour. |\n| `services/inventory/internal/api/server.go` | Accept and forward the response-validation policy. |\n| `services/commerce/internal/api/server.go` | Same. |\n| `services/payments/internal/api/server.go` | Same. |\n| `services/access/internal/api/server.go` | Same while preserving the custom request error handler. |\n| `services/catalog/internal/api/server.go` | Gate catalog’s response-only wrapper while retaining catalog’s request validator. |\n| `services/inventory/cmd/inventory/main.go` | Pass `HTTPFromEnv` policy into `Router`. |\n| `services/commerce/cmd/commerce/main.go` | Same. |\n| `services/payments/cmd/payments/main.go` | Same. |\n| `services/access/cmd/access/main.go` | Same. |\n| `services/catalog/cmd/catalog/main.go` | Pass the policy into `NewRouter`. |\n| `services/inventory/internal/api/server_test.go` | Pass enabled explicitly. |\n| `services/inventory/internal/api/seat_handler_smoke_test.go` | Pass enabled explicitly. |\n| `services/commerce/internal/api/server_test.go` | Pass enabled explicitly. |\n| `services/payments/internal/api/server_test.go` | Pass enabled explicitly. |\n| `services/payments/internal/api/psp_test.go` | Pass enabled explicitly. |\n| `services/access/internal/api/server_test.go` | Pass enabled explicitly. |\n| `services/access/internal/api/server_smoke_test.go` | Pass enabled explicitly. |\n| `services/catalog/internal/api/server_test.go` | Keep the normal catalog environment explicitly enabled and support the focused disabled wiring test. |\n| `services/catalog/internal/api/coverage_test.go` | Pin both enabled fail-closed wiring and disabled passthrough without weakening ADR-030. |\n| `compose.yaml` | Propagate the default-on environment variable to all Go services. |\n| `docs/adr/ADR-028-response-drift-fail-closed.md` | Amend placement, guarantees, sampling decision, evidence, and streaming limitation. |\n| `docs/evidence/TKT-125/response-validation-on.json` | Raw validation-on full-profile report. |\n| `docs/evidence/TKT-125/response-validation-off.json` | Raw validation-off full-profile report. |\n| `docs/evidence/TKT-125/README.md` | Record exact p99 deltas, method, commit, and decision. |\n\n`smoke/onsale_load_test.go`, `scripts/smoke.sh`, the Makefile, OpenAPI documents, and generated API files should not need modification.\n\n## 9. Risks and failure modes\n\n- **Catalog coverage silently loses its validator.** Mitigate by passing `true` explicitly from `newEnv`, retaining `TestRuntimeResponseDriftFailsClosed`, adding the disabled wiring test separately, and running the unfiltered catalog suite.\n- **TKT-108 laundering detection is bypassed.** Keep normal catalog tests enabled and preserve the exact generic mask body used by `env.validateResponse`.\n- **Smoke’s contract guarantee is weakened by environment leakage.** `compose.yaml` defaults the variable to true; CI and the final `make check` must run without the off override. Treat the off run only as measurement.\n- **The load harness performs only partial transition-body checks.** `loadtest.ValidTransitionBody` checks the target status and currently relies on middleware for full shape validation. This is acceptable for the controlled off measurement but is not a replacement production contract guarantee.\n- **A missed router call site defaults to false accidentally.** Avoid optional/variadic parameters and update every call site explicitly; compilation should fail until all are handled.\n- **Disabled mode changes more than CPU cost.** Direct writes restore the real writer’s interface and remove buffering/header replay. The passthrough test must assert original status, headers, and bytes so this is deliberate.\n- **A malformed boolean is silently treated as false.** Fail startup instead of accepting an invalid value.\n- **Near-zero results are dominated by host noise.** Use the full NFR stage on the same host and commit; repeat alternating pairs before declaring negligible.\n- **The gate is overclaimed.** Catalog uses fake-store unit coverage, smoke coverage counts an operation after any documented 2xx, and neither proves unexercised statuses or schema-valid semantic correctness. Preserve those limits in ADR-028.\n- **Streaming is “fixed” by exposing `Flush` through the buffer.** That would still not stream to the client and could violate fail-closed ordering. Leave enabled-mode streaming/hijacking unsupported and split the policy decision.\n- **Sampling sneaks in later through a numeric interpretation of the boolean.** Keep the accepted values boolean and reject a rate; sampling requires a new ADR amendment and contract.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan final — TKT-125 — critique by **main-agent** (claude-opus-5[1m]), Gate 2 self-resolved\n\n`config.models.planReview = main-agent`. Cross-model invariant holds: the draft is gpt-5.6-sol's,\nthe critique is not. **This is a delta.** The `kind=plan` draft as amended below is the plan.\n\n---\n\n## Verdict on the draft: accept, with 5 amendments and 1 factual correction\n\nThe draft is unusually well-grounded. I verified every seam, symbol, stage name and JSON field it\nnames against HEAD — **all exist**, no hallucinated seam:\n\n| Draft claim | Verified at HEAD |\n|---|---|\n| `responseValidated`, `requestValidator`, `load`, 3 exported entry points | `shared/go/contract/http.go:40,55,74,113` ✅ |\n| 5 call sites (inventory/commerce/payments `:64,64,65`, access `:42`, catalog `:104`) | ✅ all five |\n| 5 `main.go` router constructions | `catalog:149`, `commerce:158`, `access:282`, `inventory:189`, `payments:298` ✅ |\n| `duration()` / `integer()` helpers, no bool helper | ✅ `runtimecfg.go:105,117` |\n| `TestRuntimeResponseDriftFailsClosed`, `TestMain` coverage gate, `-run`-filtered skip | ✅ `coverage_test.go:87,68,72` |\n| TKT-108 mask-body detection in `env.validateResponse` | ✅ `server_test.go:1174,1189` |\n| Stage name `nfr-3000pm` | ✅ `onsale_load_test.go:430` |\n| JSON fields `hold_p99_ms` / `finalize_p99_ms` / `confirm_p99_ms` | ✅ `loadtest.go:278-280` |\n| `loadtest.ValidTransitionBody` checks only `status` | ✅ `loadtest.go:379` |\n\n**Cons-vs-Decision pass (the cheapest reject) — no self-refutation found.** The draft's sharpest\nCon is *\"the load harness performs only partial transition-body checks — `ValidTransitionBody`\nchecks the target status and relies on middleware for full shape validation\"*, which would refute\nthe validation-off measurement if the load test's verdict came from response bodies. It does not:\nthe oversell verdict is a direct SQL read of pool state (`onsale_load_test.go:165`), independent of\nthe middleware. The Con is correctly scoped and its \"acceptable for the off measurement\" disposition\nholds. Its other Con — *\"a missed router call site defaults to false accidentally\"* — is answered by\nits own design (a required parameter is a compile error, not a silent default).\n\n---\n\n## Amendments\n\n### A1 — the knob does **not** go on `runtimecfg.HTTP` (amend §1)\n\n**Draft:** add `ValidateOpenAPIResponses bool` to the `HTTP` struct, read by `HTTPFromEnv()`.\n\n**Rejected.** `HTTP.Apply(server *http.Server)` exists to set `http.Server` fields; response\nvalidation is not one. More concretely: **the gateway also calls `HTTPFromEnv()`**\n(`docs/configuration.md` §Gateway) and does no OpenAPI validation at all — the field would be dead\nweight there, and the next reader has to discover that the struct is two policies wearing one name.\n\n**Amendment:** a standalone accessor beside the others, matching `InternalTokenFromEnv()`'s shape:\n\n```go\n// runtimecfg.go\nfunc ResponseValidationFromEnv() (bool, error)   // OPENAPI_RESPONSE_VALIDATION_ENABLED, default true\n```\n\nEverything else in §1 stands: `strconv.ParseBool`, default `true`, malformed value fails startup\nnaming the variable, and the unexported `boolean(name string, fallback bool) (bool, error)` helper\nalongside `duration`/`integer`.\n\n### A2 — explicit bool threading is **accepted**, deliberately, over the shorter alternatives\n\nI considered two lower-churn designs and rejected both; recording the reasoning so it isn't\nre-litigated at review:\n\n- **Env read inside `shared/go/contract`** (≈5 files instead of ≈25, zero test churn): rejected —\n  it breaks the repo's uniform \"read config in `main` via `runtimecfg`, pass it down\" shape, and\n  hides a behaviour switch from every call site.\n- **Variadic option so absent = enabled** (zero test churn, safe default): rejected — one optional\n  parameter to save ~8 mechanical one-line edits is clever where boring wins. A required parameter\n  cannot be forgotten.\n\nThe ~8 test call sites passing `true` explicitly is churn I'm accepting on purpose: it is what keeps\nADR-030's catalog unit coverage gate visibly behind the real validator.\n\n### A3 — pre-register the materiality threshold **before** measuring (amend §5)\n\n**Draft:** decide \"material vs negligible\" after seeing the numbers, and if close, run \"two more\nalternating pairs\" — up to six full-profile runs.\n\n**Amended.** A threshold chosen after seeing the data is not a measurement, it is a rationalisation,\nand this repo has a learning about exactly that class of self-deception\n([coarse observables pass the broken build](docs/learnings/2026-07-25-coarse-observables-pass-the-broken-build.md)).\nPre-registered here, before any run:\n\n> **Material** = at the `nfr-3000pm` stage, validation-on p99 exceeds validation-off p99 by\n> **≥10% or ≥50 ms** on **any** of hold / finalize / confirm.\n> Anything below both bars on all three is **negligible** → validation stays on everywhere and the\n> ticket closes as a documentation + context-propagation fix, exactly as the COS permit.\n\nRun budget: **one pair** (on, off). A second pair only if the first straddles a bar (within ±3\npoints of 10%, or 40–60 ms). Never more than two pairs — beyond that the host noise is the finding.\n\n### A4 — `docs/configuration.md` is missing from the file list (amend §8)\n\nIt is this repo's env-var registry; every knob in `compose.yaml`'s `x-go-env` has a row there\n(verified: `DB_MAX_OPEN_CONNS`, the four `HTTP_*`). A new variable that isn't in it is\nundiscoverable. Add a row under **Go services (all five)** — *not* under **HTTP servers (services\nand gateway)**, per A1: the gateway does not read it.\n\n### A5 — two honesty clauses the draft left implicit (amend §5, §6)\n\n1. **The off-run's smoke suite is expected to pass.** If it fails, that is a finding — something in\n   the suite depends on the fail-closed wrap — and it gets reported, not worked around.\n2. **The new evidence README must state that the TKT-82 baseline was measured with validation ON.**\n   `docs/verification/on-sale-load/README.md` publishes a per-pool ceiling that TKT-20 consumes; a\n   future reader comparing it against a validation-off number would be comparing configurations\n   without knowing it.\n\n---\n\n## Factual correction — the ticket's own finding is wrong on one point\n\n> *\"it calls `context.Background()` for validation, so a drift error is logged **outside** the\n> request trace — the one place you would want `trace_id`.\"*\n\n**This is false at HEAD.** `shared/go/contract/http.go:94` already logs with\n`logger.ErrorContext(r.Context(), …)`, and `obs.NewLogger` (`shared/go/obs/obs.go:26,38`) lifts\n`trace_id` off the context's active span. **Drift logs already carry `trace_id` today.**\n\nThe real defect is smaller and different: `openapi3filter.ValidateResponse` receives\n`context.Background()`, so **validation itself** ignores request cancellation and deadlines. Worth\nfixing — it is one line and it is correct — but:\n\n- the ADR amendment and the closeout **must not claim** to have added trace correlation that\n  already existed;\n- COS 5 is therefore satisfied *as written* (\"the validator runs on the request context\") while its\n  stated *rationale* was already true. I am implementing the letter, and recording that the\n  rationale was mistaken rather than quietly inheriting it.\n\nThe draft caught the *test* consequence of this (§3: \"a trace-only test would pass without detecting\nthe defect\", hence the injected validator func) but did not flag that the ticket's premise was\nwrong. Credit where due — the injection design is right and I'm keeping it, scoped to the\nunexported `responseValidated` as the draft proposed.\n\n---\n\n## Pre-mortem lens — applied, 4 causes, 2 patched\n\n*Assume this bounced at Gate 3. What went wrong?*\n\n1. **The measurement decided nothing** — two runs, delta in the noise, and the plan had no\n   pre-agreed bar, so the write-up argues its way to whichever answer was wanted. → **patched by A3.**\n2. **A test file was missed and silently disabled validation** — a service suite constructs its\n   router somewhere I didn't grep, gets `false`, and a whole package stops enforcing ADR-028 while\n   staying green. → *not credible*: a required parameter means the compiler enumerates the call\n   sites for me. The draft is right; no patch.\n3. **The env var never reached the containers** — `compose.yaml`'s `x-go-env` isn't touched, both\n   runs measure the same configuration, and the \"delta\" is pure noise reported as a result. →\n   **patched:** the first thing the off-run's write-up must record is a positive control — the\n   off-run's evidence JSON is only valid if a service actually saw `false`. I will assert this from\n   the running stack (drift log absence is not proof; a container-env read is). Added to §5.\n4. **Scope creep into streaming** — someone \"fixes\" the Flusher/Hijacker gap under the same ticket\n   and the diff triples. → the draft already splits it out with a correct justification (fail-closed\n   requires retaining the whole response; a flush would send bytes before the validator can decide).\n   Held.\n\n---\n\n## Introduced at plan-review, reviewed by no second model\n\nThe cross-model guarantee covers the draft and, later, the code — **not** this critique's own\ninventions. These reached the (waived) Gate 2 reviewed by nobody but me:\n\n1. **A1** — `ResponseValidationFromEnv()` as a standalone accessor instead of a field on `HTTP`.\n2. **A3** — the pre-registered ≥10%-or-≥50 ms materiality bar and the two-pair run cap. *These\n   numbers are my judgement, not derived from any existing SLO in the repo.*\n3. **A5** — the off-run-must-pass clause and the TKT-82-baseline-was-ON note.\n4. **Pre-mortem cause 3's positive control** — asserting a service actually observed `false` before\n   trusting the off-run numbers.\n5. **The factual correction** on `trace_id`, and the decision to implement COS 5 anyway while\n   recording that its rationale was mistaken.\n\nThe A2 rejections (env-read-in-contract, variadic option) are also mine, but they are *rejections* —\nthey leave the draft's design standing, so nothing unreviewed enters the code from them.\n\n---\n\n## Gate 2 — self-resolved under the owner's waiver\n\nThe owner waived Gate 2 for this run and set the rule: *prefer the option the critique recommends;\nabsent a recommendation, pick the most reversible option.* Decisions taken, all recorded:\n\n| # | Decision | Rule applied | Rationale |\n|---|---|---|---|\n| D1 | **Boolean knob, full-off — no sampling** (the DoR's one deferred `uncertainties` item) | critique recommends | The draft recommends it and I concur. It is *also* the most reversible: adding sampling later is additive and needs a new ADR amendment; unwinding a shipped two-behaviours-per-endpoint contract is not. Both decision rules point the same way. |\n| D2 | Knob lives in `ResponseValidationFromEnv()`, not on `HTTP` | critique (mine) | A1. |\n| D3 | Explicit required-parameter threading, not env-read-in-library or variadic | critique (mine) | A2 — boring over clever; compile-enumerated call sites. |\n| D4 | Materiality bar fixed at ≥10% or ≥50 ms, one pair, max two | critique (mine) | A3 — pre-registration is the whole point. |\n| D5 | Streaming / `Flusher` / `Hijacker` split to a follow-up ticket | critique recommends | Draft §4; COS do not require it, and enabled-mode streaming is a policy question, not an adapter fix. **I will file the Backlog ticket at closeout, not now** — it is not needed until a streaming route exists. |\n| D6 | Full NFR profile (`ONSALE_PROFILE=full`), not the gate profile | critique recommends | The gate profile's sustained stage is 25/s × 10s = ~250 samples; a \"p99\" over 250 samples is the 3rd-worst request, i.e. noise. `nfr-3000pm` gives 9,000. Verified against `onsale_load_test.go:307,430`. |\n\n**`risk:low` remains unset and must stay unset** — this changes middleware wrapping all five\nservices on a path holding a p99 SLO.\n\n---\n\n## Chosen test seam (recorded per quality-practices §1)\n\n**Existing seams, three of them, highest-that-isolates, no new seam invented:**\n\n1. `shared/go/contract/http_test.go` — the behaviour of the knob itself (default-on, off passes\n   drift through unmodified, on is byte-identical to today, request validation stays unconditional,\n   the validator receives the request context). This is where the change lives.\n2. `shared/go/runtimecfg/runtimecfg_test.go` — parsing, default, malformed-value rejection.\n   Extends the existing `TestInvalidConfiguration`.\n3. `services/catalog/internal/api/coverage_test.go` — the *wiring*, not the middleware:\n   `TestRuntimeResponseDriftFailsClosed` stays on `true`, and one new sibling proves the disabled\n   path passes the rigged negative-price payload through. This is the ADR-030 guard.\n\n**Fixture check** (quality-practices §1 — \"a fixture built from the type under test cannot fail\"):\nthis ticket is not compatibility/schema/versioning work, so that trap does not bind. The drift\nfixtures are already hand-rigged out of schema (a literal `{\"wrong\":true}` body in the contract\ntests; a `PriceAmount = -1` store mutation in catalog) — neither is constructed from the type it is\nmeant to violate. No change needed.\n\n**Disposition sweep** (quality-practices §1 — fixtures riding a changed disposition): the disposition\nof a *drifting response* changes only when the knob is off, and nothing sets it off except the\nmeasurement run and the one new catalog test. Grepped `response violates OpenAPI contract` across\nall test trees — exactly two live assertions (`coverage_test.go:100` requires it,\n`server_test.go:1189` forbids it), both on the enabled path, both unaffected.\n\n---\n\n## Net plan\n\nThe draft's §1–§9 **as amended by A1–A5 and the factual correction above**, with §2 (boolean,\nfull-off), §4 (streaming split out) and §5's use of the full NFR profile accepted as written.\nImplementation proceeds under `agent:coding` by `config.models.implement = main-agent`, TDD, tests\nobserved red first.\n"
    },
    {
      "stage": "coding",
      "kind": "blocker",
      "body": "<!-- sdlc:stage=coding kind=blocker -->\n### ⛔ Stopped at COS 3 — the measurement cannot be taken on this host\n\nLabel stays `agent:coding`, status stays `Building`. Nothing is pushed; no PR is open. This is the\nowner's **stop condition (a)** — the gate failed twice on the same cause with no fix available to\nthe agent.\n\n### What is done and green\n\n| COS | State |\n|---|---|\n| 1 — switchable via `runtimecfg`, default on for dev/CI/smoke | ✅ `OPENAPI_RESPONSE_VALIDATION_ENABLED` / `runtimecfg.ResponseValidationFromEnv` |\n| 2 — ADR-028 amended on placement, semantics untouched | 🟡 written except the measurement section, which is marked NOT ready to merge |\n| 3 — p99 delta measured on/off, recorded as a number | ⛔ **blocked — see below** |\n| 4 — TKT-108 mask-body detection still passes | ✅ catalog unfiltered suite green |\n| 5 — validator runs on the request context | ✅ (see the correction below) |\n| 6 — `make check` green | ✅ EXIT=0, zero `FAIL`, commit `7fce06b` |\n\nCommits on `TKT-125-gate-response-validation`: `7fce06b` (implementation), `526934f` (ADR, partial).\n\n### The blocker\n\nThree attempts at `ONSALE_PROFILE=full`, all defeated by host contention — never by the diff:\n\n| # | Failure | Load avg (12 cores) | In the diff? |\n|---|---|---|---|\n| 1 | `nfr-3000pm inconclusive: 90 client-side transport errors — not a server verdict; rerun` | ~150 | no — the harness's own generator guard |\n| 2 | `TestExpiredChannelHoldFreesItsCap` — 50 ms hold TTL expired early | ~150 | no — `inventory/internal/store`, untouched |\n| 3 | **`TestExpiredChannelHoldFreesItsCap` again** + `TestScheduledReleaseIsLazyAndObservable` | **415** | no — same file |\n\nAttempt 3 never reached the load stage. `make check` passed clean between attempts 2 and 3 (load\n~40), which is the control proving these are load artifacts and not a regression: the same suites,\nthe same commit, green.\n\n**Why not just retry again:** the measurement is a p99 latency delta. A host at 12–35×\noversubscription cannot produce one that means anything, and the ticket's own context-mémo names\nthis trap ([coarse observables pass the broken build](docs/learnings/2026-07-25-coarse-observables-pass-the-broken-build.md)).\nA number obtained by retrying until the host cooperated would be worse than no number, because it\nwould land in ADR-028 and `docs/evidence/` as fact.\n\n**The materiality bar was pre-registered before any run and still stands:** material = validation-on\np99 exceeds validation-off p99 by **≥10% or ≥50 ms** on any of hold / finalize / confirm at\n`nfr-3000pm`. Below both on all three ⇒ negligible ⇒ default stays on everywhere and the ticket\ncloses as a documentation + context-propagation fix. Recording the bar in advance is what stops the\nresult being rationalised after the fact.\n\n### To resume\n\nRun both arms on a quiet host, then fill ADR-028 § The measurement and `docs/evidence/TKT-125/`:\n\n```sh\nmake build-gate-linux build-ts && mkdir -p docs/evidence/TKT-125\nOPENAPI_RESPONSE_VALIDATION_ENABLED=true  ONSALE_PROFILE=full SMOKE_TEST_TIMEOUT=40m \\\n  ONSALE_REPORT=\"$PWD/docs/evidence/TKT-125/response-validation-on.json\"  ./scripts/smoke.sh\nOPENAPI_RESPONSE_VALIDATION_ENABLED=false ONSALE_PROFILE=full SMOKE_TEST_TIMEOUT=40m \\\n  ONSALE_REPORT=\"$PWD/docs/evidence/TKT-125/response-validation-off.json\" ./scripts/smoke.sh\njq '.stages[]|select(.name==\"nfr-3000pm\")|{hold_p99_ms,finalize_p99_ms,confirm_p99_ms}' \\\n  docs/evidence/TKT-125/response-validation-*.json\n```\n\nPositive control (pre-mortem cause 3, already verified once for the ON arm): the off arm's numbers\nare only valid if a service actually saw `false` —\n`docker inspect ticketing-smoke-<slot>-inventory-1 --format '{{range .Config.Env}}{{println .}}{{end}}' | grep OPENAPI`.\n\nRemaining after the numbers land: ai-review (`gpt-5.6-sol`), PR, merge, closeout.\n\n### Correction to the ticket's finding — carried forward from `kind=plan-final`\n\n> *\"it calls `context.Background()` for validation, so a drift error is logged **outside** the\n> request trace — the one place you would want `trace_id`.\"*\n\n**False at HEAD.** `shared/go/contract/http.go:94` already logged via\n`logger.ErrorContext(r.Context(), …)`, and `obs.NewLogger` lifts `trace_id` off the context's span —\nso drift logs always carried it. The real defect was narrower: `openapi3filter.ValidateResponse`\nreceived `context.Background()`, so **validation** ignored request cancellation and deadlines. Fixed,\nand the test asserts on the context the validator itself receives — asserting on the log would have\npassed against the unfixed code. Neither the ADR nor the closeout may claim this ticket added trace\ncorrelation.\n\n### Estimate miss, recorded\n\n`kind=plan-final` predicted \"~8 mechanical test call sites passing `true`\". The real number was\n**45**, across 8 files. The design decision (explicit required parameter over a variadic option or a\nlibrary-internal env read) still holds — a required parameter cannot be forgotten — but the diff\ncarries 5× the mechanical noise I told the gate it would.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation complete — PR [#115](https://github.com/mathd/ticketing_system/pull/115)\n\n**Implementer:** `main-agent` (claude-opus-5[1m]) per `config.models.implement`. TDD, tests observed\nred before each slice.\n\n**Diff:** 27 files, +554/−80. Commits: `7fce06b` (implementation), `526934f` (ADR, partial),\n`42752a8` (measurement + ADR completed).\n\n**Gate:** `make check` EXIT=0, zero `FAIL`, on the pushed tree — including catalog's *unfiltered*\nsuite (ADR-030's coverage gate only enforces on unfiltered runs) and TKT-108's mask-body detection.\n\n### TDD sequence\n\n| Slice | Red observed | Green |\n|---|---|---|\n| A — `runtimecfg` | `undefined: ResponseValidationFromEnv` ×3 | default-on, explicit-off, malformed-value-rejected |\n| B — `shared/go/contract` | `too many arguments` across 7 existing tests + 6 new | all 13 pass |\n| C — service wiring | compile errors enumerated every call site | 5 services build |\n\n**One test was written green, and I am flagging it rather than letting it pass as red-first:**\n`TestRuntimeResponseDriftPassesThroughWhenValidationDisabled` (catalog, the ADR-030 guard) was\nwritten after the wiring existed. I mutation-checked it instead — pinning `validateResponses` to\n`true` in `NewRouter` makes it fail with `disabled validation must not substitute a status: 500`,\nand restoring makes it pass. The mutant dies, so the test detects a router that ignores the policy.\n\n### The measurement — the ticket's actual question, answered no\n\n`nfr-3000pm`, 9,000 lifecycles per run, quiet host, off arm verified *inside the container*:\n\n| Run | Validation | hold p99 | lifecycle p99 |\n|---|---|---|---|\n| A | on | 4.7 ms | 9.1 ms |\n| B | on | 8.1 ms | 17.0 ms |\n| C | **off** | 5.1 ms | 10.6 ms |\n\nThe off run lands **between** the two on runs; two same-configuration runs differ by **+72%** on hold\np99. Not material against the pre-registered bar. **No environment sets the variable to false.**\n\nFull method, discarded runs and provenance: `docs/evidence/TKT-125/README.md`.\n\n### Deviations from the plan, all recorded\n\n1. **Estimate miss.** `kind=plan-final` said \"~8 mechanical test call sites\"; the real number was\n   **45** across 8 files. The design decision (required parameter over variadic/library-env-read)\n   stands — a required parameter cannot be forgotten — but the diff carries 5× the predicted noise.\n2. **The ticket's `trace_id` premise was false**, as flagged at plan-review. Drift logs already\n   carried it (`http.go:94` used `ErrorContext(r.Context(), …)`). Only the validator's own context\n   was wrong. Neither the ADR nor the PR claims credit for adding trace correlation.\n3. **COS 3 partially met.** finalize/confirm p99 are unobtainable: the harness emits them only in a\n   JSON report written as the *last* statement of `onsaleFull`, and `sweep-600` trips the generator\n   guard on this single-host setup (3/3 runs). Lifecycle p99 covers both in aggregate, same null\n   result. The one-line fix (`defer writeReport`) touches the load harness, which the plan\n   explicitly excluded — follow-up ticket, not silent scope creep.\n4. **The pre-registered bar is flawed** and I said so in the evidence instead of adjusting it: its\n   10% relative arm is unusable when the whole p99 is under 10 ms. Applied as written; the null\n   result is robust to it either way.\n\n### Environmental failures, none in the diff\n\nFive load-profile attempts were lost to host contention (load average 150–415 on 12 cores):\ngenerator-guard trips, a 50 ms-TTL store test, a pnpm `TypeError`, and a payments recovery test that\nmissed its 45 s window right after the load stage. Each of those passed on the same commit in a\ngreen `make check` — the control. Detail in `kind=blocker` above and the evidence file.\n\nNext: adversarial review by `config.models.aiReview` = `gpt-5.6-sol`.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review — **gpt-5.6-sol** (codex `adversarial-review`, `--base origin/main --scope branch`)\n\n`config.models.aiReview = gpt-5.6-sol`. Cross-model invariant holds: `implement = main-agent`, so the\nreviewer is not the author. It was given the **diff only** — no plan, no ticket, no justification —\nand primed to assume the diff is wrong, with six named attack vectors (silent loss of the guarantee;\nenabled path byte-identity; disabled passthrough; the context change; env parsing; weakened test\nassertions).\n\n**Verdict: `approve` — no material findings.**\n\nJudged a **pass** per `codex-runner.md`: a conclusive verdict against the intended scope, not merely\nzero findings. Evidence it actually ran the scope — it traced all five production entrypoints\n(`rg RequestValidator\\(|ResponseValidator\\(`), read the diff at `--unified=40`, grepped the test diff\nspecifically for **deleted** lines (vector 6 — weakened assertions), and pulled kin-openapi's and\n`nethttp-middleware`'s source out of the module cache.\n\nIts summary: *\"All five entrypoints fail fast on malformed configuration and propagate the parsed\ndefault-on value; existing tests retain the enabled path. Enabled behavior is otherwise unchanged,\ndisabled handlers receive the original ResponseWriter, and kin-openapi v0.142.0 does not consult the\nsupplied validation context, so the context change introduces no cancellation behavior.\"*\n\n### Per-finding triage\n\n| # | Finding | Verdict | Action |\n|---|---|---|---|\n| 1 | kin-openapi v0.142.0 does not consult the validation context, so the context change introduces no cancellation behaviour | **BLOCKING — accepted** | Fixed in `f366eb4` |\n\n**Finding 1 verified independently before acting** (the reviewer is a lead generator, not an oracle).\n`openapi3filter.ValidateResponse` at\n`~/go/pkg/mod/github.com/getkin/kin-openapi@v0.142.0/openapi3filter/validate_response.go:22` takes\n`ctx context.Context` and **never reads it** — it is absent from `decodeBody`, from\n`contentType.Schema.Value.VisitJSON(...)`, and from every other line of the function body. Confirmed\nby `awk '/^func ValidateResponse/,/^}/' | grep ctx` returning only the signature line.\n\n**Why this was treated as blocking rather than a nitpick.** No logic is wrong — but the PR body, the\ncommit message and two code comments asserted the fix made validation \"observe the request's\ncancellation and deadline\", and that assertion is **false at the pinned version**. This repo treats\nan unsupported claim in an ADR or a code comment as a defect (ADR-021's discipline, and the reason\n`kind=plan-final` already corrected the *ticket's* `trace_id` premise). A ticket whose entire\ncontribution is debunking an unmeasured performance claim cannot ship an unmeasured claim of its\nown. Corrected in `shared/go/contract/http.go`, `shared/go/contract/http_test.go`,\n`docs/evidence/TKT-125/README.md` and the PR body; the ADR never carried the claim.\n\nThe change itself stands: passing `r.Context()` is correct against a future version that honours it,\nand the test asserting the validator receives the request's context is the right regression guard\neither way. It is now described as hygiene, not as a behavioural fix.\n\n**No findings were rejected or deferred** — there was one, and it was fixed.\n\n### Second pass — not run, deliberately\n\nThe fix diff is **comments and markdown only**: no control flow, no signature, no schema, no test\nlogic. `shared/go/contract/http.go` changed by one comment paragraph; `http_test.go` by one comment\nblock; the rest is documentation. That is the skill's \"pure trivia (typos, comments, formatting)\"\ncase → **one pass**, recorded here as required.\n\nFor the avoidance of doubt about what is unreviewed: the *only* code bytes not seen by the reviewer\nare comment text. Every executable line in this PR was reviewed at `f366eb4`'s parent.\n\n### Environment note — reviewer skill hijack (survivable, per `codex-runner.md` §f)\n\nThe codex run loaded an **unrelated client's** skill from\n`~/Sources/MoovAI/moov-skills/plugins/moov-livrai…` while reviewing this repo, and announced it was\n\"using the repository's architecture-review skill\". The runner reference documents this as\nunpreventable and instructs judging the output rather than the rubric — the output was concrete,\ncode-cited and correct, so the run passes. Flagged because cross-client skill bleed is worth knowing\nabout independently of this ticket.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### PO validation — self-performed under the owner-waived Gate 4\n\nMerged as **`59c0295`** (squash, PR [#115](https://github.com/mathd/ticketing_system/pull/115));\nbranch deleted local and remote. Validation below is against the **merged** `origin/main`, not the\nbranch — content re-verified after the merge (TKT-97's trap: a merge can capture a SHA that predates\nyour last commit).\n\n| # | COS | Verdict | Evidence on merged code |\n|---|---|---|---|\n| 1 | Response validation switchable via `runtimecfg`, defaulted **on** for dev/CI/smoke | ✅ met | `git show origin/main:shared/go/runtimecfg/runtimecfg.go` contains `ResponseValidationFromEnv`; `TestResponseValidationDefaultsOnAndCanBeDisabled` proves absence-of-variable ⇒ on; `compose.yaml:49` ships `${…:-true}` |\n| 2 | ADR-028 amended on placement; semantics where enabled untouched | ✅ met | § Amendment (TKT-125) present on main, \"NOT YET TAKEN\" placeholder absent; `TestResponseValidationEnabledPreservesFailClosedResponse` pins status + both headers + exact body bytes |\n| 3 | Load test run on **and** off; p99 delta recorded as a number in `docs/evidence/` | 🟡 **partially met** | `docs/evidence/TKT-125/README.md` on main records hold p99 4.7 / 8.1 ms (on) vs 5.1 ms (off) and lifecycle p99 9.1 / 17.0 vs 10.6, over 9,000 lifecycles per run. **finalize/confirm p99 are missing** — see below |\n| 4 | With validation enabled, TKT-108's mask-body detection still passes | ✅ met | catalog **unfiltered** suite green (the filter-sensitive ADR-030 gate only enforces unfiltered); `server_test.go:1189` untouched |\n| 5 | Validator runs on the request context | ✅ met **as written** | `TestResponseValidationUsesRequestContext` asserts the validator receives the request's context. Its *practical* value is nil at kin-openapi v0.142.0 — see § Honest caveats |\n| 6 | `make check` green | ✅ met | EXIT=0, zero `FAIL`, on the pushed SHA `f366eb4`; re-run post-merge on `59c0295` |\n\n### COS 3 is the one I will not mark green\n\nThe ticket asks for hold **/ finalize / confirm**. Only hold is emitted in the harness's log line;\n`finalize_p99_ms` and `confirm_p99_ms` live solely in the JSON report, and `writeReport` is the\n**last statement** of `onsaleFull` — so any aborted stage discards the entire report. On this\nsingle-host setup `sweep-600` trips the generator guard every time (3 runs of 3, 4–9 client-side\ntransport errors at 600 attempts/s), so no JSON was ever written and\n`response-validation-{on,off}.json` do not exist.\n\n**Lifecycle p99 is hold + finalize + confirm end to end** and shows the same null result, so the two\nmissing mutations are covered in aggregate — but not individually, and I am not going to call that\n\"met\". Closing it needs `defer writeReport(t, report)` in the load harness, which this ticket's plan\nexplicitly excluded from scope.\n\n### What the ticket actually established\n\nThe premise — that unconditional response validation is \"the largest self-inflicted cost standing\nagainst the project's stated scalability assumption\" — **is not supported by measurement.** Turning\nit off produces no change distinguishable from the harness's own ±72% run-to-run noise. The knob\nships because COS 1 requires it, defaulted on, with **no environment setting it false**. Nobody\nshould disable response validation on the strength of this evidence.\n\nThat is a passing outcome by the ticket's own terms: *\"If the delta is negligible, say so and keep\nvalidation everywhere — that outcome closes the ticket honestly.\"*\n\n### Honest caveats a PO should see\n\n1. **Two claims were retracted, not shipped.** The ticket's own `trace_id` premise was false (drift\n   logs already carried it), and my follow-on claim that the context change buys cancellation\n   awareness was also false — `openapi3filter.ValidateResponse` at v0.142.0 accepts the `ctx` and\n   never reads it. The second was caught by the adversarial review and verified in the module\n   source. COS 5 is therefore satisfied literally while delivering nothing observable today.\n2. **CI never ran.** GitHub Actions is billing-blocked on this account\n   (*\"The job was not started because recent account payments have failed…\"*), so all three checks\n   failed in 2s having executed **zero steps**. The merge proceeded on the local gate, which the\n   Makefile documents as mirroring CI exactly. **A red badge on this PR is not a code signal** — and\n   nothing in this repo can merge with green checks until the billing block is cleared.\n3. **No human reviewed this diff.** Gate 3 was waived. The review-guide on the PR is the audit\n   record.\n4. **One test was written green** (`TestRuntimeResponseDriftPassesThroughWhenValidationDisabled`) and\n   mutation-checked rather than observed red-first. Stated in the coding summary rather than papered\n   over.\n"
    }
  ]
}
