{
  "key": "TKT-119",
  "type": "Task",
  "summary": "Make the access alarm surface say the true thing: per-class durable error + ADR-025 §D9 PII wording",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "number": 131,
    "url": "https://github.com/mathd/ticketing_system/pull/131",
    "state": "merged"
  },
  "description": "Access boot-checks three alarm durables through the same helper: integrity (ACCESS_LIFECYCLE_ALARM_DURABLE), admission-conflict (ACCESS_ADMISSION_CONFLICT_DURABLE) and policy-conflict (ACCESS_POLICY_CONFLICT_DURABLE) — main.go:194-204. But services/access/internal/lifecyclejob/publisher.go:60-62 hardcodes \"ACCESS_LIFECYCLE_ALARM_DURABLE is required\" in the empty-durable error, and the two following error wraps say \"integrity alarm stream\" / \"integrity alarm durable\" for every class too.\n\nOperator impact: unset ACCESS_ADMISSION_CONFLICT_DURABLE and access refuses to boot telling you to set a *different* variable that is already set. Only the caller's wrap (\"admission conflict alarm route: ...\") disambiguates, and it sits outside the sentence that names the fix. This became reachable-in-practice with TKT-86, which added the first operator-facing documentation pointing at ACCESS_ADMISSION_CONFLICT_DURABLE.\n\nLikely shape: pass the env var name (or a class label) into RequireAlarmRoute and use it in all three messages. Small, isolated, no behaviour change — boot still fails, it just says the true thing.\n\nSource: TKT-86 ai-review, arm fable-5, finding 3 — triaged INCIDENTAL. Pre-existing on origin/main (predates TKT-86; confirmed there), so fixing it inside TKT-86's PR would have been scope creep.\n\n---\n\n## Absorbed TKT-120 — ADR-025 §D9 excludes a field the same ADR mandates\n\n`docs/adr/ADR-025-admission-events-and-offline-reconciliation.md:193` reads: *\"Occurrence ids and\nalarm payloads carry bounded identifiers and enums only — no buyer, no guest reference, no raw\nscanner-operator identity (ADR-003 §D3).\"*\n\nThe shipped admission-conflict payload carries `device_occurred_at` (a timestamp) and `skew_flagged`\n(a bool) alongside its four UUIDs — `services/access/internal/store/reconcile.go:297-304`. The\npolicy-conflict payload carries `version` (int) and `revisable` (bool) — `scan.go:305-315`.\n\n**Nothing is wrong with the code**: §D5 *requires* the device-claimed time. The defect is in the\ndecision record. It matters because §D9 is the PII contract a future consumer or reviewer implements\nfrom — a strict reading rejects every schema-1 alarm shipped today. The wording should name the real\nfloor (bounded identifiers, enums, and operational scalars not attributable to a person) while\nkeeping the prohibition (buyer, guest reference, raw operator identity) unchanged.\n\nThis half is a **decision, not a transcription** — settling what the PII floor admits is why it goes\nthrough the plan gate rather than straight to the default branch.\n\n## Why these are one ticket\n\nBoth are the access alarm surface telling an operator or a reviewer something untrue: one in an\nerror string, one in the ADR clause that governs the same payloads. Same files open, same reviewer\ncontext. Neither justifies its own plan gate; together they justify one.\n",
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\nSpawned from TKT-86's ai-review triage (incidental finding, deferred out of PR #110). Not shaped yet: needs the 8-item DoR bake before Gate 1."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Gate 1 validation — re-verified against HEAD `ac2952b`\n\n**Still real.** `services/access/internal/lifecyclejob/publisher.go:61` still hardcodes \"ACCESS_LIFECYCLE_ALARM_DURABLE is required\", and :65/:69/:73/:84 still say \"integrity alarm\" for all three classes.\n\n**Verdict: still worth doing → Ready.** Not DoR-baked: the 8-item shaping happens when the ticket is claimed, per the owner's drain-the-debt call."
    },
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\n### Merged TKT-120 into this ticket (backlog drain, 2026-07-29)\n\nWas: *RequireAlarmRoute's missing-durable error names ACCESS_LIFECYCLE_ALARM_DURABLE for all three alarm classes*\n\nTKT-120 is now **ADR-025 §D9 says alarm payloads are \"identifiers and enums only\" — the shipped payload also carries a timestamp** — absorbed above, and moved back to Backlog marked MERGED so it cannot be claimed twice. Its own thread stays readable on the board for the full filing history and review lineage.\n\nRationale: a plan gate + ai-review + PO gate on each half costs more than the work. See the *Why these are one ticket* section in the description."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claim — TKT-119\n\n**Branch:** `TKT-119-alarm-surface-truth` off `origin/main` @ `8cbcd0c`.\n\n**Selection reason:** next in Ready by key. Zero open blockers — the only link is\n`absorbs TKT-120`, parked in Backlog marked MERGED so it cannot be claimed twice.\n\n**Both halves re-verified at claim time** (the readiness verdict was against `ac2952b`; HEAD is now\n`8cbcd0c`):\n- `publisher.go:61` still hardcodes `\"ACCESS_LIFECYCLE_ALARM_DURABLE is required\"`, and :65/:69/:73/:84\n  still say \"integrity alarm\" for all three classes. All three call sites confirmed at\n  `main.go:197/203/208` — integrity, admission-conflict, policy-conflict, one helper.\n- ADR-025 §D9 (`:193-194`) still reads *\"bounded identifiers and enums only\"*, while the shipped\n  payloads carry `device_occurred_at`+`skew_flagged` (`store/reconcile.go:299-304`) and\n  `version`+`revisable` (`store/scan.go:305-315`).\n\n**Risk: NOT `risk:low`, deliberately.** Half 1 alone would qualify — an error string, no behaviour\nchange. But half 2 is a **decision about a PII contract**, and the ticket says so itself: *\"settling\nwhat the PII floor admits is why it goes through the plan gate\"*. A clause a future consumer\nimplements from is not a transcription fix.\n\nThis is also the second consecutive ticket where the interesting risk was not the blast radius.\nTKT-117 was `risk:low`, touched 6 files, and produced this run's only security finding. Not\nrepeating that read here.\n\n---\n\n### ⚠️ Gate waiver — this ticket ran GATELESS (process experiment)\n\nSame waiver as TKT-116/117, recorded again so this ticket's history stands alone. The repo owner\nexplicitly waived all four human gates for this run as a process experiment:\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human moves Backlog → Ready | already given |\n| 2 — plan approval | human approves the plan | **agent self-resolves** open decisions: prefer the critique's recommendation; absent one, the most reversible option. All recorded in `kind=plan-final`. |\n| 3 — review + merge | human reviews and squash-merges | **agent merges** after the ai-review converges (same triage, second-pass, churn-cap rules); review-guide posted first as the audit record |\n| 4 — PO acceptance | PO validates the COS | **agent self-validates** against test evidence on the merged code, posts the note, closes with full metrics |\n\nUnchanged: `config.models` routing with no substitution, cross-model review for both plan and code,\nTDD with tests observed red first, `make check` green before every push and after the merge, all\nmarker comments, no AI attribution.\n\n**Model routing:** `plan` = `gpt-5.6-sol` · `planReview` = `main-agent` · `implement` = `main-agent`\n· `aiReview` = `gpt-5.6-sol`. Cross-model invariant holds on both pairs.\n\n**No CI this run:** no GitHub Actions minutes, so `make check` — which mirrors CI — is the sole gate.\nBranch protection is not enforced on this repo either (protection API → 403), so nothing configured\nis bypassed by the agent merge.\n\n**Note on half 2 and ADR-021's rule.** Amending a PII clause is exactly the kind of edit that invites\nan overclaim. The amendment must widen §D9 to admit what the code already correctly emits **without**\nweakening the prohibition (buyer, guest reference, raw operator identity) — and must not describe the\nresult as a privacy guarantee it does not deliver.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — TKT-119\n**Drafted by `gpt-5.6-sol`** (codex companion, `task --effort high`, read-only), per\n`config.models.plan`. Briefed from both halves (incl. absorbed TKT-120) and the governing ADRs\nre-resolved at claim time (ADR-025 §D2/D5/D6/D9, ADR-021 §D6 + §trust boundary, ADR-003 §D3,\nADR-017/033). Verbatim below; critique follows in `kind=plan-final`.\n\n---\n\n### Draft plan — TKT-119\n\n**Decision** — First add focused red tests for class-specific alarm-route failures, then replace `RequireAlarmRoute`’s positional route strings with a named configuration struct carrying both the durable environment-variable name and human-readable class. Update its three boot callers without changing route validation. Second, amend ADR-025 §D9 and every related restatement to describe the shipped scalar payloads and the honest trust boundary. Do not change `ObserveAlarmRoute`, alarm payloads, event schemas, migrations, or generated code.\n\n**Design**\n\n**Half 1 — alarm-route diagnostics**\n\nIntroduce an exported `AlarmRoute` configuration beside `RequireAlarmRoute` in `services/access/internal/lifecyclejob/publisher.go:59`, with named `Stream`, `Durable`, `Subject`, `DurableEnv`, and `Class` fields. Update the three struct literals at `services/access/cmd/access/main.go:197`, `services/access/cmd/access/main.go:203`, and `services/access/cmd/access/main.go:208`, using the existing environment constants at `services/access/cmd/access/lifecycle.go:29`, `:30`, and `:31`.\n\nEvery error currently hardcoded at `services/access/internal/lifecyclejob/publisher.go:61`, `:65`, `:69`, `:73`, and `:84` will use `DurableEnv` or `Class`. Reject a sixth positional string: four adjacent route strings would make valid-but-wrong calls too easy. Also reject a package-wide typed enum/map for now: three named struct literals provide the necessary association without moving deployment configuration ownership into `lifecyclejob`.\n\n**Half 2 — §D9 amendment**\n\nReplace ADR-025 §D9 at `docs/adr/ADR-025-admission-events-and-offline-reconciliation.md:193` with the literal text below. It admits the existing timestamp/boolean payload at `services/access/internal/store/reconcile.go:301` and `:302`, and the version/boolean payload at `services/access/internal/store/scan.go:313` and `:314`. It preserves ADR-025’s existing statement that device time is claimed, not attested (`docs/adr/ADR-025-admission-events-and-offline-reconciliation.md:162`, `:165`) and follows ADR-021’s database-writer boundary (`docs/adr/ADR-021-ticket-lifecycle-trail-integrity.md:153`, `:154`, `:155`).\n\nReject a silent rewrite or wording such as “contains no PII”: UUIDs and a gate timestamp remain correlatable, and repository tests constrain producer shape rather than a database adversary.\n\n**Open decisions**\n\n1. **Thread both the environment-variable name and class label.** Use `DurableEnv` for the actionable missing-setting sentence and `Class` for all stream/consumer/filter errors. Admission-conflict messages should be:\n\n   ```text\n   ACCESS_ADMISSION_CONFLICT_DURABLE is required: fail-open (ADR-021 §D6) needs somewhere durable for admission conflict alarms to land\n   admission conflict alarm stream %q: %w\n   admission conflict alarm durable %q on stream %q: %w\n   admission conflict alarm durable %q: %w\n   admission conflict alarm durable %q does not filter %q (filters: %v)\n   ```\n\n2. **`ObserveAlarmRoute` does not have the same error defect and is out of scope.** Its callback deliberately suppresses transient stream/consumer/info errors (`services/access/internal/lifecyclejob/publisher.go:108`), while its returned failures are raw metric-registration errors (`services/access/internal/lifecyclejob/publisher.go:99`). It therefore emits no wrong class or env-var instruction. Its metric description omits the policy-conflict class (`services/access/internal/lifecyclejob/publisher.go:101`); record that separately rather than expanding this boot-diagnostic ticket.\n\n3. **Use one route struct, not another positional string.** Named fields make the env/class/subject association reviewable at each caller. A typed class enum would add constants and mapping machinery but would not remove the remaining positional ambiguity unless combined with the same struct.\n\n4. **No test covers these messages today.** `git grep` found no `RequireAlarmRoute` or current-message assertion under tests or `smoke/`; existing lifecyclejob alarm coverage starts at the drainer (`services/access/internal/lifecyclejob/runner_test.go:205`). Add `publisher_test.go`; no current wrong-string assertion needs updating.\n\n5. **Use the literal §D9 replacement below.**\n\n6. **The timestamp is not non-linkable.** `device_occurred_at` is not itself a person identifier, but it can correlate an occurrence with a physical gate event, logs, CCTV, or staffing data. The clause must explicitly preserve that residual linkability rather than calling the payload anonymous.\n\n7. **Move every restatement together.** Update the inaccurate comments in `reconcile.go:295`, `scan.go:302`, `lifecycle.go:349`, `alarm_envelope_test.go:21`, `occurrence_smoke_test.go:386`, and `lifecycle_smoke_test.go:887`. Also correct the stronger documentation overclaim at `docs/development.md:267`, `:269`, and `:272`, plus scope the broad §D9 references in `alarm_envelope.go:15` and `ADR-033-shared-domain-event-envelope.md:235`. Keep assertions about exact keys/types, but rename “PII floor” language to “payload/schema constraint”; those tests cannot prove non-linkability or constrain a database writer.\n\n8. **Use an inline `*Amended (TKT-119):*` marker while replacing the clause text.** This matches the inline convention at `docs/adr/ADR-032-stripe-behind-the-psp-port.md:69`; ADR-020 likewise records amendments explicitly at `docs/adr/ADR-020-catalog-index-build-concurrency.md:9`. Do not silently rewrite history or create a new ADR.\n\n**§D9 replacement text**\n\n> 9. **PII.** *Amended (TKT-119):* Occurrence ids and alarm payloads carry bounded identifiers, enums, and operational scalars that are not themselves direct person identifiers — timestamps, counters, booleans, and version numbers — but no buyer, no guest reference, and no raw scanner-operator identity (ADR-003 §D3). This is a producer-schema constraint on honest application changes, not a privacy or non-linkability guarantee and not containment against an adversary with write access to the Access database. In particular, `device_occurred_at` is device-claimed and can be correlated with a physical gate event.\n\n**Test seam**\n\n- **Half 1:** one new unit-test seam, `services/access/internal/lifecyclejob/publisher_test.go`. Use small fakes embedding the JetStream `JetStream`, `Stream`, and `Consumer` interfaces to exercise missing durable, stream lookup, consumer lookup, info lookup, and filter mismatch without a live broker. Assertions must include each expected class-specific substring and environment variable; non-integrity cases must also reject `\"integrity\"`.\n- **Half 2:** no new behavioral test. Existing byte-exact goldens cover policy and admission-conflict payloads (`services/access/internal/store/alarm_envelope_test.go:36`, `:62`), and the persisted admission-conflict contract is already pinned at `services/access/internal/store/occurrence_smoke_test.go:415`, `:536`. Only their inaccurate explanatory wording changes.\n\n**Test plan**\n\n1. Add the table-driven `RequireAlarmRoute` tests against the proposed route API; confirm they fail against the hardcoded messages.\n2. Implement `AlarmRoute`, update all three callers, and run `go test ./internal/lifecyclejob` from `services/access`.\n3. Amend §D9 and all restatements; do not update any payload golden literal.\n4. Run the access lifecyclejob/store tests.\n5. Run the repository gate: `make check` (`Makefile:17`).\n\n**DoD**\n\n1. **COS 1:** tests prove each missing durable names its own environment variable and class; every non-integrity failure path rejects the substring `integrity`.\n2. **COS 2:** the same stream, durable, and subject reach the same validation branches; missing, unavailable, unreadable, and misfiltered routes still return errors.\n3. **COS 3:** §D9 admits identifiers, enums, timestamps, booleans, counters, and versions; retains the buyer/guest/raw-operator prohibition; and explicitly limits the claim to honest producer-schema changes.\n\n**Files**\n\nModified:\n\n- `services/access/internal/lifecyclejob/publisher.go:59`\n- `services/access/cmd/access/main.go:197`\n- `docs/adr/ADR-025-admission-events-and-offline-reconciliation.md:193`\n- `docs/adr/ADR-033-shared-domain-event-envelope.md:235`\n- `docs/development.md:267`\n- `services/access/internal/store/reconcile.go:295`\n- `services/access/internal/store/scan.go:302`\n- `services/access/internal/store/lifecycle.go:349`\n- `services/access/internal/store/alarm_envelope.go:15`\n- `services/access/internal/store/alarm_envelope_test.go:21`\n- `services/access/internal/store/occurrence_smoke_test.go:386`\n- `services/access/internal/store/lifecycle_smoke_test.go:887`\n\nNew:\n\n- `services/access/internal/lifecyclejob/publisher_test.go`\n\n**Risks / Cons**\n\n- Exact error assertions are intentionally brittle because these strings are the operator remediation contract.\n- The struct still contains several strings; named fields reduce swaps but cannot prevent a semantically wrong literal.\n- “Operational scalar” must not become permission for arbitrary free text or nested data.\n- The timestamp remains linkable despite excluding direct person identifiers.\n- `ObserveAlarmRoute`’s stale two-class metric description remains as a separately identified cleanup.\n\n**Migrations / spec / schema changes**\n\nADR/spec change: ADR-025 §D9 wording only. Database migrations: none. Event payload, canonical form, subject, schema number, and wire bytes: none. ADR-017 draws the version line at consumer semantics (`docs/adr/ADR-017-domain-event-schema-evolution.md:60`), while ADR-033 requires byte-identical wire output (`docs/adr/ADR-033-shared-domain-event-envelope.md:49`); neither trigger fires.\n\n**risk:low?**\n\nAgree with leaving `risk:low` off. Runtime implementation risk is small, but this ticket changes operator-facing fail-closed diagnostics and a privacy-contract decision that is restated across code, tests, and operational documentation. Missing one restatement would recreate the contradiction, and overclaiming the PII boundary would be materially misleading.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan-final — TKT-119 (delta on the `kind=plan` draft)\n\n**Critique by `main-agent`** (`config.models.planReview`; drafter `gpt-5.6-sol` — cross-model\ninvariant holds). **The `kind=plan` draft as amended below is the plan.**\n\nGate 2 waived (see `kind=claim`); every open decision resolved by me under the waiver's rule.\n\n---\n\n#### Seam verification (`git grep -n` at `8cbcd0c`)\n\nEvery file, symbol, line and convention the draft names **exists and checks out**. Verified\nindividually rather than sampled, because this draft's file list is unusually long and a wrong entry\nwould mean editing something that does not say what the plan claims:\n\n`RequireAlarmRoute` (`publisher.go:59`) and its five hardcoded messages (:61/:65/:69/:73/:84) ·\nthe three callers (`main.go:197/203/208`) · the env constants (`cmd/access/lifecycle.go:29-31`,\nexactly as named) · `ObserveAlarmRoute` (:99) with its error-suppressing callback (:108) ·\n`docs/development.md:264-274` · `alarm_envelope.go:15` · `ADR-033:235` ·\n`lifecycle_smoke_test.go:887` · `occurrence_smoke_test.go:386` · `alarm_envelope_test.go:21` ·\nand the inline amendment convention at **`ADR-032:69`** (`*Amended (TKT-114/S2):*` mid-clause),\nwhich is the precedent the draft cites for §D8 — confirmed, so the house style is right.\n\n**Zero `RequireAlarmRoute` test coverage confirmed** — `git grep -l RequireAlarmRoute -- '*_test.go'`\nreturns nothing. The draft's answer to Q4 is correct: nothing asserts the current wrong strings, so\nno existing test has to change.\n\n---\n\n#### Decisions\n\n**D1 — Route struct over a sixth positional string. ACCEPTED as drafted.**\n`RequireAlarmRoute(ctx, js, stream, durable, subject string)` already has three adjacent strings; a\nfourth is a swap waiting to happen, and the compiler cannot catch it. Named fields make the\nenv/class/subject association reviewable at each call site. The draft's rejection of a\npackage-wide typed enum is also right for the stated reason — deployment configuration ownership\nstays in `cmd/access`, not `lifecyclejob`.\n\n**D2 — The five message texts. ACCEPTED as drafted** (Q1). `DurableEnv` in the actionable\n\"is required\" sentence, `Class` in the four broker-lookup errors. That split is correct: the first\nmessage's job is *name the variable to set*, the rest is *say which class failed*.\n\n**D3 — AMEND: `ObserveAlarmRoute`'s stale metric description IS in scope.**\nThe draft found that `publisher.go:101` describes the gauge as covering *\"(integrity and\nadmission-conflict classes)\"* — omitting policy-conflict, which has been registered since TKT-87\n(`main.go:232`) — and recommended recording it as a separate cleanup. **Rejected.** This ticket's\nsubject *is* \"make the access alarm surface say the true thing\"; a metric description that omits a\nclass it measures is the same untruth, on the same surface, in a file already being edited three\nlines away. Filing a ticket to change three words costs strictly more process than the change. The\ndraft was right that `ObserveAlarmRoute`'s **error paths** are out of scope (it suppresses transient\nlookup errors deliberately, `:108`, and returns only raw metric-registration errors) — that part\nstands. Only the description is added.\n\n**D4 — §D9 replacement text: ACCEPTED with one addition.**\nThe draft's wording is right on the hard part — it states the constraint *and* its limits, per\nADR-021's rule about not describing a control as a guarantee it is not. It also correctly refuses\n\"contains no PII\".\n\n**Added clause, closing a hole the draft itself raised in its Cons** (*\"'operational scalar' must not\nbecome permission for arbitrary free text or nested data\"*): a reader implementing from the amended\n§D9 could read \"operational scalars\" as admitting a `notes string`. A Con named in the plan and left\nun-patched is the failure mode `quality-practices.md` §1 exists to catch, so it is closed in the\nclause rather than left to review. Final text:\n\n> 9. **PII.** *Amended (TKT-119):* Occurrence ids and alarm payloads carry bounded identifiers,\n> enums drawn from fixed vocabularies, and operational scalars that are not themselves direct person\n> identifiers — timestamps, counters, booleans and version numbers — but **no free text, no nested\n> objects**, no buyer, no guest reference and no raw scanner-operator identity (ADR-003 §D3). This is\n> a producer-schema constraint on honest application changes, **not** a privacy or non-linkability\n> guarantee, and **not** containment against an adversary with write access to the Access database.\n> In particular `device_occurred_at` is device-*claimed* and correlates with a physical gate event.\n\n**D5 — AMEND the framing: §D9 never described *any* of the three payloads, not just the two newer\nones.** The ticket (and the draft's Design section) present this as the clause failing to cover\nadmission-conflict and policy-conflict. It is worse and simpler than that: `alarmData`, the\n**integrity** payload — the original class, the one §D9 was presumably written for — carries\n`OccurredAt time.Time` (`lifecycle.go:351-358`). So all three shipped payloads have always\ncontradicted the clause.\n\nThis is not pedantry, it changes the justification: the amendment is not \"widen a clause for two\nclasses that outgrew it\" but \"the clause was never accurate and three payloads have been shipping\nagainst it\". The draft does list `lifecycle.go:349` among the comments to fix, so it caught the\nsymptom without drawing the conclusion. The amendment note and the closeout should say the honest\nversion.\n\n**D6 — REJECT two of the twelve file edits.** The draft's Q7 answer sweeps up\n`services/access/internal/store/alarm_envelope.go:15` and\n`docs/adr/ADR-033-shared-domain-event-envelope.md:235`. Both were read: neither **restates** the\nrule. They *cite* it — *\"ADR-025 §D9 constrains those payloads\"* and *\"§D9 constrains the alarm\npayloads whose envelopes this ticket rewraps\"*. A pointer to §D9 stays correct after §D9 is amended;\nediting it is churn that enlarges the diff without removing any untruth. Dropped from the file list.\n\nThe other restatements stay in, because each asserts the wrong thing in its own words:\n`reconcile.go:295`, `scan.go:302`, `lifecycle.go:349`, `alarm_envelope_test.go:21`,\n`occurrence_smoke_test.go:386`, `lifecycle_smoke_test.go:887`, `docs/development.md:267-272`.\n\n**D7 — `docs/development.md`'s overclaim is the sharpest of them, and it is *not* the same error.**\nWorth separating, because fixing it wrongly would be easy. Its *description of the payload* is\nalready accurate — *\"four bounded identifiers, one device-claimed timestamp and one boolean\"* — so\nunlike §D9 it never mis-described the data. Its defect is the next sentence: *\"and no PII (ADR-003\n§D3). That floor is enforced, not just asserted\"*. What the cited test actually enforces is the key\nset and the scalar types. That is a **producer-schema** constraint; it cannot prove absence of PII\nand cannot constrain a database writer. Fix the claim, keep the (correct) description.\n\n**D8 — Inline `*Amended (TKT-119):*`, matching `ADR-032:69`. ACCEPTED** (Q8). Verified precedent.\n\n**D9 — `risk:low` stays off. ACCEPTED**, and the draft's reasoning is better than the label\nquestion deserves: the runtime risk is trivial, but the diff spans a privacy-contract decision\nrestated in seven places, and *missing one recreates the contradiction this ticket exists to remove*.\n\n---\n\n#### Introduced at plan-review, reviewed by no second model\nFour, all mine — named for the ai-review to attack:\n1. **Including `ObserveAlarmRoute`'s metric description (D3)** — an addition to the diff.\n2. **The \"no free text, no nested objects\" clause in §D9 (D4)** — I wrote this sentence; nobody else\n   has reviewed it, and it is the one part of the amendment that *narrows* rather than widens.\n3. **Dropping `alarm_envelope.go` and `ADR-033` from the file list (D6)** — a subtraction; if I am\n   wrong, an inconsistency survives in a place the draft wanted fixed.\n4. **The all-three-payloads framing (D5).**\n\n#### Reasoning lens — pre-mortem (applied; what it changed)\n*Assume this bounced at Gate 3. Why?*\n- *\"You amended a PII clause into something a reader could stuff free text into.\"* → **D4**'s added\n  bound. This was the draft's own Con, unpatched.\n- *\"You fixed §D9 and left three code comments saying the old thing.\"* → the restatement sweep, kept\n  in full (D6 removes only non-restatements).\n- *\"You changed a privacy claim in dev docs to match the ADR and broke a claim that was already\n  right.\"* → **D7**: the payload description there is accurate; only the enforcement claim is not.\n- *\"Your test asserts a string both versions produce.\"* → the fixture rule below.\n\n#### Test seams (final)\n\n| Half | Seam | Test | The input shape that lets it fail |\n|---|---|---|---|\n| 1 | **NEW** `services/access/internal/lifecyclejob/publisher_test.go` (no existing seam — verified) | `TestRequireAlarmRouteNamesItsOwnClass`, table-driven over the three classes × five failure branches | **Each case must assert the class-specific substring AND that non-integrity cases do NOT contain `\"integrity\"`.** Asserting only \"an error was returned\", or a substring both versions emit, cannot fail. The missing-durable branch (`:61`) returns before touching JetStream, so it needs **no broker at all**; the other four need small fakes embedding the `jetstream.JetStream`/`Stream`/`Consumer` interfaces. |\n| 2 | **none needed** | — | An ADR sentence and six comments. Behaviour is unchanged and the payload goldens (`alarm_envelope_test.go:36,62`) already pin the wire bytes — they must stay **byte-identical**, which is itself the check that half 2 changed no behaviour. |\n\n#### Test plan (red first)\n1. `publisher_test.go` against the new `AlarmRoute` API → red (API absent, then wrong strings).\n2. Implement `AlarmRoute` + the five messages + the three callers; add the `ObserveAlarmRoute`\n   description fix (D3).\n3. Amend §D9 (D4 text) and the six restatements (D6 list); fix `docs/development.md`'s enforcement\n   claim (D7). Touch no golden literal.\n4. `make check` — sole gate (no Actions minutes; branch protection off).\n\n#### DoD → COS\n- [ ] **COS 1** — every class's five failure paths name its own env var and class; no non-integrity\n      message contains `\"integrity\"`, asserted.\n- [ ] **COS 2** — no behaviour change: same branches, same fail-closed outcomes; alarm payload\n      goldens byte-identical.\n- [ ] **COS 3** — §D9 admits what all three shipped payloads carry, keeps the\n      buyer/guest/raw-operator prohibition, bounds \"operational scalar\" against free text and nested\n      objects, and states what it is not. All seven restatements moved together.\n- [ ] `make check` green.\n\n#### Files (draft's list, −2 per D6, +0 new beyond the test)\nModified: `services/access/internal/lifecyclejob/publisher.go` · `services/access/cmd/access/main.go` ·\n`docs/adr/ADR-025-…md` · `docs/development.md` · `services/access/internal/store/reconcile.go` ·\n`…/scan.go` · `…/lifecycle.go` · `…/alarm_envelope_test.go` · `…/occurrence_smoke_test.go` ·\n`…/lifecycle_smoke_test.go`. **New:** `services/access/internal/lifecyclejob/publisher_test.go`.\n**Migrations: none. Payload/schema/wire: none — ADR-017's and ADR-033's triggers both stay unfired.**\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Coding — TKT-119\n\n**Implemented by `main-agent`** per `config.models.implement`. PR\n[#131](https://github.com/mathd/ticketing_system/pull/131) · branch `TKT-119-alarm-surface-truth` ·\none commit (both halves are the same sentence-level defect on one surface; splitting them would\nseparate the ADR clause from the comments that restate it).\n\n```yaml\nstage: coding\nimplementer: main-agent\ncommit: f4690c7\ngate: make check — EXIT=0, log body clean\nci: none (no Actions minutes); local gate is sole gate, branch protection off (403)\ngenerated_drift: none\nmigrations: none\nschema/wire changes: none — ADR-017 and ADR-033 triggers both unfired\n```\n\n#### TDD\n`RequireAlarmRoute` had **zero** test coverage (verified: `git grep -l RequireAlarmRoute -- '*_test.go'`\nreturned nothing), so the seam is new.\n\n| Test | Red evidence |\n|---|---|\n| `TestRequireAlarmRouteNamesItsOwnClass` (3 subtests) | `undefined: AlarmRoute` ×4 — build failure before the type existed |\n\nThe missing-durable branch returns before touching JetStream, so all three classes are exercised\nwith a **nil broker** — no fakes, no live NATS, and it is exactly the message the ticket is about.\n\n**The assertion that makes it able to fail** is the negative one: for the two non-integrity classes,\n`err` must NOT contain `\"integrity\"`. Without it, every message the old code produced would still\nsatisfy a test that only checked \"an error mentioning a durable was returned\". Each case also\nasserts the other two classes' variables are absent, so a message naming *all three* would fail too.\n\n#### Half 2 needs no test, and that is a claim not an omission\nBehaviour is unchanged and the payload goldens (`alarm_envelope_test.go`) stay **byte-identical** —\nthat is itself the check that half 2 changed no wire output. Inventing a test that asserts the text\nof an ADR sentence would pin prose, not behaviour.\n\n#### Deviations from `kind=plan-final`\nTwo, both foreseen by the plan's own decisions, plus one found during implementation:\n- **D3 applied**: `ObserveAlarmRoute`'s stale metric description was fixed here rather than deferred.\n- **D6 applied**: `alarm_envelope.go` and `ADR-033` dropped — they cite §D9 rather than restating it.\n- **Found while editing**: two more stale statements in the same file — the `RequireAlarmRoute` doc\n  comment said it proves *\"an integrity alarm\"* has somewhere to land, and a comment inside\n  `ObserveAlarmRoute` said *\"two alarm classes\"*. Same defect class, same file, three lines away;\n  fixed. `git grep \"identifiers and enums\"` now returns only the amendment quoting the old wording\n  to explain why it changed, and `git grep \"and enums only\"` the same — the sweep is complete.\n\n#### Process note\n`make generate` was first run from `services/access` and exited **2** — the mis-cwd shape\n`AGENTS.md` warns about (TKT-71). Caught by reading the exit code rather than assuming; re-run from\nthe repo root, exit 0, no drift. Worth recording that the guardrail earned its place again.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### AI review — TKT-119\n\n**Reviewed by `gpt-5.6-sol`** (codex `adversarial-review`, primed for guilt, diff only), per\n`config.models.aiReview`. Cross-model invariant holds.\n\n**Three passes, every finding on the same ADR paragraph, every one a different error the previous\nfix exposed.** Half 1 (the code) was approved in pass 1 and never came back. All three findings were\nabout half 2 — a decision record — which is the half that had no test to catch it.\n\n---\n\n#### Pass 1 (vs `origin/main`) — `needs-attention`, 1 × [medium]\n\n> *D9 prohibits free text that the shipped integrity alarm still emits.* `alarmData.reason` is an\n> unconstrained string from `cause.Error()`/`chainErr.Error()`; the golden fixture carries\n> `\"reason\":\"chain head mismatch\"`.\n\n**CONFIRMED — and I had found the same defect independently, minutes earlier**, while checking the\nreview's own question list. Two paths to one finding is the cross-model check working rather than\nduplicating.\n\nIt was **my** error: the \"no free text\" bound was added at plan-review (named there as introduced\nby me with no second model on it) to stop \"operational scalars\" licensing arbitrary data. It made\n§D9 wrong in the *other* direction — newly rejecting the payload it was written to describe. The\nreviewer added two things I had missed: the golden literal, and that my rationale named the\ntimestamp as the only historical contradiction.\n\n**Fixed** by admitting a service-produced diagnostic reason string on the integrity class,\nprohibiting what actually carries risk (device-/user-supplied free text, nested objects), and saying\nplainly that `reason` is the one unbounded field and its constraint is a **producer discipline**,\nnot an enforced property.\n\nThe reviewer offered a second option — replace `reason` with a fixed vocabulary. **Deferred to\nTKT-147**, not skipped: it is a payload change firing ADR-017 §3 and ADR-033, and routing a wire\ndecision through a ticket whose COS says *\"no behaviour change\"* would be the wrong gate. The\nreviewer explicitly agreed the deferral does not block (\"no present PII path was found that\nindependently blocks this PR\").\n\n#### Pass 2 (vs the pass-1 fix) — `needs-attention`, 1 × [medium]\n\n> *The rationale miscounts the contradictions and conceals a real relaxation.*\n\n**CONFIRMED, and the second half is the serious one.** I wrote *\"keeps every prohibition\"*. That is\nfalse: the original said identifiers and enums **only**, and that word prohibited exactly the\noperational scalars and free-text `reason` the amendment admits. Recording a **relaxation** as a\ncorrection is how a PII contract quietly widens — a future reviewer would read the carve-out as\npre-existing policy rather than an accepted risk. The count was wrong too (three → six).\n\n**Fixed** by replacing the narrative with a field-level delta: Preserved / Relaxed / Added.\n\n#### Pass 3 (vs the pass-2 fix) — `needs-attention`, 1 × [medium]\n\n> *Existing prohibitions are incorrectly classified as newly added.*\n\n**CONFIRMED, and subtle.** \"Identifiers and enums **only**\" already excluded free text and nested\nobjects *by construction* — anything that is neither an identifier nor an enum was out. Listing them\nunder **Added** asserted the original permitted them. They are the surviving boundary of the\noriginal rule, retained in full but for the single `reason` carve-out.\n\nSame pass confirmed everything else: the six-field count is right (**23 fields: 13 identifiers,\n4 enums, 6 other**, enumerated), \"Preserved\" reproduces `origin/main`'s three identity exclusions\nword-for-word, \"Relaxed\" completely identifies the widening, and the code comments agree.\n\n**Fixed** by renaming to *\"Preserved and made explicit\"* with the entailment spelled out.\n\n---\n\n#### No fourth pass, and the reason is not the cap\nPass 3's recommendation was a **verbatim instruction** — rename this category, state that \"only\"\nalready entailed them. I applied it with no invention, so there is no new design content for a\nreviewer to attack. That is the one case where another pass adds nothing. (The cap was not reached:\nthree passes, and the churn rule would have permitted a fourth.)\n\n**Is three passes on one paragraph churn?** No — and the test is whether findings repeat or descend.\nThese descend: *the clause contradicts the code* → *the rationale misdescribes the clause* → *one\ncategory label in the rationale misclassifies*. No finding was re-litigated, and each was only\nvisible once the previous was fixed. Half 1 was approved in pass 1 and stayed approved.\n\n#### A run that did not count\nThe third pass was first launched with a mistyped base SHA and died at\n`git merge-base HEAD 6fee44a: Not a valid object name` — it never reached the model. Per\n`codex-runner.md` that is a **rung failure, not a review**, so it was relaunched against `d603217`\nrather than counted. Recorded because a run that exits without reviewing is exactly the shape that\ngets mistaken for a clean pass.\n\n#### Gate\n`make check` green after every fix — **four runs, all EXIT=0**, log bodies clean, against the\ncommitted tree. No CI (no Actions minutes); local gate is the sole gate. Branch protection not\nenforced (403).\n\n#### Triage ledger\n- **Blocking → fixed in PR:** the free-text contradiction, the miscount + concealed relaxation, the\n  Added misclassification.\n- **Incidental → Backlog:** **TKT-147** (replace `reason` with a fixed reason-code vocabulary —\n  a payload change, ADR-017 §3 / ADR-033).\n- **Confirmed correct by review, no action:** all three `AlarmRoute` call sites pair the right\n  durable env with the right subject constant — the exact swap the struct refactor existed to\n  prevent.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### PO validation — TKT-119 (self-validated under the Gate 4 waiver)\n\nValidated against evidence on **merged `main` @ `961726b`**.\n\n#### COS 1 — each class's boot failure names its own env var and class\n\n**Met.** On merged `main`:\n\n```\n--- PASS: TestRequireAlarmRouteNamesItsOwnClass/integrity_missing_durable\n--- PASS: TestRequireAlarmRouteNamesItsOwnClass/admission_conflict_missing_durable\n--- PASS: TestRequireAlarmRouteNamesItsOwnClass/policy_conflict_missing_durable\n```\n\nThe assertion that carries the weight is the **negative** one: each non-integrity case requires the\nmessage *not* to contain `\"integrity\"`, and requires the other two classes' variables to be absent.\nWithout it, every message the old code produced would still pass a test that merely checked \"an\nerror mentioning a durable came back\". `RequireAlarmRoute` had **zero** coverage before this.\n\nThe ai-review independently verified the part no test can: all three call sites pair the right\n`DurableEnv` with the right `Durable` and `Subject`. That was the swap the struct existed to\nprevent, and it is the one thing a green test could not have proven.\n\n#### COS 2 — no behaviour change\n\n**Met.** Same branches, same order, same fail-closed outcomes; the alarm payload goldens are\n**byte-identical**, which is itself the proof that half 2 changed no wire output. ADR-017's and\nADR-033's schema triggers both stay unfired.\n\n#### COS 3 — §D9 admits what ships, keeps the prohibitions, does not overclaim\n\n**Met, after three corrections.** Final state:\n- **Admits** all six non-identifier/non-enum fields across the three classes — independently\n  enumerated by the reviewer (23 fields: 13 identifiers, 4 enums, 6 other).\n- **Preserves** the three identity exclusions, confirmed word-for-word against `origin/main`.\n- **Bounds** the widening: no device- or user-supplied free text, no nested objects.\n- **States what it is not** — a producer-schema constraint, not a privacy or non-linkability\n  guarantee, and no containment against a database writer (ADR-021 §The trust boundary).\n- **Names the residual risk**: `reason` is the one unbounded field; its constraint is a producer\n  *discipline*, not an enforced property.\n\nRestatement sweep verified on merged `main`: `git grep \"identifiers and enums\"` returns **4 hits,\nall of them the amendment quoting the old wording to explain why it changed**. No live restatement\nof the false claim survives.\n\n---\n\n### What a human should look at anyway\n\n1. **§D9 is now a recorded relaxation of a PII contract.** That is the honest description and it\n   took three passes to arrive at, but the substance is: operational scalars and one free-text\n   `reason` are now *permitted* where the old clause said \"only identifiers and enums\". Nothing\n   about what ships changed — the clause was always false — but the *policy* is now written wider,\n   deliberately. If that is not the intended policy, the fix is a payload change (TKT-147), not\n   different wording.\n2. **`reason` remains unbounded.** ADR-025 §D9 says a diagnostic reason must be built from this\n   service's own errors and never from scanner, device or buyer input. **Nothing enforces that.**\n   The review confirmed no present PII path, but it is a discipline, and TKT-147 exists to make it\n   enforceable.\n3. **A struct refactor on a boot path.** No test can prove a call site pairs the right constants;\n   review did. Worth a second pair of eyes on `main.go:197/206/214` precisely because it is the kind\n   of thing that looks obviously right.\n\n**Deferred:** TKT-147.\n**Preview:** no deploy environment; `make up` is the only surface.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-119\n\nPR [#131](https://github.com/mathd/ticketing_system/pull/131) squash-merged as `961726b`, branch\ndeleted. Content verified **on `main` by grep** (`AlarmRoute` ×8, the amended §D9 category present),\nnot by the merge badge. `HEAD == origin/<branch>` asserted before merging. Post-merge `make check`:\n**EXIT=0**.\n\n#### Durations\nFrom the `sdlc-state` commit log. All `-0700`.\n\n| stage | duration |\n|---|---|\n| Ready (queued) | → 2026-07-30 13:32 |\n| **Planning** | 13:32:03 → 13:43:05 · **11m 02s** |\n| ↳ `agent:planning` (draft, `gpt-5.6-sol`) | 13:32:03 → 13:41:51 · **9m 48s** |\n| ↳ `agent:plan-review` (critique, `main-agent`) | 13:41:52 → 13:43:04 · **1m 12s** |\n| **Building** | 13:43:05 → 14:20:40 · **37m 35s** |\n| ↳ `agent:coding` (`main-agent`) | 13:43:05 → 13:49:50 · **6m 45s** |\n| ↳ `agent:ai-review` (**3 passes**, `gpt-5.6-sol`) | 13:49:51 → 14:20:40 · **30m 49s** |\n| PO Review → Done | self-validated, **no wait** |\n| **Ready → Done (agent wall-clock)** | **~50m** |\n\n**human-wait total: 0.**\n\n**`agent:ai-review` was 4.5× `agent:coding`** (30m49s vs 6m45s) — an even sharper ratio than\nTKT-117's. The implementation was small and right; the *decision record* took three passes. Third\nconsecutive ticket where review, not coding, was the bulk of the work.\n\n#### Diff\n`+200/−31 (11 files)` merged, plus `+27` docs in `04e5f24`. ~40 lines of production change; the rest\nis one ADR clause, its six restatements, and the new `publisher_test.go`. No migrations, no\npayload/wire change — ADR-017 and ADR-033 triggers both unfired, alarm goldens byte-identical.\n\n#### Gate / CI\n`make check` green **five times** (post-coding, after each of three review fixes, post-merge), all\n`EXIT=0`, log bodies clean, against the committed tree. **No CI** — no Actions minutes; local gate\nis the sole gate. Branch protection not enforced (403).\n\n---\n\n### learnings:\n\nOne promoted, **technical/process**, repo-local house, committed directly in `04e5f24`:\n\n**[Record a relaxation as a relaxation](../docs/learnings/2026-07-30-record-a-relaxation-as-a-relaxation.md)**\n— a clause that widens a constraint must say it widens it, as a delta (preserved / relaxed /\npreserved-and-made-explicit); a relaxation recorded as a correction lets the next reviewer treat the\ncarve-out as pre-existing policy. Two corollaries: **every finding landed on the half with no test**\n(the code half was approved in pass 1 and never returned), so governing prose deserves *more*\nadversarial attention than tested code, not less; and when sweeping restatements, a **citation is\nnot a restatement** — pointers stay correct after an amendment and editing them is churn.\n\n**No skill edit.** Readable from `docs/learnings/` when needed.\n\n**Incidental → Backlog:** **TKT-147** (replace `alarmData.reason` with a fixed reason-code\nvocabulary — a payload change firing ADR-017 §3 / ADR-033).\n\n---\n\n### retro:\n\n**(1) What would have made this ticket faster or better?**\n\n- **The `plan-final` \"introduced at plan-review, reviewed by no second model\" section paid for\n  itself, twice.** Both blocking findings in this run's last two tickets were items I added at\n  plan-review — by construction the only design content no second model sees. Here I listed the\n  \"no free text\" bound as item 2 of four, went to check it against the payloads while the review ran,\n  and found the contradiction independently *before* the reviewer reported it. I had treated that\n  section as bookkeeping in earlier tickets. It is the highest-yield paragraph in the plan.\n  **Concrete change:** `SKILL.md` requires the section but does not say what to *do* with it. Add:\n  *verify each listed item against the code yourself before the ai-review returns — it is the only\n  design content with no second reader.*\n- **Three passes on one paragraph is a signal about where tests are, not about the paragraph.** Half\n  1 (code, tested) was approved in pass 1. Half 2 (an ADR clause, untestable) took three. Bundling a\n  tested half with an untestable one made the ticket look half-reviewed at every checkpoint.\n  **Concrete change:** none to the skill — but worth the humans knowing that \"small isolated fix +\n  decision record\" is a bundle whose review cost is dominated entirely by the second half.\n- **The ticket's own framing was wrong and the plan inherited it.** It said the two *newer* alarm\n  classes violated §D9. The integrity class — the original one — violated it too, and had since it\n  shipped. Neither the drafter nor I caught that until I enumerated the payload structs during\n  critique. Reading the *third* instance when a ticket names two is cheap and would have reframed\n  the amendment from the start.\n\n**(2) What should the humans change?**\n\n- **`risk:low` was correctly withheld here, and the reasoning is worth codifying.** The rule I\n  applied: *does this ticket change something a future implementer will implement FROM?* An ADR\n  clause, an error string an operator acts on, a schema — those are not low-risk regardless of line\n  count. `SKILL.md`'s `risk:low` invariant lists file-count and subsystem triggers only.\n- **Same Gate 4 recommendation as TKT-116 and TKT-117, now on its third instance.** This ticket\n  shipped a *deliberate widening of a PII contract*. It is correct, it is recorded honestly, and it\n  is exactly the class of decision a PO should see rather than read about afterwards. Three tickets,\n  three cases where the agent made a defensible call that a human would reasonably want to weigh.\n  If any single retro item from this run is worth acting on, it is this one.\n- **A mistyped base SHA produced a review that never ran** (`git merge-base HEAD 6fee44a: Not a\n  valid object name`) and exited in a way that could be mistaken for a clean pass. Caught by reading\n  the output rather than the exit status, per `codex-runner.md`. Worth noting the guardrail earned\n  its place again — this is the third distinct \"exit code lies\" shape this run has hit.\n\n**Interactive follow-up:** the one appliable item is the `SKILL.md` addition on verifying\nplan-review additions. Say the word and I will patch it alongside TKT-116's and TKT-117's; otherwise\nall five go to the weekly registry review.\n"
    }
  ],
  "links": [
    {
      "type": "absorbs",
      "key": "TKT-120"
    }
  ]
}
