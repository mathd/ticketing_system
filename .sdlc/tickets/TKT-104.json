{
  "key": "TKT-104",
  "type": "Story",
  "status": "Planning",
  "labels": [
    "agent:planning"
  ],
  "assignee": "agent (main-agent / Claude Opus 4.8)",
  "pr": null,
  "summary": "US-021: Edit a published map safely — pinned seats never orphaned or duplicated",
  "description": "### US-021: Edit a published map safely — pinned seats never orphaned or duplicated\n**As** an organizer, **I want** to edit a published seat map (rename/add/remove sections, rows, seats) and get a new version, **with a guarantee that no seat already sold or held is orphaned or duplicated**. **Priority:** P2 **Depends on:** TKT-103\n\nThis is the epic's headline COS: *maps are versioned; edits never orphan or duplicate sold/held seats*. Per the decomposition decision, TKT-3 proves this **at the catalog layer via a seat-identity pinning contract** — a published seat identity is immutable across versions, and the editing path is defined so a version bump preserves every pinned seat. The interplay with live **held** seats (per-seat inventory holds) is exercised in TKT-80, which lists that AC; this story defines and proves the **pinning contract** TKT-80 consumes, not the hold itself. Backend correctness + tests here; the editing UI is TKT-105.\n\n**Acceptance Criteria:**\n- [ ] Editing a published map produces a **new version**; the previous published version stays immutable. The new version is created under the ADR-018 lock/emit discipline where a transition is involved.\n- [ ] The **pinning contract** is defined and documented (an ADR): a seat identity that any sale/hold references is stable across versions — it cannot be silently renumbered, merged, or dropped. An edit that would orphan such a seat is **rejected** (or expressed as an explicit, recorded remap), never silently applied.\n- [ ] A test proves the invariant against the sold case: given a seat referenced by a confirmed/sold record, no edit sequence orphans it (its identity still resolves) and none duplicates it (it maps to exactly one seat in the new version).\n- [ ] A concurrency test mirrors the sold-inventory race: concurrent edit vs. a sale/hold-reference against the same seat set never lands the map in a state where a pinned seat is lost or doubled (the check-then-act race is closed by the ADR-018 lock).\n- [ ] The pinning contract is written so TKT-80's held-seat AC can consume it without a catalog change (the contract, not the hold, is the deliverable).\n\n**Quality gates (every story):**\n- `make check` green (lint + unit + integration + smoke; mirrors CI)\n- Contract tests pass for any touched service boundary (ADR-009 drift check)\n- New/changed public read endpoints declare an ADR-004 TTL tier with rationale\n- New migrations run out-of-band via the service `migrate` subcommand (ADR-022) and build within the 30s startup budget with plain `CREATE INDEX` (ADR-020)\n- Every owned entity is `organizer_id`-scoped (ADR-002)\n\nParent epic: TKT-3. Full lineage: docs/product/prd-v1.md §Capability map (TKT-3).",
  "context": {
    "binding": [
      {
        "id": "ADR-018",
        "rule": "State-deriving catalog transitions decide under a row lock (`SELECT ... FOR UPDATE`) and emit the domain event only after the store commits, marking the outbox column on success. The lock closes the check-then-act race against concurrent transitions.",
        "date": "2026-07-15"
      },
      {
        "id": "ADR-021",
        "rule": "State inside the database cannot constrain an adversary who writes to the database. Say which adversary you mean before writing 'tamper-evident'. (Relevant here as a discipline: 'safe against sold inventory' is a concurrency/consistency guarantee under honest writers, NOT a tamper claim — do not overclaim.)",
        "date": "2026-07-15"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-018",
        "why": "The new-version transition decides under a row lock and emits after commit; the lock is what closes the edit-vs-sale race that would orphan a seat."
      },
      {
        "id": "ADR-026",
        "why": "Precedent for 'edit vs live demand': capacity cuts clamp to max(new, confirmed+held), forward-only, never force-release. A seat-map edit against sold seats follows the same never-strand-a-confirmed-claim spirit."
      },
      {
        "id": "ADR-005",
        "why": "Seat identity is a slot attribute the claim core references; versioning must keep the identity the claim pinned resolvable."
      },
      {
        "id": "ADR-021",
        "why": "Discipline check: name the adversary. This is honest-writer consistency, not tamper-evidence — do not overclaim in the ADR."
      }
    ],
    "reference": [
      {
        "summary": "PRD v1 §Capability map — TKT-3 row is the source of truth for scope",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "TKT-3 epic — COS: authoring UI, versioning safe against sold inventory, GA+seated coexist",
        "link": ".sdlc board TKT-3"
      },
      {
        "summary": "ADR-018 row-lock-decide-emit-after-commit — the concurrency precedent this story instantiates for map edits",
        "link": "docs/adr/ADR-018-catalog-slot-transition-concurrency.md"
      },
      {
        "summary": "TKT-80 held-seat AC — the consumer of the pinning contract this story defines",
        "link": ".sdlc board TKT-80"
      }
    ],
    "capturedAt": "2026-07-20"
  },
  "links": [
    {
      "type": "blocks",
      "key": "TKT-105"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ DoR shaped — TKT-104 — all 8 items met/deferred (Gate 1 unblocked)\n\nShaped from the code-seam survey + the TKT-3 decomposition + owner decisions (2026-07-20: back-office = Astro+React; versioning proof is catalog-side via a pinning contract). **No `open owner:human` items remain** — the board hard-block is cleared.\n\n| DoR item | State |\n|---|---|\n| `objective` | met |\n| `scope` | met |\n| `uncertainties` | deferred |\n| `dependencies` | met |\n| `approach` | met |\n| `first_slice` | met |\n| `success` | met |\n| `context_memo` | met |\n\n**Deferred (safely resolved in Planning, no major redesign):**\n- `uncertainties` — Whether an orphaning edit is hard-rejected vs expressed as an explicit recorded remap — a design choice for the ADR/plan, both satisfy the COS. Not a product decision.\n\n**Ready to prioritize (Gate 1).**"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### 🤖 Claimed — TKT-104 → Planning (agent:planning)\n\n**⚠️ GATELESS RUN — all four human gates waived by explicit owner directive (process experiment).** The repo owner is deliberately running this ticket Ready→Done in one continuous autonomous pass, waiving Gate 1 (already given — ticket was in Ready), Gate 2 (plan approval), Gate 3 (review+merge), and Gate 4 (PO acceptance). Gate replacements per the owner's brief:\n- **Gate 2:** after the cross-model plan critique, the orchestrator resolves open decisions itself (prefer the critique's recommendation; else the most reversible option) and records each in plan-final. Per `ab-stages.md` §plan.6, the orchestrator stands in as the Gate-2 judge for the A/B plan arms — arm mapping is disclosed immediately, not anonymized.\n- **Gate 3:** after ai-review converges, the orchestrator squash-merges the PR and deletes the branch itself; the review-guide is still posted first as the audit record.\n- **Gate 4:** the orchestrator self-validates each COS against test evidence on merged code, then closes with the full metrics comment.\n\nNon-negotiables retained: config.models routing (no substitution), cross-model review for plan+code, TDD tests-red-first, `make check` green before every push and after merge, all marker comments, no AI attribution in commits/PRs.\n\n**Blocker check:** blocked-by TKT-103 (US-020, publish seat maps) is **Done** ✓ — zero open blockers. TKT-104 in turn blocks TKT-105 (editing UI); unaffected.\n\n**Selection reason:** TKT-104 was the sole ticket in Ready with all DoR items met/deferred and its dependency satisfied. It is the epic's headline COS (maps versioned; edits never orphan/duplicate a sold/held seat), proven catalog-side via a seat-identity pinning contract.\n\n**Effective config for this run** (read from working tree at run start): `plan` and `aiReview` are A/B arrays `[\"deepseek-v4-pro:medium@claudex\", \"gpt-5.6-luna:high@claudex\"]`; `planReview` and `implement` = `main-agent`. Cross-model invariant holds on both pairs (planner arms ≠ main-agent reviewer; main-agent implementer ≠ review arms). The `:medium` deepseek arm reflects an uncommitted arm bump present in the working tree at run start (committed HEAD carries `:low`); the bump is internal-maintenance config and will be re-applied to the default branch after this ticket.\n\n**risk:low?** Not set, not claimed — this touches a new catalog transition (edit→new-version), a new migration, and a new ADR (the pinning contract). Plan gate stays substantive (it's the A/B-judged step above). Branch: `TKT-104-edit-published-map-seat-pinning` off `origin/main`."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### 📝 Plan drafts — A/B stage (2 arms, config.models.plan)\n\n**An A/B plan ran: 2 arms, same brief, same input, both read-only claudex drafters.** Per `ab-stages.md` §plan.6 (gate waived by owner directive ≠ risk:low): the orchestrator stands in as the Gate-2 judge, so arm mapping is **disclosed immediately** (anonymization is moot when the judge launched the arms).\n\n| Arm | Model / harness / effort |\n|---|---|\n| **A** | `deepseek-v4-pro:medium@claudex` |\n| **B** | `gpt-5.6-luna:high@claudex` |\n\n**⚠️ Harness-integrity finding (Arm A):** the deepseek arm was launched read-only (`--allowedTools \"Read,Grep,Glob\"`) yet **wrote a 22KB file to `docs/product/TKT-104-execution-plan.md`** — the claudex allowlist did NOT enforce read-only for this model/harness. Per `claudex-runner.md` the allowlist IS the sandbox; it failed here. The stray file was removed (planning is read-only; it is not a ticket deliverable — the ADR is). Content was preserved and is used as the arm's draft. Logged as a measurement note (retro item: the read-only claudex guarantee is not reliable for deepseek-v4-pro — treat claudex plan drafts as advisory and always clean the tree after).\n\n---\n#### Arm A (deepseek-v4-pro:medium@claudex) — design summary\n**Pinning contract via a real `seat_map_pins` table** (the fact-source TKT-80 writes into; `pinned_by text` free-form so no catalog change to consume). `EditSeatMap` locks the predecessor `seat_maps` row `FOR UPDATE`, reads the family's pinned set, validates every pinned identity appears exactly once in the submitted geometry, atomically inserts a new published version (`version+1`, same `map_family_id`), commits, then reuses the existing `seat_map.published` event (no bump). **Crux insight (COS-4):** a `FOR UPDATE` on the edit path alone does NOT serialize a concurrent pin INSERT into a *separate* table — so the contract includes **`PinSeat`/`UnpinSeat` store functions that take the SAME predecessor row lock** (two-sided lock). Recommendation: **hard-reject**. No new HTTP endpoint (deliverable is the contract; UI is TKT-105). Migration `0011` adds `map_family_id` + `seat_map_pins`. Full plan (with test plan sub-cases and the two-sided-lock derivation) captured in the run log.\n\n#### Arm B (gpt-5.6-luna:high@claudex) — design summary\n`EditPublishedSeatMap` creates a new **draft** version (clone-then-publish-separately), predecessor immutable, `FOR UPDATE` on source, identity-set-preservation validation (every source identity survives exactly once), reject removals/identity-changing renames/duplicates. **Hard-reject** recommendation. No new event, no new endpoint. Migration `0011` adds map-family/lineage metadata. Sold-reference modeled **synthetically in the test** (a retained `seat_identity` string) — does NOT add a pin table; leaves the concurrent-pin write path implicit (its race test says goroutine B 'acquires the same row lock' without defining the seam that makes that real). Cleaner structure, 5 named tests each mapped to a COS.\n\n---\n**planReview (config.models.planReview = main-agent) and Gate-2 selection follow in the plan-final comment.**"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "The epic's headline COS: edits to a published map must never orphan or duplicate a sold/held seat. Proven catalog-side via a seat-identity pinning contract (owner decision 2026-07-20), independent of inventory seat-holds."
    },
    "scope": {
      "state": "met",
      "note": "IN: editing a published map yields a NEW version (prior version immutable); a seat-identity pinning contract (published seat identity stable across versions) documented as an ADR; sold-case invariant test; concurrency test (edit vs sale-reference). OUT: building inventory per-seat holds and the HELD-seat interplay (that is TKT-80's AC, which consumes this contract); the editing UI (TKT-105)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Whether an orphaning edit is hard-rejected vs expressed as an explicit recorded remap — a design choice for the ADR/plan, both satisfy the COS. Not a product decision."
    },
    "dependencies": {
      "state": "met",
      "note": "blocked-by TKT-103 (needs a published map + the seated slot/contract to edit and to reference from a sale). Linked. Delivers the pinning contract TKT-80 (cross-epic) later consumes."
    },
    "approach": {
      "state": "met",
      "note": "New-version transition under the ADR-018 lock/emit discipline; the lock closes the edit-vs-sale race. Pinning-contract ADR: a seat identity any sale references is immutable/resolvable across versions. Spirit of ADR-026 (never strand a confirmed claim; forward-only). Discipline (ADR-021): this is honest-writer consistency, NOT a tamper claim — do not overclaim."
    },
    "first_slice": {
      "state": "met",
      "note": "Backend safety invariant + the pinning-contract ADR + sold-case & concurrency tests. Watch the ~400-line budget; if the plan estimates over, split the ADR/contract out as its own slice first (noted in the epic verdict)."
    },
    "success": {
      "state": "met",
      "note": "COS: an edit produces a new version, prior stays immutable; a seat referenced by a sold record is never orphaned (still resolves) nor duplicated (maps to exactly one seat) across any edit sequence; a concurrency test proves the edit-vs-sale race is closed; the pinning contract is written so TKT-80's held-seat AC consumes it with no catalog change."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked: ADR-018 (concurrency precedent), ADR-026 (never-strand-a-confirmed-claim spirit), ADR-005 (identity the claim references stays resolvable), ADR-021 (name-the-adversary discipline; do not overclaim tamper-evidence). governingAdrs present."
    }
  }
}
