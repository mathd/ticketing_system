{
  "key": "TKT-106",
  "type": "Task",
  "status": "Building",
  "labels": [
    "agent:ai-review"
  ],
  "assignee": "agent (main-agent / Opus 4.8)",
  "pr": {
    "number": 83,
    "url": "https://github.com/mathd/ticketing_system/pull/83",
    "state": "open"
  },
  "summary": "Smoke contract validator skips catalog (allowlist omits it)",
  "description": "### Smoke contract-validator allowlist omits catalog\n\n`smoke/contract_validation_test.go:~59` gates response contract-validation on `strings.Contains(\" inventory commerce payments access \", \" \"+service+\" \")` — **catalog is not in the allowlist**, so `validateServiceResponse` returns early for every `/api/catalog/*` response reached through the gateway in smoke. Catalog responses ARE still validated in-process (catalog's `NewRouter` wraps handlers in the response validator, ADR-028), so this is not a correctness hole — but the smoke suite gives catalog reads a false sense of end-to-end contract coverage. Surfaced during TKT-101 ai-review (kimi-k3): the new `/api/catalog/public/venues` smoke assertion runs the validator as a no-op.\n\n**Acceptance:** add `catalog` to the allowlist; FIRST verify no existing catalog endpoint drifts once validated (the reviewer flagged this may surface latent drift on endpoints never smoke-validated). If drift surfaces, fix the drift or scope the change per endpoint.\n\nOrigin: TKT-101 ai-review incidental finding (pre-existing, not introduced by TKT-101).",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ DoR shaped — TKT-106 — all 8 items met/deferred (Gate 1 unblocked)\n\nShaped from the seam (verified at HEAD: the allowlist `' inventory commerce payments access '` at `smoke/contract_validation_test.go:59` omits catalog). Self-contained, no blockers.\n\n| item | state |\n|---|---|\n| objective | met |\n| scope | met |\n| uncertainties | deferred |\n| dependencies | met |\n| approach | met |\n| first_slice | met |\n| success | met |\n| context_memo | met |\n\n**Deferred:** whether flipping catalog on surfaces latent response-drift is a build-time discovery — the AC prescribes verify-first-then-fix/scope. Governing: **ADR-028** (the fail-closed check being extended) + **ADR-009** (any drift is fixed at the contract). **Ready to prioritize (Gate 1).**"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed TKT-106 — Ready → Planning\n\n**Selection reason.** Top of Ready, fully shaped (DoR 8/8 met/deferred), zero open\nblockers (`links: []`), self-contained. Seam re-verified at HEAD before claiming:\n`smoke/contract_validation_test.go:59` allowlist `\" inventory commerce payments access \"`\nomits `catalog`.\n\n**Branch:** `TKT-106-smoke-catalog-contract-allowlist` off `origin/main` @ `4c0bb8e`.\n\n**Assignee:** agent (main-agent / Opus 4.8).\n\n---\n\n#### ⚠️ Gateless run — human gates waived by repo owner (process experiment)\n\nThe repo owner is running this ticket **fully autonomously, Ready → Done, gateless**, and\nhas **deliberately waived the four human gates** for this run. Recorded here so the board\nhistory shows this ticket ran without human gates:\n\n- **Gate 1 (priority):** given — ticket was placed in Ready by the owner.\n- **Gate 2 (plan approval):** waived. After the cross-model plan critique, the agent\n  resolves open decisions itself (prefer the critique's recommendation; else the most\n  reversible option) and records each self-made decision + rationale in `kind=plan-final`.\n- **Gate 3 (review + merge):** waived. After the adversarial ai-review converges (same\n  triage / second-pass / churn-cap rules), the agent squash-merges the PR itself and\n  deletes the branch. The review-guide is still posted first as the audit record.\n- **Gate 4 (PO acceptance):** waived. The agent self-validates each COS against test\n  evidence on the merged code, posts the validation note, and closes with the full\n  metrics comment.\n\n**Non-negotiable (unchanged):** config.models routing (no model substitution), cross-model\nreview for plan and code, TDD with tests observed red first, `make check` green before\nevery push and after merge, all marker comments at each stage, no AI attribution in\ncommits/PRs.\n\n**Model routing for this ticket** (from `.claude/sdlc.config.json`):\n- plan draft = `deepseek-v4-pro:high@claudex`\n- plan review = `fable-5:medium` (≠ plan ✓)\n- implement = `main-agent`\n- ai-review = A/B `[deepseek-v4-pro:high@claudex, gpt-5.6-luna:high@claudex]` (≠ implement ✓)\n\nClaudex proxy pre-flight: env present, `/v1/models` answered, both models listed."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — TKT-106 — by `deepseek-v4-pro:high@claudex` (read-only, allowlist Read/Grep/Glob)\n\nDrafted by the configured `config.models.plan` model via the claudex harness. Sandbox verified clean afterward (`git status` empty — no stray-write breach this run, unlike TKT-104). Seam references spot-checked by the orchestrator before posting: `TestCatalogPublicationAndStorefront` exists (`smoke/catalog_publication_test.go:73`); the spec-byte-identity assertion the draft cites is real (`catalog_publication_test.go:189-198`). Cross-model critique by `fable-5:medium` follows.\n\n---\n\n# Execution plan — TKT-106: Smoke contract validator skips catalog (allowlist omits it)\n\n## 1. Governing ADRs\n\n**ADR-028** (response-schema drift fails closed with 500).\n- Constraint for THIS ticket: any drift found is fixed at the contract (spec/handler + regen), never by loosening the validator. Every catalog handler response is already validated in-process with `IncludeResponseStatus: true`; smoke validation is a second, end-to-end layer. If smoke surfaces drift the in-process validator didn't catch, that's a bug in the in-process wiring, not a smoke false positive — fix the root cause (spec or handler).\n- Also constrains: the smoke validator currently does NOT set `IncludeResponseStatus: true` (see Risk 3). We do NOT add it in this ticket — that would be a behavior change for all five services and is out of scope (TKT-95 territory).\n\n**ADR-009** (contract-first APIs).\n- Constraint for THIS ticket: the OpenAPI document is the source of truth. Any drift fix happens at `services/catalog/api/openapi.yaml` or the handler, followed by `make generate` if generated types are affected. The smoke suite already asserts the served spec is byte-identical to the committed file (`catalog_publication_test.go:190-198`).\n\n## 2. Files touched\n\n| File | Change |\n|---|---|\n| `smoke/contract_validation_test.go:59` | Add `catalog` to the allowlist string — **only line 59** |\n\n## 3. Test seam & TDD ordering\n\n### 3.1 The \"red\" observation (current state is already red)\n\nThe validator is a no-op for catalog today. We can prove this with a focused assertion, but the simpler observation is: adding catalog to the allowlist and running the smoke suite constitutes the red→green transition. If catalog endpoints are already in-spec end-to-end, the suite passes (green). If drift exists, the suite fails (red) and we fix it.\n\n### 3.2 Recommended ordering\n\n1. **Make the one-line change** (see §4).\n2. **Bring up the full stack**: `make up`\n3. **Run the contract-validation smoke directly**, isolating the catalog path:\n   ```bash\n   go test -tags=smoke -count=1 -run 'TestCatalogPublicationAndStorefront' ./smoke/... 2>&1 | tee /tmp/smoke-catalog.log\n   ```\n   This exercises the richest catalog surface through `validateServiceResponse` (venue, event, performance, ticket-type create, publish, storefront reads). If it passes, expand to the full suite.\n4. **Run the full smoke suite, unfiltered**:\n   ```bash\n   go test -tags=smoke -count=1 ./smoke/... 2>&1 | tee /tmp/smoke-full.log\n   ```\n   This exercises ALL catalog call sites: `checkout_test.go`, `operational_holds_test.go`, `inventory_offering_state_test.go`, etc.\n5. **Read PASS/FAIL from the log body**, not exit code. A contract-violation failure prints:\n   ```\n   GET /venues response 200 violates the catalog contract: <detail>; body=<actual>\n   ```\n6. **If all passes**: green. Ticket is functionally done — run `make check` for the final gate.\n7. **If any test fails with a contract-violation message**: follow the drift decision tree (§5).\n\n## 4. The exact one-line change\n\n**File**: `smoke/contract_validation_test.go`, line 59\n\n**Current:**\n```go\nif !found || !strings.Contains(\" inventory commerce payments access \", \" \"+service+\" \") || path == \"openapi.yaml\" {\n```\n\n**Final:**\n```go\nif !found || !strings.Contains(\" catalog inventory commerce payments access \", \" \"+service+\" \") || path == \"openapi.yaml\" {\n```\n\nOnly `catalog` is added to the allowlist string. No other change — the `path == \"openapi.yaml\"` guard already covers `/api/catalog/openapi.yaml`, and the `internal/` 404 guard on lines 64-66 is service-agnostic and already covers `/api/catalog/internal/*`.\n\n## 5. Drift-discovery step & decision tree\n\n### 5.1 How to read the failure\n\nA contract-violation failure from the smoke validator prints:\n```\n<method> <path> response <status> violates the catalog contract: <kin-openapi error>; body=<response body bytes>\n```\n\n- `<path>` is the **rewritten** path (gateway prefix stripped), e.g. `/venues` not `/api/catalog/venues`.\n- `<status>` is the HTTP status the handler returned.\n- `<kin-openapi error>` explains WHY validation failed (missing field, wrong type, extra property, etc.).\n- `<body>` is the raw response bytes — copy-pasteable for comparison.\n\n### 5.2 Most likely drift scenarios (ordered by likelihood)\n\n1. **No drift at all (most likely)** — The in-process validator already enforces the same contract with the same kin-openapi library. The only difference is `IncludeResponseStatus: true` in-process vs. `false` (default) in smoke. Body and header validation are identical. Since every catalog handler response already passes in-process, body/header drift is very unlikely. The smoke suite passes on first run.\n\n2. **Undocumented error status returns a body that doesn't match the `Error` schema** — say a handler returns 409 with `{\"error\": \"...\", \"detail\": \"...\"}` instead of `{\"error\": \"...\"}`. In-process catches this with 500 (body mismatch → `IncludeResponseStatus: true` doesn't matter here — the 409 IS documented, the body just doesn't match). So the in-process 500 would prevent this from reaching smoke. **Risk: near-zero** — already caught in-process.\n\n3. **A 500 body reaching smoke from a drifted in-process handler** — If a handler drifts, in-process 500s with `{\"error\":\"response violates OpenAPI contract\"}`. Smoke receives this 500. With `IncludeResponseStatus: false` (smoke default), an undocumented 500 does NOT fail validation — kin-openapi silently skips body/header validation for undocumented statuses. So smoke would NOT produce a contract-violation error. Instead, the smoke test itself would fail because it expected a 2xx status (e.g., `created()` checks for 201). **Risk: caught, but as a test assertion failure, not a contract-violation message.** This is acceptable — the drift is still detected.\n\n4. **Header drift** — e.g., a documented header is missing. The in-process validator would catch it (same kin-openapi), so it never reaches smoke. **Risk: near-zero.**\n\n### 5.3 Decision tree if drift IS found\n\n```\nSmoke fails with \"violates the catalog contract\"\n│\n├─ Genuine body/header mismatch (e.g., extra field, missing required field, wrong type)\n│  ├─ Fix the spec (openapi.yaml) if the handler behavior is correct\n│  │  └─ Run `make generate` → re-run smoke\n│  ├─ Fix the handler if the spec is correct\n│  │  └─ Re-run smoke\n│  └─ Both per ADR-009/028: fix at contract level, never weaken the validator\n│\n├─ False positive from kin-openapi strictness difference (unlikely, same library)\n│  └─ If smoke flags something in-process doesn't: investigate the discrepancy\n│     (different kin-openapi version? different request reconstruction?)\n│     └─ Fix the root cause, not the smoke guard\n│\n└─ Per-endpoint scope-out (last resort, explicit owner approval)\n   └─ Document reason in a comment next to the allowlist or in the endpoint's spec\n      Example format: \"// <endpoint> excluded because <reason> — TKT-106\"\n```\n\n**Decision**: `owner: human` — which drift instances to fix vs. scope out. The human-waived gate resolves this by: prefer the reviewer's recommendation, else most reversible option (fix at spec-level is most reversible — it's a YAML edit + regen, backed by git).\n\n## 6. Risks\n\n| # | Risk | Severity | Mitigation |\n|---|---|---|---|\n| 1 | **Smoke discovers pre-existing catalog drift** — unlikely (in-process validator already guards it) but possible if the in-process wrap isn't covering every handler, or if smoke's request reconstruction differs from the original request in a way that makes route matching fail | Low | Follow drift decision tree (§5). If it surfaces, the in-process gap is a bug worth finding. |\n| 2 | **Missing 500 in catalog spec means drift-triggered 500s are invisible to contract validation in smoke** — `IncludeResponseStatus` is false in smoke, so undocumented 500 is silently skipped. The drift IS still caught (test assertion fails on wrong status), just not as a contract-violation message. | Low | Out of scope for this ticket. Adding `IncludeResponseStatus: true` to smoke would be a fleet-wide behavior change (TKT-95 territory) and would also require every service to document 500. Catalog's in-process validator already guards this dimension. |\n| 3 | **Performance impact** — every `/api/catalog/*` smoke call now goes through response buffering + kin-openapi validation | Negligible | Catalog responses are small JSON payloads (< few KB). The smoke suite already validates four other services identically. |\n| 4 | **Scope creep into `uncovered2xxOps` or `TestCommittedServiceContractsAreComplete`** — both deliberately exclude catalog for documented reasons | None (prevented) | The plan explicitly scopes the change to line 59 only. The other two hardcoded lists stay unchanged. |\n| 5 | **`/api/catalog/openapi.yaml` caught by smoke validator** — would fail because it's raw YAML, not JSON | None (prevented) | Line 59 already has `path == \"openapi.yaml\"` return-early. |\n\n## 7. DoD (Definition of Done)\n\n1. [ ] `smoke/contract_validation_test.go:59` — `catalog` added to the allowlist string.\n2. [ ] `make check` passes (lint + test + build for Go & TS, gateway smoke suite).\n3. [ ] Full smoke suite with the stack running (`make up` + `go test -tags=smoke -count=1 ./smoke/...`) passes — catalog responses are end-to-end contract-validated.\n4. [ ] Any drift found is resolved at the contract level (spec/handler + regen per ADR-009/028) OR explicitly scoped out per-endpoint with a documented reason.\n5. [ ] No other service lists in `coverage_test.go:55` or `contract_validation_test.go:124` are touched.\n\n## 8. Open decisions\n\n| # | Decision | Owner | Resolution rule (human-waived gate) |\n|---|---|---|---|\n| D1 | If drift surfaces: fix at spec/handler vs. scope out per-endpoint | human | Prefer reviewer recommendation; else fix at spec-level (most reversible — YAML edit + regen, git-backed) |"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan-final (DELTA) — TKT-106 — cross-model critique resolved, Gate 2 waived (self-finalized)\n\nCritique by `fable-5:medium` (adversarial subagent). It CONFIRMED every seam the draft named\n(allowlist @ `contract_validation_test.go:59`; both other catalog-free service lists at\n`coverage_test.go:55` and `contract_validation_test.go:124`; in-process `IncludeResponseStatus:true`\n@ `shared/go/contract/http.go:87`; smoke validator's absent Options @ `:87-92`). The\n`deepseek-v4-pro:high@claudex` draft above (kind=plan) **as amended below is the plan.**\n\n#### Amendments (critique-driven)\n\n- **§3 TDD — REDRAWN (was: \"run the whole suite and see\").** The draft had no red-first\n  observation — before and after the flip the suite is green, so nothing is ever seen red, which\n  is the exact silent-no-op failure this ticket fixes. **Adopted the critique's recommendation:**\n  add a focused, stack-free probe test in the smoke package — build a `GET /api/catalog/...`\n  request + a spec-valid 200 body, call `validateServiceResponse`, assert the `smokeCoverage` map\n  (`coverage_test.go:42`) now contains the catalog operation key. **RED today** (early return at\n  :59 → key absent), **GREEN after** the allowlist flip. Runs with `go test -tags smoke -run <name>`\n  — no compose stack. Doubles as a permanent regression pin on the allowlist (nothing pins it today —\n  that is how the bug shipped).\n- **§3/§7 drift-verification uses `make smoke`, NOT `make up` + ad-hoc `go test`.** The critique\n  showed the ad-hoc form lacks the per-run `SMOKE_INTERNAL_TOKEN` (`scripts/smoke.sh:51-53`,\n  no fallback in `operational_holds_test.go`) and won't match the compose project name several\n  tests `exec` against — those would fail for harness reasons and pollute the drift signal.\n  `make smoke` (or full `make check`) is the drift-discovery + gate step. Exit code is authoritative\n  (`smoke.sh` runs `set -euo pipefail`).\n- **§5/§6 risk relabel.** The realistic failure class is **route-match Fatals**\n  (`t.Fatalf(\"... is not committed in the catalog contract\")` @ `:84`) on gateway-authored\n  responses on `/api/catalog/*` paths the spec doesn't list — NOT schema drift. Schema drift is\n  near-impossible to surface because catalog's `ResponseValidator` wrap already covers its handler\n  responses *including* its 400 path (`server.go:67-92`) with a strictly-stricter validator, so any\n  real drift is already an ADR-028 500 failing existing status assertions. On a route-match Fatal:\n  classify the gateway-boundary response with a documented per-shape guard like the existing\n  `internal/` 404 carve-out — **never** by removing catalog from the allowlist.\n\n#### Self-made decisions (Gate 2 waived — recorded per run directive)\n\n- **D1 — if drift surfaces: fix-at-spec vs per-endpoint scope-out.** Resolved: **prefer fix at the\n  contract (spec/handler + `make generate`)** — most reversible (a YAML/handler edit, git-backed),\n  and mandated by ADR-009/ADR-028 (\"never loosen the validator\"). Per-endpoint scope-out only for a\n  genuinely gateway-authored (not catalog-authored) response, documented inline like the `internal/`\n  guard. Rationale: matches the critique's reversibility default and the governing ADRs.\n- **D2 — adopt NEW-2 (add `catalog` to `TestCommittedServiceContractsAreComplete:124`)?** The\n  critique RECOMMENDED it to make COS(2) non-vacuous (a schema-less catalog response would otherwise\n  validate against nothing). Resolved: **adopt.** Pre-verified safe — catalog's spec passes the\n  completeness gate today (35 ops, all operationId'd, all responses incl. `$ref` BadRequest/NotFound\n  carry content+schema; 0 problems). One-token, fully reversible. Rationale: prefer the critique's\n  recommendation (Gate-2-waiver rule); it directly serves COS(2)'s intent and is low-risk.\n\n#### REQUIRED — introduced at plan-review, reviewed by no second model\n\nTwo elements were **added** by the plan-review pass (fable-5), so they reach the (waived) Gate 2\nreviewed by nobody but their author. Both carry to the ai-review hand-off for a second model's eyes:\n\n1. **The red-first probe test** `TestValidateServiceResponseCoversCatalog` (design + fixture shape) —\n   fable-5's invention, not in the deepseek draft.\n2. **Adding `catalog` to `TestCommittedServiceContractsAreComplete`** (NEW-2 / D2) — fable-5's\n   invention.\n\nThe line-59 flip and the drift analysis came from the draft (deepseek), reviewed by fable-5 — those\nare cross-model-covered. The two items above are not.\n\n#### Final scope (files touched)\n\n- `smoke/contract_validation_test.go` — (a) line 59: add `catalog` to the allowlist string;\n  (b) line 124: add `\"catalog\"` to the completeness-test service list; (c) new test\n  `TestValidateServiceResponseCoversCatalog`.\n- `services/catalog/api/openapi.yaml` and/or a catalog handler — **only if** drift surfaces (D1).\n- **Untouched (deliberate):** `coverage_test.go:55` `uncovered2xxOps` service list (catalog's\n  coverage lives in its unit suite by design).\n\n#### DoD\n\n1. `catalog` in the `validateServiceResponse` allowlist (`:59`).\n2. `TestValidateServiceResponseCoversCatalog` committed, observed RED before the flip, GREEN after.\n3. `catalog` in `TestCommittedServiceContractsAreComplete` (`:124`), green.\n4. Full `make check` green (includes `make smoke` end-to-end catalog validation).\n5. Any drift resolved at the contract (D1) or documented per-endpoint scope-out.\n6. `uncovered2xxOps` service list untouched.\n\n**Governing ADRs (this ticket):** ADR-028 — the fail-closed response check being extended to\ncatalog end-to-end; any drift fixed at the contract, never by loosening the validator. ADR-009 —\ncontract-first; fix drift at spec/handler + regen.\n\nGate 2 waived by repo owner (gateless run) → proceeding to Building."
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation complete — TKT-106 — by `main-agent` (Opus 4.8), TDD\n\n**PR:** #83 — `TKT-106-smoke-catalog-contract-allowlist` @ `e437668`.\n\n**TDD, tests observed RED first.** The red-first artifact\n(`TestValidateServiceResponseCoversCatalog`) was run BEFORE the allowlist flip and\nobserved FAIL (catalog absent from allowlist → `validateServiceResponse` returns\nearly → coverage key not recorded):\n```\n--- FAIL: TestValidateServiceResponseCoversCatalog\n    validateServiceResponse did not contract-validate catalog: \"catalog listPublicVenues\"\n    ... catalog is likely missing from the allowlist ... (TKT-106).\n```\nThen the allowlist was flipped and the same test observed PASS. This is stack-free\n(`go test -tags smoke -run ...`, no compose), and doubles as a permanent regression pin.\n\n**Changes (3, all in `smoke/`):**\n1. `contract_validation_test.go:59` — added `catalog` to the `validateServiceResponse`\n   allowlist string.\n2. `contract_validation_catalog_test.go` (new) — the red/green pin, order-independent\n   (snapshot→clear→validate→restore the coverage-map entry so it asserts even after a\n   prior test recorded the op).\n3. `contract_validation_test.go:124` — added `catalog` to\n   `TestCommittedServiceContractsAreComplete` so end-to-end validation is non-vacuous\n   (plan-review NEW-2 / decision D2).\n\n**Drift verification (COS 3 — \"verify first\").** NO drift surfaced. Full `make check`\ngreen on the pushed tree: `MAKE_CHECK_EXIT=0`, `ok ticketing/smoke 77.298s`,\n`TestValidateServiceResponseCoversCatalog PASS`, `TestCommittedServiceContractsAreComplete/catalog PASS`,\nzero `violates the`/`not committed in` Fatals. D1's fix-or-scope branch was not triggered.\n\n**Deliberately untouched:** `coverage_test.go:55` `uncovered2xxOps` service list —\ncatalog's 2xx coverage lives in its unit suite by design.\n\n**Gate mechanics note.** The gate was run as the sole command with an explicit exit\nsentinel (`make check > log 2>&1; echo MAKE_CHECK_EXIT=$? > done`) and PASS/FAIL read\nfrom the sentinel + log body, per AGENTS.md/TKT-101. (A stray `pgrep`-based watcher\nself-matched its own command line and briefly reported a false \"still running\" — the\nauthoritative signal was the sentinel, which read 0.)\n\nImplementer = `main-agent`. Next: A/B ai-review by `deepseek-v4-pro:high@claudex` +\n`gpt-5.6-luna:high@claudex` (both ≠ implementer)."
    }
  ],
  "links": [],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "The smoke suite's response contract-validator (smoke/contract_validation_test.go:59) allowlists only ' inventory commerce payments access ' — CATALOG is omitted, so validateServiceResponse returns early for every /api/catalog/* response reached through the gateway in smoke. Not a correctness hole (catalog validates in-process via its own NewRouter/ADR-028 wrapper), but it gives catalog reads a FALSE sense of end-to-end contract coverage in smoke."
    },
    "scope": {
      "state": "met",
      "note": "IN: add 'catalog' to the smoke allowlist so /api/catalog/* responses are contract-validated end-to-end through the gateway; FIRST verify no existing catalog endpoint drifts once validated (the reviewer flagged latent drift risk on endpoints never smoke-validated). If drift surfaces, fix it or scope per-endpoint. OUT: broadening to non-catalog services (they're already in the allowlist); refactoring the smoke helper structure (that's TKT-95)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Whether flipping catalog on surfaces latent response-drift on some catalog endpoint is a build-time discovery, not a shaping unknown — the AC already prescribes: verify first, then fix or per-endpoint-scope. No product/architecture unknown; worst case is a per-endpoint follow-up."
    },
    "dependencies": {
      "state": "met",
      "note": "No open blockers. Touches only smoke/contract_validation_test.go (allowlist string) + possibly catalog spec/handlers IF drift is found. Related but independent: TKT-95 (unify smoke HTTP helpers) — not a blocker. Seam verified at HEAD: allowlist at line 59."
    },
    "approach": {
      "state": "met",
      "note": "Add 'catalog' to the allowlist string in smoke/contract_validation_test.go:59 (' catalog inventory commerce payments access '). Run the smoke suite; if any /api/catalog/* response now fails the response validator, that is real latent drift — fix the spec or handler (ADR-009/ADR-028) or, if a specific endpoint is legitimately unvalidatable, scope it out with a documented reason. Keep in-process catalog validation unchanged."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice: flip the allowlist, run smoke, resolve whatever drift surfaces. Sprint-sized (likely small; the size depends entirely on whether latent drift appears)."
    },
    "success": {
      "state": "met",
      "note": "COS: (1) 'catalog' is in the smoke response-validator allowlist; (2) the smoke suite contract-validates /api/catalog/* responses end-to-end through the gateway; (3) no endpoint drifts (or any drift found is fixed / explicitly scoped with a reason); (4) make check green."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked: ADR-028 (response drift fail-closed — the validation being extended to catalog) and ADR-009 (contract-first, the drift this would catch). governingAdrs present."
    }
  },
  "context": {
    "binding": [
      {
        "id": "ADR-028",
        "rule": "Responses are validated against the spec and fail closed on drift. This ticket extends that end-to-end smoke check to catalog, which was silently skipped.",
        "date": "2026-07-20"
      },
      {
        "id": "ADR-009",
        "rule": "Contract-first: the OpenAPI doc is the source of truth; smoke contract-validation is how per-boundary drift is caught end-to-end. Catalog being skipped means a class of drift went unchecked in smoke.",
        "date": "2026-07-12"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-028",
        "why": "The validator being extended to catalog is the ADR-028 fail-closed response check; read it to understand what 'drift' means when deciding how to resolve any surfaced failure."
      },
      {
        "id": "ADR-009",
        "why": "Any drift found is fixed at the contract level (spec/handler + regen), not by loosening the validator."
      }
    ],
    "reference": [
      {
        "summary": "Origin: TKT-101 ai-review incidental (kimi-k3) — pre-existing, not introduced by TKT-101",
        "link": ".sdlc board TKT-101"
      },
      {
        "summary": "The allowlist that omits catalog",
        "link": "smoke/contract_validation_test.go:59"
      },
      {
        "summary": "Related follow-up (independent): unify smoke HTTP helpers",
        "link": ".sdlc board TKT-95"
      }
    ],
    "capturedAt": "2026-07-20"
  }
}
