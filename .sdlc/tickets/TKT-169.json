{
  "key": "TKT-169",
  "type": "Task",
  "parent": "TKT-9",
  "classification": "complex",
  "summary": "May a ticket be exchanged after it has been used, and does the entry carry forward?",
  "status": "Backlog",
  "labels": [],
  "assignee": null,
  "pr": null,
  "description": "Found by the TKT-166 plan draft, reading ADR-039 against the code.\n\nADR-039 §2 claims *every refusal happens before money*. It does not cover an **already-used** source ticket. Commerce checks seated, currency and availability; nothing asks access whether the source tickets have been redeemed.\n\nSo a buyer can exchange, **scan in during `switch_pending`**, and then have the used ticket voided and a fresh unredeemed replacement issued — **two admissions for one exchange**. The window is normally short (the outbox drains fast) and grows with any delay in the switch.\n\n**TKT-166 deliberately did not fix this**, and the reasoning matters: the money moved in TKT-158, so refusing to switch would strand a paid exchange — worse than the hole. It switches as its AC requires and qualifies the ADR claim instead.\n\n**This needs a product decision before an implementation**, which is why it is a ticket:\n- May a used ticket be exchanged at all? (Refusing means a pre-money access check on TKT-158's path, adding a dependency to the money path.)\n- If yes, should the **entry carry forward** — the replacement issued already-redeemed?\n- What does that mean for a **multi-entry pass**, where 'used' is not binary (ADR-005/TKT-87)?\n\n**Acceptance Criteria:**\n- [ ] A decision, recorded as an ADR amendment: refuse pre-money, carry the entry forward, or accept the current behaviour with the exposure named.\n- [ ] Whatever is decided, a test drives the exchange-then-scan-then-switch sequence and pins the outcome, because today nothing does.\n- [ ] ADR-039 §2's qualification is replaced by the decision.\n\nQuality gates: `make check` green; `access verify-lifecycle` green.",
  "context": {
    "capturedAt": "2026-07-31",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-07-31",
        "rule": "Money is integer minor units + ISO currency code — floats are banned on money paths."
      },
      {
        "id": "PRD-v1",
        "date": "2026-07-31",
        "rule": "Traceability is a must, for money and tickets: money-relevant records land as immutable, hash-chained, signed journal entries from the first order (corrections = compensating entries; read models = projections), and every ticket/entitlement state change (issued, delivered, transferred, resold, exchanged, redeemed, reissued, invalidated) is an append-only lifecycle event linked to its order."
      }
    ],
    "reference": [
      {
        "summary": "Epic lineage and capability map",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "Service layout and ownership",
        "link": "docs/architecture.md"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-036",
        "why": "Catalog is the SINGLE authority for the rule-resolved unit price and commerce consumes `resolved_price` without recomputing it. Any re-pricing (an exchange) resolves through catalog and persists the §5 provenance snapshot for the NEW reservation — never copies the old snapshot."
      },
      {
        "id": "ADR-003",
        "why": "Corrections are COMPENSATING ENTRIES, never mutations. A refund/exchange appends a new signed fact; the original sale fact and its hash link are untouched. The payments journal has a DB trigger that raises on UPDATE/DELETE — any design that 'adjusts' a fact is rejected by the database, not by review."
      },
      {
        "id": "ADR-032",
        "why": "All provider money movement goes through the PSP port. `/internal/psp/refund` and the `payment_compensations` table already exist (TKT-114/116) — the PRIMARY KEY (organizer, source_idempotency_key, kind) is what converges duplicate refunds onto ONE provider idempotency key. Reuse it; do not add a parallel refund path."
      },
      {
        "id": "ADR-021",
        "why": "Every lifecycle event goes through store.appendLifecycle — the verifier asserts one-to-one coverage, so a direct INSERT into lifecycle_events reads as tampering and fails `access verify-lifecycle` in the gate. Canonical forms are pinned by golden literals; changing a byte is a canonical-version migration, not a test update. Also: name the adversary before writing 'tamper-evident'."
      },
      {
        "id": "ADR-011",
        "why": "The checkout journal protocol defines how commerce records money facts (order_facts row → payments /internal/facts, deterministic fact_id, idempotent). A post-purchase money movement uses the same protocol; do not invent a second one."
      },
      {
        "id": "ADR-010",
        "why": "Inventory's claim transaction is the contention path. Giving capacity back must go through the same row-locked transaction discipline; a plain UPDATE outside it can drive confirmed_quantity negative or double-return under concurrency."
      },
      {
        "id": "ADR-028",
        "why": "Fail closed. Responses are validated against the spec; an undeclared status code becomes a 500. Every new endpoint must declare every status it can answer, and a downstream answer commerce cannot validate aborts rather than degrades."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first. A new operation is written in the service's openapi.yaml first, then `openapi_gen.go` / `api-types.gen.ts` are regenerated and COMMITTED before the gate runs — check-generate diffs against HEAD, so an uncommitted regen reads as drift."
      },
      {
        "id": "ADR-030",
        "why": "Commerce and inventory ARE inside the smoke happy-path coverage gate. A new documented 2xx operation must be exercised through the running stack or the suite fails."
      },
      {
        "id": "ADR-022",
        "why": "Migrations run out-of-band via each binary's `migrate` subcommand as a one-shot Compose job. Add the SQL under the service's internal/store/migrations/ with the next number; the server path never migrates."
      }
    ]
  },
  "readiness": {
    "objective": {
      "state": "met",
      "note": "A reachable double-admission for one exchange, currently unpinned by any test."
    },
    "scope": {
      "state": "open",
      "note": "Refuse pre-money vs carry the entry forward vs accept — each has a different blast radius, and multi-entry passes complicate all three."
    },
    "uncertainties": {
      "state": "open",
      "note": "'Used' is not binary for a pass. That is the hard part and it is a product question."
    },
    "dependencies": {
      "state": "met",
      "note": "None blocking. Interacts with ADR-005's re-entry policy and TKT-87's admission vocabulary."
    },
    "approach": {
      "state": "open",
      "note": "Blocked on the decision."
    },
    "first_slice": {
      "state": "open",
      "note": "Blocked on the decision."
    },
    "success": {
      "state": "met",
      "note": "The exchange-then-scan-then-switch sequence has a pinned, intended outcome."
    },
    "context_memo": {
      "state": "met",
      "note": "Inherited from TKT-166. ADR-039 §2 carries the qualification this replaces; ADR-005 governs re-entry."
    }
  },
  "links": [
    {
      "type": "found-in",
      "key": "TKT-166"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ⚠️ Implementation readiness — CONCERNS\n\nA decision ticket. The engineering is small once the product question is answered, and unanswerable before.\n\n**Concerns:** (1) The multi-entry pass case means 'already used' is not a boolean, so a rule written for single-entry tickets will not transfer. (2) Refusing pre-money adds an access dependency to the money path, which this epic has twice judged the wrong trade for refunds — but an exchange is not a refund, and TKT-158 already accepted that distinction.\n\n**Verdict:** CONCERNS — shapeable, needs the decision first."
    }
  ]
}
