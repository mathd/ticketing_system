{
  "key": "TKT-26",
  "type": "Story",
  "summary": "US-002: Create and publish a GA event",
  "status": "Building",
  "labels": [
    "agent:coding"
  ],
  "assignee": "claude",
  "pr": null,
  "parent": "TKT-1",
  "classification": "simple",
  "description": "**As** an organizer, **I want** to create a venue, event, performance and GA ticket type via the catalog API and see it listed on the storefront.\n\n**Priority:** P0\n**Depends on:** US-001 (TKT-25)\n\n**Acceptance Criteria:**\n- [ ] Catalog API: create venue (GA capacity), event, dated performance, ticket type with EUR price; persisted; OpenAPI contract published\n- [ ] Publishing a performance emits a domain event; storefront lists published performances with name/date/price (browser-verified)\n- [ ] Public catalog reads (event list, event detail) carry explicit Cache-Control TTLs per ADR-004 tiers; the storefront page issues one aggregated call and does not re-fetch within the TTL\n- [ ] Event content fields are localizable; the storefront renders FR and EN (locale switch) with locale-aware date/money formatting (TKT-36 plumbing born here)\n- [ ] Entities carry an organizer_id even though v1 has one organizer\n\nQuality gates: `make check` green (defined in US-001, mirrors CI); contract tests for touched service boundaries; UI: browser-verified with screenshot in PR; money paths: NF525 journal chain verifies (from US-004).",
  "context": {
    "binding": [
      {
        "id": "ADR-001",
        "rule": "We adopt Go for all backend services and TypeScript + React for the three frontends (storefront, back office, scanner). Money is represented as integer minor units + ISO currency code everywhere; floats are banned on money paths.",
        "date": "2026-07-12"
      },
      {
        "id": "ADR-002",
        "rule": "We build five Go services (catalog, inventory, commerce, payments, access) behind an API gateway, with an event bus for the domain event stream; every entity carries a tenant/organizer id from day one. Docker Compose only.",
        "date": "2026-07-12"
      },
      {
        "id": "ADR-004",
        "rule": "Every public read endpoint declares a volatility-tiered TTL with explicit Cache-Control (seat maps: hours; event lists/detail: minutes; price display: ~1 min; availability: seconds; hold/order/scan state: never cached); hot events are served from in-memory structures refreshed/invalidated from the write path; frontends make few aggregated calls (one per page view, not per widget) and refresh on the endpoint's TTL cadence; writes are never cached.",
        "date": "2026-07-12"
      },
      {
        "id": "ADR-005",
        "rule": "Every admission-granting offer (performance, festival day, park operating day) is a dated slot in one inventory machinery; passes are entitlements to claim slot entries; lodging/day resources reserve through the same contention-safe core; slot attributes carry domain specifics — the claim primitive does not fork.",
        "date": "2026-07-12"
      }
    ],
    "reference": [
      {
        "summary": "PRD v1 — Milestone 1 walking skeleton stories",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "ADR-002 service map (five services, gateway, event bus)",
        "link": "docs/adr/ADR-002-services-from-day-one.md"
      }
    ],
    "capturedAt": "2026-07-12"
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "Scope amended pre-Gate-1 (owner directive 2026-07-12): public catalog reads must be cacheable with ADR-004 TTL tiers, storefront call-frugal. New AC added; ADR-004 baked into context binding."
    },
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "Backlog grilling 2026-07-12 (owner decisions): FR+EN locale plumbing is born in this story (owner: i18n from the start)."
    },
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "Final review fix: ADR-005 (unified dated-slot) baked into context — the 'performance' created here is the first dated slot — its shape must not preclude park days/festival days."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n**DoR verdict: Ready-eligible.** 6/7 items `met`, `uncertainties` `deferred` (all plan-resolvable, no pending product decision - the 2026-07-12 grilling already settled i18n-from-birth, ADR-004 tiers, and the ADR-005 slot shape; ADR-006 settled the shell framework).\n\nGrounded in code (not assumed): catalog `main.go` is a skeleton with chi/pgx/NATS wired but zero domain routes; gateway route table already reserves `/api/catalog/` for this story's contract; `web/storefront` is the nginx placeholder and is NOT in the pnpm workspace (only `web/scanner` is) - the Astro app must join workspace + `make check` + compose; no migration tooling exists anywhere - this story births the first schema.\n\n**Blockers: none open.** TKT-39 Done (ADR-006 Accepted, PR #4 merged @ 603ae00).\n\n**risk:low NOT suggested** - production code across three surfaces (catalog service, gateway contract, storefront rebuild), first DB schema, workspace/CI surface, well over 5 files. The plan gate applies."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n**Claimed TKT-26** - US-002: Create and publish a GA event.\n\n- **Selection reason:** owner directive this session (\"Let's go and take TKT-26\"); P0, head of the M1 walking skeleton queue; its only blocker TKT-39 is Done (ADR-006 Accepted, PR #4 merged @ 603ae00). Gate 1 was the owner's explicit chat instruction; DoR verdict posted above (6 met / 1 deferred).\n- **No risk:low** - production code on three surfaces (catalog domain routes + first DB schema, gateway public contract, storefront rebuilt as Astro 7 shell), joins the pnpm workspace and make check. Plan gate applies (Gate 2).\n- **Worktree:** `~/sources/ticketing_system.TKT-26-ga-event` (branch `TKT-26-ga-event` off origin/main, via worktrunk).\n- **Drive style:** gates only (asked once at run start)."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n## Execution plan — US-002: Create and publish a GA event\n\n**Grounding (verified in-repo):** catalog `main.go` is a skeleton with chi/pgx/NATS wired, zero domain routes; gateway's route table already reserves `/api/catalog/` and its header says \"real public contracts arrive with US-002's OpenAPI work\"; `web/storefront` is a static nginx placeholder NOT in the pnpm workspace; no migration tooling exists anywhere; the PLATFORM stream (`platform.>`) is provisioned at stack init; catalog DB + role exist (`deploy/postgres-init`); the smoke suite runs `-tags smoke` against an isolated compose stack with host-built artifacts; plain `go test` (test-go) runs with **no DB or Docker available**.\n\n### Test seams (chosen first)\n\n1. **Primary: the existing smoke suite** (`smoke/smoke_test.go`) — the whole AC path through the real gateway: create venue→event→performance→ticket type via `/api/catalog/*`, publish, assert the domain event on the PLATFORM stream, assert the storefront HTML (FR + EN) lists name/date/price, assert Cache-Control tiers. Highest existing seam; no new seam invented.\n2. **Catalog unit seam:** handlers tested with `httptest` against the chi router + a **repo interface fake** (test-go has no Postgres). Real SQL is exercised only in smoke (real Postgres 18.4). Event emission tested with a publisher-port fake.\n3. **Storefront unit seam:** vitest (same toolchain as scanner: oxlint/vitest/tsc) for locale-aware date/money formatting (integer minor units → display, no floats) and the SSR TTL-cache module.\n\n### Design decisions (approve at Gate 2; each gets a mini-ADR)\n\n- **ADR-008 — migrations:** `pressly/goose` v3 as a library, SQL files embedded (`embed.FS`) in the service binary, applied at startup before listen. No separate migrate container; per-service ownership matches ADR-007. Includes a seed migration inserting the single v1 organizer (AC5).\n- **ADR-009 — contract-first APIs:** OpenAPI 3.1 spec is the source of truth at `services/catalog/api/openapi.yaml`; `oapi-codegen` v2 generates the chi server interface + types; kin-openapi request-validation middleware enforces the spec at runtime; the spec is served at `GET /api/catalog/openapi.yaml` (\"published\" = committed + served). Storefront TS types generated via `openapi-typescript` (committed, regenerated by script).\n\n### Domain model (catalog schema, all rows carry `organizer_id`)\n\n`organizers` · `venues` (GA capacity int) · `events` (content as per-locale JSONB `{fr:{name,description},en:{...}}` — new locales without schema change, TKT-36) · `performances` (dated slot per ADR-005: `starts_at timestamptz`, `timezone`, `status draft|published`, `published_at`; no concert-specific fields — must not preclude festival/park days) · `ticket_types` (name localized JSONB, `price_amount bigint` minor units + `currency char(3)`, ADR-001).\n\n### API surface (v1, no auth — trusted local API, out of scope per DoR)\n\nWrites: `POST /venues`, `POST /events`, `POST /performances`, `POST /ticket-types`, `POST /performances/{id}/publish`. Publish flips status and emits the domain event.\nPublic reads (aggregated, one call per page view per ADR-004): `GET /public/performances?locale=` (list: everything the list page needs) and `GET /public/performances/{id}?locale=` (detail). Both carry the **minutes tier**: `Cache-Control: public, max-age=300`. Writes + publish: `Cache-Control: no-store`.\n\n### Domain event (first one — sets the envelope precedent)\n\nSubject `platform.catalog.performance.published` (fits the provisioned `platform.>`). Envelope: `{id, type, occurred_at, data:{performance_id, event_id, organizer_id}, schema: 1}`. Published to JetStream **after** the DB commit — plain publish, no outbox yet: at-least-once is not guaranteed and that is **recorded as a known deferral** (ADR-007 anticipates outboxes \"in later stories\"; US-004's journal work is where reliability machinery lands). No consumer exists in M1; smoke asserts the message on the stream directly (pattern already in `TestJetStreamPersists`).\n\n### Storefront (Astro 7 shell — first real ADR-006 integration)\n\n- New Astro app replaces the placeholder: `astro@7.0.7`, `@astrojs/node` (standalone), `@astrojs/react`, real `i18n.routing` (locales `fr`/`en`, `prefixDefaultLocale: true`). Routes: `/[locale]/events` (list), `/[locale]/events/[id]` (detail), locale switch in the layout. Joins `pnpm-workspace.yaml` → `pnpm -r lint/test/build` covers it (gate unchanged in shape).\n- **Caching ownership declaration (required by ADR-006 for every storefront story):** this story's reads are event list + detail, minutes tier ⇒ **page-layer owned, SSR-embedded** (price included — single-source: the page's one aggregated read carries it; the public API copy of the same read is the page's *source*, not a second independently-cached browser-side copy — no island fetches it). No availability anywhere (that island arrives in US-003). `src/middleware.ts` sets `Cache-Control: public, max-age=300` on event pages, `no-store` elsewhere; `/healthz` route added (no-store).\n- **AC3 \"no re-fetch within TTL\":** SSR in-memory TTL cache keyed by (route-class, locale), honoring the API response's `max-age`. Unit-tested in vitest; smoke asserts headers on both HTML and API, and best-effort asserts the no-refetch behavior by hitting the page twice and querying Prometheus for the catalog request-count delta (dropped if flaky — the TTL cache unit test + headers are the reliable assertions).\n- SSR fetches go **through the gateway** (`GATEWAY_URL=http://gateway:8080` in compose) — the storefront consumes the public contract (ADR-002); no compose deadlock since both healthchecks are self-only.\n- **Containers:** hermetic `web/storefront/Dockerfile` (pnpm build in-image, node runtime running `dist/server/entry.mjs`); fast-path `Dockerfile.smoke` copying host-built `dist/` (scanner pattern); compose healthcheck kept wget-compatible (alpine node image).\n\n### Gateway\n\nNo route change needed (`/api/catalog/` exists; prefix-strip verified in code). Only compose env additions.\n\n### Steps (TDD order)\n\n1. ADR-008 + ADR-009 committed (Proposed → Accepted at Gate 2).\n2. Catalog: failing handler tests (httptest + repo fake) → spec → generated server → domain logic → migrations → real repo (pgx).\n3. Publish flow: failing test with publisher fake → NATS publisher impl.\n4. Storefront: vitest for formatting + TTL cache → Astro app → pages → middleware.\n5. Smoke: extend with the full create→publish→storefront flow, header/tier assertions, FR+EN, stream assertion; update `TestStorefrontServedThroughGateway` (placeholder text goes away).\n6. Compose + workspace wiring; `make check` green end-to-end; browser screenshot for the PR (chrome-devtools).\n\n### Pre-mortem (lens: pre-mortem — assume Gate 2/3 bounced; what went wrong?)\n\n1. **Biggest story yet — diff too big to review.** Three surfaces is inherent to the walking skeleton (vertical by design), but reviewability is managed: commits stratified per surface (ADRs / catalog / storefront / smoke), review-guide walks by concern. If Gate 2 judges it too big anyway: natural split = catalog API+event (a) / storefront slice (b). Flagged, not chosen — the story is demoable only whole, and M1 stories are deliberately vertical.\n2. **Gate speed regression (TKT-42 just fixed it).** Astro build (~800 npm packages) joins `pnpm -r build`, a second web container joins smoke. Mitigations: `Dockerfile.smoke` host-built fast path; CI pnpm cache already exists. I will measure `make check` before/after and report the delta in the PR; if warm-gate time regresses badly, that's a Gate 3 conversation, not a silent cost.\n3. **Real-integration surprises the spike explicitly did not cover** (its risk register): Astro SSR fetch against the real Go gateway (error/latency behavior), `X-Forwarded-*` handling, path handling for `/api/catalog/openapi.yaml` through the prefix-strip. Mitigated: smoke exercises exactly this path; failures surface in the gate, not at demo time.\n4. **Catalog SQL bugs found only in smoke** (unit seam uses a repo fake). Accepted trade-off (test-go has no DB); the smoke flow covers every query this story adds. If smoke-only coverage proves too coarse, a build-tagged repo integration test against the smoke stack's Postgres is the escape hatch — noted, not built now.\n5. **Event envelope becomes accidental precedent.** First domain event on the bus — the envelope shape will be copied by US-003..006. Kept minimal + versioned (`schema: 1`), and ADR-009 documents it so the precedent is deliberate.\n\n**Deviations/deferrals recorded:** no outbox (publish-after-commit, at-least-once NOT guaranteed — deferred to US-004's reliability work); no auth on write endpoints (v1 trusted local); Prometheus no-refetch smoke assertion is best-effort."
    },
    {
      "stage": "plan-review",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=plan-review kind=plan-final -->\n## Plan revised after codex cross-model critique\n\nCodex verdict: **unsound as written, repairable** — 12 findings (1 blocking, 10 significant, 1 minor). All 12 verified against the repo and **accepted**; none rejected. The blocking one was a genuine correctness bug in the plan. Revisions:\n\n**1. Cache key aliasing (blocking).** The SSR cache key was `(route-class, locale)` — `/fr/events/B` would serve A's name/price for 5 minutes. **Fixed: key = the full request URL** (locale + route + id); stated as the rule \"the cache key includes every representation selector\".\n\n**2+3. Single cache owner + remaining-freshness propagation (ADR-006 clause 3 / ADR-004 rule 1).** Layered independent caches (SSR data cache 300s + browser/CDN HTML cache 300s) could double effective staleness to ~10 min. **Fixed: the storefront SSR page-data cache is the single cache owner of the minutes tier.** The catalog API's `Cache-Control: public, max-age=300, s-maxage=300` (s-maxage added per ADR-004 \"CDN-ready\") governs that cache; the HTML response's `max-age`/`s-maxage` is the **remaining** freshness of the data it embeds (`300 − data age`), so total staleness can never exceed the tier. The API's only consumer is the page's SSR read — no browser-side copy exists (single-source holds structurally).\n\n**4. AC3 verification is now mandatory + deterministic**, not best-effort: (a) vitest spy-fetch test — N identical requests within TTL ⇒ exactly 1 upstream call; (b) the page emits an `X-Page-Data-Age` debug header; smoke hits the page twice and asserts the second response's age > 0 (served from the SSR cache, no catalog re-fetch). The flaky Prometheus-delta idea is dropped.\n\n**5. Public contract is event-rooted, as the AC says.** `GET /public/events?locale=` (published events, each with upcoming performances + from-price) and `GET /public/events/{id}?locale=` (event detail: venue, all published performances, each with its ticket types/prices). Storefront detail route is `/[locale]/events/[eventId]`. Cardinality defined: an event with two performances appears once, listing both slots; displayed price = each performance's ticket-type prices (v1: one type ⇒ one price, list shows \"from\" min).\n\n**6. Browser verification is a defined flow, not one screenshot:** scripted browser session (chrome-devtools) — load `/fr/events` → locale-switch to EN (link must preserve the route) → navigate to detail → assert localized name/description, `Intl` date and EUR money rendering in both locales; FR + EN screenshots attached to the PR. Smoke keeps the raw-HTML assertions.\n\n**7. Contract drift is gate-enforced.** oapi-codegen + openapi-typescript pinned; generation via `make generate`; a drift check joins `make check` (regenerate → `git diff --exit-code` on generated files). Response conformance: kin-openapi **response** validation wrapped around handler tests (not just request validation); smoke asserts the spec served through the gateway is byte-identical to the committed file.\n\n**8+9. Build/smoke wiring completed (was silently missing):** storefront compose build context changes to root (`context: ., dockerfile: web/storefront/Dockerfile`, scanner pattern — required for pnpm workspace COPY); `compose.smoke.yaml` gains the storefront `Dockerfile.smoke` override; `scripts/smoke.sh` preflights `web/storefront/dist`; `make clean` covers it.\n\n**10. JetStream assertion is race-free and correlated:** smoke creates a filtered ephemeral consumer on `platform.catalog.performance.published` **before** acting, then creates+publishes a uniquely-named performance and matches `data.performance_id` in the received envelope (pattern of the existing `TestJetStreamPersists`). Publish itself is **JetStream ack'd publish** (synchronous `js.Publish`), not fire-and-forget — the stream durably has the event on the happy path; the outbox deferral now covers only crash-between-commit-and-publish (still recorded, US-004).\n\n**11. Tenancy semantics made explicit:** `organizers` is the tenant root (no self-referential organizer_id — the plan's \"all rows\" wording was wrong). Write payloads carry a required `organizer_id` (FK-validated); cross-entity relations are validated same-organizer (performance↔event↔venue, ticket-type↔performance); public reads are cross-organizer but scoped `status=published`; no hard-coded seeded ID in repositories — the seed is data, not code.\n\n**12. OpenAPI path vs prefix-strip:** catalog registers `GET /openapi.yaml`; the public URL is `/api/catalog/openapi.yaml` (gateway strips the prefix); smoke tests it through the gateway.\n\nEverything else in the kind=plan comment stands (seams, ADR-008 goose / ADR-009 contract-first, schema, event envelope, Astro shell, steps, pre-mortem). Pre-mortem lens line: pre-mortem lens applied in the draft (5 causes, 2 absorbed into the design); the codex pass then forced the six design-level revisions above — the two passes caught disjoint problems, as intended.\n\n**→ needs:human — Gate 2: approve the plan (incl. ADR-008/ADR-009) to move Building.**"
    }
  ],
  "links": [
    {
      "type": "blocks",
      "key": "TKT-27"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "First real vertical slice after the scaffold: catalog write path -> domain event -> cacheable public reads -> Astro storefront listing. M1 walking skeleton, P0."
    },
    "scope": {
      "state": "met",
      "note": "IN: catalog API create venue(GA capacity)/event/dated performance/ticket type (single EUR price, integer minor units); publish emits domain event on PLATFORM stream (ADR-007); OpenAPI contract published + registered at gateway /api/catalog/; storefront rebuilt as Astro 7 shell (replaces nginx placeholder) listing published performances, FR/EN locale switch, ADR-004 tiers, one aggregated call per page; organizer_id on every entity. OUT: auth/authn, back-office UI (creation via API only), event editing/versioning, inventory/holds (US-003), fees/promos/rules, seat maps, event consumers (emission only - no consumer exists yet in M1)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "All resolvable in Planning, none needs a product call: (1) migration tooling - none exists yet, first schema is born here; plan proposes (new-tool choice may warrant a mini-ADR); (2) OpenAPI spec-first vs code-generated - plan proposes; (3) Astro storefront joins the pnpm workspace + make check + compose (SSR container via @astrojs/node behind the gateway) - first real integration, carries ADR-006's declared risk register (gateway wiring, contract generation, CDN s-maxage honoring); (4) localizable-content shape must allow new locales without schema change (TKT-36)."
    },
    "dependencies": {
      "state": "met",
      "note": "TKT-25 (scaffold) Done; TKT-39 (Astro spike) Done, ADR-006 Accepted, PR #4 merged. Zero open blockers."
    },
    "approach": {
      "state": "met",
      "note": "Catalog: chi + pgx + NATS already wired in the skeleton; add domain routes + Postgres schema (catalog DB exists per ADR-007). Gateway: route table ready, contract registration is the US-002 work it announces. Storefront: Astro 7 SSR (@astrojs/node), Cache-Control per route class via src/middleware.ts, availability-free cached HTML per the ADR-006 ownership rule, i18n.routing for FR/EN. Performance is the first dated slot - shape must not preclude festival/park days (ADR-005)."
    },
    "first_slice": {
      "state": "met",
      "note": "The story is itself the M1 slice: create via API -> published -> visible on storefront in a browser. Demoable alone."
    },
    "success": {
      "state": "met",
      "note": "COS = the 5 ACs on the ticket + quality gates: make check green, contract tests on touched boundaries, browser-verified UI with screenshot in PR."
    }
  }
}
