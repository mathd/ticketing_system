{
  "key": "TKT-152",
  "type": "Story",
  "parent": "TKT-5",
  "classification": "complex",
  "summary": "US-P3: Early-bird tiers switch over by the clock alone — no manual intervention",
  "status": "Planning",
  "labels": [
    "agent:planning"
  ],
  "assignee": "agent (orchestrator: main-agent / Claude Opus 5)",
  "pr": null,
  "description": "**As** an organizer, **I want** to schedule an early-bird price and its successors up\nfront, **so that** the price changes on time without anyone being awake for it.\n\n**Priority:** P0  **Depends on:** TKT-151 ✅ (closed — `price_rules`, the comparator and the\nprovenance shape exist; `SelectPricingRule` already takes the evaluation instant)\n\nTKT-5's second COS: *scheduled price changes (early-bird tiers) apply without manual intervention*.\nThe lazy correct reading of \"without manual intervention\" is **no scheduler at all** — a rule\ncarries an effective window and resolution is evaluated against an instant, so the tier flips by\nthe passage of time and nothing has to run. A plan proposing a cron/job must argue why\nevaluation-time windowing is insufficient.\n\n> **ACs re-synced at claim** — one changed. \"Caller-supplied evaluation instant\" now means the\n> **store/resolver** caller, not an HTTP parameter. See the `kind=claim` comment.\n\n**Acceptance Criteria:**\n- [ ] Migration **0013** adds `effective_from` / `effective_until` to `price_rules`, both nullable\n  (unbounded), with `CHECK (effective_from IS NULL OR effective_until IS NULL OR effective_from <\n  effective_until)` so a reversed window is unrepresentable (ADR-036 §4 step 2). The columns and\n  the filter ship **together** — a column that accepts a timed rule the resolver ignores passes\n  every all-NULL test.\n- [ ] The interval is **half-open `[effective_from, effective_until)`** and that is stated in the\n  OpenAPI description. An inclusive/exclusive ambiguity at a tier boundary is a real money bug.\n- [ ] Resolution filters by the evaluation instant, so **no scheduled job, cron or write** is\n  required for a tier to take effect. A test proves it: from one fixed dataset **with no write in\n  between**, resolving at `effective_until - 1ns` yields the early-bird tier and at\n  `effective_until` yields the next one.\n- [ ] Overlapping windows at the same scope level resolve deterministically by a documented rule,\n  proven by a test with two deliberately overlapping tiers. It must compose with TKT-151's existing\n  tie-break (priority, then lowest id) rather than replace it.\n- [ ] **§4 step 1 narrows**: the currency check now skips rules whose window has already closed\n  (`effective_until <= at`) — they are inert. It still catches anything current **or still in the\n  future**. TKT-151 checks every rule because with no windows every rule is unbounded; this ticket\n  is where that becomes a real distinction, and the ADR already specifies it.\n- [ ] Provenance reports rules that lost **on time** as `outside_window_past` /\n  `outside_window_future` — the enum values TKT-151 declared and could not emit. An expired rule\n  never appears as a winner; with all tiers expired, resolution falls back exactly as TKT-151\n  specified.\n- [ ] `effective_from` / `effective_until` in the provenance stop being always-null. **The shape\n  does not change** — TKT-151 declared them required-and-nullable precisely so TKT-153's persisted\n  snapshots span one format, not two.\n- [ ] The window filter is **index-backed** per ADR-019 (result scoped *and* scan scoped, using the\n  variadic `explainGenericPlan` and asserting the access condition, not just the plan's\n  vocabulary); plain `CREATE INDEX` (ADR-020). OpenAPI regenerated and committed before the gate;\n  the operation stays covered by catalog's own coverage test (ADR-030).\n- [ ] The ADR-004 tier is **re-argued, not inherited**: a response whose correctness expires at a\n  known instant cannot be cached past that instant. ADR-036 §6 already fixed `no-store` for\n  sale-time resolution — state how that interacts with a tier boundary, and what a future\n  browse-time read would owe.\n\nQuality gates: `make check` green (lint + unit + integration, mirrors CI); contract tests for any\ntouched service boundary (OpenAPI drift check, ADR-009); public-read stories declare their ADR-004\nTTL tier with rationale; entities `organizer_id`-scoped (ADR-002); money is integer minor units +\nISO-4217 code, floats banned (ADR-001).",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Early-bird pricing is table stakes; doing it by hand at midnight is the failure mode this removes."
    },
    "scope": {
      "state": "met",
      "note": "IN: effective windows on rules, evaluation-time filtering, overlap semantics, provenance of the active tier, TTL interaction. OUT: quantity-tiered / demand-based pricing, channel windows (TKT-17), commerce wiring (TKT-153)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Overlap tie-break (narrowest-window vs latest-start) is a design call resolvable in Planning against TKT-150's conflict semantics; it does not need a product decision."
    },
    "dependencies": {
      "state": "met",
      "note": "blocked-by TKT-151."
    },
    "approach": {
      "state": "met",
      "note": "Extend TKT-151's rule table with the window columns and thread an evaluation instant through the same pure resolver — same test seam, table-driven with fixed instants (no wall-clock in tests)."
    },
    "first_slice": {
      "state": "met",
      "note": "Windows on the existing rule shape; demoable by resolving the same data at two instants and getting two prices."
    },
    "success": {
      "state": "met",
      "note": "COS-2 of TKT-5, proven by the no-write-in-between boundary test."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked below; governingAdrs resolved from docs/adr/ at 2026-07-30 and to be re-resolved at claim time."
    }
  },
  "context": {
    "capturedAt": "2026-07-30 (revalidated at claim, 2026-07-31)",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-07-30",
        "rule": "Money is integer minor units + ISO currency code — floats are banned on money paths."
      },
      {
        "id": "TKT-134",
        "date": "2026-07-29",
        "rule": "Pin time-sensitive assertions to the DB's clock, not the test process's, so the boundary under test is the only moving part."
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-036",
        "why": "THE SPECIFICATION, and it already answers most of this ticket: §4 step 2 (half-open windows + the reversed-window CHECK), §4 step 1 (currency skips rules whose window has CLOSED — the narrowing this ticket implements), §5 (outside_window_past/future, declared by TKT-151 and emitted here), §6 (no-store). Amended twice since this mémo was baked."
      },
      {
        "id": "ADR-002",
        "why": "AMENDED by ADR-036: rule-based unit-price resolution is CATALOG's. The mémo's original entry said commerce owns it — do not follow it."
      },
      {
        "id": "ADR-019",
        "why": "Two tests. And sharpened by TKT-151: asserting the index NAME appears is not enough — a read widened to scope_level alone uses the same index and scans the organizer's whole rule set. Assert the scoping columns are inside the Index Cond."
      },
      {
        "id": "ADR-004",
        "why": "Re-argue the tier here rather than inherit it: this is the ticket that makes a response's correctness EXPIRE at a known instant, which is the actual reason for no-store."
      },
      {
        "id": "ADR-001",
        "why": "Money stays integer minor units; a window changes which rule applies, never how an amount is computed."
      },
      {
        "id": "ADR-009",
        "why": "Regenerate openapi_gen.go / api-types.gen.ts and COMMIT them before the gate — check-generate diffs against HEAD."
      },
      {
        "id": "ADR-022",
        "why": "Migration 0013 runs out-of-band via the binary's migrate subcommand; the server path never migrates."
      },
      {
        "id": "ADR-020",
        "why": "Plain CREATE INDEX for any window index."
      },
      {
        "id": "ADR-030",
        "why": "Catalog covers its operations in its own coverage_test.go, not the smoke gate."
      }
    ],
    "reference": [
      {
        "summary": "TKT-134's learning — pin the hold by DB time so release is the test's only moving boundary — is the closest precedent for testing a time boundary in this repo.",
        "link": "docs/learnings/"
      },
      {
        "summary": "The existing operating-window columns on `performances` (migration 0004) show the repo's convention for representable time bounds.",
        "link": "services/catalog/internal/store/migrations/0004_typed_slot.sql"
      },
      {
        "summary": "Time-window fixtures must be relative to now — a calendar-literal fixture judged against now() is green at merge and fails once the clock crosses it; CI, mutation testing and review all pass because the defect only exists later. Build window fixtures from time.Now() with a deliberate offset, pinned once per run.",
        "link": "docs/learnings/2026-07-18-time-window-fixtures-must-be-relative.md"
      }
    ]
  },
  "comments": [
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-152\n\n**Gate 1 waived** (epic TKT-5's `kind=claim`): the agent performed `Backlog → Ready` as the owner's\nproxy.\n\n**Selection reason.** TKT-151 closed, so **both** TKT-152 and TKT-153 became claimable at once —\nthis is the first real choice in the epic. Taking **TKT-152 first**, deliberately:\n\n- TKT-153 persists a **provenance snapshot** on the reservation. If TKT-152 lands after it, the\n  window fields go from always-null to populated *underneath* stored snapshots, and TKT-153's\n  format claim gets tested by production data rather than by a test.\n- TKT-152 is additive to a seam TKT-151 already shaped (`SelectPricingRule` takes `at`; the\n  provenance already declares the window fields). TKT-153 changes a **money path in commerce** —\n  the riskier of the two, and better done against a finished pricing model than a half-finished one.\n- Neither blocks the other, so this is ordering by risk, not by dependency. Recorded because the\n  board cannot show a reason the graph does not contain.\n\n**Not `risk:low`.** A migration, a change to the comparator's eligibility rules, and a currency-check\nnarrowing that ADR-036 spent two review passes getting right. The plan gate applies, resolved under\nthe Gate 2 waiver.\n\n---\n\n#### AC re-synced to TKT-151's decision\n\n| Was | Now | Why |\n|---|---|---|\n| \"Resolution filters by the **caller-supplied** evaluation instant (defaulting to now)\" | The instant is supplied by the **store/resolver** caller. It is **not** an HTTP parameter, and the boundary proof drives it at that seam. | TKT-151 established this and it is a security property, not a style choice: an HTTP-supplied instant on a sale-time price endpoint lets anyone request early-bird pricing after the window closed. The no-write-in-between proof works unchanged one layer down. Decided at TKT-151's plan-final (decision 4) and flagged there as changing a downstream ticket's ACs on my own authority — restated here so this ticket's plan draft sees it. |\n\nTwo ACs were also **sharpened** by things TKT-151's reviews established: the reversed-window CHECK\n(ADR-036 §4 step 2), and the ADR-019 evidence now requiring the **access condition** to name the\nscoping columns rather than the index merely appearing in the plan.\n\n#### Context-mémo revalidated at pickup\n\nBaked at decomposition (`capturedAt: 2026-07-30`). Since then **ADR-036 landed and was amended\ntwice** (TKT-150's own review rounds, then TKT-151's), and **ADR-002 was amended** by ADR-036 on the\npricing row. The mémo's `governingAdrs` are rewritten below to match what is now true, not what was\ntrue when the epic was decomposed.\n\nThe learning already in this ticket's mémo — *time-window fixtures must be relative to now* — is\nthe single most relevant thing in it, and this is the ticket it was written for.\n\nBranch: `TKT-152-effective-windows`.\n"
    }
  ],
  "links": [
    {
      "type": "blocked-by",
      "key": "TKT-151"
    }
  ]
}
