{
  "key": "TKT-230",
  "type": "Task",
  "summary": "Flaky: smoke DB-backed tests fail intermittently under load, a different test each run (commerce, inventory)",
  "status": "Building",
  "labels": [
    "agent:ai-review"
  ],
  "assignee": "agent",
  "pr": {
    "number": 188,
    "url": "https://github.com/mathd/ticketing_system/pull/188",
    "state": "open"
  },
  "links": [],
  "description": "Observed 2026-08-08 while gating **TKT-229**, on four consecutive `make check` runs. Each run failed\nthe **smoke** stage on a **different** DB-backed test, none of them related to the change under gate\n(TypeScript only):\n\n| Run | Failing test | Package |\n|---|---|---|\n| 1 | `TestGroupReservationRepeatedDrawDownIsQuantityNeutral` | `inventory/internal/store` |\n| 2 | `TestFullSeatedReturnReleasesEverySeat` | `inventory/internal/store` |\n| 3 | `TestBackfillOwesEventsForPreExistingCompletedOrders` | `commerce/internal/store` |\n| 4 (control, pristine `main`) | — smoke never reached; failed earlier at `test-ts` (the TKT-229 flake) | — |\n\n**Not TKT-198 and not TKT-229.** Zero `already exists` / `partial migration` errors in every run, so\nthe migration race TKT-198 fixed is genuinely gone. TKT-229's diff is TypeScript only and cannot\nreach a Go smoke test. The control run on pristine `main` confirms the TS flake is TKT-229's and is\nfixed by it.\n\n**A sample failure, and why it looks like ordering rather than logic:**\n\n```\nreservations_smoke_test.go:167: history[0].Action = draw_down, want reserve\n```\n\nThe assertion wants `[\"reserve\",\"draw_down\",\"draw_down\",\"draw_down\"]` and got a draw_down first.\nThat is a **history ordering** claim. If `History()` orders by a timestamp with insufficient\nresolution, rows written inside the same transaction — or the same millisecond — can tie, and the\ntie-break is then whatever the database returns. Under load the writes bunch up and ties become\nlikely. **That is a hypothesis from one sample, not a diagnosis** — the other two failures were not\ninvestigated and may be unrelated.\n\n**Why it matters.** This is the third distinct load-sensitivity defect found in two days\n(TKT-198 migration race, TKT-229 quadratic TS fixture, this). Each one makes `make check`\nnon-deterministic, and a gate that fails on machine load cannot certify anything — the same\nblocking property that made TKT-198 urgent.\n\n**Conditions of Success:**\n- [ ] The mechanism is identified for **each** observed failure, or the ones that turn out to share a\n      cause are grouped with the evidence that says so.\n- [ ] The failures reproduce deterministically before the fix, or the ticket says why not.\n- [ ] No assertion is widened to pass. If `History()` really is ambiguously ordered, the fix is a\n      total order in the query, not a laxer test.\n- [ ] `make check` green three consecutive times **on a loaded machine**.\n\n**Do not** fix by adding retries or by loosening the ordering assertions — an ordering guarantee the\ntests stop checking is an ordering guarantee the code stops having.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\nFiled by **TKT-229**'s `agent:ai-review`/merge stage (gateless run, 2026-08-08) as an incidental. Not shaped — needs `shaping.md` before Gate 1. Evidence logs were at /tmp/g229_8.log, /tmp/g229_9.log, /tmp/gmain.log and /tmp/gctrl.log at the time of filing; the failing test names and the control-run result are transcribed above because those files are temporary."
    },
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\n### A fourth instance, on `main` after both fixes merged (2026-08-08)\n\nA confirmation run of `make check` on `main` at `a0a596d` — TKT-198 and TKT-229 both merged — failed\nthe smoke stage on a **fourth distinct test**:\n\n```\n--- FAIL: TestMigrationsRanBeforeServicesStarted/payments\nsmoke_test.go:346: ticketing-smoke-0-payments-migrate-1 finished at 00:48:25.681558716,\nbut ticketing-smoke-0-payments-1 started at 00:48:25.162768220\n— the service started before its migrations completed\n```\n\n**519 milliseconds.** Four of the five services passed; only `payments` inverted.\n\nThis strengthens the diagnosis: the failing tests share no code, but they all assert an **ordering or\ntiming relation with a small margin** — history row order, container start order. Under CPU\ncontention the margins close and the assertion flips. Zero `already exists` errors in this run, so\nTKT-198's fix continues to hold.\n\n**Note this one is about ADR-022's core guarantee** (`service_completed_successfully` gating), so the\nright fix is probably to assert the *Compose dependency condition* rather than to compare wall-clock\ntimestamps — a test that measures a guarantee by timing can fail while the guarantee holds. That is a\nhypothesis for the plan, not a decision.\n\n**Running tally, all on unrelated code:** `TestGroupReservationRepeatedDrawDownIsQuantityNeutral`,\n`TestFullSeatedReturnReleasesEverySeat`, `TestBackfillOwesEventsForPreExistingCompletedOrders`,\n`TestMigrationsRanBeforeServicesStarted`. CI passes all of them on an unloaded runner."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Readiness verdict: **READY** (8/8 met) — scope narrowed, two failures split out\n\nShaped 2026-08-09 against `main` @ `4c995e8`. The `uncertainties` and `approach` items were\nresolved **by measurement**, not by assumption, because the ticket's own COS forbids widening an\nassertion to pass and that only makes sense once the mechanism is known.\n\n#### The mechanism, proven\n\n`claim_history` is read with `ORDER BY occurred_at, id` (`operational.go:336`, `capacity.go:131`).\n`occurred_at` is `timestamptz DEFAULT now()`; `id` is `uuid.New()` — **UUIDv4, random, no time\ncomponent** (`store.go:102`). So the order is *total but not meaningful*: rows that tie on\n`occurred_at` are ordered by a coin flip. That is why it survived review.\n\nMeasured against a real PostgreSQL (throwaway container, since torn down):\n\n| Scenario | Rows | Distinct `occurred_at` | Collisions |\n|---|---|---|---|\n| 2000 inserts, one transaction | 2000 | 1 | 1999 |\n| 300 inserts, separate transactions, serial | 300 | 300 | **0** |\n| 1200 inserts, 8 concurrent writers, separate transactions | 1200 | 1199 | **1** |\n\nThe colliding pair was `{w4_35, w2_32}` — two *different* writers at\n`2026-08-09 04:30:49.058874+00`, identical to the microsecond.\n\nSerial → no collisions (passes on an idle machine). Concurrent → collisions appear (fails under\ngate load). The observed `history[0].Action = draw_down, want reserve` is precisely a tie\nresolved the wrong way. Note this is **not** the familiar \"`now()` is transaction-start\" intra-\ntransaction tie: `reserve` and each `draw_down` are written in *separate* transactions\n(`reservations.go:132` and `:235`), and separate transactions still collide under concurrency.\n\n#### Scope narrowed — and why\n\nThe ticket's COS says the mechanism must be identified for **each** observed failure. Two of the\nfour are **not** explained by the above and are being split out rather than carried:\n\n- `TestBackfillOwesEventsForPreExistingCompletedOrders` (commerce) — uninvestigated.\n- `TestMigrationsRanBeforeServicesStarted/payments` — a 519ms container-start inversion; a\n  different assertion class entirely, and per the ticket's own note the right fix is probably to\n  assert the Compose dependency condition rather than compare wall-clock timestamps.\n\nBundling unexplained failures into a ticket whose COS demands explained ones guarantees it cannot\nclose honestly. Also named as a deliberate exclusion: `access.lifecycle_events` uses the same\n`ORDER BY occurred_at,id` shape (`migration_smoke_test.go:130`) and may share the latent\nambiguity — out of scope here, flagged so it is a choice and not an oversight.\n\n#### Approach constraint the plan must solve\n\n`claim_history` is append-only with a `BEFORE UPDATE OR DELETE` trigger\n(`0003_operational_holds.sql:42-50`). The trigger does not fire on INSERT, but **any backfill of\nexisting rows is an UPDATE and is refused by the database**. Adding a `NOT NULL` monotonic column\nto a table whose existing rows cannot be rewritten is the real design question — the in-repo\nprecedent is `bigserial` (`access/.../0003_lifecycle_integrity.sql:87,128`).\n\n`risk:low` **not** claimed: this adds a data migration, which the label's own carve-outs exclude.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — and this run is **gateless by owner waiver**\n\n**⚠ Waiver, recorded so the board history shows it.** The repo owner (Mathieu Dupuis) explicitly\nwaived **all four human gates** for this run as a deliberate process experiment, and directed the\nagent to run `Ready → Done` continuously. Substitutions, as instructed:\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human moves Backlog → Ready | owner named the ticket; agent moved it |\n| 2 — plan approval | human approves the plan | agent resolves open decisions itself, preferring the critique's recommendation; else the most reversible option. Every self-made decision recorded in `kind=plan-final`. |\n| 3 — review + merge | human reviews and squash-merges | agent squash-merges after the adversarial review converges, having posted the review-guide first as the audit record |\n| 4 — PO acceptance | PO validates COS | agent self-validates each COS against test evidence on the merged code |\n\nEverything else in the skill applies unchanged: `config.models` routing with no substitutions,\ncross-model review for both plan and code, TDD with tests observed red first, `make check` green\nbefore every push and after the merge, all marker comments, no AI attribution anywhere.\n\nThe `overrides:` list in the closing `kind=metrics` comment is the audit record for this waiver:\nevery point where a review pass raised a **blocking** objection and the agent proceeded anyway.\n\n**Selection reason.** Owner-directed. Chosen over TKT-218 and TKT-149 — the two tickets originally\nbundled into this run — because shaping proved those two are not implementable as written:\nTKT-218's premise did not reproduce under 8 runs at load, and TKT-149 asks for a diagnostic that\nhas existed since TKT-82. Both were returned to the owner as decisions. TKT-230 is the only one of\nthe three with a proven defect, and it is actively making `make check` non-deterministic, which\nblocks confident gating of every other ticket.\n\n**Blockers:** none. TKT-198 and TKT-229 are merged; neither is implicated.\n\n**Risk verdict: `risk:low` NOT claimed.** The work adds a **data migration** to an append-only\ntable — one of the label's four explicit carve-outs (auth, data migrations, CI/deploy config,\n>5 files). Stating it explicitly per the skill's rule, even though the diff will be small: the\ncarve-out is exactly the case whose risk is obvious to the claimer and invisible in the thread\nafterwards. The plan-review stage is therefore load-bearing here and is not skipped.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n**Drafted by `gpt-5.6-luna`** (via the Codex companion, `task --effort high`, read-only — the\ndrafter never touched the tree), per `config.models.plan`. Job `task-mslb9v9l-boubsd`, ~3m50s.\nBriefed by the orchestrator from the ticket + the baked context-mémo, including the required\n\"governing ADRs\" section re-resolved against `docs/adr/` at HEAD `4c995e8`.\n\nPosted verbatim below as drafted. Critique follows in `agent:plan-review`.\n\n---\n\n# TKT-230 execution plan\n\n## 1. DoD\n\n- `History` and `CapacityHistory` return a deterministic chronological order when `occurred_at` ties.\n- New `claim_history` rows receive a database-assigned append sequence.\n- Existing rows are not rewritten, and the append-only trigger remains enforced.\n- The regression test fails on the current revision because UUID ordering reverses two same-timestamp rows, then passes after the change.\n- No retries, widened assertions, or `-count=N`.\n- Inventory has no remaining ordered `claim_history` reader using only `ORDER BY occurred_at, id`.\n- `make check` passes three consecutive times on a loaded machine.\n\nThe current action vocabulary is:\n\n`create`, `place`, `release`, `convert`, `finalize`, `confirm`, `expire`, `adjust_capacity`, `reserve`, `draw_down`, and `refund_return` (`services/inventory/internal/store/migrations/0010_refund_capacity_returns.sql:22-27`).\n\n## 2. The design decision\n\nAdd a nullable `BIGINT append_order` to `claim_history`, backed by a PostgreSQL sequence and populated by a `BEFORE INSERT` trigger.\n\nThe read order becomes:\n\n```sql\nORDER BY occurred_at, append_order NULLS FIRST, id\n```\n\n`occurred_at` remains the primary chronological key. `append_order` resolves timestamp ties for all rows created after the migration. `id` remains only as the final tie-breaker for legacy rows that have no append order.\n\n### Migration shape\n\nCreate `0012_claim_history_append_order.sql`:\n\n```sql\nCREATE SEQUENCE claim_history_append_order_seq AS bigint;\n\nALTER TABLE claim_history\n  ADD COLUMN append_order bigint\n    CONSTRAINT claim_history_append_order_positive\n    CHECK (append_order IS NULL OR append_order > 0);\n\nALTER SEQUENCE claim_history_append_order_seq\n  OWNED BY claim_history.append_order;\n\nALTER TABLE claim_history\n  ALTER COLUMN append_order\n  SET DEFAULT nextval('claim_history_append_order_seq');\n\nCREATE UNIQUE INDEX claim_history_append_order_uidx\n  ON claim_history (append_order)\n  WHERE append_order IS NOT NULL;\n```\n\nAdd an insert trigger that assigns `nextval(...)` when an INSERT explicitly supplies `NULL`, so every future row receives an order value even if a new writer bypasses the default.\n\nReplace the existing lookup indexes with ordered variants:\n\n```sql\nCREATE INDEX claim_history_claim\n  ON claim_history (claim_id, occurred_at, append_order NULLS FIRST, id);\n\nCREATE INDEX claim_history_pool\n  ON claim_history (pool_id, occurred_at, append_order NULLS FIRST, id)\n  WHERE pool_id IS NOT NULL;\n```\n\nUse plain `CREATE INDEX`, not `CREATE INDEX CONCURRENTLY`, per `docs/adr/ADR-020-catalog-index-build-concurrency.md:80-115`.\n\nThe migration must not contain an `UPDATE claim_history`. The existing trigger rejects updates and deletes (`services/inventory/internal/store/migrations/0003_operational_holds.sql:41-50`), and ADR-023 explicitly defines the trail as append-only with no invented baseline rows (`docs/adr/ADR-023-operational-holds-and-conversion.md:56-62`).\n\nPre-existing rows remain `append_order IS NULL`. They cannot be assigned their true historical append position: the database has only `occurred_at` and a random UUID, and rewriting them would violate the trail contract. For legacy rows sharing a timestamp, `id` preserves the existing total-order fallback, but that order must be described as a stable legacy tie-break—not reconstructed chronology. New rows have a meaningful insertion-order tie-break.\n\n### Rejected alternatives\n\n- **Backfill a `NOT NULL` column with `UPDATE`.** Rejected because the append-only trigger refuses it, and the original append order is not recoverable.\n- **Populate legacy rows from `(occurred_at, id)`.** Rejected because it would canonize the UUID coin flip as if it were historical truth.\n- **Use only a higher-precision timestamp or `clock_timestamp()`.** Rejected because separate concurrent transactions can still receive equal timestamps, which is the observed failure.\n- **Use UUID ordering as the permanent solution.** Rejected because `appendHistory` supplies UUIDv4 values with no time component (`services/inventory/internal/store/store.go:102-105`).\n- **Add a companion order table.** This is technically viable and resembles the access-service integrity side table (`services/access/internal/store/migrations/0003_lifecycle_integrity.sql:18-34`), but it adds a join and a second coverage invariant. An inline nullable sequence plus an insert trigger solves new-row ordering without changing the immutable row shape; it still honestly leaves legacy chronology unresolved.\n\nThe migration remains embedded and owned by inventory. It will be applied by the inventory `migrate` job, not the server path (`docs/adr/ADR-022-out-of-band-service-migrations.md:101-111`; `services/inventory/cmd/inventory/main.go:64-80`).\n\n## 3. Files to change\n\n- `services/inventory/internal/store/migrations/0012_claim_history_append_order.sql`\n  - Add the nullable append-order column, sequence, default, insert trigger, uniqueness constraint/indexes, and guarded down migration.\n  - Do not rewrite existing `claim_history` rows.\n\n- `services/inventory/internal/store/operational.go:331-349`\n  - Change `History` from `ORDER BY occurred_at, id` to `ORDER BY occurred_at, append_order NULLS FIRST, id`.\n\n- `services/inventory/internal/store/capacity.go:119-145`\n  - Apply the same ordering to `CapacityHistory`.\n\n- `services/inventory/internal/store/history_order_smoke_test.go` (new)\n  - Add the deterministic timestamp-tie regression test.\n  - Add migration/trigger assertions if the test harness can exercise the upgrade from migration 0011.\n\nNo writer SQL needs to change: `sweepExpired` (`services/inventory/internal/store/store.go:68-76`), `appendHistory` (`services/inventory/internal/store/store.go:102-105`), and capacity adjustment inserts (`services/inventory/internal/store/capacity.go:83-85`) all omit the new column and will use the database assignment trigger/default.\n\n## 4. Test plan\n\n### Deterministic pre-fix regression\n\nAdd a smoke test that:\n\n1. Creates an inventory pool and fixture claim.\n2. Begins one SQL transaction.\n3. Reads one timestamp once, for example `SELECT now()`.\n4. Inserts a `reserve` history row with a deliberately high UUID.\n5. Inserts a `draw_down` history row with a deliberately low UUID.\n6. Supplies the exact same timestamp to both INSERTs.\n7. Commits and calls `st.History`.\n8. Asserts the result is:\n\n```text\nreserve, draw_down\n```\n\nThe current query at `services/inventory/internal/store/operational.go:335-336` sorts the low UUID first, so this test fails deterministically before the fix. It does not depend on load, scheduling, transaction-start timestamp behavior, retries, or repeated test runs. After the migration, the first INSERT receives the lower append sequence and the assertion passes.\n\nThe test should also assert both new rows have non-null, distinct append-order values.\n\n### Existing tests that must remain green\n\n- `TestGroupReservationRepeatedDrawDownIsQuantityNeutral` in `services/inventory/internal/store/reservations_smoke_test.go:84-173`\n  - Verifies the exact `reserve`, `draw_down`, `draw_down`, `draw_down` order that currently flakes.\n  - Also verifies child linkage, quantity neutrality, and idempotent replay.\n\n- `TestGroupReservationPlacementExpiryAndAudit` in `services/inventory/internal/store/reservations_smoke_test.go:25-82`\n  - Verifies `reserve` precedes the later `expire` audit event.\n\n- `TestOperationalConvertIsQuantityNeutralAndAudited` in `services/inventory/internal/store/operational_smoke_test.go:135-211`\n  - Verifies `place`, `convert`, `release` ordering and confirms UPDATE/DELETE remain rejected.\n\n- `TestCapacityAdjustmentIdempotencyAndAudit` in `services/inventory/internal/store/capacity_adjustments_smoke_test.go:127-197`\n  - Verifies ordered capacity audit records, immutable replay results, and append-only enforcement.\n\n- Refund-return replay tests\n  - `services/inventory/internal/store/refund_returns.go:88-101` reads a unique idempotency receipt by organizer and key, so it should remain unaffected by history presentation ordering.\n\nFinally, run the loaded `make check` gate three consecutive times.\n\n## 5. Risks\n\n- **Legacy ambiguity remains bounded.** Rows written before migration cannot receive their true append position. Their same-timestamp order remains the old UUID fallback. The implementation must not claim to reconstruct chronology that the stored data cannot prove.\n\n- **Sequence order is allocation order, not a gapless commit counter.** Rolled-back transactions may consume sequence values. That is acceptable because the requirement is a total order, not gaplessness. For the affected reservation paths, mutations are serialized under the pool lock (`docs/adr/ADR-027-group-agency-reservations.md:40-63`).\n\n- **Replay semantics should not change.** ADR-027 stores staff keys in `claim_history` (`docs/adr/ADR-027-group-agency-reservations.md:64-67`), but the implementation resolves replays by the unique `(organizer_id, idempotency_key)` lookup (`services/inventory/internal/store/operational.go:82-99`). Changing `History` ordering must not change which immutable receipt is returned.\n\n- **All INSERT paths must remain covered.** The sequence trigger should cover `appendHistory`, expiry-sweep inserts, capacity adjustments, refund returns, and direct SQL fixtures. A migration test should verify an explicit `NULL` append order is assigned rather than accepted.\n\n- **Index migration duration.** Rebuilding the two inventory history indexes takes locks and remains subject to the 30-second migration deadline. ADR-022 makes this an out-of-band one-shot job, but it does not remove the deadline (`docs/adr/ADR-022-out-of-band-service-migrations.md:107-121`).\n\n- **Out-of-scope reader.** The access service still has legacy `occurred_at,id` ordering paths, including `services/access/internal/store/postgres.go:228-237` and `services/access/internal/store/lifecycle_checkpoint.go:317`. It must not be changed under TKT-230; that is the deliberate TKT-231/TKT-232 exclusion.\n\n- Other `claim_history` reads found are key-based lookups, existence checks, counts, or aggregates (`services/inventory/internal/store/operational.go:87-99`, `services/inventory/internal/store/refund_returns.go:94-101`, `services/inventory/internal/store/store.go:160-168`, `smoke/onsale_load_test.go:210-211`). They do not require chronological ordering.\n\n## 6. What I could not verify\n\n- I did not run PostgreSQL migrations or the new regression test.\n- I did not reproduce the current failure against a live loaded database.\n- I did not run `make check` three consecutive times.\n- I did not measure the rebuilt index duration or inspect the resulting query plans.\n- I did not verify the actual volume or distribution of pre-existing `claim_history` rows in a deployed database."
    },
    {
      "stage": "plan-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=plan-review kind=summary -->\n### Plan critique — by `main-agent` (per `config.models.planReview`; drafter was `gpt-5.6-luna`, so the pair is cross-model)\n\nAdversarial pass. Every file, symbol, line reference and seam the draft names was checked against\nHEAD `4c995e8`, and the two load-bearing design claims were **tested against a real PostgreSQL**\nrather than reasoned about.\n\n#### Existence checks — all pass\n\n| Draft claim | Verdict |\n|---|---|\n| Action vocabulary incl. `adjust_capacity`, `reserve`, `draw_down`, `refund_return` @ `0010:22-27` | **Confirmed**, exact |\n| `claim_history_pool` index on `(pool_id, occurred_at) WHERE pool_id IS NOT NULL` @ `0006:24` | **Confirmed**, exact |\n| `claim_id` was made nullable by `0006:14` (pool-shaped rows) | **Confirmed** — matters for the test fixture |\n| Append-only trigger is `BEFORE UPDATE OR DELETE`, not INSERT @ `0003:41-50` | **Confirmed** |\n| `appendHistory` supplies `uuid.New()` @ `store.go:102` | **Confirmed** (UUIDv4) |\n| Ordering sites `operational.go:336`, `capacity.go:131` | **Confirmed**, both `ORDER BY occurred_at, id` |\n| Replay resolves by unique `(organizer_id, idempotency_key)`, not by order @ `operational.go:82-99` | **Confirmed** — `registryLookup` is a single-row key lookup |\n| `migrate` is a subcommand with a 30s bound, server never migrates @ `main.go:64-80` | **Confirmed** |\n| Access service retains `occurred_at,id` ordering (out of scope) | **Confirmed** @ `postgres.go:228-237` |\n\nInsert-site sweep run independently: **four** real `INSERT INTO claim_history` sites\n(`store.go` x2, `capacity.go`, `provision_smoke_test.go`); none names an ordering column, so a\ncolumn DEFAULT reaches all of them. The draft's list is complete.\n\n#### Design claims — tested, not assumed\n\n**1. Is the extra `BEFORE INSERT` trigger redundant with the DEFAULT?** The draft proposes both.\nI suspected belt-and-braces. It is not:\n\n```sql\nCREATE SEQUENCE s AS bigint;\nCREATE TABLE t(id uuid PRIMARY KEY, ord bigint DEFAULT nextval('s'));\nINSERT INTO t(id) VALUES (gen_random_uuid());            -- ord = 1\nINSERT INTO t(id, ord) VALUES (gen_random_uuid(), NULL); -- ord = NULL  <-- DEFAULT bypassed\n```\nAn explicit `NULL` **bypasses the DEFAULT**. A future writer that lists the column and passes NULL\nwould silently mint an unordered row. **Accepted as drafted** — the trigger closes a real gap.\n\n**2. Does `NULLS FIRST` actually do the right thing for mixed legacy/new rows?**\n\n```\n action     | ord\n legacy_lo  |        <- legacy rows first, among themselves by UUID (the honest fallback)\n legacy_hi  |\n new_first  |   1    <- new rows after, in true append order\n new_second |   2\n```\n**Confirmed.** Legacy rows keep their existing (arbitrary but stable) relative order and sort\nbefore ordered rows; new rows are correctly sequenced. No interleaving pathology.\n\n#### Verdicts\n\n- **§1 DoD — accept.** Ties the DoD to a test that fails first. Good.\n- **§2 design — accept.** Nullable `bigint` + sequence DEFAULT + `BEFORE INSERT` trigger, ordered\n  `occurred_at, append_order NULLS FIRST, id`. It is the only shape that satisfies ADR-003/ADR-023\n  simultaneously: a `NOT NULL` column would require a backfill `UPDATE`, which the trigger refuses\n  *by design*. The draft reached this itself and said so.\n- **§2 rejected alternatives — accept, and the reasoning is the best part of the draft.**\n  Especially *\"populate legacy rows from `(occurred_at, id)` — rejected because it would canonize\n  the UUID coin flip as if it were historical truth.\"* That is exactly right and is the trap a\n  weaker plan falls into.\n- **§3 files — accept**, with one amendment (below).\n- **§4 test plan — accept the shape, amend the fixture** (below).\n- **§5 risks — accept.** The sequence-gaps point is correctly characterized as acceptable\n  (total order required, not gaplessness). The ADR-027 replay risk is correctly *closed* rather\n  than left open, and I verified the closure independently.\n- **§6 could-not-verify — accept, and it is honest.** All five items are real gaps that the\n  implementation stage closes by running things.\n\n#### Amendments\n\n**A. The test fixture must satisfy `claim_history_shape`.** The draft's fixture inserts two rows\nwith a shared timestamp but does not mention the CHECK constraint added by `0006:20-23`: exactly\none of (claim-shaped, pool-capacity-shaped) per row. A fixture inserting a `reserve` row with both\n`claim_id` and `pool_id` set — or neither — is rejected by the database, and the test fails for\nthe wrong reason. **The fixture must be claim-shaped**: `claim_id NOT NULL`, `pool_id NULL`,\n`target_capacity NULL`. This is the `fixture too small` trap from `AGENTS.md` in its other form —\nhere the fixture would fail to build at all, which is at least loud.\n\n**B. Do not rebuild `claim_history_claim` / `claim_history_pool` unless the query plan needs it.**\nThe draft proposes replacing both lookup indexes with ordered variants. That is speculative: the\nordering columns are a *tie-break*, and both queries already filter to a single `claim_id` /\n`pool_id`, so the row count per key is tiny and a sort over it is free. Rebuilding two indexes\ninside a 30s migration bound is a real cost against no measured benefit, and ADR-020's whole point\nis that this repo does not have the tooling to make index builds cheap. **Amended: add the column,\nthe sequence, the trigger and the unique index on `append_order`; leave the two lookup indexes\nalone.** If a plan regression shows up, that is a separate, measured change.\n\n**C. Add a test that the trigger fires on an explicit NULL.** The draft mentions this under risks\n(\"A migration test should verify an explicit `NULL` append order is assigned rather than\naccepted\") but does not put it in the test plan. Promote it: it is the only thing that proves the\ntrigger, as opposed to the DEFAULT, is doing anything. Without it the trigger is untested code.\n\n**D. Mutation-check the new tests.** Repo rule (`AGENTS.md`, TKT-22): break what each new test\ncovers, observe red, restore. Applies to the ordering test *and* the trigger test.\n\n#### Introduced at plan-review, reviewed by no second model\n\nPer the skill's required section — design elements **I added** rather than accepted or amended\nfrom the draft, which therefore reach implementation reviewed only by their author:\n\n1. **Amendment B (do not rebuild the two lookup indexes).** This is a *subtraction* from the\n   draft, and it is mine. Risk if wrong: a query-plan regression on history reads. Mitigated by\n   being the reversible direction — adding an index later is trivial; an unnecessary index rebuild\n   inside a migration bound is not. Flagged again at the Gate 2 hand-off below.\n2. **Amendment A's specific fixture shape** (claim-shaped, not pool-shaped). Derived from reading\n   `0006:20-23`, but the *choice* of which shape to use is mine.\n\nAmendments C and D are promotions of things the draft already said, not inventions.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan final — delta on the `kind=plan` draft\n\n**The `kind=plan` draft as amended below is the plan.** Not restated; resume reads draft + this\ndelta together.\n\n#### Amendments carried from `agent:plan-review`\n\n- **A.** The regression-test fixture must be **claim-shaped** (`claim_id` NOT NULL, `pool_id`\n  NULL, `target_capacity` NULL) to satisfy `claim_history_shape` (`0006:20-23`).\n- **B.** **Do not** rebuild `claim_history_claim` / `claim_history_pool`. Add only: the nullable\n  `append_order bigint`, its sequence + DEFAULT, the `BEFORE INSERT` trigger, and the partial\n  unique index on `append_order`.\n- **C.** The test plan gains a case proving the **trigger** (not just the DEFAULT) assigns a value\n  when a writer passes an explicit `NULL`.\n- **D.** Every new test gets the mutation check: break what it covers, observe red, restore.\n\n#### Decisions made by the agent (Gate 2 waived by the repo owner)\n\nRecorded per the owner's instruction — the rule given was: prefer what the critique recommends;\nwhere it makes no recommendation, choose the most reversible option.\n\n| # | Decision | Rule applied | Rationale |\n|---|---|---|---|\n| 1 | Nullable `append_order` + sequence DEFAULT + `BEFORE INSERT` trigger; order `occurred_at, append_order NULLS FIRST, id` | critique recommends (accepted draft §2) | Only shape satisfying ADR-003 + ADR-023 together; a NOT NULL column needs a backfill UPDATE the append-only trigger refuses **by design**. Verified against real PostgreSQL. |\n| 2 | Legacy rows keep `append_order IS NULL` and fall back to UUID order | critique recommends | Reconstructing their true order is impossible; inventing one would canonize a coin flip as history and violate ADR-003's reconstruction guarantee. |\n| 3 | **Leave the two lookup indexes alone** | most reversible | The critique introduced this (see \"reviewed by no second model\" above). Adding an index later is cheap; an unmeasured rebuild inside a 30s migration bound is not. |\n| 4 | Migration number `0012` | no recommendation; only correct option | `0011` is the current head (`0011_seat_claim_adjacency.sql`). |\n| 5 | Scope stays at inventory `claim_history`; `access.lifecycle_events` untouched | shaping decision, reaffirmed | It uses the same `ORDER BY occurred_at,id` shape (`postgres.go:228-237`) and may share the defect, but changing it is unscoped work on a second service's audit trail. Named as a deliberate exclusion, not an oversight. |\n\n#### Required section — introduced at plan-review, reviewed by no second model\n\n**Not empty.** Two items, both mine, restated here so they are visible at the (waived) Gate 2:\n\n1. **Amendment B** — the subtraction of the two index rebuilds. The riskiest thing in this plan\n   *because* nobody reviewed it: it is a change to what the draft proposed, made by the reviewer,\n   and the implementation will not re-question it.\n2. **Amendment A's fixture shape** — claim-shaped rather than pool-shaped.\n\nBoth will be exercised by the `agent:ai-review` pass on the resulting diff, which is a different\nmodel again (`gpt-5.6-luna` reviewing `main-agent`'s implementation).\n\n#### Test seam\n\n`services/inventory/internal/store/history_order_smoke_test.go` (new), using the existing\n`storeForTest` harness (`operational_smoke_test.go:19-45`) — per-test schema on\n`INVENTORY_MIGRATION_TEST_DATABASE_URL`, skipped when unset. The tie is forced **deterministically**\nby inserting two rows with one shared `SELECT now()` value and hand-chosen UUIDs (high first, low\nsecond) so today's `ORDER BY occurred_at, id` returns them **reversed**. No `-count=N`, no load\ndependence.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implemented by `main-agent` (per `config.models.implement`)\n\nPR **#188** — `TKT-230-claim-history-total-order` @ `afb5bcf`.\n\n```yaml\nstage: coding\nimplementer: main-agent\ntdd: red observed first on both tests\nmutation_check: both tests, both confirmed red when their subject was removed\nlocal_gate: make check EXIT=0, log body free of failure signatures\nfiles: 4 (+1 migration, +1 test file, 2 one-line ORDER BY changes)\n```\n\n#### What was built\n\n1. **`migrations/0012_claim_history_append_order.sql`** (new) — `claim_history_append_order_seq`,\n   a **nullable** `append_order bigint` with a `nextval` DEFAULT, a `BEFORE INSERT` trigger, a\n   partial unique index, and a guarded down-migration.\n2. **`operational.go:336`** and **`capacity.go:131`** — `ORDER BY occurred_at, append_order NULLS\n   FIRST, id`.\n3. **`history_order_smoke_test.go`** (new) — the two tests.\n\nPer plan-final amendment **B**, the two lookup indexes were **not** rebuilt.\n\n#### TDD evidence — red first, for the right reason\n\nThe first red was the *wrong* red and was fixed before proceeding: the fixture called\n`PlaceOperationalHold` with `purpose=\"hold\"`, which `claims_kind_shape` (migration 0007) rejects\n(`house|artist|kill|other`). With a valid fixture:\n\n```\n--- FAIL: TestHistoryOrdersTiedTimestampsByAppendOrder\n    tied rows returned in append order = [release place], want [place release]\n    — a tie on occurred_at is being broken by the random uuid, not by append order\n--- FAIL: TestClaimHistoryAssignsAppendOrderOnExplicitNull\n    ERROR: column \"append_order\" of relation \"claim_history\" does not exist\n```\n\nThe first is the defect itself, reproduced **deterministically** on a quiet machine — which is\nwhat the ticket demanded and what `-count=N` could never deliver.\n\n#### Mutation checks (repo rule, `AGENTS.md` / TKT-22)\n\n| Test | Mutation | Result |\n|---|---|---|\n| `TestHistoryOrdersTiedTimestampsByAppendOrder` | revert `ORDER BY` to `occurred_at, id` | **red** — `[release place]` |\n| `TestClaimHistoryAssignsAppendOrderOnExplicitNull` | delete the `BEFORE INSERT` trigger | **red** — \"stored as NULL\" |\n\nBoth restored and re-verified green afterwards. Neither test can pass against the code it covers\nbeing absent — which is the property the rule exists to establish.\n\n#### Regression surface\n\nFull `services/inventory/internal/store` smoke suite green (51.7s), including\n`TestGroupReservationRepeatedDrawDownIsQuantityNeutral` — the test whose flake opened this ticket.\n\n#### A note on what this does NOT prove\n\nThe gate ran green **once**, on a quiet machine. COS 4 asks for `make check` green **three\nconsecutive times on a loaded machine**, and that is validated at PO-review (gate 4), not here.\nFixing an ordering ambiguity cannot fix TKT-231's or TKT-232's failures, so a loaded run may still\ngo red on those — that would not be a regression from this change.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review — `gpt-5.6-luna` (per `config.models.aiReview`; implementer was `main-agent`)\n\n**Three passes, and the counter was reset once — legitimately.** The skill caps passes at two per\n*stable* diff, but resets when a pass proves the previous pass's fix wrong at the root. Pass 2 did\nexactly that: it showed the `FOR UPDATE` justification pass 1 rested on was **false**, forcing a\nstructural change (the `occurred_at` default). Pass 3 reviewed that change — the branch's\nleast-reviewed, riskiest commit. Absolute cap is 4; stopped at 3, on a diff that is now stable.\n\nEight findings across three passes. **Six accepted and fixed, two refused with reasons and a\nticket.** Every finding was verified against the revision under review before being actioned.\n\n#### Pass 1 (`afb5bcf`) — 2 findings\n\n| # | Sev | Finding | Verdict |\n|---|---|---|---|\n| 1 | high | Ordering model wrong + fixture too weak | **Split.** Fixture half **accepted** — verified: `PlaceOperationalHold` writes its own `place` row, so `[place(fixture), place(test), release(test)]`, and asserting `tail == [place release]` on names alone still passes if the row under test vanishes. Now asserts row **identity** over the whole history. Model half **refused** — see below. |\n| 2 | medium | Validated CHECK + partial unique index each scan the table under `ACCESS EXCLUSIVE`, inside goose's single transaction, against a 30s bound | **Accepted.** Both removed. |\n\n#### Pass 2 (`8e000b5`) — 3 findings, all [high], all real\n\n| # | Finding | Verdict |\n|---|---|---|\n| 1 | `HistoryID` makes every history response violate its OpenAPI schema; ADR-028 fails closed → three GET endpoints 500 | **Accepted** — and had already been found independently while the review ran. Both handlers do `write(w, 200, entries)` on the *store* struct. Now `json:\"-\"`, verified absent from marshalled output. |\n| 2 | The pool-lock justification is **wrong**: `now()` is fixed at the transaction's first statement, before `FOR UPDATE` is acquired | **Accepted — this one was the most valuable finding of the run.** My justification was wrong and I verified the reviewer was right rather than defending it. **Reproduced against real PostgreSQL:** the transaction that committed **first** stamped `05:40:12.96`; the one that committed **second** stamped `05:40:11.90`. An inversion `History()` would have returned as fact. Fixed at the root: `occurred_at` now defaults to `clock_timestamp()`. |\n| 3 | \"The trigger is the only writer\" is false — it filled in NULLs only, so any INSERT/COPY/restore could supply its own value | **Accepted.** Trigger now **owns** the value unconditionally. Also accepted the reviewer's correct point that `NOT VALID` makes a CHECK scan-free — my previous commit's \"all-or-nothing scan\" claim was wrong. Also fixed from this finding: both trigger-disabling tests now restore via `t.Cleanup`, since a trailing statement is skipped on `t.Fatal`. |\n\n#### Pass 3 (`42f22ca`) — 3 findings, all [high]\n\n| # | Finding | Verdict |\n|---|---|---|\n| 1 | Down migration does not restore `occurred_at`'s default | **Accepted.** Real bug, cheap fix. Down restores `now()`; a new test asserts the default before *and* after, mutation-checked. |\n| 2 | Wall-clock time remains the primary ordering authority and is non-monotonic | **REFUSED → TKT-234.** Correct on the facts. Refused *for this ticket*: pre-existing since migration 0003 and **narrowed** by this change, not opened; the proposed remedy (make `append_order` primary) is the same one pass 2 proposed and this branch **disproved** — `TestHistoryOrdersDistinctTimestampsByOccurredAt` fails against it; and the same assumption governs `access.lifecycle_events`, so fixing inventory alone leaves the platform inconsistent. It is an ADR-level decision. |\n| 3 | No guarded restore path — the trigger renumbers a restore that loads rows out of order | **REFUSED → TKT-234.** Accurate, but there is no restore path in this repo to guard: no backfill, no COPY importer, no documented procedure. Asks for an operational procedure to be designed and tested. The reviewer's own recommendation is recorded verbatim on TKT-234 as the starting point. |\n\n#### The refusals, stated plainly\n\nAn agent overrode a reviewer's **\"Do not ship\"** twice. Both refusals are recorded on **TKT-234**\nso a human can re-take the decision, and neither weakens what this ticket claims: the defect it\nexists to close — a tie on `occurred_at` broken by a random UUID — is closed and proved closed.\n\nWhat would change my mind: evidence that a backward clock step is *reachable* in this deployment\n(finding 2), or the appearance of any real restore path for `claim_history` (finding 3). Neither\nexists today.\n\n#### Tests\n\nSeven tests, all observed **red first** and all **mutation-checked** — including the four written\nmid-fix, where the skill notes the discipline usually lapses:\n\n| Test | Mutation | Result |\n|---|---|---|\n| tied timestamps → append order | revert `ORDER BY` | red |\n| distinct timestamps → `occurred_at` | make `append_order` primary | red |\n| legacy NULL rows sort first | `NULLS LAST` | red |\n| trigger owns the value | weaken to fill-in-NULLs | red |\n| CHECK rejects non-positive | drop the CHECK | red |\n| Down restores the default | drop the restore line | red |\n\n#### Incidentals filed, not fixed here\n\n- **TKT-231** — commerce backfill flake, mechanism unknown.\n- **TKT-232** — `TestMigrationsRanBeforeServicesStarted` measures an ADR-022 guarantee by wall clock.\n- **TKT-233** — `TestExpiredChannelHoldFreesItsCap` races its own 50ms TTL; found during this gate,\n  proved pre-existing (base `4c995e8` 3/3 green, branch 3/3 green).\n- **TKT-234** — the two refused findings above.\n"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "make check is non-deterministic: DB-backed smoke tests fail under machine load, a different test each run. A gate that fails on load cannot certify anything. This ticket closes the ONE mechanism that has been proven; the other observed failures are split out (see scope)."
    },
    "scope": {
      "state": "met",
      "note": "IN: claim_history ordering in inventory — the ORDER BY occurred_at, id pattern at operational.go:336 and capacity.go:131, and whatever total order replaces it. OUT, split to their own tickets because their mechanism is NOT established: TestBackfillOwesEventsForPreExistingCompletedOrders (commerce) and TestMigrationsRanBeforeServicesStarted/payments (a 519ms container-start inversion, a different assertion class). OUT: access.lifecycle_events, which uses the same ORDER BY occurred_at,id shape and may share the latent ambiguity — named here so it is a deliberate exclusion rather than an oversight."
    },
    "uncertainties": {
      "state": "met",
      "note": "RESOLVED at shaping by measurement against a real PostgreSQL. claim_history orders by (occurred_at, id): occurred_at is timestamptz DEFAULT now(), id is uuid.New() = UUIDv4, random, no time component. Measured: 300 inserts in separate serial transactions -> 300 distinct timestamps, 0 collisions. 1200 inserts across 8 CONCURRENT writers in separate transactions -> 1199 distinct, 1 collision ({w4_35, w2_32}, two different writers, identical to the microsecond). A tie is then broken by a random UUID, so History() returns an arbitrary order. That is exactly the observed 'history[0].Action = draw_down, want reserve'. Serial passes, concurrent fails: the idle/loaded split the flake reports. NOTE the order is already TOTAL but not MEANINGFUL, which is why it survived review."
    },
    "dependencies": {
      "state": "met",
      "note": "None open. TKT-198 (migration race) and TKT-229 (quadratic TS fixture) are merged; neither is implicated — zero 'already exists' errors in every observed run."
    },
    "approach": {
      "state": "met",
      "note": "Give claim_history a monotonic tie-break and order by it. Precedent in-repo: access lifecycle_event_integrity.sequence and lifecycle_checkpoints.checkpoint_id are bigserial for exactly this reason (0003_lifecycle_integrity.sql:87,128). Constraint from ADR-003/ADR-023: claim_history is append-only and carries a BEFORE UPDATE OR DELETE trigger (0003_operational_holds.sql:42-50) — the trigger does not fire on INSERT, but any backfill of existing rows would be an UPDATE and is therefore refused by construction. A new NOT NULL column with a sequence default on a table whose existing rows must not be rewritten is the actual design question for the plan. COS forbids widening the assertion instead."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice, one PR: migration adding the monotonic column + switch both ORDER BY sites + a test that FAILS on the current code. The test must force the tie deterministically (insert two history rows with an identical occurred_at and assert the returned order), not rely on -count=N or on load — same discipline as TKT-132/134."
    },
    "success": {
      "state": "met",
      "note": "COS already on the ticket, narrowed to the in-scope half: (1) mechanism identified for the inventory history failures — done at shaping, evidence above; (2) the failure reproduces deterministically before the fix; (3) no assertion widened — a total order in the query, not a laxer test; (4) make check green three consecutive times on a loaded machine."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked below at shaping time, governingAdrs resolved against docs/adr/ at HEAD 4c995e8."
    }
  },
  "context": {
    "binding": [],
    "governingAdrs": [
      {
        "id": "ADR-003",
        "why": "claim_history is one of the append-only trails. Its ordering is a RECONSTRUCTION guarantee — 'any ticket's authoritative history is reconstructible from its trace' — so an ambiguous order is a defect against this ADR, not merely a flaky test. It also forbids fixing the flake by rewriting or reordering existing rows."
      },
      {
        "id": "ADR-023",
        "why": "Defines claim_history as append-only with action/actor/reason/quantities per mutation, and puts staff idempotency keys in it under a unique partial index. The append-only trigger it introduced (BEFORE UPDATE OR DELETE) means a backfill of a new ordering column on existing rows is refused by the database."
      },
      {
        "id": "ADR-027",
        "why": "Group reservations register staff keys in claim_history with actions reserve/draw_down — the exact rows whose order the failing assertion checks. Its staff-op shape is 'pool lock -> claim_history registry replay -> ...', so history order participates in idempotency replay, not just in reporting."
      },
      {
        "id": "ADR-022",
        "why": "Migrations run out-of-band via each binary's migrate subcommand as a one-shot Compose job. A new inventory migration must work under that path; the server path never migrates."
      },
      {
        "id": "ADR-008",
        "why": "Still governs everything ADR-022 did not supersede: goose-as-library, embedded SQL, per-service ownership, fail-fast, 30s bound. The new migration is inventory's."
      }
    ],
    "reference": [
      {
        "summary": "Failing assertion: reservations_smoke_test.go:167, wants [reserve, draw_down x3]",
        "link": "services/inventory/internal/store/reservations_smoke_test.go"
      },
      {
        "summary": "Ordering sites to change: operational.go:336 and capacity.go:131",
        "link": "services/inventory/internal/store/operational.go"
      },
      {
        "summary": "appendHistory supplies uuid.New() (UUIDv4) as the tie-break",
        "link": "services/inventory/internal/store/store.go"
      },
      {
        "summary": "Append-only trigger + schema",
        "link": "services/inventory/internal/store/migrations/0003_operational_holds.sql"
      },
      {
        "summary": "In-repo bigserial precedent for an append-only trail's order",
        "link": "services/access/internal/store/migrations/0003_lifecycle_integrity.sql"
      }
    ],
    "capturedAt": "2026-08-09"
  }
}
