{
  "key": "TKT-108",
  "type": "Task",
  "status": "Building",
  "labels": [
    "agent:ai-review"
  ],
  "assignee": "claude (fable-5 session)",
  "pr": {
    "number": 84,
    "url": "https://github.com/mathd/ticketing_system/pull/84",
    "state": "open"
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ DoR shaped — TKT-108 — all 8 items met/deferred (Gate 1 unblocked)\n\nShaped from the code seams (verified at HEAD: `http.go:87` IncludeResponseStatus:true; **0** `500` decls in the catalog spec; `writeStoreError` default-500 `server.go:225`; test validator `server_test.go:1168`). Self-contained, no blockers, no human decision pending.\n\n| item | state |\n|---|---|\n| objective | met |\n| scope | met |\n| uncertainties | deferred |\n| dependencies | met |\n| approach | met |\n| first_slice | met |\n| success | met |\n| context_memo | met |\n\n**Deferred:** the explicit-500-per-op vs spec-level-`default`-response choice is a plan-time decision against the real spec — no product/architecture unknown. Governing: **ADR-028** (the masking mechanism to satisfy) + **ADR-009** (regen/drift). **Ready to prioritize (Gate 1).**"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-108 (fully autonomous, gateless run)\n\n**Selection reason:** in Ready, zero open blockers (no links on the ticket), all 8 DoR items met/deferred, self-contained scope (catalog spec + regen + test-validator alignment).\n\n**⚠️ GATE WAIVER — recorded per repo-owner directive (2026-07-21):** the owner is deliberately waiving all four human gates for this run as a process experiment. Gate 1: satisfied by Ready status. Gate 2: orchestrator resolves open decisions after cross-model critique (prefer the critique's recommendation; else most reversible), recorded in plan-final. Gate 3: orchestrator squash-merges after ai-review converges, review-guide posted first as audit record. Gate 4: orchestrator self-validates COS against test evidence on merged code, posts validation note + full metrics. Everything else in the skill applies unchanged: config.models routing, cross-model review both stages, TDD red-first, make check green before every push and after merge, all markers, no AI attribution.\n\nBranch: `TKT-108-declare-500-catalog-spec` off `origin/main` (786faa8)."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan drafts — A/B stage (2 arms), gateless run\n\n**A/B ran: 2 arms** (config `models.plan`). Even ticket number → first config arm is **A**. Gate 2 is owner-waived, so per ab-stages §plan.6 the arm mapping is disclosed immediately: **Arm A = `deepseek-v4-pro:high@claudex`**, **Arm B = `gpt-5.6-luna` (codex runner)**. Caveat for the measurement log: the configured arms differ in model AND harness AND effort — the comparison can't attribute wins to a single axis.\n\n**Harness incidents (measurement findings):**\n- Arm A attempt 1+2: proxy 502 `unknown provider for model deepseek-v4-pro` — CLIProxyAPI conf lacked the model mapping (upstream OpenCode Go plan does offer it). Owner approved a config fix (`/opt/homebrew/etc/cliproxyapi.conf` + restart); arm A then ran as configured. A `gpt-5.6-sol` codex fallback was launched between attempts per the claudex-runner ladder and **cancelled unused** (logged as aborted, not scored).\n- Arm B: companion `task` job hung (log stale 35 min, no final answer) → killed → **raw `codex exec -m gpt-5.6-luna -s read-only` fallback** produced the draft. Same model, degraded runner rung; recorded per codex-runner ladder rung 2.\n- **Sandbox breach (repeat of TKT-104's shape):** during the deepseek@claudex draft, a stray fabricated `docs/adr/ADR-030-stripe-behind-psp-port.md` (status \"Accepted\", inventing a PSP decision that is TKT-56's human-gated call) appeared in the tree despite `--allowedTools Read,Grep,Glob`. Quarantined out of the repo; never reached any commit.\n\nBoth drafts converge on **explicit per-operation `500` via a shared `#/components/responses/InternalError`** and reject spec-level `default` (kin-openapi resolves `default` before the `IncludeResponseStatus` gate, so `default` would document ANY status and gut ADR-028's status check; also the ADR-028 coverage gate exempts default-only ops). They diverge on breadth: A = 10 emit-failing operations only; B = every operation that can reach `writeStoreError` (~34 ops, all but `getOpenAPISpec`).\n\n---\n## Arm A draft (deepseek-v4-pro:high@claudex)\n\nNow I have all the data I need. Here's the plan.\n\n---\n\n# TKT-108: Execution Plan\n\n## 1. DoD checklist\n\n- [ ] COS-2 (test validator alignment): `validateResponse` in `server_test.go` sets `IncludeResponseStatus: true` — any undocumented status fails the test suite.\n- [ ] COS-3 (spec declarations): The 10 emit-failing operations declare `'500'` referencing a shared `#/components/responses/InternalError`. The new component uses the existing `Error` schema.\n- [ ] COS-1 (body survives): At least one existing emit-failure test asserts the recovery body (\"retry publish\") is intact — not the generic contract-violation body.\n- [ ] COS-4 (regenerated types): `make generate` committed; `make check-generate` passes (no diff drift).\n- [ ] COS-5 (gate green): `make check` passes.\n\n---\n\n## 2. Decision: per-operation explicit `500` (not `default`)\n\n**Chosen: per-operation explicit `500` via a shared `#/components/responses/InternalError`.**\n\nTrade-off table:\n\n| | Explicit `500` per op | Spec-level `default` |\n|---|---|---|\n| Precision | Only the ops that can 500 | Accepts ANY undocumented status |\n| Maintainability | 10 ops × 1 line each | 1 line total |\n| ADR-028 alignment | Still catches non-500 drift | Masks non-500 drift (a handler returning 201 against a 200-only spec passes validation silently) |\n| `writeStoreError` catch-all | Not covered (separate concern) | Covered automatically |\n\n**Rationale, grounded in kin-openapi v0.142.0 `validate_response.go:48-56`:**\n\n```go\nresponseRef := responses.Status(status)\nif responseRef == nil {\n    responseRef = responses.Default()  // <-- fallback\n}\nif responseRef == nil {\n    if !options.IncludeResponseStatus {\n        return nil\n    }\n    return &ResponseError{...}\n}\n```\n\nA `default` response resolves ANY undocumented status — the validator finds it at line 50, never reaches the `IncludeResponseStatus` gate at line 54, and validates the body against its schema. Since the catalog's error bodies are `{\"error\":\"…\"}` — which matches the `Error` schema — a handler returning 201/302/418 would pass validation silently. That's the opposite of what ADR-028 chose: fail-closed so drift surfaces loudly. Per-operation `500` keeps the fail-closed contract intact for non-500 drift while fixing the 500 masking problem.\n\nThe `writeStoreError` default arm at `server.go:225` is a genuine catch-all that could fire on any write op from an unanticipated store error. That's a separate concern — the ticket scope is emit-failing endpoints, and the default arm on other ops is a \"should never happen\" path. If it does happen, the contract-violation body is at least a signal.\n\n---\n\n## 3. Files to touch\n\n### 3.1 `services/catalog/api/openapi.yaml`\n\n**Add `InternalError` response component** (under `components.responses`, next to `NotFound`):\n\n```yaml\nInternalError:\n  description: Internal server error\n  content:\n    application/json:\n      schema:\n        $ref: '#/components/schemas/Error'\n```\n\n**Add `'500': {$ref: '#/components/responses/InternalError'}` to the `responses` block of these 10 operations:**\n\n| Operation | Path | 500 source in server.go |\n|---|---|---|\n| `publishPerformance` | `/performances/{performanceId}/publish` | :545 |\n| `archivePerformance` | `/performances/{performanceId}/archive` | :570, :577 |\n| `closeSlot` | `/performances/{performanceId}/close` | :779, :791 |\n| `reopenSlot` | `/performances/{performanceId}/reopen` | :779, :791 |\n| `publishSeries` | `/series/{seriesId}/publish` | :606 |\n| `archiveSeries` | `/series/{seriesId}/archive` | :626, :635 |\n| `publishFestival` | `/festivals/{festivalId}/publish` | :677 |\n| `archiveFestival` | `/festivals/{festivalId}/archive` | :697, :706 |\n| `publishSeatMap` | `/seat-maps/{seatMapId}/publish` | :1051 |\n| `editSeatMap` | `/seat-maps/{seatMapId}/edit` | :1098 |\n\n### 3.2 `services/catalog/internal/api/server_test.go`\n\n**a) `validateResponse` (~line 1168): add `IncludeResponseStatus: true`.**\n\nChange:\n```go\ninput := &openapi3filter.ResponseValidationInput{\n    RequestValidationInput: &openapi3filter.RequestValidationInput{\n        Request: req, PathParams: pathParams, Route: route,\n    },\n    Status: rec.Code,\n    Header: rec.Header(),\n    Body:   io.NopCloser(bytes.NewReader(rec.Body.Bytes())),\n}\n```\nTo:\n```go\ninput := &openapi3filter.ResponseValidationInput{\n    RequestValidationInput: &openapi3filter.RequestValidationInput{\n        Request: req, PathParams: pathParams, Route: route,\n    },\n    Status: rec.Code,\n    Header: rec.Header(),\n    Body:   io.NopCloser(bytes.NewReader(rec.Body.Bytes())),\n    Options: &openapi3filter.Options{IncludeResponseStatus: true},\n}\n```\n\n**b) `TestPublishRetriesEmissionAfterFailure` (~line 1319): add body assertion.**\n\nAfter `rec.Code != http.StatusInternalServerError`, add:\n```go\nif !strings.Contains(rec.Body.String(), \"retry publish\") {\n    t.Fatalf(\"recovery body lost — got: %s\", rec.Body.String())\n}\n```\n\nThis is the COS-1 proof: it fails when the ADR-028 validator overwrites the body.\n\n### 3.3 Regenerated files (committed by `make generate`)\n\n- `services/catalog/internal/api/openapi_gen.go` — likely no diff (chi-server doesn't generate response types; models don't include response statuses), but MUST be regenerated and checked.\n- `web/storefront/src/lib/api-types.gen.ts` — likely no diff (no new schemas), but must be regenerated.\n- `web/backoffice/src/lib/api-types.gen.ts` — same.\n\n---\n\n## 4. Test plan & TDD order\n\n### TDD order (COS-2: \"test is written FIRST and observed red\")\n\n**Step 1 — RED: Add `IncludeResponseStatus: true` to `validateResponse`.**\n\nRun: `go test ./services/catalog/internal/api/ -run \"TestPublishRetriesEmissionAfterFailure|TestArchiveRetriesEmissionAfterFailure|TestPublishSeatMapEmitFailureRetries|TestEditSeatMapEmitFailureReturns500|TestCloseRetriesEmissionAfterFailure\"`\n\n**Expected red**: All 5 tests fail because `validateResponse` now rejects the undocumented 500 status. The test output will say `response for POST /performances/.../publish violates the contract: status is not supported`.\n\n**Step 2 — GREEN: Add `InternalError` component + `'500'` to the 10 ops in the spec.**\n\nRe-run same tests. Expected green.\n\n**Step 3 — COS-1 body check: Add the body assertion to `TestPublishRetriesEmissionAfterFailure`.**\n\nWith the spec fix in place, the production `ResponseValidator` no longer rewrites the body → the test sees the original \"retry publish\" body → assertion passes.\n\n### Existing tests that serve as the red→green signal\n\n| Test | File | Line | What it exercises |\n|---|---|---|---|\n| `TestPublishRetriesEmissionAfterFailure` | server_test.go | 1319 | `publishPerformance` 500 |\n| `TestArchiveRetriesEmissionAfterFailure` | server_test.go | 1385 | `archivePerformance` 500 |\n| `TestArchiveEmitsOwedPublishBeforeArchive` | server_test.go | 1400 | `publishPerformance` 500 |\n| `TestArchiveRetryReplaysOwedPublishBeforeArchive` | server_test.go | 1420 | `publishPerformance` + `archivePerformance` 500 |\n| `TestCloseRetriesEmissionAfterFailure` | server_test.go | 2168 | `closeSlot` 500 |\n| `TestCloseEmitsOwedPublishBeforeClosure` | server_test.go | 2118 | `publishPerformance` 500 |\n| `TestArchiveRefusedWhileClosureOwed` | server_test.go | 2148 | `closeSlot` 500 |\n| `TestPublishSeatMapEmitFailureRetries` | seatmap_test.go | 157 | `publishSeatMap` 500 |\n| `TestEditSeatMapEmitFailureReturns500` | seatmap_edit_test.go | 94 | `editSeatMap` 500 |\n\n### Seam: failing publisher injection\n\nThe `fakePublisher` struct at `server_test.go:941-954` has per-operation failure toggles:\n\n| Field | Controls |\n|---|---|\n| `failNext` | `PerformancePublished` |\n| `failArchiveNext` | `PerformanceArchived` |\n| `failClosureNext` | `SlotClosed` / `SlotReopened` |\n| `failSeatMapNext` | `SeatMapPublished` |\n\nSet `e.pub.failNext = true` before calling `e.do(...)` → the next `PerformancePublished` call returns `errors.New(\"nats down\")` → handler writes 500.\n\n### Operations WITHOUT emit-failure test coverage\n\n- `reopenSlot` — uses `emitClosure` which has 500 paths at :779 and :791, but `failClosureNext` toggles `SlotReopened` too. No dedicated test exercises the reopen emit-failure path. Not required by COS (one test proves the mask is lifted), but noted.\n- `publishSeries`, `archiveSeries`, `publishFestival`, `archiveFestival` — no dedicated emit-failure tests. The same pattern applies (`failNext` → 500). Not required by COS.\n\n### What `make check` exercises after the change\n\n`make check` runs: `check-generate` → `lint` (golangci-lint + eslint) → `test` (`go test ./...` + `pnpm test`) → `build` → `smoke` (gateway smoke suite). The smoke suite exercises catalog endpoints directly (not through the gateway's response validator), so it won't hit the ADR-028 mask. The unit tests are the primary validation.\n\n---\n\n## 5. Risks / pre-mortem\n\n1. **The existing tests never check the body.** They assert `rec.Code == 500` but the ADR-028 validator rewrites only the body, not the status. The tests have been green while the recovery message was silently dropped for months. **Mitigation**: add the body assertion to `TestPublishRetriesEmissionAfterFailure` so at least one test catches the regression.\n\n2. **`make generate` may produce no diff on `openapi_gen.go`.** The oapi-codegen chi-server output doesn't materialize response statuses into Go types — only the strict-server generator does. If `openapi_gen.go` is unchanged, `check-generate` passes trivially. No action needed, but verify post-regeneration.\n\n3. **TS types likely unchanged.** Adding response statuses (no new schemas) shouldn't change openapi-typescript output. Verify after `make generate` — if no diff, no commit needed for TS files.\n\n4. **The `writeStoreError` default arm on non-emit-failing operations remains masked.** Operations like `POST /venues` or `POST /events` still have an undocumented 500 via `server.go:225`. If the fakeStore returns an unhandled sentinel in a future test, it will fail on the undocumented 500. **Mitigation**: not in scope for this ticket; the default arm is a genuine \"should never happen\" path on those ops.\n\n5. **`reopenSlot` emit-failure path is untested.** Uses the same `emitClosure` helper as `closeSlot`, so the code path is shared — but no dedicated test exercises `pub.SlotReopened` failing. A regression in the reopen emit path would not be caught. Low risk since the code path is shared and `closeSlot` covers it.\n\n\n---\n## Arm B draft (gpt-5.6-luna via raw codex exec)\n\n# TKT-108 execution plan\n\n## DoD\n\n- [ ] Emit-failure responses retain their original recovery messages through `contract.ResponseValidator`.\n- [ ] `services/catalog/internal/api/server_test.go` validates response status codes with `IncludeResponseStatus: true`.\n- [ ] Every catalog operation capable of returning a handler-level 500 documents `500`.\n- [ ] Regenerate Go and both TypeScript generated outputs; `make check-generate` passes.\n- [ ] `make check` passes.\n- [ ] No recovery messages or production handler behavior change.\n\n## Decision\n\nUse an explicit `500` response referencing a shared `#/components/responses/InternalError`.\n\nRationale:\n\n- `kin-openapi` first checks `responses.Status(status)`, then falls back to `responses.Default()`. A `default` response therefore documents any status, even with `IncludeResponseStatus: true`; it would weaken ADR-028’s fail-closed status protection.\n- An explicit `500` allows the known recovery response while continuing to reject undocumented statuses such as `501`.\n- OpenAPI has no global response declaration; `default` is per operation.\n- `oapi-codegen` is configured only for `chi-server` and `models`, so response declarations should not materially change the generated Go server interface. `openapi-typescript` will add explicit `500` response keys to each operation. Both generators must still run under ADR-009.\n\nThe implementation should inventory the full real status surface, not only the four named handlers. In addition to `PublishPerformance`, `ArchivePerformance`, `PublishSeatMap`, and `EditSeatMap`, `server.go` contains emit-failure 500s in series/festival transitions and close/reopen, and `writeStoreError` is reachable from nearly every catalog operation.\n\n## Files to touch\n\n- `services/catalog/api/openapi.yaml`\n  - Add `components.responses.InternalError`, using the existing `Error` schema.\n  - Add `500: $ref: '#/components/responses/InternalError'` to every operation whose handler can reach `writeStoreError` or emit a domain event:\n    - `createVenue`\n    - `createSeatMap`\n    - `addSeatMapSection`\n    - `addSeatMapRow`\n    - `addSeatMapSeat`\n    - `publishSeatMap`\n    - `editSeatMap`\n    - `updateVenueGaCapacity`\n    - `createEvent`\n    - `createPerformance`\n    - `publishPerformance`\n    - `archivePerformance`\n    - `closeSlot`\n    - `reopenSlot`\n    - `createSeries`\n    - `attachPerformanceToSeries`\n    - `publishSeries`\n    - `archiveSeries`\n    - `createSeason`\n    - `attachSeriesToSeason`\n    - `attachEventToSeason`\n    - `createFestival`\n    - `attachDayToFestival`\n    - `publishFestival`\n    - `archiveFestival`\n    - `createTicketType`\n    - `listPublicEvents`\n    - `getPublicEvent`\n    - `getPublicSeason`\n    - `getPublicFestival`\n    - `listPublicVenues`\n    - `listVenueSeatMaps`\n    - `getPublicSeatMapGeometry`\n    - `listSeatMapVersions`\n  - Do not add it to `getOpenAPISpec`, whose handler does not use the failing store/event paths.\n\n- `services/catalog/internal/api/server_test.go`\n  - Add `Options: &openapi3filter.Options{IncludeResponseStatus: true}` to `env.validateResponse`.\n  - Keep the existing `newEnv` construction unchanged: it already creates `fakeStore` and `fakePublisher`, then mounts `NewRouter`, which includes the production `ResponseValidator`.\n\n- `services/catalog/internal/api/seatmap_test.go`\n  - First change `TestPublishSeatMapEmitFailureRetries` to assert the exact recovery body:\n    `seat map is published but the domain event was not emitted; retry publish`.\n  - Assert that the generic ADR-028 body is not returned.\n\n- `services/catalog/internal/api/server_test.go`\n  - Strengthen existing publish/archive emit-failure tests to assert their original recovery messages where useful:\n    - `TestPublishRetriesEmissionAfterFailure`\n    - `TestArchiveRetriesEmissionAfterFailure`\n  - The existing `fakePublisher.failNext` and `failArchiveNext` flags already provide the required failure injection.\n\n- `services/catalog/internal/api/seatmap_edit_test.go`\n  - Strengthen `TestEditSeatMapEmitFailureReturns500` with the exact edit recovery message, confirming the new-version retry instruction survives validation.\n\n- `services/catalog/internal/api/openapi_gen.go`\n  - Regenerate with `make generate`; likely no semantic change is expected because the configured oapi-codegen targets are server interfaces and models, not response clients.\n  - Commit the regenerated result if the pinned generator produces any output change.\n\n- `web/backoffice/src/lib/api-types.gen.ts`\n- `web/storefront/src/lib/api-types.gen.ts`\n  - Regenerate from the catalog spec.\n  - Confirm affected operation response maps contain `500: components[\"responses\"][\"InternalError\"]`.\n\n## Test plan and test seam\n\nTDD order:\n\n1. Add the exact-body assertion first to `TestPublishSeatMapEmitFailureRetries`.\n2. Run the focused catalog API test and observe red: production `NewRouter` currently masks the body as `{\"error\":\"response violates OpenAPI contract\"}` because 500 is undocumented.\n3. Set `IncludeResponseStatus: true` in `env.validateResponse`, while the spec still lacks 500 declarations.\n4. Run the catalog handler tests and observe failures such as `status is not supported`; this proves the test helper now checks undocumented statuses.\n5. Add the shared `InternalError` response and explicit 500 references.\n6. Regenerate all outputs and rerun the focused tests, then the full gate.\n\nThe failing-publisher seam is already present:\n\n- `newEnv` creates `fakePublisher`.\n- `NewServer(st, pub, ...)` receives that fake.\n- `NewRouter` wraps the generated handler with the real `contract.ResponseValidator`.\n- Tests call `e.do`, which sends requests through the complete production handler stack.\n- Set `e.pub.failSeatMapNext`, `e.pub.failNext`, or `e.pub.failArchiveNext` before the request; each fake failure is one-shot, allowing the following retry assertion.\n\nValidation commands:\n\n- `cd services/catalog && go test ./internal/api`\n- `make check-generate`\n- `make check`\n\n## Risks / pre-mortem\n\n- Only the four named endpoints receive 500 declarations; series, festival, close, reopen, or generic store-error paths remain masked. Mitigation: use the complete operation inventory above.\n- A per-operation `default` is chosen because it is shorter. Mitigation: retain explicit 500 responses so ADR-028 continues rejecting other undocumented statuses.\n- The test helper is aligned but tests bypass the production validator. Mitigation: keep failure tests routed through `newEnv` and `e.do`.\n- Generated files are edited manually or only one frontend is regenerated. Mitigation: run `make generate`, then `make check-generate`.\n- The shared response component has the wrong body schema or content type. Mitigation: reference the existing `Error` schema with `application/json`, matching all current handler error bodies.\n- The status inventory is inferred only from direct `writeStoreError` calls and misses transition helpers. Mitigation: include `writeSeriesTransitionError`, `writeFestivalTransitionError`, and `emitClosure` callers in the audit.\n\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan-final — delta on the kind=plan drafts (Gate 2 waived by owner; orchestrator stood in as judge)\n\n**Selection: Arm B's spine (gpt-5.6-luna) wins**, amended per the fable-5 critique. Reason: B's 34-operation inventory is what the ticket's Scope IN actually requires (\"the writeStoreError default surface\"), verified operation-by-operation by the critique; A's 10-op reading contradicts the scope line. B's regen predictions were also the factually correct ones. A/B mapping was disclosed in the kind=plan comment (waived-gate protocol, ab-stages §plan.6). Logged to ab-tests results (B won, A lost, sol fallback aborted/unscored).\n\n**planReview:** fable-5 (config `fable-5:medium`; note — the Agent harness exposes no per-call effort override, so the critique ran at session-default effort; recorded as a measurement caveat). Critique verified every named symbol/test/line at HEAD, both coverage gates, and vendored kin-openapi v0.142.0 source.\n\n**Amendments adopted (the kind=plan Arm B draft as amended is the plan):**\n1. Import A's concrete diffs with anchors: `validateResponse` Options edit (`server_test.go:1168`) + `\"retry publish\"` body assertion (`TestPublishRetriesEmissionAfterFailure`, :1319).\n2. Import A's red-run test table as the checklist (correct `TestArchiveRetryReplaysOwedPublishBeforeArchive` to :1424).\n3. Import A's `default:`-rejection rationale (kin-openapi `validate_response.go:48-57`: `default` resolves any status before the `IncludeResponseStatus` gate → a 201/418 would pass silently), plus the coverage-gate argument: `default:`-only ops are exempt from the 2xx coverage gate (ADR-028).\n4. Correct B's regen wording: `openapi_gen.go` WILL diff (new `type InternalError = Error` alias); both TS gen files WILL gain `500:` response keys.\n5. Risks rewritten as risks of the chosen plan; pre-mortem folded in (TS typecheck exposure via full gate; single body assertion is the only body-mask guard — status masking is guarded suite-wide by the aligned test validator; a future op #35 can re-open the hole — follow-up, not scope-creep; `reopenSlot` emit path untested — low risk, shared `emitClosure` path; focused-run coverage-gate behavior noted for TDD).\n6. TDD order: TWO separately observed reds — (red 1, COS-1) exact-body assertion on `TestPublishSeatMapEmitFailureRetries` + `\"retry publish\"` assertion fails against the production mask; (red 2, COS-2) `IncludeResponseStatus: true` in the test validator fails emit-failure tests with `status is not supported`; then green via `components.responses.InternalError` + `'500'` on all 34 ops (all but `getOpenAPISpec`); then `make generate`; then full gate.\n\n**Self-made decisions (gateless-run record):**\n- D1: breadth = 34 ops, not 10 — per critique recommendation (waiver rule: prefer the critique's recommendation).\n- D2: explicit `500` via shared `#/components/responses/InternalError`, NOT spec-level `default` — both arms and the critique independently converge; grounded in kin-openapi v0.142.0 source + ADR-028 coverage-gate exemption.\n- D3: stray fabricated ADR-030 from the deepseek arm quarantined to scratchpad, not committed — it invents TKT-56's human-gated PSP decision.\n\n**Introduced at plan-review, reviewed by no second model** (per skill; these reach Building reviewed only by their author, fable-5):\n- The `/internal/*` handlers (`server.go:73-75`) are outside the spec ⇒ outside the mask (closes the inventory-completeness question).\n- Both coverage gates are 2xx-only ⇒ unaffected by 500 declarations.\n- The `default:`-exempts-from-coverage-gate argument.\n- Pre-mortem items: TS typecheck exposure, future-op hole, focused-run gate behavior.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Coding done — PR #84 open\n\n**Implementer:** main-agent (fable-5 session), per config. TDD per plan-final: two separately observed reds — (1) recovery-body assertions on 3 emit-failure tests failed against the production mask (`response violates OpenAPI contract`); (2) `IncludeResponseStatus: true` in the test validator failed archive/close tests with `status is not supported` — then green via `components/responses/InternalError` + `'500'` on 34/35 operations (all but `getOpenAPISpec`).\n\n**Diff:** 7 files, +183/−0 (spec, 3 test files, Go gen +3, 2× TS gen +43 — regen exactly as plan-final predicted). Commit `d82665a`.\n\n**Gate:** `make check` EXIT=0 post-commit (first run failed as expected pre-commit: `check-generate` diffs gen files against the index). Judged from sentinel file + log body per skill rules.\n\nStage: coding → ai-review (A/B, 2 arms per config)."
    }
  ],
  "summary": "Declare 500 (or a shared default) on emit-failing catalog endpoints so ADR-028 doesn't mask recovery messages",
  "description": "### Incidental finding spun out of TKT-105 ai-review (deepseek-v4-pro@claudex, confirmed against HEAD)\n\n**Problem:** every catalog endpoint that can return HTTP 500 on a domain-event emission failure (`PublishSeatMap`, `PublishPerformance`, `EditSeatMap` [TKT-105], and any `writeStoreError` default) returns a **500 that is not declared in `services/catalog/api/openapi.yaml`**. The ADR-028 response validator (`shared/go/contract/http.go:87`, `IncludeResponseStatus: true`) treats an undocumented status as drift and **overwrites the handler's body** with `{\"error\":\"response violates OpenAPI contract\"}` — so the actionable recovery hint (e.g. 'retry by publishing the new version') never reaches the client.\n\n**Scope:** repo-wide and pre-existing — 0 of the catalog spec's endpoints declare a 500. TKT-105 did not introduce it and deliberately did NOT fix only its own endpoints (that would leave publish inconsistent). Fix once, consistently: either declare a `500` response (shared `#/components/responses/InternalError`) on every emit-failing operation, or add a spec-level default response, and regenerate. Then the recovery messages survive.\n\n**Also fold in (same area, TKT-105 ai-review incidental #E):** the catalog handler test helper `validateResponse` (`server_test.go`) does NOT set `IncludeResponseStatus: true`, so it passes on undocumented statuses that production rejects — which is why the masked-500 gap was invisible to tests. Align the test validator with production so this can't recur silently.\n\n**Acceptance:** an emit-failure 500 (unit test with a failing publisher) reaches the client with its original recovery body intact through the production ResponseValidator; the handler test validator checks status codes; `make check` green.\n\nSource: TKT-105 PR #82 ai-review, finding #1 (+#3). Governing: ADR-028 (response drift fail-closed), ADR-009 (contract-first).",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Emit-failure 500s on catalog endpoints are undeclared in the OpenAPI spec, so the ADR-028 response validator (shared/go/contract/http.go:87, IncludeResponseStatus:true) overwrites the handler's actionable recovery body with the generic contract-violation message — the client never sees how to recover. Repo-wide (0 of the catalog spec's endpoints declare 500)."
    },
    "scope": {
      "state": "met",
      "note": "IN: declare a 500 response consistently on catalog operations that can emit-fail (PublishSeatMap, PublishPerformance, EditSeatMap, and the writeStoreError default surface) — via a shared #/components/responses/InternalError OR a spec-level default response; regenerate Go+TS types; align the catalog handler test helper validateResponse (server_test.go:1168) to set IncludeResponseStatus:true so undocumented statuses fail tests too. OUT: changing the actual recovery behavior/messages (only making them survive the validator); other services' specs (catalog only — same pattern applies elsewhere but is separate work)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Design choice — per-operation explicit 500 vs one spec-level default response — is a plan-time decision, settled against the real spec/handlers; no product or architecture unknown. Whether adding 500 surfaces any secondary drift is a build-time test question, not a blocker."
    },
    "dependencies": {
      "state": "met",
      "note": "No open blockers. Touches only shared/go/contract (validator, unchanged) and services/catalog (spec+handlers+tests). All seams verified at HEAD: http.go:87 IncludeResponseStatus:true; 0 '500' decls in the catalog spec; writeStoreError default-500 at server.go:225; test validateResponse at server_test.go:1168."
    },
    "approach": {
      "state": "met",
      "note": "Add a reusable 500 response to services/catalog/api/openapi.yaml (either a shared components/responses/InternalError referencing the Error schema, or a `default` response) and attach it to every operation whose handler can reach a 500 (emit-failure paths + writeStoreError default). `make generate` → commit regenerated Go (openapi_gen.go) + TS (backoffice+storefront api-types.gen.ts) per ADR-009; check-generate must pass. Set IncludeResponseStatus:true in the test env's validateResponse so a future undocumented status fails the handler tests. Add a unit test with a failing publisher asserting the ORIGINAL recovery body survives through the production ResponseValidator."
    },
    "first_slice": {
      "state": "met",
      "note": "The full fix is one sprint-sized slice: spec 500 declaration + regen + test-validator alignment + one failing-publisher assertion that the recovery body survives. Not decomposable further without leaving publish/edit inconsistent (the whole point)."
    },
    "success": {
      "state": "met",
      "note": "COS: (1) an emit-failure 500 (unit test, failing publisher) reaches the client with its ORIGINAL recovery body intact through the production ResponseValidator — not the generic 'response violates OpenAPI contract'; (2) the catalog handler test validator checks status codes (IncludeResponseStatus:true), so an undocumented status fails a test; (3) every emit-failing catalog operation declares 500 in the spec; (4) regenerated types committed, check-generate passes; (5) make check green."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked: ADR-028 (response drift fail-closed — the mechanism being worked around) and ADR-009 (contract-first, regen + drift gate). governingAdrs present."
    }
  },
  "context": {
    "binding": [
      {
        "id": "ADR-028",
        "rule": "The response validator fails closed on drift, INCLUDING an undocumented status (IncludeResponseStatus:true): a status not in the spec is overwritten with a generic contract-violation body. This is the exact mechanism to satisfy by declaring 500.",
        "date": "2026-07-20"
      },
      {
        "id": "ADR-009",
        "rule": "Contract-first: the OpenAPI doc is the source of truth; Go+TS types are generated and committed; make check-generate fails on drift. A new/changed response requires regenerating all affected gen files.",
        "date": "2026-07-12"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-028",
        "why": "The fix is precisely to make the declared response set match reality so the fail-closed validator stops masking the 500 body. Read it before deciding explicit-500 vs default response."
      },
      {
        "id": "ADR-009",
        "why": "Any spec change means regenerate + commit Go (openapi_gen.go) and both TS gen files (backoffice+storefront); the drift gate enforces it."
      }
    ],
    "reference": [
      {
        "summary": "Origin: TKT-105 PR #82 ai-review finding #1 (masked-500) + #3 (test validator ignores status)",
        "link": ".sdlc board TKT-105"
      },
      {
        "summary": "The validator + the exact masking behavior",
        "link": "shared/go/contract/http.go:87"
      },
      {
        "summary": "writeStoreError default-500 + emit-failure 500 sites",
        "link": "services/catalog/internal/api/server.go:225"
      }
    ],
    "capturedAt": "2026-07-20"
  }
}
