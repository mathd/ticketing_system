{
  "key": "TKT-249",
  "type": "Task",
  "summary": "Make inventory migration 0016 online — it needs a quiesced cutover today (TKT-246)",
  "status": "Done",
  "labels": [],
  "assignee": null,
  "pr": null,
  "parent": null,
  "links": [],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n**Shaping verdict: NOT ready — two items `open`, both `owner: \"human\"`, both the same decision.**\n\nThe run was requested as \"move to Ready and run to Done\". Read the board first: TKT-249 was in **Backlog** with `readiness: null`, no context-mémo and no comments, so it was shaped rather than entered at Ready.\n\n**Items met (6/8):** `objective`, `scope`, `dependencies`, `first_slice`, `success`, `context_memo`.\n\n**Items open (2/8):** `approach` and `uncertainties` — one decision wearing two labels.\n\n### Why `approach` is held rather than `deferred`\n\nThe DoR's carve-out is explicit: `approach: deferred` does **not** pass when the ticket touches data migrations. This ticket *is* a data migration. So it holds at Gate 1 with `owner: \"human\"`.\n\n### The proposed remedy, verified rather than inherited\n\nThe ticket text proposes splitting the column add from the index builds and building the indexes `CONCURRENTLY`, and names ADR-020's preconditions as the obstacle. Checked against the code:\n\n- **Precondition (1)** — migrations decoupled from startup — **satisfied** (ADR-022).\n- **Precondition (2)** — a second replica or external writer — **still false.** `compose.yaml` runs inventory single-instance with no `replicas`; `deploy/` holds one bootstrap SQL file; `docs/conventions/continuous-delivery.md` states Compose is the only runtime topology.\n- **Precondition (3)** — a populated target table — **still false.** Nothing is deployed.\n\nThey are conjunctive. So adopting CIC today buys the same nothing ADR-020 already reasoned through, while taking on its named traps: `NO TRANSACTION` applies per file to Up *and* Down; the 30s deadline at `services/inventory/cmd/inventory/main.go:72` **is** propagated to goose's non-transactional branch and must be removed in the same change, never before; a cancelled CIC leaves an **INVALID** index that `IF NOT EXISTS` silently accepts.\n\nThat does not make the ticket wrong — it makes its own COS accurate: *\"either ADR-020 has to be revisited or the approach has to avoid it, and either is a decision this ticket owns rather than inherits.\"*\n\n### The choice the human owns at Gate 1\n\n- **(a) Amend ADR-020** to scope CIC adoption to inventory ahead of preconditions (2)/(3) — pay the traps now for a benefit that arrives with production.\n- **(b) Split without CIC** — column add in one migration, indexes in another. Shorter locks, honest about not being fully online. Does not touch ADR-020's decision.\n- **(c) Rule it premature** — correct `0016`'s deploy note so the cutover requirement stops reading as a defect, record the trigger, close.\n\n### Two findings the plan will need either way\n\n1. **0016 is already applied and cannot be edited in place.** `smoke_test.go:263` derives the expected goose version from migration *filenames*, and 0016 is recorded as applied in every database. Forward migrations are the only mechanism.\n2. **COS 4 (\"measured, not asserted\") has no fixture today.** No populated `claims` table exists anywhere, so the measurement requires constructing one. That is real work, not a verification step.\n\n### One live tripwire the mémo now carries\n\n`channel_allocation_seller_smoke_test.go:336` pins that a partner's key is stored **verbatim** and the namespace is the `reseller_scope` **column**, never a Go-side decoration. The obvious \"schema-tolerant read\" fallback — decorate the key when the column is missing — is precisely the DoS TKT-246 removed. A plan that reaches for it would be correct against every ADR it cites and still wrong.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n**Closed without code.** Gate 1 decision by the owner: option **(c)** — the ticket's premise is premature; correct the record and close. Resolved as internal maintenance per `SKILL.md` § *What this pipeline is for* (writing down a decision → direct commit, no PR, no gates), not routed through Planning/Building.\n\nCommit: `9fb27d75` on `main`. `make check` green (sentinel `EXIT=0`, log body checked for failure markers separately).\n\n### Why (c) rather than (a) or (b)\n\nADR-020's preconditions are **conjunctive**. (1) satisfied by ADR-022; **(2)** a second replica or external writer and **(3)** a populated target table are both still false for inventory — single-instance under Compose, `deploy/` is one bootstrap SQL file, nothing deployed.\n\n- **(a) amend ADR-020 and adopt CIC** — takes the ADR's named traps (`NO TRANSACTION` applies to Up *and* Down; the 30s bound at `services/inventory/cmd/inventory/main.go:72` reaches goose's non-transactional branch and must be dropped in the same change; a cancelled build leaves an INVALID index `IF NOT EXISTS` silently accepts) for a benefit that arrives only with production. Re-opening ADR-020 should be triggered by its preconditions turning true, not by a review pass rediscovering the migration is slow.\n- **(b) split without CIC** — two shorter ACCESS EXCLUSIVE windows are still not online. Changes the migration's shape and no observable property, then gets rewritten again by whoever actually adopts CIC. Inert-mechanism shape.\n- **COS 4 was unsatisfiable as written** — \"measured against a representative populated `claims` table, not asserted\". No such table exists and no workload defines \"representative\", so satisfying it meant measuring against an invented fixture and reporting the number as evidence.\n\n### What shipped\n\n1. **`0016`'s deploy note** — its re-open trigger was prose with no ticket behind it (\"that is a ticket, not a tweak\" — and there was none). Now names the two conditions that reverse the decision, records that (a) and (b) were considered and why they were rejected, and warns that the migration is already applied everywhere while `smoke_test.go:263` derives the expected goose version from **filenames** — so the fix is forward migrations, never an edit to that file.\n2. **ADR-020** — titled and reasoned for *catalog*, yet `0016` cites its preconditions and they are the repo's test for adopting CIC **anywhere**. Amended to say so, plus a `References` entry. A reader checking inventory no longer finds an ADR that never mentions it.\n\nComments only; goose does not checksum, so no applied database is affected (verified: no non-comment lines in the migration diff, no checksum logic in the inventory migrate path).\n\n### A correction worth recording\n\nMy initial recommendation argued the note \"says a quiesced cutover *is required*, present tense, as if it were a live constraint\" — inherited from the ticket's own COS. Reading the file showed the note **already** scoped the claim: it says the cutover is acceptable today, gives both reasons, and says when it stops being acceptable. The ticket's characterization was true but incomplete, and I repeated it in the paragraph where I was recommending a course of action. Caught before the edit, but it is exactly the shaping rule's *\"a remedy proposed in the ticket text is a hypothesis, not a finding\"* — applied to a **characterization of existing code**, not just to a proposed remedy.\n\n| stage | duration |\n|---|---|\n| Backlog (created → shaped) | n/a (created 2026-08-24 earlier this session) |\n| Backlog shaping → Gate 1 decision | one session |\n| Ready / Planning / Building / PO Review | **not entered** — closed from Backlog |\n\n- **human-wait total:** one Gate 1 decision (the only gate that is human in both modes; not waived).\n- **diff:** +29/−1 (2 files). No PR — direct commit per the maintenance routing rule.\n- **overrides:** `none`. No gate was waived: Gate 1 was taken by the owner, and Gates 2–4 were never reached because the ticket closed without code. Nothing was overridden against a stated objection.\n\n### learnings:\n\n1. **A \"make X online\" ticket whose service has no deployment is a decision ticket, not a work ticket** — target house: `docs/learnings/`. The shape recurs: an ai-review pass records an accepted production cost, a follow-up ticket is filed to remove it, and the follow-up is unbuildable because the conditions that make the cost real do not exist yet. The correct output is a sharpened trigger, not a migration. **Not promoted to `AGENTS.md`** — ADR-020 already carries the reasoning, and the rule here is \"cite, never restate\".\n2. **An ADR whose title names one service can be the repo-wide test without saying so** — target house: none, fixed in place. ADR-020 was being applied cross-service by convention with nothing recording it. Worth watching for in other ADRs; not yet a pattern.\n\n### retro:\n\n**(1) What would have made this ticket faster or better?**\nThe ticket was handed over as \"move it to Ready and run it\" while sitting in Backlog with `readiness: null`. The skill's verify-the-status rule caught it, and that check is what surfaced the whole decision — entering at Ready would have produced a plan for work that should not happen. **The rule earned its place; no change needed.** The one concrete gap: the ticket's COS asserted a characterization of existing code (*\"the note currently says a quiesced cutover is required\"*) that was incomplete, and the shaping rule about verifying proposed remedies does not obviously extend to verifying **claims about what the code already says**. → *Concrete change:* in `references/shaping.md`, the paragraph beginning *\"A remedy proposed in the ticket text is a hypothesis\"* should add one sentence: a ticket's description of what existing code or comments **currently say** is equally a hypothesis, and quoting the file beats paraphrasing the ticket. **Not applied** — offered to the owner below.\n\n**(2) What should the humans change?**\nNothing in the config or the process. One observation: this ticket was filed by an ai-review pass 3 `[high]`, and review passes are good at spotting an accepted cost but have no way to judge whether the conditions making it costly exist. → *Concrete change (optional):* when a review finding is converted into a ticket, the ticket could carry a `trigger:` line stating what must become true for the work to be worth doing. That is a `decomposition.md` addition, not a skill rule. **Not applied** — low confidence it generalizes from one instance.\n"
    }
  ],
  "description": "**The accepted cost of TKT-246's `claims.reseller_scope` migration, recorded so it is\nfindable before it becomes an incident.**\n\n`0016_claim_reseller_scope.sql` is **not online and not expand/contract** (ADR-022). goose runs it in\none transaction: `ADD COLUMN` takes ACCESS EXCLUSIVE and holds it to commit, and two full unique\nindex builds happen inside that lock — so **readers** of `claims` block for the duration, not just\nwriters, against ADR-008's 30s bound.\n\nThe binaries are also incompatible in both directions across it:\n- new binary on schema 0015 → every hold 500s (it selects `reseller_scope`)\n- previous binary on 0016 → writes NULL scope and decorates the key itself, so a partner retry\n  misses its row and places a **second hold**\n\n**This was accepted for TKT-246 only because the partner write path shipped in that same ticket** —\nthere are no partner claims in any database yet, so the second-hold window has nothing to act on,\nand every current `claims` table is small. Neither condition survives contact with production\npartner volume.\n\n**Conditions of Success:**\n- The column add is separated from the index builds, and the indexes are built outside a\n  transaction. Note **ADR-020's preconditions for `CREATE INDEX CONCURRENTLY` are still not met** —\n  that ADR has to be revisited or the approach has to avoid it, and either is a decision this ticket\n  owns rather than inherits.\n- One release where the binary tolerates both schemas, so a rolling deploy is possible.\n- The deploy note at the top of `0016` is updated or deleted — it currently says a quiesced cutover\n  is required, and that statement must not outlive the constraint.\n- Measured against a representative populated `claims` table, not asserted.\n\nSurfaced by TKT-246's ai-review pass 3 [high]. Non-goals: changing the namespace semantics.",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "0016 is a quiesced-cutover migration (ACCESS EXCLUSIVE held across ADD COLUMN + two unique index builds, in one goose transaction) with a two-way binary incompatibility. Verified at HEAD: services/inventory/internal/store/store.go:470 selects reseller_scope, so a new binary on schema 0015 errors on every hold. Accepted for TKT-246 only because no partner claims exist yet; neither the empty-table nor the no-partner-volume condition survives production."
    },
    "scope": {
      "state": "met",
      "note": "IN: new inventory migrations that make the reseller_scope index topology reachable online; a schema-tolerant read for one release; correcting/removing 0016's deploy note; a measurement against a populated claims table. OUT: 0016 is already applied in every database and is NOT edited in place (goose version history is immutable; smoke_test.go:263 latestMigrationVersion derives from filenames, so forward migrations are the only mechanism). OUT: namespace semantics (ticket's stated non-goal). OUT: making other services' migrations online."
    },
    "uncertainties": {
      "state": "met",
      "note": "Same decision as approach; resolved with it."
    },
    "dependencies": {
      "state": "met",
      "note": "No open blockers, no blocked-by links. TKT-246 (which shipped 0016) is merged. Depends on ADR-020 and ADR-022 as decisions, not as work."
    },
    "approach": {
      "state": "met",
      "note": "RESOLVED at Gate 1 by the owner: option (c) — rule the ticket premature, correct the record, close without code. Rationale in the kind=metrics comment."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice: the migration split + schema-tolerant read + deploy-note correction + the measurement, all in inventory. Nothing to defer to a second ticket."
    },
    "success": {
      "state": "met",
      "note": "The ticket's four COS are concrete and each has an observable: indexes built outside a transaction; one release where the binary tolerates both schemas; the deploy note updated or deleted; a measurement rather than an assertion. Note COS 2 needs a test that can actually fail — a schema-tolerant read is exactly the shape where a test seeds the NEW schema and proves nothing about the OLD one."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked below. governingAdrs and governingTests both populated; every cited ADR verified to resolve at HEAD via git cat-file."
    }
  },
  "context": {
    "governingAdrs": [
      {
        "id": "ADR-020-catalog-index-build-concurrency.md",
        "constrains": "THE decision this ticket must either satisfy or amend. CIC is NOT adopted; preconditions are conjunctive and (2)/(3) are still false at HEAD (verified against compose.yaml and docs/conventions/continuous-delivery.md). Its 'Traps for whoever adopts it' section is a checklist this ticket must answer item by item if it chooses CIC: NO TRANSACTION is per-file (Up AND Down), the 30s deadline reaches the non-transactional branch and must be removed in the SAME change, a cancelled CIC leaves an INVALID index that IF NOT EXISTS hides, recovery needs plain DROP INDEX. Also: 'scope any adoption to indexes added to PRE-EXISTING tables' — claims is pre-existing, so that clause is satisfied. The ADR is written about catalog; extending it to inventory is itself an amendment, not an inheritance."
      },
      {
        "id": "ADR-022-out-of-band-service-migrations.md",
        "constrains": "Migrations run as a one-shot Compose job (inventory-migrate) that the service waits on via service_completed_successfully; the server path never migrates. Any new migration this ticket adds runs there, not at startup. The 30s deadline and fail-fast are explicitly retained ('The 30s deadline stays… this is not that change'), so removing it is an ADR-022 amendment too. Its three gate assertions constrain what a new migration may break."
      },
      {
        "id": "ADR-008-embedded-migrations.md",
        "constrains": "goose-as-library, SQL embedded via embed.FS, per-service schema ownership, fail-fast, 30s bound. Superseded on PLACEMENT ONLY by ADR-022 — everything else stands. The 30s bound is ADR-008's, which is why relaxing it touches two ADRs."
      }
    ],
    "governingTests": [
      {
        "path": "smoke/smoke_test.go:304 TestMigrationsAppliedOutOfBand",
        "invariant": "Every service database is at the highest goose version derivable from its checked-in migration FILENAMES (latestMigrationVersion, smoke_test.go:263). Adding 0019/0020 to inventory automatically raises the expected version — so a new migration that fails to apply fails this, and a migration file added but not applied is caught. This is the tripwire that makes editing 0016 in place useless: the version is derived from names, and 0016 is already recorded as applied."
      },
      {
        "path": "smoke/smoke_test.go:567 TestMigrationsRanBeforeServicesStarted",
        "invariant": "Each migrate job exited 0 AND its service was created declaring service_completed_successfully on that job, required:true — read from the container's depends_on label plus the merged compose config. A migration that fails, or a compose edge weakened to make a slow CIC build tolerable, fails here."
      },
      {
        "path": "smoke/smoke_test.go:622 TestServerModeDoesNotMigrate",
        "invariant": "Catalog in server mode against an empty database never creates goose_db_version. Constrains the shape of any 'schema-tolerant read' solution: tolerance must live in the QUERY, never in a startup-time schema probe that migrates or repairs."
      },
      {
        "path": "services/inventory/internal/store/channel_allocation_seller_smoke_test.go:336",
        "invariant": "A partner's idempotency key is stored VERBATIM and the namespace is the reseller_scope COLUMN, never a decoration on the caller's key. This is TKT-246's authorization fix, pinned. A schema-tolerant read that falls back to Go-side key decoration on the old schema would re-introduce exactly the DoS this test exists to prevent — it is a live tripwire against the obvious fallback."
      },
      {
        "path": "scripts/check-adr-numbers.sh",
        "invariant": "ADR numbering/registry consistency — relevant because this ticket may amend ADR-020 and ADR-022 rather than author a new one."
      }
    ],
    "notes": "The two-way incompatibility is asymmetric and both directions are verified at HEAD. Forward (new binary, old schema): store.go:470, seat_claims.go:713 and :1588 all name reseller_scope in SQL, so every hold errors — a hard, loud failure. Backward (old binary, new schema): the old binary writes NULL scope and decorates the key in Go, so a partner retry misses its row and places a SECOND hold — silent, and the one that costs money. A schema-tolerant read only addresses the forward direction; the backward direction is closed by ordering (migrate before the old binary is gone is exactly what must NOT happen), which is a deploy-sequencing constraint, not code."
  }
}
