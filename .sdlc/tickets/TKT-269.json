{
  "key": "TKT-269",
  "type": "Story",
  "parent": "TKT-19",
  "classification": "complex",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "url": "https://github.com/mathd/ticketing_system/pull/313",
    "number": 313,
    "state": "merged"
  },
  "summary": "An offline admit of a refunded ticket raises an admission-conflict alarm instead of passing silently",
  "description": "Split out of **TKT-162** at shaping (2026-08-22) as the half that needs **no product decision**.\n\nTKT-162 has two branches in its AC1: *\"a scanner that went offline before a refund, and syncs after it, does not admit the refunded holder — **or**, if it already did, the operator is told through a channel that means **this** rather than *the chain is suspect*.\"*\n\nThe **first** branch (a revocation feed pushed to scanners) is blocked on an undecided product call — how stale a scanner's view may be, and whether it fails open or closed past that. The **second** branch is not blocked on anything: the channel already exists, and nothing is asking for it to be used.\n\n**Today an offline admit of a refunded ticket is silent.** `ReconcileAdmission` (`services/access/internal/store/reconcile.go:73`) never consults `ticketRefunded`. The occurrence is appended as an ordinary `redeemed` event, `access verify-lifecycle` verifies clean, and the trail reads as a normal admission. Nobody — operator, organizer, finance — ever learns the refunded holder walked in. TKT-157 closed the live path (`postgres.go:319` single-entry, `scan.go:95` pass); this is the offline half, and ADR-038 §4 says so explicitly.\n\nThis ticket makes it **visible**. It does not make it *stop* — stopping it is TKT-162's feed and needs the staleness decision.\n\n**What it must NOT do — the constraint that shapes the whole ticket.** Reconciliation is *recording, not deciding* (ADR-025 §D2/§D6). The person is already physically inside. Refusing to record the occurrence, or downgrading it so the trail looks tidier, would falsify the trail rather than protect a gate — ADR-038 §4 rejected exactly that, and TKT-162's AC2 restates it. **An alarm is additive: the occurrence is recorded exactly as it is today, and the alarm is owed alongside it.**\n\n**Why the admission-conflict class and not the integrity class.** `oweAlarm` writes the **integrity** channel, which means *the chain is suspect*. A refunded-but-admitted ticket is not an integrity event — the chain is perfectly valid, the world disagreed with it — and overloading that channel would also make TKT-147 (replacing its free-text reason with fixed reason codes) worse. `SubjectAdmissionConflictAlarm` / `platform.access.admission-conflict.alarm` (`reconcile.go:18`) is the class that already means precisely this, and `oweConflictAlarm` (`reconcile.go:309`) is the existing shape to follow — it commits the alarm into the shared outbox **in the same transaction** as the append, which is what makes the alarm and the fact inseparable.\n\n**Acceptance Criteria:**\n- [ ] Reconciling an occurrence for a **refunded** ticket owes an admission-conflict alarm on `platform.access.admission-conflict.alarm`, committed in the **same transaction** as the append — never one without the other.\n- [ ] The occurrence is still **recorded exactly as it is today**. No occurrence is dropped, refused, or downgraded to make the trail look better (ADR-025 §D2, ADR-038 §4, TKT-162 AC2). A test asserts the lifecycle row is present and unchanged in shape.\n- [ ] `access verify-lifecycle` verifies clean afterwards, and the canonical golden literals are **unchanged** — the alarm rides the outbox, nothing new enters the canonical form (ADR-025 §D8).\n- [ ] A **replay** of the same occurrence (`ReconcileSynced`) owes **no second alarm** — the existing replay-before-everything ordering (`reconcile.go:124`) must keep holding.\n- [ ] The refunded case is covered on **each branch it can reach**, not just the convenient one — see the plan note below; a single test proves one branch and is silent about the others.\n\n**Explicitly out of scope** (TKT-162 keeps these): the revocation feed endpoint, any scanner-side change, the staleness ceiling, and any fail-open/fail-closed behaviour at a gate. Nothing in this ticket changes what a scanner does.\n\nQuality gates: `make check` green; `access verify-lifecycle` green; contract tests.",
  "links": [
    {
      "type": "split-from",
      "key": "TKT-162"
    }
  ],
  "context": {
    "capturedAt": "2026-08-22",
    "binding": [
      {
        "id": "PRD-v1",
        "date": "2026-08-22",
        "rule": "Traceability is a must, for money and tickets: every ticket/entitlement state change is an append-only lifecycle event linked to its order."
      }
    ],
    "reference": [
      {
        "summary": "Parent ticket and the branch this splits from",
        "link": "TKT-162"
      },
      {
        "summary": "Service layout and ownership",
        "link": "docs/architecture.md"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-025",
        "why": "§D2/§D6 are the whole frame. Reconciliation is RECORDING, NOT DECIDING — it cannot retroactively deny, so every occurrence still lands. §D6 defines the admission-conflict alarm class as deliberately separate from integrity alarms (the chain is valid; the world disagreed). §D2 says pass reconciliation NEVER mints conflict events — which is why a pass slot is excluded from this ticket's alarm, not an oversight. §D8: no new field enters the canonical form; reasons live in the alarm payload."
      },
      {
        "id": "ADR-038",
        "why": "§4 is the ticket's direct lineage: it states that a refunded ticket is refused by a LIVE gate only, that an offline scanner will still admit it, and that refusing to RECORD the admission would falsify the trail rather than protect a gate. It also explicitly rejected raising an INTEGRITY alarm for this case. Read it before touching the reconcile path."
      },
      {
        "id": "ADR-021",
        "why": "Every lifecycle event goes through store.appendLifecycle — the verifier asserts one-to-one coverage, so a direct INSERT into lifecycle_events reads as tampering and fails `access verify-lifecycle` in the gate. Canonical forms are pinned by golden literals. Also: NAME THE ADVERSARY — this alarm is honest-writer visibility, not tamper-evidence; a writer with database access can delete the refunded row."
      },
      {
        "id": "ADR-017",
        "why": "If the conflict alarm PAYLOAD changes, bump on CONSUMER SEMANTICS, not parse compatibility, and consumers dispatch on `schema` BEFORE decoding `data`. The admission-conflict alarm is a schema-1 contract. A fixture built from the type under test cannot fail."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first. IF the plan chooses to add a wire outcome value, openapi.yaml changes first, then openapi_gen.go is regenerated AND COMMITTED before the gate runs — check-generate diffs against HEAD, so an uncommitted regen reads as drift. If the plan takes the recommended contract-free route, this ADR simply does not bind."
      },
      {
        "id": "ADR-028",
        "why": "Fail closed: responses are validated against the spec and an undeclared status code becomes a 500. Relevant only if the wire shape changes; the reconcile route must keep answering exactly the statuses it declares."
      }
    ],
    "governingTests": [
      {
        "id": "services/access/internal/store/occurrence_smoke_test.go",
        "why": "The reconciliation suite this change sits inside: TestReconcileConflictAppendsDuplicateAdmitAndOwesAlarm (:543) is the exact pattern to follow for asserting an owed alarm; TestReconcileRecordsOfflineRedemption (:340) pins the no-prior-admission branch; TestReconcileBrokenChainRecordsQuarantineSide (:662) pins the quarantine branch that deliberately owes NO conflict alarm — do not 'fix' it."
      },
      {
        "id": "services/access/internal/store/pass_admission_smoke_test.go",
        "why": "TestPassReconcileRecordsFactualEntryExitOnly (:354) pins ADR-025 §D2's rule that pass reconciliation records facts only and never mints conflict events. A refunded alarm extended to pass slots would go red here — which is the correct outcome, not a test to update."
      },
      {
        "id": "services/access/internal/store/refunds_smoke_test.go",
        "why": "TestScanOfRefundedTicketIsRefused (:126) and TestRefundedRefusalPrecedesIntegrityFailOpen (:171) pin the LIVE half from TKT-157 and ADR-038 §4's ordering. This ticket must not regress either; they are also the nearest precedent for how a refunded fixture is built."
      },
      {
        "id": "scripts/smoke.sh:502,549",
        "why": "`access verify-lifecycle` runs inside the gate AND the suite deliberately corrupts the trail to assert the verifier REFUSES (502-507). Any direct INSERT into lifecycle_events, or any canonical-form change, fails here."
      },
      {
        "id": "services/access/internal/store/alarm_envelope_test.go",
        "why": "Pins the alarm envelope shapes including admissionConflictAlarmEnvelope. If the payload struct is touched at all, this is where the schema-1 contract is enforced (ADR-017)."
      }
    ]
  },
  "readiness": {
    "objective": {
      "state": "met",
      "note": "An offline admit of a refunded ticket is currently invisible — recorded as an ordinary redemption, chain verifying clean, nobody informed. Making it visible is independently valuable and is the half of TKT-162 AC1 that needs no product decision."
    },
    "scope": {
      "state": "met",
      "note": "IN: a refunded check inside ReconcileAdmission that owes an admission-conflict alarm alongside the existing append. OUT: the revocation feed, any scanner change, the staleness ceiling, and any fail-open/closed gate behaviour — all retained by TKT-162. The boundary is sharp because this ticket changes only what is RECORDED about an occurrence, never whether it is recorded and never what any gate does."
    },
    "uncertainties": {
      "state": "met",
      "note": "One genuine design choice, and it is small enough to settle in the plan rather than by a product call: whether the wire `result` gains a distinct value. `ReconcileResult.result` is a CLOSED enum in the contract — [recorded, conflict, synced, rejected] (services/access/api/openapi.yaml:236) — so returning a new outcome is an OpenAPI change plus a regenerate-and-commit (ADR-009), while keeping `recorded` and owing only the alarm is contract-free. Recommend CONTRACT-FREE: the scanner is explicitly out of scope, it does not act on the result (reconciliation is recording, not deciding), and an unknown enum value reaching an old scanner is a version-skew problem this ticket has no reason to create. The plan states the choice and its reason either way. No other unknowns: the alarm class, the payload type, the outbox mechanism and the transaction discipline all already exist and are being reused, not designed."
    },
    "dependencies": {
      "state": "met",
      "note": "No blockers. TKT-157 (live refusal, the helper this reuses) is Done. TKT-162 is the PARENT of this split and stays in Backlog — it is not a blocker: this ticket is deliberately the branch that does not depend on TKT-162's undecided staleness call, and nothing here constrains what that decision can later be. TKT-147 (fixed reason codes for the integrity channel) is untouched — this ticket deliberately avoids that channel."
    },
    "approach": {
      "state": "met",
      "note": "Verified against HEAD, not proposed. `ticketRefunded(ctx, tx, ticketID)` (services/access/internal/store/refunds.go:195) reads `lifecycle_events` for an event_type 'refunded' on the CALLER'S transaction, and ReconcileAdmission already holds the ticket row lock (`SELECT ... FROM tickets ... FOR UPDATE`, reconcile.go:96) — so the helper drops in with no new locking and no new query shape. `oweConflictAlarm` (reconcile.go:309) is the existing alarm shape: it INSERTs the envelope into lifecycle_integrity_alarm_outbox inside the caller's transaction, which is what makes the alarm and the recorded fact atomic. NOTE the alarm payload type `conflictAlarmData` (reconcile.go:297) is a fixed struct on a schema-1 contract; if the refunded case needs to be DISTINGUISHABLE from a duplicate-admit conflict in the payload, that is an ADR-017 change — bump on CONSUMER SEMANTICS, not parse compatibility — so prefer reusing the payload as-is unless the plan argues a consumer genuinely cannot tell them apart. AUTH/MONEY NOTE: this touches neither — it adds no endpoint, no credential path, and no money field; it only adds an outbox row beside an append that already happens."
    },
    "first_slice": {
      "state": "met",
      "note": "This ticket IS the slice — it was carved out of TKT-162 precisely to be one. No further splitting: the change is one guard plus one alarm call in a single function, and cutting it smaller would ship a check that owes nothing."
    },
    "success": {
      "state": "met",
      "note": "The five ACs on the ticket are each independently testable and none needs a number that does not exist yet. THE TEST PLAN'S LOAD-BEARING QUESTION, established while reading the code so the drafter inherits it: a refunded ticket can reach reconciliation through SEVERAL DISTINCT BRANCHES, and a fixture that lands on one proves nothing about the others — (a) single-entry with NO prior redemption (reconcile.go:190, appends 'redeemed'), (b) single-entry WITH a prior redemption (reconcile.go:215, appends duplicate_admit and ALREADY owes a conflict alarm — so the assertion here is about the refunded reason, not about an alarm existing), (c) a PASS slot (reconcile.go:166, appends entry/exit and never mints conflict events per ADR-025 §D2), and (d) a BROKEN CHAIN (reconcile.go:135, lands quarantine-side and deliberately owes no conflict alarm). The plan must state which of these four the AC applies to and WHY the others are excluded — (c) and (d) in particular have standing ADR reasons not to alarm, so silently covering them would contradict ADR-025. MUTATION CHECK the guard: delete the refunded check and each test must go red for the branch it claims; a test that stays green is about something else. And derive the assertion from the RULE ('an offline admit of a voided ticket is never silent'), never from what a run produced."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked at creation from TKT-162's mémo, then re-verified at HEAD on 2026-08-22 rather than inherited: every ADR below resolves to a real file in docs/adr/, and governingTests is populated from the actual suite."
    }
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ Implementation readiness — PASS (8/8)\n\nShaped at creation, split out of **TKT-162**. Every item was resolved by **reading the code at HEAD**, not by asking — which is the point of the split: this is the branch of TKT-162's AC1 that needs no product decision.\n\n| Item | State | Resolved by |\n|---|---|---|\n| `objective` | met | ADR-038 §4 states the gap; confirmed live in `reconcile.go:73` |\n| `scope` | met | In/out boundary is sharp — records more *about* an occurrence, never changes *whether* it is recorded |\n| `uncertainties` | met | One design choice (wire enum), settled in-plan with a recommendation |\n| `dependencies` | met | No blockers; TKT-157 Done, TKT-162 deliberately not a blocker |\n| `approach` | met | Both mechanisms verified to exist and be reusable as-is |\n| `first_slice` | met | This ticket *is* the slice |\n| `success` | met | 5 ACs, all testable; branch enumeration recorded below |\n| `context_memo` | met | 6 ADRs + 5 governing tests, each verified to resolve at HEAD |\n\n**Why `approach` is `met` and not `owner: human`.** `shaping.md` forbids deferring the approach on an auth or money surface. This touches neither: no endpoint, no credential path, no money field — it adds an outbox row beside an append that already happens. Both moving parts already exist and are being *reused*, not designed:\n\n- `ticketRefunded(ctx, tx, ticketID)` (`refunds.go:195`) reads on the **caller's transaction**, and `ReconcileAdmission` already holds the ticket row lock (`reconcile.go:96`) — no new locking, no new query shape.\n- `oweConflictAlarm` (`reconcile.go:309`) INSERTs the envelope into the shared outbox **inside the caller's transaction** — which is exactly what makes the alarm and the recorded fact atomic, as AC1 requires.\n\n**The one design choice, with a recommendation.** `ReconcileResult.result` is a **closed enum** on the wire (`openapi.yaml:236`). Returning a distinct outcome is an OpenAPI change plus regenerate-and-commit (ADR-009); keeping `recorded` and owing only the alarm is contract-free. **Recommend contract-free** — the scanner is out of scope and does not act on the result (reconciliation is recording, not deciding), so a new enum value would create a version-skew problem this ticket has no reason to create. The plan states the choice and its reason either way.\n\n### ⚠️ The trap this ticket will fall into, named now\n\nA refunded ticket can reach reconciliation through **four distinct branches**, and a fixture that lands on one proves nothing about the others:\n\n| Branch | Line | Today's behaviour |\n|---|---|---|\n| (a) single-entry, **no** prior redemption | `reconcile.go:190` | appends `redeemed` |\n| (b) single-entry, **with** prior redemption | `reconcile.go:215` | appends `duplicate_admit`, **already** owes a conflict alarm |\n| (c) **pass** slot | `reconcile.go:166` | appends `entry`/`exit`, never mints conflict events (ADR-025 §D2) |\n| (d) **broken chain** | `reconcile.go:135` | lands quarantine-side, deliberately owes **no** conflict alarm |\n\nTwo of these have *standing ADR reasons* not to alarm. (c) is pinned by `TestPassReconcileRecordsFactualEntryExitOnly` and (d) by `TestReconcileBrokenChainRecordsQuarantineSide` — a change that extends the refunded alarm into them goes red there, and **that is the correct outcome, not a test to update**. Branch (b) already owes an alarm, so a test there asserting merely \"an alarm exists\" is vacuous — it passes with the new guard deleted.\n\n**The plan must state which branches the AC applies to and why the others are excluded**, then mutation-check the guard: delete the refunded check and each test goes red for the branch it claims. Derive assertions from the rule — *an offline admit of a voided ticket is never silent* — never from what a run produced.\n\n### Risk verdict\n\n**`risk:low` NOT claimed.** By blast radius it would qualify — one guard, one alarm call, one function, no auth, no money, no migration. But `SKILL.md` is explicit that `risk:low` is about blast radius *and* that the two come apart: keep the plan gate when the central question is a design question whose answer is non-obvious, **above all \"how do I construct a test that can actually fail?\"** That is precisely this ticket — the four-branch table above is the whole difficulty, two branches must *not* be covered, and one of the coverable ones already owes an alarm for a different reason. TKT-266 is the cited precedent: tests-only, two files, correctly low-blast-radius, and it still burned three build cycles on fixture design because every obvious fixture was unfalsifiable. A plan critique costs one delegated read and catches that class cheaply.\n\n### ⛔ Gate 1 — ready for your call\n\nAll eight DoR items are `met`, so the board will accept the drag to **Ready**. Priority remains yours."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — running gateless per `config.gates: autonomous`\n\n**Gate 1 was taken by the owner**, explicitly, in session (\"you do it, move it to ready\"). Recording it here because the transition was executed by the agent on the owner's instruction rather than dragged on the board — the decision is the owner's, the keystroke was mine. Gate 1 is the one gate autonomous mode does not replace, so it is worth the audit line.\n\nGates 2–4 are taken by the agent per `config.gates: autonomous`. Everything else is unchanged and non-negotiable: `config.models` routing as declared, cross-model review for both plan and code, TDD with tests observed red first, `make check` green before every push and after the merge, and no AI attribution anywhere.\n\n**Blockers:** none. `links` carries one `split-from` → TKT-162, which is a lineage record, not a dependency. TKT-162 is deliberately *not* a blocker: it stops a refunded holder getting in, this ticket records that one did, and neither needs the other to ship.\n\n**Selection reason:** taken on the owner's direct instruction. It is also the only ticket on the board in Ready, and the only one of the two shaped this session that is unblocked — TKT-162 remains held at Gate 1 on an undecided product call (staleness ceiling).\n\n**Branch:** `TKT-269-refunded-offline-admit-alarm`, cut from `origin/main` at `f8daadf2`.\n\n### Risk verdict — `risk:low` NOT claimed\n\nStated explicitly, per the rule that silence is what lets a mislabeled ticket through.\n\nBy **blast radius** it would qualify: one guard and one alarm call inside a single function, no auth surface, no money path, no migration, no CI/deploy config, well under five files. None of `risk:low`'s carve-outs are touched.\n\nIt is refused anyway, because blast radius and difficulty come apart and the label is challengeable in the direction of *more* scrutiny. This ticket's central question is a **design question with a non-obvious answer** — specifically *\"how do I construct a test that can actually fail?\"*, which `SKILL.md` names as the shape that recurs here. Three concrete reasons:\n\n1. A refunded ticket reaches reconciliation through **four distinct branches** (`reconcile.go:190` / `:215` / `:166` / `:135`).\n2. **Two of them must deliberately NOT alarm** — pass slots (ADR-025 §D2) and broken chains — each pinned by an existing test that will correctly go red if the change overreaches.\n3. The duplicate-admit branch **already owes a conflict alarm**, so a test there asserting merely \"an alarm exists\" is vacuous: it passes with the new guard deleted.\n\nTKT-266 is the precedent — tests-only, two files, correctly low-blast-radius, and it still burned three build cycles because every obvious fixture was unfalsifiable for structural reasons. A plan critique costs one delegated read and catches that class cheaply. Keeping the plan gate buys the branch-coverage argument before code exists rather than after.\n\n**Plan stage next:** drafted by `gpt-5.6-luna` (per `config.models.plan`), critiqued by the main agent (`config.models.planReview`) — the cross-model pair is satisfied, author ≠ reviewer."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — by `gpt-5.6-luna` (per `config.models.plan`)\n\nDrafted read-only via the codex companion (`task`, effort high), briefed with the ticket, the six governing ADRs, the five governing tests, the verified code extracts, and the four-branch table. Reproduced below as returned; my critique follows in the next comment.\n\n---\n\n#### 1. Definition of done\n\nA refunded single-entry ticket reconciled from offline with no prior redemption: records the occurrence as the same `redeemed` lifecycle event as today; returns `recorded`, not a new wire outcome; writes exactly one `platform.access.admission-conflict.alarm`; commits the lifecycle row and alarm outbox row atomically; replays as `synced` without another lifecycle row or alarm; leaves `access verify-lifecycle` clean; leaves the schema-1 alarm payload and canonical golden bytes unchanged. Pass slots and broken-chain reconciliation retain existing behaviour.\n\n#### 2. Branch decision\n\n| Branch | Alarm? | Decision |\n|---|---|---|\n| (a) single-entry, no prior redemption (`reconcile.go:188-206`) | **Yes** | Add the refunded check here. Append `redeemed`, then owe the alarm before the existing commit. Keep outcome `recorded`. |\n| (b) single-entry, prior redemption (`reconcile.go:212-232`) | **Yes (already)** | No new alarm logic needed — this branch already appends `duplicate_admit` and calls `oweConflictAlarm`. |\n| (c) pass slot (`reconcile.go:164-186`) | **No** | ADR-025 §D2: pass reconciliation records factual `entry`/`exit` only; policy conflicts are derived projections. Extending here contradicts `TestPassReconcileRecordsFactualEntryExitOnly`. |\n| (d) broken chain (`reconcile.go:135-162`) | **No** | Quarantine/integrity path; ADR-021 owns the chain problem and the existing test explicitly requires no conflict alarm. |\n\nReplay stays first, so a retry returns `synced` before refund handling and cannot owe a second alarm. *\"The new behavior is only observable on branch (a).\"*\n\n#### 3. Design choices\n\n**Wire outcome — keep existing.** (a) `recorded`, (b) `conflict`, (c)/(d) `recorded`, replay `synced`. A new enum adds no scanner behaviour, would require `openapi.yaml` + regenerate + commit under ADR-009, and introduces needless version skew.\n\n**Alarm payload — reuse `conflictAlarmData` unchanged.** It already carries organizer, ticket, occurrence, device time and skew. A consumer can distinguish the refund case by inspecting the ticket's lifecycle state. Adding a reason field is an ADR-017 schema decision and would invalidate the payload key-set and golden tests. Keep `SubjectAdmissionConflictAlarm`; do not use the integrity class.\n\n#### 4. Files to touch\n\n- `reconcile.go` — call `ticketRefunded` on the existing transaction and ticket lock after the pass branch returns; in the no-prior-redemption branch call `oweConflictAlarm` after `appendLifecycle` and before `tx.Commit()`, only when refunded; leave branch (b) unchanged; generalize the `oweConflictAlarm` comment; do not touch `conflictAlarmData`, the envelope, canonical data or result types.\n- `occurrence_smoke_test.go` — branch (a), (b), (d) and rollback coverage.\n- `pass_admission_smoke_test.go` — branch (c) refunded fixture.\n\nDo not modify: `openapi.yaml`, generated files, `alarm_envelope_test.go`, migrations, ADRs.\n\n#### 5. Test plan (tests first)\n\n- **A. `TestReconcileRefundedTicketRecordsRedemptionAndOwesAlarm`** — branch (a). Issue, refund via `RefundOrderTickets`, reconcile a new occurrence. Invariant: *\"A refunded physical admission remains a normal recorded redemption, with one admission-conflict alarm for that occurrence.\"* Asserts `recorded`; one `redeemed` row with `id == occurrenceID` and device timestamp; no `duplicate_admit`; exactly one outbox row on the conflict subject; schema 1 with matching ids; `VerifyLifecycle(RequireCoverage: true)` clean; replay → `synced` with no extra row or alarm. Mutations: remove the refunded check or the new `oweConflictAlarm` → alarm assertion red; remove the replay guard → replay assertion red.\n- **B. `TestReconcileRefundedDuplicateStillOwesOneAlarm`** — branch (b). Redeem live with occ A, refund, reconcile occ B. Asserts `conflict`, one `duplicate_admit`, one alarm, replay adds neither. *Explicitly noted:* deleting the new refunded check does **not** make this red, and that is intentional — branch (b)'s alarm predates TKT-269, so this test protects existing behaviour rather than pretending to prove the new guard.\n- **C. `TestPassReconcileRefundedTicketRecordsFactWithoutConflictAlarm`** — branch (c). Refunded pass ticket, reconcile an `entry`. Asserts `recorded`, one `entry`, no `redeemed`/`duplicate_admit`, **no** conflict alarm. Mutations: moving the refund alarm before the pass split, or alarming on every refunded reconciliation.\n- **D. `TestReconcileRefundedBrokenChainRecordsQuarantineWithoutConflictAlarm`** — branch (d). Refund, `corruptChain`, reconcile. Asserts `recorded`, quarantine row present, no lifecycle append, **no** conflict alarm. Mutations: alarming from the quarantine branch, or moving refund handling before `verifyTicketChain`.\n- **E. `TestReconcileRefundedAlarmAndAppendRollbackTogether`** — atomicity. Install a temporary trigger rejecting inserts into `lifecycle_integrity_alarm_outbox`, then reconcile. Asserts an error, **no** committed `redeemed` row, no alarm, and the `refunded` row intact. Rationale given: *\"A successful-path test alone cannot prove transaction coupling… both artifacts are present when everything succeeds even if the implementation accidentally uses two transactions.\"* Mutations: committing between append and alarm; writing the alarm in a separate transaction.\n- **F. Existing regressions, unmodified** — `TestWireGoldenAdmissionConflictAlarm`, `assertConflictAlarmPIIFloor`, `access verify-lifecycle`.\n\n#### 6. Risks and traps\n\nRefund handling goes **after** the replay guard (else a retry owes a second alarm) and **after** the chain and pass exclusions (a global guard violates ADR-025 for passes and ADR-021 for quarantine). Branch (a) stays `recorded`, not `conflict` — the person is already inside. No `refunded` field in the payload. No lifecycle event to represent the alarm. Seed refunds through `RefundOrderTickets`, never a hand-written insert, so the real `refunded` event participates in the signed chain. Alarm and append both before the existing commit. *\"The alarm gives honest-writer operational visibility. It does not extend ADR-021's tamper-evidence claims.\"*"
    },
    {
      "stage": "plan-review",
      "kind": "plan",
      "body": "<!-- sdlc:stage=plan-review kind=plan -->\n### Plan critique — by the main agent (per `config.models.planReview`)\n\nCross-model pair satisfied: drafted by `gpt-5.6-luna`, critiqued here by a different model. Adversarial pass, not a rubber stamp. **Every file, symbol and test seam the draft names was verified to exist at HEAD** (`git grep -n … HEAD`) — a drafter that cannot run the gate will hallucinate seams, so this was checked rather than assumed.\n\n#### Seam verification — all present\n\n| Named | Verified |\n|---|---|\n| `ticketRefunded` | `refunds.go:195` ✓ (takes the caller's `*sql.Tx`) |\n| `oweConflictAlarm` | `reconcile.go:306` ✓ |\n| `conflictAlarmData` | `reconcile.go:297` ✓ |\n| `corruptChain` | `lifecycle_smoke_test.go:57` ✓ |\n| `issueAndSeed` | `pass_admission_smoke_test.go:175` ✓ |\n| `TestWireGoldenAdmissionConflictAlarm` | `alarm_envelope_test.go:63` ✓ |\n| `VerifyLifecycle` / `RequireCoverage` | `lifecycle_verify.go:39` / `:25` ✓ |\n| `TestPassReconcileRecordsFactualEntryExitOnly` | `pass_admission_smoke_test.go:354` ✓ |\n\nNo hallucinated seams. Line ranges for the four branches are accurate to within a line or two of my own read.\n\n#### ACCEPTED\n\n**The branch decision, all four rows.** Independently confirmed, and two exclusions are *enforced*, not merely argued:\n- (d) — `TestReconcileBrokenChainRecordsQuarantineSide` (`occurrence_smoke_test.go:689`) already asserts `count(*) … WHERE subject = SubjectAdmissionConflictAlarm` is **0**. Alarming from the quarantine branch goes red there today.\n- (c) — `TestPassReconcileRecordsFactualEntryExitOnly` pins the same for passes.\n\n**Reuse the payload unchanged.** Stronger than the draft argues. `TestWireGoldenAdmissionConflictAlarm` (`alarm_envelope_test.go:71`) pins the envelope as a **byte-exact string literal**, and `assertConflictAlarmPIIFloor` asserts the `data` key set **exactly**, with a message stating a new field is a PII decision (ADR-025 §D9) *and* a schema decision (ADR-017 §3). The payload is frozen by two independent mechanisms. Reuse is the only viable option; the draft reached the right answer with a weaker argument than the code actually provides.\n\n**Contract-free wire outcome.** Confirmed: `server.go:532` maps `string(result.Outcome)` straight through to a **closed** wire enum (`openapi.yaml:236`). A new value would need openapi + regenerate + commit before the gate (`check-generate` diffs against HEAD) and would create skew for a scanner that is explicitly out of scope and does not act on the result. Agreed.\n\n**Test E (atomicity) — the draft's best contribution, and it was not requested.** The brief did not ask for a failure-injection test. The reasoning is exactly right: on the success path both artifacts exist whether or not they share a transaction, so a happy-path test *cannot* distinguish one transaction from two. This is precisely the \"green test that cannot reach the failing state\" shape in `docs/learnings/2026-08-10-a-green-test-that-cannot-reach-the-failing-state.md`. Accepted as written, and it is the test I would have been most likely to omit.\n\n**Test B's honesty.** The draft states outright that deleting the new guard leaves B green, and that B protects pre-existing behaviour rather than proving the new one. That is the vacuity trap named in the brief, correctly identified and correctly *labelled* rather than papered over. Accepted with an amendment below.\n\n#### AMENDED\n\n**A1 — Test B must additionally assert the alarm COUNT is exactly one, and that assertion must be argued.** The draft asserts \"exactly one admission-conflict alarm exists for occurrence B\" but does not say what would produce two. It matters: if the implementation places the refunded check *before* the (a)/(b) split — the most natural way to write it — a refunded ticket on branch (b) gets the pre-existing `duplicate_admit` alarm **plus** a new refunded alarm, i.e. **two alarms for one occurrence**. That is a real defect this test can catch, and it is the *only* branch where the count can go wrong. Amend B's mutation list to name it explicitly: *\"hoist the refunded check above the prior-`redeemed` query → B fails on the count.\"* This converts B from a pure regression test into one that constrains the new code's placement.\n\n**A2 — Test A must assert the `refunded` row is untouched and the chain still covers it.** The draft asserts `VerifyLifecycle(RequireCoverage: true)` succeeds, which is necessary but not the whole of AC2/AC3. Add: the ticket's `refunded` lifecycle row is still present and its `id` is unchanged after reconciliation. Cheap, and it pins \"the alarm is additive\" against an implementation that ever tried to reconcile the two facts by touching the earlier one.\n\n**A3 — Fixture note the draft gets right but does not justify.** Test A says \"issue one single-entry ticket through `issueTicket`, refund it through `RefundOrderTickets`\". This works only because `issueTicket` (`lifecycle_smoke_test.go:30`) mints a fresh `OrderID` holding exactly that one ticket, so `RefundOrderTickets(org, s.id.OrderID, refundID, 1)` selects it. Verified. Recording it because a reader would otherwise reach for `issueOrder` and change the fixture's shape for no reason.\n\n**A4 — State the seed-justification for every seed (the TKT-241 rule).** The brief did not carry this and the draft did not volunteer it. For each test, ask of each seed *\"if I delete this, what goes red?\"* Concretely: in Test C the **refund** seed is the one at risk — a pass reconciliation records `entry` and raises no conflict alarm whether or not the ticket is refunded, so C's assertions are satisfied by a fixture with no refund at all. **C is therefore vacuous as specified.** It must be repaired the same way (d) is meaningful: the assertion has to be *\"no conflict alarm even though this ticket IS refunded\"*, which only bites against an implementation that alarms globally. The fix is not a new assertion but a **mutation requirement**: C must be shown red against a deliberately-global refunded guard, and that run must be recorded. Without that demonstration C proves the pass branch works, which is already proven by the test beside it.\n\n#### REJECTED\n\n**R1 — \"The new behavior is only observable on branch (a).\" Rejected as stated.** It is true of the *alarm's existence* and false of the *implementation's correctness*, and the distinction is exactly what A1 catches. Branch (b) is where a misplaced guard produces a duplicate alarm; branch (c) and (d) are where a global guard produces a forbidden one. Three of the four branches constrain the code even though only one changes behaviour. Left unamended, this sentence invites an implementer to test only (a) and treat B/C/D as ceremony — which is how the fixture-that-cannot-fail class gets shipped. The branch table is right; this summary of it is wrong.\n\n**R2 — \"A consumer can distinguish the refund case by inspecting the ticket's lifecycle state.\" Rejected as a justification, while keeping the conclusion.** The conclusion (do not extend the payload) is correct and forced by the golden test. The reason given is not load-bearing and is partly wrong: an alarm consumer is a NATS subscriber, and nothing in the platform gives it a read path into access's `lifecycle_events`. The honest justification is: **the payload is frozen by a byte-exact golden and an exact key-set assertion, and distinguishing the two conflict causes is not in this ticket's ACs.** If an operator ever does need to tell them apart, that is a follow-up with an ADR-017 bump — not something to hand-wave as already solved. Recording this so a future reader does not inherit a false capability claim.\n\n#### Introduced at plan-review, reviewed by no second model\n\nPer the skill's required section — design elements *added* here rather than accepted, amended or rejected from the draft. These reach Gate 2 reviewed by nobody but their author:\n\n1. **A1's duplicate-alarm-on-(b) failure mode** and the resulting placement constraint (the refunded check must sit *inside* the no-prior-redemption branch, below the `redeemed` lookup — not above the (a)/(b) split).\n2. **A4's finding that Test C is vacuous as specified**, and the requirement that C be demonstrated red against a global-guard mutation rather than merely written.\n\nBoth are testable claims rather than design preferences, and both will be *executed* during the build — A1 by writing B's count assertion and mutating the placement, A4 by the recorded mutation run. A claim is a hypothesis until it is executed (ADR-053 / TKT-236), so neither is treated as settled by having been written here.\n\n#### Verdict\n\n**Accept with amendments A1–A4 and rejections R1–R2.** The draft is sound on every decision that carries risk — the branch table, the two exclusions, the frozen payload, the contract-free wire — and it volunteered the atomicity test unprompted. Its one real weakness is the vacuity of Test C, which is the same class of defect the ticket itself exists to avoid, and which the draft caught in Test B but not in the test beside it."
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan final — Gate 2 taken by the agent (`config.gates: autonomous`)\n\n**The `kind=plan` draft as amended below is the plan.** Delta only — the draft and this comment are read together on resume, so nothing above is restated.\n\n#### Amendments carried (from `kind=plan-review`)\n\n- **A1** — Test B additionally asserts the alarm count for occurrence B is **exactly one**, with the mutation named: *hoisting the refunded check above the prior-`redeemed` query produces two alarms for one occurrence* (the pre-existing `duplicate_admit` alarm plus a new refunded one). This is what makes B constrain the new code's placement rather than only guard old behaviour.\n- **A2** — Test A additionally asserts the ticket's `refunded` lifecycle row is still present with an unchanged `id` after reconciliation. Pins \"the alarm is additive\" (AC2/AC3).\n- **A3** — Fixture recorded: `issueTicket` mints a one-ticket order, so `RefundOrderTickets(org, s.id.OrderID, refundID, 1)` selects it. Do not reach for `issueOrder`.\n- **A4** — **Test C is vacuous as specified and must be repaired by demonstration.** Its assertions hold with the refund seed deleted, because a pass reconciliation raises no conflict alarm regardless. C must be shown **red against a deliberately-global refunded guard**, and that run recorded in the coding stage comment. Without the demonstration, C proves only what the test beside it already proves.\n- **R1** — the draft's summary sentence *\"the new behavior is only observable on branch (a)\"* is **struck**. True of the alarm's existence, false of the implementation's correctness: (b) constrains placement, (c) and (d) forbid a global guard. Three of four branches constrain the code.\n- **R2** — the justification *\"a consumer can distinguish the refund case by inspecting the ticket's lifecycle state\"* is **struck** while its conclusion stands. An alarm consumer is a NATS subscriber with no read path into access's `lifecycle_events`. The operative reason not to extend the payload is that it is frozen by a byte-exact golden (`alarm_envelope_test.go:71`) and an exact key-set assertion, and that distinguishing the two conflict causes is not in this ticket's ACs.\n\n#### Decisions resolved by the agent at Gate 2\n\nAutonomous mode: every open decision is resolved here, preferring the critique's recommendation, and the most reversible option where the critique made none.\n\n1. **Wire outcome — contract-free.** Branch (a) keeps `recorded`. Critique-recommended and the more reversible choice: adding an enum value later is additive, removing one is a breaking contract change. No `openapi.yaml` edit, so ADR-009's regenerate-and-commit does not arise.\n2. **Alarm payload — reused unchanged.** Forced by the golden and the PII floor, not merely preferred.\n3. **Guard placement — inside the no-prior-redemption branch, below the `redeemed` lookup.** This is A1's constraint made explicit as a design decision rather than left to the implementer. It follows from the branch table: above the (a)/(b) split double-alarms branch (b); above the pass split or the chain verify violates ADR-025 §D2 and ADR-021 respectively.\n4. **`ticketRefunded` reuse rather than a new query.** Existing helper, takes the caller's `*sql.Tx`, and `ReconcileAdmission` already holds the ticket row lock (`reconcile.go:96`) — no new locking, no new query shape, nothing to get wrong under concurrency.\n5. **Test order — E before C.** Not in the draft. E (atomicity) is the test most likely to change the implementation's shape; writing it late risks discovering the transaction coupling is wrong after the other four are green and feel finished.\n\n#### Introduced at plan-review, reviewed by no second model\n\nRestated here per the skill, because these reach Gate 2 with only one model behind them:\n\n1. **A1's duplicate-alarm-on-(b) failure mode**, and decision 3 above, which is derived from it.\n2. **A4's finding that Test C is vacuous as specified**, and the requirement to demonstrate it red against a global-guard mutation.\n\nBoth are hypotheses until executed. A1 is settled by writing B's count assertion and mutating the placement; A4 by the recorded mutation run. Neither is treated as proven by having been argued — the run is the evidence (ADR-053, TKT-236).\n\n#### Definition of done\n\nThe draft's DoD, plus: **five mutation runs recorded** in the coding stage comment — one per test A, B, C, D, E — each naming the mutation, and each showing the test red before the fix and green after. For C specifically the mutation is the global guard; for B it is the hoisted check. `make check` green, `access verify-lifecycle` green, the three pre-existing regression suites untouched and still green.\n\n#### Risk verdict, restated\n\n`risk:low` remains **not claimed** (see `kind=claim`). Nothing in planning changed the blast radius — still one guard, one alarm call, no auth, no money, no migration — and nothing changed the reason for the refusal either: the difficulty is entirely in constructing tests that can fail, and the critique found one of the five already could not.\n\n### ⛔→✅ Gate 2 taken — transitioning to Building\n\n`agent:plan-review` → `agent:coding`."
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation complete — by the main agent (per `config.models.implement`)\n\n```yaml\nstage: coding\nimplementer: main-agent\nbranch: TKT-269-refunded-offline-admit-alarm\ncommit: 1978d397\npr: https://github.com/mathd/ticketing_system/pull/313\ngate: make check — sentinel EXIT=0, 1311 log lines, no failure markers\nverify_lifecycle: clean (twice, in-gate)\ndiff: 3 files, +322/-2\ncontract_change: none\nmigration: none\n```\n\n#### The change\n\nOne guard and one alarm call in `ReconcileAdmission`, inside the no-prior-redemption branch, **below** the `redeemed` lookup. Plus a generalized doc comment on `oweConflictAlarm`, which now has two callers. `ticketRefunded` is reused on the caller's transaction under the ticket row lock the function already holds — no new locking, no new query shape.\n\n#### TDD order\n\nTests first. Initial run with the implementation absent: **A and E red, B/C/D green** — correct, since B, C and D assert behaviour that must *not* change. That is precisely why they can only be proven by mutation, and why the plan critique's A4 mattered.\n\n#### Mutation evidence — the load-bearing part\n\nFive mutations, each run against the full new-test set. Every test is red against the mutation it claims to catch, and green otherwise.\n\n| # | Mutation | Red | Green through it |\n|---|---|---|---|\n| 1 | guard disabled (`if false && refunded`) | **A**, **E** | B, C, D |\n| 2 | guard hoisted above the `redeemed` lookup | **B** (`2 alarms, want 1`) | A, C, D, E |\n| 3 | global guard above the pass split | **B**, **C** | A, D, E — *and* the pre-existing `TestPassReconcileRecordsFactualEntryExitOnly` |\n| 4 | guard above `verifyTicketChain` | **B**, **D** | A, C, E — *and* the pre-existing `TestReconcileBrokenChainRecordsQuarantineSide` |\n| 5 | alarm committed in its own transaction after `tx.Commit()` | **E** | A, B, C, D |\n\n**Mutation 2 validates plan-review A1 empirically.** Hoisting the check above the `redeemed` lookup makes a refunded ticket on the duplicate-admit path owe **two alarms for one occurrence**. A and E stay green through it, so test B is the *only* thing standing between that defect and main. A1 was introduced at plan-review and reviewed by no second model; it is now executed rather than argued (ADR-053 / TKT-236).\n\n**Mutation 3 settles plan-review A4.** The charge was that Test C is vacuous — that its assertions hold with the refund seed deleted. Under the global-guard mutation C goes **red** while the pre-existing pass test beside it stays **green**, because that test's ticket is not refunded. The refund seed is therefore load-bearing, demonstrated rather than asserted (the TKT-241 rule: *\"if I delete this seed, what goes red?\"*). Mutation 4 shows the identical pattern for D against the pre-existing quarantine test.\n\n**Mutation 5 is why the atomicity test exists.** Only E catches the two-transaction split; A stays green because on the success path both artifacts exist whether or not they share a transaction. The drafter volunteered this test unprompted and it is the one I was most likely to have omitted.\n\nNo mutation residue: `reconcile.go` restored from a pre-mutation copy and grepped for markers (0 hits) before committing.\n\n#### Regressions\n\nFull access store smoke suite green (24.5s locally, 26.1s in-gate) — including `TestWireGoldenAdmissionConflictAlarm`, `assertConflictAlarmPIIFloor`, both pre-existing pass tests, the quarantine test, and the TKT-157 live-refusal tests. None modified.\n\n#### Deviations from the plan\n\nNone on substance. One thing the plan did not anticipate: the harness reported the backgrounded gate as **\"exit code 0\" while it was still executing lint**. Caught because the sentinel file was absent and the log was 22 lines — the skill's rule to judge a background gate by the sentinel plus the log body, never the harness status, earned its place again. Noted for the retro.\n\n`agent:coding` → `agent:ai-review`. Review by `gpt-5.6-luna` (per `config.models.aiReview`) on the branch diff vs `origin/main`, primed for guilt and given the diff only — not the plan."
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review — by `gpt-5.6-luna` (per `config.models.aiReview`)\n\n```yaml\nstage: ai-review\nreviewer: gpt-5.6-luna\ncommand: adversarial-review --base origin/main --scope branch\nartifact: 4642 bytes, conclusive verdict present\nverdict: approve — no material findings\npasses: 1 (second pass not earned — see below)\n```\n\n**Artifact checked before it was read for findings.** 4642 bytes, ends on an explicit `Verdict: approve` with a summary — not a zero-byte run and not a mid-sentence truncation. Both failure shapes the runner doc names were ruled out first, because a review that never ran is indistinguishable from one that found nothing.\n\n**The run was genuine.** 14 investigative commands: `reconcile.go` in full, the new tests at `occurrence_smoke_test.go:900-1150`, `refunds.go:190-220`, `exchanges.go`, `scan.go:70-125`, `0007_refunded_lifecycle.sql`, the fixture helpers, ADR-025 §D3/§D6, ADR-038, and the pre-existing quarantine test. It opened at `needs-attention` and moved to `approve` **after** investigating — not a rubber stamp that started approving.\n\nIt also read `/home/mathieu/.ai/skills/review/SKILL.md` — the documented, unpreventable local-skill hijack. Per the runner doc that is irrelevant to the judgement: the run returned a conclusive verdict against the intended scope, which is the pass condition. Recorded for honesty, not as a caveat on the result.\n\n**Reviewer's finding: none.** Quoted: *\"The refunded check is correctly limited to single-entry, verified-chain, no-prior-redemption admissions; replay, duplicate, pass, exit, broken-chain, concurrency, and rollback paths are covered coherently.\"*\n\n#### Second pass: NOT earned, and why\n\nThe rule is that a second pass judges **the fix diff** — code no one has reviewed. Zero findings means zero fixes means **no new code exists**. The diff `gpt-5.6-luna` reviewed is byte-identical to the diff at merge (`1978d397`, unchanged since the push). A second pass would re-review the same bytes, which is churn, not convergence. One pass, recorded as such.\n\n#### Findings triage\n\n| Finding | Source | Verdict |\n|---|---|---|\n| *(none returned)* | reviewer | — |\n| Exchanged tickets have the identical offline gap | **agent, during review** | **Incidental → new ticket TKT-270** |\n\n**The one finding this stage produced came from me, not the reviewer**, while checking the review's own prompt about a refunded-and-exchanged ticket. `ticketExchanged` (`exchanges.go:208`) is consulted on both live paths — `postgres.go:332` and `scan.go:106` — and appears **nowhere** in `reconcile.go`. Offline, an exchanged ticket is admitted and recorded as an ordinary redemption, silently. TKT-166/ADR-039 refuses it live for a *different and arguably sharper* reason than the refund case: the replacement is live elsewhere, so admitting the original admits the exchange twice.\n\n**Triaged incidental, not blocking, and deliberately not folded in.** This ticket's ACs say *refunded* throughout; widening scope at review time is exactly the drift the pipeline exists to stop. Filed as **TKT-270** with the analysis, the template, and one AC that makes it more than a copy-paste: a ticket that is both refunded **and** exchanged must owe exactly **one** alarm, a case TKT-269 never had to answer and which the naive fix (a second independent check beside the first) gets wrong.\n\n#### Rebase\n\nNot needed. `origin/main` is unchanged at `f8daadf2` since the branch was cut; `git rev-parse HEAD` == `git rev-parse origin/TKT-269-refunded-offline-admit-alarm` == `1978d397`.\n\n`agent:ai-review` → Gate 3, taken by the agent per `config.gates: autonomous`. Review-guide goes on the PR **first** — it is the audit record — then the pre-merge push check, then squash-merge."
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### PO validation — self-validated by the agent (`config.gates: autonomous`)\n\nMerged as `b033ee31` (squash of `1978d397`, PR #313). Branch deleted. **Content verified on `origin/main`, not just the merge badge** — `git grep` finds the guard at `reconcile.go:221` on the remote branch, which is the check TKT-97 exists to enforce.\n\nEach AC validated against **test evidence on the merged code**, with the specific test named. This is not \"the suite is green\" — it is which assertion proves which clause.\n\n| # | Acceptance criterion | Evidence | Verdict |\n|---|---|---|---|\n| 1 | Reconciling a **refunded** ticket owes an admission-conflict alarm, in the **same transaction** as the append | `TestReconcileRefundedTicketRecordsRedemptionAndOwesAlarm` asserts exactly one outbox row on `platform.access.admission-conflict.alarm`, schema-1, naming this ticket and occurrence. Transactionality proven separately by `TestReconcileRefundedAlarmAndAppendCommitTogether` — verified red against a two-transaction implementation (mutation 5) | ✅ |\n| 2 | The occurrence is still recorded **exactly as today**; nothing dropped or downgraded | Same test: outcome `recorded` (not refused, not `conflict`), a `redeemed` row keyed on the occurrence id carrying the **device-claimed** time, zero `duplicate_admit`. Plus the additivity assertion — the pre-existing `refunded` event id is unchanged after reconciliation | ✅ |\n| 3 | `access verify-lifecycle` clean; canonical golden literals **unchanged** | `VerifyLifecycle(RequireCoverage: true)` asserted inside test A; `access verify-lifecycle` verified clean twice in the gate run. `TestWireGoldenAdmissionConflictAlarm` (byte-exact envelope) and `assertConflictAlarmPIIFloor` (exact key set) both untouched and green — the payload was reused, never extended | ✅ |\n| 4 | A **replay** owes **no second alarm** | Test A replays the same occurrence: outcome `synced`, `redeemed` count still 1, alarm count still 1. Same assertion in test B for the conflict path | ✅ |\n| 5 | Covered on **each branch it can reach**, with excluded branches justified | Four branches, four tests, five mutation runs — full table in `kind=coding`. (a) alarms; (b) alarms once, not twice; (c) pass and (d) broken chain **must not** alarm, each with a standing ADR reason, each verified red against the mutation that would break it | ✅ |\n\n#### The two ACs that could have been faked, and why they are not\n\nAC5's exclusions (c) and (d) assert an alarm must **not** appear — the shape that passes with its fixture deleted. The proof is not the assertion, it is **mutation 3 and mutation 4**: under a global-guard mutation the new tests go red while the **pre-existing** pass and quarantine tests stay **green**, because their tickets are not refunded. That green/red split is the evidence that the refund seed is load-bearing rather than decoration (the TKT-241 rule).\n\nAC1's atomicity clause is unfalsifiable on the happy path — both artifacts exist whether or not they share a transaction. Only the failure-injection test distinguishes them, and it was verified red against exactly the implementation it forbids.\n\n#### Not demonstrable through a user-facing surface, by design\n\nThere is no UI for this and there should not be. The deliverable is an operational alarm on a NATS subject consumed by operators, and the ticket explicitly changes nothing a scanner does. Validation is therefore through code and test evidence, which is the route the pipeline prescribes when a COS is not user-demonstrable. No preview link applies.\n\n#### What a reader should NOT conclude from this ticket\n\n- **A refunded ticket still gets in at an offline gate.** This makes the admission *visible*; it does not stop it. Stopping it is **TKT-162** (revocation feed), still in Backlog on an undecided staleness policy.\n- **This is honest-writer visibility, not tamper-evidence** (ADR-021). A writer with Access database access can delete the `refunded` row and the alarm never fires.\n- **Exchanged tickets remain silent.** Same gap, arguably sharper — the replacement is live elsewhere. Filed as **TKT-270**, not fixed here.\n\nGate 4 taken by the agent. Transitioning to **Done** after the closeout below."
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-269\n\nMerged `b033ee31` (PR #313). Post-merge `make check` on `main`: sentinel **EXIT=0**, 1309 log lines, no failure markers. Branch deleted.\n\n#### Durations\n\nWall-clock from the `sdlc-state` commit log. **All four gates were agent-taken except Gate 1**, so there is no human wait to report anywhere in this run.\n\n| Stage | Duration |\n|---|---|\n| Backlog (created → Ready) | 13m 45s |\n| Ready (→ Planning) | 30s |\n| Planning (→ Building) | 8m 33s |\n| ├ `agent:planning` (draft by `gpt-5.6-luna`) | ~7m 56s |\n| └ `agent:plan-review` (critique, main-agent) | ~37s |\n| Building (→ PO Review) | 24m 42s |\n| ├ `agent:coding` (TDD + 5 mutations + gate) | ~16m 4s |\n| └ `agent:ai-review` (`gpt-5.6-luna` + triage) | ~8m 38s |\n| PO Review (→ Done) | ~6m |\n| **Total Backlog → Done** | **~54m** |\n\n`human-wait total`: **n/a (autonomous)** — Gate 1 was taken by the owner in-session, gates 2–4 by the agent.\n\n`diff`: **+322 / −2 (3 files)** — 41 lines of production code in `reconcile.go`, 281 lines of tests.\n\n#### overrides:\n\n**none — earned, not assumed.**\n\nChecked against the actual record rather than asserted. No review pass, at either stage, stated a blocking objection that was proceeded past:\n\n- **Plan-review (main-agent)** raised four amendments (A1–A4) and two rejections (R1–R2). **All six were adopted**, not overridden — A1 became a design decision (guard placement), A4 became a mandatory mutation demonstration. Nothing was argued down.\n- **ai-review (`gpt-5.6-luna`)** returned `approve` with **zero** findings after a genuine 14-command investigation.\n- The single finding the review stage produced came from the agent, not a reviewer, and was **filed** (TKT-270) rather than dismissed — which is triage, not an override.\n\nThe one judgement call worth naming, since an owner re-taking these decisions would want it visible: **one ai-review pass, not two.** Zero findings means zero fixes means no unreviewed fix diff; the bytes `gpt-5.6-luna` reviewed are byte-identical to the merged bytes (`1978d397`, unchanged from push to merge). That is the rule's own condition for a single pass, not an exception to it.\n\n#### learnings:\n\nTwo candidates, both **already recorded in the repo** and therefore *cited, not restated* — the skill's bar for an edit is that a learning would change future behaviour **and** isn't readable from the repo when needed.\n\n1. **A \"must not happen\" test needs its negative seed proven by mutation, not by argument.** Two of the five tests here assert an alarm must *not* appear, and both pass with their refund seed deleted. What made them real was the green/red split under mutation: the new tests go red while the *pre-existing* pass and quarantine tests stay green, because their tickets are not refunded. This is exactly [a fixture that seeds two mechanisms](docs/learnings/2026-08-16-a-fixture-that-seeds-two-mechanisms.md) and the \"if I delete this seed, what goes red?\" rule — **no new learning, the existing one applied and confirmed**. Target house: none; already in `AGENTS.md`.\n2. **A guard's placement can be the entire defect surface, and only a count assertion finds it.** Mutation 2 (hoisting the check one branch higher) produces *two alarms for one physical admission*, and tests A and E both stay green through it. This is the N-predicates/short-circuit family already in `AGENTS.md` via TKT-251, seen from the other side: not \"one test per predicate\" but \"one test per *placement*\". Marginal enough that it does not earn a skill edit. Target house: none.\n\nNothing promoted. Both are instances of rules the repo already carries, which is the honest answer rather than manufacturing a novel one.\n\n#### retro:\n\n**(1) What would have made this ticket faster or better?**\n\nThe context-mémo did its job — `governingTests` (added during TKT-162's re-shaping, hours before this ticket existed) is what put the four-test migration set and the pass/quarantine tripwires in front of the drafter. Worth noting because that field was **missing** from TKT-162's original bake and its absence would have cost a gate failure here.\n\nThe real friction was **the harness reporting a backgrounded `make check` as \"exit code 0\" while it was still running lint** — twice in this run. Both times the sentinel file was absent and the log was short, which is the only reason it was caught. `AGENTS.md`/`SKILL.md` already carry this rule (TKT-101, TKT-235) and it held. **No change needed; the rule earned its place again.** Concrete non-change: I considered proposing a `make check` wrapper that writes the sentinel itself, but rejected it — the discipline is already written down and a wrapper would just move the trust.\n\n**(2) What should the humans change?**\n\nOne concrete item, and it is about **ticket sequencing, not process**:\n\n- **TKT-270 should be prioritized close behind this one, or deliberately deferred with the reason recorded.** Exchanged tickets have the identical offline gap and arguably a sharper one — an exchanged ticket's replacement is *live elsewhere*, so an offline admit of the original can admit the exchange twice on the same night. Leaving the refund half closed and the exchange half open is a state where the system looks fixed and is half-fixed, which is worse for an operator's mental model than either extreme. **Where to apply:** the board — Gate 1 on TKT-270.\n\nNo skill edits proposed. No `.sdlc/` or config changes proposed. The flow worked as written; the two rules that were load-bearing (mutation-prove the negative seed; never trust a background gate's harness status) are both already in the repo.\n\n#### Project DoD\n\n- [x] PR merged, content verified on `origin/main` (not the badge — `git grep` at `reconcile.go:221`)\n- [x] `make check` green pre-push **and** post-merge on `main` (sentinel + log body, both read)\n- [x] CI green (`check`, `gate-selftest`, `changes`, `repository-scan`)\n- [x] `access verify-lifecycle` clean\n- [x] Cross-model review, author ≠ reviewer, both stages\n- [x] No AI attribution in commit, PR, or any artifact\n- [x] Branch deleted\n- [x] Incidental finding filed (TKT-270), not silently dropped"
    }
  ]
}
