{
  "key": "TKT-267",
  "type": "Task",
  "parent": "TKT-9",
  "classification": "simple",
  "summary": "ClaimOutstandingExchangeReversals leases malformed cross-tenant rows before filtering them",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "number": 311,
    "url": "https://github.com/mathd/ticketing_system/pull/311",
    "state": "merged"
  },
  "links": [
    {
      "type": "found-in",
      "key": "TKT-260"
    }
  ],
  "description": "Found by the adversarial ai-review of TKT-260 (`gpt-5.6-luna`), rated **[high]**. Recorded rather\nthan absorbed: TKT-260's subject is one predicate on `LoadExchangeSwitch`, and this is a different\ndefect of a different KIND in a sibling query. TKT-260 corrected the two places its own diff\noverclaimed about this and changed no behaviour here.\n\n`ClaimOutstandingExchangeReversals` (`services/commerce/internal/store/exchange_reversal.go:59`) is\nthree stages, and the organizer scoping is only on the last one:\n\n    WITH claimable AS (\n        SELECT organizer_id, id FROM order_exchanges\n        WHERE settled_at IS NOT NULL AND tickets_exchanged_at IS NOT NULL\n          AND capacity_returned_at IS NULL AND reversal_parked_at IS NULL\n          AND reversal_next_attempt_at<=now()\n          AND (reversal_lease_until IS NULL OR reversal_lease_until<=now())\n        ORDER BY reversal_next_attempt_at FOR UPDATE SKIP LOCKED LIMIT $1\n    ), claimed AS (\n        UPDATE order_exchanges x SET reversal_lease_until=..., reversal_claim_id=$3\n        WHERE (x.organizer_id, x.id) IN (SELECT organizer_id, id FROM claimable)\n        RETURNING ...\n    )\n    SELECT ... FROM claimed c\n    JOIN orders o ON o.id = c.source_order_id\n    JOIN reservations res ON res.id = o.reservation_id AND res.organizer_id = c.organizer_id\n\n**Neither `claimable` nor `claimed` validates the source reservation's organizer.** A malformed row\n— settled, switched, capacity outstanding, but whose source reservation belongs to another organizer\n— therefore:\n\n1. consumes one of the `LIMIT $1` claim slots,\n2. has `reversal_lease_until` and `reversal_claim_id` **written**,\n3. is then dropped by the final join, so it is never returned to the caller,\n4. and because it is never returned, `ReleaseExchangeReversalClaim` and\n   `AbandonExchangeReversalClaim` are never called on it — nothing charges an attempt, nothing parks\n   it, nothing clears the lease.\n\nIt re-takes a slot on every lease expiry, forever, and the function reports no error. Because the\nclaim is `ORDER BY reversal_next_attempt_at` under a `LIMIT`, and a pass is bounded at\n`MaxBatchesPerPass`, this is the same head-of-line blocking the awaiting-switch exclusion was added\nto prevent (see the long comment on that conjunct) — arrived at by a different route.\n\n**This is a LIVENESS defect, not a leak.** No foreign `hold_id` escapes: the final join is correctly\nscoped, which is why the row disappears rather than being served. The harm is that valid\ncapacity-return work is delayed or starved while the sweep silently spins on rows it can never\ndrive. Note this is the opposite failure direction from TKT-260's: there the unscoped join was too\npermissive; here the scoped join is correct and the stages *before* it are too permissive.\n\n**Severity is bounded by the same fact TKT-260's was.** No code path writes a mismatched pair today,\nso nothing is starving right now. Per ADR-021, name the adversary: a writer with commerce DB access\ncan create the row, and against that adversary this is honest-writer consistency, not\ntamper-evidence. But unlike TKT-260 the consequence here is a *live* one once such a row exists —\na stuck sweep, not a wrong answer — which is why it is worth its own ticket rather than a note.\n\n**Acceptance Criteria:**\n- [ ] The organizer validation happens **before** a row is leased — the predicate belongs in\n      `claimable`, not only in the final join. (A correlated `EXISTS` over `orders`/`reservations`\n      inside `claimable` is the obvious shape; the `FOR UPDATE SKIP LOCKED` interaction needs\n      checking, since `FOR UPDATE` against a join has its own rules.)\n- [ ] A store-tier smoke test (real PostgreSQL) that seeds **one malformed row and one valid row\n      with `limit=1`**, and asserts the valid row is returned. Then assert the malformed row's\n      `reversal_lease_until` and `reversal_claim_id` are still NULL — the slot was not consumed.\n      Both halves matter: \"the valid row came back\" alone can pass by ordering luck.\n- [ ] Delete the new predicate and the test must go red on the lease assertion.\n- [ ] The five existing conjunct tests stay green — this adds a sixth guard, and AGENTS.md's rule\n      applies: one case per predicate, each satisfying the earlier ones, each mutated separately.\n- [ ] The comment at `exchange_reversal.go:117` and the ADR-063 bullet TKT-260 amended are updated\n      to say the gap is closed; both currently point here.\n\nQuality gates: `make check` green; money untouched.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Filed from TKT-260's ai-review — not yet shaped\n\nNeeds the 8-item DoR before Gate 1. Likely `risk:low` once shaped, but that call belongs to shaping.\n\n**The interesting half is the test, and it is NOT the same shape as TKT-260's.** TKT-260's property\nwas fail-closed on a single-valued join, so its test asserts an error. This one's property is that\na row **is not written to** — the assertion is on `reversal_lease_until`/`reversal_claim_id`\nremaining NULL after a claim that did not return it. A test that only asserts \"the valid row came\nback\" is the vacuous shape: with a `LIMIT` and an `ORDER BY`, it can pass by ordering luck while the\nmalformed row was leased anyway. Assert what the write left behind, not what the read returned.\n\n**Whoever shapes it should check whether the refund side has the same shape.**\n`ClaimOutstandingReversals` in `refund_reversal.go` carries the sibling predicate from ADR-062 F3 at\nits final join; if its CTE has the same stage ordering, this ticket is two queries, not one — and\nthat changes the risk call."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Shaping verdict: READY for Gate 1 — 7 met, 1 deferred, 0 open, 0 blockers\n\nRan the shaping pass against the code at `308d2efe`. Verdict below; the `readiness` object and the\ncontext-mémo are on the ticket.\n\n**The defect is real and the ticket's proposed remedy survives the read.** Migration 0010 gives\n`order_exchanges` a `source_order_id uuid NOT NULL REFERENCES orders(id)` and nothing that ties its\n`organizer_id` to the source reservation's, so the malformed pair is constructible and the three-stage\nquery behaves exactly as filed. `approach` inherits the ticket's `EXISTS`-in-`claimable` shape as\nverified, not assumed.\n\n**The prior comment's suspicion is confirmed: this is two queries, not one.** `ClaimOutstandingReversals`\n(`refund_reversal.go:74`) has the identical `claimable` → `claimed` → scoped-final-join structure, with\nthe organizer predicate only on the last stage. Scope now covers both. One consequence the scope note\nrecords: ADR-062 contains **zero** occurrences of \"organizer\" — the refund side's fix is recorded\nnowhere unless this ticket adds the bullet ADR-063 already has.\n\n**The sixth conjunct is a comment liability, not just a test one.** The claim is documented as FIVE\nconjuncts in two places — the function doc (`exchange_reversal.go:56`) and the smoke file's header\n(\"the five claim conjuncts\"). Both become false the moment this merges. Recorded in `approach` beside\nthe per-predicate test rule, because a stale count in a comment that *exists to tell the next reader\nhow many tests to expect* is how the next ticket under-tests.\n\n**One item deferred, deliberately, and it is the only real unknown:** whether `FOR UPDATE SKIP LOCKED`\ntolerates a correlated `EXISTS` in the same `SELECT` without extending the lock to `orders` and\n`reservations`. Postgres documents `FOR UPDATE` as locking the tables named in the `FROM` list, and a\n`WHERE`-clause subquery is not in it — but that is **an inference from documented semantics, not a fact\nverified against this database**, and Planning must execute it rather than repeat it. The `note` carries\na fallback shape (`EXISTS` on the `claimed` UPDATE instead) and says plainly that the fallback is\nstrictly weaker — it stops the lease but not the slot consumption, which is half the AC.\n\n**Risk verdict: `risk:low` NOT claimed.** No auth, no money, no migration, no CI config — but two\nconcurrency-path queries under `FOR UPDATE SKIP LOCKED` where the open question is the SQL shape\nitself, and the wrong shape makes sweep replicas contend on `reservations` in a way no unit test\nwould surface. Reasoning in full on the mémo's `riskCall`.\n\n⛔ **Gate 1 is the human's in both modes.** The autonomous run stops here: this ticket was in `Backlog`\nwith `readiness: null`, so entering at `Ready` would have skipped exactly the shaping that found the\nsecond query and the stale conjunct count. Awaiting prioritization to `Ready`."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — running gateless per `config.gates: autonomous`\n\nGate 1 was taken by the owner explicitly (\"do it, move it and run it\") after the shaping verdict.\nGates 2–4 are agent judgement per § Autonomous mode; every other rule holds unchanged — cross-model\nreview on both the plan and the code, TDD with tests observed red first, the local gate green before\nevery push, and no AI attribution anywhere.\n\n**Blockers: zero.** The only link is `found-in → TKT-260`, which is merged at `308d2efe`; it is\nprovenance, not a dependency.\n\n**Branch:** `TKT-267-scope-reversal-claims-before-leasing`, off `origin/main` at `308d2efe`.\n\n**Selection reason:** taken by name. It is the highest-value item its parent epic (TKT-9) currently\nhas in flight — a `[high]` liveness defect filed by TKT-260's own ai-review, with the fix location\nalready identified and an ADR bullet that names this ticket as its owner and is false until it lands.\n\n**Risk verdict: `risk:low` NOT claimed — stated explicitly rather than left silent.** Checked all\nfour carve-outs: no auth surface, no money path (the diff cannot reach an amount), no data migration\n(no schema change — see the mémo's scope OUT), no CI/deploy config. Two files of production SQL, two\ntest files, one ADR. It nonetheless keeps the plan gate, because the one genuinely open item is the\n**SQL shape itself** on a `FOR UPDATE SKIP LOCKED` concurrency path in two queries, and the wrong\nshape — an `EXISTS` that extends the row lock to `orders`/`reservations` — makes sweep replicas\ncontend on the reservation table. That is a production-visible regression no unit test would show,\nwhich is exactly the class `risk:low` must not wave through."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — by `gpt-5.6-luna` (`config.models.plan`), read-only\n\nDrafted against the real code at `308d2efe` via the codex companion (`task`, effort high, no `--write`;\nworktree verified clean after). Reproduced verbatim below; my critique follows as `kind=plan-final`.\n\n---\n\n# TKT-267 execution plan\n\nNo files were modified. The worktree is clean at `308d2efe`.\n\n## Recommendation\n\nAdd the organizer check to `claimable` in both queries. Keep the final joins unchanged as defense in depth.\n\nUse an explicit lock target, `FOR UPDATE OF ... SKIP LOCKED`, so the intended lock scope is visible in the SQL.\n\n### Exchange query\n\n```sql\nWITH claimable AS (\n    SELECT oe.organizer_id, oe.id\n    FROM order_exchanges AS oe\n    WHERE oe.settled_at IS NOT NULL\n      AND oe.tickets_exchanged_at IS NOT NULL\n      AND oe.capacity_returned_at IS NULL\n      AND oe.reversal_parked_at IS NULL\n      AND oe.reversal_next_attempt_at <= now()\n      AND (oe.reversal_lease_until IS NULL\n           OR oe.reversal_lease_until <= now())\n      AND EXISTS (\n          SELECT 1\n          FROM orders AS o\n          JOIN reservations AS res\n            ON res.id = o.reservation_id\n           AND res.organizer_id = oe.organizer_id\n          WHERE o.id = oe.source_order_id\n      )\n    ORDER BY oe.reversal_next_attempt_at\n    FOR UPDATE OF oe SKIP LOCKED\n    LIMIT $1\n),\nclaimed AS (\n    -- Existing UPDATE, including the full (organizer_id, id) key.\n)\n-- Existing final SELECT and organizer-scoped reservation join.\n```\n\n### Refund query\n\n```sql\nWITH claimable AS (\n    SELECT rf.organizer_id, rf.id\n    FROM order_refunds AS rf\n    WHERE rf.status = 'completed'\n      AND (rf.tickets_voided_at IS NULL\n           OR rf.capacity_returned_at IS NULL)\n      AND rf.reversal_parked_at IS NULL\n      AND rf.reversal_next_attempt_at <= now()\n      AND (rf.reversal_lease_until IS NULL\n           OR rf.reversal_lease_until <= now())\n      AND EXISTS (\n          SELECT 1\n          FROM orders AS o\n          JOIN reservations AS res\n            ON res.id = o.reservation_id\n           AND res.organizer_id = rf.organizer_id\n          WHERE o.id = rf.order_id\n      )\n    ORDER BY rf.reversal_next_attempt_at\n    FOR UPDATE OF rf SKIP LOCKED\n    LIMIT $1\n),\nclaimed AS (\n    -- Existing UPDATE, including the full (organizer_id, id) key.\n)\n-- Existing final SELECT and organizer-scoped reservation join.\n```\n\nThe `EXISTS` is correlated on the exchange/refund organizer and source order. It filters malformed rows before ordering, limiting, and leasing.\n\n## Alternative ranking\n\n| Rank | Shape | Decision |\n|---|---|---|\n| 1 | Correlated `EXISTS` in `claimable` | Recommended. Meets both acceptance halves and leaves only the queue table in the locking query's top-level `FROM`. |\n| 2 | `IN (SELECT ...)` or equivalent lateral lookup | Functionally possible, but less direct. A `LEFT JOIN LATERAL` places more relations in the query tree and must retain an explicit `FOR UPDATE OF order_exchanges/order_refunds` to avoid lock-scope ambiguity. |\n| 3 | Predicate only in `claimed` UPDATE | Fallback only. It prevents the lease but still consumes a `LIMIT` slot, so it does not fix head-of-line blocking. The acceptance criterion would have to be weakened to “malformed rows are not leased.” |\n\n## Proving the lock behavior\n\nAdd a real-PostgreSQL smoke proof for each claim path.\n\n1. Seed two eligible rows with deterministic ordering.\n2. Begin transaction A and call the production claim function through the transaction, with `limit=1`. Keep transaction A open.\n3. Begin a second claim through another connection. It must skip the row locked by transaction A and claim the next row. This proves the actual statement still uses `SKIP LOCKED`, rather than merely filtering the lease written by transaction A.\n4. From a separate connection, attempt `SELECT ... FOR UPDATE NOWAIT` on transaction A's source reservation. It must succeed.\n5. Attempt the equivalent lock on transaction A's exchange/refund row. It must fail with PostgreSQL's lock-not-available error.\n\nThis uses the shipped query, not a copied test query. If necessary, extract each SQL statement into an unexported constant so the production function and the diagnostic test execute the same text.\n\n## Index verification\n\nNo migration is needed.\n\nThe existing indexes remain applicable:\n\n- `order_exchanges_reversal_queue_idx`\n- `order_refunds_reversal_queue_idx`\n\nThe new `EXISTS` cannot be part of a PostgreSQL partial-index predicate, but it does not invalidate the existing queue index. PostgreSQL can scan the partial queue index for the existing actionable predicates and evaluate the correlated lookup for each candidate. The order and reservation lookups should use their primary or unique indexes.\n\nVerify the exact production statements with `EXPLAIN`, using enough seeded rows that a sequential scan is not the reasonable plan. Require:\n\n- an index scan using the relevant reversal queue index;\n- indexed lookup of the source order and reservation;\n- no assertion about unrelated join access paths.\n\nIf the plan chooses a sequential scan only because the fixture is tiny, enlarge the fixture. Do not mask the result with `enable_seqscan = off`.\n\n## Files to touch\n\n- [`services/commerce/internal/store/exchange_reversal.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/store/exchange_reversal.go:56)\n  - Add the `EXISTS` predicate to `claimable`.\n  - Change “FIVE independent conjuncts” to “SIX”.\n  - Rewrite the comment around the current TKT-267 gap to say malformed rows are filtered before the limit and lease.\n  - State that this is honest-writer consistency, not tamper-evidence.\n\n- [`services/commerce/internal/store/refund_reversal.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/store/refund_reversal.go:55)\n  - Add the equivalent predicate to `claimable`.\n  - Add organizer/source-reservation eligibility documentation.\n\n- [`services/commerce/internal/store/exchange_reversal_smoke_test.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/store/exchange_reversal_smoke_test.go:19)\n  - Update “five claim conjuncts” to “six”.\n  - Add the malformed-row test and lock-scope proof.\n  - Keep `TestConjunct1` through `TestConjunct5` unchanged in behavior.\n\n- [`services/commerce/internal/store/refund_reversal_smoke_test.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/store/refund_reversal_smoke_test.go:15)\n  - Add the malformed-row test and lock-scope proof.\n\n- [`docs/adr/ADR-062-refund-reversal-reconciliation.md`](/home/mathieu/sources/ticketing_system/docs/adr/ADR-062-refund-reversal-reconciliation.md:164)\n  - Add the equivalent closed-gap bullet for refund claims.\n\n- [`docs/adr/ADR-063-exchange-reversal-reconciliation.md`](/home/mathieu/sources/ticketing_system/docs/adr/ADR-063-exchange-reversal-reconciliation.md:198)\n  - Rewrite the TKT-267 bullet to say the claim-side gap is closed.\n  - Preserve the statements about `LoadExchangeSwitch`, `tickets_exchanged_at`, and awaiting-switch exclusion.\n\nDo not touch migrations, final joins, `LoadExchangeSwitch`, runner tests, ADR-021, or the channel seam test.\n\n## Test plan\n\nFor each query:\n\n- Seed one normal completed order/reservation.\n- Seed a malformed row by giving the exchange/refund a different `organizer_id` from its source reservation.\n- Seed one valid row.\n- Make the malformed row older than the valid row.\n- Call the claim function with `limit=1`.\n- Assert the valid row is returned.\n- Read the malformed row directly and assert both `reversal_lease_until` and `reversal_claim_id` are `NULL`.\n\nUse non-fatal assertions or check the malformed lease first so deleting the new predicate visibly fails on the lease assertion. With the predicate removed, the older malformed row consumes the slot, receives the lease, disappears at the final join, and the test reports the defect.\n\nAlso verify:\n\n- deleting the exchange-side `EXISTS` turns the exchange smoke test red;\n- deleting the refund-side `EXISTS` turns the refund smoke test red;\n- all five existing exchange conjunct tests stay green;\n- `TestAnActionableRowIsClaimedDespiteAnOlderAwaitingSwitchBacklog` stays green;\n- `TestTheClaimedHoldBelongsToTheClaimingTenant` stays green;\n- `TestTwoTenantsSharingAnExchangeIDAreBothDriven` stays green;\n- `make check` passes.\n\n## Definition of done\n\n- Both claim paths validate organizer ownership before `LIMIT`, `FOR UPDATE`, and leasing.\n- The malformed-row smoke test proves both slot preservation and untouched lease fields.\n- The transaction-level proof demonstrates that only the queue row is locked and that `SKIP LOCKED` still works.\n- The queue indexes remain visible in the production query plans.\n- ADR-062 and ADR-063 describe the gap as closed.\n- Documentation says the fix addresses honest-writer consistency. It does not claim malformed rows cannot exist.\n- No money logic, schema, migration, or switch-marker behavior changes.\n- `make check` is green.\n\nA database constraint or trigger enforcing the cross-table organizer invariant could be considered separately. It is not part of TKT-267 and would still be an honest-writer consistency control, not tamper-evidence against someone with commerce database write access."
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan-final — critique by `main-agent` (`config.models.planReview`), Gate 2 taken autonomously\n\nThe `kind=plan` draft **as amended below is the plan**. Delta only; the draft is not restated.\n\nCross-model pairing honoured: drafted by `gpt-5.6-luna`, critiqued by `main-agent` (this session).\nEvery file, symbol and line anchor the draft names was verified to exist at `308d2efe`.\n\n---\n\n#### ACCEPTED as drafted\n\n- The core shape — correlated `EXISTS` inside `claimable`, both queries, final joins untouched as\n  defence in depth. This is the right call and it is **proven, not argued** (see below).\n- No migration. Confirmed: the queue indexes are partial on row-local conjuncts and supply the\n  `ORDER BY`; the `EXISTS` adds a PK probe per surviving candidate and cannot be an index predicate.\n- The `EXPLAIN` discipline, including *\"do not mask the result with `enable_seqscan = off`\"* and\n  *\"if the plan chooses a sequential scan only because the fixture is tiny, enlarge the fixture.\"*\n  That is the right instinct and it is the same rule ADR-019 §2 already carries.\n- The DoD's insistence that documentation must not claim malformed rows cannot exist (ADR-021).\n- Filing the DB-constraint idea as a separate consideration rather than smuggling it in.\n\n#### ACCEPTED with evidence the draft could not supply — the lock question is CLOSED\n\nThe draft correctly flagged the `FOR UPDATE` interaction as the thing to prove, then specified a\nfive-step in-test proof. **I executed the question directly against PostgreSQL 17 before accepting**,\nbecause the brief said prove-don't-assume and a plan that only *schedules* the proof still leaves\nBuilding to discover a blocker:\n\n- With the correlated `EXISTS`, `pg_locks` shows `order_exchanges` → `RowShareLock` and\n  `orders`/`reservations` → `AccessShareLock` only. The row lock does **not** extend.\n- Held a `FOR UPDATE` in one session; a concurrent `UPDATE reservations` on the traversed row\n  returned `UPDATE 1` immediately under `SET lock_timeout='2s'`. **No replica contention.**\n- Full AC scenario: malformed row made the **oldest** (so ordering luck is excluded), `limit=1` →\n  the valid row is returned and the malformed row's `lease_until`/`claim_id` stay NULL.\n\n**AMENDMENT — `FOR UPDATE OF <alias>` is ACCEPTED, and for a better reason than the draft gave.**\nThe draft argued visible intent. I tested it: the aliased form produces byte-identical lock scope to\nthe plain form, so it costs nothing — and `FOR UPDATE OF nosuch` is a **hard parse error**\n(`relation \"nosuch\" in FOR UPDATE clause not found in FROM clause`). It therefore cannot silently\nwiden lock scope through a typo, which makes it a guard rather than a comment. Adopted.\n\n#### AMENDED — the consequence is worse than either the ticket or the draft states\n\nNeither names this, and it changes how the fix should be described. `RunOnce` in **both** runners\n(`exchangesweep/runner.go:236`, `reversal/runner.go:226`) exits on:\n\n    if len(claimed) < r.batch || fresh == 0 { return resolved }\n\nA malformed row is leased by `claimed` and then dropped by the final join, so the store returns\n**fewer rows than it leased** — which the runner reads as a short batch, i.e. *\"the queue is\ndrained\"*, and returns. So a single malformed row does not merely consume one slot per pass: it\n**terminates the drain after the first batch**, cutting a pass from its `MaxBatchesPerPass × batch`\nbound (64 × 16 = 1024) to at most one batch per tick. A batch composed only of malformed rows hits\n`len(claimed) == 0` and ends the pass having driven nothing.\n\nRequired of the implementer: say this in the code comment and the ADR bullets. \"Consumes a claim\nslot\" understates it — the accurate statement is *\"a leased-then-dropped row makes the batch look\nshort, which the drain reads as an empty queue.\"*\n\n#### AMENDED — the test plan's red-proof is under-specified\n\nThe draft says *\"use non-fatal assertions or check the malformed lease first.\"* That is the right\nworry, stated too weakly for a rule this pipeline has been burned by (AGENTS.md: a green test that\ncannot reach the failing state; a test that pins the harness).\n\nBinding instead:\n1. **Assert the lease/claim NULLs FIRST**, before asserting the valid row came back. With `t.Fatalf`\n   ordering, a predicate deletion must go red on the **lease** assertion — the one that is about the\n   write — not on the return, which can pass or fail for ordering reasons.\n2. **The malformed row must be the OLDEST** in the fixture. Without that, `ORDER BY\n   reversal_next_attempt_at LIMIT 1` may skip it for reasons unrelated to the predicate, and the\n   test proves nothing. (This is the same trap `TestAnActionableRowIsClaimedDespiteAnOlderAwaitingSwitchBacklog`\n   already dodges by making the ineligible rows older.)\n3. **Run the mutation and record the observed failure line** in the coding stage comment — not\n   \"the test would fail\", the actual red.\n\n#### AMENDED — scope of the comment-count fix, corrected\n\nThe draft says to change \"FIVE\" → \"SIX\" in `exchange_reversal.go` and \"five claim conjuncts\" → \"six\"\nin the smoke file. I grepped: there are **three** sites, not two —\n`exchange_reversal.go:56`, `exchange_reversal_smoke_test.go:19`, and\n`exchange_reversal_smoke_test.go:142` (\"The claim predicate has five independent conjuncts\").\nAll three go false on merge. The refund side asserts **no count** in prose (its conjunct tests are\nnamed descriptively), so the draft's instruction to add \"organizer/source-reservation eligibility\ndocumentation\" there is accepted as *new* prose, not a count edit.\n\n#### REJECTED — the SQL-constant extraction\n\nThe draft hedges: *\"if necessary, extract each SQL statement into an unexported constant so the\nproduction function and the diagnostic test execute the same text.\"* Not necessary, and I checked\nrather than assumed: `ClaimOutstandingExchangeReversals` takes `store.OutboxDB`, which is satisfied\nby `*sql.Tx` (`store.go:79-82`). The lock proof can therefore call the **shipped function** inside a\ntransaction with no refactor at all. Rejected as unnecessary churn on a hot path — and the\nextraction would have been the more dangerous option, since a constant shared with a test is exactly\nhow a test starts pinning the harness instead of the contract.\n\n#### Decisions I made at Gate 2 (autonomous mode — no human resolved these)\n\n1. **Both queries in one ticket** — kept from shaping. Splitting would ship an ADR bullet claiming\n   the class is closed while the sibling query still has the defect.\n2. **No schema constraint** — the more reversible option, and it keeps the diff off migrations.\n   Recorded as a follow-up candidate at closeout, per the draft's own suggestion.\n3. **`FOR UPDATE OF <alias>` adopted** — on the tested evidence above, not on the draft's argument.\n4. **ADR-062 gets a new bullet.** It contains zero occurrences of \"organizer\" today, so without one\n   the refund-side fix is recorded nowhere. This is the least reversible of the four and the one I\n   would most want a human to see; it is additive prose to an accepted ADR, which is why I took it.\n\n#### Introduced at plan-review, reviewed by no second model\n\nPer the skill this must be named, because the cross-model guarantee covers the draft and the code,\nnot my own additions to the plan:\n\n- **The `RunOnce` short-batch amplification** and the requirement to document it. Found by me\n  reading both runners; no second model has evaluated the claim or my reading of it. It is a claim\n  about existing code, testable at review time — the ai-review pass should be pointed at it.\n- **The three-site comment count** (the draft found two). Mechanical and grep-verifiable.\n- **The binding red-proof ordering rule** (assert NULLs first, malformed row oldest).\n- **The rejection of the SQL-constant extraction**, resting on my reading of `OutboxDB`.\n\n#### Not in scope, unchanged from shaping\n\nFinal joins; `LoadExchangeSwitch`; unparking; migrations; the switch-marker behaviour; ADR-021;\n`channel_seam_test.go`.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Coding complete — implemented by `main-agent` (`config.models.implement`), PR #311\n\n**Branch** `TKT-267-scope-reversal-claims-before-leasing` @ `713149e4` · **Diff** 6 files, +239/−33\n**Gate** `make check` — sentinel `EXIT=0`, zero matches for `Error [0-9]+|FAIL|drifted` in the log\nbody. Judged from both, per the hard rule; the smoke stage shows\n`ok ticketing/services/commerce/internal/store 21.615s` (vs 0.568s for the tagged-out unit run), so\nthe new tests executed against the real stack rather than being skipped.\n\n#### TDD, in order\n\n1. Both tests written and **observed RED** before any production edit — and red on the **lease**\n   assertion (`exchange_reversal_smoke_test.go:804`, `refund_reversal_smoke_test.go:675`), which is\n   the plan-final's binding requirement: the write is what the conjunct guards, and the return\n   assertion can pass or fail for ordering reasons.\n2. Implemented both predicates.\n3. Both go green; the five existing conjuncts, `TestTheClaimedHoldBelongsToTheClaimingTenant`,\n   `TestAnActionableRowIsClaimedDespiteAnOlderAwaitingSwitchBacklog` and\n   `TestClaimingIsScopedByTheCompositeKeyNotTheRefundIDAlone` all stay green.\n\n#### Mutation checks — both, with the result that matters\n\n| Mutation | New test | Pre-existing guards |\n|---|---|---|\n| delete exchange `EXISTS` | RED at `:804` (lease) | **all green** |\n| delete refund `EXISTS` | RED at `:675` (lease) | all green |\n\nThe second column is the point (AGENTS.md): a mutation caught by a lower tier proves the mechanism\nis live, not that this test caught it. Deleting the exchange predicate left every prior guard green,\nso the new tier is genuinely what detects it.\n\n#### The plan-final's open item, closed by measurement\n\n- `pg_locks` under the `EXISTS`: `order_exchanges` → `RowShareLock`; `orders` / `reservations` →\n  `AccessShareLock` only. A concurrent `UPDATE` of a traversed reservation returned `UPDATE 1`\n  immediately under `SET lock_timeout='2s'` while a claimant held its row lock. **No replica\n  contention** — the risk that justified withholding `risk:low` does not materialise.\n- `FOR UPDATE OF nosuch` is a hard parse error, so the aliased form cannot silently widen lock scope.\n- `EXPLAIN` on 20,000 exchanges / 103 actionable:\n  `Index Scan using order_exchanges_reversal_queue_idx`, `EXISTS` index-backed at both hops\n  (`orders_pkey`, then an index-only scan on reservations). ADR-019's rule satisfied. The fixture was\n  **enlarged** rather than `enable_seqscan=off` — my first attempt seeded 20k orders and zero\n  exchanges and produced a Seq Scan, which is exactly the tiny-fixture artifact the plan warned about.\n\n#### One correction from mid-run, recorded because it could mislead a later reader\n\nMy first ad-hoc PostgreSQL container was **17**, and six unrelated `unclaim` tests failed on\n`RETURNING OLD.column` — a **PostgreSQL 18** feature this stack requires (`unclaim.go:65` says so).\nThat was my harness, not the code. I verified it reproduced on a clean `main` before concluding, then\nmoved to 18.4 to match `compose.yaml`. A later run also showed outbox failures that turned out to be\nresidue from an earlier full-suite run on a shared database; on a **fresh** database the whole store\nsuite passes except `TestCommerceSmokeDatabasesAreIsolated`, which fails only because my harness set\none DSN of five — that test doing precisely its job.\n\n#### Deliverables against the AC\n\n- [x] Organizer validation before the lease — in `claimable`, both queries.\n- [x] Store-tier smoke test per query, real PostgreSQL, one malformed + one valid row at `limit=1`,\n      asserting the returned row **and** the untouched `reversal_lease_until` / `reversal_claim_id`.\n      Malformed row seeded as the **oldest** so ordering luck cannot mask a deleted predicate.\n- [x] Predicate deletion turns the test red on the lease assertion.\n- [x] The five existing conjunct tests stay green.\n- [x] `exchange_reversal.go:117` comment rewritten; ADR-063 bullet says closed.\n- [x] **Beyond the AC:** the conjunct count was stale in **three** sites, not the two the draft found\n      (`exchange_reversal.go:56`, `exchange_reversal_smoke_test.go:19` and `:142`) — all updated.\n      ADR-062 gained the bullet it never had (zero occurrences of \"organizer\" before this).\n- [x] `make check` green; money untouched; no schema change.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### ai-review — `gpt-5.6-luna` (`config.models.aiReview`). Pass 1 conclusive; pass 2 FAILED TO RUN.\n\n#### Pass 1 — on `713149e4`, verdict `needs-attention`, 2 findings, both [medium], no blockers\n\n| # | Finding | Verdict | Action |\n|---|---|---|---|\n| F1 | No concurrency test proves the lock target | **ACCEPTED — valid** | Fixed in `e810e22a` |\n| F2 | `EXISTS` can scan an arbitrarily large rejected prefix | **ACCEPTED — valid, and I quantified it** | Comment corrected; filed TKT-268 |\n\n**F1.** Correct and worth catching. I had proved lock scope by hand during plan-review and left **no\nregression guard** — the tests ran one claim and inspected the result, so a rewrite that widened the\nlock would have kept every one of them green. Added `TestTheClaimLocksTheQueueRowOnlyAndNeverTwice`:\nit runs the shipped function inside a `*sql.Tx` (which satisfies `OutboxDB`, so it is the production\nquery and not a copy — this is also why the draft's \"extract the SQL into a constant\" suggestion was\ncorrectly rejected at plan-review) and asserts from a second connection that the traversed\nreservation is still writable and that `SKIP LOCKED` still fences a second claimant.\n\n**F2.** Correct, and the review deserves credit for it: my comment said the predicate cost\n*\"nothing\"*, and that is **false**. Measured on PostgreSQL 18.4:\n\n| malformed rows ahead of the queue | time | buffers |\n|---|---|---|\n| 0 (today's population) | ~0.1ms subplan, 16 probes | 112 |\n| 500 | ~4ms | — |\n| 50,000 | **263ms** | 350,585 |\n| 50,000, predicate removed | 0.34ms | 35 |\n\n775x at the extreme. Accepted for this ticket rather than fixed, for two reasons stated in the code:\nthe triggering population is the one no code path creates (the same fact that bounds the original\ndefect), and **the alternative is worse** — without the predicate those rows are leased and dropped,\nwhich wedges the sweep outright rather than slowing it. Slow beats stuck. The durable fix is a schema\nchange, out of scope here by the approved plan; filed as **TKT-268** with the measurements attached.\n\n#### What I found while verifying F1's fix — a claim of my OWN that was wrong\n\nMutating `FOR UPDATE OF oe` to a bare `FOR UPDATE` did **not** turn the new test red. Rather than\npatch the test, I measured all four combinations:\n\n    EXISTS + bare FOR UPDATE  -> orders/reservations AccessShareLock\n    EXISTS + FOR UPDATE OF oe -> orders/reservations AccessShareLock\n    JOIN   + bare FOR UPDATE  -> orders/reservations ROWSHARELOCK   <- the real regression\n    JOIN   + FOR UPDATE OF oe -> orders/reservations AccessShareLock\n\nThe narrow lock is bought by the `EXISTS` being a **WHERE-clause subquery rather than a FROM-list\nentry** — **not** by `OF oe`, which is a no-op in the current shape. My plan-final accepted `OF oe`\non the reasoning that it was load-bearing; it is not, today. It earns its place as insurance against\nthe join rewrite, and the test **does** go red on that rewrite (verified: `lock timeout` on the\nconcurrent write). Comment rewritten to credit the mechanism that actually does the work. This is the\nplan-review-introduced element I flagged as \"reviewed by no second model\" — and it was indeed wrong.\n\n#### ⛔ Pass 2 did NOT run — reporting rather than substituting\n\nThe fixes were non-trivial (a new concurrency test, corrected claims), so the rules require a second\npass on the **fix diff** — code nobody has reviewed. It failed on both rungs of the escalation ladder:\n\n- **Rung 1** (companion `adversarial-review` on `713149e4..e810e22a`): reported `Phase: done,\n  Duration: 1m 45s` and produced a **zero-byte** output file. The documented empty-output shape.\n- **Rung 2** (raw `codex exec -m gpt-5.6-luna -s read-only`, prompt via stdin, diff inlined): produced\n  80KB, was **hijacked by local skills** (`unslop`, a foreign `review` dispatcher — documented as\n  survivable-not-preventable), and **died mid-investigation with no verdict**. Salvage attempt found\n  no findings, only investigation trace.\n- **Rung 3 is stop and report.** No third improvised attempt, and **no self-review substituted** — a\n  skipped review that looks done is the most dangerous failure this pipeline has.\n\n**So the fix diff in `e810e22a` carries NO independent adversarial review. Stated plainly here and in\nthe review-guide.** What I did instead, labelled as my own work rather than as a review:\n\n- Ran the mutation that matters for the new test — the join rewrite — and confirmed it goes red.\n- Settled the sharpest question I had put to the reviewer: *can the second-claimant half pass for the\n  wrong reason?* If the row were excluded by the **lease predicate** (written by the first claim)\n  rather than by `SKIP LOCKED`, the assertion would prove nothing. Executed: to a second connection\n  the lease still reads free (`lease IS NULL` is true — the write is uncommitted and invisible) while\n  the claim-shaped read returns **0 rows**. Only `SKIP LOCKED` can account for that. The assertion\n  tests locking.\n- Re-ran `make check` on the fix diff: sentinel `EXIT=0`, no failure matches, smoke store 21.5s.\n\nResidual risk is **unquantified** on the fix diff, and that is the honest word for it.\n\n#### Churn cap\n\nPass 1 converged; pass 2 never executed, so no churn. Not stopping at the cap — stopping at a\n**runner failure**, which is a different thing and is why it is recorded this way.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### Merged as `bb7cdf35`, self-validated against the COS on the merged code\n\nPR #311 squash-merged, branch deleted. **Content verified on `origin/main` itself** (`git grep`\nagainst the remote ref), not from the merge badge — the stacked-PR trap. Pre-merge, `git rev-parse\nHEAD` matched `origin/<branch>`, so the reviewed SHA is the merged SHA.\n\n#### COS, each against test evidence\n\n| # | Condition | Evidence |\n|---|---|---|\n| 1 | Organizer validation happens **before** a row is leased | `AND EXISTS (...)` inside `claimable` in **both** queries, confirmed present on `origin/main`. The `FOR UPDATE`/`SKIP LOCKED` interaction was the open question and is closed by measurement, not argument — the row lock stays on the queue table. |\n| 2 | Store-tier smoke test, real PostgreSQL, one malformed + one valid row at `limit=1`, asserting the valid row returns **and** the malformed row's lease/claim stay NULL | `TestConjunct6...IsNotLeased` and `TestARefund...IsNotLeased`, both on `origin/main`. Malformed row seeded as the **oldest** so ordering luck cannot mask a deleted predicate. |\n| 3 | Deleting the predicate turns the test red **on the lease assertion** | Executed. Red at the lease assertion in both, and — the part that matters — **every pre-existing guard stayed green**, so the new tier is what catches it. |\n| 4 | The five existing conjunct tests stay green | Green in every gate run, including post-merge. |\n| 5 | `exchange_reversal.go:117` comment and the ADR-063 bullet say the gap is closed | Both rewritten. ADR-062 gained the equivalent bullet — it had **zero** occurrences of \"organizer\" before. |\n| 6 | `make check` green; money untouched | Green on all four commits and after the merge. No money path, no schema change, no migration. |\n\n**Delivered beyond the AC:** the refund query (the ticket named only the exchange side); the\n`RunOnce` short-batch amplification, documented in both queries and both ADRs; a third stale\nconjunct-count site the plan draft missed; and a lock-scope regression test that did not exist.\n\n#### What a reviewer should look at first\n\nThe one judgement call is in COS 1's neighbourhood: the `EXISTS` cost is **linear in the rejected\nprefix** (0 malformed rows negligible; 50,000 → 263ms vs 0.34ms unfiltered). Accepted because\nwithout the predicate those rows are leased and dropped and the sweep wedges outright — slow beats\nstuck — **not** because the bad population is impossible. TKT-268 owns the durable schema fix.\n\n#### Follow-ups this ticket created\n\n- **TKT-268** (new, Backlog) — bound the rejected-prefix cost at the schema level. Carries the\n  measurements so shaping need not re-derive them.\n- **TKT-266** (existing, re-shaped, **held at Gate 1**) — this change makes its stated acceptance\n  criteria **unsatisfiable**: conjunct 6 filters the row its fixture needs before the final join can\n  see it. Three options on the ticket, all `owner: human`. Its underlying defect is unchanged and\n  still open — verified by mutation, not assumed.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-267, merged `bb7cdf35`, post-merge `make check` green on `main`\n\nDurations from the `sdlc-state` branch log (`git log -- .sdlc/tickets/TKT-267.json`), not hand-tracked.\n\n| stage | duration |\n|---|---|\n| Backlog (shaping) | 23:40 → 00:14 (~34m, incl. filing) |\n| Ready (claim) | 00:14:02 → 00:14:22 (20s) |\n| Planning — `agent:planning` (draft, `gpt-5.6-luna`) | 00:14:22 → 00:21:30 (~7m) |\n| Planning — `agent:plan-review` (critique, `main-agent`) | 00:21:30 → 00:23:11 (~2m) |\n| Building — `agent:coding` | 00:23:11 → 00:40:58 (~18m) |\n| Building — `agent:ai-review` (3 passes + 4 gate runs) | 00:40:58 → 01:46:43 (~66m) |\n| PO Review | 01:46:43 → closeout (~2m) |\n\n**human-wait total:** `n/a (autonomous)` — except Gate 1, which the owner took explicitly.\n**diff:** +409/−33 (6 files).\n\n#### overrides: — every point where a blocking objection was overridden\n\nMandatory on a gateless run. **Three entries; none is `none`.**\n\n1. **Escalation-ladder override at Gate 3, authorised by the owner.** After ai-review pass 2 failed\n   on rung 1 (zero-byte output) and rung 2 (raw `codex exec` hijacked by local skills, died with no\n   verdict), the rule is *stop and report*. I stopped and asked. The owner chose **retry**, which\n   overrides \"no rung twice\". **This was the right call and the evidence says so**: the retry found\n   F3 — the `lock_timeout` set on a pool rather than a connection — a real defect that would have\n   shipped a test passing on pool-reuse luck. Residual risk: none I can identify; the retry was\n   conclusive and pass 3 converged on top of it.\n2. **Churn cap exceeded — 3 passes on a 2-pass nominal cap, justified as convergence.** Both\n   required conditions hold: **every** pass invalidated the previous pass's fix **at its root**\n   (pass 2 refuted pass 1's lock test; pass 3 refuted pass 2's probe), and the finding count is\n   **strictly decreasing** — 2 → 4 → 1. Each replacement was structurally smaller. No pass\n   re-litigated settled ground. Carrier of residual risk: the pass-3 fix (`b2a8c3b3`) is a\n   one-assertion change that I mutation-verified myself; it is the least-reviewed commit on the\n   branch. **Stated plainly rather than smoothed over.**\n3. **ai-review F2 accepted rather than fixed.** The reviewer said *\"do not ship yet\"* pending a\n   bounded rejected-prefix cost. I shipped. Reviewer's reason: the `EXISTS` cost is unbounded in the\n   malformed population (measured 263ms at 50k rows vs 0.34ms). My reason: the alternative is\n   strictly worse — without the predicate those rows are leased and dropped and the sweep wedges\n   outright — and the fix is a schema change explicitly out of the approved plan's scope.\n   **Residual risk carried by TKT-268**, filed with the measurements attached.\n\n#### A claim this ticket made about itself and had to retract\n\nMy `kind=plan-final` accepted `FOR UPDATE OF <alias>` on the reasoning that it kept the lock narrow.\nIt does not — measurement showed the narrow lock comes from the `EXISTS` being a WHERE-clause\nsubquery, and `OF oe` is a no-op in the current shape (kept as insurance against a join rewrite,\nwhich the test does catch). This was exactly the item my plan-final had flagged under *\"introduced at\nplan-review, reviewed by no second model\"*, and it was indeed wrong. **The flag worked; the reasoning\nbehind it did not.**\n\n#### learnings:\n\n1. **`SET <session-parameter>` on a `*sql.DB` binds to a connection, not the pool** — so a timeout\n   set through the pool may not apply to the statement that needs it. The test still fails, but\n   slowly and for a reason the reader misattributes. → **`docs/learnings/`** (incident narrative),\n   plus a one-line rule in **`AGENTS.md`**: *pin the connection when a test depends on session state.*\n   This is a new shape: the existing \"test pins the harness\" rule covers assertions about the\n   instrumentation; this is the instrumentation silently not being in effect at all.\n2. **A predicate that closes a defect can make a sibling predicate unreachable-by-input, and\n   therefore untestable.** Conjunct 6 shadows the final join's identical check — TKT-266's stated\n   acceptance criteria became unsatisfiable as a direct result of this ticket. The general rule:\n   when adding a guard upstream of an existing equivalent one, ask what tests the downstream one\n   still has. → **`docs/learnings/`**, referenced from TKT-266.\n3. **\"No code path creates it\" is not the same as \"it cannot exist\", and writing the first next to an\n   ADR-021 paragraph saying the second is a contradiction a reviewer will catch.** → covered by\n   ADR-021's existing *name the adversary* rule; **no new artifact**, cite it.\n4. **A mutation that does not go red is a finding about your model, not a bug in the test.** Swapping\n   `FOR UPDATE OF oe` for a bare `FOR UPDATE` stayed green; investigating instead of patching is what\n   produced the corrected four-case lock table. → **`AGENTS.md`**, one line under the existing\n   mutation-testing rules.\n\nItems 1, 2 and 4 are repo-local and go through a normal gated ticket per the 3-houses rule; item 3 is\nalready recorded.\n\n#### retro:\n\n**(1) What would have made this ticket faster or better?**\n\nThe shaping's `uncertainties: deferred` note said the `FOR UPDATE` question was *\"documented\nsemantics, not verified against this database\"* and left it for Planning. I then verified it during\n**plan-review** anyway, in about four minutes with a throwaway container. **The DoR has no item for\n\"a cheap experiment would settle this now.\"** `deferred` sent it downstream when the answer cost less\nthan writing the deferral did. *Concrete change:* in `references/shaping.md` § The DoR, add to the\n`uncertainties` row — *\"before deferring a technical unknown, ask whether a throwaway container or a\nten-line script settles it inside shaping; if so, run it and record the result rather than the\nquestion.\"*\n\n**(2) What should the humans change?**\n\nThe codex runner is unreliable in a way that costs a full stage: **rung 1 returned zero bytes and\nreported `Phase: done`**, which is indistinguishable from success unless you check the byte count.\nThe skill already says \"exit 0 ≠ success\"; it does not say *check the artifact is non-empty before\nbelieving a completed review*. *Concrete change:* in `references/codex-runner.md` § Judging the\noutput, promote the empty-output check to a **required step** — *\"before triaging findings, assert\nthe output is non-empty and contains a verdict; a zero-byte artifact with `Phase: done` is the most\ncommon failure and reads exactly like a clean review.\"* Second, smaller: `config.gates: autonomous`\nhas no guidance for a runner failure at ai-review specifically — the § Autonomous mode stop-list\ncovers \"a configured model is unreachable after the documented escalation ladder\", which I applied,\nbut the right *default* (stop vs. proceed-and-record) is worth stating rather than leaving to a\nquestion each time.\n\n**Offered:** I can apply both patches now — they are skill/process edits, so per the pipeline's own\nrule they go **straight to the default branch**, no ticket. Say the word and I will; otherwise they\nland in the weekly registry review.\n"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "ClaimOutstandingExchangeReversals leases and slot-consumes a row whose source reservation belongs to another organizer, then drops it at the final join, so it is never released/abandoned and re-takes a claim slot on every lease expiry. Liveness, not leak."
    },
    "scope": {
      "state": "met",
      "note": "IN: the claimable CTE of ClaimOutstandingExchangeReversals (exchange_reversal.go) AND of ClaimOutstandingReversals (refund_reversal.go) -- verified at HEAD to have the identical three-stage shape, so this is two queries; the sixth-conjunct test for each; the exchange_reversal.go:117 comment and the ADR-063 bullet. OUT: any change to the final joins (both already correct), any change to LoadExchangeSwitch (TKT-260 closed it), unparking, and adding a DB-level constraint forbidding the malformed pair (a schema change is a bigger call than this ticket -- record as follow-up if the planner wants it)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "One open technical detail, safely resolvable in Planning against real PostgreSQL: FOR UPDATE + a correlated EXISTS in the same SELECT. Postgres forbids FOR UPDATE on the nullable side of an outer join and locks every table named in the FROM list, but a subquery in WHERE is NOT part of the FROM list, so `SELECT organizer_id,id FROM order_exchanges WHERE ... AND EXISTS (SELECT 1 FROM orders o JOIN reservations r ON r.id=o.reservation_id AND r.organizer_id=order_exchanges.organizer_id WHERE o.id=order_exchanges.source_order_id) FOR UPDATE SKIP LOCKED` should lock only order_exchanges. This is a documented-semantics inference, NOT verified against this database -- Planning must run it. Fallback if it does not hold: keep claimable as-is and add the EXISTS to the `claimed` UPDATE's WHERE, which leases nothing it will not return, at the cost of the row still consuming a LIMIT slot in claimable. That fallback is strictly weaker (it fixes the write, not the head-of-line blocking) and would need the AC restated, so prefer the EXISTS-in-claimable shape."
    },
    "dependencies": {
      "state": "met",
      "note": "No blockers. TKT-260 (found-in) is merged at 308d2efe; its comment at exchange_reversal.go:117 already names this ticket and is one of the deliverables."
    },
    "approach": {
      "state": "met",
      "note": "The ticket's proposed remedy (correlated EXISTS inside `claimable`) was VERIFIED against the code, not inherited: order_exchanges has no FK tying organizer_id to the source reservation's (migration 0010 lines 19-21 -- only source_order_id REFERENCES orders(id)), so the malformed pair is seedable and the defect is real. GUARD PREDICATE COUNT: the claim currently has FIVE conjuncts, each with its own smoke test (TestConjunct1..5 at exchange_reversal_smoke_test.go:149-233), and the function doc-comment says 'FIVE independent conjuncts'. This adds a SIXTH; per AGENTS.md the new test must satisfy the earlier five and be mutated separately, the five existing must stay green, and BOTH the doc-comment and the file header comment ('the five claim conjuncts') must be updated to six or they become false. Same for the refund side, which has its own conjunct set. Note the new predicate is not a plain conjunct on the row's own columns -- it is a join-existence check -- so a test that deletes it must go red on the LEASE assertion, not on 'the row came back'."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice, both queries. Splitting exchange from refund would ship a ticket whose ADR bullet claims the class is closed while the sibling query still has it, and the second half is a copy of the first."
    },
    "success": {
      "state": "met",
      "note": "COS restated in the description; the load-bearing one is the NULL-lease assertion, and the mutation proof is that deleting the predicate turns THAT assertion red (not the return assertion, which can pass by ordering luck)."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked below; governingAdrs and governingTests both populated and each cited file verified to exist at HEAD."
    }
  },
  "context": {
    "summary": "Close a liveness defect in the exchange (and sibling refund) reversal claim queries: organizer validation of the source reservation happens only at the final join, after the claimable CTE has selected and the claimed CTE has leased. A cross-tenant-malformed row therefore burns a LIMIT slot and takes a lease it never gives back.",
    "touchedAreas": [
      "services/commerce/internal/store/exchange_reversal.go (ClaimOutstandingExchangeReversals, the claimable CTE)",
      "services/commerce/internal/store/refund_reversal.go (ClaimOutstandingReversals, same shape -- verified at HEAD)",
      "services/commerce/internal/store/exchange_reversal_smoke_test.go (+ refund_reversal_smoke_test.go)",
      "docs/adr/ADR-063-exchange-reversal-reconciliation.md (the 'what this does not cover' bullet at ~line 204)"
    ],
    "governingAdrs": [
      {
        "adr": "ADR-063-exchange-reversal-reconciliation.md",
        "constrains": "This ADR OWNS the claim query. Its 'what this does not cover' bullet currently names TKT-267 as the owner of exactly this defect and must be rewritten to say it is closed -- leaving it is how an ADR ends up describing a gap that no longer exists. Also: the sweep NEVER writes tickets_exchanged_at (that is access's fact) and the claim deliberately excludes awaiting-switch rows; do not touch either conjunct."
      },
      {
        "adr": "ADR-062-refund-reversal-reconciliation.md",
        "constrains": "The refund lifecycle this one was copied from. Its ai-review F3 is why every statement keys on the FULL composite (organizer_id, id) -- the new predicate must not weaken that. ADR-062 says nothing about organizer-scoping the reservation join (checked: zero matches for 'organizer'), so if the refund side is fixed here, ADR-062 needs the bullet ADR-063 already has, or the refund fix is recorded nowhere."
      },
      {
        "adr": "ADR-021-ticket-lifecycle-trail-integrity.md",
        "constrains": "Name the adversary. This is honest-writer consistency, NOT tamper-evidence: a writer with commerce DB access creates the malformed row in the first place. Any comment or ADR text written for this ticket must say so, and must NOT claim the row can no longer exist -- only that the sweep no longer wedges on it."
      },
      {
        "adr": "ADR-019-catalog-read-path-scoping.md",
        "constrains": "Rule cited by migration 0022 itself: a scoped read is only scoped if an index backs the filter. The new EXISTS runs per candidate row inside a FOR UPDATE SKIP LOCKED scan already narrowed by the partial queue index; check that the EXISTS is index-backed at both hops (orders PK, reservations PK) and does not turn the claim into a seq scan on a hot path."
      },
      {
        "adr": "ADR-038-refund-reversal-ticket-voiding.md",
        "constrains": "Named by the file comment as the reason the sweep must not set the switch marker (the one write that can OVERSELL). Background, not a direct constraint on this diff."
      }
    ],
    "governingTests": [
      {
        "test": "services/commerce/internal/store/exchange_reversal_smoke_test.go TestConjunct1..TestConjunct5",
        "pins": "One case per claim conjunct, each satisfying the earlier ones. AGENTS.md's short-circuit rule: the new sixth guard needs its own case that PASSES all five, and each of the five must stay green. The file header comment says 'the five claim conjuncts' and the function doc says 'FIVE independent conjuncts' -- both become false on merge if not updated."
      },
      {
        "test": "services/commerce/internal/store/exchange_reversal_smoke_test.go TestAnActionableRowIsClaimedDespiteAnOlderAwaitingSwitchBacklog",
        "pins": "The head-of-line-blocking property this ticket's defect re-creates by a different route. It seeds 12 older ineligible rows against limit=5; the new test is its sibling and should be read before writing one, because it is the existing proof that 'the valid row came back' is only half an assertion."
      },
      {
        "test": "services/commerce/internal/store/exchange_reversal_smoke_test.go TestTheClaimedHoldBelongsToTheClaimingTenant",
        "pins": "The final join's correctness -- the half that is ALREADY right. A fix must not make this test's mechanism redundant-looking and must leave it green; it is the assertion that no foreign hold_id escapes."
      },
      {
        "test": "services/commerce/internal/exchangesweep/runner_test.go TestTwoTenantsSharingAnExchangeIDAreBothDriven",
        "pins": "Same-id-different-tenant rows are BOTH legitimate work. A predicate written as if id were globally unique breaks this at the decision tier."
      },
      {
        "test": "services/commerce/internal/api/channel_seam_test.go",
        "pins": "A live cross-cutting tripwire in the same service carrying a do-not-delete instruction. Not expected to be touched here, but named so the drafter knows commerce has one."
      }
    ],
    "seedFixture": "settledExchange(t, db, ctx, key, mutate) at exchange_reversal_smoke_test.go:42 writes order_exchanges directly (so it can express states the happy path cannot) and calls seedCompleted (refunds_smoke_test.go:27) for the order+reservation. seedCompleted generates a fresh OrganizerID per call and writes reservations.organizer_id from it -- so the MALFORMED row is built by seeding a normal completed order, then inserting the order_exchanges row with a DIFFERENT organizer_id from the reservation's. Watch migration 0010's all-or-nothing basis/settlement CHECKs (they broke eleven fixtures at once on TKT-259's first gate run); settledExchange already satisfies them.",
    "riskCall": "NOT risk:low. Four carve-outs checked: no auth, no money (the diff cannot touch an amount), no data migration (no schema change planned -- see scope OUT), no CI/deploy config. But it is a concurrency/liveness path under FOR UPDATE SKIP LOCKED in two queries, the correct SQL shape is the one genuinely open question, and a wrong shape (e.g. an EXISTS that makes FOR UPDATE lock orders/reservations too) would make sweep runners contend on the reservation table -- a production-visible regression that no unit test would show. That earns the plan gate."
  }
}
