{
  "key": "TKT-167",
  "type": "Story",
  "parent": "TKT-9",
  "classification": "complex",
  "summary": "An exchange interrupted after the money moved must resume from what it persisted",
  "description": "Found by the TKT-158 adversarial review (findings P3-1 and P3-4), carried out of that ticket when it hit the churn cap.\n\nTKT-158 persists a **basis** — target hold, replacement reservation, unit amount, slot, total, signed delta, price snapshot — *before* calling payments, exactly so a retry can settle the same numbers. It then does not use it on the path that needs it most: an **unsettled** replay re-resolves the price and re-submits the hold before loading the basis. A price change makes inventory reject the same hold key on a different fingerprint; a catalog outage stops recovery outright. The window this closes is the one the basis was built for — an upgrade whose charge SUCCEEDED and whose local settlement write failed.\n\n`RecordExchangeBasis` also ignores `RowsAffected`, so a losing concurrent writer receives nil and continues with a basis it did not persist — it can then win the reservation INSERT and store a snapshot that disagrees with `order_exchanges.target_price_snapshot`.\n\n**This is the same problem TKT-163 tracks for refunds** — *an obligation interrupted after money moved needs a durable resume path* — and the two should probably be designed together rather than solved twice.\n\n**Acceptance Criteria:**\n- [ ] An unsettled exchange with a recorded basis resumes from it: no catalog call, no new hold, settling the persisted total and delta. A test drives a failure after the provider call and proves the retry completes without re-pricing.\n- [ ] The resume works when catalog is **unavailable** and when the target price has **changed** — both are tested, because both are how the current path fails.\n- [ ] `RecordExchangeBasis` has record-or-load semantics: the caller continues only with the authoritative persisted basis, never its own local copy. A concurrent test proves the loser uses the winner's basis.\n- [ ] The resume never re-settles: a retry after a successful provider call makes no second movement. Proven against payments-side evidence, not commerce's own flags.\n\nQuality gates: `make check` green; contract tests; money integer minor units.",
  "context": {
    "capturedAt": "2026-07-31",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-07-31",
        "rule": "Money is integer minor units + ISO currency code — floats are banned on money paths."
      },
      {
        "id": "PRD-v1",
        "date": "2026-07-31",
        "rule": "Traceability is a must, for money and tickets: money-relevant records land as immutable, hash-chained, signed journal entries from the first order (corrections = compensating entries; read models = projections), and every ticket/entitlement state change (issued, delivered, transferred, resold, exchanged, redeemed, reissued, invalidated) is an append-only lifecycle event linked to its order."
      }
    ],
    "reference": [
      {
        "summary": "Epic lineage and capability map",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "Service layout and ownership",
        "link": "docs/architecture.md"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-036",
        "why": "Catalog is the SINGLE authority for the rule-resolved unit price and commerce consumes `resolved_price` without recomputing it. Any re-pricing (an exchange) resolves through catalog and persists the §5 provenance snapshot for the NEW reservation — never copies the old snapshot."
      },
      {
        "id": "ADR-003",
        "why": "Corrections are COMPENSATING ENTRIES, never mutations. A refund/exchange appends a new signed fact; the original sale fact and its hash link are untouched. The payments journal has a DB trigger that raises on UPDATE/DELETE — any design that 'adjusts' a fact is rejected by the database, not by review."
      },
      {
        "id": "ADR-032",
        "why": "All provider money movement goes through the PSP port. `/internal/psp/refund` and the `payment_compensations` table already exist (TKT-114/116) — the PRIMARY KEY (organizer, source_idempotency_key, kind) is what converges duplicate refunds onto ONE provider idempotency key. Reuse it; do not add a parallel refund path."
      },
      {
        "id": "ADR-021",
        "why": "Every lifecycle event goes through store.appendLifecycle — the verifier asserts one-to-one coverage, so a direct INSERT into lifecycle_events reads as tampering and fails `access verify-lifecycle` in the gate. Canonical forms are pinned by golden literals; changing a byte is a canonical-version migration, not a test update. Also: name the adversary before writing 'tamper-evident'."
      },
      {
        "id": "ADR-011",
        "why": "The checkout journal protocol defines how commerce records money facts (order_facts row → payments /internal/facts, deterministic fact_id, idempotent). A post-purchase money movement uses the same protocol; do not invent a second one."
      },
      {
        "id": "ADR-010",
        "why": "Inventory's claim transaction is the contention path. Giving capacity back must go through the same row-locked transaction discipline; a plain UPDATE outside it can drive confirmed_quantity negative or double-return under concurrency."
      },
      {
        "id": "ADR-028",
        "why": "Fail closed. Responses are validated against the spec; an undeclared status code becomes a 500. Every new endpoint must declare every status it can answer, and a downstream answer commerce cannot validate aborts rather than degrades."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first. A new operation is written in the service's openapi.yaml first, then `openapi_gen.go` / `api-types.gen.ts` are regenerated and COMMITTED before the gate runs — check-generate diffs against HEAD, so an uncommitted regen reads as drift."
      },
      {
        "id": "ADR-030",
        "why": "Commerce and inventory ARE inside the smoke happy-path coverage gate. A new documented 2xx operation must be exercised through the running stack or the suite fails."
      },
      {
        "id": "ADR-022",
        "why": "Migrations run out-of-band via each binary's `migrate` subcommand as a one-shot Compose job. Add the SQL under the service's internal/store/migrations/ with the next number; the server path never migrates."
      }
    ]
  },
  "readiness": {
    "objective": {
      "state": "met",
      "note": "TKT-158 built the durable basis and then did not use it where it matters; an interrupted exchange can strand with the buyer charged."
    },
    "scope": {
      "state": "met",
      "note": "IN: resume-from-basis on unsettled replay, record-or-load basis semantics. OUT: the forward path (TKT-158), the entitlement switch (TKT-166), payments amount evidence (TKT-168)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Whether this is a handler-level resume or a runner is the same open question as TKT-163. Deciding both together is likely cheaper than deciding either alone."
    },
    "dependencies": {
      "state": "met",
      "note": "blocked-by TKT-158, which persists the basis this resumes from. Worth sequencing with TKT-163."
    },
    "approach": {
      "state": "met",
      "note": "Branch on a recorded basis immediately after the fingerprint check, before any external call; return the persisted basis from RecordExchangeBasis."
    },
    "first_slice": {
      "state": "met",
      "note": "An upgrade interrupted after the charge, resumed to completion with catalog stopped."
    },
    "success": {
      "state": "met",
      "note": "Demoable: charge, kill the settlement write, restart, replay the key, watch it finish with no second provider movement."
    },
    "context_memo": {
      "state": "met",
      "note": "Inherited from TKT-158. ADR-039 §1 and §3 govern; ADR-037 §5's stored-occurred_at rule applies to the facts written on resume."
    }
  },
  "links": [
    {
      "type": "blocked-by",
      "key": "TKT-158"
    },
    {
      "type": "found-in",
      "key": "TKT-158"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ Implementation readiness — PASS\n\nCarved out of TKT-158 at its churn-cap stop, with the design half-built: the basis it resumes from already exists and is already constrained by the database.\n\n**Concerns:** none blocking. One to carry: **design with TKT-163.** Both are \"an obligation interrupted after money moved needs a durable resume\", and solving them separately risks two mechanisms for one problem.\n\n**Verdict:** PASS."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Gate 1 — promoted to Ready (owner, 2026-08-17)\n\n**An exchange interrupted after the money moved must resume from what it persisted.**\n\nBlocker satisfied: `blocked-by TKT-158` is **Done**, and TKT-158 is precisely what persists the\nbasis this ticket resumes from.\n\n**Sequencing note for whoever claims it:** take this **before** TKT-168. This one fixes the\nbehaviour (an unsettled replay re-resolves the price instead of using the persisted basis, so a\nprice change makes inventory reject the retry); TKT-168 makes that behaviour *provable*. Doing them\nthe other way round produces a test that pins the current, wrong amounts.\n\n**Verified before promoting, not assumed:** all eight DoR items are `met` or `deferred` with none\n`open`, and every `blocked-by` link resolves to a **Done** ticket. Promoted as part of a sweep of\nevery fully-shaped Backlog ticket, on the owner's instruction.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — Ready → Planning\n\nRunning **gateless per `config.gates: autonomous`**: gates 2–4 are taken by agent judgement, Gate 1\nwas the owner's (2026-08-17 promotion comment). Local tracker backend (`config.tracker: \"local\"`).\n\n**Selection reason:** the owner asked for TKT-167 by name. It is genuinely in `Ready` (verified\nagainst the board, not the request), all eight DoR items are `met`/`deferred` with none `open`, and\nits single `blocked-by` — **TKT-158** — is **Done** (PR #143 merged). The ticket's own Gate 1 note\nsequences it **before TKT-168**, which is still in `Ready`; taking this one first is what that note\nasks for.\n\n**Risk verdict: `risk:low` NOT claimed.** Stated explicitly rather than left implicit. This touches a\n**money path** — the exchange settlement sequence in `services/commerce/internal/api/exchanges.go`\nand `RecordExchangeBasis` in `.../store/exchanges.go` — which is one of the label's named carve-outs.\nThe plan gate stands.\n\n**Branch:** `TKT-167-resume-exchange-from-basis` off `origin/main` (`a4a1b986`)."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — by `gpt-5.6-luna` (codex, effort high)\n\nRouted per `config.models.plan: \"gpt-5.6-luna\"`. Read-only run (companion `task`, no `--write`);\nthe working tree was clean afterwards. 6m25s. The drafter read the real code — `exchanges.go`\n(both api and store), migration 0010, the payments settlement smoke tests, `smoke/coverage_test.go`\nand inventory's `CreateHold` — rather than reasoning from the brief alone.\n\nBrief included the required **governing ADRs** section (re-resolved against `docs/adr/` at claim\ntime, not the mémo's 2026-07-31 snapshot) and the required **governing tests** section.\n\n---\n\n# TKT-167 Execution Plan\n\nNo files were edited. The working tree remains clean on `TKT-167-resume-exchange-from-basis`.\n\n## DoD\n\n- COS 1: An unsettled exchange with `basis_at` resumes from the persisted basis, with no catalog request or new inventory hold, and completes using the stored total and delta.\n- COS 2: The same resume test passes with catalog unavailable and with a changed target price.\n- COS 3: `RecordExchangeBasis` returns the authoritative stored basis and identifies whether the caller wrote it; a deterministic concurrent loser uses the winner’s basis.\n- COS 4: A retry after provider success causes exactly one payments-side movement, verified outside commerce’s exchange flags.\n- Quality: contract tests pass, integer minor-unit arithmetic remains unchanged, and `make check` is green.\n\n## Resume branch\n\nAdd the branch immediately after the settled replay at `services/commerce/internal/api/exchanges.go:100`, before the current `LoadExchangeSource` call at `:126`:\n\n```text\nif found && existing.BasisRecorded && !existing.Settled\n```\n\nThe branch should:\n\n1. Call `BindOrderExchange` as a replay-safe, commerce-database-only source read. Its existing-found path at `services/commerce/internal/store/exchanges.go:243-255` returns the persisted exchange plus `SourceReservation`, `BuyerID`, `HoldID`, `SlotID`, and `PaymentSourceKey`; it does not insert or re-run eligibility checks.\n2. Continue directly with the existing settlement sequence:\n   - step 10: `replacementGross`\n   - step 11: `transitionExchangeHold(..., \"finalize\")`\n   - step 12: `settleExchangeDelta`\n   - step 13: `transitionExchangeHold(..., \"confirm\")`\n   - step 14: `exchangeFacts`\n   - step 15: `persistExchangeReplacement`\n   - step 16: `CompleteExchangeSettlement`\n3. Return the normal exchange response with `replay: true`.\n\nFactor steps 10–16 into a new unexported helper, for example `completeExchangeFromBasis` (NEW), and call it from both the forward path and the resume branch. This prevents the two paths from acquiring different retry semantics.\n\nThe resume branch must not call:\n\n- `LoadExchangeSource` as an eligibility check;\n- `claimIsSeated`;\n- `repriceExchangeTarget`;\n- `holdExchangeTarget`;\n- a new exchange bind or new target hold.\n\nThe `BindOrderExchange` call is only the existing-row replay read and source-field refresh. It deliberately reaches the existing-row branch before the status/refund eligibility checks, so a source that became otherwise ineligible after money moved is still resumed. A persisted basis represents an already-created financial obligation; refusing it because current eligibility changed would strand settled money.\n\nThe catalog skip is compatible with ADR-036: catalog resolved the target at basis time, and the persisted `TargetUnitAmount`, `TargetTotal`, and `TargetPriceSnapshot` are the immutable result of that authority. Resume reuses that stored resolution rather than inventing a second price.\n\nThe existing retry-safe properties remain the basis of the shared helper:\n\n- inventory transitions are idempotent at `services/inventory/internal/store/store.go:665-701`;\n- charges use `exchange-charge:<exchange id>` at `services/commerce/internal/api/exchanges.go:469-480`;\n- refunds use `exchange-refund:<exchange id>` at `:483-491`;\n- facts use deterministic IDs and `ON CONFLICT DO NOTHING` at `:557-579`;\n- replacement persistence uses `ON CONFLICT(id) DO NOTHING` at `:630-637`;\n- settlement completion is guarded by `settled_at` at `services/commerce/internal/store/exchanges.go:400-430`.\n\n`exchangeFacts` must continue using `ex.CreatedAt` as its stored timestamp (`exchanges.go:557-560`), preserving ADR-037’s replay requirement.\n\n## Record-or-load semantics\n\nChange:\n\n```go\nRecordExchangeBasis(...) (bool, error)\n```\n\nto:\n\n```go\nRecordExchangeBasis(...) (ExchangeBasis, bool, error)\n```\n\nThe first result is always the authoritative persisted basis. The boolean is true only when this invocation wrote the basis.\n\nUse two statements:\n\n1. `UPDATE ... WHERE organizer_id=$1 AND id=$2 AND basis_at IS NULL RETURNING ...`\n2. If the update returns `sql.ErrNoRows`, `SELECT` the complete basis from the same exchange row.\n\nThe update must return all basis columns:\n\n- `target_hold_id`\n- `replacement_reservation_id`\n- `target_total`\n- `delta_amount`\n- `target_unit_amount`\n- `target_slot_id`\n- `target_price_snapshot`\n\nIf neither statement finds the exchange, return `sql.ErrNoRows`; do not return an apparently valid zero basis.\n\nPostgreSQL row-locking makes the concurrency behavior precise: a losing update waits for the winner’s row update/commit, then affects zero rows because `basis_at` is no longer null; its fallback `SELECT` reads the winner’s committed basis. The handler must always copy the returned basis into `ex`, even when the boolean is false. The current 409 path at `services/commerce/internal/api/exchanges.go:227-234` is removed.\n\n## Files touched\n\n- `services/commerce/internal/api/exchanges.go`\n  - Add the unsettled-with-basis branch after the settled replay.\n  - Factor steps 10–16 into a NEW shared helper.\n  - Use the authoritative basis returned by `RecordExchangeBasis`.\n  - Preserve existing 502/202/500 behavior; no new response status.\n\n- `services/commerce/internal/store/exchanges.go`\n  - Change `RecordExchangeBasis` to record-or-load with `UPDATE ... RETURNING` plus fallback `SELECT`.\n  - Return `ExchangeBasis`, writer ownership, and error.\n  - Keep the existing basis-shape and integer invariants enforced by migration 0010.\n\n- `services/commerce/internal/store/exchanges_smoke_test.go`\n  - Update existing call sites for the new signature.\n  - Add a deterministic concurrent record-or-load test.\n\n- `services/commerce/internal/api/exchanges_smoke_test.go` (NEW)\n  - `//go:build smoke` test package.\n  - Open and migrate `COMMERCE_TEST_DATABASE_URL`.\n  - Wire `New(db, client, catalogURL, inventoryURL, paymentsURL, token)`.\n  - Drive the real `exchangeOrder` handler through the real commerce DB and router, with `httptest` catalog, inventory, and payments services.\n\nNo migration is required: the existing basis columns already support this behavior. Migration `0020_reseller_credentials.sql` is currently the highest commerce migration. No `openapi.yaml`, generated file, or ADR changes are required because the operation and statuses are unchanged. Existing coverage in `smoke/happy_path_coverage_test.go:273-356` already exercises the documented exchange 2xx operation.\n\n## Test plan\n\n### COS 1 — resume from persisted basis\n\nFile: NEW `services/commerce/internal/api/exchanges_smoke_test.go`; API smoke tier with real commerce PostgreSQL and `httptest` dependencies.\n\nFixture:\n\n- Seed one completed source reservation/order with quantity greater than one and integer prices.\n- Catalog returns the original target resolution on the first call.\n- Inventory returns `seated:false`, creates one target hold, and accepts finalize/confirm.\n- Payments accepts the charge, then returns 503 for the first facts request. This creates the exact state: provider movement succeeded, facts/completion failed, `basis_at` set, `settled_at` null.\n- Retry with the same request.\n\nAssertions:\n\n- First request returns the existing journal-unavailable failure.\n- Retry returns 200.\n- Response `target_total` and `delta_amount` equal the original persisted basis.\n- The replacement reservation uses the stored unit amount, total, slot, hold, and price snapshot.\n- Catalog call count is one.\n- Inventory create-hold count is one.\n- `settled_at` becomes non-null and exactly one replacement order exists.\n\nMutation caught: deleting the resume branch causes the retry to call catalog and inventory again and fail or diverge. A lower-tier test cannot observe the real handler’s database state transition or failure after the provider call.\n\n### COS 2 — catalog unavailable and changed price\n\nFile: same NEW API smoke file; two subtests using the same fixture shape.\n\nAssertions:\n\n- Unavailable case: catalog returns 500 after the first attempt; retry still completes with catalog call count one.\n- Changed-price case: catalog would return a different valid unit amount on a second call; retry still completes with the original target total/unit amount and hold count one.\n\nMutations caught:\n\n- Removing the resume branch makes the unavailable case return 502.\n- Re-resolving the target on resume makes the changed-price case either use the wrong amount or hit inventory’s changed-fingerprint refusal.\n- Recreating the hold makes the hold count exceed one.\n\nThe inventory behavior is verified in `services/inventory/internal/store/store.go:438-463`: same key plus changed fingerprint returns `ErrIdempotency`.\n\n### COS 3 — record-or-load and concurrent loser\n\nFile: `services/commerce/internal/store/exchanges_smoke_test.go`; real PostgreSQL store smoke tier.\n\nFixture:\n\n- Bind one exchange.\n- Start a transaction that locks the exchange row with `SELECT ... FOR UPDATE`.\n- Start the loser goroutine calling `RecordExchangeBasis` with basis B.\n- Verify the loser is waiting on the row lock, then have the winner transaction write and commit basis A.\n- Assert the loser returns basis A, `written == false`, and does not return basis B.\n- Assert the initial writer returns basis A with `written == true`.\n\nUse distinct valid bases whose hold IDs, slot IDs, unit amount, total, delta, and snapshots all differ, so the test can distinguish local input from the persisted winner.\n\nMutations caught:\n\n- Returning the caller’s input after a zero-row update makes the loser return B.\n- Omitting the fallback `SELECT` makes the loser return no basis.\n- Removing `basis_at IS NULL` allows the loser to overwrite A.\n- Continuing the handler on the old local basis is caught by the exact basis comparison.\n\nExisting store tests at `exchanges_smoke_test.go:276-305` already establish the database constraints; this test adds the missing concurrency and authoritative-read behavior.\n\n### COS 4 — no second money movement\n\nFile: NEW `services/commerce/internal/api/exchanges_smoke_test.go`; real commerce DB plus a payments-side idempotency stub.\n\nPayments evidence:\n\n- The payments stub records every `/internal/charges` request by idempotency key.\n- It maintains a provider-side movement count keyed by `exchange-charge:<exchange ID>`, incrementing only on the first key and returning a replay thereafter.\n- Assert one provider-side movement for the exchange key after the initial failed-later-step request and its retry.\n- Also assert that all charge submissions use the same deterministic exchange key.\n\nThis is deliberately not an assertion on commerce’s `settled_at`, `Settled` flag, or response body. It counts provider-side movement in the payments test double. The existing real-payments proof remains `services/payments/internal/api/settlement_charge_smoke_test.go:180-203`, which counts `countingPSP.Authorize` calls and validates replay behavior; its durable operation evidence is the `payment_operations` row count at `:136-165`.\n\nMutation caught: deleting or changing the idempotency key in `settleExchangeDelta` causes the payments stub to record two movements. A commerce-only assertion would remain green if the provider call were replaced with a successful no-op.\n\n## Risks\n\n- The inventory behavior has been verified from source: changed request fingerprints under a reused key return `ErrIdempotency` at `services/inventory/internal/store/store.go:447-450`; same-key identical requests replay at `:463`. The resume branch must therefore never reconstruct the hold request.\n- A persisted hold may already be `finalizing` or `confirmed`; inventory’s transition code explicitly handles those replays. An expired hold remains a refusal, with no attempt to create a replacement hold.\n- `BindOrderExchange` must remain the source refresh path for resume because it supplies `SourceReservation`, `BuyerID`, `HoldID`, `SlotID`, and `PaymentSourceKey` at `services/commerce/internal/store/exchanges.go:253-255`. Omitting these fields breaks `persistExchangeReplacement` and downgrade settlement.\n- `SourceGrossTotal`, `TargetTicketTypeID`, `Quantity`, and `Currency` are already projected by `lookupExchange` at `:312-334`; do not add duplicate or mutable re-reads.\n- The record-or-load fallback must execute after the losing update has observed the winner’s commit. A test that merely calls the function twice sequentially would not prove concurrency.\n- No floats or new monetary representations may be introduced. Existing migration checks at `services/commerce/internal/store/migrations/0010_order_exchanges.sql:61-85` remain authoritative.\n- Do not alter `TargetPriceSnapshot` canonical bytes. Resume must persist the exact snapshot already stored at basis time.\n- Do not add a new HTTP status. The existing operation contract is `services/commerce/api/openapi.yaml:397-417`; generated files should remain unchanged.\n- Run generation before `make check` only if a contract change is accidentally introduced, and commit regenerated files before the gate as required by ADR-009.\n\n## Out of scope\n\n- Forward exchange behavior from TKT-158.\n- Entitlement switching from TKT-166.\n- Payments amount evidence from TKT-168.\n- Refund recovery from TKT-163.\n\nThe shared shape should remain reusable by TKT-163: persist the authoritative monetary basis before the provider call, resume from that basis, and use the payments-side idempotency identity as the no-double-movement boundary. TKT-163 should reuse the pattern, not be implemented or coupled into this ticket."
    },
    {
      "stage": "plan-review",
      "kind": "plan",
      "body": "<!-- sdlc:stage=plan-review kind=plan -->\n### Plan critique — by `main-agent` (adversarial)\n\nRouted per `config.models.planReview: \"main-agent\"`. Cross-model invariant holds: the draft is\n`gpt-5.6-luna`, this pass is the session model — `plan != planReview`.\n\n**Every file, symbol and line the draft names was checked against HEAD**, because a drafter that\ncannot run the gate hallucinates seams. Results below.\n\n#### Verification of the draft's factual claims — all PASS\n\n| Draft claim | Verdict |\n|---|---|\n| `BindOrderExchange`'s `if found` branch returns **before** the `status != \"completed\"` check and the `order_refunds` exclusion, and sets `SourceReservation, BuyerID, HoldID, SlotID, PaymentSourceKey` | **TRUE** — `store/exchanges.go:245-255`. This is the draft's best find and it removes the need for any new store function. |\n| `lookupExchange` already projects `SourceGrossTotal`, `Quantity`, `Currency`, `TargetTicketTypeID` and every basis column | **TRUE** — `store/exchanges.go:311-334`. |\n| Inventory refuses a reused key with a changed fingerprint | **TRUE**, and sharper than the ticket states it. `fingerprint(org, slot, ticketType, qty, unitAmount, currency, channel, presaleCode, reseller)` at `services/inventory/internal/store/store.go:447` includes **`unitAmount`** — so a target price change is *exactly* what makes the same `exchange-target:` key return `ErrIdempotency`. The ticket's causal claim is now executed-and-read, not argued. |\n| Inventory transitions replay: `c.Status == target` is satisfied, and `finalizing` on a `confirmed` claim is satisfied | **TRUE** — `store.go:667-676`. |\n| Settlement guarded on `settled_at IS NULL`; facts `ON CONFLICT DO NOTHING` with deterministic ids; replacement `ON CONFLICT(id) DO NOTHING` | **TRUE**. |\n| No migration needed; 0020 is the highest commerce migration | **TRUE**. |\n| The exchange operation is already inside the happy-path coverage gate | **TRUE** — `smoke/happy_path_coverage_test.go:273+` drives a real exchange through the gateway. |\n| No new HTTP status is introduced | **TRUE of the plan** — but see [amend-1]. |\n\n#### Accepted without change\n\n- **The resume branch's placement** (after the settled replay, before `LoadExchangeSource`) and its\n  refuse-list (no `claimIsSeated`, no `repriceExchangeTarget`, no `holdExchangeTarget`).\n- **Using `BindOrderExchange` as the source refresh.** It is a commerce-DB-only read whose replay\n  branch precedes eligibility, so it costs nothing against COS 1/2 and supplies the five fields\n  `LookupExchangeFor` does not project. The draft answered the brief's explicit question here\n  rather than assuming.\n- **Resume even when the source became ineligible.** Correct, and for the reason given: the money\n  already moved. Refusing would strand it — which is the exact failure this ticket exists to close.\n- **The ADR-036 argument.** Catalog *was* the authority; the persisted snapshot is that authority's\n  result, and reusing it is not a second pricing. §5's provenance requirement is satisfied by\n  storing the snapshot taken at basis time, which `persistExchangeReplacement` already does.\n- **record-or-load via `UPDATE … RETURNING` + fallback `SELECT`**, returning\n  `(ExchangeBasis, bool, error)` and `sql.ErrNoRows` when the row is absent rather than a zero\n  basis. The row-lock reasoning (loser waits, then affects zero rows, then reads the committed\n  winner) is correct for PostgreSQL's read-committed default.\n- **Factoring steps 10–16 into one shared helper** so the forward and resume paths cannot acquire\n  different retry semantics. This is the right structural call, not just tidiness.\n\n#### Amendments\n\n**[amend-1] — the 202 is an UNDECLARED status, and it is on this ticket's own path.**\nThe draft says \"no new HTTP status\" and \"generated files should remain unchanged\". True of what it\nproposes — but it inspected `openapi.yaml:397-417` and did not notice that `exchangeOrder` declares\n`200/400/404/409/500/502/503` and **not 202**, while the handler writes\n`202 {\"status\":\"confirmation_pending\"}` at `api/exchanges.go` on a confirm failure. Commerce mounts\n`contract.RequestValidatorWithSecurity(..., validateResponses)` (`api/server.go:239`) and\n`runtimecfg.ResponseValidationFromEnv` **defaults it on** (`cmd/commerce/main.go:180`), so under\nADR-028 that 202 fails closed as a **500** in any real deployment.\n\nThis is a pre-existing TKT-158 defect, but it is not incidental to TKT-167: the 202 branch is\n*precisely* the \"charged, not settled\" state this ticket resumes from. The caller that must retry is\ntoday told `500 persist exchange` instead of `202 confirmation_pending` — an outage-shaped answer to\na recoverable state, which is the worst possible signal for the recovery path being built here.\n\n**Amendment:** declare `'202'` on `exchangeOrder` in `services/commerce/api/openapi.yaml` with a\nschema for `{exchange_id, status}`, regenerate `openapi_gen.go` and `api-types.gen.ts`, and\n**commit the regen before running the gate** (ADR-009; `check-generate` diffs HEAD). Add a test that\npins the 202 as declared. This turns [amend-1] into a contract change, so the draft's \"no generated\nfile changes\" line is superseded.\n\n*Alternative considered and rejected:* file it as an incidental backlog ticket. Rejected because\nthe ticket's COS 1 says the retry \"completes\", and the state it retries FROM is unreachable-as-\nspecified while the first attempt 500s. Shipping the resume without this would mean the demo in the\n`success` readiness note — \"kill the settlement write, restart, replay the key, watch it finish\" —\ncannot be driven through the documented contract.\n\n**[amend-2] — the new API-tier smoke seam must justify its own existence, per the repo's\ntier rule.** The draft proposes `services/commerce/internal/api/exchanges_smoke_test.go` (NEW,\n`//go:build smoke`, real DB + `httptest` stubs). Accepted — it is genuinely the only tier that can\ncount catalog calls on a retry while observing real `order_exchanges` rows. But `AGENTS.md`'s rule\nis *\"a test must live at the tier its mechanism does\"*, and the corollary the draft did not state:\n**every stub in that fixture must be able to fail the test.** Amendment: the test plan must say, per\nstub, what deleting it turns red. A stub catalog that is never called on the resume path is the\npoint of COS 1 — so its call **counter** is load-bearing and its response body is not; say so, and\ndo not seed a response the resume can silently consume.\n\n**[amend-3] — COS 4's evidence is a test double, and the plan must not overclaim it.** The draft\nproposes counting movements in the `httptest` payments stub, and separately cites the real\n`countingPSP` proof at `services/payments/internal/api/settlement_charge_smoke_test.go:180-203`.\nThat is honest but the wording drifts: a stub in commerce's own test binary is **commerce-side**\nevidence of what commerce *sent*, not payments-side evidence of what *moved*. Amendment: state it\nas what it is — *\"exactly one charge submission under the deterministic key\n`exchange-charge:<id>`\"* — and rest the no-double-movement claim on payments' existing\n`countingPSP` + `payment_operations` proof that one key means one `Authorize`. Two facts, composed,\neach proven where its mechanism lives. Do **not** write \"proven against payments-side evidence\"\nabout a stub.\n\n**[amend-4] — the delta==0 case must be in the fixture, or the test cannot see the bug it\nguards.** `settleExchangeDelta` returns nil immediately when `delta == 0` — no provider call at all.\nA resume fixture built only on an upgrade proves the charge is not repeated; it says nothing about\nwhether the resume path is even reachable when nothing moved. Amendment: the COS 1 fixture uses an\n**upgrade** (delta > 0) so there is a real provider movement to not repeat — that is the ticket's\nnamed first slice and it must stay — and one subtest covers **delta == 0**, where the interruption\nis after `finalize` and the assertion is that the resume completes without a catalog call. A\ndowngrade subtest is *nice-to-have*, not required: it exercises a different payments endpoint but\nthe same resume branch.\n\n#### Rejected\n\n**[reject-1] — \"Run generation before `make check` only if a contract change is accidentally\nintroduced.\"** Wrong framing, and [amend-1] makes it moot: the contract change is now deliberate.\nRegeneration is a planned step, not a contingency.\n\n#### Pre-mortem — it is three weeks later and this shipped broken. What happened?\n\n1. **The resume branch was never entered**, because `LookupExchangeFor` returned\n   `ErrExchangeConflict` on a fingerprint mismatch and the retry did not reproduce the original\n   `actor`/`reason` byte-for-byte (`exchangeFingerprint` hashes source order + target type + actor +\n   reason). A recovery caller that reconstructs the request from memory gets a 409, not a resume.\n   **Mitigation:** the COS 1 test must retry with the *same* request body, and one assertion must\n   pin that a *different* actor still conflicts — otherwise the resume looks reachable in the test\n   and is not in the field.\n2. **The test passed against a fixture that could not fail.** The stub catalog kept answering, so\n   \"no catalog call\" was asserted by a counter nobody could drive to 1 — or the interruption was\n   simulated by writing `order_exchanges` directly, so `basis_at` was set by the fixture rather than\n   by the code under test and the handler's own basis write was never exercised.\n   **Mitigation:** drive the interruption through the **handler** (payments stub 503s the facts\n   call), never by seeding the row. This is the repo's standing rule and it is the single most\n   likely way this ticket ships vacuous.\n3. **The shared helper changed the forward path's semantics.** Extracting steps 10–16 is a\n   refactor of a money path; a subtly different error mapping in the extracted helper changes what\n   the *forward* path answers on a payments failure, and no COS covers the forward path.\n   **Mitigation:** the existing exchange tests must stay green **unmodified**; if any needs editing\n   to pass, that is the signal, not a chore.\n4. **The 202 fix was skipped as out of scope** and the recovery caller kept seeing 500s.\n   Addressed by [amend-1].\n\n#### Introduced at plan-review, reviewed by no second model\n\nPer the skill's required section — these reached the gate reviewed only by their author (me):\n\n1. **[amend-1]**, the openapi `202` declaration + regeneration. This is a *contract change* the\n   draft did not propose, and it is the largest single addition. It carries real risk: a new\n   declared status touches `openapi_gen.go`, `api-types.gen.ts`, and ADR-030's coverage gate.\n2. **[amend-4]**, the delta==0 subtest requirement.\n3. **Pre-mortem item 1** (the fingerprint/actor reachability trap) and its assertion requirement.\n4. **Pre-mortem item 3** (existing exchange tests must stay green *unmodified* as the refactor's\n   guard).\n\nThese four are named again in the Gate 2 hand-off below.\n\n**Verdict: ACCEPT with amendments 1–4 and reject-1.** The draft's structure is sound and its\ncentral factual find is correct and load-bearing."
    }
  ],
  "status": "Planning",
  "labels": [
    "agent:plan-review"
  ],
  "assignee": "agent",
  "pr": null
}
