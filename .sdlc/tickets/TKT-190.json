{
  "key": "TKT-190",
  "type": "Story",
  "summary": "US-B1: Staff sign-in — a session gates the back office",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "url": "https://github.com/mathd/ticketing_system/pull/155",
    "number": 155,
    "state": "merged"
  },
  "parent": "TKT-22",
  "description": "**As** an organizer's staff member, **I want** to sign in to the back office with an account, **so that** the admin surface is not open to anyone who can reach the gateway.\n\nToday `/admin/` is served to any caller and every catalog write endpoint is reachable unauthenticated through `/api/catalog/`. This story introduces the staff principal and the session that carries it. Roles arrive in US-B2 — this story only proves *who*, not *what they may do*.\n\n**In scope:** a staff account store (identifier + KDF-hashed password + role column, unused until US-B2), a bootstrap account provisioned by a documented operator step, a login form, a signed/opaque session cookie, an edge check that refuses `/admin/*` without a valid session, sign-out, and an ADR recording where staff identity lives and the CSRF posture.\n\n**Out of scope:** SSO/OIDC, password reset, MFA, self-service signup, multi-organizer tenancy, rate limiting on login (name it as a follow-up ticket, do not build it).\n\n**Conditions of Success:**\n- [ ] An unauthenticated request to `/admin/` **and** to any `/admin/<subpath>` is refused/redirected to the login page — the venue list never renders.\n- [ ] A provisioned staff account signs in through the login form and lands on the venue list; the session survives a page reload.\n- [ ] Sign-out invalidates the session server-side: replaying the same cookie no longer opens `/admin/`.\n- [ ] A wrong password and an unknown account are indistinguishable to the caller (same status, same body, no timing-obvious short-circuit).\n- [ ] The password is stored only as a salted hash from a vetted KDF; no plaintext password appears in any log line or trace attribute.\n- [ ] The session cookie is `HttpOnly`, `SameSite=Lax`, `Path=/`, `Secure` when served over TLS, and has a bounded lifetime.\n- [ ] An ADR records: where staff identity lives (and why, against ADR-002's five-service cut), the session mechanism, and how CSRF is addressed now that a cookie-bearing session coexists with `checkOrigin:false`.\n\n**Quality gates (every story):**\n- `make check` green (lint + test + build, Go & TS + gateway smoke suite).\n- Contract tests pass for any touched service boundary; OpenAPI regenerated and committed before the gate (ADR-009).\n- UI story: the write forms are **submitted** against the real stack (`make up`), not just rendered — API smoke cannot see the SSR layer (TKT-105 learning).\n- Read endpoints declare their ADR-004 TTL tier; auth/session responses are `no-store`.\n",
  "links": [],
  "context": {
    "binding": [],
    "governingAdrs": [
      {
        "id": "ADR-002",
        "why": "The gateway is the single public entry point and its route table IS the security boundary; /api/<svc>/internal/ is unreachable from the edge by construction. Anything this ticket adds to the edge must keep that construction true."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first: a new HTTP surface is authored in the service's openapi.yaml and the generated code committed BEFORE the gate runs — check-generate diffs against HEAD, so an uncommitted regen reads as drift."
      },
      {
        "id": "ADR-028",
        "why": "Response drift fails closed: every status code the handler can emit must be declared, or the validator rejects the response at runtime."
      },
      {
        "id": "ADR-006",
        "why": "The back-office is Astro SSR behind the gateway. `security.checkOrigin` is already false in web/backoffice/astro.config.mjs and form actions must be RELATIVE — an absolute action trips the check even same-origin."
      },
      {
        "id": "ADR-003",
        "why": "Sets the no-raw-PII-in-append-only-records rule. A staff account's identifier is PII: if any auth event is written to an append-only trail here, it carries a pseudonymous staff id, never the email."
      },
      {
        "id": "ADR-004",
        "why": "Declares the volatility tiers. Auth and session responses are the 'never' tier — Cache-Control: no-store, and the login page must not be cached by the proxy."
      }
    ],
    "reference": [
      {
        "summary": "checkOrigin is already disabled for the back-office and form actions must be relative — the trap that silently broke two tickets' write forms.",
        "link": "docs/learnings/2026-07-20-browser-submit-is-the-only-checkorigin-catch.md"
      },
      {
        "summary": "INTERNAL_SERVICE_TOKEN (shared/go/runtimecfg) is the existing service-to-service credential and the precedent for 'no default ships, startup fails without one' (TKT-83). A bootstrap staff credential should follow the same shape.",
        "link": "shared/go/runtimecfg/runtimecfg.go"
      },
      {
        "summary": "Gateway route table + edge denial helper — the place an edge auth check attaches.",
        "link": "gateway/cmd/gateway/main.go"
      },
      {
        "summary": "The gateway has no database today; every other service owns one (compose.yaml, per-service *-migrate one-shot jobs per ADR-022).",
        "link": "compose.yaml"
      }
    ],
    "capturedAt": "2026-08-03"
  },
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Problem and why stated in the description; lineage to TKT-22's COS."
    },
    "scope": {
      "state": "met",
      "note": "In-scope and out-of-scope both enumerated on the ticket."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Named in the description's out-of-scope list; the remaining design choices are safely settleable in Planning without a mid-sprint redesign."
    },
    "dependencies": {
      "state": "met",
      "note": "blocked-by links wired; catalog/commerce/access endpoints this depends on are shipped and stable."
    },
    "approach": {
      "state": "deferred",
      "note": "Where staff identity lives (gateway-owned store vs a sixth service) is settled by US-B1's plan and recorded as an ADR to be authored; later stories inherit that decision."
    },
    "first_slice": {
      "state": "met",
      "note": "The ticket IS one vertical slice — schema/backend/UI/tests — demoable on its own."
    },
    "success": {
      "state": "met",
      "note": "COS are verifiable criteria plus the project quality gates."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked at decomposition with governingAdrs resolved against docs/adr/ at HEAD 2026-08-03; every cited ADR file verified to exist."
    }
  },
  "comments": [
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n**Claimed.** Branch `tkt-190-staff-sign-in` off `origin/main` @ `796e9054`.\n\n**Blockers:** none. TKT-190 carries no `blocked-by` edge — it is the root of the TKT-22 chain and\nthe prefactor every later child gates on.\n\n**Selection reason:** the only claimable child of TKT-22. TKT-191…194 are all transitively blocked\nby this ticket (191 → 190; 192 → 191; 193 → 191; 194 → 193), so it also has the highest unblocking\npower on the board — closing it releases the entire epic.\n\n**Risk:** `risk:low` is **not** claimed and must not be. This ticket introduces authentication, a\nnew credential store and a session cookie: it is the exact class the label's invariant carves out.\nThe plan gate applies (waived for this run per the Gate-1 waiver on TKT-22, and replaced by the\nself-resolution rule recorded there — not skipped).\n\n**Gates:** this ticket runs under the owner's gateless waiver recorded on TKT-22. Gate 2, 3 and 4\nare replaced, not removed; every marker comment, the cross-model reviews and the local gate are\nunchanged.\n\n**Model routing for this ticket** (`.claude/sdlc.config.json`, no substitutions):\n`plan` → `gpt-5.6-sol` (codex) · `planReview` → main-agent · `implement` → main-agent ·\n`aiReview` → `gpt-5.6-sol` (codex). Both cross-model pairs hold: plan ≠ planReview, implement ≠ aiReview."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n**Plan draft — written by `gpt-5.6-sol` (codex, effort high, read-only, no `--write`).**\n`config.models.plan = gpt-5.6-sol`; no substitution. Session `019fc9ee-7e18-75c3-a4ee-2c54d3a14c0f`.\nBrief: ticket + COS + the context-mémo's `governingAdrs` (re-resolved against `docs/adr/` at claim\ntime) + verified code facts. The drafter read every ADR in `docs/adr/`, the gateway, the Astro\nback-office, `shared/go/obs` and the Astro 7 node-adapter internals before answering. It confirms\nit modified no file.\n\nCritique follows in `kind=plan-final`; per the skill the critic is never handed the author's\njustification, and this draft is reproduced verbatim below as the audit record.\n\n---\n\n# TKT-190 execution plan — Staff sign-in gates the back office\n\n## Decision\n\nStore staff accounts in the existing Catalog database, because Catalog already owns organizers and tenant-scoped configuration. Authenticate through a new internal Catalog endpoint. Keep opaque, server-side sessions in the Astro back-office process and enforce authentication in Astro middleware before any protected `/admin/*` route renders.\n\nThis preserves ADR-002’s five-service boundary: no sixth service, no gateway database, and no externally reachable internal endpoint. The ticket ADR records this as a refinement of Catalog’s ownership rather than a deviation.\n\n| Candidate | What it buys | Cost | Reversibility |\n|---|---|---|---|\n| Gateway owns the staff store and enforces at the edge | One enforcement point before Astro; all admin traffic is stopped at the public boundary | Gives the currently stateless gateway a database, migrations, KDF and session responsibilities; requires a new migration job/database role; mixes identity storage with routing; complicates login and health exceptions | Low–medium. The route wrapper is removable, but moving credentials and sessions out of a gateway schema later is a data migration |\n| Sixth `identity` service | Clean reusable identity boundary and natural home for future authentication expansion | Direct ADR-002 deviation; new service, database, OpenAPI, migrations, Compose jobs, health/readiness and operational ownership; much larger review surface | Low after adoption because every caller becomes coupled to it |\n| Astro middleware and session store, backed by Catalog accounts | Enforcement sits next to the protected SSR pages and forms; reuses Catalog’s organizer database and existing internal credential; no public route or database topology change | Sessions are initially process-local; a back-office restart signs everyone out, and multiple replicas are unsupported until sessions move to shared storage | High. A later identity service replaces one internal client, and a persistent session store replaces one module without moving the page-level enforcement |\n\nRecommendation: the third option.\n\nThe concrete split is:\n\n- Catalog owns persistent `staff_accounts`.\n- The role is stored as opaque, non-empty text but is neither returned in the authentication response nor loaded into the session. TKT-191 defines and enforces role semantics.\n- A new internal `POST /internal/staff/authenticate` verifies credentials and returns only `staff_id` and `organizer_id`.\n- Astro calls that endpoint directly over the Compose network using `CATALOG_URL` and `X-Internal-Token`. It must not call it through `/api/catalog/internal/`, which remains edge-denied.\n- Astro creates a random 256-bit opaque session token and retains the principal and absolute expiry in an in-process map.\n- New `web/backoffice/src/middleware.ts` enforces the session for protected admin routes.\n- Explicit unauthenticated exceptions are limited to:\n\n  - `GET`/`HEAD /admin/login`\n  - `GET`/`HEAD /admin/healthz`\n  - `/admin/_astro/*` assets required to render the login page\n\nAll other paths below `/admin/`, including unknown paths, pass through authentication before routing. This means anonymous callers cannot distinguish a real protected page from an unknown admin path.\n\nA back-office process restart invalidating every session is safe and documented. Shared or restart-surviving sessions are deliberately deferred rather than introducing another persistent store in this ticket.\n\n## Account and session shape\n\nThe new Catalog migration creates `staff_accounts` with:\n\n- UUID primary key\n- `organizer_id` referencing the existing organizer table\n- original identifier plus a normalized, organizer-scoped unique lookup key\n- bcrypt password hash\n- opaque non-empty `role`\n- ordinary creation/update timestamps\n\nThe identifier may be PII, but this is a mutable account table, not an append-only audit record. No sign-in event is added to an append-only trail in this ticket.\n\nUse `golang.org/x/crypto/bcrypt`, already present indirectly in the Catalog module, as the vetted salted adaptive KDF. It becomes a direct dependency. Authentication performs exactly one bcrypt comparison for both known and unknown identifiers; an unknown identifier is compared with a fixed, valid dummy bcrypt hash at the same cost. The final result remains invalid even if comparison with the dummy hash succeeds.\n\nThe bootstrap command is a new Catalog subcommand:\n\n```text\ncatalog provision-staff --organizer-id … --identifier … --role … --password-stdin\n```\n\nIt:\n\n- reads the password only from standard input;\n- never accepts it through an argument or logs it;\n- creates rather than silently overwrites an existing account;\n- prints only the new pseudonymous staff UUID;\n- assumes migrations were already run by the existing `catalog-migrate` job.\n\nThe session cookie has:\n\n- a non-identifying opaque value;\n- `HttpOnly`;\n- `SameSite=Lax`;\n- `Path=/`;\n- `Secure` when the trusted forwarded protocol is `https`;\n- `Max-Age` and `Expires` matching an eight-hour absolute lifetime.\n\nSession lookup rejects and lazily removes expired entries. Sign-out deletes the entry before emitting an expired cookie, so replaying the captured token fails server-side.\n\nAll login, logout, authentication-error and authenticated admin responses carry `Cache-Control: no-store`.\n\n## Explicit CSRF posture\n\nAstro’s built-in `security.checkOrigin` remains disabled because Astro’s Node adapter constructs its request URL from the upstream socket and rewritten `Host`; it does not reconcile that URL with the gateway’s forwarded public host and protocol. Simply turning it back on would recreate TKT-105 and would still be wrong behind TLS termination.\n\nInstead, the new Astro middleware performs a proxy-aware origin check for every unsafe method:\n\n1. Construct the expected public origin from `X-Forwarded-Proto` and `X-Forwarded-Host`.\n2. Require the request’s `Origin` header to match it exactly.\n3. Reject a missing or mismatched origin with a generic `403` and `Cache-Control: no-store`.\n4. Run this check before login, logout, session mutation, or existing admin form handlers.\n\nThe gateway’s `ReverseProxy.SetXForwarded()` supplies these values from the actual inbound gateway request. Direct unsafe requests to the Astro container without those headers fail closed; the direct Compose health probe remains unaffected because it is a `GET`.\n\n`SameSite=Lax` is defense in depth, not the primary CSRF control. A synchronizer token is not selected because the trusted gateway already supplies the original origin, while tokens would add state and hidden fields to every existing seat-map form. If the back office is later exposed anywhere except through this gateway, the proxy trust decision must be revisited.\n\nTests must prove both sides: a same-origin form submission succeeds, while a cross-origin logout is refused and leaves the session usable.\n\n## Contract and implementation sequence\n\nBecause authentication adds a service HTTP surface:\n\n1. Write and observe the contract/API tests red.\n2. Edit [services/catalog/api/openapi.yaml](/Users/matdupui/Sources/Mathieu/ticketing_system.tkt-184-tighten-review-findings/services/catalog/api/openapi.yaml).\n3. Declare every possible response: `200`, `400`, `401`, and `500`, including `Cache-Control: no-store`.\n4. Run `make generate`.\n5. Commit the OpenAPI and all three generated outputs before the final gate:\n\n   - `services/catalog/internal/api/openapi_gen.go`\n   - `web/backoffice/src/lib/api-types.gen.ts`\n   - `web/storefront/src/lib/api-types.gen.ts`\n\n6. Implement the handler and store.\n7. Run `make check` as the sole command in its final shell invocation.\n\nThe endpoint remains unreachable from outside because the existing `/api/catalog/internal/` gateway registration still resolves to `edgeDenied`.\n\n## Test seam and COS coverage\n\n| COS | Test and layer |\n|---|---|\n| 1. Anonymous root and subpaths refused | Vitest exercises the new middleware’s route policy. `smoke/backoffice_venue_list_test.go` replaces the anonymous-success expectation in `TestBackofficeShellServedThroughGateway`: with redirects disabled, `GET /admin/`, an existing venue path and an unknown admin path must redirect to `/admin/login`; the bodies must not contain `La Grande Salle`. It also verifies `/admin/healthz` remains healthy. |\n| 2. Provision, login, venue list, reload | `scripts/smoke.sh` generates an unlogged random password and provisions the account through the real new Catalog CLI. The smoke test fetches the real login form, checks its relative action and field names, submits the same URL-encoded form with the gateway origin, follows the success redirect and sees the seeded venue. A second GET with the retained cookie must still render it. |\n| 3. Server-side sign-out | The smoke test captures the original session cookie, submits the real relative logout form, then manually replays the captured cookie in a client without a cookie jar. `/admin/` must redirect to login. A session unit test separately proves deletion and expiry behavior. |\n| 4. Unknown and wrong passwords indistinguishable | `services/catalog/internal/store/staff_test.go` uses an injected comparison seam to prove exactly one KDF comparison on both lookup outcomes. `services/catalog/internal/api/server_test.go` proves the same Catalog status/body. The composed login smoke test submits an unknown identifier and a wrong password and compares the final status and response bytes exactly. The page must not echo the submitted identifier. |\n| 5. Salted KDF hash; no plaintext logging | Store tests verify two provisions of the same password produce distinct hashes and both verify through bcrypt; the database never contains the input password. Catalog handler/CLI tests capture logs and assert the password is absent. Existing `obs.RequestLogger` logs method/path/status/duration rather than bodies; the implementation must add no password-bearing log or trace attributes. |\n| 6. Cookie attributes and lifetime | `web/backoffice/test/session.test.ts` asserts `HttpOnly`, `SameSite=Lax`, `Path=/`, eight-hour `Max-Age`/`Expires`, conditional `Secure`, expiration and deletion using a fake clock. Smoke checks the real HTTP `Set-Cookie`; the HTTPS-specific branch remains a unit test because the local stack is HTTP. |\n| 7. ADR and CSRF posture | New ADR-042 records placement, reversibility, session mechanics, CSRF enforcement and the adversary boundary. Vitest proves origin matching and missing/mismatched rejection; smoke proves same-origin login and an existing venue mutation pass through the real gateway. |\n\nThe existing seams being extended are:\n\n- `newFakeStore()` in `services/catalog/internal/api/server_test.go`\n- operation coverage in `services/catalog/internal/api/coverage_test.go`\n- `get()` and `getWithHeaders()` in `smoke/smoke_test.go`\n- `TestBackofficeShellServedThroughGateway` in `smoke/backoffice_venue_list_test.go`\n\nNew smoke helpers, explicitly new, will provide a redirect-disabled cookie client and URL-encoded form submission.\n\nThese composed-stack tests exercise the real gateway, Astro SSR server, Catalog service, PostgreSQL migration, cookie and redirect headers. They do not prove browser cookie-policy enforcement, DOM form serialization, navigation behavior, or Astro’s behavior in a real browser engine. The DoD therefore retains a manual browser submission check.\n\n## Red-first TDD order\n\nNo production implementation for a slice begins until its targeted test has been run and failed for the stated reason.\n\n1. **Anonymous gate smoke test**\n\n   Add the root, existing subpath and unknown subpath assertions. They fail because current `/admin/` returns `200` and renders `La Grande Salle`.\n\n2. **Middleware policy tests**\n\n   Assert protected paths redirect, login/assets/health are the only exceptions, and unsafe requests require the forwarded-origin match. They fail because `src/middleware.ts` does not exist.\n\n3. **Migration and account-store tests**\n\n   Assert the staff table, organizer-scoped uniqueness, role presence, create-only provisioning and bcrypt hashing. They fail because migration `0015` and the store operations do not exist.\n\n4. **Unknown-account KDF test**\n\n   Assert known-wrong and unknown identifiers each invoke the comparison seam exactly once using valid same-cost hashes and return the same invalid result. It fails because no authenticator exists. The dummy fixture is a fixed literal not generated from the implementation under test.\n\n5. **Internal API contract tests**\n\n   Assert the authentication operation exists, declares `200/400/401/500`, carries `no-store`, returns a principal on success and byte-identical `401` responses for wrong and unknown accounts. Initially the path is absent/404 and operation coverage fails.\n\n6. **Bootstrap command tests**\n\n   Assert password input comes from stdin, the stored value is a bcrypt hash, duplicate creation fails, and captured output/logs omit the password and identifier. Initially the subcommand is unknown.\n\n7. **Session tests**\n\n   Assert random-token creation, reload lookup, absolute expiry, deletion, replay failure and exact cookie attributes for HTTP and forwarded HTTPS. They fail because the session module does not exist.\n\n8. **Login and logout smoke tests**\n\n   Provision through the packaged CLI, submit the real login form, reload, submit logout and replay the old cookie. They fail first because the pages/session flow are absent.\n\n9. **Caller indistinguishability and CSRF interaction smoke tests**\n\n   Compare unknown/wrong login bytes, reject cross-origin logout without deleting the session, then submit one existing seat-map write form successfully with the correct origin. These fail until the generic error page and proxy-aware CSRF layer compose correctly.\n\nAfter each batch passes locally, run the broader relevant package tests. The final full gate is `make check`, alone in its command invocation.\n\n## Files\n\n### Catalog\n\n| File | Change |\n|---|---|\n| `services/catalog/internal/store/migrations/0015_staff_accounts.sql` **NEW — migration risk** | Add the organizer-scoped staff account table and constraints. |\n| `services/catalog/internal/store/staff.go` **NEW** | Account provisioning, lookup and bcrypt authentication with the dummy-hash path. |\n| `services/catalog/internal/store/store.go` | Add the focused staff operations and account/principal types to the existing store contract. |\n| `services/catalog/internal/store/staff_test.go` **NEW** | KDF, normalization, duplicate and unknown-account unit tests. |\n| `services/catalog/internal/store/staff_smoke_test.go` **NEW** | Exercise provisioning/authentication against migrated PostgreSQL. |\n| `services/catalog/internal/store/migration_smoke_test.go` | Prove migration `0015` is applied out-of-band and its constraints exist. |\n| `services/catalog/api/openapi.yaml` | Contract-first internal authentication operation and complete response set. |\n| `services/catalog/internal/api/openapi_gen.go` | Regenerated Catalog server types and interface. |\n| `services/catalog/internal/api/server.go` | Implement the internal authentication operation and `no-store` responses. |\n| `services/catalog/internal/api/server_test.go` | Extend existing `newFakeStore()` and test success/error equivalence. |\n| `services/catalog/internal/api/coverage_test.go` | Add the new operation to Catalog’s per-operation coverage proof. |\n| `services/catalog/cmd/catalog/main.go` | Register and implement the `provision-staff` subcommand. |\n| `services/catalog/cmd/catalog/main_test.go` **NEW** | Test stdin-only provisioning and secret-free output/logging. |\n| `services/catalog/go.mod` | Promote the already-resolved `golang.org/x/crypto` module to a direct bcrypt dependency. |\n\n### Back office\n\n| File | Change |\n|---|---|\n| `web/backoffice/astro.config.mjs` | Retain `checkOrigin:false`, replacing its revisit comment with the chosen proxy-aware middleware posture. |\n| `web/backoffice/src/env.d.ts` **NEW** | Type the authenticated `Astro.locals` principal. |\n| `web/backoffice/src/lib/api.ts` | Add the direct internal Catalog authentication client and replace the default organizer on authenticated page calls with the session principal. |\n| `web/backoffice/src/lib/session.ts` **NEW** | Opaque token generation, process-local session storage, expiry, deletion and cookie construction. |\n| `web/backoffice/src/middleware.ts` **NEW** | Authentication gate, explicit exceptions, `no-store`, and proxy-aware origin enforcement. |\n| `web/backoffice/src/pages/login.astro` **NEW** | GET/POST login form with a relative action and generic invalid-credentials response. |\n| `web/backoffice/src/pages/logout.ts` **NEW** | POST-only server-side invalidation and cookie expiry. |\n| `web/backoffice/src/pages/index.astro` | Use the authenticated organizer and expose a relative logout form on the landing page. |\n| `web/backoffice/src/pages/venues/[id].astro` | Source the organizer from `Astro.locals` for reads and writes. |\n| `web/backoffice/src/lib/api-types.gen.ts` | Regenerated Catalog contract types. |\n| `web/backoffice/test/api.test.ts` | Test the internal authentication request, token header and error mapping. |\n| `web/backoffice/test/session.test.ts` **NEW** | Session lifetime, invalidation and cookie tests. |\n| `web/backoffice/test/middleware.test.ts` **NEW** | Route exemptions, gate behavior, CSRF matching and `no-store` tests. |\n\n### Stack, smoke and documentation\n\n| File | Change |\n|---|---|\n| `compose.yaml` **Compose risk** | Give back-office SSR `CATALOG_URL` and the existing `INTERNAL_SERVICE_TOKEN`; reuse the existing `catalog-migrate` job—no new database or migration job. |\n| `scripts/smoke.sh` | Generate an unlogged smoke password, provision through the packaged Catalog CLI and pass the credential only to the smoke process. |\n| `smoke/backoffice_venue_list_test.go` | Replace the anonymous-success proof with gate, login, reload, logout-replay and CSRF interaction flows. |\n| `smoke/smoke_test.go` | Add the new form/cookie/redirect helpers. |\n| `web/storefront/src/lib/api-types.gen.ts` | Regenerated Catalog contract types. |\n| `docs/adr/ADR-042-staff-identity-and-backoffice-sessions.md` **NEW** | Record identity placement, alternatives, reversibility, sessions, CSRF and adversary boundary. |\n| `docs/development.md` | Document bootstrap, sign-in/out behavior, restart invalidation and the browser verification procedure. |\n| `docs/configuration.md` | Document the back-office Catalog URL and internal credential use. |\n\nNo CI configuration changes are planned.\n\n## Definition of done\n\nIn addition to all seven conditions of success:\n\n- The gateway retains no database and its route table and `/api/catalog/internal/` edge denial are unchanged.\n- Catalog remains the only database owner for staff accounts; the existing `catalog-migrate` job applies migration `0015`.\n- Role is persisted but not interpreted, returned, or enforced.\n- Staff provisioning is create-only, stdin-password-only and documented.\n- Admin pages no longer use `DEFAULT_ORGANIZER_ID`; organizer scope comes from the authenticated principal.\n- Login, logout and authenticated HTML carry `Cache-Control: no-store`.\n- A back-office restart’s sign-everyone-out behavior and the single-replica limitation are documented.\n- The ADR names the actual adversary: this blocks unauthenticated network callers and stolen-cookie replay after logout. It is not tamper-evidence against a Catalog database writer, a holder of the internal service token, or a controller of the back-office process.\n- The service contract and generated outputs are committed before the gate.\n- `make deps` is run first if executing from a clean clone.\n- `make check` passes as the sole command in its shell invocation.\n- With `make up`, a real browser:\n\n  1. is redirected from `/admin/` to login;\n  2. submits the login form;\n  3. reloads the venue list successfully;\n  4. submits at least one existing venue/seat-map write form through the new origin check;\n  5. submits logout and is refused afterward.\n\n- A follow-up ticket is created and named **“Back-office login rate limiting and abuse telemetry.”** No rate limiting or lockout policy is designed here.\n\n## Risks and cheapest early checks\n\n1. **Astro base-path middleware semantics or an over-broad exception**\n\n   First run the composed anonymous tests against `/admin/`, a real venue path, an unknown path and `/admin/healthz`. This finds route normalization and health-probe mistakes before account work.\n\n2. **Forwarded-origin reconstruction differs from the gateway behavior**\n\n   Make the first authenticated smoke submission include a real gateway-proxied POST, then test a mismatched origin. A correct unit helper with the wrong production headers is insufficient.\n\n3. **Two individually correct fixes compose badly**\n\n   The final interaction test must perform login, an existing authoring write, cross-origin logout rejection, valid logout and cookie replay in one session. This checks the replacement origin guard against the forms that `checkOrigin:false` previously allowed.\n\n4. **The unknown-account fixture cannot distinguish a KDF short-circuit**\n\n   Count calls through an injected comparison seam and use a fixed valid dummy hash. Do not construct the fixture from the authentication helper under test.\n\n5. **Process-local session state is duplicated by bundling or module loading**\n\n   Run login, reload and logout replay through the built standalone Astro server early. Unit tests alone cannot prove the deployed bundle shares one session map.\n\n6. **Internal authentication accidentally becomes public**\n\n   Add a smoke assertion that `/api/catalog/internal/staff/authenticate` returns the gateway’s distinguishable edge-denial body even when sent the internal token.\n\n7. **bcrypt cost makes the gate or login unacceptably slow**\n\n   Benchmark one success, known-wrong and unknown comparison with the chosen cost before fixing the constant. All three paths must retain the same cost.\n\n## Size estimate\n\nExpected total: approximately **950–1,150 changed lines**, of which roughly **250–350 are generated OpenAPI/TypeScript output** and **700–800 are human-authored implementation, tests, migration, ADR and operator documentation**.\n\nThis exceeds the ~400-line reading target. The design already cuts the largest optional surfaces:\n\n- no sixth service;\n- no gateway database;\n- no persistent session schema or session API;\n- no audit trail;\n- no role enforcement;\n- no rate limiting;\n- no authentication framework.\n\nReaching 400 lines would require dropping the real provisioning path, composed-stack tests, CSRF interaction coverage, or letting Astro access Catalog’s database directly. Each would remove a stated success condition or violate the service ownership model, so none is cut.\n\nFor reviewability, implement it as three ordered, independently reviewable commits:\n\n1. ADR, migration, OpenAPI and generated contract;\n2. Catalog account store, authentication and provisioning;\n3. Astro session/gate plus composed smoke proof and documentation.\n\nNo files were modified while preparing this plan.\n\nCodex session ID: 019fc9ee-7e18-75c3-a4ee-2c54d3a14c0f\nResume in Codex: codex resume 019fc9ee-7e18-75c3-a4ee-2c54d3a14c0f\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan-final — TKT-190\n\n**The `kind=plan` draft as amended below is the plan.** This is a delta, not a restatement:\nresume reads the draft and this comment together.\n\nCritique by **main-agent** (`config.models.planReview = main-agent`); draft by `gpt-5.6-sol`.\nCross-model pair holds. The critic was not shown the drafter's justification beyond the draft\nitself, and wrote its own independent prior on the central decision **before** reading the draft.\n\n---\n\n#### 1. Seam verification — every file and symbol the draft names, checked at HEAD\n\n`git grep -n` / `ls` at `796e9054`. **Zero hallucinated seams** — unusual, and worth recording.\n\n| Named by the draft | Verdict |\n|---|---|\n| `newFakeStore()` in `services/catalog/internal/api/server_test.go` | ✅ `server_test.go:78` |\n| `services/catalog/internal/api/coverage_test.go` | ✅ exists; enforces every documented 2xx op has a happy-path test through `env.do` |\n| `get()` / `getWithHeaders()` in smoke | ✅ `smoke_test.go:60` / `catalog_publication_test.go:49` (the draft attributes both to `smoke_test.go`; `getWithHeaders` actually lives in `catalog_publication_test.go` — cosmetic, same package) |\n| `TestBackofficeShellServedThroughGateway` | ✅ `smoke/backoffice_venue_list_test.go`, asserts **200** + renders `La Grande Salle` — COS-1 makes it false, and the draft correctly plans to replace it |\n| `services/catalog/internal/store/migration_smoke_test.go` | ✅ exists, alongside 11 sibling `*_smoke_test.go` |\n| `services/catalog/internal/store/store.go` | ✅ exists |\n| `services/catalog/cmd/catalog/main.go` subcommand hook | ✅ `main.go:34-37`, `subcommands()` map — `provision-staff` slots in with no new machinery |\n| `golang.org/x/crypto` indirect in the catalog module | ✅ `services/catalog/go.mod:41` at **v0.54.0** — promoting to direct must keep that exact version (ADR-035 / `check-dep-drift`) |\n| `web/storefront/src/lib/api-types.gen.ts` must be regenerated | ✅ correct and easy to miss — `make generate` regenerates storefront **and** backoffice from the catalog spec, and `check-generate` diffs both |\n| `docs/configuration.md` | ✅ exists |\n| `web/backoffice/src/env.d.ts` marked **NEW** | ✅ correctly marked — backoffice has none; `web/storefront/src/env.d.ts` is the precedent |\n| Astro `defineMiddleware` + `AstroCookies` on Astro **7.0.9** | ✅ both present in the installed tree |\n\nWire-level facts also checked, not just symbols: `smoke.sh` already runs `compose exec -T <svc> /app <subcommand>` (lines 182, 196, 223–224), so CLI-based smoke provisioning follows an existing pattern rather than inventing one.\n\n#### 2. Cons read against the Decision, before any code check\n\nThe draft's own Con on its chosen option is *\"sessions are initially process-local; a back-office restart signs everyone out, and multiple replicas are unsupported.\"* **This does not refute the Decision** — deployment is Docker Compose, single replica (ADR-002), and the draft documents the limitation instead of hiding it. Accepted.\n\nThe one place the draft argues against itself is **size**: it states the ~400-line target, lands at **950–1,150**, and then argues nothing can be cut. See amendment **A4** — I agree with the conclusion but not with leaving it unrecorded as a decision.\n\n#### 3. Amendments\n\n**A1 — BLOCKING, amended. The back-office must not receive `INTERNAL_SERVICE_TOKEN`.**\n\nThe draft proposes `POST /internal/staff/authenticate` on catalog, called by Astro **directly over\nthe Compose network** using `CATALOG_URL` + `X-Internal-Token`, adding both to the backoffice\ncontainer in `compose.yaml`.\n\nRejected, for a reason the draft could not see from the code alone: **`INTERNAL_SERVICE_TOKEN` is\none shared value across every Go service** (`compose.yaml:32`, the `&go-env` anchor). Handing it to\nthe back-office does not grant \"the ability to authenticate staff\" — it grants the credential that\nalso opens `commerce`'s `/internal/orders/{id}/refunds`, `inventory`'s\n`/internal/operational-holds` and `/internal/slots/{id}/capacity-adjustments`, and `access`'s\nrefund surface. The back-office is a public-facing Node SSR process that renders operator input;\nthis makes any SSRF or template-injection defect there escalate to the full internal API of all\nfive services.\n\nAnd it buys nothing. The stated benefit is that the authenticate endpoint is not publicly\nreachable — but **COS-1 requires `/admin/login` to be anonymous**, so an attacker already has an\nunlimited, unauthenticated credential-stuffing channel through the login form itself. Internal-only\nplacement moves the front door, it does not lock it.\n\n**Amended design:**\n- The operation is a **normal, public catalog operation** — `POST /staff/authenticate` — declared\n  in `services/catalog/api/openapi.yaml` like every other public op, with **no** `X-Internal-Token`\n  requirement. Its only credential is the staff password.\n- The back-office calls it **through the gateway**, via the existing `postCatalog` helper in\n  `web/backoffice/src/lib/api.ts`. One network path, one helper, the invariant that file already\n  documents (*\"the back-office consumes the public contract, ADR-002\"*) stays true.\n- **`compose.yaml` is not modified at all.** That removes this ticket's only pre-declared\n  Compose-risk change.\n\nSecondary support: catalog's three existing `/internal/` routes (`server.go:104-106`) are\nhand-registered in chi and **absent from `openapi.yaml`**. Adding a *declared* `/internal/` op\nwould have introduced a fourth, inconsistent convention. Under A1 the question disappears.\n\nTrade-off recorded: the endpoint is now reachable from the internet. Per the argument above the\ncredential-stuffing surface is unchanged, but the follow-up ticket the draft already requires\n(*\"Back-office login rate limiting and abuse telemetry\"*) must now be scoped to **the endpoint and\nthe form**, not the form alone. That is a wording change to the follow-up, not new work here.\n\n**A2 — amended, wording. The CSRF section states its two controls backwards.**\n\nThe draft writes *\"`SameSite=Lax` is defense in depth, not the primary CSRF control.\"* That is the\nwrong way round in a way that will mislead the next reader: `SameSite=Lax` is what stops the\nsession cookie being **transmitted** on a cross-site POST at all, in every current browser — the\nforged request arrives unauthenticated and is redirected to login. It is a primary control.\n\nBoth controls stay; the plan must describe what each one actually does:\n- **`SameSite=Lax`** — the cookie is not sent cross-site, so a forged POST carries no session.\n  Depends on browser cooperation.\n- **the proxy-aware forwarded-origin check** — the server-side control, which does **not** depend\n  on browser cooperation and also fails closed for non-browser callers.\n\nKeeping both is right and the draft's reasoning for rejecting a synchronizer token is accepted\n(hidden fields on every existing seat-map form for no gain over an origin the trusted gateway\nalready supplies).\n\n**A3 — amended, one added check. Rejecting a missing `Origin` is correct and is the riskiest line in the plan.**\n\nCurrent browsers send `Origin` on same-origin POSTs, but the composed smoke test **cannot prove\nthat** — it sets the header itself, so it proves the server's logic and nothing about real browser\nbehavior. Two requirements:\n- the DoD's manual browser step must submit **login, one existing seat-map write form, and\n  logout** and confirm none is 403'd (the draft's step 4 already does the write form; login and\n  logout must be named too);\n- `Go`'s `SetXForwarded()` **overwrites** `X-Forwarded-Host` / `X-Forwarded-Proto` on the outbound\n  request, so they cannot be forged *through the gateway*. They can be forged by anything already\n  inside the Compose network. The ADR must say so — it does not weaken the control against the\n  adversary this ticket is about, but an unqualified claim would violate ADR-021's rule.\n\n**A4 — accepted with a recorded decision. The size overrun stands.**\n\n950–1,150 lines against a ~400 target. I checked whether it splits: it does not, because\n`decomposition.md` forbids splitting a slice into horizontal layers, and every candidate cut\n(catalog store alone, contract alone) is a layer that reaches PO Review with nothing to demo. The\nprovisioning CLI cannot be replaced by a seeded default without shipping a checked-in default\ncredential — precisely what TKT-83 removed. So: accept, and mitigate at review time.\n- The draft's **three ordered commits** become mandatory, not advisory.\n- The Gate-3 review-guide must be ordered by risk, with the CSRF/session interaction first.\n- ~250–350 of those lines are generated output, which is diffed by `check-generate` and not read\n  line-by-line by a human.\n\n**A5 — accepted, no change.** The red-first TDD order, the injected KDF-comparison seam with a\n**fixed literal dummy hash** (explicitly *not* constructed from the code under test — the\n`fixture-too-small` trap, TKT-61), the `/admin/healthz` exemption, the unknown-admin-path\nindistinguishability, `no-store` on every auth response, create-only stdin-only provisioning, and\nthe adversary boundary in the DoD are all accepted as drafted.\n\n#### 4. Decisions made by the agent under the Gate-2 waiver\n\nGate 2 is waived for this run (see the waiver on TKT-22). Rule applied: **take the critique's\nrecommendation; where it makes none, take the most reversible option.** Every decision below is\nmine, not the owner's.\n\n| # | Decision | Rule applied | Rationale |\n|---|---|---|---|\n| D1 | Staff identity lives in **catalog**, not a sixth service and not the gateway | critique agrees with draft | ADR-002 already assigns *organizers/tenants* to catalog; one migration `0015` against a new service (module, image, compose service, migrate job, smoke override, `go.work`, gateway route). Most reversible: the table moves later like any other domain's would. |\n| D2 | Enforcement in **back-office Astro middleware**, not at the gateway | critique agrees with draft | It is the app that serves `/admin/*`; `defineMiddleware` is a verified seam; the gateway stays a stateless route table with zero requires. |\n| D3 | **A1** — public `POST /staff/authenticate`, no internal token in the back-office | critique recommends | Blast radius: one shared token unlocks every service's internal surface. Also the more reversible option — it touches no compose file. |\n| D4 | Sessions **in-process in Astro**, no session table | most reversible | No schema to migrate away from. Cost (restart signs everyone out, single replica) is documented and true under Compose. |\n| D5 | Accept the **size overrun** rather than split or cut | most reversible | Splitting produces horizontal layers that cannot pass Gate 4; cutting removes a COS. Mitigated by three ordered commits + a risk-ordered review guide. |\n| D6 | Admin pages stop using `DEFAULT_ORGANIZER_ID` and read the organizer from the principal | draft | ~10 lines, and `api.ts` already carries the comment *\"When admin auth lands this comes from the session.\"* No other ticket owns it — TKT-191 puts per-organizer scoping out of scope. |\n\n#### 5. Introduced at plan-review, reviewed by no second model\n\nRequired section. These reach Gate 2 (waived) having been written by the critic and reviewed by\nnobody else — the cross-model guarantee covers the draft and, later, the code, **not** the critic's\nown inventions. Named again here so the code review is pointed at them:\n\n1. **A1 in full** — the public-endpoint design, the claim that it does not widen the\n   credential-stuffing surface, and the removal of the compose change. This is a **security design\n   decision invented at critique time.** The `gpt-5.6-sol` ai-review at Gate 3 must be pointed at\n   it explicitly.\n2. **A2's re-characterisation** of which CSRF control is primary.\n3. **A3's `SetXForwarded` overwrite claim** — read from Go's documented behavior, not proven by a\n   test in this repo. If the implementation depends on it, it needs a test.\n4. **D5**, the decision to ship an over-target diff.\n\n#### 6. Lens\n\nDraft came with its own Risks section (7 items). Critic applied a **different, named lens —\ninversion**: *what would guarantee this ticket fails?* Three answers, and where each landed:\n- gate `/admin/*` without exempting `/admin/healthz` → container unhealthy → `gateway: depends_on:\n  service_healthy` never satisfies → **the whole stack fails to start**, with a failure that looks\n  nothing like an auth bug. *Already handled by the draft.*\n- leave `TestBackofficeShellServedThroughGateway` asserting 200 → the gate goes red at the very end\n  of a long run. *Already handled by the draft.*\n- hand the public-facing web shell the credential that unlocks every service's internal API →\n  **not** handled by the draft; became **A1**.\n\nTwo of three inversion answers were already covered, which is the strongest evidence for this\ndraft's quality. The third is the one blocking amendment.\n\n#### 7. Verdict\n\n**Accepted as amended.** Proceed to Building under the Gate-2 waiver. Label → `agent:coding`.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation — TKT-190\n\n```yaml\nstage: coding\nimplementer: main-agent          # config.models.implement = main-agent, no substitution\nbranch: tkt-190-staff-sign-in\nbase: origin/main @ 796e9054\npr: https://github.com/mathd/ticketing_system/pull/155\ncommits: 3 (ordered, per plan-final A4)\n  409f62fa  catalog owns staff accounts, and a public authenticate endpoint\n  55dfa669  a staff session gates the back office, with a proxy-aware origin check\n  42542f6f  assert where an anonymous caller lands, not one status code\ngate: make check — GREEN on the pushed tree (2nd run)\n```\n\n#### TDD — tests observed red before every implementation\n\n| # | Test | Observed red as |\n|---|---|---|\n| 1 | `store/staff_test.go` — KDF comparison count, dummy-hash non-bypass, normalization, bcrypt 72-byte bound, salting | `undefined: authenticateStaff`, `undefined: staffBcryptCost`, … (build failure) |\n| 2 | `api/staff_test.go` — principal, identical refusals, store-failure ≠ 401, contract-level password bound | `*Server does not implement ServerInterface (missing method AuthenticateStaff)` |\n| 3 | `cmd/catalog/provision_staff_test.go` — stdin-only, id-only output, create-only, empty password, flag validation | `undefined: provisionStaff` |\n| 4 | `web/backoffice/test/session.test.ts` — token entropy, absolute expiry, destroy, cookie attributes | module `../src/lib/session` does not resolve |\n| 5 | `web/backoffice/test/gate.test.ts` — exemption list, lookalike paths, origin check incl. proxy chains | module `../src/lib/gate` does not resolve |\n| 6 | `web/backoffice/test/api.test.ts` — client posts through the gateway, 401→null, 500→throw | 4 failed / 13 passed |\n| 7 | `smoke/backoffice_venue_list_test.go` — the whole flow through the real stack | pre-existing test asserted `GET /admin/` → **200**, which COS-1 makes false |\n\n#### Gate runs — read from the log body, not the exit code\n\n**Run 1: RED.** `EXIT=2` while the harness reported **exit code 0** — the TKT-101 divergence,\nverbatim. Judged by `grep -E 'Error [0-9]+|FAIL|drifted'` on the log, which is the only reason it\nwas caught instead of pushed.\n\nOne failure, one cause: `TestBackofficeRefusesAnonymousCallers`, on `GET /admin` **without a\ntrailing slash** — Astro answers its own **307** normalization to `/admin/` before any middleware\nruns, so the chain is `307 → /admin/ → 302 → login`. The chain is safe; the assertion was\nover-specified, demanding an immediate 302 on a path COS-1 does not even name (I added it).\n\nDiagnosed as an **implementation-layer** issue in the *test*, not a plan-layer one: the code does\nwhat COS-1 promises. Fixed by asserting the **end state** — follow the chain, require it to\nterminate at the login page, and fail on venue data in *any* response along the way. That is\nstrictly stronger than the status-code equality it replaced: a stray 200 anywhere in the chain is\nnow a named failure.\n\n**Run 2: GREEN.** `EXIT=0`, zero failure signatures in the log.\n\nEverything else passed on run 1 already: sign-in through the real form, reload, server-side\nsign-out, **cookie replay after sign-out refused**, all three cross-origin refusals, and\n`/admin/healthz` still anonymous.\n\n#### Deviations from `kind=plan-final`, and why\n\n1. **`identifier_key` is UNIQUE across the table, not per organizer** (the draft said\n   organizer-scoped). Sign-in takes an identifier and no organizer, so an organizer-scoped key makes\n   \"who is signing in?\" ambiguous the moment two organizers share an address. v1 has one organizer;\n   multi-organizer sign-in needs a selector on the form and is out of scope. Recorded in the\n   migration and ADR-042 § Decision (4).\n2. **No `compose.yaml` change at all**, as plan-final A1 required — the back office does not receive\n   `CATALOG_URL` or `INTERNAL_SERVICE_TOKEN`, and reaches catalog through the gateway with the\n   existing `postCatalog` helper. This ticket's only pre-declared Compose risk is gone.\n3. **`scripts/smoke.sh` provisions through the real CLI** (plan, unchanged) — per-run random\n   password, stdin not argv, never written to a file or the log.\n4. **The gate policy lives in `src/lib/gate.ts`**, not inside `middleware.ts` as the draft's file\n   list implied. `middleware.ts` imports `astro:middleware`, which does not resolve under vitest;\n   splitting the two rules into a plain module is what makes them unit-testable at all.\n\n#### Follow-up created\n\n**TKT-195** — *Back-office login rate limiting and abuse telemetry*. ADR-042 cites it by key, so it\nhad to exist for the citation to be honest. Its COS require covering **both** entry points, because\nTKT-190's argument for a public authenticate endpoint rests on the form and the endpoint being\nequivalent in exposure — a control on one of them does not discharge it.\n\n#### Size\n\n+2,050 / −38 across 25 files, of which ~430 lines are generated contract output\n(`openapi_gen.go`, two `api-types.gen.ts`) and ~700 are tests. Over the ~400 target, as plan-final\nD5 accepted and explained; mitigated by three ordered commits and a risk-ordered review guide.\n\nLabel → `agent:ai-review`. Review runs on `gpt-5.6-sol` (`config.models.aiReview`), given the diff\nand no plan — deliberately, so it evaluates the code against reality rather than against my intent.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review — TKT-190\n\n```yaml\nreviewer: gpt-5.6-sol (codex adversarial-review)   # config.models.aiReview, no substitution\nimplementer: main-agent                            # cross-model pair intact\npasses: 4 (absolute churn cap)\nfindings: 9 — 8 fixed, 1 deferred to a new ticket, 0 rejected\ngate: make check GREEN on the pushed tree (992cc125)\nreview-guide: https://github.com/mathd/ticketing_system/pull/155#issuecomment-5173260086\n```\n\nEvery pass was given the **diff and no plan** — deliberately, so the reviewer judged the code\nagainst reality rather than against my intent.\n\n#### Per-finding verdicts\n\n**Pass 1** — verdict *needs-attention*, 1 high + 4 medium.\n\n| # | Finding | Verdict | Where |\n|---|---|---|---|\n| F1 | Unauthenticated bcrypt endpoint enables CPU exhaustion and unlimited guessing | **Deferred → TKT-195** | argued below |\n| F2 | Multibyte password bypasses the KDF timing equalization | **Blocking → fixed** | `staff.go:118` |\n| F3 | Session cookie `Path=/` discloses it to every app on the shared origin | **Blocking → fixed** | `session.ts` |\n| F4 | Expired sessions retained unless their exact token is replayed | **Blocking → fixed** | `session.ts` |\n| F5 | Cross-origin smoke test cannot detect an origin check running after auth | **Blocking → fixed** | `backoffice_venue_list_test.go` |\n\n**F2 was the best catch and I had missed it entirely.** OpenAPI's `maxLength` counts *characters*;\nbcrypt's 72 counts *bytes*. 72 multibyte characters clear the contract at three times the limit,\nand bcrypt then returns `ErrPasswordTooLong` **without doing the work** — removing the only thing\nmasking the cost difference between a row that was found and `sql.ErrNoRows`. The enumeration\noracle COS-4 exists to close, reopened by a password that can never log anyone in.\n\n**Pass 2** (on pass 1's fix diff) — 2 medium, both in *my* fixes.\n\n| # | Finding | Verdict |\n|---|---|---|\n| S1 | The sweep is not a bound — one credential can mint unlimited concurrent sessions | **Blocking → fixed** (cap of 5 per staff, per-principal so it cannot DoS a colleague) |\n| S2 | The rewritten cross-origin test still cannot prove ordering | **Blocking → fixed** |\n\nS1 was as much a *comment* defect as a code one: mine claimed the map was bounded by \"the staff\nheadcount\", which was simply false. Pass 2 also explicitly cleared the two fixes I was least sure\nof — `len()` on a Go string does measure the bytes handed to bcrypt, and `Path=/admin` does match\n`/admin` and `/admin/*` with the deletion path matching exactly.\n\n**Pass 3** (on pass 2's fix diff) — 2 medium, both in *those* fixes.\n\n| # | Finding | Verdict |\n|---|---|---|\n| T1 | Clock rollback makes the cap evict the newest session | **Blocking → fixed** (evict by Map insertion order; the sort is deleted, not replaced) |\n| T2 | The ordering tests exercise the helper, not the composition that must enforce it | **Blocking → fixed** (`gateRequest`, Astro-free, owning the only calls to `next` AND `lookup`) |\n\n**Pass 4** — 1 high, and *\"no other material defect was found in the reviewed session ordering or\ngate composition\"*. The finding was two TypeScript errors in my new tests, which `make check`\ncaught independently in the same minute: `astro check` typechecks `test/**`, vitest does not, so\nthey ran green while `tsc` rejected the file. Fixed; gate green.\n\n#### Second-pass call, and the churn cap\n\nNon-trivial by every criterion in the rule — blocking findings fixed, security paths, new control\nflow, new and rewritten tests — so a second pass was mandatory. Passes 3 and 4 were **permitted by\nthe reset clause**: each pass proved the previous pass's fix wrong at the root, which the rule\ndistinguishes from churn. The signals agree it was convergence, not thrashing: severity fell every\nround (high → medium → medium → one typecheck error), no finding was re-litigated, nothing already\ntriaged resurfaced, and the final pass explicitly cleared the areas the earlier ones had attacked.\n\nPass 4 is the **absolute cap of 4**. No further passes will run on this ticket regardless.\n\n#### The one deferred finding, and why I proceeded past a \"do not ship\"\n\nF1 asked for endpoint rate limiting shipped atomically with authentication, and suggested a\nnarrowly scoped credential instead of the shared internal token.\n\nThe concern is real; **its own recommendation does not close it**. COS-1 requires `/admin/login` to\nbe anonymous — a staff member cannot sign in through a page that needs a session — so an attacker\ndrives the identical bcrypt work through the form no matter how the endpoint is credentialed. The\ncredentialed variant's actual effect would be to put `INTERNAL_SERVICE_TOKEN` (one shared value,\n`compose.yaml:32`) inside a public-facing Node SSR process, where it also opens commerce's refunds\nand inventory's operational holds. That is a large, hard-to-reverse increase in blast radius bought\nfor no reduction in the attack it names.\n\n**TKT-195** carries the control that does close it, blocked-by this ticket, with COS requiring it\ncover **both** entry points — because this very argument rests on the form and the endpoint being\nequivalent in exposure, so a control on one of them does not discharge it.\n\nThis is recorded as a **proceed past a stated blocking finding**, not as a resolved one. It is the\nfirst thing the review guide asks a reader to disagree with.\n\n#### Browser verification (AGENTS.md, TKT-105)\n\nDriven in **real Google Chrome** via Playwright against `make up` — **13/13**. It proves the three\nthings the smoke suite structurally cannot, because the smoke suite composes the requests itself:\na browser-composed same-origin form POST is accepted (the origin check does not reject real\nbrowsers); Chrome stores the cookie `HttpOnly` / `SameSite=Lax` / `Path=/admin` and **does not send\nit to the storefront on the same origin** (F3's fix, confirmed from the browser's own behaviour);\nand a genuine cross-site form POST from another origin mints no session.\n\nThe script lives in this run's scratchpad and is **not in the repo** — there is still no e2e\nharness, and adding one is a decision of its own. Named in the retro.\n\nLabel → `needs:human`; under the waiver the agent performs the Gate-3 merge itself, after posting\nthe guide.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### PO validation — TKT-190 (self-validated under the Gate-4 waiver)\n\nMerged as **`e4075d9e`** on `main`; branch deleted. Verified by reading the merged **content** on\n`origin/main`, not the merge badge — every fix round is present, including the last commit\n(TKT-97's failure mode was a merge that captured a pre-fix SHA).\n\n`make check` re-run **on merged `main`**, judged from the log body.\n\nEach COS below is validated against **named test evidence on the merged code**, plus a real browser\nwhere the test suite structurally cannot reach.\n\n| # | COS | Evidence on merged `main` | Verdict |\n|---|---|---|---|\n| 1 | `/admin/` and any `/admin/<subpath>` refused; the venue list never renders | `TestBackofficeRefusesAnonymousCallers` (smoke, real stack — follows the redirect chain for `/admin/`, `/admin`, a real subpath and an unknown path, failing on venue data in **any** response along the way) · `gate.test.ts` exemption + lookalike tests · browser check #1–2 | ✅ |\n| 2 | A provisioned account signs in through the form and reaches the venue list; survives reload | `TestBackofficeSignInReloadAndSignOut` (submits the real form through the real gateway; asserts the venue renders twice) · browser #3–5, #10 | ✅ |\n| 3 | Sign-out invalidates **server-side**; replay fails | same test — captures the cookie, signs out, then **replays it from a jar-less client** · browser #11–12 (replayed in a clean context) | ✅ |\n| 4 | Wrong password and unknown account indistinguishable, with no KDF short-circuit | `TestBackofficeRefusesBadCredentialsIdentically` (byte-identical wire responses, no identifier echo) · `TestAuthenticateStaffComparesOnceForUnknownAndKnownAccounts` (**counts** KDF comparisons on both paths, asserts equal cost) · `TestAuthenticateStaffRejectsThePasswordThatMatchesTheDummyHash` · `TestAuthenticateStaffRefusesOverlongPasswordsWithoutTouchingTheDatabase` | ✅ |\n| 5 | Salted KDF hash only; no plaintext in logs | `TestHashPasswordSaltsEachHash` · `TestStaffAccountStoresNoPlaintext` (**real Postgres**: reads the column back and asserts the plaintext is absent) · `TestProvisionStaffPrintsOnlyThePseudonymousID` (asserts neither password nor identifier is printed) | ✅ |\n| 6 | Cookie `HttpOnly`, `SameSite=Lax`, `Path`, `Secure`-capable, bounded lifetime | `session.test.ts` cookie attributes · `sessionCookie()` in smoke asserts them **on the wire** · browser #6–9 — **as Chrome actually stored them** | ✅ |\n| 7 | An ADR records placement, session mechanism and CSRF posture | **ADR-042**, incl. a § naming what is closed and what is **not** | ✅ |\n\n**COS-6 note, in the interest of precision:** the cookie is `Path=/admin`, not `Path=/` as the COS\ndraft assumed. That change came out of ai-review F3 and is stricter, not looser — all four apps\nshare one origin behind the gateway, so `Path=/` handed a live back-office credential to every\nstorefront page view and every `/api/*` call. Chrome-observed confirmation that it is no longer\nsent to the storefront is browser check #9.\n\n**Browser verification (AGENTS.md / TKT-105): real Google Chrome, 13/13.** It covers the three\nthings the smoke suite cannot, because smoke composes the requests itself: a browser-composed\nsame-origin form POST is *accepted* (proving the new origin check does not reject real browsers —\nthe exact class of failure TKT-105 shipped twice); Chrome's stored cookie attributes; and a genuine\ncross-site form POST from another origin, which mints no session.\n\n---\n\n#### What a PO should know before accepting\n\n1. **This gates the back-office UI, not the catalog API.** `/api/catalog/*` writes remain fully\n   unauthenticated — anyone who can reach the gateway can still create and publish events by\n   calling it directly. **TKT-191** closes that. ADR-042 § *The adversary this constrains* is\n   explicit, and the phrase \"the back office is authenticated\" must not be read as \"the catalog is\".\n2. **Login attempts are unbounded.** No rate limiting, by decision, and an adversarial reviewer\n   called that [high] \"do not ship\". The reasoning for proceeding, and why the reviewer's own\n   suggested fix would not have closed it, is in the `kind=summary` (ai-review) comment and the\n   review guide. **TKT-195** carries the control.\n3. **A back-office restart signs everyone out**, and a second replica would not share sessions.\n   Deliberate (ADR-042), documented in `docs/development.md`.\n4. **One staff member holds at most five concurrent sessions**; a sixth sign-in ends the oldest.\n5. **`role` is stored and enforced nowhere.** TKT-191 defines the matrix.\n\nNothing here is a defect found after the fact — all five are recorded scope decisions, and each\nnames the ticket that changes it.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-190 (US-B1)\n\nDurations from the `sdlc-state` changelog (`git log -- .sdlc/tickets/TKT-190.json`), never\nhand-tracked. All times local, 2026-08-03.\n\n| stage | duration |\n|---|---|\n| Backlog | 29s |\n| Ready | 45s |\n| **Planning** | **18m 23s** |\n| — `agent:planning` (draft, `gpt-5.6-sol`) | 16m 47s |\n| — `agent:plan-review` (critique, main-agent) | 1m 36s |\n| **Building** | **1h 03m 43s** |\n| — `agent:coding` (main-agent, TDD) | 25m 34s |\n| — `agent:ai-review` (4 passes, `gpt-5.6-sol`) | 38m 08s |\n| PO Review | ~4m (self-validated) |\n| **Total, Ready → Done** | **~1h 27m** |\n\n**human-wait total: 0s.** All four gates were waived (TKT-22 waiver). Gate 3's `needs:human` state\nlasted 20 seconds — the time between posting the review guide and the agent performing the merge\nitself. In a gated run this row is the pipeline's likely bottleneck; here it measures nothing, and\nthat is the experiment's whole point. **Not a speed result to generalize from:** the human absence\nalso removed every opportunity for a human to catch what four review passes did not.\n\n**diff: +2,984 / −28 (36 files)** — ~430 generated (`openapi_gen.go`, two `api-types.gen.ts`),\n~1,050 tests, ~300 docs + ADR. Over the ~400 target, accepted knowingly at plan-review (D5) after\nchecking that every split produced a horizontal layer that could not pass Gate 4 alone.\n\n**gate:** GREEN — EXIT=0, zero `Error|FAIL|drifted` signatures in the log body (on merged `main`, judged from the log body)\n**merge:** `e4075d9e`, squash, branch deleted, merged **content** verified on `origin/main`\n\n#### Review economics\n\n| pass | findings | fixed | reviewer |\n|---|---|---|---|\n| 1 (PR diff) | 1 high, 4 medium | 4 | `gpt-5.6-sol` |\n| 2 (pass 1's fixes) | 2 medium | 2 | `gpt-5.6-sol` |\n| 3 (pass 2's fixes) | 2 medium | 2 | `gpt-5.6-sol` |\n| 4 (pass 3's fixes) | 1 (a typecheck error the gate caught independently) | 1 | `gpt-5.6-sol` |\n\n**Six of nine findings were in code written to fix an earlier finding.** Only three were in the\noriginal implementation. Converged: severity fell monotonically, nothing was re-litigated, and pass\n4 explicitly cleared the areas earlier passes had attacked.\n\n#### Newly unblocked\n\n- **TKT-191** (US-B2, the role matrix) — its only blocker was this ticket. Not auto-moved to\n  Ready; that stays a human transition, granted here by the run's Gate-1 waiver.\n- TKT-192/193/194 remain transitively blocked behind TKT-191 / TKT-193.\n\n---\n\n### learnings:\n\n1. **`maxLength` in OpenAPI counts characters; bcrypt counts bytes — and the gap is a security\n   defect, not a validation nit.** 72 multibyte characters pass a `maxLength: 72` contract at three\n   times bcrypt's limit, and bcrypt then refuses *without doing the work*, removing the timing mask\n   that an equal-cost dummy-hash comparison exists to provide. → **`docs/learnings/`** (technical,\n   repo-local), and worth a line in AGENTS.md's money/auth guidance only if a second instance\n   appears. Generalizes past bcrypt: any byte-bounded primitive behind a character-bounded contract.\n\n2. **A cookie's `Path` is a security boundary when one origin serves several apps.** The gateway\n   fronts storefront `/`, scanner `/scanner/`, APIs `/api/*` and back office `/admin/` on one\n   origin, so `Path=/` hands a staff credential to every one of them, and `HttpOnly` does not help\n   — it stops scripts reading the cookie, not servers receiving it. → **`docs/learnings/`**;\n   directly relevant to TKT-193/194, which add more back-office surface.\n\n3. **An ordering guarantee cannot be tested at the wire.** \"Refused before any credential is read\"\n   and \"handler ran, then the response was replaced with a 403\" emit identical bytes — the\n   discarded response takes its `Set-Cookie` with it. Testing the *check* is not enough either;\n   only testing the *composition*, with the downstream call instrumented, pins it. → **AGENTS.md**,\n   as a rule for security-ordering claims. This is the same shape as the existing ADR-021 rule\n   (\"say which adversary you mean\"): state the claim precisely, then test the thing you claimed.\n\n4. **`Date.now()` is not monotonic, so expiry is not a proxy for issue order.** A backwards clock\n   step inverts them. Map insertion order is free and cannot be moved by a clock. → **`docs/learnings/`**.\n\n5. **The gate's harness-reported exit code diverged from its real one again** (TKT-101's shape):\n   `make check` exited 2 while the harness reported 0. Caught only by grepping the log body. The\n   existing rule held — this is a **confirmation**, not a new learning, and its value is that the\n   rule earned its keep a second time.\n\n### retro:\n\n**(1) What would have made this ticket faster or better?**\n\n- **A browser harness exists in everything but name, and the repo has been paying for its absence\n  since TKT-105.** AGENTS.md says \"there is no Playwright/e2e harness in-repo yet\" and treats\n  browser-submit as a manual step. It cost ~90 lines and the system Chrome already installed, and\n  it caught nothing *because* the smoke suite had already been written to cover the same ground —\n  but it is the only thing that can ever verify `SameSite`, cookie `Path` and browser-composed\n  `Origin`, all three of which this ticket turned into load-bearing controls. Every remaining\n  TKT-22 child is a UI ticket. **Concrete change: a ticket to add `playwright` as a dev dependency\n  of `web/`, one `browser-check` make target, and an AGENTS.md edit replacing \"no harness exists\"\n  with how to run it.** Not done here — it is a new dependency and a gate change, which is its own\n  decision, not a thing to slip into an authentication ticket.\n\n- **The plan-review caught a security design flaw the drafter could not see, because the fact that\n  mattered was in `compose.yaml`, not in any ADR.** `INTERNAL_SERVICE_TOKEN` being *one shared\n  value across all five services* is what made the draft's design wrong, and nothing in the mémo,\n  the ticket or the governing ADRs says it. **Concrete change: `references/decomposition.md`\n  § context-mémo — the `reference` list should include the shared credentials and their blast\n  radius for any ticket touching auth.** I already wrote this ticket's mémo entry by hand; the\n  guidance should make it routine.\n\n**(2) What should the humans change?**\n\n- **The `risk:low` question never came up, and it should have been impossible to skip.** This\n  ticket touches authentication, a migration and a new credential store — the exact carve-out list.\n  Nothing prompted a check; it was simply obvious. On a less obvious ticket it would not be.\n  **Concrete change: `SKILL.md` § step 2, make the claim comment state a risk verdict explicitly\n  (\"risk:low claimed / not claimed, because …\"), so silence is not the default.** I did this\n  voluntarily in the `kind=claim` comment; the skill does not ask for it.\n\n- **The gateless run's real cost is concentrated in one place, and it is not the code.** Nine\n  findings were caught by the machinery. The one thing the machinery could not settle is whether a\n  public credential endpoint with no rate limit should ship — a reviewer said no, I said yes, and\n  the argument for proceeding is genuinely contestable. **Concrete change: when gates are waived,\n  the closeout should name every point where the agent overrode a stated blocking objection, as a\n  short list, so the owner can review exactly those.** For this ticket that list has **one** entry:\n  ai-review F1 (see the review guide § 1). Offered as a `SKILL.md` addition to the waiver protocol.\n\n**V0 is interactive: I can apply the two `SKILL.md`/`decomposition.md` edits and file the browser\nharness ticket now — say the word.** Not applied unprompted: the skill's own bar is that a change\nmust alter agent behaviour in a future session, and these three do, but they are edits to shared\nprocess files rather than to this ticket's code.\n"
    }
  ]
}
