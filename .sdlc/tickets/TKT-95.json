{
  "key": "TKT-95",
  "type": "Task",
  "summary": "Unify smoke HTTP helpers on contract-validating variants, then drive the 5 uncovered catalog ops through them",
  "status": "Planning",
  "labels": [
    "agent:plan-review",
    "risk:low"
  ],
  "assignee": "agent",
  "pr": null,
  "classification": "standard",
  "description": "Follow-up from TKT-47 retro. The smoke suite has validating helpers (postJSON, getWithHeaders, get, internalJSON) and raw ones (holdRequest, postScan) — the split means the per-operation coverage gate only sees traffic through the former, and its scope comment has to disclaim the rest. Unify on validating helpers (raw helpers keep their concurrency-safe shape but report through a chokepoint the gate can see), then delete the scope disclaimers.\n\nConditions of Success:\n- All smoke HTTP traffic to the five services flows through helpers that validate against the committed contracts\n- holdRequest/postScan callers keep their concurrency semantics (no t.Fatal from non-test goroutines)\n- The scope disclaimers in smoke/coverage_test.go shrink accordingly\n- make check passes\n\n---\n\n## Absorbed TKT-111 — the 5 smoke-uncovered catalog ops (TKT-105 surface)\n\nOrigin: TKT-109 closeout, ADR-030's named negative consequence. Driving these through smoke has\nreal-stack coverage value independent of which layer owns the gate:\n\n- `getPublicSeatMapGeometry`, `listVenueSeatMaps`, `listSeatMapVersions` — trivial GETs against the\n  seat-map fixture smoke already builds (`smoke/catalog_publication_test.go`, seat-map section)\n- `updateVenueGaCapacity` — venue GA capacity write\n- `editSeatMap` — write fixture; surfaces ADR-029 pin semantics (**read ADR-029 first**)\n\n**Not** a gate change: `smokeCoverageGatedServices` stays as pinned by\n`TestCatalogCoverageGateIsDeliberatelyUnitScoped` (ADR-030). Flipping catalog into the gate is a\nseparate ADR-030 amendment ticket — see the ADR's recorded flip-cost.\n\n## Why these are one ticket, and in this order\n\nThey compose: unifying the helpers first means the 5 new ops route through validating helpers by\nconstruction and land inside the gate's view. Done the other way round, the new smoke traffic is\nwritten against raw helpers and then rewritten. Both halves are **test-only, no runtime code** —\n`risk:low`, one PR, one gate run.\n",
  "links": [
    {
      "type": "found-in",
      "key": "TKT-47"
    },
    {
      "type": "absorbs",
      "key": "TKT-111"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Gate 1 validation — re-verified against HEAD `ac2952b`\n\n**Still real.** `smoke/inventory_contention_test.go:19` (`holdRequest`) and `smoke/checkout_test.go:95` (`postScan`) are still raw, and the disclaimer is still there at `smoke/coverage_test.go:17` (\"like holdRequest/postScan are invisible to the gate\"). Unchanged since filing.\n\n**Verdict: still worth doing → Ready.** Not DoR-baked: the 8-item shaping happens when the ticket is claimed, per the owner's drain-the-debt call."
    },
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\n### Merged TKT-111 into this ticket (backlog drain, 2026-07-29)\n\nWas: *Unify smoke HTTP helpers on contract-validating variants*\n\nTKT-111 is now **Drive the 5 smoke-uncovered catalog ops (TKT-105 surface) through smoke happy-paths** — absorbed above, and moved back to Backlog marked MERGED so it cannot be claimed twice. Its own thread stays readable on the board for the full filing history and review lineage.\n\nRationale: a plan gate + ai-review + PO gate on each half costs more than the work. See the *Why these are one ticket* section in the description."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-95\n\n**Branch:** `TKT-95-unify-smoke-validating-helpers` off `origin/main` @ `ac2952b`.\n**Backend:** local tracker (`.sdlc/`), state on `sdlc-state`.\n\n**Selection reason:** the ticket was already in Ready with a re-verified Gate 1 readiness comment\n(the raw helpers and the disclaimer are still present at HEAD), and it carries the absorbed\nTKT-111 half. Zero open blockers — its two links are `found-in TKT-47` and `absorbs TKT-111`,\nneither of which blocks.\n\n### ⚠ Human gates waived for this run — process experiment\n\nThe repo owner has **deliberately waived all four human gates** for this ticket and asked for a\nsingle continuous Ready → Done run in fully autonomous mode. Recording it here so the board\nhistory shows this ticket ran **gateless**:\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human moves Backlog → Ready | already given (ticket was in Ready) |\n| 2 — plan approval | human moves Planning → Building | **waived** — agent resolves open decisions after the cross-model critique (prefer the critique's recommendation; else the most reversible option) and records each in `kind=plan-final` |\n| 3 — review + merge | human reviews the PR and squash-merges | **waived** — agent squash-merges and deletes the branch after the ai-review converges; the review-guide is still posted first, as the audit record |\n| 4 — PO acceptance | PO validates COS and moves to Done | **waived** — agent self-validates each COS against test evidence on the merged code |\n\nEverything else in the skill applies unchanged: `config.models` routing with no substitutions,\ncross-model review for both plan and code, TDD with tests observed red first, `make check` green\nbefore every push and after the merge, all marker comments at every stage, no AI attribution in\ncommits or PRs. The owner listed five stop-and-report conditions (gate/CI failing twice on the\nsame cause, unanticipated auth/money/migration/CI scope, a blocking finding surviving the churn\ncap, an unreachable configured model, an ambiguous destructive action); none apply so far.\n\n**`risk:low` claimed and justified** (per the invariant that the label is challengeable): both\nhalves are **test-only** — `smoke/` and nothing else. No runtime code, no auth, no money path, no\nmigration, no CI/deploy config. It touches ~6 files, over the >5-file trigger, but every one of\nthem is a `//go:build smoke` test file, so the risk the trigger exists to catch (blast radius into\nshipped code) is structurally absent. If implementation turns out to need a change under\n`services/` or `shared/`, the label comes off and this run stops for the owner.\nNote the label changes nothing about the flow here anyway — the waiver already covers Gate 2, and\nthe full plan draft + cross-model critique runs regardless.\n\n### DoR baked at claim time\nAll 8 items `met` (the Gate 1 comment deferred the bake to claim time, per the owner's\ndrain-the-debt call). The context-mémo is on the ticket; its `governingAdrs` were re-resolved\nagainst `docs/adr/` at HEAD rather than reused from a shaping-time snapshot — **ADR-028**\n(why smoke validates at all), **ADR-030** (catalog is deliberately outside the smoke coverage\ngate — this ticket adds catalog traffic but must not touch `smokeCoverageGatedServices`), and\n**ADR-029** (`editSeatMap` = new published version, orphaning edits hard-rejected). All three\nresolve to real files at HEAD."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Execution plan (draft) — TKT-95\n\n**Drafted by `gpt-5.6-sol`** (Codex, `task`, effort=high, read-only — `write: false` in the job\nstate; no file was modified). `config.models.plan = gpt-5.6-sol`. Brief: the ticket + COS, the\ncontext-mémo, the three governing ADRs with what each constrains *for this ticket*, the raw-helper\ncall-site inventory, and the stated hard part (the chokepoints are `*testing.T`-bound and\n`t.Fatalf`). The drafter was not given a preferred design.\n\nPosted verbatim below. Critique by `main-agent` follows in `kind=plan-final`.\n\n---\n\n# TKT-95 execution plan\n\n## Repository findings that affect the plan\n\nThe current tree contains more concurrency hazards and validator bypasses than the brief’s helper list identifies:\n\n- `holdRequest` at `smoke/inventory_contention_test.go:247` is inside a `go func` beginning at line 246, not sequential.\n- `postWithKey(t, ...)` runs in goroutines at `smoke/checkout_test.go:272` and `:376`.\n- `internalJSON(t, ...)` runs in goroutines at `smoke/group_reservations_test.go:242` and `smoke/inventory_contention_test.go:254`.\n- The concurrent catalog archive sends raw service traffic at `smoke/catalog_publication_test.go:312-327`.\n- The on-sale path’s `timedPost` at `smoke/onsale_load_test.go:38` is also raw service traffic and is part of the unfiltered smoke run.\n\nThese must be included for COS 1 to be literally true and to avoid retaining existing off-goroutine `t.Fatalf` paths.\n\n## Decision\n\nRefactor the two chokepoints into `testing.T`-free, error-returning validators while preserving coverage recording inside them:\n\n```go\nfunc validateServiceResponse(\n    request *http.Request,\n    status int,\n    header http.Header,\n    body []byte,\n) error\n\nfunc validateDirectServiceResponse(\n    service string,\n    request *http.Request,\n    status int,\n    header http.Header,\n    body []byte,\n) error\n```\n\nThey will retain the current exclusions, route lookup, `recordSmokeCoverage` call, and full `openapi3filter.ValidateResponse` behavior, but replace each `t.Fatalf` with a contextual `fmt.Errorf`. Coverage remains mutex-protected and is still recorded after successful route resolution and before response validation, as it is today.\n\nTest-goroutine helpers such as `postJSON`, `getWithHeaders`, `get`, `internalJSON`, `postWithKey`, and `postSeatHold` will call the validators and immediately `t.Fatal` on a returned error.\n\nConcurrent paths will never receive `*testing.T`. `holdRequest` will keep its current signature and concurrency behavior:\n\n```go\nfunc holdRequest(url, key string, body any) (int, []byte)\n```\n\nAfter receiving the response, it calls `validateServiceResponse`. A returned violation is appended to a package-level, mutex-protected accumulator through these newly proposed symbols:\n\n```go\nfunc recordSmokeContractViolation(err error)\nfunc collectedSmokeContractViolations() []error\n```\n\n`TestMain` in `smoke/coverage_test.go:119` will inspect that accumulator immediately after `m.Run()`, print every violation with its method/path/status context, and force exit code 1. This check will be unconditional, including focused `-run` executions; only the existing operation-coverage check remains filtered.\n\nThat guarantees every concurrent test reaches the assertion because:\n\n- Every smoke package execution goes through `TestMain`.\n- The hold goroutines are joined through existing `WaitGroup`s, result channels, or load-stage completion before their tests return.\n- Therefore their validation errors are recorded before `m.Run()` returns.\n- If a test fails earlier, the suite is already red; `TestMain` will still print any accumulated contract violations available at that point.\n- No accumulator code calls `t`, and no validation failure is reclassified as a benign contention/load outcome.\n\nConcurrent helpers that already have result channels will return validation errors through those channels and fail on the test goroutine. The global accumulator is reserved for high-volume callback paths such as `holdRequest` and `timedPost`, where threading an error through every load outcome could allow a contract failure to be mistaken for an expected 409.\n\n## Options considered\n\n### Option A — error-returning chokepoints plus a suite-level concurrent violation accumulator\n\nChosen.\n\nPros:\n\n- Keeps route matching, coverage recording, and schema validation in one implementation.\n- No `testing.T` crosses into a request goroutine.\n- Preserves `holdRequest`’s current signature and contention timing.\n- Naturally covers the on-sale callback path without changing its result taxonomy.\n- `TestMain` makes omission of a per-test “assert accumulated errors” call impossible.\n- Focused runs still fail on contract drift.\n\nCons:\n\n- A concurrent violation is attributed at suite end instead of immediately to one test.\n- Negative validator tests cannot deliberately leave an error in the parent process; they need isolation through a subprocess.\n- The accumulator is shared state and therefore needs mutex protection and diagnostic messages containing method, path, status, service, and body context.\n\nThese costs do not negate the decision: the suite remains fail-closed, and the subprocess seam below pins both attribution content and exit behavior.\n\n### Option B — pass a per-test accumulator into every concurrent helper\n\nExample proposed shape:\n\n```go\nfunc holdRequest(\n    violations *responseValidationErrors,\n    url, key string,\n    body any,\n) (int, []byte)\n```\n\nPros:\n\n- Better per-test attribution.\n- The test could assert immediately after its `WaitGroup`.\n- No package-global violation state.\n\nCons:\n\n- Every concurrent call site must construct, pass, and eventually assert the accumulator.\n- One omitted assertion silently converts contract drift into coverage credit, the failure mode this ticket is intended to remove.\n- `t.Cleanup` avoids that omission but delays failure anyway and cannot safely use `t.Fatal`.\n- Threading the accumulator through `checkoutAttempt` and the load-stage callbacks creates broad signature churn in timing-sensitive code.\n\nLost because it provides weaker mechanical assurance that every concurrent path actually asserts its errors.\n\n### Option C — return validation errors through each goroutine’s result channel\n\nPros:\n\n- No global state.\n- Failure is reported by the owning test immediately after synchronization.\n- Fits existing result structs such as the concurrent archive and scan race.\n\nCons:\n\n- Each caller must remember to add an error field and check it.\n- The large hold bursts and `loadtest.RunStage` callbacks do not have one central result channel suited to fatal contract errors.\n- A contract error could be classified as an expected rejection or advisory load error unless it is carried separately through every layer.\n\nUse this locally where an existing result channel already exists, but not as the primary design.\n\n### Option D — pass `*testing.T` to `holdRequest` and call the current validator in goroutines\n\nPros:\n\n- Smallest textual diff.\n\nCons:\n\n- `t.Fatalf`/`FailNow` is illegal outside the test goroutine.\n- Existing `postWithKey(t, ...)` and `internalJSON(t, ...)` goroutine calls demonstrate the same hazard.\n- Directly violates COS 2.\n\nRejected.\n\n## Test seam and red-first story\n\nUse the existing stack-free contract-validation seam in `smoke/contract_validation_test.go`, beside the current chokepoints and `TestCommittedServiceContractsAreComplete`. Retain and adapt `TestValidateServiceResponseCoversCatalog` in `smoke/contract_validation_catalog_test.go:35` to the new error-returning signature.\n\nAdd one helper-level test, provisionally `TestConcurrentHoldRequestContractValidation`, using `httptest.Server`:\n\n1. Valid-response arm:\n\n   - Serve `POST /api/inventory/holds` with a contract-valid 201 `Hold`.\n   - Clear and later restore the `\"inventory createHold\"` coverage entry, following the isolation pattern already used by `TestValidateServiceResponseCoversCatalog`.\n   - Invoke the existing `holdRequest` from a goroutine and wait for it.\n   - Assert that this call re-recorded `\"inventory createHold\"`.\n\n   This is a genuine compile-compatible red-first assertion: today `holdRequest` never invokes a chokepoint, so the coverage entry remains absent and the assertion fails.\n\n2. Invalid-response arm:\n\n   - Run a child copy of the current test binary, filtered to this test and selected by an environment marker.\n   - In the child, serve 201 with `{}` for `/api/inventory/holds`, invoke `holdRequest` from a goroutine, and wait.\n   - The parent asserts that the child exits non-zero and reports a response-contract violation.\n   - Today the child exits zero because raw `holdRequest` accepts `{}`, so the parent’s assertion fails red.\n   - After the refactor, the child records the error without touching `t`; child `TestMain` turns it into exit code 1, making the parent pass.\n\nThis one seam proves the two non-obvious properties: concurrent traffic records coverage, and invalid concurrent traffic fails the suite from the test runner rather than calling `FailNow` in the worker.\n\n## File-by-file plan\n\n1. `smoke/contract_validation_test.go`\n\n   - Convert `validateServiceResponse` at line 50 and `validateDirectServiceResponse` at line 94 to the error-returning signatures above.\n   - Preserve all current exclusions, including gateway-denied internal 404s and `/openapi.yaml`.\n   - Preserve coverage recording at lines 79 and 104.\n   - Add the mutex-protected concurrent violation accumulator.\n   - Add the stack-free concurrent `holdRequest` red/green test.\n   - Keep `TestGatewayDeniesGenericInternalRoutes` raw: its responses are gateway refusals, explicitly not service responses.\n\n2. `smoke/coverage_test.go`\n\n   - Replace the scope disclaimer at lines 15-18: `holdRequest`/`postScan` will no longer be invisible.\n   - State the remaining precise limits: explicit 2xx operation keys, one 2xx status is sufficient, catalog is excluded from `uncovered2xxOps`, and deliberate non-service refusals/health/frontend traffic are not contract operations.\n   - Extend `TestMain` at line 119 to report accumulated contract violations and force a non-zero exit before applying the existing unfiltered coverage gate.\n   - Leave `smokeCoverageGatedServices` exactly `[\"inventory\", \"commerce\", \"payments\", \"access\"]`.\n   - Do not change or weaken `TestCatalogCoverageGateIsDeliberatelyUnitScoped`.\n\n3. `smoke/contract_validation_catalog_test.go`\n\n   - Update `TestValidateServiceResponseCoversCatalog` at line 35 to check the new returned error on the test goroutine.\n   - Preserve its isolated `\"catalog listPublicVenues\"` coverage assertion.\n\n4. `smoke/catalog_publication_test.go`\n\n   - Update `postJSON`, `getWithHeaders`, and `postSeatHold` to handle returned validation errors on the test goroutine.\n   - Prefer implementing `postSeatHold` through existing `postWithKey` to remove its duplicate HTTP transport.\n   - In the concurrent archive block at lines 312-327, validate inside the worker without `t`, put any error in `archiveResult.err`, then assert it in the existing result loop.\n   - Add Half B to `TestSeatedPublicationCoexistsWithGA` after the original version-1 publish event has been consumed at lines 489-510 and before the seated performance/draft-map creation at line 513. This ordering avoids both pins and list contamination from the later draft map.\n   - Consume and assert the edit’s version-2 `seat_map.published` event so it is not left queued.\n\n5. `smoke/checkout_test.go`\n\n   - Update `postWithKey` to handle the error-returning validator.\n   - Delete raw `postScan` at line 95.\n   - Replace its four sequential callers at lines 194, 205, 218, and 244 with `postJSON(t, ..., map[string]string{\"qr_payload\": ...})`; retain the existing 200/409/422 and body assertions.\n   - Introduce a no-`t` result variant beneath `postWithKey` for concurrent use.\n   - At lines 272 and 376, use that result variant and carry `err` through `scanResult`/`checkoutResult`; assert errors only while consuming results on the test goroutine.\n\n6. `smoke/inventory_contention_test.go`\n\n   - Make `holdRequest` contract-validate every delivered response and record any returned error in the concurrent accumulator.\n   - Replace sequential calls at lines 56, 79, 83, 194, and 317 with existing `postWithKey(t, ...)` so they fail immediately.\n   - Keep calls at lines 67, 144, 247, and 266 on the concurrency-safe `holdRequest` path.\n   - Replace the goroutine call to `internalJSON(t, ...)` at line 254 with the no-`t` result variant; send its error with the body and assert after synchronization.\n\n7. `smoke/inventory_offering_state_test.go`\n\n   - Replace the sequential `holdRequest` calls at lines 63, 84, 101, and 110 with `postWithKey(t, ...)`.\n   - Preserve all status and coded-conflict assertions.\n\n8. `smoke/group_reservations_test.go`\n\n   - Remove the duplicate `internalValidated` implementation at lines 25-49 and route sequential internal traffic through `internalJSON`.\n   - Replace sequential `holdRequest` calls at lines 115 and 157 with `postWithKey`.\n   - Keep the public hold burst at line 256 on concurrent-safe `holdRequest`.\n   - Replace `internalJSON(t, ...)` in the draw-down goroutine at line 242 with the no-`t` result variant and assert the returned error through `drawDone`.\n\n9. `smoke/operational_holds_test.go`\n\n   - Split `internalJSON` into a no-`t`, error-returning transport/validation implementation plus the existing `t` wrapper.\n   - Update the wrapper for the new direct validator signature.\n   - Keep the line-139 burst on concurrency-safe `holdRequest`.\n   - Replace the sequential commerce order call at line 219 with `postWithKey`.\n\n10. `smoke/onsale_load_test.go`\n\n    - After `timedPost` fully reads a delivered response, invoke the gateway or direct-service error-returning validator.\n    - Record validation failures in the suite accumulator rather than returning them as transport errors or load outcomes.\n    - Keep validation outside the measured `d` interval so local schema-walking time does not contaminate the server-latency evidence.\n    - Preserve redirect policy, transport classification, and all load-stage semantics.\n\n11. `smoke/smoke_test.go`\n\n    - Update `get` at line 60 to fail on a returned validation error.\n    - Retain the validator’s existing early return for `/healthz/all` and other non-service paths.\n\n12. `smoke/happy_path_coverage_test.go`\n\n    - Update the file comment: the suite no longer needs duplicate traffic merely to expose raw-helper operations.\n    - Remove the redundant live access scan driver now that the checkout feature’s scan requests validate and record naturally.\n    - Keep fixture-producing intermediate calls, such as the hold needed for `releaseHold` and the order needed for `getBuyerDeliveryEmail`.\n    - Reconcile one issued ticket directly; preserve coverage of `reconcileScans`.\n\n## Half B endpoint and assertion plan\n\nThe three GET operations have only required path parameters; the OpenAPI operations at lines 806, 829, and 851 specify no query parameters.\n\n| Operation | Exact request | Fixture and assertions |\n|---|---|---|\n| `updateVenueGaCapacity` | `POST /api/catalog/venues/{venueId}/ga-capacity` | Use the `venue` created at `catalog_publication_test.go:420`. Body: `{\"organizer_id\": organizerID, \"ga_capacity\": 450}` per `VenueGaCapacityUpdate`. Assert 200, same venue ID, echoed capacity 450. |\n| `editSeatMap` | `POST /api/catalog/seat-maps/{seatMapId}/edit` | Use published version 1 after its event assertion and before any hold/pin. Submit full replacement `sections`: `Orchestra` → row `A` → seats `1` and `2`, with every required `name`/`label`/`position` field. This retains `Orchestra/A/1`, creates version 2, and has no conflicting pins. Assert 201, new ID, version 2, `status == \"published\"`, non-empty `published_at`, and a schema-1 publish event for the new ID/version. |\n| `getPublicSeatMapGeometry` | `GET /api/catalog/public/seat-maps/{seatMapId}` | Use both old version-1 ID and returned version-2 ID. Assert 200 and hours-tier cache header; version 1 still has only `Orchestra/A/1`, while version 2 has `Orchestra/A/1` and `Orchestra/A/2`. This pins predecessor immutability without claiming tamper-evidence. |\n| `listVenueSeatMaps` | `GET /api/catalog/public/venues/{venueId}/seat-maps` | Call before the later `draftMap` at line 524 exists. Assert 200, hours-tier cache header, exactly versions 1 and 2 for the venue, with their distinct IDs and expected statuses. |\n| `listSeatMapVersions` | `GET /api/catalog/public/seat-maps/{seatMapId}/versions` | Resolve through the original version-1 ID. Assert 200, hours-tier cache header, `current_version == 2`, two entries newest-first `[2,1]`, distinct IDs, and `published_at` on both. |\n\nAll five requests use `postJSON` or `getWithHeaders`, so their responses traverse `validateServiceResponse`. No catalog coverage-gate assertion is added; their value is real gateway/store/migration execution, consistent with ADR-030.\n\n## Test plan\n\n1. Add and run the stack-free red-first helper test described above. Before implementation:\n\n   - `\"inventory createHold\"` is not recorded.\n   - The malformed-response child exits zero.\n   - Both assertions are red for the current raw helper.\n\n2. Adapt `TestValidateServiceResponseCoversCatalog` and keep its existing coverage isolation.\n\n3. Preserve `TestCatalogCoverageGateIsDeliberatelyUnitScoped` unchanged and verify:\n\n   - Exact ordered service list remains inventory, commerce, payments, access.\n   - `uncovered2xxOps()` cannot report catalog.\n\n4. Exercise the existing contention, group reservation, operational hold, concurrent checkout, concurrent scan, archive, and on-sale tests through their existing synchronization. New assertions check returned errors only on the test goroutine.\n\n5. Exercise all five Half B operations in `TestSeatedPublicationCoexistsWithGA` with the exact assertions in the table.\n\n6. Audit remaining HTTP call sites with `rg` after the refactor. Raw clients may remain only for deliberately non-service traffic such as:\n\n   - `TestGatewayDeniesGenericInternalRoutes`\n   - storefront/scanner shell checks\n   - health/readiness paths not represented as service contract operations\n   - helper implementations that invoke a validator before returning\n\n   No raw call that receives a catalog, inventory, commerce, payments, or access operation response may remain.\n\n7. Run formatting/lint-focused checks, then the required full gate:\n\n```sh\nmake check\n```\n\nNo regeneration should be necessary because neither OpenAPI nor generated/runtime code changes.\n\n## Definition of done\n\n- [ ] Every delivered response for a documented operation in catalog, inventory, commerce, payments, or access reaches one of the two contract validators.\n- [ ] `validateServiceResponse` and `validateDirectServiceResponse` return errors and never call `testing.T`.\n- [ ] Sequential helpers turn every validator error into an immediate test failure.\n- [ ] Concurrent `holdRequest` and `timedPost` traffic records coverage and accumulates violations without touching `t`.\n- [ ] `TestMain` unconditionally turns accumulated violations into exit code 1, including focused runs.\n- [ ] Existing concurrent `postWithKey` and `internalJSON` callers no longer receive `*testing.T`.\n- [ ] `postScan` is removed and all four feature calls use a validating helper.\n- [ ] `smoke/coverage_test.go` no longer claims raw hold/scan traffic is invisible.\n- [ ] `smokeCoverageGatedServices` and `TestCatalogCoverageGateIsDeliberatelyUnitScoped` remain unchanged.\n- [ ] `GET /api/catalog/public/seat-maps/{seatMapId}` is driven and asserts immutable old/new geometry.\n- [ ] `GET /api/catalog/public/venues/{venueId}/seat-maps` is driven and asserts both versions.\n- [ ] `GET /api/catalog/public/seat-maps/{seatMapId}/versions` is driven and asserts newest-first `[2,1]` with `current_version == 2`.\n- [ ] `POST /api/catalog/venues/{venueId}/ga-capacity` is driven and asserts the new capacity.\n- [ ] `POST /api/catalog/seat-maps/{seatMapId}/edit` is driven with a full, non-orphaning replacement before any pin exists and asserts a new published version.\n- [ ] No tamper-evidence claim is introduced; the edit assertions are described as honest-writer consistency and immutability.\n- [ ] `make check` passes.\n\n## Risks\n\n- A suite-level accumulator checked only when `m.Run()` returns zero would hide its diagnostics behind another failure. Inspect and print it regardless of the existing exit code.\n- Applying the existing `-run` coverage exemption to contract violations would let focused concurrent tests pass on drift. The violation check must remain unconditional.\n- `recordSmokeCoverage` currently runs before body validation. Preserve that ordering, but ensure an invalid response always leaves a recorded violation; otherwise it could count as covered and pass.\n- Returning validation errors through the load-test `error` channel could classify schema drift as a transport error or an expected 409. Keep contract violations on the dedicated accumulator.\n- Running local validation inside the `timedPost` duration would alter TKT-82/TKT-125 latency evidence. Measure through body read as today, then validate.\n- Leaving even one `postWithKey(t, ...)` or `internalJSON(t, ...)` in a goroutine retains the illegal `FailNow` path despite fixing `holdRequest`.\n- Moving the seat-map edit after `postSeatHold` would test the pinned case and could 409; the happy path must execute before any pin.\n- Executing `listVenueSeatMaps` after the later draft map is created would make an “exactly two versions” assertion false for fixture reasons.\n- Leaving the edit’s publish event queued could make a later event assertion consume the wrong message. Consume and correlate the version-2 event immediately.\n- Weakening validation to log-and-continue would contradict ADR-028. Every returned validation error must cause either immediate test failure or `TestMain` failure.\n- Adding catalog to `smokeCoverageGatedServices`, adding a catalog allowlist entry, or using a stack-free coverage record to claim Half B is covered would violate ADR-030.\n\n## Explicitly out of scope\n\n- No service, gateway, frontend, database, migration, OpenAPI, generated-code, or runtime middleware changes.\n- No amendment to ADR-028, ADR-029, or ADR-030.\n- No addition of catalog to `uncovered2xxOps` or `smokeCoverageGatedServices`.\n- No weakening of `TestCatalogCoverageGateIsDeliberatelyUnitScoped`.\n- No orphaning-edit, edit-vs-pin race, advisory-lock, or malicious-database-writer test; those are already governed by ADR-029 and lower-level tests.\n- No tamper-evidence claim.\n- No new browser/UI form flow.\n- No change to on-sale request rate, classification, redirect policy, or latency thresholds.\n- Deliberate raw gateway-refusal and non-service frontend/health checks remain raw because there is no service operation response to validate."
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Smoke HTTP traffic splits between contract-validating helpers and raw ones; the per-operation coverage gate only sees the former, so its scope comment carries a disclaimer. Unify on validation, then use the unified surface to drive the 5 smoke-uncovered catalog ops."
    },
    "scope": {
      "state": "met",
      "note": "IN: smoke/ test helpers + the 5 catalog ops (getPublicSeatMapGeometry, listVenueSeatMaps, listSeatMapVersions, updateVenueGaCapacity, editSeatMap) + the coverage_test.go disclaimer. OUT: any runtime/service code; any change to smokeCoverageGatedServices (ADR-030 pin stays)."
    },
    "uncertainties": {
      "state": "met",
      "note": "The one real unknown is how to validate from a non-test goroutine: validateServiceResponse takes *testing.T and t.Fatalf's, which is illegal off the test goroutine. Resolved by design at Planning (error-returning chokepoint + collected violations), not by a spike."
    },
    "dependencies": {
      "state": "met",
      "note": "No open blockers. links: found-in TKT-47 (origin), absorbs TKT-111 (merged, parked in Backlog as MERGED)."
    },
    "approach": {
      "state": "met",
      "note": "Refactor validateServiceResponse/validateDirectServiceResponse into an error-returning core; keep the *testing.T wrappers for the sequential callers; give holdRequest/postScan a goroutine-safe path that records coverage and accumulates violations for the test body to assert. Then add the 5 catalog ops onto the existing seat-map + venue fixtures."
    },
    "first_slice": {
      "state": "met",
      "note": "The whole ticket is one slice: test-only, one PR, one gate run (see the description's 'Why these are one ticket, and in this order')."
    },
    "success": {
      "state": "met",
      "note": "4 COS in the description + the absorbed TKT-111 half: the 5 named catalog ops driven through validating helpers."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked below at claim time (governingAdrs re-resolved against docs/adr/ at HEAD ac2952b)."
    }
  },
  "context": {
    "area": "smoke/ end-to-end suite (Go, build tag `smoke`), package smoke_test",
    "governingAdrs": [
      {
        "adr": "ADR-028-response-drift-fail-closed.md",
        "constrains": "Why the smoke suite contract-validates at all: responses fail closed on schema drift in the running services. The smoke chokepoints are the test-side mirror of that decision — widening validation coverage is the point of this ticket, so no helper may be made 'validating' by weakening what a violation does (it must still fail the test)."
      },
      {
        "adr": "ADR-030-catalog-coverage-gate-scope.md",
        "constrains": "Catalog is deliberately OUT of the smoke coverage gate; its per-operation gate lives in services/catalog/internal/api. `smokeCoverageGatedServices` = [inventory commerce payments access] is pinned exactly and ordered by TestCatalogCoverageGateIsDeliberatelyUnitScoped (smoke/coverage_test.go:44). This ticket adds catalog smoke traffic but MUST NOT add catalog to the gate — that is a separate ADR-030 amendment ticket."
      },
      {
        "adr": "ADR-029-seat-identity-pinning-contract.md",
        "constrains": "editSeatMap produces a NEW published version, the old one stays immutable; an edit that would orphan a pinned seat identity is hard-rejected, and the edit-vs-sale race is closed by a family-scoped advisory lock. The smoke happy-path for editSeatMap must exercise a non-orphaning edit on a fixture with no conflicting pins, and must not assert tamper-evidence (honest-writer consistency only)."
      }
    ],
    "keyFiles": {
      "smoke/contract_validation_test.go": "validateServiceResponse (gateway paths, :50) and validateDirectServiceResponse (direct service URLs, :94) — both *testing.T-bound, both call recordSmokeCoverage then openapi3filter.ValidateResponse and t.Fatalf on violation.",
      "smoke/coverage_test.go": "the coverage map + recordSmokeCoverage (:65), the scope disclaimer comment (:16-21), the ADR-030 pin test (:44), uncovered2xxOps + TestMain enforcement (:119).",
      "smoke/catalog_publication_test.go": "postJSON (:32), getWithHeaders (:48), created (:60), postSeatHold (:660); TestSeatedPublicationCoexistsWithGA (:407) builds the venue + seat-map + section/row/seat + published-map fixture the 5 new catalog ops attach to.",
      "smoke/smoke_test.go": "get (:60) + retry (:47).",
      "smoke/operational_holds_test.go": "internalJSON (:30) + directService (url -> contract name).",
      "smoke/inventory_contention_test.go": "holdRequest (:19) — RAW; called from goroutines at :67, :144, :266 and sequentially at :56, :79, :83, :247, :317.",
      "smoke/checkout_test.go": "postScan (:95) — RAW; all 4 callers (:194, :205, :218, :244) are sequential, and it returns an error rather than t.Fatal-ing."
    },
    "rawHelperCallSites": "holdRequest: inventory_contention_test.go x8, group_reservations_test.go x3 (:115, :157, :256 — :256 in a goroutine), inventory_offering_state_test.go x4, operational_holds_test.go x2 (:139 in a goroutine). postScan: checkout_test.go x4, all sequential.",
    "theHardPart": "validateServiceResponse/validateDirectServiceResponse take *testing.T and t.Fatalf. t.Fatalf from a non-test goroutine is illegal (runtime.Goexit on the wrong goroutine); that is exactly why holdRequest is raw. Any unification must give the concurrent callers a path that records coverage + detects violations WITHOUT calling t.Fatalf/t.Fatal off the test goroutine.",
    "notInScope": "No runtime/service code. No change to smokeCoverageGatedServices or to coverageAllowlist. No new e2e harness.",
    "gate": "make check (mirrors CI). The smoke suite runs under scripts/smoke.sh with SMOKE_INTERNAL_TOKEN generated per invocation."
  }
}
