{
  "key": "TKT-172",
  "type": "Story",
  "parent": "TKT-35",
  "classification": "complex",
  "summary": "US-020: A buyer can see which seats are already taken",
  "status": "Done",
  "labels": [],
  "assignee": "agent (orchestrator: main-agent / Claude Opus 5)",
  "pr": "https://github.com/mathd/ticketing_system/pull/146",
  "links": [],
  "description": "**As** a buyer looking at a seated performance, **I want** the map to show me which seats are still free, **so that** I can pick one that I will actually get.\n\n**Priority:** P2  **Depends on:** — (TKT-80/US-017 shipped the seat-claim core; TKT-103/104/105 shipped the seat maps)\n\n**The gap.** Every ingredient of a seat picker exists except the one that makes it a picker. Catalog serves the geometry publicly (`GET /public/seat-maps/{seatMapId}`, hours tier per the ADR-004 TKT-107 amendment) and inventory holds seats atomically (`POST /holds/seats`, `claim_seats.released_at IS NULL` is the liveness predicate — ADR-031). But **nothing tells a buyer which seats are occupied**, and nothing on the public catalog contract says a performance is seated at all: `PublicPerformanceDetail` carries `venue` and `ticket_types` and no `seat_map_id`, even though `performances.seat_map_id` has existed since catalog migration 0010. So the storefront can neither discover that a slot is seated nor render occupancy.\n\n**Scope is the read path only.** This story adds no write, no UI, and no selection rules.\n\n**Acceptance Criteria:**\n- [ ] Catalog's `PublicPerformanceDetail` gains an optional `seat_map_id` (the published version the slot is seated against; absent for a GA slot), sourced from `performances.seat_map_id`. A GA performance's payload is byte-identical to today's.\n- [ ] Inventory serves a public seat-occupancy read for a seated slot returning the slot id, its `seat_map_id`, and the seat identities that are **not claimable right now**. Liveness uses the same predicate the claim path enforces (`claim_seats.released_at IS NULL`, ADR-031) — **not** claim status, which leaks the `finalizing` window.\n- [ ] The read declares and emits ADR-004 rule 1's **seconds** tier (`public, max-age=5, s-maxage=5`), the same tier as `getAvailability`, declared in the contract as a single-value enum so ADR-028's response validator fails closed on drift — the shape TKT-110 established for `AvailabilityCacheControl`.\n- [ ] A GA slot is refused with a 409 (it has no seats), and an unknown slot with a 404 — distinguishable, not collapsed.\n- [ ] The occupancy query is scoped by pool and proven scoped: a test asserts both the result set and that the plan uses `claim_seats_one_live_per_seat` / `claim_seats_by_claim` rather than a sequential scan (ADR-019's two-test rule, applied to inventory).\n- [ ] Both specs declared, generated code (`openapi_gen.go`, `api-types.gen.ts`) committed before the gate, and the new operations covered by the smoke suite (ADR-009 / ADR-028 / ADR-030).\n\nQuality gates: `make check` green; contract tests; `organizer_id`-scoped where the surrounding operations are. **This ticket moves no money and issues no ticket.**",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Without it a seat picker cannot exist: the buyer has geometry but no occupancy, and cannot even tell a seated performance from a GA one."
    },
    "scope": {
      "state": "met",
      "note": "IN: the catalog `seat_map_id` exposure, the inventory occupancy read, its cache tier, its error taxonomy, its index proof. OUT: any UI (TKT-174), any write (TKT-173), orphan rules (TKT-175), best-available (TKT-81)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Whether the occupancy read lives at `/slots/{id}/seats` or extends `/slots/{id}/availability` with a seat list is a Planning-time call. A separate path is the more reversible option (it does not change an existing hot response shape) and is the presumed default; the AC constrains the observable properties, not the URL."
    },
    "dependencies": {
      "state": "met",
      "note": "None open. TKT-80 (seat claims), TKT-103/104/105 (seat maps, publish, edit), TKT-110 (the declared-tier pattern) are all merged."
    },
    "approach": {
      "state": "met",
      "note": "Mirror `getAvailability` end to end — the handler, the declared single-value cache header, the 404/409 taxonomy and the smoke registration all have a working precedent one operation away in the same service."
    },
    "first_slice": {
      "state": "met",
      "note": "One seated slot with one held seat: the read returns exactly that identity, at the seconds tier."
    },
    "success": {
      "state": "met",
      "note": "Demoable through the gateway with curl: hold a seat, watch it appear in the occupancy read, release it, watch it leave."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked at decomposition (2026-08-01). Re-resolve `governingAdrs` against docs/adr/ at claim time — ADR-029/031 are recent and this epic's area is actively moving."
    }
  },
  "context": {
    "capturedAt": "2026-08-01",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-08-01",
        "rule": "Local gate: `make check` (lint + test + build, Go & TS, plus the gateway smoke suite; run `make deps` first on a clean clone). CI runs the same gate — keep them mirrored. Commit regenerated code (`openapi_gen.go`, `api-types.gen.ts`) before running the gate — `check-generate` diffs against HEAD, so an uncommitted regen reads as drift and fails the run."
      }
    ],
    "reference": [
      {
        "summary": "Epic lineage and capability map (US-017/US-018 are the seat-claim core; this epic is the buyer-facing picker)",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "Service layout and ownership",
        "link": "docs/architecture.md"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-004",
        "why": "Fixes the tier: remaining availability is the **seconds** tier (`max-age=5`), and seat-map geometry is the hours tier. Occupancy is availability, not geometry — do not inherit the seat-map tier just because the payload names seats. Rule 1 also requires the tier be *declared*, not merely emitted."
      },
      {
        "id": "ADR-028",
        "why": "The response validator fails closed, so a declared header the handler does not emit (or vice versa) is a 5xx, not a lint warning. The single-value enum + handler constant pairing (`CacheControlPublicAvailability`) is the shape that survives it."
      },
      {
        "id": "ADR-031",
        "why": "`released_at IS NULL` — not claim status — is the liveness predicate for a seat. Reading occupancy by status leaks the `finalizing` window and shows a sold seat as free. Also: expired holds are swept lazily on the next seat hold, so a quiet pool can carry stale live rows; decide deliberately whether the read sweeps, filters on `expires_at`, or reports them."
      },
      {
        "id": "ADR-019",
        "why": "Written for catalog but its rule is general and this AC applies it here: a scoped read is only scoped if an index backs the filter, and proving it takes two tests (the result is scoped **and** the scan is)."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first: both specs change, both generated artifacts are committed **before** the gate runs, or `check-generate` reads the regen as drift."
      },
      {
        "id": "ADR-030",
        "why": "Defines what the coverage gate demands of a new catalog operation — the `seat_map_id` field lands on an existing operation, so check whether that trips the gate's scope."
      },
      {
        "id": "ADR-010",
        "why": "The claim transaction owns the lock order and the pool row. A read that takes locks in a different order, or holds one across an HTTP call, is the classic way to deadlock the on-sale path this read is meant to serve."
      }
    ]
  },
  "comments": [
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-172\n\n**Selection reason.** Only claimable child of TKT-35: zero open blockers (`links: []`), and it blocks the other three (172 → 173 → 174 → 175), so it has maximum unblocking power in this epic. Nothing else is in flight.\n\n**Blocker check.** Zero `blocked-by` edges. Its own dependencies — TKT-80 (seat claims), TKT-103/104/105 (seat maps), TKT-110 (the declared-cache-tier pattern) — are all merged to `main`.\n\n**Branch.** `TKT-172-seat-occupancy-read`, cut from `origin/main` at `1883a4e5`.\n\n---\n\n### ⚠️ Gate waiver — this ticket runs GATELESS\n\nThe repo owner has **deliberately waived all four human gates** for TKT-35 and its children, as a process experiment, and instructed the agent to run Ready → Done in one continuous pass. Recording it here so the board history shows which tickets ran without human gates.\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human drags Backlog → Ready | given by the owner's instruction at decomposition |\n| 2 — plan approval | human drags Planning → Building | agent resolves open decisions itself: prefer what the cross-model critique recommends; absent a recommendation, pick the most reversible option. Every self-made decision and its rationale is recorded in `kind=plan-final`. |\n| 3 — review + merge | human reviews the PR and squash-merges | agent squash-merges after the adversarial ai-review converges (same triage, second-pass and churn-cap rules). The review-guide is still posted on the PR first — it becomes the audit record. |\n| 4 — PO acceptance | PO validates and drags to Done | agent self-validates each COS against test evidence on the merged code, posts the validation note, closes with the full metrics comment. |\n\n**Unchanged and non-negotiable:** `config.models` routing (no substitutions), cross-model review for both plan and code, TDD with tests observed red first, `make check` green before every push and after the merge, every marker comment posted, no AI attribution in commits or PRs.\n\n**Standing stop conditions** (the agent halts and reports instead of proceeding): the gate or CI fails twice on the same cause with no clear fix; the work would touch auth, money paths, data migrations or CI/deploy config in a way the ticket and plan did not anticipate; a blocking review finding survives the churn cap; a configured model is unreachable after the documented escalation ladder; or a destructive/irreversible action's right choice is ambiguous.\n\n**Stop-condition pre-check for this ticket:** it adds a read endpoint and one optional response field. It moves no money, issues no ticket, and touches no auth or CI config. It *does* need an inventory migration only if the occupancy query needs an index that `0009_seat_claims.sql` does not already provide — `claim_seats_one_live_per_seat (pool_id, seat_identity) WHERE released_at IS NULL` and `claim_seats_by_claim (claim_id)` both exist, so the expectation is **no migration**. If Planning concludes otherwise, that is a data-migration touch the ticket did not anticipate and the run stops here for a human.\n\n**Model routing for this ticket** (from `.claude/sdlc.config.json`, unmodified):\n\n| Stage | Model | Cross-model check |\n|---|---|---|\n| plan | `gpt-5.6-sol` (codex) | ✅ ≠ planReview |\n| planReview | `main-agent` (Claude Opus 5) | ✅ |\n| implement | `main-agent` (Claude Opus 5) | ✅ ≠ aiReview |\n| aiReview | `gpt-5.6-sol` (codex) | ✅ |\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Execution plan — TKT-172 (draft)\n\n**Drafted by `gpt-5.6-sol`** (codex, effort high, read-only — no `--write`), per `config.models.plan`. Run: `task --prompt-file .plan-brief.172.md --model gpt-5.6-sol --effort high --background`, job `task-msb17xe4-3q490m`, completed, conclusive.\n\nThe brief it was given carried the ticket, the six ACs, the eight governing ADRs with what each constrains *for this ticket*, the verbatim binding gate rule from `AGENTS.md`, and pointers into the real code (the `getAvailability` precedent, the `claim_seats` schema, the `liveClaims`/`consumingClaims` predicates, the lazy `sweepExpired` path, the catalog public select, and the smoke-coverage machinery). The drafter read the code itself; nothing below is taken on trust — every seam it names was re-verified at HEAD before this was posted (see the plan-review comment).\n\n---\n\n## 1. Definition of Done\n\n- [ ] **AC1:** `PublicPerformanceDetail` declares optional UUID `seat_map_id`; `publicPerformancesSelect` hydrates it from `performances.seat_map_id`; event and season detail emit it for seated slots. Ordinary GA and festival-day objects omit the key and retain their current serialized bytes.\n- [ ] **AC2:** `GET /slots/{id}/seat-occupancy?organizer_id={uuid}` returns `slot_id`, inventory’s bound `seat_map_id`, and sorted `unavailable_seat_identities`. Held/unexpired, `finalizing`, and confirmed claims are included; released, expired, and due-but-unswept held claims are excluded.\n- [ ] **AC3:** The occupancy `200` declares a required single-value `Cache-Control` enum and emits exactly `public, max-age=5, s-maxage=5`; missing or different values fail closed under ADR-028.\n- [ ] **AC4:** An organizer-scoped pool miss returns 404. An existing GA pool returns 409 through the existing `ErrPoolKindMismatch` branch in `problem()`.\n- [ ] **AC5:** The result test proves another pool cannot leak seats into the response. A separate EXPLAIN smoke test proves the shipped query reaches `claim_seats` through `claim_seats_one_live_per_seat` and performs no sequential scan of that relation.\n- [ ] **AC6:** Both OpenAPI specs are updated; generated Go and catalog-derived TypeScript artifacts are committed before the gate; the real-stack smoke suite drives the catalog field and the new inventory operation through the gateway; `make check` passes.\n\n## 2. The test seam\n\nThe first red test goes in a **new** real-Postgres file, `services/inventory/internal/store/seat_occupancy_smoke_test.go`:\n\n- `TestSeatOccupancyResultIsPoolScopedAndUsesClaimLiveness`\n- `TestSeatOccupancyDistinguishesUnknownAndGA`\n- `TestSeatOccupancyIsIndexScoped`\n\nThis is the first seam because the risky behavior is relational: partial-index liveness, lazy expiry, cross-pool scoping, confirmed/finalizing handling, and PostgreSQL’s actual plan. A plain `*_test.go` with mocks cannot establish any of those. Initially the test will fail to compile because the proposed `(*Postgres).SeatOccupancy` method is absent; after a minimal stub it must remain red on the expected result set before SQL is implemented.\n\nAdditional tests, still written before their corresponding implementation:\n\n- **Existing** `services/catalog/internal/store/seatmap_publish_smoke_test.go`: add `TestPublishedEventReadCarriesSeatMapID` so the positional `publicPerformancesSelect`/`rows.Scan` path is exercised against PostgreSQL.\n- **Existing** `services/catalog/internal/api/seatmap_test.go`: add `TestPublicPerformanceDetailSeatMapID` through `env.do`, covering the generated response type and real contract validator.\n- **Existing** `services/catalog/internal/api/server_test.go`: extend `TestPublicDetail` and `TestFestivalPublicGroupedRead` with GA/festival omission assertions.\n- **Existing** `services/inventory/internal/api/availability_cache_tier_test.go`: add `TestSeatOccupancyCacheTierIsContractEnforced`, modeled on `TestAvailabilityCacheTierIsContractEnforced`.\n- **Existing** `smoke/catalog_publication_test.go`: extend `TestSeatedPublicationCoexistsWithGA` to drive both public reads through the gateway.\n\n## 3. Files to change\n\nHand-written:\n\n- `services/catalog/api/openapi.yaml` — add optional UUID `seat_map_id` to `PublicPerformanceDetail`; do not add it to `required`.\n- `services/catalog/internal/store/postgres.go` — add `p.seat_map_id` to `publicPerformancesSelect`, scan through `uuid.NullUUID`, and populate the existing `Performance.SeatMapID`.\n- `services/catalog/internal/api/server.go` — copy `pa.Performance.SeatMapID` into generated `PublicPerformanceDetail.SeatMapId` in `publicEventDetail`; leave the festival-day builder nil.\n- `services/catalog/internal/api/seatmap_test.go` — add the seated public-detail contract test.\n- `services/catalog/internal/api/server_test.go` — pin ordinary GA and festival-day omission, including the GA raw-object regression.\n- `services/catalog/internal/store/seatmap_publish_smoke_test.go` — test real-Postgres public hydration for seated and GA performances.\n- `services/inventory/api/openapi.yaml` — declare `getSeatOccupancy`, its response schema, 400/404/409/500 responses, and its required seconds-tier header.\n- `services/inventory/internal/store/seat_claims.go` — add the proposed `SeatOccupancy` result type, production `seatOccupancyQuery` constant, and `(*Postgres).SeatOccupancy`.\n- `services/inventory/internal/store/seat_occupancy_smoke_test.go` — **new file** for liveness, error distinction, result scoping, and EXPLAIN proof.\n- `services/inventory/internal/api/server.go` — mount the public GET, parse the existing path/query identifiers, call the store, emit the cache header, and use `problem()` for errors.\n- `services/inventory/internal/api/availability_cache_tier_test.go` — pin the new header constant/declaration pair, including missing and wrong values.\n- `smoke/catalog_publication_test.go` — assert catalog discovery, empty/occupied inventory responses, cache header, GA 409, unknown 404, and thereby register `getSeatOccupancy` with smoke coverage.\n\nGenerated by `make generate`:\n\n- `services/catalog/internal/api/openapi_gen.go`\n- `services/inventory/internal/api/openapi_gen.go`\n- `web/storefront/src/lib/api-types.gen.ts`\n- `web/backoffice/src/lib/api-types.gen.ts`\n\nIntentional non-changes:\n\n- No migration file.\n- No `gateway/cmd/gateway/main.go`: `/api/inventory/` already proxies every non-`internal` child path.\n- No `smoke/coverage_test.go`: `getWithHeaders` calls `validateServiceResponse`, which automatically records `getSeatOccupancy`; the unfiltered coverage gate then enforces it.\n- No storefront or back-office implementation beyond regenerated catalog types.\n- No catalog festival SQL change: `publishedFestivalPerformancesQuery` reads only grouped festival days, which `CreatePerformance` forbids from being seated.\n\n## 4. Design decisions\n\n- **Endpoint and shape:** choose `GET /slots/{id}/seat-occupancy?organizer_id=…`, operation ID `getSeatOccupancy`, returning:\n\n  ```json\n  {\n    \"slot_id\": \"uuid\",\n    \"seat_map_id\": \"uuid\",\n    \"unavailable_seat_identities\": [\"Orchestra/A/1\"]\n  }\n  ```\n\n  The list is sorted by `seat_identity` and is `[]`, not `null`, when empty. Reject embedding geometry: catalog already owns it, and joining service databases violates ADR-002. Reject buyer/session-specific data: this is a shared-cacheable public read.\n\n- **Liveness:** query `claim_seats` with both `cs.released_at IS NULL` and the existing `consumingClaims` predicate from `channel_allocations.go`. This includes confirmed plus `liveClaims`—held/unexpired and `finalizing`.\n\n  Reject raw claim status because it can omit `finalizing`. Reject `liveClaims` alone because it omits confirmed sales. Reject `released_at IS NULL` alone because it reports lazily-unswept expired holds as occupied.\n\n- **Expired holds:** filter a due held claim out using `expires_at > now()` via `consumingClaims`, but do not sweep it. The next claim transaction would sweep and release it before attempting the unique-index insert, so it is claimable in fact. A cacheable GET must remain non-writing and lock-free; sweeping would turn it into a mutation and introduce pool-lock contention.\n\n- **Query shape:** use one organizer-scoped SELECT rooted at `inventory_pools`, left-joining the live seat rows. This distinguishes an empty seated pool from an unknown pool without a cross-service call. It takes no row or advisory locks and holds nothing across HTTP.\n\n- **GA versus unknown:** zero pool rows yields existing `ErrNotFound` → 404. A returned pool with `inventory_kind != 'seated'` or no valid `seat_map_id` yields existing `ErrPoolKindMismatch` → 409 through the already-shipped `problem()` branch. No new error type is needed.\n\n- **Which `seat_map_id` inventory echoes:** use `inventory_pools.seat_map_id`, provisioned from catalog’s schema-4 publication. Reject calling catalog at read time: it adds failure/latency to the hot read and violates service ownership. Catalog independently emits `performances.seat_map_id`; consumers can detect eventual projection skew without either service joining the other.\n\n- **Catalog publication semantics:** expose the field only on performances already returned by `p.status='published'`. `CreatePerformance` verifies the referenced map is published at binding time; migration `0010_publish_seat_maps.sql`’s composite FK guarantees only map existence plus matching organizer and venue. Do not rejoin or filter on current seat-map status: the field names the exact version the performance was bound to, not the family’s newest version.\n\n- **Cache declaration:** add a dedicated `SeatOccupancyCacheControl` header component and `CacheControlPublicSeatOccupancy` constant with the same seconds-tier value. Reusing the existing names was rejected because their current comments describe `getAvailability` specifically; the explicit pair keeps each operation’s declaration and emission independently fail-closed.\n\n## 5. Test plan\n\n- Catalog store:\n\n  - Seated and GA performances under one published event.\n  - `GetPublishedEvent` returns the exact bound map for the seated performance and nil for GA.\n  - This catches a missing `p.seat_map_id` projection or mismatched positional scan.\n\n- Catalog API:\n\n  - Seated public event detail contains the exact UUID.\n  - Ordinary GA’s nested performance object matches a pre-change raw JSON literal, proving omission rather than merely decoding into a nil pointer.\n  - `TestFestivalPublicGroupedRead` confirms festival days still omit the shared optional field.\n  - Draft/archived slots remain absent through the existing published predicate.\n\n- Inventory store:\n\n  - Include unexpired held, `finalizing`, and confirmed seats.\n  - Exclude released seats.\n  - Backdate a held claim’s `expires_at` while leaving `status='held'` and `released_at IS NULL`; exclude it and then verify the GET did not mutate either column.\n  - Exclude claims from another pool, including the same seat identity.\n  - Return sorted identities and a non-nil empty slice.\n  - Echo the requested slot and pool’s seat map.\n  - Unknown/wrong-organizer pool → `ErrNotFound`; GA pool → `ErrPoolKindMismatch`.\n\n- Index proof:\n\n  - EXPLAIN the proposed `seatOccupancyQuery` constant—the exact production statement, not a reduced copy.\n  - Mirror catalog’s `explainGenericPlan`: server-side `PREPARE`, `SET LOCAL plan_cache_mode=force_generic_plan`, `EXPLAIN EXECUTE`, and assert both `$1` and `$2` survive.\n  - Seed enough foreign pools/claims to make a sequential scan unattractive, run `ANALYZE`, assert `claim_seats_one_live_per_seat` appears, and reject `Seq Scan on claim_seats`.\n\n- Cache contract:\n\n  - `CacheControlPublicSeatOccupancy` validates as 200.\n  - A missing header and a different tier each become 500.\n  - Real-stack smoke confirms the actual handler emits the exact header.\n\n- HTTP/smoke:\n\n  - Before a hold: seated response is empty with correct IDs/header.\n  - After `postSeatHold`: response contains `Orchestra/A/1`.\n  - GA slot returns 409; random slot returns 404.\n  - Catalog detail identifies the seated performance and leaves the GA performance unchanged.\n  - The validated 200 records `inventory getSeatOccupancy`; no allowlist entry is permitted.\n\n- Execution order:\n\n  - Run the targeted tests red before store/handler implementation.\n  - Implement and run targeted plain and `-tags smoke` suites green.\n  - Run `make generate`, review all four generated paths, and commit regenerated artifacts before invoking the gate.\n  - On a clean clone run `make deps`; finish with `make check`.\n\n## 6. Risks\n\n1. **Incorrect liveness around expiry or checkout.** Raw status loses `finalizing`; raw child-row liveness retains expired holds; `liveClaims` loses confirmed seats. The lifecycle matrix and due-unswept non-mutation test catch each variant.\n2. **Catalog positional scan drift.** Adding the SELECT column without the matching `rows.Scan` target fails only at runtime. The catalog store smoke test and real-stack public-detail request catch it.\n3. **A logically scoped query still scans all seats.** The existing index makes a migration unnecessary, but only the EXPLAIN test establishes that the shipped SQL actually uses it under seeded statistics.\n4. **Contract/header drift becomes a production 500.** This is intentional ADR-028 behavior; the focused cache-tier test catches missing, mismatched, or weakened enum declarations before smoke.\n5. **Projection lag between catalog and inventory.** Catalog can expose seating before the publication consumer has provisioned inventory, temporarily yielding inventory 404. This already exists for availability; smoke must retry provisioning before asserting occupancy. The ticket should not add a synchronous catalog call.\n6. **Optional field accidentally changes GA bytes.** Generated `*UUID` plus `omitempty`, the raw GA golden, and festival regression catch this.\n7. **Planner-test brittleness.** Seeded statistics and a future PostgreSQL version may choose a different valid plan. Keep the assertion narrow: index presence plus no sequential scan on `claim_seats`, not every join node.\n\n**Migration status: no migration is needed.** `claim_seats_one_live_per_seat (pool_id, seat_identity) WHERE released_at IS NULL` exactly backs the scoped live-child predicate, while claims are reached by their existing key. If the production query cannot be made to use that index in the required EXPLAIN proof, stop and escalate rather than adding an index silently.\n\n## 7. Open decisions\n\nNone remain unresolved from HEAD. The endpoint name, response vocabulary, non-writing expiry treatment, inventory-side map echo, and catalog publication behavior are fixed above. The only stop condition is the unexpected failure of the existing partial index to support the exact production query under the required plan proof."
    },
    {
      "stage": "plan-review",
      "kind": "plan",
      "body": "<!-- sdlc:stage=plan-review kind=plan -->\n### Adversarial plan critique — TKT-172\n\n**Critiqued by `main-agent` (Claude Opus 5)**, per `config.models.planReview`. Cross-model check holds: the draft is `gpt-5.6-sol`'s, the critique is not.\n\nThis is a strong draft. It is conclusive, it cites real code throughout, and it independently reached the two conclusions that matter most (the conjunctive liveness predicate, and no migration). The findings below are two substantive amendments and a set of accepted parts — not a rubber stamp, and not padding.\n\n#### Existence audit — every seam the draft names, checked at HEAD\n\n`git grep` / `test -f` at `1883a4e5`. A drafter that cannot run the gate hallucinates seams; this one did not.\n\n| Named | Exists |\n|---|---|\n| `services/catalog/internal/store/seatmap_publish_smoke_test.go` | ✅ |\n| `services/catalog/internal/api/server_test.go` · `TestPublicDetail` · `TestFestivalPublicGroupedRead` | ✅ all three |\n| `services/catalog/internal/api/seatmap_test.go` | ✅ |\n| `services/inventory/internal/api/availability_cache_tier_test.go` · `TestAvailabilityCacheTierIsContractEnforced` | ✅ both |\n| `smoke/catalog_publication_test.go` · `TestSeatedPublicationCoexistsWithGA` · `postSeatHold` | ✅ all three |\n| `getWithHeaders` → `validateServiceResponse` (coverage auto-records) | ✅ verified in the body at `smoke/catalog_publication_test.go:49-59` |\n| `explainGenericPlan` · `assertReachesVia` | ✅ — but **catalog's store test package only** (`festival_smoke_test.go`, `season_smoke_test.go`, `pricing_smoke_test.go`, `seatmap_smoke_test.go`). Unexported, different package. The draft says \"mirror\", which is the correct word: inventory needs its own copy. Flagging so nobody plans on importing it. |\n| `web/backoffice/src/lib/api-types.gen.ts` · `web/storefront/src/lib/api-types.gen.ts` | ✅ both, both catalog-derived |\n| `problem()` → `ErrPoolKindMismatch` = 409, `ErrNotFound` = 404 | ✅ `services/inventory/internal/api/server.go:88-110`, both branches already shipped — AC4 needs no new error type, exactly as claimed |\n| Optional-field codegen shape (`*T` + `omitempty`) | ✅ `PublicEventDetail.Description *string \\`json:\"description,omitempty\"\\`` — AC1's byte-identical GA payload is achievable |\n| `publishedFestivalPerformancesQuery` needs no change | ✅ and the reason is stronger than stated: `postgres.go:500` refuses a seated festival day outright (`\"festival day cannot be seated\"`), so the omission is enforced at write time, not merely conventional |\n| Season read also emits the field | ✅ `publicEventDetail` is called from three sites (`server.go:1067`, `:1116`) and both public-performance query variants share `publicPerformancesSelect` — the draft is right that one projection change covers event **and** season |\n\nVerdict on the audit: **no hallucinated seams**.\n\n---\n\n#### Finding 1 — ACCEPTED, and the reason is stronger than the draft knows (raise to a required test case)\n\nThe draft filters on `cs.released_at IS NULL` **AND** `consumingClaims`, and justifies the conjunction by expiry: `released_at IS NULL` alone would report a lazily-unswept expired hold as occupied.\n\nThat is correct but it is not the load-bearing case. **The load-bearing case is a refund.** `services/inventory/internal/store/refund_returns.go:113-137`: a **full** refund of a seated claim sets `claim_seats.released_at`, but leaves `claims.status = 'confirmed'` — the comment there says so explicitly, and `releaseSeatsForTerminal` deliberately does not help because \"a returned claim stays confirmed\".\n\nSo for a fully refunded seated claim:\n\n| Predicate | Result |\n|---|---|\n| `consumingClaims` alone | seat reported **occupied** — ✗ wrong, and the seat is unsellable forever on the picker |\n| `released_at IS NULL` alone | correct here, wrong on unswept expiry |\n| **both, conjoined** (the draft's choice) | ✅ correct on both |\n\nThe two conjuncts are load-bearing for **different** failure modes and neither subsumes the other. That is precisely the kind of pair a later \"simplification\" deletes half of.\n\n**Amendment A1 (required):** the test plan gains an explicit case — *a fully refunded confirmed seated claim's seats are reported free* — and the query constant carries a comment naming both reasons (expiry sweep AND `refund_returns.go`'s confirmed-but-released state) with the file cited. Without the test, this correctness is accidental.\n\n#### Finding 2 — GAP: the response cannot distinguish \"free seat\" from \"slot not on sale\"\n\nThe draft's response is `{slot_id, seat_map_id, unavailable_seat_identities}`. Nothing conveys offering state.\n\n`Availability` carries a **required** `offering_status` for exactly this reason — TKT-75 AC2, quoted in the inventory spec: *\"counters stay factual, but `available` is 0 unless open\"*, and `problem()` carries `slot_archived`/`slot_closed` machine codes because \"clients need to tell 'sold out, retry later / join waitlist' from 'stop offering this slot'\". `guardOffering` refuses the claim on a closed or archived slot.\n\nAs drafted, an archived slot returns `200` with an empty `unavailable_seat_identities` — a map rendering as **entirely free** on a slot where every claim will be refused. That is the same defect TKT-75 fixed for GA, reintroduced one endpoint over.\n\nRejected alternative: 409 on a closed slot. It collides with AC4's GA-mismatch 409, and it is wrong for an archived event whose page is still viewable.\n\n**Amendment A2 (required):** the response gains a required `offering_status` mirroring `Availability`'s (`open|closed|archived`), derived by the existing `offeringStatus(lifecycle, closure)` helper from the pool row **already being selected** — zero extra query cost. Consumers keep one call, and TKT-174 gets the signal it needs without a second round trip.\n\n#### Finding 3 — minor, mechanical\n\nEvery schema in `services/inventory/api/openapi.yaml` sets `additionalProperties: false`. The draft does not say so for the new response schema. **Amendment A3:** it does too, and the empty list serializes as `[]`, never `null` (the draft already says the latter — keeping it visible because it is a real Go slice-initialization trap).\n\n#### Accepted without change\n\n- **Endpoint `GET /slots/{id}/seat-occupancy?organizer_id=…`**, `getSeatOccupancy` — a new path rather than extending `getAvailability`'s response. Accepted: it is the more reversible option (it does not reshape the hottest public read), and it keeps the seconds-tier availability payload small.\n- **Echo `inventory_pools.seat_map_id`, not catalog's.** Accepted and it is the right call for the right reason: a read-time catalog call would put a cross-service hop inside a public read (ADR-002/ADR-010), and having both services publish their own view is what makes projection skew *detectable* instead of hidden.\n- **Do not sweep expired holds in the GET.** Accepted. A cacheable public GET that writes is a mutation wearing a read's clothes; it would take the pool lock the on-sale path needs (ADR-010). Filtering on `expires_at` gives the correct answer without it.\n- **No migration.** Independently confirmed: `claim_seats_one_live_per_seat (pool_id, seat_identity) WHERE released_at IS NULL` exactly backs the scoped live-child predicate. The draft's escalation condition — if EXPLAIN cannot be made to use it, stop rather than add an index silently — is the right stop, and it matches this run's standing stop conditions.\n- **A separate `SeatOccupancyCacheControl` header + `CacheControlPublicSeatOccupancy` constant** rather than reusing the availability pair. Accepted: ADR-028 fails closed per declaration, so one shared declaration would couple two operations' drift detection.\n- **No `smoke/coverage_test.go` edit.** Verified: `getWithHeaders` routes through `validateServiceResponse`, which records coverage automatically; inventory *is* in `smokeCoverageGatedServices`, so an undriven `getSeatOccupancy` fails the suite by itself. Correct as drafted — and it means the smoke test is mandatory, not optional.\n- **Test seam = a new real-Postgres `seat_occupancy_smoke_test.go`.** Accepted. The whole risk surface is relational (partial-index liveness, lazy expiry, cross-pool scoping, the planner). A mock-backed unit test would assert nothing that can break.\n\n#### Pre-mortem — it is six weeks later and this shipped broken. What happened?\n\n1. **A refunded seat became unsellable** and nobody noticed until an organizer complained about a permanently dark seat. → Closed by A1's test.\n2. **A closed slot rendered a fully free map** and buyers clicked into refusals. → Closed by A2.\n3. **The EXPLAIN test was green for the wrong reason** — too few seeded rows, so the planner ignored every index and \"no seq scan on claim_seats\" held trivially. Catalog's `TestGetPublishedFestivalIsIndexScoped` seeds 2000 rows and `ANALYZE`s precisely because of this, and says so in its comment. → The inventory copy must seed comparably and assert the index *by name*, not merely the absence of a scan.\n4. **The GA payload changed** because the field was declared required, or the generator emitted a zero-value UUID. → Closed by the raw-JSON golden the draft already plans, which asserts omission rather than decoding into a nil pointer. Keep that literal.\n5. **Catalog exposed `seat_map_id` before inventory had provisioned the pool**, so the storefront asked inventory and got a 404. The draft names this (risk 5) and correctly refuses to fix it with a synchronous call. → The smoke test must poll for provisioning, as availability's does today; the *contract* consequence is that a 404 from this endpoint is not necessarily \"no such slot\", which TKT-174 must not treat as fatal.\n\n#### Verdict\n\n**ACCEPT with amendments A1, A2, A3.** No part rejected. The draft's design decisions stand; two required test/response additions close two real defects it did not see.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan final — TKT-172\n\n**The `kind=plan` draft, as amended below, is the plan.** This comment is a delta, not a restatement.\n\n#### Amendments carried from plan-review\n\n- **A1 — the conjunctive liveness predicate gets a named test and a cited comment.** Test plan gains: *a fully refunded confirmed seated claim reports its seats free*. `refund_returns.go:113-137` leaves `claims.status='confirmed'` while setting `claim_seats.released_at`, so `consumingClaims` alone reports a refunded seat as occupied forever. The query constant carries a comment naming both reasons the conjunction exists (unswept expiry **and** the refunded-confirmed state), with the file cited, so a later simplification has to argue with a comment before it deletes a conjunct.\n- **A2 — the response gains a required `offering_status`** (`open|closed|archived`), mirroring `Availability`'s, derived by the existing `offeringStatus(lifecycle, closure)` from the pool row already being selected. Without it an archived slot answers `200 {unavailable: []}` — a map rendering as entirely free where every claim will be refused. That is TKT-75's defect one endpoint over.\n- **A3 — the new response schema sets `additionalProperties: false`** (every inventory schema does) and the seat list serializes as `[]`, never `null`.\n\nEverything else in the draft is accepted as written, including the parts I explicitly considered and did not change: the separate endpoint, the inventory-side `seat_map_id` echo, the non-writing treatment of expired holds, the dedicated cache-header pair, and the new real-Postgres test file as the first seam.\n\n#### Decisions made by the agent under the waived Gate 2\n\nThe owner waived plan approval for this run. Per the waiver's rule — take what the critique recommends; absent a recommendation, take the most reversible option — every open item is resolved here, with its rationale:\n\n| # | Decision | How it was resolved | Rationale |\n|---|---|---|---|\n| D1 | Adopt A1, A2, A3 | Critique recommended all three as required | Waiver rule 1: prefer what the critique recommends. All three are additive and testable. |\n| D2 | Endpoint shape: new path vs. extending `getAvailability` | New path `/slots/{id}/seat-occupancy` | Draft's choice, critique concurred. Also the **more reversible** option: deleting an unused new endpoint is cheap; un-reshaping the hottest public read's response is not. |\n| D3 | `offering_status` — required or optional on the new response | **Required** | Optional would let a future handler omit it and silently reintroduce the defect A2 closes. `Availability` makes it required for the same reason. |\n| D4 | Whether a closed slot should be an error instead of a status field | Status field, `200` | An error collides with AC4's 409 and is wrong for an archived-but-viewable page. Reversible: a later ticket can add a refusal on top of a status field; removing an error clients depend on is harder. |\n| D5 | Where the inventory EXPLAIN helpers come from | A local copy in inventory's store test package | `explainGenericPlan`/`assertReachesVia` are unexported in catalog's store test package. Copying ~40 test-only lines beats exporting cross-service test machinery. Judged more reversible than creating a shared test module for two callers. |\n\n#### Introduced at plan-review, reviewed by no second model\n\nThe cross-model guarantee covers the draft (`gpt-5.6-sol` wrote it, `main-agent` reviewed it) and it will cover the code (`main-agent` implements, `gpt-5.6-sol` reviews). It does **not** cover what plan-review *added* rather than accepted, amended or rejected. Those elements reach implementation reviewed only by their author:\n\n1. **A2's `offering_status` field** — the requirement, its enum, its required-ness, and the choice of a status field over a refusal (D3, D4). Invented at plan-review. The `Availability` precedent it argues from is real and was verified, but the decision to mirror it here is unreviewed.\n2. **A1's specific test case** — the refunded-confirmed scenario. The underlying fact (`refund_returns.go` leaves the claim confirmed) was verified in the code; the judgement that it warrants a dedicated test rather than a comment is unreviewed.\n\nBoth are flagged again for the ai-review stage, which sees the diff without this plan — the right condition under which to catch a bad addition.\n\n#### Stop-condition status\n\nRe-checked against this run's standing conditions before implementation: **no migration** (independently confirmed — the existing partial index backs the predicate); no auth surface; no CI/deploy config; no money path. The one live escalation is the draft's own: if the EXPLAIN proof cannot be made to use `claim_seats_one_live_per_seat` on the shipped query, **stop and report** rather than adding an index.\n\n**Gate 2 is waived — proceeding directly to Building.**\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation — TKT-172\n\n**Implemented by `main-agent` (Claude Opus 5)**, per `config.models.implement`. PR: https://github.com/mathd/ticketing_system/pull/146 · branch `TKT-172-seat-occupancy-read` · commit `6edfa7f6`.\n\n```yaml\nstage: coding\nimplementer: main-agent (Claude Opus 5)\nplan: kind=plan (gpt-5.6-sol) as amended by kind=plan-final\ngate: make check — EXIT=0, log body checked for 'Error N|FAIL|drifted|panic'\nmigration: none (as planned)\n```\n\n#### TDD — each step observed red first\n\n| Test | Red how | Then |\n|---|---|---|\n| `TestSeatOccupancy*` (7 cases, new file `seat_occupancy_smoke_test.go`) | `vet: undefined: SeatOccupancy` | store implemented → green |\n| `TestSeatOccupancyCacheTierIsContractEnforced` | `undefined: CacheControlPublicSeatOccupancy` | constant + handler + contract → green |\n| `TestPublicPerformanceDetailCarriesSeatMapID` | `p.SeatMapId undefined (type PublicPerformanceDetail has no field)` | contract field + regen + mapper → green |\n| `TestPublicEventReadCarriesSeatMapID` (catalog store, real Postgres) | written **after** the projection change — so it was re-verified red by disabling the hydration branch (`if seatMap.Valid && false`), which produced `seated slot hydrated SeatMapID = <nil>`, then restored | green |\n\nThat fourth row is an honest deviation from strict red-first ordering, recorded rather than smoothed over: the test was written after its implementation and its redness was established retroactively.\n\n#### What shipped\n\n- **Inventory** — `GET /slots/{id}/seat-occupancy` (`getSeatOccupancy`), returning `{slot_id, seat_map_id, offering_status, unavailable_seat_identities}`. New `SeatOccupancyCacheControl` header component + `CacheControlPublicSeatOccupancy` constant, both pinned to ADR-004's seconds tier. `store.SeatOccupancy` + the `seatOccupancySeatsQuery` const.\n- **Catalog** — optional `seat_map_id` on `PublicPerformanceDetail`, hydrated by adding `p.seat_map_id` to `publicPerformancesSelect` and its positional scan. Reaches the event **and** season reads through the one shared `publicEventDetail` builder; the festival query is untouched because a festival day cannot be seated (the write path refuses it at `postgres.go:500`).\n- **Smoke** — `TestSeatedPublicationCoexistsWithGA` extended to drive both reads through the gateway: catalog discovery (seated names its map, GA omits the key), occupancy empty before the hold and `[Orchestra/A/1]` after it, the seconds-tier header, GA→409, unknown→404.\n\n#### Deviations from the plan, and why\n\n1. **Two queries instead of one.** The plan proposed a single SELECT rooted at `inventory_pools` left-joining the live seat rows. Implemented as a pool-row query followed by the seat query. Reason: aggregating the seats into an array would introduce Postgres array scanning, which this repo has no precedent for anywhere in any store, and the single-query shape offered nothing in return — both are pool-scoped, both are lock-free, and the EXPLAIN proof targets the seat query, which is the one AC5 is about. Splitting also keeps the unknown-vs-empty distinction on a plain `ErrNoRows`.\n2. **The EXPLAIN fixture is 200 pools, not one big one.** The plan said \"seed enough foreign pools/claims to make a sequential scan unattractive\". The first attempt seeded one other pool with 5000 seats and **failed**:\n   ```\n   Seq Scan on claim_seats cs  (cost=0.00..109.53 rows=2501 width=23)\n         Filter: ((released_at IS NULL) AND (pool_id = $1))\n   ```\n   That is not a scoping defect — a generic plan has no bound value, so the planner estimates `pool_id = $1` at `1/n_distinct(pool_id)`, and with two pools that is half the table, where a sequential scan is genuinely correct. What makes the index right under a **blind** plan is many distinct pools, which is also what production looks like. Re-seeded as 200 pools × 25 live seats → green.\n\n   **This is the one case where the run's stop condition nearly fired.** The plan's own escalation rule was: if EXPLAIN cannot be made to use `claim_seats_one_live_per_seat`, stop and report rather than add an index. It was not triggered, because the diagnosis showed the fixture was wrong and not the index — and the fix was to the fixture, not to the schema. No migration was added, which was the thing the stop condition existed to protect.\n\n#### Gate evidence\n\n`make check` → `EXIT=0`, and the log body was searched for `Error [0-9]+|FAIL|drifted|panic:` with zero matches (the exit code alone is not trusted for a background run). Both smoke store packages ran under the smoke tags: `ok ticketing/services/catalog/internal/store 9.178s`, `ok ticketing/services/inventory/internal/store 16.000s`.\n\nOne check deserves calling out because it is load-bearing and non-obvious: **the smoke coverage gate is what proves the new endpoint was actually exercised end to end.** Inventory is in `smokeCoverageGatedServices`, and `TestMain` fails an unfiltered run when any documented 2xx operation went undriven. The suite passed unfiltered, so `getSeatOccupancy` received a contract-validated 200 through the gateway — which also proves the gateway proxies it (no gateway change was needed; `/api/inventory/` already covers every non-`internal` child path).\n\nNo browser verification: this ticket adds no form and changes no UI. The regenerated `api-types.gen.ts` files are types only.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial code review — TKT-172\n\n**Reviewed by `gpt-5.6-sol`** (codex `adversarial-review`), per `config.models.aiReview`. Cross-model check holds: `main-agent` implemented, `gpt-5.6-sol` reviewed. Each pass got the diff and a guilt-primed focus, never the plan.\n\n**Three passes.** The cap is two per *stable* diff; pass 2 proved pass 1's fix wrong at the root, which resets the counter (absolute cap 4). Pass 3 found no production defect, so the diff is stable and the review stops here.\n\n| Pass | Base | Verdict | Findings |\n|---|---|---|---|\n| 1 | `origin/main` | needs-attention | 2 high, 1 medium |\n| 2 | `6edfa7f6` (pass 1's fix diff) | needs-attention | 1 high, 2 medium |\n| 3 | `130226b7` (pass 2's rewrite) | needs-attention, **\"no production defect was proven\"** | 1 medium, coverage only |\n\nFindings shrank 3 → 3 → 1 and moved from correctness to coverage. That is convergence, not churn — pass 2's findings were about code pass 1 did not exist to review.\n\n---\n\n#### Pass 1\n\n**1-1 [high] Occupancy ignored the pool's aggregate ceiling — FIXED.** ✅ Verified real at `HEAD`: `seat_claims.go:222` refuses with `ErrUnavailable` once `confirmed + held + qty > limit`, where `limit` is `target_capacity` or `capacity`. A draining cut (TKT-76) takes headroom to zero without releasing a single `claim_seats` row and without moving `offering_status`, so every unheld identity stayed absent from the list while every claim on it failed — the response said \"free\" about seats no claim would grant, which AC2 forbids. Fixed by adding a headroom field. *(How it was fixed was itself wrong — see 2-1.)*\n\n**1-2 [high] Frozen transaction time vs. a queued claim — REJECTED, with reason.** ✅ The mechanism is real: `now()` is transaction-start time, `CreateSeatHold` begins its transaction *before* waiting on the pool lock, so a hold expiring during the wait is still live to that claim while the read already calls it free. Rejected as blocking here on three grounds: (a) the disagreement is bounded by the lock wait and lands on the side ADR-004 explicitly accepts — *\"a stale 'available' display is acceptable and resolved by the atomic claim\"*; (b) the root cause is that **buyer TTLs anchor to transaction-start `now()`**, which is already filed as **TKT-148** and enumerates this exact code path; (c) the recommended fix changes expiry semantics **on the claim path**, which is a contention path this ticket did not anticipate and one of this run's standing stop conditions. Not re-filed — TKT-148 covers it.\n\n**1-3 [medium] The EXPLAIN test could pass on an unscoped read — FIXED.** ✅ Verified real: requiring the index name to appear and forbidding a sequential table scan is jointly satisfiable by a plan that scans the *whole* partial index and filters afterwards. The assertion now also requires `Index Cond: (pool_id = $1)` — the scoping parameter must bind inside the index lookup, not in a filter on its output.\n\n#### Pass 2 — on pass 1's fix diff\n\n**2-1 [high] The headroom field quoted the WRONG rule — FIXED.** ✅ Verified real, and this is the finding that mattered most. Pass 1's fix sourced the field from `Availability(..., \"\")`, whose `available` subtracts unsold channel reservations. `CreateSeatHold` does not: it admits against `target_capacity`/`capacity` alone, and `ReplaceChannelAllocations` does **not** kind-check, so a seated pool *can* carry allocations. With an allocation reserving the remaining capacity the response said `0` while the seat claim succeeded — suppressing claimable inventory, and making the comment that claimed the two computations agree simply false. Now derived from `CreateSeatHold`'s own test verbatim, pinned by `TestSeatOccupancyCapacityMirrorsTheSeatedAdmissionRule`. The underlying product question is filed as **TKT-176**.\n\n**2-2 [medium] Headroom can be positive when no seat is claimable — FIXED, by naming.** ✅ Verified real: inventory does not hold the seat universe (that is the seat map, in catalog), and a seated pool is provisioned from the venue's GA snapshot. A ten-seat map in a hundred-seat venue reports 90 with every seat sold. Not clampable here without the cross-service call the claim path deliberately avoids — so the field was renamed `available` → **`remaining_capacity`**, the contract says CEILING in as many words, and `TestSeatOccupancyCapacityIsACeilingNotASeatCount` keeps the distinction visible. The name *was* the false claim.\n\n**2-3 [medium] Internally torn cacheable response — FIXED.** ✅ Verified real: separate READ COMMITTED statements meant a claim committing between them produced headroom for one more seat beside a list already containing the seat that consumed it — then cached for five seconds. Now one `READ ONLY REPEATABLE READ` transaction. The response is allowed to be *stale* (ADR-004 resolves that at the claim); it is not allowed to be *self-contradictory*, because a picker cannot reconcile the two halves.\n\n#### Pass 3 — on pass 2's rewrite\n\n**3-1 [medium] The new tests could not detect the rewrite's own regressions — FIXED (partly), with one part rejected.** ✅ Verified real: every capacity test used a single pool and only ordinary held claims, so they stayed green through a predicate that double-counted confirmed claims or leaked another pool's holds. Two tests added and **verified by mutation**:\n\n| Mutation applied | Test that caught it | Observed |\n|---|---|---|\n| `liveClaims` → `consumingClaims` in the aggregate | `…CapacityAcrossTheClaimLifecycle` | `remaining_capacity = 8 want 9` — the confirmed claim counted twice |\n| aggregate scoped by `organizer_id` instead of `pool_id` | `…CapacityIsSlotScoped` | `slot A = capacity 3` — the other pool's 7 holds leaked |\n\n**Rejected part:** the pass also asked for a test proving both fields come from the pre-commit snapshot by interleaving a committed claim between the two statements. Deterministically that needs a **test seam inside `SeatOccupancy`**; the alternative is racing a goroutine against an unsynchronised window, i.e. a flaky test in a suite that already carries one (TKT-149). Production code should not grow a hook to observe its own isolation level for a two-line property visible at the top of the method. Recorded here rather than silently skipped.\n\n---\n\n#### Triage ledger\n\n| Finding | Verdict | Where it went |\n|---|---|---|\n| 1-1 aggregate ceiling | blocking → fixed | `be6fecbb` (final form) |\n| 1-2 transaction-time expiry | rejected, reason stated | already TKT-148 |\n| 1-3 vacuous EXPLAIN assertion | blocking → fixed | `130226b7` |\n| 2-1 wrong rule quoted | blocking → fixed | `be6fecbb`; question → **TKT-176** |\n| 2-2 ceiling misnamed | blocking → fixed | `be6fecbb` |\n| 2-3 torn snapshot | blocking → fixed | `be6fecbb` |\n| 3-1 mutation-insensitive tests | blocking → fixed (2 of 3) | `2a11e787`; 3rd rejected, reason stated |\n\n**Second/third-pass call:** required at every step. Pass 1's fixes changed a public response schema and added logic; pass 2's rewrote the derivation and the transaction semantics. Neither was trivia. Pass 3 was earned by the reset rule and justified itself — it found the tests were weaker than they read.\n\n#### Gate\n\n`make check` run to `EXIT=0` after **every** commit (four times), each judged from the log body (`Error N|FAIL|drifted|panic:` → zero matches), never from the reported exit code alone. `git rev-parse HEAD` == `git rev-parse origin/TKT-172-seat-occupancy-read` = `2a11e787` — verified before merge per the TKT-97 rule.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### PO validation — TKT-172 (self-validated under the waived Gate 4)\n\nMerged as `8b009bf4` (squash of PR #146), branch deleted. `make check` re-run **on merged `main`** → `EXIT=0`, log body clean of `Error N|FAIL|drifted|panic:`.\n\nThe owner waived PO acceptance for this run, so the COS are validated here against test evidence on the merged code rather than by a human exercising the product. Each row names the artifact that would fail if the claim were false — a claim with no such artifact is marked as unproven rather than asserted.\n\n| AC | Met | Evidence on merged code |\n|---|---|---|\n| **1.** `PublicPerformanceDetail` gains optional `seat_map_id`; GA payload byte-identical | ✅ | `TestPublicPerformanceDetailCarriesSeatMapID` asserts the seated value **and** asserts on raw JSON that the GA object has no `seat_map_id` key at all — a struct assertion would pass on an explicit `null`. `TestPublicEventReadCarriesSeatMapID` covers the positional scan against real Postgres; re-verified red by disabling the hydration branch. Existing `TestPublicDetail` / `TestFestivalPublicGroupedRead` still green. |\n| **2.** Public seat-occupancy read; liveness is the claim path's notion, not raw status | ✅ | `TestSeatOccupancyCountsOnlyClaimableSeats` walks held / finalizing / confirmed / released / unswept-expired in one pool. `TestSeatOccupancyFreesFullyRefundedSeats` covers the case a status-only predicate gets wrong forever. Both halves of the conjunction are load-bearing and each has its own test. |\n| **3.** Declares and emits ADR-004's seconds tier, fail-closed on drift | ✅ | `TestSeatOccupancyCacheTierIsContractEnforced` — three cases: the handler constant validates 200, a different tier is a 500, a **missing** header is a 500. Smoke asserts the real handler emits `public, max-age=5, s-maxage=5` through the gateway. |\n| **4.** GA slot → 409, unknown slot → 404, distinguishable | ✅ | `TestSeatOccupancyDistinguishesUnknownAndGA` at the store; smoke asserts both status codes through the gateway. No new error type — the already-shipped `problem()` branches carry it. |\n| **5.** Query scoped by pool, and **proven** scoped | ✅ | Two tests, per ADR-019's rule. Result: `TestSeatOccupancyIsPoolScoped` (identical seat identity on another pool must not leak). Scan: `TestSeatOccupancyIsIndexScoped` EXPLAINs the shipped statement under `force_generic_plan` and requires `Index Cond: (pool_id = $1)` — not merely the index name, which a full index scan would also satisfy. |\n| **6.** Specs declared, generated code committed before the gate, smoke coverage | ✅ | Both specs changed; `openapi_gen.go` ×2 and `api-types.gen.ts` ×2 committed before every gate run (`check-generate` passed four times). Coverage is not asserted by hand: inventory is in `smokeCoverageGatedServices`, so an undriven `getSeatOccupancy` fails the suite on its own — the suite passed unfiltered. |\n\n#### What a human would still want to see with their own eyes\n\nHonest limits of a self-validation, stated rather than glossed:\n\n- **Nobody has looked at a seat picker**, because there is no picker yet — that is TKT-174. What is proven here is that the two reads answer correctly and consistently; whether they are *sufficient* to render a map is a claim TKT-174 will test for real.\n- **No browser was driven.** Correct for this ticket (no form, no UI — the regenerated `api-types.gen.ts` files are types only), but it means the repo's browser-submit rule was satisfied by not applying, not by passing.\n- **`remaining_capacity` is a ceiling, not a seat count** (see the review guide). A picker that reads it as \"seats left\" will be wrong on a small map in a large venue. The contract says so and a test pins it, but this is the field most likely to be misused by the next ticket.\n\n#### Newly unblocked\n\n**TKT-173** (US-021: a buyer reserves the exact seats they named) — its only blocker was this ticket. Not auto-moved to Ready; that promotion happens at claim time under the waiver.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-172\n\nMerged `8b009bf4` · PR [#146](https://github.com/mathd/ticketing_system/pull/146) · branch `TKT-172-seat-occupancy-read` deleted (remote and local).\n\n#### Durations\n\nFrom the `sdlc-state` branch commit log (`git log -- .sdlc/tickets/TKT-172.json`), not hand-tracked.\n\n| Stage | Duration |\n|---|---|\n| Backlog (created → Ready) | 0h 00m 46s |\n| Ready (→ Planning, claim) | 0h 00m 35s |\n| Planning — `agent:planning` (plan draft, `gpt-5.6-sol`) | 0h 17m 23s |\n| Planning — `agent:plan-review` (critique + plan-final, `main-agent`) | < 1s (posted in the same board write as the Building transition) |\n| Building — `agent:coding` (TDD + gate + PR) | 0h 16m 13s |\n| Building — `agent:ai-review` (3 passes + fixes + 3 further gate runs) | 0h 30m 25s |\n| PO Review → Done | same session, immediately after the post-merge gate |\n| **Total Ready → merged** | **~1h 05m** |\n\n`human-wait total`: **0s** — all four gates were waived by the owner for this run. This is the metric the pipeline exists to measure and it is exactly the number that makes this run *not* comparable to a gated one. Recorded as zero, not as n/a: the gates were skipped, not merely fast.\n\n#### Diff\n\n`+1243 / −13 (15 files)`\n\nLarger than the ~400-line target named at decomposition. Cause: **1122 of those lines are test and contract**, not implementation. The store change is ~70 lines and the handler ~20. Three of the four commits are review fixes, two of which added tests rather than logic. The ~400-line guidance is about *what a human must read at Gate 3*, and by that measure the reviewable surface is the two SQL constants and their comments.\n\n#### Learnings\n\n1. **A conjunctive predicate whose halves close different failure modes needs each half tested separately, or the correctness is accidental.** `released_at IS NULL AND consumingClaims` — one half covers lazily-unswept expiry, the other covers the refunded-but-still-`confirmed` state, and neither subsumes the other. Nothing in the code says that; the next reader deletes one. → **`docs/LEARNINGS.md`** (repo-local), phrased generally: when a predicate is a conjunction because of two *unrelated* upstream behaviours, cite both in the constant and give each its own test.\n2. **A plan assertion that names an index proves less than it looks.** \"Index name appears AND no Seq Scan\" is jointly satisfiable by a full partial-index scan. The binding claim is the *access condition* (`Index Cond: (pool_id = $1)`). This affects every existing ADR-019 index-proof test in the repo — catalog's `assertReachesVia` has the same weakness. → **`docs/LEARNINGS.md`**, and worth an **ADR-019 amendment** proposing the stronger assertion; filed as a note here rather than done unilaterally, since amending an ADR from a ticket's closeout is exactly the \"decide vs. write down\" line the skill draws.\n3. **An EXPLAIN fixture needs many distinct values of the scoping column, not one large other-bucket.** A generic plan estimates `col = $1` at `1/n_distinct`, so two pools make a sequential scan genuinely correct and the test fails for a reason unrelated to scoping. Cost one debugging cycle and nearly fired this run's stop condition. → **`docs/LEARNINGS.md`**.\n4. **Naming is a correctness claim on a public contract.** `available` was factually derived but read as \"seats available\"; on a small map in a large venue that reading is wrong by 90. The fix was the name, not the number. → too situational for the skill; belongs in the ticket record.\n\nNone of the four is a skill edit: all are readable from the repo when needed, which is the bar the skill sets for its own rules.\n\n#### Newly unblocked\n\n**TKT-173** — sole blocker cleared.\n\n#### Incidentals filed\n\n**TKT-176** — channel allocations are permitted on seated pools by the store but ignored entirely by the seat claim path. Surfaced by ai-review pass 2; a product decision, not a bug fix. Should be decided before TKT-174 ships a picker.\n\nNot filed: the read-vs-claim expiry disagreement (ai-review 1-2) — its root cause is already **TKT-148**, and filing a second ticket for the same mechanism would be noise.\n\n#### retro\n\n**(1) What would have made this ticket faster or better?**\n\n- **The context-mémo's `governingAdrs` did its job, and the one thing it could not carry cost the most.** ADR-031 was in the mémo with \"expiry is swept lazily\" spelled out, and the plan drafter used it correctly on its first attempt. What no ADR carried was that a **full refund leaves the claim `confirmed` while releasing its seats** — that lives only in a comment in `refund_returns.go`. Neither the drafter nor the first review pass found it; it was found by reading the refund code during plan-review specifically because the ticket touched seat liveness. *Concrete change:* when a ticket's mémo names ADR-031, it should also name `services/inventory/internal/store/refund_returns.go` as a **reference** entry — \"the one place where claim status and seat liveness are deliberately decoupled\". Applied to TKT-173's and TKT-175's mémos would be premature (they may not touch it); worth adding to TKT-164's, which is explicitly about seat/ticket association.\n- **Three ai-review passes was the right number and the cap rules said so correctly.** Pass 2 invalidated pass 1's fix at the root, which reset the counter; pass 3 then found no production defect and the diff was declared stable. Had the cap been applied mechanically as \"two and stop\", the branch would have merged with `available` quoting the wrong rule under a comment claiming it quoted the right one. No change needed — recording that the reset clause earned itself.\n\n**(2) What should the humans change?**\n\n- **`ADR-019` should say what an index proof must assert.** It currently says \"prove the scan is scoped\" and the repo's implementation of that (`assertReachesVia`) is satisfiable by a full index scan. That is a decision-level gap, not a documentation one, so it wants a ticket rather than a direct commit — the skill's own \"decide vs. write down\" line. *Concrete:* file a ticket to amend ADR-019 with the access-condition requirement and to strengthen the four existing catalog index-proof tests.\n- **The gate-waiver experiment should be judged on this ticket's ai-review, not its duration.** ~1h05m Ready→merged with zero human wait is the headline number, but the interesting datum is that **six of seven review findings were real and three were blocking**, and the two most serious were introduced by the review's own earlier fixes. A gated run would have put a human between passes. Whether that would have caught the `available`-quotes-the-wrong-rule defect *earlier* or merely *later* is the actual experimental question, and this run cannot answer it alone. *Concrete:* run at least one child of TKT-35 **gated** and compare the finding profile, otherwise the experiment has no control arm.\n\n*Offer:* the ADR-019 amendment ticket and the TKT-164 mémo addition are both small and I can apply them now on request; neither was done unilaterally because both change artifacts outside this ticket's scope.\n"
    }
  ]
}
