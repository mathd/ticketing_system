{
  "key": "TKT-240",
  "type": "Story",
  "summary": "US-CH6: Reseller API — an external partner sells from shared inventory",
  "status": "Planning",
  "labels": [
    "agent:plan-review"
  ],
  "assignee": "agent",
  "pr": null,
  "parent": "TKT-17",
  "description": "The last and largest slice: an external reseller sells from the same inventory as everyone else, with the same no-oversell guarantee and per-reseller attribution. This introduces a **third identity class** — a machine-to-machine partner credential, alongside staff sessions (ADR-042), internal tokens (ADR-043) and customer sessions (ADR-049) — and closes the known commerce→inventory channel seam.\n\n**Conditions of Success:**\n- A partner credential class exists: per-reseller API credentials, scoped to one organizer and one channel from the registry (US-CH1). Credentials are stored hashed, never recoverable, and are revocable. **An ADR records this identity class** and its relationship to the three existing ones.\n- The reseller surface is a **contract operation, not an internal route** — per ADR-043's line, it declares `security:` in the OpenAPI document and is validator-enforced, not header-compared in a handler. It is reachable from the edge (unlike `/internal/`), so the gateway gains an external partner route.\n- The partner may: read availability for its channel, hold, and confirm a sale. It may **not** read anything scoped outside its organizer+channel.\n- **The commerce→inventory channel seam is closed for GA pools**: a sale naming a channel consumes *that channel's* allocation. Today `services/commerce/internal/api/server.go:260-263` documents that channel is deliberately not forwarded to inventory, with the consequence that a reseller-channel sale takes reseller fees while eating public inventory. **Closing this will make some currently-succeeding reserves return 409** — that behavior change is intended, must be called out in the PR, and must be covered by a test that asserts the new refusal.\n- **The seated half of the same seam stays with TKT-176 and is an explicit non-goal here.** `CreateSeatHold` admits against capacity alone and `SeatHoldCreate` has no `channel` field at all, so a seated claim ignores allocations entirely — a separate defect with its own display consequences, already ticketed. This story must not silently half-fix it: if the reseller surface is offered on a seated pool, it refuses rather than overselling the channel, and the refusal cites TKT-176.\n- Per-reseller attribution: the claim and the order carry the channel and the reseller identity, so settlement (TKT-217/ADR-048) can already split by it.\n- Rate limiting per ADR-051's per-subject/per-source discipline applies to the partner surface.\n- Tests: an unauthenticated call is refused by the validator (not the handler); a credential scoped to channel A cannot hold on channel B; the no-oversell contention proof holds with a reseller participating; the seam-closure 409; and a revoked credential is refused immediately.\n\nQuality gates: `make check` green; contention smoke extended with a partner participant; OpenAPI regenerated and committed before the gate; the identity-class ADR merged with the code.\n\nParent: TKT-17. Depends on US-CH1 (channel registry), US-CH3 (per-channel price — a reseller with its own price is the point) and US-CH4 (windows). Non-goals: reseller settlement reports (TKT-23), partner self-service onboarding UI, OAuth flows beyond a credential.",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "The epic's headline COS: an external reseller sells from shared inventory with the same no-oversell guarantee and per-reseller attribution. Nothing of it exists today."
    },
    "scope": {
      "state": "met",
      "note": "IN: partner credential class + ADR, contract-declared external surface, availability/hold/confirm scoped to organizer+channel, the commerce→inventory seam closure, attribution, ADR-051 rate limiting. OUT: settlement reporting (TKT-23), partner onboarding UI, OAuth."
    },
    "uncertainties": {
      "state": "met",
      "owner": "human",
      "note": "AUTONOMOUS-RUN WAIVER applies to the WORK, and the owner has additionally instructed that this ticket STOP BEFORE MERGE for a human to re-take Gates 2 and 3. The two shape decisions — (1) credential shape: hashed bearer vs mTLS vs signed requests; (2) whether the seam closure ships behind a per-organizer opt-in or unconditionally — are drafted by the agent and recorded in kind=plan-final, but they are presented to the owner at Gate 2 rather than self-approved. Resolved for shaping purposes: the questions are identified and owned, which is what the DoR item asks."
    },
    "dependencies": {
      "state": "met",
      "note": "Verified on the board at claim time: TKT-235, TKT-237 and TKT-238 are all Done (as are TKT-236 and TKT-239). Zero open blockers."
    },
    "approach": {
      "state": "met",
      "note": "ADR-043 decides the mechanism outright: a public partner API is a contract operation, so it declares security: and is validator-enforced — NOT the inline header comparison commerce uses on internal routes. Verified at claim time that catalog already runs the declared-security idiom. SEAM LOCATION CORRECTED: the shaping-time citation of server.go:260-263 was stale; the seam is the comment at server.go:303 and the hold body at server.go:646, plus the same text in commerce/api/openapi.yaml and the generated openapi_gen.go. GA/SEATED OWNERSHIP RESOLVED: TKT-240 takes the GA half, TKT-176 keeps the seated half — verified that SeatHoldCreate carries no channel field at all, so the seated half cannot be half-fixed by accident here; the reseller surface refuses on seated pools citing TKT-176."
    },
    "first_slice": {
      "state": "met",
      "note": "Credential + scoped availability/hold/confirm + seam closure in one slice. Splitting the seam closure out would ship a reseller API that oversells its own channel, which is the opposite of the COS."
    },
    "success": {
      "state": "met",
      "note": "COS above; the no-oversell-with-a-partner contention proof and the seam-closure 409 test are the load-bearing ones."
    },
    "context_memo": {
      "state": "met",
      "note": "Re-resolved at claim time rather than trusted: governingTests baked (it was ABSENT — the gap that failed TKT-235's gate twice), 4 post-bake ADRs added, all cited ADRs and all test names verified to resolve at HEAD, stale line numbers corrected, and the ADR-055 numbering collision recorded."
    }
  },
  "context": {
    "binding": [
      {
        "id": "ADR-001",
        "rule": "Money is represented as integer minor units + ISO currency code everywhere; floats are banned on money paths.",
        "date": "2026-07-12"
      },
      {
        "id": "ADR-003",
        "rule": "Money-relevant records land as immutable, hash-chained, signed journal entries from the first order; every ticket/entitlement state change is an append-only lifecycle event linked to its order. No raw PII in any append-only record — pseudonymous IDs only.",
        "date": "2026-07-12"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-043",
        "why": "The guard line is contract operation vs internal route: a public partner API is a contract operation and must declare security: and be validator-enforced, not header-compared in a handler."
      },
      {
        "id": "ADR-051",
        "why": "Public customer-surface rate limiting: per-subject and per-source limits — an external partner API needs the same treatment."
      },
      {
        "id": "ADR-010",
        "why": "The inventory pool row is the single serialization point; no background sweeper may be required for correctness."
      },
      {
        "id": "ADR-024",
        "why": "Channel codes are exact opaque strings — no normalization, no case folding; a registry must not introduce an FK from claims (historical attribution) nor a counter; release stays a clock_timestamp() predicate under the pool FOR UPDATE."
      },
      {
        "id": "ADR-009",
        "why": "OpenAPI is the source of truth; generated code must be committed before the gate or check-generate reads the regen as drift."
      },
      {
        "id": "ADR-028",
        "why": "Response validation is fail-closed; a response that drifts from the contract is a 5xx, not a silent pass."
      },
      {
        "id": "ADR-002",
        "why": "Catalog owns rule and entity definitions; commerce owns pricing/fee/promo evaluation. A channel registry is a definition and belongs in catalog."
      },
      {
        "id": "ADR-042",
        "why": "Staff identity/back-office sessions — one of the three existing identity classes the new partner credential must be positioned against."
      },
      {
        "id": "ADR-049",
        "why": "Customer identity/storefront sessions — the second existing class; the partner credential is explicitly a third, not a reuse of either."
      },
      {
        "id": "ADR-048",
        "why": "Settlement ledger — per-reseller attribution must land in a shape settlement can already split by."
      },
      {
        "id": "ADR-021",
        "why": "Name the adversary before writing 'tamper-evident' or 'secure': a hashed credential protects against credential disclosure at rest, not against a writer with DB access."
      },
      {
        "id": "ADR-053",
        "why": "Landed after this mémo was baked (TKT-236). The back office reads the channel registry with its OWN catalog credential — the precedent for a narrowly-scoped, purpose-specific credential rather than a shared token. The partner credential is the same shape of decision one step further out."
      },
      {
        "id": "ADR-054",
        "why": "Landed after this mémo was baked (TKT-238). Per-channel sales windows as DB-time predicates on the allocation row. Refusal precedence interacts: window → code → capacity."
      },
      {
        "id": "ADR-055-presale-unlock-codes",
        "why": "Landed after this mémo was baked (TKT-239). TWO facts bind TKT-240: (1) the pool row lock serializes ONLY what is pool_id-scoped, so ANY per-reseller quota or cross-slot counter needs its own row lock and a MULTI-SLOT contention proof — a single-slot test will pass while it oversells; (2) refusal precedence is window → code → capacity in both claim paths, and a new reseller refusal must slot into that order deliberately, or a gated channel reads as a sellout exactly when the on-sale is busiest (TKT-238's finding)."
      },
      {
        "id": "ADR-055-on-sale-write-rate-limiting",
        "why": "Landed after this mémo was baked (security review). Extends ADR-051 to the on-sale write path. The partner surface is a write path and inherits this discipline; read it WITH ADR-051 rather than instead of it. (Note the number collision recorded in `reference`.)"
      }
    ],
    "reference": [
      {
        "summary": "PRD v1 — TKT-17 capability row + US-015 non-goals (registry/pricing/windows stay in TKT-17)",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "THE SEAM, line numbers corrected at claim time (the shaping-time citation of :260-263 was STALE — that range is now unrelated HTTP plumbing). The comment explaining the deliberate omission is at server.go:303; the inventory hold body that omits channel is built at server.go:646. The same prohibition text also lives in services/commerce/api/openapi.yaml (~:518-530) and is baked into the generated openapi_gen.go (~:550-553), so closing the seam edits three places and requires a regen commit before the gate.",
        "link": "services/commerce/internal/api/server.go:303,646"
      },
      {
        "summary": "TKT-176 (open, Backlog) owns the SEATED half of the same seam. VERIFIED at claim time: SeatHoldCreate (openapi_gen.go:442-449) has organizer/slot/ticket_type/seat_identities/unit_amount/currency and NO channel field at all. TKT-240 takes the GA half only; the reseller surface must REFUSE on a seated pool and cite TKT-176 rather than half-fixing it.",
        "link": ".sdlc/tickets/TKT-176.json"
      },
      {
        "summary": "ADR-043's own table: catalog already runs declared-security over 37 operations, so the contract-declared idiom is existing, not new",
        "link": "docs/adr/ADR-043-where-a-service-auth-guard-lives.md"
      },
      {
        "summary": "CREDENTIAL BLAST RADIUS — INTERNAL_SERVICE_TOKEN is one shared value opening commerce refunds and inventory operational holds; the partner credential must NOT be that token, nor share its scope (TKT-190 proposed exactly this and it was caught at plan-review)",
        "link": "compose.yaml"
      },
      {
        "summary": "Gateway route table (routes map at main.go:29). Internal routes are 404'd at the edge; an external partner route is a new edge binding. The internal-denial loop registers only for prefixes under /api/.",
        "link": "gateway/cmd/gateway/main.go:29"
      },
      {
        "summary": "REGISTRY DEFECT found at claim time, NOT this ticket's to fix but it blocks picking an ADR number: ADR-055 is used TWICE — ADR-055-on-sale-write-rate-limiting.md (from the 2026-08-10 security review, commit 1783c843) and ADR-055-presale-unlock-codes.md (TKT-239, commit 179034c7). Both Accepted, both cited from code. TKT-240's new identity-class ADR must therefore take ADR-056 and must NOT assume 055 is free. Flagged to the owner.",
        "link": "docs/adr/"
      }
    ],
    "capturedAt": "2026-08-09",
    "governingTests": [
      {
        "name": "TestCatalogSpecGuardsEveryUnsafeOperationAndOnlyThose",
        "path": "services/catalog/internal/api/write_credential_test.go",
        "invariant": "In catalog's spec, every unsafe operation requires CatalogStaffWriteCredential and declares 401; every safe one must explicitly opt out with `security: []`. Derived from the spec in both directions, so it needs no list edit — but catalog's DOCUMENT-LEVEL default security (services/catalog/api/openapi.yaml:21) means a new public/partner read inherits the staff credential unless it opts out."
      },
      {
        "name": "TestDeclaredCacheControlValuesUseRegisteredTiers",
        "path": "shared/go/cachetier/spec_audit_test.go",
        "invariant": "CLOSED ALLOWLIST across all five specs. Three hardcoded things bite: `wantSpecs = 5` (a sixth service spec fails outright), `wantDeclarations` (every operation declaring a Cache-Control — INCLUDING on a 401 or 429 — must be listed; shrinking coverage also fails), and `freeFormAllowed` (closed: a new Cache-Control must be a single-valued enum component, not a bare string). It also hard-refuses `callbacks:`/`webhooks:` in a spec. This is the test that failed TKT-235 twice."
      },
      {
        "name": "TestMain / uncovered2xxOps + TestCatalogCoverageGateIsDeliberatelyUnitScoped",
        "path": "smoke/coverage_test.go",
        "invariant": "EVERY documented 2xx operation in inventory/commerce/payments/access must be exercised through the running smoke stack, or the whole smoke suite fails. `coverageAllowlist` is currently EMPTY (verified) — TKT-240 adding an entry would be the first since it was drained, so prefer writing a real driver. `smokeCoverageGatedServices` is pinned exact-and-ordered against ADR-030."
      },
      {
        "name": "TestMain (catalog coverage)",
        "path": "services/catalog/internal/api/coverage_test.go",
        "invariant": "Catalog's equivalent, in its unit suite, with NO allowlist at all: a new catalog operation must have a happy-path unit test through `env.do`."
      },
      {
        "name": "TestTheEnumerationCoversEveryInternalRouteCommerceServes",
        "path": "services/commerce/internal/api/staff_credential_test.go",
        "invariant": "A hand-written table and the real chi router must describe the same /internal/ surface, both directions, with a `len(onRouter) < 8` floor. NOTE for this ticket: it walks ONLY /internal/ — a partner surface mounted at a non-/internal/ prefix in commerce is invisible to it. That is the shape of hole to watch, not a reason to relax."
      },
      {
        "name": "TestCredentialsAreDistinctRefusesEveryCollidingPair",
        "path": "services/commerce/cmd/commerce/credentials_test.go",
        "invariant": "Commerce's credentials must hold pairwise-distinct values, enumerated exhaustively. A FIFTH commerce credential (a partner secret) requires extending the signature AND adding every new pair — the file says a credential added to the wiring but not the check is the one whose separation is never verified."
      },
      {
        "name": "TestGatewayDeniesGenericInternalRoutes",
        "path": "smoke/contract_validation_test.go",
        "invariant": "/api/<svc>/internal/* is 404'd AT THE EDGE with the gateway's own exact body. Hardcoded probe table. Related: gateway's internal-denial loop only registers for prefixes starting with `/api/` — a partner prefix outside /api/ silently gets no internal denial."
      },
      {
        "name": "TestBackofficeRoute / TestEncodedSeparatorCannotReachInternalRoutes / TestAForgedXForwardedForDoesNotReachTheUpstream",
        "path": "gateway/cmd/gateway/main_test.go",
        "invariant": "The only unit-level pin of the gateway `routes` map; encoded-separator denial; and — load-bearing for ADR-051 rate limiting — the proxy REPLACES X-Forwarded-For with the connecting peer, which is what makes a per-source limit non-forgeable."
      },
      {
        "name": "TestCommittedServiceContractsAreComplete",
        "path": "smoke/contract_validation_test.go",
        "invariant": "Every operation needs a non-empty operationId and EVERY response of every operation needs a committed body schema — including a new 401 and 429."
      },
      {
        "name": "TestNoProductionCodeUpdatesOrderAttribution + TestTheAllowlistCannotBeWidened + TestOrderSQLIsWrittenAsLiterals",
        "path": "services/commerce/internal/store/attribution_invariant_test.go",
        "invariant": "An AST scan of all commerce production Go: exactly TWO SQL statements may write orders.customer_id, matched whole-text including predicates, and order SQL must be written as literals so the scanner can see it. Directly in TKT-240's path: per-reseller attribution on the order. A new order INSERT built by string concatenation fails here."
      },
      {
        "name": "TestEveryStoreMethodIsClassifiedForPublicReads",
        "path": "services/catalog/internal/store/public_read_invalidation_test.go",
        "invariant": "Every method on catalog's store.Store interface must be classified in `readOnlyStoreMethods` or `publicReadEffect`. Applies only if partner credential storage lands in catalog."
      },
      {
        "name": "TestMigrationsAppliedOutOfBand + TestMigrationsRanBeforeServicesStarted + TestServerModeDoesNotMigrate",
        "path": "smoke/smoke_test.go",
        "invariant": "ADR-022: migrations run out-of-band as a Compose job the service waits on; the server path never migrates. Migration filenames are gate-enforced as NNNN_name.sql. NOTE: there is NO migration-number collision test — `max(version_id)` would not detect two files sharing a prefix; goose refuses only at apply time inside the smoke stack. Check the next free number by hand."
      },
      {
        "name": "TestCreateHoldRejectsBadChannel",
        "path": "services/inventory/internal/api/server_test.go",
        "invariant": "Inventory already validates channel_code on holds and engages the allocation cap. Forwarding channel from commerce (the seam closure) engages this 409 path — which is the intended breaking change."
      },
      {
        "name": "TestOnSaleWritesAreRateLimitedSeparatelyFromSignIn",
        "path": "services/commerce/internal/api/ratelimit_test.go",
        "invariant": "The SHAPE a new limiter must follow: prove the budget is finite AND that it is a separate bucket from neighbouring surfaces. There is NO repo-wide test asserting which routes must have a limiter — ADR-051 coverage on the partner surface is a discipline the plan must carry, not something the gate will catch. Also: a 429 must be DECLARED in the spec or ADR-028 fail-closed turns it into a 500."
      },
      {
        "name": "check-generate (Makefile target, not a Go test)",
        "path": "Makefile",
        "invariant": "Regenerated openapi_gen.go / api-types.gen.ts must be COMMITTED BEFORE the gate — it diffs against HEAD, so an uncommitted regen reads as drift. The seam's prohibition text is baked into services/commerce/internal/api/openapi_gen.go, so editing the OpenAPI description alone fails the gate until regenerated."
      }
    ],
    "reResolvedAt": "2026-08-10 (claim-time re-resolution: all 11 original governingAdrs verified to resolve at HEAD; 4 ADRs added; governingTests baked from absent; every test name verified with `git grep -n \"func Test...\"` at HEAD; the seam's line numbers corrected)"
  },
  "links": [
    {
      "type": "blocked-by",
      "key": "TKT-235"
    },
    {
      "type": "blocked-by",
      "key": "TKT-237"
    },
    {
      "type": "blocked-by",
      "key": "TKT-238"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ⚠️ Implementation readiness — CONCERNS\n\n| Criterion | Status | Note |\n|---|---|---|\n| Objective | ✅ | The epic's headline COS |\n| Scope | ✅ | Credential + scoped surface + seam closure; settlement reporting stays TKT-23 |\n| Uncertainties | ⚠️ | Two auth-shape decisions, normally `owner: human` — waived to the agent for this run by explicit owner instruction |\n| Dependencies | ⚠️ | Blocked by TKT-235, TKT-237, TKT-238 |\n| Approach | ✅ | ADR-043 decides the mechanism outright; verified the declared-security idiom already exists in catalog |\n| First slice | ✅ | Splitting the seam closure out would ship a reseller API that oversells its own channel |\n| Success | ✅ | Contention-with-partner and seam-closure-409 tests are load-bearing |\n| Context-mémo | ✅ | Baked, incl. the INTERNAL_SERVICE_TOKEN blast-radius entry (TKT-190's worked example) |\n\n**Concerns:**\n1. **New auth surface, externally reachable.** A third identity class. Normally `approach: deferred` would be refused here outright (shaping.md's four-surface exception); it is `met` because ADR-043 already decides the mechanism, and the residual shape questions are recorded as waived-to-agent rather than hidden.\n2. **Intended breaking behavior change.** Closing the commerce→inventory seam turns some currently-succeeding reserves into 409s. Intended, tested, and called out — but it is the highest-blast-radius change in the epic.\n3. **Data migrations** on the inventory hot path.\n4. Blocked by three siblings.\n\n**Verdict:** CONCERNS — the largest and riskiest child. Proceeding under the owner's explicit waiver of stop-condition (b); every overridden objection is recorded in the closeout's `overrides:` list."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Re-shaping at claim time — verdict: READY, with the merge gate restored\n\nThe mémo was baked 2026-08-09. This is the claim-time re-resolution the skill requires (never trust\na handoff's or a prompt's account of a ticket's state). Five things changed.\n\n| Criterion | Was | Now | Why |\n|---|---|---|---|\n| Objective | ✅ | ✅ | unchanged |\n| Scope | ✅ | ✅ | unchanged |\n| Uncertainties | ⚠️ open | ✅ | The two auth-shape decisions are identified and owned. They are **drafted** by the agent but **presented at Gate 2**, not self-approved — see below. |\n| Dependencies | ⚠️ open | ✅ | TKT-235 / TKT-237 / TKT-238 all verified **Done** on the board. Zero open blockers. |\n| Approach | ✅ | ✅ | Kept, but **corrected** — see finding 1. |\n| First slice | ✅ | ✅ | unchanged |\n| Success | ✅ | ✅ | unchanged |\n| Context-mémo | ✅ | ✅ | Kept, but **substantially rebuilt** — see findings 2–4. |\n\n#### 1. The COS cited a stale seam location, and it is corrected\n\nThe description cites `services/commerce/internal/api/server.go:260-263`. That range is now unrelated\nHTTP plumbing. Verified at HEAD (`a3b947e8`), the seam is:\n\n- `server.go:303` — the comment explaining the deliberate omission\n- `server.go:646` — where the inventory hold body is built without `channel`\n\nAnd it is in **three** places, not one: the same prohibition text is in\n`services/commerce/api/openapi.yaml` (~:518-530) and is **baked into the generated**\n`services/commerce/internal/api/openapi_gen.go` (~:550-553). Closing the seam therefore edits the\nspec and requires a **committed regen before the gate** — `check-generate` diffs against HEAD.\n\n#### 2. The GA / seated ownership question, resolved rather than assumed\n\nThe code comment says *\"TKT-176 owns that question\"*; TKT-240's COS claims the seam. Resolved:\n**TKT-240 takes the GA half, TKT-176 keeps the seated half.** This is not a silent reassignment —\nthe COS already scopes the seated half out as an explicit non-goal, and the code independently\nconfirms the split is clean: `SeatHoldCreate` (`openapi_gen.go:442-449`) carries organizer, slot,\nticket type, seat identities, unit amount and currency, and **no `channel` field at all**. The\nseated half therefore cannot be half-fixed here by accident. The reseller surface **refuses** on a\nseated pool and cites TKT-176.\n\n#### 3. `governingTests` was ABSENT from the mémo — now baked (15 entries)\n\nThis is the gap that failed **TKT-235's gate twice**: a plan can be correct against every governing\nADR and still fail, because these invariants are discoverable only by failing the gate. Every name\nbelow was verified to resolve at HEAD with `git grep -n \"func Test…\"`. The ones most likely to bite:\n\n- **`TestDeclaredCacheControlValuesUseRegisteredTiers`** (`shared/go/cachetier/spec_audit_test.go`) —\n  a **closed allowlist**. `wantDeclarations` must list every operation declaring a `Cache-Control`\n  *including on a 401 or 429*; `freeFormAllowed` is closed; and it **hard-refuses `callbacks:`** in a\n  spec.\n- **`smoke/coverage_test.go`** — every documented 2xx operation in inventory/commerce/payments/access\n  must be driven through the running stack or the whole smoke suite fails. `coverageAllowlist` is\n  **empty** (verified) — an entry here would be the first since it was drained, so write a driver.\n- **`TestNoProductionCodeUpdatesOrderAttribution`** — exactly **two** SQL statements may write\n  `orders.customer_id`, matched whole-text, and order SQL must be **literals** so the AST scanner can\n  see it. Per-reseller attribution walks straight into this.\n- **`TestCredentialsAreDistinctRefusesEveryCollidingPair`** — a fifth commerce credential requires\n  extending the signature *and* every new pair.\n- **No repo-wide test asserts which routes must have a rate limiter.** ADR-051 coverage on the\n  partner surface is a discipline the plan must carry; the gate will not catch its absence. And a\n  `429` must be **declared** in the spec or ADR-028 fail-closed turns it into a 500.\n- **No migration-number collision test exists** — `max(version_id)` cannot see two files sharing a\n  prefix. Check the next free number by hand.\n\n#### 4. Four ADRs landed after the bake, and one is a numbering collision\n\nAdded to `governingAdrs`: **ADR-053** (a narrowly-scoped purpose-specific credential — the direct\nprecedent), **ADR-054** (window → code → capacity refusal precedence), and **both** ADR-055s.\n\n⚠️ **Registry defect, not this ticket's to fix but it blocks picking a number:** `ADR-055` is used\n**twice** — `ADR-055-on-sale-write-rate-limiting.md` (security review, `1783c843`) and\n`ADR-055-presale-unlock-codes.md` (TKT-239, `179034c7`). Both Accepted, both cited from code.\nTKT-240's identity-class ADR must take **ADR-056** and must not assume 055 is free.\n\nFrom ADR-055-presale: **the pool row lock serializes only what is `pool_id`-scoped.** If TKT-240 adds\nany per-reseller quota or cross-slot counter it has the same problem, and a single-slot contention\ntest will pass while it oversells. TKT-239's proof uses three slots.\n\n#### 5. The merge gate is restored for this ticket\n\nThe epic ran under an owner waiver of all four human gates. **This ticket stops before merge.** Six\nconsecutive closeouts recommended it and the owner has now instructed it. Reasons: a **third machine\nidentity class**, externally reachable; an **intended breaking change** that turns currently-succeeding\nreserves into 409s; and TKT-239 having just demonstrated the failure mode where a confidently\nreasoned plan decision was wrong *and the test asserted the defect as correct*\n(`docs/learnings/2026-08-10-a-green-test-can-bless-the-defect.md`).\n\n**Verdict: READY.** All 8 DoR items `met`, zero open blockers. Gate 1 is the owner's to clear.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-240 (US-CH6, reseller API)\n\n**Branch:** `TKT-240-reseller-api` off `origin/main` @ `a3b947e8` (verified local == origin at claim\ntime). **Blockers:** re-verified from the board, zero open — TKT-235, TKT-237, TKT-238 all Done.\n\n**Selection reason:** the sixth and last child of epic TKT-17, and the only one left. Its five\nsiblings are Done and merged; the epic's headline COS — an external reseller sells from shared\ninventory with the same no-oversell guarantee and per-reseller attribution — is delivered by this\nticket or not at all.\n\n#### Risk verdict: `risk:low` NOT claimed — and it would be wrong to\n\nStated explicitly because silence is what lets a mislabeled ticket through. This ticket hits **four\nof the four** carve-outs the label names, and the plan gate must apply:\n\n| Carve-out | Hit? | |\n|---|---|---|\n| **auth** | yes | Introduces a **third machine identity class** (partner credentials) alongside staff sessions (ADR-042), internal tokens (ADR-043) and customer sessions (ADR-049) — and unlike `/internal/`, it is **reachable from the edge**. |\n| **data migrations** | yes | A new hashed-credential table on a service touching the inventory hot path. |\n| **CI/deploy config** | yes | A new gateway route binding, and Compose wiring for the credential + its out-of-band migrate job (ADR-022). |\n| **>5 files** | yes | Spec + generated code + handlers + store + migration + gateway + tests, across at least commerce, inventory and the gateway. |\n\nIt also carries an **intended breaking change**: closing the commerce→inventory GA channel seam makes\nsome **currently-succeeding reserves start returning 409**. That is the highest-blast-radius change\nin the epic, it is deliberate, and it must be called out in the PR and pinned by a test.\n\n#### Gate posture for this ticket — a deliberate departure from the epic's waiver\n\nEpic TKT-17 ran under the owner's explicit waiver of all four human gates, as a process experiment.\n**That waiver no longer applies to this ticket.** The owner has instructed that TKT-240 be built and\nreviewed but **stop before merge**, restoring Gates 2 and 3; Gate 1 was cleared by the owner\nexplicitly rather than by the waiver.\n\nThe reasoning, recorded here because it is the kind of thing that is unrecoverable later: the\nmachinery this pipeline runs catches ordinary defects well, and across five children it did. What it\ndemonstrably does **not** settle is whether a confidently-reasoned decision is *correct*. TKT-239\nclosed with exactly that failure — a plan decision that was wrong, shipped past a mutation-checked\nsuite, with the ticket's own test **asserting the defect as correct** because the assertion was\nwritten by observing what the code did rather than derived from the requirement\n(`docs/learnings/2026-08-10-a-green-test-can-bless-the-defect.md`). A third identity class on an\nedge-reachable surface is the worst possible place to re-run that experiment.\n\n**Consequence for the two open auth-shape decisions** — credential shape (hashed bearer vs mTLS vs\nsigned requests), and whether the seam closure ships behind a per-organizer opt-in or unconditionally:\nthey are **drafted with a recommendation and presented at Gate 2**, not self-approved into\n`kind=plan-final` the way the waiver would have allowed.\n\n#### Model routing (config, not inference)\n\n`plan: gpt-5.6-luna` · `planReview: main-agent` · `implement: main-agent` · `aiReview: gpt-5.6-luna`.\nBoth cross-model pairs hold (`plan ≠ planReview`, `implement ≠ aiReview`). No substitutions.\n\n→ `Ready` → `Planning`, label `agent:planning`.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Execution plan — drafted by `gpt-5.6-luna` (config.models.plan), via the codex companion\n\nModel routing per `.claude/sdlc.config.json`, not inferred. Run: `task --model gpt-5.6-luna\n--effort high`, job `task-mso15gq2-0rgkfk`, ~6min, read-only (no `--write`). The drafter read the\nreal code — the job log shows it reading the seam, the store, the migrations, the presale code path,\nthe customer store and the sibling tickets' board threads.\n\nPosted verbatim below. **It has NOT yet been critiqued** — the `kind=plan-final` delta records what\nsurvives.\n\n---\n\n# TKT-240 execution plan\n\nAll existing paths and symbols named below were verified with `git grep -n`. Items marked **NEW** are proposed additions. No files were modified.\n\n## 1. Recommendations on the open decisions\n\n### D1 — credential shape\n\nRecommend a per-reseller hashed bearer credential.\n\nUse the existing scanner-device pattern in [`services/access/internal/store/scanner_devices.go`](/home/mathieu/sources/ticketing_system/services/access/internal/store/scanner_devices.go):\n\n- generate 32 random bytes;\n- return the plaintext only during provisioning;\n- store only a SHA-256 hash;\n- authenticate with `token_hash = $1 AND revoked_at IS NULL`;\n- revoke by setting `revoked_at`.\n\nCommerce already has the relevant validator seam in [`shared/go/contract/http.go`](/home/mathieu/sources/ticketing_system/shared/go/contract/http.go), while [`shared/go/httpx/credential.go`](/home/mathieu/sources/ticketing_system/shared/go/httpx/credential.go) only supports static configured credentials. There is no existing mTLS or signed-request implementation.\n\nTrade-offs:\n\n- Bearer: lowest partner operating cost and immediate revocation; replayable if stolen, so TLS, idempotency keys, and rate limiting remain necessary.\n- mTLS: better possession semantics and replay resistance, but requires certificate issuance, rotation, edge TLS support, and partner operational maturity that this repository does not have.\n- HMAC signed requests: potentially strongest fit for replay control, but requires canonicalization, timestamp/nonce policy, replay storage, and partner SDK support that also do not exist.\n\nThe most reversible option is hashed bearer: HMAC or mTLS can be added later as another OpenAPI security scheme without changing existing credential rows or order attribution.\n\nDo not add a fifth static commerce environment credential. Keep `credentialsAreDistinct` and `TestCredentialsAreDistinctRefusesEveryCollidingPair` unchanged by storing partner credentials in the database. The partner credential must never be `INTERNAL_SERVICE_TOKEN`.\n\n### D2 — seam closure rollout\n\nRecommend unconditional closure.\n\nTKT-235’s history (`67cb07f9`) deliberately kept the channel registry additive and rejected a similar feature flag. An organizer opt-in would:\n\n- leave the known oversell seam live for opted-out organizers;\n- create a second source of truth for whether channel behavior is active;\n- add rollout, migration, and flag-removal work;\n- make the intended 409 behavior less observable.\n\nThe most reversible option is an opt-in flag, because it can be disabled per organizer. It is nevertheless the weaker recommendation because it contradicts the COS and leaves the defect present. The owner should explicitly approve unconditional behavior at the plan gate because existing channelled commerce traffic will change.\n\n## 2. Ownership and surface\n\n### Catalog\n\nCatalog continues to own channel definitions through its existing `/channels` operations and `ChannelKindReseller`. It should not own partner credentials:\n\n- credentials are machine identities for selling orders, not catalog definitions;\n- ADR-002 puts orders and buyer/sale orchestration in commerce;\n- ADR-042’s catalog ownership rationale applies to staff administering organizers, not partners selling tickets.\n\nThe provisioning command should validate that the organizer/channel exists and has reseller kind through catalog’s existing operator channel read, but this is an issuance-time check, not a cross-service foreign key.\n\n### Commerce\n\nCommerce owns:\n\n- `reseller_credentials`;\n- validator authentication and the authenticated partner scope;\n- partner availability, hold, and confirm-sale contract operations;\n- reservation and order attribution;\n- partner rate-limit buckets.\n\nRecommended external operations in [`services/commerce/api/openapi.yaml`](/home/mathieu/sources/ticketing_system/services/commerce/api/openapi.yaml) are **NEW**:\n\n- `GET /partners/slots/{id}/availability`, operationId `getPartnerAvailability`;\n- `POST /partners/reservations`, operationId `createPartnerReservation`;\n- `POST /partners/orders`, operationId `confirmPartnerSale`.\n\nThe partner reservation schema should require `organizer_id`, `channel_code`, `ticket_type_id`, and GA `quantity`; it should not accept `seat_identities`. The handler compares organizer/channel selectors with the authenticated credential scope and never treats request fields as authority.\n\nThe OpenAPI operations must declare:\n\n```yaml\nsecurity:\n  - PartnerCredential: []\n```\n\nThe validator authentication function, not a handler header comparison, resolves the credential and places `{credential_id, reseller_id, organizer_id, channel_code}` into request context. Follow Access’s pointer-slot pattern in `services/access/internal/api/server.go`.\n\nPartner confirmation must use a SQL-scoped reservation lookup requiring:\n\n```sql\nWHERE id = $1\n  AND organizer_id = $2\n  AND channel_code = $3\n  AND reseller_id = $4\n```\n\nThat predicate is essential; a handler-only comparison is insufficient.\n\nAn operator-only provisioning surface is needed because self-service onboarding is explicitly out of scope:\n\n- [`services/commerce/internal/store/partner_credentials.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/store/partner_credentials.go) — **NEW**;\n- [`services/commerce/cmd/commerce/partner_credentials.go`](/home/mathieu/sources/ticketing_system/services/commerce/cmd/commerce/partner_credentials.go) — **NEW**;\n- `enrol-reseller` and `revoke-reseller` subcommands — **NEW**.\n\nThe plaintext token is printed once. The CLI should validate the catalog channel as an organizer-owned reseller channel before inserting the credential.\n\n### Inventory\n\nInventory remains the correctness boundary and continues to own:\n\n- pool locking;\n- channel allocation capacity;\n- claim attribution;\n- seated-pool rejection.\n\nAdd nullable `reseller_id` to claims, without a foreign key. Historical claims must survive commerce credential deletion or channel changes, consistent with ADR-024.\n\nCommerce forwards `channel_code` and `reseller_id` only for the GA hold body at [`services/commerce/internal/api/server.go:646`](/home/mathieu/sources/ticketing_system/services/commerce/internal/api/server.go:646). It must not add a channel to `SeatHoldCreate`; the seated seam remains TKT-176.\n\nThe existing commerce-to-inventory internal confirmation continues using `INTERNAL_SERVICE_TOKEN`. The partner token is never forwarded downstream.\n\n### Gateway\n\nAdd an explicit route-table entry in [`gateway/cmd/gateway/main.go`](/home/mathieu/sources/ticketing_system/gateway/cmd/gateway/main.go):\n\n```go\n\"/api/commerce/partners/\": \"COMMERCE_URL\",\n```\n\nThe broader `/api/commerce/` route currently catches this path, but the explicit entry makes the partner edge surface deliberate and gives it its own `/internal/` denial registration. Add edge coverage in [`gateway/cmd/gateway/main_test.go`](/home/mathieu/sources/ticketing_system/gateway/cmd/gateway/main_test.go) and [`smoke/contract_validation_test.go`](/home/mathieu/sources/ticketing_system/smoke/contract_validation_test.go).\n\n## 3. Test seam\n\n| Condition | Test tier and proposed test | Why this tier |\n|---|---|---|\n| Missing credential is refused by validator | Commerce API unit, **NEW** `TestPartnerRequestWithoutCredentialIsRefusedByValidatorBeforeHandler` in **NEW** `services/commerce/internal/api/partner_test.go` | The mechanism is OpenAPI `security:` plus `RequestValidatorWithSecurity`. The test must prove the handler is not entered. |\n| Credential is hashed and revocable | Commerce store-smoke, **NEW** tests in **NEW** `services/commerce/internal/store/partner_credentials_smoke_test.go` | Hash lookup and `revoked_at IS NULL` are SQL behavior. A fake credential store cannot prove them. |\n| Channel A cannot hold on channel B | Commerce store-smoke, **NEW** `TestPartnerReservationScopeIsEnforcedBySQL`; end-to-end confirmation in **NEW** `smoke/partner_api_test.go` | The store-smoke test proves the actual organizer/channel/reseller predicate. The smoke test proves validator scope, routing, and the real stack agree. |\n| Partner may read only scoped availability | Inventory availability test around existing `availability.Service.Read` in [`services/inventory/internal/availability/cache.go`](/home/mathieu/sources/ticketing_system/services/inventory/internal/availability/cache.go), plus **NEW** real-stack smoke coverage | The cache/read path is the mechanism for availability. Do not assert this only against a commerce fake. |\n| No-oversell with reseller participation | **NEW** `TestPartnerContentionSharesThePoolCapacity` in `smoke/partner_contention_test.go` | The invariant is established by the running inventory PostgreSQL claim path and pool-row lock. |\n| GA seam closure returns 409 | Inventory store-smoke plus **NEW** `TestChannelledCommerceReserveRefusesWhenAllocationIsExhausted` in `smoke/partner_api_test.go` | Update the existing commerce unit seam test to prove the inventory request contains `channel`; the stack test proves the 409 comes from real allocation SQL. Retain `TestCreateHoldRejectsBadChannel` in [`services/inventory/internal/api/server_test.go`](/home/mathieu/sources/ticketing_system/services/inventory/internal/api/server_test.go). |\n| Revoked credential is refused immediately | Store-smoke SQL test plus **NEW** `TestRevokedPartnerCredentialIsRefusedImmediately` in `smoke/partner_api_test.go` | The store test proves revocation is in the lookup predicate; smoke proves the validator sees it on the next request with no cache. |\n| Seated reseller attempt refuses | Inventory/API smoke, **NEW** `TestPartnerSaleOnSeatedPoolRefusesWithTKT176Code` | The pool kind is known only in the inventory claim transaction. A commerce fake cannot prove the seated pool was actually detected. |\n| Partner rate limits are separate | Extend [`services/commerce/internal/api/ratelimit_test.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/api/ratelimit_test.go) with **NEW** `TestPartnerSubjectAndSourceBudgetsAreSeparate` | This follows `TestOnSaleWritesAreRateLimitedSeparatelyFromSignIn`: prove finite budgets and bucket separation. |\n| Every new 2xx is covered | Extend [`smoke/happy_path_coverage_test.go`](/home/mathieu/sources/ticketing_system/smoke/happy_path_coverage_test.go) | `coverageAllowlist` is empty and must remain empty. New operations need real happy-path drivers. |\n\nThe cross-tenant assertion must not live only in an API test backed by an in-memory fake. The SQL predicate belongs in the commerce store-smoke test; the API smoke test is a second-layer integration check.\n\nRequired existing gate updates:\n\n- `TestCommittedServiceContractsComplete`: new 401, 403, 409, 429, and all 2xx responses need schemas.\n- `TestDeclaredCacheControlValuesUseRegisteredTiers`: if partner availability declares `Cache-Control: no-store`, use a single-valued enum component and add the declaration to `wantDeclarations`. Do not add a bare string or silently omit a 401/429 declaration.\n- `TestNoProductionCodeUpdatesOrderAttribution`, `TestEveryProductionOrderInsertCarriesAttribution`, `TestOrderSQLIsWrittenAsLiterals`: preserve literal SQL and ensure every order insert names `customer_id`, `channel_code`, and `reseller_id`.\n- `TestTheEnumerationCoversEveryInternalRouteCommerceServes`: do not add partner routes to this table; it intentionally walks only `/internal/`.\n- `TestCredentialsAreDistinctRefusesEveryCollidingPair`: remains four static commerce credentials because partner credentials are database rows.\n- `TestMigrationsAppliedOutOfBand`, `TestMigrationsRanBeforeServicesStarted`, and `TestServerModeDoesNotMigrate`: verify both existing migration jobs apply the new migrations.\n\n## 4. Contention proof\n\nUse a GA pool with capacity 10 and a reseller channel allocation capped at 6, modeled on `TestChannelAllocationContention` in [`smoke/inventory_contention_test.go`](/home/mathieu/sources/ticketing_system/smoke/inventory_contention_test.go).\n\nRun concurrent requests through:\n\n- the new commerce partner hold endpoint using the reseller credential and channel;\n- ordinary channelled inventory holds or ordinary commerce reservations;\n- public unchannelled holds competing for the remaining pool capacity.\n\nAssert:\n\n- exactly 6 reseller-channel claims succeed;\n- exactly 4 public claims succeed;\n- total claims equal 10;\n- no response outside the documented 201/409 set occurs;\n- availability reports zero remaining capacity.\n\nThe design adds no per-reseller quota or cross-slot counter. Therefore the existing pool-row lock is sufficient. If a future implementation adds a reseller quota, it must introduce its own locked row, define lock order relative to the pool row, and use a three-slot contention fixture; a single-slot test would not prove a cross-slot counter safe.\n\n## 5. File-by-file change list\n\nExisting files:\n\n- [`docs/adr/ADR-043-where-a-service-auth-guard-lives.md`](/home/mathieu/sources/ticketing_system/docs/adr/ADR-043-where-a-service-auth-guard-lives.md) — use as the security placement constraint.\n- [`services/commerce/api/openapi.yaml`](/home/mathieu/sources/ticketing_system/services/commerce/api/openapi.yaml) — partner security scheme, paths, schemas, errors, and rate-limit responses.\n- [`services/commerce/internal/api/server.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/api/server.go) — validator wiring, partner routes, reservation scope, GA inventory body, claim attribution.\n- [`services/commerce/internal/api/ratelimit.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/api/ratelimit.go) — partner subject/source buckets.\n- [`services/commerce/internal/api/catalog_fees_test.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/api/catalog_fees_test.go) and [`catalog_pricing_test.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/api/catalog_pricing_test.go) — preserve channel pricing tests and add forwarding assertions.\n- [`services/commerce/internal/api/exchanges.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/api/exchanges.go) — carry channel/reseller attribution into replacement reservations and orders.\n- [`services/commerce/internal/store/exchanges.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/store/exchanges.go) — extend `ExchangeSource`, `Exchange`, `LoadExchangeSource`, and `BindOrderExchange`.\n- [`services/commerce/internal/store/attribution_invariant_test.go`](/home/mathieu/sources/ticketing_system/services/commerce/internal/store/attribution_invariant_test.go) — extend literal/order attribution invariants.\n- [`services/commerce/cmd/commerce/main.go`](/home/mathieu/sources/ticketing_system/services/commerce/cmd/commerce/main.go) — register operator credential subcommands; do not add a static partner environment credential.\n- [`services/inventory/api/openapi.yaml`](/home/mathieu/sources/ticketing_system/services/inventory/api/openapi.yaml) — claim reseller attribution and explicit pool-kind error code if needed.\n- [`services/inventory/internal/api/server.go`](/home/mathieu/sources/ticketing_system/services/inventory/internal/api/server.go) — map `ErrPoolKindMismatch` to a machine-readable 409 body.\n- [`services/inventory/internal/store/store.go`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/store.go) — add reseller hold option, persistence, replay scan, and claim projection.\n- [`gateway/cmd/gateway/main.go`](/home/mathieu/sources/ticketing_system/gateway/cmd/gateway/main.go) and [`main_test.go`](/home/mathieu/sources/ticketing_system/gateway/cmd/gateway/main_test.go).\n- [`smoke/contract_validation_test.go`](/home/mathieu/sources/ticketing_system/smoke/contract_validation_test.go).\n- [`smoke/happy_path_coverage_test.go`](/home/mathieu/sources/ticketing_system/smoke/happy_path_coverage_test.go).\n- [`smoke/inventory_contention_test.go`](/home/mathieu/sources/ticketing_system/smoke/inventory_contention_test.go), if the partner contention proof is colocated there.\n- [`shared/go/cachetier/spec_audit_test.go`](/home/mathieu/sources/ticketing_system/shared/go/cachetier/spec_audit_test.go), only if new cache declarations are added.\n\n**NEW files:**\n\n- `docs/adr/ADR-056-partner-credential-identity.md`\n- `services/commerce/internal/api/partner.go`\n- `services/commerce/internal/api/partner_auth.go`\n- `services/commerce/internal/api/partner_test.go`\n- `services/commerce/internal/store/partner_credentials.go`\n- `services/commerce/internal/store/partner_credentials_smoke_test.go`\n- `services/commerce/cmd/commerce/partner_credentials.go`\n- `services/commerce/internal/store/migrations/0020_reseller_credentials_and_attribution.sql`\n- `services/inventory/internal/store/migrations/0015_reseller_claim_attribution.sql`\n- `smoke/partner_api_test.go`\n- `smoke/partner_contention_test.go`\n\nGenerated outputs must be regenerated and committed, not hand-edited:\n\n- `services/commerce/internal/api/openapi_gen.go`\n- `services/inventory/internal/api/openapi_gen.go`\n- whichever of `web/storefront/src/lib/api-types.gen.ts`, `web/storefront/src/lib/commerce-api-types.gen.ts`, and `web/backoffice/src/lib/api-types.gen.ts` are changed by `make generate`.\n\n## 6. Migration plan\n\nThe next free numbers are:\n\n- Commerce: **0020**\n- Inventory: **0015**\n\n### Commerce migration 0020\n\n`services/commerce/internal/store/migrations/0020_reseller_credentials_and_attribution.sql` should add:\n\n```sql\nCREATE TABLE reseller_credentials (\n  id uuid PRIMARY KEY,\n  reseller_id uuid NOT NULL,\n  organizer_id uuid NOT NULL,\n  channel_code text NOT NULL CHECK (length(channel_code) BETWEEN 1 AND 100),\n  token_hash text NOT NULL UNIQUE,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  revoked_at timestamptz\n);\n\nALTER TABLE reservations\n  ADD COLUMN reseller_id uuid,\n  ADD CONSTRAINT reservations_reseller_requires_channel\n  CHECK (reseller_id IS NULL OR channel_code IS NOT NULL);\n\nALTER TABLE orders\n  ADD COLUMN channel_code text\n    CHECK (channel_code IS NULL OR length(channel_code) BETWEEN 1 AND 100),\n  ADD COLUMN reseller_id uuid,\n  ADD CONSTRAINT orders_reseller_requires_channel\n  CHECK (reseller_id IS NULL OR channel_code IS NOT NULL);\n```\n\nNo foreign key should cross the catalog/commerce database boundary. The credential table’s unique hash is the authentication lookup index. Do not add a per-reseller quota counter.\n\nThe down migration must refuse to discard non-null reseller attribution or credential rows, following the refusal style in migrations `0016` and `0019`.\n\n### Inventory migration 0015\n\n`services/inventory/internal/store/migrations/0015_reseller_claim_attribution.sql` should add:\n\n```sql\nALTER TABLE claims ADD COLUMN reseller_id uuid;\n```\n\nNo FK and no counter. Claims are historical inventory attribution and must remain readable even if the commerce credential is revoked or deleted.\n\nBoth migrations are applied by the existing `commerce-migrate` and `inventory-migrate` Compose jobs. No server-path migration code and no new migration service are needed. The implementation must run the migration smoke tests and update any hard-coded expected versions if the tests require it.\n\n## 7. Refusal precedence\n\nUse this ordering:\n\n1. Validator authentication:\n   - missing, malformed, unknown, or revoked credential: `401`;\n   - handler must not run.\n2. Partner scope:\n   - organizer/channel selector differs from the authenticated scope: `403` with a declared `partner_scope_mismatch` error.\n   - this happens before pricing, inventory, or capacity work.\n3. Seated-pool policy:\n   - after inventory resolves the pool kind, a reseller quantity claim on a seated pool returns `409`;\n   - body should include a stable code such as `reseller_seated_pool` and an error message explicitly containing `TKT-176`.\n4. Inventory channel policy:\n   - `window → presale code → capacity`, as required by ADR-055;\n   - channel-window refusal remains `channel_window_closed`;\n   - invalid/missing presale code remains `presale_code_invalid`;\n   - exhausted allocation returns the existing capacity-style `409`.\n\nThe seated refusal is deliberately structural and occurs before window/code/capacity arithmetic: a quantity claim cannot be valid for a seated pool. Add a fixture where the seated pool is also closed/full so the test proves it does not accidentally become a generic sellout response.\n\n## 8. Ordered TDD steps\n\n1. Add the partner OpenAPI paths, security scheme, schemas, and all declared error responses.  \n   First failure: contract/generation tests report missing operation IDs, schemas, or security declarations.\n\n2. Add `TestPartnerRequestWithoutCredentialIsRefusedByValidatorBeforeHandler`.  \n   First failure: the request reaches the handler or returns a non-contract 401 body.\n\n3. Add the credential store and migration smoke tests for hashing, exact organizer/channel scope, and revocation.  \n   First failure: plaintext is recoverable, a revoked row authenticates, or scope is not persisted exactly.\n\n4. Add validator context scope propagation and partner route wiring.  \n   First failure: authenticated scope is absent from the handler, or an unknown OpenAPI security scheme is accepted.\n\n5. Add partner subject/source limiter tests and route-group coverage.  \n   First failure: one partner route bypasses a limiter or partner traffic consumes the customer/checkout bucket.\n\n6. Add inventory claim attribution tests and migration `0015`.  \n   First failure: `reseller_id` is lost on insert/replay or operational claims can accidentally receive reseller attribution.\n\n7. Add commerce reservation/order attribution tests and migration `0020`.  \n   First failure: a partner reservation/order lacks channel or reseller identity, or the SQL scanner misses a new order insert.\n\n8. Extend exchange source/replacement tests.  \n   First failure: an exchanged reseller order becomes channel-less or loses reseller attribution.\n\n9. Change the GA inventory request in `server.go` to forward channel and reseller identity.  \n   First failure: the unit seam test sees a body without `channel`, or a channel allocation exhaustion still returns success.\n\n10. Add explicit seated-pool refusal mapping and tests.  \n    First failure: a seated reseller request returns generic inventory failure or a response without the TKT-176 citation.\n\n11. Add smoke happy-path drivers for all three partner operations.  \n    First failure: `uncovered2xxOps` reports a new commerce operation.\n\n12. Add the real-stack scope, revocation, seam-closure 409, and contention tests.  \n    First failure: the API fake passes but the real SQL path leaks scope, permits a revoked token, or oversells.\n\n13. Run `make generate`, commit generated outputs, then run `make check`.  \n    First failure: `check-generate`, cache-tier allowlists, gateway route tests, or migration wiring detects incomplete integration.\n\n## 9. Risks\n\n- **Intentional 409 blast radius:** every existing commerce reservation that names a channel will now consume that inventory channel allocation. If the allocation is absent or exhausted, it will begin returning 409. This affects existing traffic beyond the reseller feature and must be called out prominently in the PR.\n- The seated commerce seam remains intentionally unchanged and belongs to TKT-176. Do not add `channel` to `SeatHoldCreate`.\n- A bearer token is replayable if stolen. This plan does not claim replay protection; it protects against credential disclosure at rest and supports immediate revocation. A database writer can still replace hashes or revocation state, per ADR-021.\n- Catalog channel registry state is in another database. Credential issuance can validate registry membership, but the credential table cannot enforce it with a cross-service FK.\n- Adding a per-reseller quota later would invalidate the simple contention proof and require its own row lock plus a three-slot test.\n- Response validation will turn undeclared 401/403/409/429 bodies into failures or 500s. Every new refusal must be in OpenAPI before implementation is considered complete.\n- The gateway’s explicit partner prefix must retain the existing encoded-separator and `/internal/` denial behavior.\n- Commerce order attribution has an exchange path as well as the normal checkout path. Updating only `claimOrder` would lose attribution during exchanges.\n- Rate limiting remains in-process and per replica, as prescribed by ADR-051; it is not a distributed on-sale defense."
    },
    {
      "stage": "plan-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=plan-review kind=summary -->\n### Adversarial critique of the plan draft — by `main-agent` (config.models.planReview)\n\nCross-model: drafted by `gpt-5.6-luna`, critiqued here by the session model. I did my **own read of\nthe code first, before opening the draft** — deliberately, because a critique written after reading a\nplan tends to check the plan's internal consistency instead of reality.\n\n**Verification performed.** Every existing file the draft names resolves at HEAD (11/11). Every\nsymbol resolves (`ErrPoolKindMismatch`, `ChannelKindReseller`, `LoadExchangeSource`,\n`BindOrderExchange`, `availability.Service.Read`, `TestChannelAllocationContention`). Migration\nnumbers verified free (commerce **0020**, inventory **0015**). One test name is wrong:\n`TestCommittedServiceContractsComplete` → **`TestCommittedServiceContractsAreComplete`**.\n\n**Overall: accept the architecture, with one blocking defect and four amendments.** The draft is\nbetter than my own independent reading on two points and I am adopting its answers over mine.\n\n---\n\n## B1 — BLOCKING. The proposed gateway route breaks every partner request\n\nThe draft (§2 Gateway) adds:\n\n```go\n\"/api/commerce/partners/\": \"COMMERCE_URL\",\n```\n\n**This does not make the surface \"deliberate\" — it 404s it.** `gateway/cmd/gateway/main.go:287-291`:\n\n```go\npr.Out.URL.Path = strings.TrimPrefix(pr.In.URL.Path, strings.TrimSuffix(prefix, \"/\"))\n```\n\nThe gateway strips **the whole matched prefix**, and ServeMux gives the match to the longest one. With\nthe entry added, `/api/commerce/partners/reservations` is matched by `/api/commerce/partners/` and\narrives at commerce as **`/reservations`** — not `/partners/reservations`. Commerce has no such route.\nEvery partner call fails, and it fails *only through the gateway*, so a commerce unit test passes\nwhile the real stack is broken.\n\nSecond effect: `backends()` (`main.go:301-311`) derives service names from the same table by trimming\n`/api/`, so the entry adds a phantom `commerce/partners` service to `/readyz` and `/healthz/all`.\n\n**Amendment: do not add the entry.** `/api/commerce/` already routes partner paths correctly, and\n`main.go:230` already registers the `internal/` denial for the `/api/commerce/` prefix — the partner\nsurface inherits it. The draft's own stated goal (an explicit, edge-reachable partner surface) is\nachieved by the OpenAPI `security:` declaration plus the route *inside* commerce. If we want the edge\nsurface pinned, pin it with a **test** (`TestBackofficeRoute`-style assertion + a\n`TestGatewayDeniesGenericInternalRoutes` probe), not with a route-table entry.\n\n## B2 — The D2 recommendation is right, but its stated evidence is false — and it is MY false premise\n\nThe draft (§1 D2) says: *\"TKT-235's history (`67cb07f9`) deliberately kept the channel registry\nadditive and rejected a similar feature flag.\"*\n\n**The commit exists; the claim about it does not.** `git show 67cb07f9` contains no occurrence of\n`feature flag`, `opt-in`, `opt in` or `toggle`. I also searched every sibling's board thread on\n`sdlc-state`: TKT-235's thread has no flag discussion at all.\n\n**This is my own error, laundered.** I put \"TKT-235 explicitly decided against a similar flag\" into\nthe drafting brief as settled fact — I inherited it from the run handoff without checking, exactly the\nmistake `shaping.md` warns about (\"a remedy proposed in the ticket text is a hypothesis\"). The drafter\ndid the reasonable thing with a premise its briefer asserted, and handed it back with a commit hash\nattached, which made it look verified. **Recording it because a fixed error and an unnoticed one look\nidentical later** — and because this is the third time in this epic that a confident, ticket-numbered\npremise turned out to be unsupported.\n\n**The real precedent is stronger and points the same way:** TKT-237 (US-CH3) shipped its refusal\nchange **unconditionally** — its own `kind=summary` reads *\"The guard is now **unconditional for a\nchannelled sale**, and the refusal is correct rather than …\"*. That is a much closer analogue than the\nregistry ticket: a channelled sale that starts refusing.\n\n**Recommendation stands (unconditional), evidence replaced.** Note the draft also contradicts itself\nin §1 D2 — *\"The most reversible option is an opt-in flag\"* — while §1 D1 uses reversibility as the\ndeciding criterion. The honest framing: **unconditional is the less reversible option and is still\ncorrect**, because an opt-in leaves a known oversell defect live for opted-out organizers and creates\na second source of truth. Reversibility is a tiebreaker, not a trump card. **This decision goes to the\nowner at Gate 2 regardless** — it changes existing traffic.\n\n## B3 — ADOPTED OVER MY OWN ANALYSIS: the scanner-device precedent (D1)\n\nThe draft found `services/access/internal/store/scanner_devices.go`, which I had missed. Verified:\n32 random bytes, plaintext returned only at provisioning, **SHA-256** stored, authentication by\n`token_hash = $1 AND revoked_at IS NULL`, revocation by setting `revoked_at`.\n\nThis is a better answer than the one I reached independently (I was reasoning from catalog's bcrypt\nstaff hashing toward \"id + fast hash\"). It is the same conclusion, but it is an **existing in-repo\npattern for exactly this problem — a machine credential authenticated on every request** — rather\nthan a derivation. It also disposes of my concern about bcrypt-per-request latency and about not being\nable to look up a bcrypt hash without scanning every row: a SHA-256 `token_hash` with a UNIQUE index\nis a single indexed lookup.\n\nAdopted. One amendment: the draft's table has both `id` and `token_hash UNIQUE`. Authentication\nshould key on **`token_hash` alone** (as scanner devices do), not on a caller-supplied id — a\ncredential id in the header is one more caller-supplied input to get wrong, and it buys nothing when\nthe hash is unique and indexed. Keep `id` as the revocation/administrative handle only.\n\n## B4 — The scope predicate is right; the reason it matters must be recorded (ADR-053)\n\nThe draft (§2) requires confirmation to be SQL-scoped:\n\n```sql\nWHERE id = $1 AND organizer_id = $2 AND channel_code = $3 AND reseller_id = $4\n```\n\nand says *\"a handler-only comparison is insufficient\"*. **Agreed, and this is the single most\nimportant line in the plan.** ADR-053 records this exact failure mode as having been gotten wrong\ntwice: catalog's staff credential can enumerate and mutate **across tenants** because both the list's\nand the update's `organizer_id` are **caller-supplied** — pinned today by\n`TestStaffCredentialCanStillEnumerateAndMutateAcrossTenants` (verified present,\n`channels_test.go:721`).\n\n**Amendment — make the rule explicit in the plan and the ADR:** the partner's scope is **derived from\nthe credential row**, never read from the request. Where the request also carries `organizer_id` /\n`channel_code` (it must, for idempotency terms), the handler **compares and refuses on mismatch**;\nthe request value is never the authority. ADR-053 also supplies two transferable test rules the draft\ndoes not state:\n\n- **\"An allowance is only narrow if something refuses what is next to it.\"** Add a test that the\n  partner credential is refused on the route *beside* the ones it opens.\n- A mutation check in ADR-053 found that widening the guard to a prefix **killed no test**, because\n  hand-mounted handlers carry their own checks. So the scope assertion must be made **against the\n  predicate directly** — which is what the draft's store-smoke tier already does. Keep it there.\n\n## B5 — The contention proof is sound, but for a reason the draft never states\n\nThe draft (§4) proposes capacity 10 / reseller cap 6 and asserts exactly 6 reseller + 4 public = 10.\nThose numbers are correct — the **existing** `TestChannelAllocationContention`\n(`smoke/inventory_contention_test.go`) already asserts exactly 6/4 over 30 concurrent requests. But\nthe draft presents them as if arithmetic made them obvious, and a reader could reasonably fear they\nare interleaving-dependent.\n\nThey are not, and the reason should be in the plan because it is what the test is really proving.\n`services/inventory/internal/store/reservations.go:177-186`, an **unchanneled** claim admits only if\n`confirmed + held + qty + reserved <= limit`, where `reserved` is `SUM(GREATEST(cap - used, 0))` over\nactive allocations (`channel_allocations.go:95-101`). So as the reseller channel consumes, `reserved`\n**shrinks by exactly what the channel's `held` grows** — the sum is invariant. The 6/4 split is\norder-independent by construction, not by luck.\n\n**Amendment:** say this in the plan, and have the partner contention test assert the *invariant*\n(\"the pool never grants more than capacity, and the channel never grants more than its cap\") in\naddition to the two literal counts. Per the repo's newest learning\n(`2026-08-10-a-green-test-can-bless-the-defect.md`), a test that pins only the numbers 6 and 4 pins\nthe behaviour; the invariant stated without naming the mechanism is what pins the rule. Derive\n6 and 4 from cap and capacity in the test, don't hardcode them.\n\nI confirm the draft's §4 closing claim: **it adds no per-reseller quota and no cross-slot counter**,\nso the single pool-row lock is sufficient and ADR-055-presale's three-slot warning does not bite. The\ndraft correctly states what would change that. Good.\n\n## B6 — Amendments to the refusal precedence (§7)\n\nThe four-level ordering (validator 401 → scope 403 → seated 409 → window/code/capacity) is right,\nand putting authorization **before** any stock question is correct — a partner-scope refusal must\nnever be maskable by a full pool (TKT-238's finding).\n\nTwo corrections:\n\n1. **The seated refusal is NOT this ticket's mechanism, and the plan must not imply it is.** The\n   draft says *\"after inventory resolves the pool kind\"*. Verified: `store.go:276-286` **already**\n   refuses the GA quantity path on a seated pool, under the `FOR UPDATE` pool lock, returning\n   `ErrPoolKindMismatch` — with the comment *\"else fungible tickets would sell over reserved seats\n   (AC2)\"*. So TKT-240's work here is **translating an existing refusal** into a partner-facing body\n   citing TKT-176, not creating a guard. The draft's §5 line\n   (\"map `ErrPoolKindMismatch` to a machine-readable 409\") is the correct scope; §7 overstates it.\n   **Consequence for the test tier:** the assertion that a seated pool cannot be oversold belongs\n   where `inventory_kind` is read (store tier, already covered); the *new* test is only that the\n   partner surface returns the documented body. Do not write a commerce-tier test against a fake and\n   call it proof of seated safety.\n\n2. **Commerce declares no `403` anywhere today** (verified: zero occurrences in\n   `services/commerce/api/openapi.yaml`). So the scope-mismatch 403 needs a new declared response\n   *and* a body schema, or ADR-028 fail-closed turns it into a 500 —\n   `TestCommittedServiceContractsAreComplete` requires a schema on **every** response. The draft says\n   this in §9 generally; it must be a concrete step.\n\n## B7 — Smaller amendments\n\n- **The `channel` field name.** Inventory's hold takes **`channel`** (`HoldCreate` in\n  `services/inventory/api/openapi.yaml:429`), *not* `channel_code`. Commerce's request field is\n  `channel_code`. The draft says \"forwards `channel_code`\" in §2 — if implemented literally, inventory\n  ignores the key and the seam **fails open**: no allocation consulted, sale succeeds, seam still\n  open, everything green. This is the highest-risk silent no-op in the ticket. The step-9 test must\n  assert the **outbound body key**, and the smoke test must observe a real 409.\n- **Two production order inserts, not one.** Verified: `services/commerce/internal/api/server.go:1006`\n  and `services/commerce/internal/api/exchanges.go:560`. The draft is right to flag the exchange path\n  (§9) — this is a good catch and it is exactly where attribution would be silently lost.\n- **Do not extend the attribution allowlist.** The draft's §5 line \"extend literal/order attribution\n  invariants\" is ambiguous and the ambiguity is dangerous.\n  `TestNoProductionCodeUpdatesOrderAttribution` sanctions exactly **two** whole-text-matched UPDATEs\n  of `orders.customer_id` and `TestTheAllowlistCannotBeWidened` guards it. Adding `reseller_id` and\n  `channel_code` as **INSERT** columns does not touch that allowlist; adding a third UPDATE entry\n  would, and would be a blocking objection. State the distinction in the plan. Note\n  `orders.customer_id` is **nullable** (`0016_order_customer_attribution.sql:18`, no NOT NULL), so a\n  reseller order may carry NULL — but the column must still be **named** in the insert\n  (`TestEveryProductionOrderInsertCarriesAttribution`).\n- **Rate limiting: the partner surface has a real subject, and the draft under-uses it.**\n  `ratelimit.go:128-131` explains that the on-sale write path is source-keyed only *because* \"neither\n  operation has a subject that exists before the body is decoded\". The partner surface is different —\n  the credential identifies the reseller **in the validator, before the body is decoded**. So a\n  genuine per-**subject** (reseller) limit is available, which is what ADR-051 actually asks for.\n  Per-source alone is weak here: a partner is a server with a stable IP that legitimately sends volume.\n  The draft's test name (`TestPartnerSubjectAndSourceBudgetsAreSeparate`) implies both; the design\n  section should say so too.\n- **`429`/`401`/`403` and the cachetier allowlist.** The draft's §3 note is correct and worth keeping\n  literal: any `Cache-Control` declared on a new response — *including a 401 or 429* — must be added\n  to `wantDeclarations` in `shared/go/cachetier/spec_audit_test.go` and must be a **single-valued enum\n  component**, not a bare string (`freeFormAllowed` is closed).\n- **ADR number 056 confirmed**, and the reason must be recorded: `ADR-055` is currently taken **twice**\n  (`ADR-055-on-sale-write-rate-limiting.md` from the security review, `ADR-055-presale-unlock-codes.md`\n  from TKT-239 — both Accepted, both cited from code). Not this ticket's to fix; flagged to the owner.\n\n## What I rejected from the draft\n\n- **The gateway route entry (B1)** — rejected outright, it is a defect.\n- **The D2 evidence (B2)** — rejected; recommendation kept on different grounds.\n- Nothing else. The rest is accepted or amended above.\n\n## Introduced at plan-review, reviewed by no second model\n\nPer the skill's requirement, design elements **I added** here rather than accepting/amending/rejecting\nfrom the draft — these reach Gate 2 reviewed by nobody but their author:\n\n1. **The `reserved`-shrinks-as-the-channel-consumes invariant argument (B5)** and the instruction to\n   assert the invariant and derive 6/4 rather than hardcode them.\n2. **Keying authentication on `token_hash` alone, with `id` as an administrative handle only (B3).**\n3. **The \"refuse what is next to it\" test and the assert-against-the-predicate rule (B4)**, imported\n   from ADR-053.\n4. **The per-subject (reseller-keyed) rate limit argument (B7)** — that the partner surface has a\n   subject where the checkout path does not.\n\n## Open for the owner at Gate 2\n\nBoth decisions are **drafted with a recommendation, not self-approved** — the owner restored Gates 2\nand 3 for this ticket.\n\n- **D1 — credential shape.** Recommend **hashed bearer**, following the scanner-device pattern\n  (SHA-256, `revoked_at`), keyed on `token_hash`. No mTLS/HMAC implementation exists in the repo.\n- **D2 — seam closure rollout.** Recommend **unconditional**, on TKT-237's precedent (not TKT-235's,\n  see B2). This makes currently-succeeding channelled reserves start returning 409.\n"
    }
  ]
}
