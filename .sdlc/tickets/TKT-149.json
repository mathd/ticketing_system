{
  "key": "TKT-149",
  "type": "Task",
  "summary": "Flaky: TestOnsaleLoadProof fails on a single 5xx in 250 requests (observed during TKT-134)",
  "status": "Backlog",
  "labels": [],
  "assignee": null,
  "pr": null,
  "links": [
    {
      "type": "relates",
      "key": "TKT-134"
    }
  ],
  "description": "Observed during TKT-134's gate runs, on a tree whose only change was a test file in a different Go module (`services/inventory/internal/store`), so it cannot have been caused by that work.\n\n```\nonsale_load_test.go:283: stage sustained  offered=250 started=250 dropped=0 ok=249 rejected=0\n                         client-err=0 server-err=1 inflight max/peak=64/1\n                         hold p50/p95/p99=2.7/3.9/4.5ms lifecycle p50/p95/p99=5.8/8.1/10.4ms\n                         lag p99=2.2ms achieved=25.0 ok/s\nonsale_load_test.go:385: 1 attempts hit server-side errors before the fill (5xx/unexpected status/malformed body)\n--- FAIL: TestOnsaleLoadProof (11.81s)\n```\n\nPassed on an immediate re-run of the identical tree (`EXIT=0`, `PASS: TestOnsaleLoadProof (12.80s)`).\n\n**What to establish:**\n- [ ] What the single 5xx actually was. The assertion reports a count, not the response — so the test proves \"something 5xx'd\" and cannot say what, which is the first thing to fix regardless of the outcome.\n- [ ] Whether a lone server error in 250 attempts should fail the proof at all. TKT-92/93 set this harness's error taxonomy; a zero-tolerance threshold on a load test that shares a machine with the rest of the gate may be the wrong bar — or may be exactly right, in which case the 5xx is a real defect and this is not a flake ticket.\n- [ ] Same discipline as TKT-132/134: no `-count=N`. Either capture the failing response to identify it, or force the condition deterministically.\n\n**Note the pattern.** This is the third flake in this area (TKT-132 recovery sweep, TKT-134 lazy release, this). All three are timing-sensitive assertions over a shared, loaded stack. Worth asking once whether the gate's smoke stage needs isolation guarantees it does not currently have, rather than fixing them one at a time.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Implementation readiness — CONCERNS (shaped 2026-08-08)\n\nFiled unshaped; Gate 1 was hard-blocking it. Shaped now.\n\n| Criterion | Status | Note |\n|---|---|---|\n| Objective | OK | No gate failure on one transient 5xx; still catches a real regression |\n| Scope | OK | The load proof's error accounting only |\n| Uncertainties | **WARN** | The 5xx is unexplained — it may be the finding, not the noise |\n| Dependencies | OK | None |\n| Approach | **WARN** | Diagnose before tolerating |\n| AC verifiable | OK | Must prove the proof can still fail |\n| Context-mémo | OK | Re-resolved today |\n\n**Concern:** the tempting fix — allow 1 error in 250 — is exactly the move `AGENTS.md` warns about\nwith bounded replacements: *a bounded or optimized replacement is a new implementation, so ask what\nthe version it replaced would have caught*. A proof that tolerates an unexplained 5xx has quietly\nlowered the NFR TKT-82 established. Diagnose first; tolerate only with a stated rationale.\n\n**Lowest urgency of the three test-integrity tickets** (TKT-198 red now, TKT-218 fails open, this one\nfails closed and rarely) — but it is also the cheapest to make diagnosable, which is the first slice."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ⚠️ MATERIAL CORRECTION — the first acceptance criterion is already satisfied, and was when the ticket was filed\n\nBacklog review 2026-08-26 at HEAD `1aaee7cd`. **Still hold at Gate 1, but for a different reason than the ticket gives.**\n\n**AC #1 is false.** The ticket says the assertion *\"reports a count, not the response — so the test proves 'something 5xx'd' and cannot say what, which is the first thing to fix regardless of the outcome\"*, and the earlier readiness comment calls that the cheapest first slice. The capability already exists and predates the ticket:\n\n- Every `KindServerError` path sets a diagnostic `Note` with status code + 200 chars of body — six sites at `smoke/onsale_load_test.go:123,141,149,161,165,172`.\n- The runner retains the first three: `smoke/internal/loadtest/loadtest.go:137-141` (`ErrorNotes`).\n- `logStage` prints them immediately before the stats line: `smoke/onsale_load_test.go:280-281`.\n\nAll shipped in **TKT-92 (`9254e057`)**. Confirmed against the contemporaneous tree — `git show c46440cb:smoke/onsale_load_test.go` (last commit before 2026-07-31) already has the `ErrorNotes` loop and all six `Note` sites. So the failure pasted in the ticket **should have been preceded by a `stage sustained error sample: …` line**; either the paste was trimmed or it was not read.\n\n**What is genuinely intact.** Zero tolerance is still in force (`onsale_load_test.go:409-411` pre-fill, `:451-453` post-reject, both `t.Fatalf` on `ServerErrors != 0`), and the TKT-92/93 taxonomy is unchanged (`loadtest.go:132-141`).\n\n**One more framing correction.** The earlier comment ranks this *\"lowest urgency of the three test-integrity tickets — TKT-198 red now, TKT-218 fails open\"*. TKT-132, TKT-134, TKT-198, TKT-213 and TKT-218 are **all Done**. This is the last one standing. It is also not in `make check` — it is the on-demand `onsale-load-full` target (`Makefile:175-178`), so the \"shares a machine with the rest of the gate\" argument needs re-checking against how it is actually invoked.\n\n**The next action is a retrieval, not a ticket.** Strike AC #1, then recover the full TKT-134 gate-run log with its `error sample` line — or re-run `make onsale-load-full` and capture one. Only then is the remaining question (is one 5xx in 250 a defect or acceptable noise?) answerable, and the AGENTS.md trap the earlier comment names is exactly right: do not tolerate an unexplained 5xx, because a proof that does has quietly lowered TKT-82's NFR.\n"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "TestOnsaleLoadProof does not fail the gate for a single transient 5xx in 250 requests, without becoming unable to detect a real server-error regression."
    },
    "scope": {
      "state": "met",
      "note": "services/.../onsale_load_test.go and its error accounting. Not the on-sale NFR targets themselves (TKT-82)."
    },
    "uncertainties": {
      "state": "open",
      "note": "OPEN: what produced the single 5xx? Until that is known, any tolerance is a guess. A load proof that tolerates errors it cannot explain is weaker than one that fails - the 5xx may be the finding."
    },
    "dependencies": {
      "state": "met",
      "note": "None."
    },
    "approach": {
      "state": "open",
      "note": "OPEN. Explain the 5xx first (capture the response body/status on failure if it is not already captured); only then decide whether a bounded tolerance is honest or whether the error is a real defect to fix."
    },
    "first_slice": {
      "state": "met",
      "note": "Make the failure diagnosable: record what the failing request actually returned, so the next occurrence answers the question instead of re-raising it."
    },
    "success": {
      "state": "met",
      "note": "Either the 5xx is explained and fixed, or a tolerance is introduced WITH its rationale and a test proving the proof still fails on a sustained error rate."
    },
    "context_memo": {
      "state": "met",
      "note": "Re-resolved 2026-08-08."
    }
  },
  "parent": "TKT-37"
}