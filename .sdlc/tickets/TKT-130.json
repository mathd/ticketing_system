{
  "key": "TKT-130",
  "type": "Task",
  "status": "PO Review",
  "labels": [
    "risk:low",
    "needs:human"
  ],
  "assignee": "agent",
  "pr": {
    "number": 122,
    "url": "https://github.com/mathd/ticketing_system/pull/122",
    "state": "merged"
  },
  "summary": "onsaleFull discards all evidence when any stage aborts — defer writeReport",
  "description": "### Finding (TKT-125 measurement, 2026-07-27)\n\n`writeReport(t, report)` is the **last statement** of `onsaleFull` (`smoke/onsale_load_test.go`). Every `generatorHealthy` check is a `t.Fatalf`, so a single aborted stage discards the whole accumulated `report` — including stages that completed cleanly minutes earlier.\n\nThis cost TKT-125 its COS: on a single-host setup the ceiling sweep's `sweep-600` stage trips the generator guard **every time** (3 runs of 3, on 4-9 client-side transport errors at 600 attempts/s), so no JSON was ever emitted, even though the `nfr-3000pm` stage the ticket actually needed had completed 9,000/9,000 clean. `finalize_p99_ms` and `confirm_p99_ms` exist only in that report, so they could not be recorded.\n\n### Scope\n\n`defer writeReport(t, report)` (or equivalent) so a partial run still emits what it measured. The report already carries per-stage entries appended as they complete, so partial output is meaningful, not misleading — but consider marking the report `partial: true` so a reader cannot mistake an aborted run for a complete one.\n\n### Acceptance Criteria\n\n- [ ] An `onsaleFull` run whose sweep aborts still writes `ONSALE_REPORT` containing every completed stage\n- [ ] The report distinguishes complete from partial runs\n- [ ] TKT-125's missing `finalize_p99_ms` / `confirm_p99_ms` are obtainable by re-running the two arms\n- [ ] `make check` green",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "An aborted stage discards every stage that already completed; TKT-125 lost finalize_p99_ms/confirm_p99_ms to it."
    },
    "scope": {
      "state": "met",
      "note": "IN: defer writeReport + a partial marker in smoke/onsale_load_test.go. OUT: re-running the ceiling sweep to harvest TKT-125s numbers, and any change to the generatorHealthy guard itself."
    },
    "uncertainties": {
      "state": "met",
      "note": "Only whether a partial report can mislead a reader — closed by the partial marker (AC 2). t.Fatalf runs deferred functions, so defer is sufficient; no goroutine-exit path in onsaleFull bypasses it."
    },
    "dependencies": {
      "state": "met",
      "note": "None. smoke/onsale_load_test.go is untouched by anything in flight."
    },
    "approach": {
      "state": "met",
      "note": "defer writeReport(t, report) at the top of onsaleFull (currently the last statement, :519) + report.partial set true unless the run reaches the end."
    },
    "first_slice": {
      "state": "met",
      "note": "One function, one defer, one field. Whole ticket is the slice."
    },
    "success": {
      "state": "met",
      "note": "AC 3 resolved to mechanism-only (see readiness comment) — an injected abort proves the completed stages survive; harvesting TKT-125s numbers is a separate run, not this diff."
    },
    "context_memo": {
      "state": "deferred",
      "note": "No ADR governs the smoke load harness reporting format — checked all of docs/adr/ at HEAD. ADR-030 is the catalog coverage gate, not this. Recorded as a finding, not an omission."
    }
  },
  "context": {
    "binding": [],
    "governingAdrs": [],
    "reference": [
      {
        "summary": "the report writer, last statement of onsaleFull",
        "link": "smoke/onsale_load_test.go:519"
      },
      {
        "summary": "the run that lost its numbers to this",
        "link": "docs/evidence/TKT-125/README.md"
      }
    ],
    "capturedAt": "2026-07-28"
  },
  "links": [],
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\nFrom TKT-125's measurement. The one-line fix was deliberately **not** taken in TKT-125 because that ticket's plan explicitly recorded that `smoke/onsale_load_test.go` would not be modified — filing rather than widening scope. See `docs/evidence/TKT-125/README.md` § Known gap."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### DoR — 8/8 (1 deferred). Shaped 2026-07-28.\n\n**Decision taken at shaping (owner call, delegated):** AC 3 is **mechanism-only**. The ticket\ndelivers \"a partial report survives an aborted sweep\"; it does **not** require re-running the\nceiling sweep to actually record TKT-125's `finalize_p99_ms` / `confirm_p99_ms`. Reason: on a\nsingle-host setup `sweep-600` trips the generator guard every time (3/3 runs), so binding this\ndiff to a green full run binds it to a host it cannot have. Rewrite AC 3 as: *an `onsaleFull`\nrun aborted at `sweep-600` writes a report containing the `nfr-3000pm` stage's\n`finalize_p99_ms` and `confirm_p99_ms`* — provable by an injected abort in seconds.\n\n`context_memo` is `deferred`, not skipped: no ADR governs the load harness's reporting. Checked\ndocs/adr/ at HEAD; the nearest candidate (ADR-030) is the catalog coverage gate.\n\n**Verdict: READY.** `risk:low` stands — one `defer`, one bool, no source outside `smoke/`.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — branch `TKT-130-defer-onsale-report` off `origin/main` @ 2fb5709\n\n**Selection reason:** owner-directed run. Top of Ready, DoR 8/8 (one `deferred`, justified),\nzero blockers (`links: []`), `risk:low` restated below and standing.\n\n**`risk:low` restated (challengeable, per the skill):** the diff is confined to\n`smoke/onsale_load_test.go` plus one `bool` field on `loadtest.Report` — test harness only. No\nauth, no money path, no migration, no CI/deploy config, well under 5 files. If planning finds the\nchange has to reach beyond `smoke/`, the label comes off and the plan gate comes back.\n\n---\n\n### ⚠️ PROCESS WAIVER — this ticket runs **gateless**, by owner instruction\n\nThe repo owner explicitly waived **all four human gates** for this run as a process experiment.\nRecorded here so the board history shows what was and was not human-checked:\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human moves Backlog → Ready | **already given** (ticket was in Ready) |\n| 2 — plan approval | human moves Planning → Building | **waived.** Agent resolves open decisions itself: prefer the plan-critique's recommendation; absent one, the most reversible option. Every self-made decision is recorded in `kind=plan-final`. (Also nominally skipped anyway by `risk:low`.) |\n| 3 — review + merge | human reviews PR + squash-merges | **waived.** Agent squash-merges and deletes the branch after ai-review converges. The `review-guide` is still posted on the PR first, as the audit record. |\n| 4 — PO acceptance | PO validates COS + moves to Done | **waived.** Agent self-validates each COS against test evidence on the merged code, posts the validation note, then closes with `kind=metrics`. |\n\n**Unchanged and non-negotiable for this run:** `config.models` routing with no substitutions\n(`plan`=gpt-5.6-sol, `planReview`=main-agent, `implement`=main-agent, `aiReview`=gpt-5.6-sol);\ncross-model review on both plan and code; TDD with the test observed **red first**; `make check`\ngreen before every push and again after the merge; every marker comment posted at its stage; no\nAI attribution in any commit or PR.\n\n**Standing stop conditions** (agent halts and reports instead of proceeding): gate or CI fails\ntwice on the same cause with no clear fix; the work would touch auth, money paths, data\nmigrations, or CI/deploy config in a way the ticket and plan did not anticipate; a blocking review\nfinding survives the churn cap; a configured model is unreachable after the documented escalation\nladder; or a destructive/hard-to-reverse action where the right call is ambiguous.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — authored by **gpt-5.6-sol** (`config.models.plan`), effort high, read-only\n\nRun via the codex companion `task` command, no `--write`. Verbatim below; critique follows in\n`kind=plan-final`.\n\n---\n\n## DoD\n\n- `onsaleFull` writes `ONSALE_REPORT` from a deferred path whenever reporting has begun, including after `t.Fatalf`.\n- An aborted run contains every previously accepted stage; specifically, `nfr-3000pm` retains `finalize_p99_ms` and `confirm_p99_ms`.\n- Every new report explicitly contains `\"partial\": true` after an abort and `\"partial\": false` after normal completion.\n- The aborting `sweep-600` stage remains excluded because `generatorHealthy` runs before its append (`smoke/onsale_load_test.go:474-480`); invalid generator evidence must not be published.\n- `generatorHealthy` is unchanged.\n- The normal `make check` gate passes. No real ceiling sweep is run for this ticket.\n\n## Plan\n\n1. Add `Partial bool \\`json:\"partial\"\\`` to `loadtest.Report` at `smoke/internal/loadtest/loadtest.go:312-329`. Do not use `omitempty`, so complete reports explicitly serialize `false`.\n\n2. Add a small reporting-lifecycle helper near `writeReport` (`smoke/onsale_load_test.go:250-263`) that:\n\n   - Sets `report.Partial = true`.\n   - Defers `writeReport(t, report)`.\n   - Invokes the supplied profile body synchronously.\n   - Sets `report.Partial = false` only after that body returns normally.\n\n   Wrap the existing `onsaleFull` body with this helper. Keep the stage order and `generatorHealthy` closure (`:409-418`) unchanged. The NFR report is already appended before the sweep (`:430-441`), and its percentile fields originate at `smoke/internal/loadtest/loadtest.go:289-304`.\n\n3. Apply the same helper to `onsaleGate`, replacing its terminal write at `smoke/onsale_load_test.go:391`. It has the identical loss mode after appending stages (`:303-342`) and shares the same report schema; profile-dependent semantics would be surprising.\n\n4. Add the regression tests described below, then run `make check`.\n\n## Test seam\n\nAdd `smoke/onsale_load_report_test.go` with the `smoke` build tag.\n\n- Complete-path test: set `ONSALE_REPORT` to `t.TempDir()`, invoke the reporting helper with a callback that returns normally, parse the JSON, and assert the `partial` key is present and `false`.\n- Abort-path test: execute the current test binary as a subprocess with an environment sentinel and temporary `ONSALE_REPORT`. In child mode, invoke the same reporting helper; append a synthetic `nfr-3000pm` `StageReport` with distinct `FinalizeP99Ms` and `ConfirmP99Ms`, then call `t.Fatalf(\"injected abort at sweep-600\")`.\n- The parent must assert:\n\n  - The subprocess exits non-zero for the expected injected diagnostic.\n  - The JSON file exists and parses.\n  - `partial` is explicitly `true`.\n  - `nfr-3000pm` exists with the exact injected finalize/confirm p99 values.\n\nThe subprocess is necessary because an expected `t.Fatal` cannot be neutralized by a subtest: it permanently fails its parent test. The deferred writer still receives a real `*testing.T`; any writer failure therefore remains a genuine child failure.\n\nThis test runs in the normal gate because `make check` includes `smoke` (`Makefile:17,102-107`), and the smoke script runs the package without an allowlist (`scripts/smoke.sh:190-199`). The injected body uses no database, HTTP, or timed load and completes in seconds.\n\n## Files touched\n\n- `smoke/internal/loadtest/loadtest.go` — add the serialized `Partial` field.\n- `smoke/internal/loadtest/loadtest_test.go` — extend the existing JSON metadata coverage at `:329-354` to pin the explicit `partial` key.\n- `smoke/onsale_load_test.go` — add the deferred lifecycle helper; route both profiles through it; remove both terminal `writeReport` calls.\n- `smoke/onsale_load_report_test.go` — add normal-return and subprocess-fatal regression tests.\n\n## DECISION: partial marker\n\nOptions are an explicit boolean or a completion-status enum.\n\nRecommendation: `Partial bool \\`json:\"partial\"\\``. It is the smallest additive format change and matches the ticket language. Set it `true` before entering the run and clear it only on normal return.\n\nTrade-off: older reports omit the field, and decoding them directly into a Go `bool` yields `false`. Readers must treat a missing key as legacy/unknown—not proof of completion. A status enum would preserve that distinction automatically but adds unnecessary schema and consumer complexity for this ticket.\n\n## Control-flow finding and risks\n\nThere is no current goroutine-exit path around the defer. `RunStage` launches workers but waits for all of them before returning (`smoke/internal/loadtest/loadtest.go:109-147`); workers return `Outcome` values and never receive `*testing.T`. All `generatorHealthy` calls and other fatal checks execute synchronously on the test goroutine.\n\nRisks:\n\n- `partial:false` means the profile reached its end, not that every nonfatal assertion passed; `t.Errorf` should still produce a complete report.\n- `os.Exit`, `SIGKILL`, a process crash, or an unwritable report path cannot be rescued by a defer.\n- The callback/helper extraction must remain synchronous; moving the profile body into a goroutine would invalidate the guarantee.\n- Do not move the failed sweep append ahead of `generatorHealthy`; that would publish explicitly inconclusive evidence.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan-final — **delta** on the `kind=plan` draft. Critique by **main-agent** (`config.models.planReview`).\n\n**The `kind=plan` draft as amended below is the plan.** Nothing restated; read the two together.\n\n---\n\n## 1. Existence check on every seam the draft named (`git grep` at HEAD `2fb5709`)\n\nA drafter that cannot run the gate will hallucinate seams, so each was resolved before critique:\n\n| Draft citation | Verdict |\n|---|---|\n| `smoke/onsale_load_test.go:474-480` — `generatorHealthy(r)` precedes the sweep append | ✅ exists, order as claimed (`:477` guard, `:478` append) |\n| `smoke/internal/loadtest/loadtest.go:312-329` — `type Report struct` | ✅ exists |\n| `…loadtest.go:289-304` — `FinalizeP99Ms` / `ConfirmP99Ms` built in `StageResult.Report()` | ✅ exists (`:297-298`) |\n| `…loadtest.go:109-147` — `RunStage` joins its workers before returning | ✅ `wg.Wait()` at `:145`; workers never receive `*testing.T` |\n| `…loadtest_test.go:329-354` — `TestReportJSONCarriesRequiredMetadata` | ✅ exists, is the right place to pin the new key |\n| `Makefile:17,102-107` — `check` includes `smoke` | ✅ exists |\n| `scripts/smoke.sh:190-199` — package run with no `-run` allowlist | ✅ `:199` runs `go test -tags smoke -count=1 -v ./...` |\n| `smoke/onsale_load_test.go:391` — `onsaleGate`'s own terminal `writeReport` | ✅ exists — same loss mode, the draft is right to catch it |\n\nNo hallucinated seam. The control-flow finding (no goroutine-exit path bypasses the defer) is\nindependently confirmed: `RunStage`'s workers return `Outcome` values and never touch `*testing.T`,\nso every fatal in `onsaleFull` fires on the test goroutine.\n\n## 2. Findings — accept / amend / reject\n\n**ACCEPT — `Partial bool` with `json:\"partial\"`, no `omitempty`.** The draft's `DECISION:` block\nrecommends the boolean over a status enum; taken. Explicit `false` on complete runs is the load-\nbearing part — with `omitempty` a complete run and a legacy run would both present as *absent*.\nThe draft's own caveat stands and is worth repeating in the code: **a missing key is legacy/unknown,\nnot proof of completion.** Reports written before this ticket (e.g. any TKT-82 artifact) decode to\n`Partial=false` and must not be read as \"verified complete\".\n\n**ACCEPT — set `true` on entry, clear only on normal return.** Fail-closed: every abnormal exit path,\nnamed or not, leaves the report marked partial. The inverse (set `true` in the failure handler) would\nrequire enumerating the failure paths, which is how this class of bug recurs.\n\n**ACCEPT — `onsaleGate` gets the same treatment.** Same file, same loss mode, same schema. Cheap, and\nprofile-dependent reporting semantics would be a trap for the next reader. Note it is near-inert in\npractice: the gate profile runs with `ONSALE_REPORT` unset, so `writeReport` returns at\n`onsale_load_test.go:252` — this buys consistency, not behaviour.\n\n**ACCEPT — the aborting stage stays out of the report.** `generatorHealthy` fires *before* the append\n(`:477-478`), so a sweep stage that trips the guard is never published. That is correct and must not\nbe \"fixed\": `loadtest.CeilingInconclusive` is a **generator** verdict, and publishing an inconclusive\nstage as server evidence is precisely the dishonesty the guard exists to prevent. The draft flags\nthis in its risks; promoting it to a DoD line.\n\n**AMEND — the subprocess test needs a condition the draft did not state.** The draft proposes\nre-executing the test binary with an env sentinel, and it is right that an expected `t.Fatal` cannot\nbe neutralised in-process by a subtest. But it did not check what else that child process runs: the\nsmoke package has a **`TestMain` at `smoke/coverage_test.go:119`** that, after `m.Run()`, enforces the\nuncovered-2xx-operations coverage gate and can turn a `0` into a `1`. The child is only safe because\nthat gate is disarmed on filtered runs (`:122-123`: `flag.Lookup(\"test.run\")` non-empty ⇒ skip).\n**So the child MUST be invoked with a `-test.run=^…$` filter** — not merely as a convenience to\nselect the test, but as the thing that keeps `TestMain`'s coverage gate from firing in a child that\nby construction runs one test. Without it the child's exit code stops meaning \"the injected abort\nhappened\". The parent asserts a non-zero exit, so this would not have produced a false green — but it\nwould have produced a green for the wrong reason, which is worse in an evidence harness.\nSecond condition, same area: the child test must **`t.Skip` when its sentinel env var is absent**, so\nit is inert in the ordinary gate run rather than aborting it.\n\n**AMEND — assert the abort's own evidence, not just the file's existence.** The draft's parent\nassertions are right but stop short: pin that the *aborting* stage is **absent** from the written\nreport, alongside the completed `nfr-3000pm` stage being present with its exact injected\n`finalize_p99_ms` / `confirm_p99_ms`. Presence-only assertions would still pass if a future change\nstarted publishing inconclusive stages — the one outcome the guard exists to prevent.\n\n**REJECT — nothing.** No part of the draft is wrong.\n\n**NOTED, out of scope — `partial:false` means \"reached the end\", not \"passed\".** A run where a\n`t.Errorf` SLO assertion failed still completes and still writes `partial:false`. The draft flags\nthis. I am deliberately **not** adding a second `failed` field: AC 2 asks the report to distinguish\ncomplete from partial runs, and it does. Conflating \"incomplete\" with \"failed\" in one boolean would\nmake both unreadable. If a reader ever needs run-outcome in the JSON, that is a new ticket. Recording\nthe limitation here so it is a known boundary, not an oversight.\n\n## 3. Decisions I made myself (Gate 2 waived — recorded per the run's terms)\n\nGate 2 is waived for this run **and** nominally skipped anyway by `risk:low`. Rule applied: prefer\nthe critique's recommendation; absent one, the most reversible option.\n\n| # | Decision | How resolved | Rationale |\n|---|---|---|---|\n| D1 | Partial marker: boolean vs. status enum | **boolean `partial`** | The draft's `DECISION:` block recommends it — critique-recommended option taken as-is. Smallest additive schema change; an enum can be added later without breaking a boolean reader. |\n| D2 | Abort-injection seam: subprocess re-exec vs. in-process `runtime.Goexit` in a goroutine | **subprocess re-exec** | The draft recommends it. I weighed the smaller in-process alternative (a `runtime.Goexit()` body in a goroutine — `t.FailNow` *is* `runtime.Goexit`, so it exercises the same primitive in ~⅓ the code) and rejected it: it proves the property through *my belief about* what `t.Fatalf` does, where the subprocess proves it through the real `t.Fatalf`. `AGENTS.md` names exactly this trap — a fixture built from the type under test encodes the compatibility it claims to prove. Cost is ~20 lines and one process spawn of a stack-free test. |\n| D3 | Does `onsaleGate` change too, when the ticket names only `onsaleFull`? | **yes** | Draft-recommended; same file, so inside the ticket's stated scope (`smoke/onsale_load_test.go`). Reversible — it is one call site. |\n| D4 | Add a `failed` field alongside `partial`? | **no** | No critique recommendation, so: most reversible = don't add schema. Adding a field later is additive; removing a published one is not. |\n\n`risk:low` **stands** after planning: two test files plus one struct field, all under `smoke/`. No\nauth, no money path, no migration, no CI/deploy config. The stop-condition for touching those was\nnot triggered.\n\n## 4. Introduced at plan-review, reviewed by no second model\n\nPer the skill: design elements *added* at 3b reach the gate with only their author's eyes on them.\nNamed again here and in the code review focus.\n\n1. **The `-test.run=^…$` filter on the child invocation, as a correctness requirement rather than a\n   convenience** — with the reason (`TestMain`'s coverage gate at `smoke/coverage_test.go:119-127`).\n   Mine; gpt-5.6-sol never saw `TestMain`.\n2. **The `t.Skip`-unless-sentinel guard on the child test**, so it is inert in the ordinary gate.\n   Mine.\n3. **The assertion that the aborting stage is absent from the written report** (not merely that\n   completed stages are present). Mine.\n\nAll three are test-only. The ai-review stage (`gpt-5.6-sol`) sees them as part of the diff, which is\nthe second pair of eyes they are otherwise missing — the review focus will name them explicitly.\n\n## 5. TDD order for `agent:coding`\n\n1. Write both tests first; run them; **observe red** — the abort-path test must fail against the\n   current terminal `writeReport`, and the JSON test must fail against a `Report` with no `Partial`.\n   Capture that red output; it is the evidence for AC 1 and AC 2.\n2. Add `Partial` to `loadtest.Report`; add the deferred lifecycle helper; route both profiles\n   through it; delete both terminal `writeReport` calls.\n3. Green, then the full `make check` (run as the sole command in its shell call, exit code read from\n   a file, per the skill's gate rules).\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implemented by **main-agent** (`config.models.implement`) — PR [#122](https://github.com/mathd/ticketing_system/pull/122), commit `84f40be`\n\n```yaml\nstage: coding\nimplementer: main-agent\nbranch: TKT-130-defer-onsale-report\nbase: origin/main @ 2fb5709\npr: 122\ncommit: 84f40be\ndiff: +41 / -2 (3 files, 1 new)\ngate: make check EXIT=0 (sentinel file), 0 failure signatures in the log body\n```\n\n### TDD — red observed first, and it was a *behavioural* red\n\nThe first red was only a compile error (`Report has no field Partial`, `undefined: beginReport`),\nwhich proves nothing about the defect. So I added the field plus a **deliberately wrong\n`beginReport` reproducing the pre-fix semantics** (write only at completion) and re-ran:\n\n```\n--- FAIL: TestOnsaleReportSurvivesAnAbortedStage (0.02s)\n    aborted run wrote no report: open …/report.json: no such file or directory\n    child output:\n        onsale_load_report_test.go:88: injected abort at sweep-600\n        --- FAIL: TestOnsaleReportSurvivesAnAbortedStage\n```\n\nThat is the TKT-125 failure mode reproduced in 0.02 s — the child aborted, exited non-zero, and left\nno JSON. Then the scaffold was replaced with the real `beginReport` and both tests went green. The\ncompletion-path test passed against the scaffold, correctly: the pre-fix code did write on a clean\nrun; only the abort path was broken.\n\n### What shipped\n\n| File | Change |\n|---|---|\n| `smoke/internal/loadtest/loadtest.go` | `Partial bool \\`json:\"partial\"\\`` on `Report`, no `omitempty`, with the reasoning in a comment |\n| `smoke/onsale_load_test.go` | `beginReport` helper; both profiles route through it; both terminal `writeReport` calls become `complete()` |\n| `smoke/internal/loadtest/loadtest_test.go` | pins that a complete report serializes `\"partial\": false` **explicitly** (guards against someone adding `omitempty`) |\n| `smoke/onsale_load_report_test.go` *(new)* | the two mechanism tests |\n\n### Deviations from `kind=plan-final`, and why\n\n1. **`t.Cleanup` instead of `defer`.** The plan said `defer writeReport`. A `defer` *inside* a helper\n   fires when the helper returns — immediately — so the returned-completion-func shape requires\n   `t.Cleanup`. Same guarantee for the same reason (`FailNow` is `runtime.Goexit`; cleanups run on\n   abort exactly as defers do), and it avoids reindenting ~120 lines of profile body into a closure,\n   which the plan's `reportRun(t, r, func(){…})` shape would have cost. Net effect on the diff:\n   4 changed lines at the call sites instead of a whole-function reindent.\n2. **The child guard is an env check with an early return, not `t.Skip`.** Plan-final item 2 of\n   §4 called for `t.Skip` unless the sentinel is set. One test function that dispatches on the env\n   var is simpler and removes the \"skipped test\" noise from every ordinary gate run. Same intent,\n   and it makes the parent's `-test.run=^`+`t.Name()`+`$` self-referential, so the filter can't drift\n   from the test name.\n\nEverything else is as planned. The three plan-review-only additions (§4 of `kind=plan-final`) all\nshipped and are in the diff the reviewer sees: the load-bearing `-test.run` filter, the child guard,\nand the assertion that the aborting stage is **absent** from the report.\n\n### `risk:low` after implementation\n\n**Stands.** 3 files, all under `smoke/`, test-harness only. No auth, no money path, no migration, no\nCI/deploy config — none of the run's stop conditions were triggered.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review by **gpt-5.6-sol** (`config.models.aiReview`) — two passes, both conclusive\n\nRun via the codex companion `adversarial-review`, primed for guilt, **diff only** — the plan and\nthe reasoning were deliberately withheld so the reviewer judged the code against reality rather\nthan against my intent.\n\n| Pass | Base | Verdict | Findings |\n|---|---|---|---|\n| 1 | `origin/main` | needs-attention | 1 high, 2 medium |\n| 2 | `84f40be` (the **fix diff** only) | needs-attention | 1 medium — none against the fixes themselves |\n\n---\n\n## Pass 1 — per-finding verdicts\n\nEach verified against the revision under review (`git grep` at the branch HEAD), not the base.\n\n**[high] The writer is armed after fallible setup, so an early abort leaves stale evidence**\n(`smoke/onsale_load_test.go:421-426`) — **ACCEPTED, fixed in PR** (`07d9f37`).\nVerified true: both profiles ran `inventoryAdminConn` / `statStatementsSetup` (and, in the gate,\n`publishedSlot`) *before* `NewReport`. All can `t.Fatalf`. Nothing was written, so a previous run's\nfile survived at the configured path and read as this run's output.\n\nNot a regression this diff introduced — pre-fix, an abort there wrote nothing either. But the diff\nmakes it *more* misleading, because it establishes the expectation that an aborted run leaves a\nreport. Stale evidence is worse than none. `NewReport` depends on nothing but `gitSHA`, so the fix\nis a pure reorder; `loadStart` still sits immediately before the first stage, so the timed window\nand the 30s deadline are untouched (checked explicitly, `:328`/`:370`).\n\n**[medium] A bool cannot preserve the claimed legacy \"unknown\" state**\n(`smoke/internal/loadtest/loadtest.go:329-335`) — **ACCEPTED as a documentation defect; the type\nstands.**\nVerified true, and the reviewer's citation is real: `docs/evidence/TKT-82/full-profile.json` exists\nand has no `partial` key, so it decodes to `Partial=false`, indistinguishable from a completed run.\n**My comment claimed three states from a two-state type — that was a false claim, and it is the\nfinding.** Fixed by correcting the claim: the raw-JSON distinction is real, the decoded one is not,\nand a typed reader that must not confuse them has to check key presence itself.\n\nThe type is **not** changed. `*bool`/enum/schema-version were considered and rejected at planning\n(`kind=plan-final` D1/D4, taking the plan draft's own recommendation): a tri-state costs every\nreader a nil-check to serve one test harness with one producer and, in practice, human readers of\nthe JSON. Rejecting the reviewer's *recommendation* while accepting its *finding* — the finding was\nthat the documentation lied, and it did.\n\n**[medium] The aborted-stage absence assertion is vacuous**\n(`smoke/onsale_load_report_test.go:55-88`) — **ACCEPTED, assertion deleted.**\nVerified true and it is the sharpest finding of the run. The child never constructs or appends a\nsweep stage, so the assertion passed *by construction* and would have kept passing with the\nproduction guard-and-append ordering inverted — the one thing it claimed to protect. This is\n`AGENTS.md`'s \"a fixture built from the type under test cannot fail\" in miniature, and it was\n**mine**: `kind=plan-final` §4 item 3 introduced it at plan-review, where no second model saw it.\nThe cross-model review caught it at the next stage, which is the point of the invariant.\n\nDeleted rather than repaired. The property is real but lives in the profile bodies, which need the\nstack; the reviewer's fix (extract guard-and-append into a shared helper) would change\n`generatorHealthy`, which the ticket puts explicitly out of scope. A comment now records where the\nproperty actually lives and that this test does not cover it.\n\n## Pass 2 — the fix diff, which no one had reviewed\n\nSecond pass **earned**: the pass-1 fixes were non-trivial by the skill's definition — a blocking\nfinding was fixed, and the fix reordered statements in two production profile functions.\nScoped to `84f40be..HEAD` so it judged the *fixes*, not the original feature.\n\nOn the three fixes it returned: the reorder is \"semantically safe\", the `Partial` comment is\n\"accurate\", and the deleted assertion \"was vacuous\" — i.e. **no finding against any fix**. Its one\nnew finding:\n\n**[medium] The armed-before-setup ordering has no regression test**\n(`smoke/onsale_load_report_test.go:80-91`) — **ACCEPTED as real, triaged INCIDENTAL → filed as\n[TKT-138](#), not fixed here.**\nLiterally true: the child calls `beginReport` itself, so the test would still pass if the hoist were\nreverted. Reasons for filing rather than fixing:\n1. The reviewer's own recommendation is to *\"refactor the profile setup behind an injectable\n   callback\"* — production restructuring of both profile bodies, outside this ticket's scope\n   (`defer writeReport` + a partial marker; the shaping decision put profile-orchestration changes\n   out).\n2. It would inject **fresh unreviewed production code at the churn cap**. Two passes on a stable\n   diff is the limit, and the convergence exception does not apply — pass 2 proved no earlier fix\n   wrong. Writing new production code and shipping it without a pass is the failure the cap exists\n   to prevent.\n3. Nothing shipped is wrong. The behaviour is correct and strictly better than `origin/main`; the\n   gap is a guard on an ordering property.\n\n**Stated plainly, because it is a real hole:** the armed-before-setup ordering is **unguarded** —\na future edit could revert it silently. Said again in the PR review-guide.\n\n## Churn-cap status\n\nTwo passes, stable diff, **converged**: pass 2 raised nothing against pass 1's fixes. Stopping here\nis the rule, not a shortcut. No unreviewed fix diff is being merged — the fix diff *is* what pass 2\nreviewed, and pass 2's own finding is deferred to TKT-138 rather than patched.\n\n## Reviewer-hijack note (recorded, not an excuse)\n\nPass 2's run was hijacked by an **unrelated client's** skill (`moov-skills` under\n`~/Sources/MoovAI/`) — the documented, unpreventable `codex-runner.md` §(f) failure mode; it read\nthat skill before returning to the diff. Judged on output, per that doc: it returned concrete,\nfile-and-line-cited findings against the correct scope and a conclusive verdict, so it is a **pass**.\nFlagged because a cross-client context leak into a review is worth someone's attention on its own —\ncarried into the retro.\n\n## Gate\n\n`make check` **EXIT=0**, zero failure signatures in the log body (both checked — TKT-101). The gate\nprofile `onsaleGate` runs for real in the smoke stage, so it exercises the hoisted `beginReport`.\n\nOne process note for the retro: the first re-run of the gate silently did nothing — the shell's cwd\nwas still `smoke/` from an earlier call, so `make check` died with `No rule to make target 'check'`\n**while the harness reported the background job as exit 0**. TKT-71's shape and TKT-101's divergence\nin one event. The exit-code sentinel file caught it (`EXIT=2`); the harness status would not have.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### Self-validation against the merged code — **Gate 4 waived**, agent-performed\n\nMerged as `64e1f06` on `main` (squash of PR #122). Every check below was run against the **merged\ncommit**, not the branch. No PO validated this; the run was authorised gateless by the repo owner\nand this note is the substitute record.\n\n### The evidence, produced on merged `main`\n\nAn aborted run, driven through the real code path:\n\n```\n$ ONSALE_REPORT=… TKT130_ABORT_CHILD=1 go test -tags smoke \\\n    -run '^TestOnsaleReportSurvivesAnAbortedStage$' .\nchild_exit=1                     ← the run really did abort\n\n$ cat $ONSALE_REPORT\npartial      : True\nstages kept  : ['nfr-3000pm']\n  finalize_p99_ms: 12.5   confirm_p99_ms: 7.25\n```\n\nBefore this ticket that file did not exist at all — which is precisely how TKT-125 lost these two\nnumbers.\n\n### COS\n\n| # | Condition | Verdict | Evidence |\n|---|---|---|---|\n| 1 | A run whose sweep aborts still writes `ONSALE_REPORT` with every completed stage | **MET** | Above: the run exits non-zero and the report exists with `nfr-3000pm` intact. Pinned by `TestOnsaleReportSurvivesAnAbortedStage`, which failed with `aborted run wrote no report` against pre-fix semantics |\n| 2 | The report distinguishes complete from partial runs | **MET** | `partial: true` above; `partial: false` on the completed path (`TestOnsaleReportMarksACompletedRunComplete`). Serialized explicitly — `loadtest_test.go` fails if anyone adds `omitempty`. **Bounded**: it distinguishes *reach*, not pass/fail, and cannot identify a pre-field legacy report once decoded — both stated in the field's comment |\n| 3 | TKT-125's missing `finalize_p99_ms` / `confirm_p99_ms` are obtainable by re-running the two arms | **MET as scoped** | Both fields survive the abort with their exact values (12.5 / 7.25). AC 3 was resolved at shaping to **mechanism-only**: the mechanism that lost them is fixed, so a re-run would now retain them. Actually re-running the two arms is a separate measurement run and was explicitly out of scope — `sweep-600` trips the generator guard every time on a single-host box, so binding this diff to a green full run would bind it to a host it cannot have |\n| 4 | `make check` green | **MET** | Green on the branch (`EXIT=0`) and re-run on merged `main` — result in the closing `kind=metrics` comment |\n\n### What a PO should know before accepting\n\n1. **The armed-before-setup ordering is unguarded.** The hoist that stops a dead setup from leaving\n   a previous run's file in place has no regression test — revert it and nothing fails. Filed as\n   **TKT-138** (Backlog, unshaped). Raised by the second review pass, triaged incidental because its\n   fix is a production refactor this ticket's scope excludes.\n2. **`partial: false` ≠ passed.** A run whose SLO assertion fails via `t.Errorf` still reaches the\n   end and still writes `partial: false`. It is complete evidence *of a failure*. No `failed` field\n   was added — a deliberate decision, not an oversight.\n3. **AC 3 is satisfied at the mechanism, not the measurement.** Nobody has re-measured TKT-125's\n   arms. If the goal was the numbers themselves, this ticket does not deliver them and a follow-up\n   measurement run on a host that can hold 600 attempts/s is needed.\n4. **Nothing here is user-visible.** Test-harness only, three files under `smoke/`. There is no\n   preview link and nothing for a browser to submit — the repo's browser-verification rule applies\n   to web-UI write paths and is not engaged.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-130, merged `64e1f06`, **run gateless by owner waiver**\n\nDurations from the `sdlc-state` changelog (`git log -- .sdlc/tickets/TKT-130.json`), not hand-tracked.\n\n| Stage | Duration |\n|---|---|\n| `Backlog` (created → Ready) | 12h 54m (shaped 2026-07-28; sat overnight) |\n| `Ready` (Ready → claim) | 39m 34s |\n| `Planning` (claim → Building) | **9m 20s** |\n| ├ `agent:planning` (draft, gpt-5.6-sol) | 8m 19s |\n| └ `agent:plan-review` + finalize (main-agent) | 1m 01s |\n| `Building` (Building → PO Review) | **20m 22s** |\n| ├ `agent:coding` (TDD, gate, PR) | 7m 29s |\n| ├ `agent:ai-review` (2 passes + fixes + re-gate) | 11m 24s |\n| └ `needs:human` → merge | 1m 28s |\n| `PO Review` (→ Done) | 5m (self-validation + post-merge gate) |\n| **Total, Ready → Done** | **≈ 1h 15m** |\n\n**human-wait total: 0m.** All four gates waived by the repo owner for this run; `needs:human` was\nheld for 1m 28s by the agent itself between posting the review-guide and merging. This is the\nheadline number of the experiment: the two gates that normally dominate (2 and 3) cost nothing here,\nand the whole Ready→Done path ran in a little over an hour of wall clock, of which ~28 min was\n`make check` (three full runs).\n\n**diff: +180 / −7 (4 files)** per `gh pr view`, across 2 commits. The functional change is far\nsmaller than that number suggests — `git diff --stat` on the first commit was **+41/−2 (3 files)**;\nthe rest is the second commit's comments and the review-driven reorder. New file:\n`smoke/onsale_load_report_test.go`.\n\n**models** (per `config.models`, no substitutions): `plan` = gpt-5.6-sol · `planReview` = main-agent\n· `implement` = main-agent · `aiReview` = gpt-5.6-sol. Both cross-model pairs distinct, checked at\nclaim time. Codex ladder never escalated past rung 1; all three delegated runs returned conclusive\nverdicts on first attempt.\n\n---\n\n### learnings:\n\n1. **A plan-review-only addition was the one thing the code review had to delete.** `kind=plan-final`\n   §4 named three design elements introduced at 3b with no second model on them. One — \"assert the\n   aborting stage is absent from the report\" — turned out **vacuous**: the fixture never appends a\n   sweep stage, so it passed by construction and would have kept passing with the production\n   ordering inverted. The skill's §4 requirement is what made it findable, and the cross-model code\n   review is what found it. *Target house: none — this is the existing rule working as designed.\n   Worth citing in a future retro as evidence the §4 section earns its keep, not a new rule.*\n\n2. **The gate's own failure modes fired again, both at once, and the sentinel is what caught it.**\n   One `make check` re-run silently did nothing: the shell's cwd was still `smoke/` from a previous\n   call, so make died with `No rule to make target 'check'` — **while the harness reported the\n   background job as exit 0**. That is TKT-71's shape and TKT-101's divergence in a single event.\n   The `EXIT=$?`-to-a-file sentinel reported `EXIT=2` and caught it; the harness status would have\n   waved it through. *Target house: repo-local — `AGENTS.md`/`docs/learnings/` already carry both\n   rules (TKT-71, TKT-101, TKT-106). Nothing new to promote; this is a **third** independent\n   recurrence, which is itself the signal that the mitigation (always `make -C <abs-path>`) should\n   be the default phrasing rather than advice.*\n\n3. **A cross-client skill leak into a code review.** The second review pass was hijacked by\n   `~/Sources/MoovAI/moov-skills/...` — a *different client's* skill — while reviewing this repo.\n   `codex-runner.md` §(f) documents hijacks as survivable and unpreventable, and the run did return\n   good findings, so it passed. But \"unpreventable\" was written about *rubric* contamination; a\n   review of client A's code loading client B's proprietary skill is a confidentiality-shaped\n   concern, not just a quality one. *Target house: process — worth a decision by the repo owner\n   about whether codex stages should run with a constrained skill path. Not filed as a ticket\n   because it is about the local machine's configuration, not this repo.*\n\n4. **Compile-red is not red.** The first TDD red was only `undefined: beginReport` — that proves the\n   test references a symbol, nothing more. Adding the field plus a scaffold reproducing the *pre-fix\n   semantics* produced the real red (`aborted run wrote no report`) in 0.02s, which is the evidence\n   AC 1 actually needs. *Target house: repo-local candidate — `AGENTS.md` has no line on this and\n   the distinction is cheap to state. Offered below as an appliable retro item.*\n\n---\n\n### retro:\n\n**(1) What would have made this ticket faster or better?**\n\n- **The shaping was excellent and it showed.** DoR 8/8 with a pre-resolved AC 3 (mechanism-only) and\n  an explicit OUT list meant zero ambiguity to resolve at planning; the two self-made decisions were\n  genuine design choices, not gap-filling. Concrete change: **none** — this ticket is the argument\n  for shaping this hard.\n- **The `context_memo` was `deferred` with \"no ADR governs this\", and that held up** — I re-resolved\n  against `docs/adr/` at claim time as the skill requires and confirmed it, in about 60 seconds.\n  Cheap, and it would have caught drift. Change: none.\n- **Real cost driver: three full `make check` runs, ~28 min of the ~75.** One was pure waste (the\n  cwd failure). Appliable change: **`docs/development.md` (or `AGENTS.md` gate section) should state\n  the gate invocation as `make -C <repo-root> check`, not `make check`** — the absolute-path form is\n  immune to the cwd trap that has now cost three tickets (TKT-71, TKT-87, this one).\n\n**(2) What should the humans change?**\n\n- **`AGENTS.md`, TDD section — add one line: \"a compile error is not a red test.\"** Concretely:\n  *\"When the test cannot compile against the pre-fix tree, add the minimal type/stub so it compiles\n  and reproduces the old behaviour, and observe it fail on the behaviour. A red that is only\n  `undefined: X` proves the test names a symbol, not that it detects the defect.\"* Two sentences,\n  changes agent behaviour in future sessions, and is not readable from the repo today.\n- **The skill's churn-cap section deserves a named case for \"pass 2 finds a gap whose fix is out of\n  scope.\"** This run hit it cleanly: pass 2 raised no defect in the fixes but wanted a production\n  refactor to guard them. Filing (TKT-138) rather than fixing was the right call, but the skill only\n  describes blocking/incidental/rejected in the abstract. Concrete edit: add to\n  `references/quality-practices.md` §2 a line that *a finding whose only fix is production\n  restructuring outside the ticket's stated scope is incidental by construction — file it, and say\n  in the review-guide that the property is unguarded.*\n- **Decide whether the codex stages should run with a constrained skill path** (learning 3). Owner\n  call, not a repo change.\n\n**Applied in this session:** none. V0 says offer, then apply on approval — and the gateless waiver\ncovered *this ticket's* gates, not authority to edit `AGENTS.md`, the skill, or `docs/` on the\nowner's behalf. All three items above are concrete enough to apply on a word.\n\n---\n\n### DoD verification\n\n- PR #122 **MERGED** (`64e1f06` on `main`), squash, remote branch **deleted** (0 refs matching `TKT-130*`).\n- `make check` on **merged main**: **EXIT=0**, 0 failure signatures in the log body.\n- Both new tests observed passing inside that post-merge gate run, not only locally.\n- Follow-up **TKT-138** filed in Backlog for the one accepted-but-unfixed finding.\n- No AI attribution in either commit message or the PR.\n"
    }
  ]
}
