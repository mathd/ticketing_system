{
  "key": "TKT-176",
  "type": "Task",
  "classification": "complex",
  "summary": "Channel allocations apply to GA pools but a seated claim ignores them entirely (TKT-172 ai-review)",
  "status": "Backlog",
  "labels": [],
  "assignee": null,
  "pr": null,
  "links": [],
  "description": "**Surfaced by TKT-172's second ai-review pass.** Not a defect that ticket introduced — a pre-existing inconsistency it had to navigate around, and whose resolution changes what a seat picker should display.\n\n**The facts, verified at `2a11e787`:**\n\n- `ReplaceChannelAllocations` (`services/inventory/internal/store/channel_allocations.go`) does **not** check `inventory_kind`. It reads `COALESCE(target_capacity, capacity) FROM inventory_pools ... FOR UPDATE` and writes the allocation. A **seated** pool can therefore carry channel allocations today.\n- The public availability read subtracts unsold channel reservations from `available` (`reservedForChannelsSQL`, `store.go`).\n- `CreateSeatHold` (`services/inventory/internal/store/seat_claims.go`) admits against `target_capacity`/`capacity` **alone**. It never consults `channel_allocations`, and `SeatHoldCreate` has no `channel` field at all — unlike `HoldCreate`, and unlike `getAvailability`, which takes `channel` as a query parameter.\n\nSo on a seated pool with an active allocation, the availability read says one thing and the seat claim does another. TKT-172 chose to have `remaining_capacity` mirror the **claim path**, because a read whose purpose is \"will a claim succeed\" must quote the rule that decides, and a test (`TestSeatOccupancyCapacityMirrorsTheSeatedAdmissionRule`) pins that choice. That is the right local decision and it does not settle the product question.\n\n**The product question, which is genuinely open:** should reserved seating support channel allocations at all?\n\n- **If yes** — a reseller allocation on a seated event is a normal thing to want (an agency gets 200 seats of a 2000-seat house) — then a quantity cap is probably the wrong shape for it. On a seated map an allocation almost certainly wants to name *which* seats, not how many, which is closer to an operational hold (ADR-023) over a seat set than to ADR-024's counters. Then `createSeatHold` needs a channel, and TKT-172's read needs to become channel-aware.\n- **If no** — then `ReplaceChannelAllocations` should refuse a seated pool with `ErrPoolKindMismatch`, exactly as `placeOperationalHold`, `placeGroupReservation` and `createHold` already do (`operational.go:128`, `reservations.go:57`, `store.go:208`). That refusal is the cheap, reversible option and makes the current divergence unrepresentable rather than merely unobserved.\n\n**Why it matters beyond tidiness.** Whichever way it goes, it changes what a buyer-facing seat picker (TKT-174) may show. If allocations stay and stay quantity-shaped, the picker can offer a seat that the claim grants but that the organizer believed was reserved for a reseller — an oversell of the allocation, invisible to the counters.\n\n**Suggested first step:** decide the question, then either add the refusal (small, and mostly a test) or write the ADR for seat-set-shaped allocations (not small). Do not \"fix\" it by making `CreateSeatHold` subtract the reservation — that would silently give allocations a meaning on seated pools that nobody designed.\n\nBlocks nothing today. Should be decided before TKT-174 ships a picker, and certainly before any seated event is sold through a reseller channel.\n\n---\n\n**AMENDED 2026-08-17 (TKT-248).** Everything above still holds at the STORE tier — re-verified on\n`origin/main`, not assumed: `ReplaceChannelAllocations` still does not check `inventory_kind`, so a\nseated pool can still carry allocations; `CreateSeatHold` still admits against capacity alone; and\n`SeatHoldCreate` still has no `channel` field.\n\n**What changed is the way in, and it is now shut at the contract.** A seated claim cannot carry a\nchannel from any route:\n\n- the **public** reserve no longer accepts `channel_code` at all — the field was removed from\n  `ReservationCreate` (TKT-248, ADR-060), because catalog resolves both fee *and* price rules on it\n  and an unauthenticated caller could therefore choose their own price basis;\n- the **partner** reserve is GA-only — `PartnerReservationCreate` declares no `seat_identities`, and\n  the contract says why: *\"seated pools do not consult channel allocations at all (TKT-176 owns that\n  seam), so a seated partner sale would claim an authorization this contract cannot deliver.\"*\n\n`services/commerce/internal/api/channel_seam_test.go`'s `TestASeatedSaleCannotCarryAChannelAtAll`\npins both, including which schema each operation references.\n\n**So this ticket is no longer \"close a hole\" — it is \"decide whether to open one.\"** The practical\nconsequences:\n\n1. **The oversell risk named above is not currently reachable through the API.** A seat picker\n   cannot offer a seat against a channel a caller named, because no caller can name one. The\n   divergence between the availability read and the claim path is still real, but it is now only\n   reachable by an operator writing allocations onto a seated pool directly — which nothing stops.\n2. **The urgency changed, not the question.** \"Before TKT-174 ships a picker\" no longer applies —\n   TKT-174 shipped, and the contract is what keeps it safe. The remaining trigger is unchanged and\n   still real: **before any seated event is sold through a reseller channel.**\n3. **The cheap option got cheaper and the expensive one got more expensive.** The \"if no\" branch —\n   refuse a seated pool in `ReplaceChannelAllocations` with `ErrPoolKindMismatch` — now merely\n   makes unrepresentable a state the API already cannot exercise, so it is close to pure tidying\n   with a test. The \"if yes\" branch now needs *two* contract changes as well as the ADR: a seated\n   partner request shape, and a channel on the seat-hold path.\n\n**Unchanged and still the point:** do not \"fix\" this by making `CreateSeatHold` subtract the channel\nreservation. That would give allocations a meaning on seated pools that nobody designed, and it\nwould do so without either contract admitting it.\n",
  "context": {
    "capturedAt": "2026-08-02",
    "binding": [],
    "reference": [
      {
        "summary": "Where the divergence was found and why TKT-172 chose the claim path's rule",
        "link": "https://github.com/mathd/ticketing_system/pull/146"
      },
      {
        "summary": "TKT-248/ADR-060 shut both routes into this seam: the public reserve lost channel_code entirely and the partner reserve is GA-only. TestASeatedSaleCannotCarryAChannelAtAll pins it, including which schema each operation references.",
        "link": "docs/adr/ADR-060-public-reservation-channel-entitlement.md"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-024",
        "why": "Defines channel allocations as quantity caps with scheduled release. It is the decision this ticket asks to either extend to seated pools or explicitly exclude them from — and it was written before seat-level claims existed."
      },
      {
        "id": "ADR-023",
        "why": "Operational holds are the existing shape for 'these specific units are not for public sale'. If a seated allocation should name seats rather than a count, this is the pattern it resembles, not ADR-024's."
      },
      {
        "id": "ADR-010",
        "why": "Whatever is decided, the check belongs inside the claim transaction that already holds the pool lock — not as an advisory read beside it."
      }
    ]
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\nFiled from TKT-172's ai-review triage as an **incidental** — a real finding outside that ticket's scope, recorded rather than folded into its PR.\n\nNot shaped yet: the 8-item DoR is unfilled because the first item is a product decision (`owner: human`) that shaping cannot resolve on its own. Needs a decision on whether reserved seating supports channel allocations before it can be sized."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Description amended — TKT-248 shut the way in (2026-08-17)\n\nNot a re-shaping; the readiness verdict below is untouched and this ticket stays in Backlog. One\namendment appended to the description, because the ticket would otherwise read as describing a\nreachable hole when the route into it is now closed at the contract.\n\n**Re-verified on `origin/main` rather than assumed** — all three original facts still hold:\n`ReplaceChannelAllocations` still ignores `inventory_kind`, `CreateSeatHold` still admits against\ncapacity alone, and `SeatHoldCreate` still has no `channel` field.\n\n**What changed:** a seated claim can no longer carry a channel from any route. The public reserve\nlost `channel_code` (TKT-248/ADR-060) and the partner reserve is GA-only.\n\n**Why that matters to whoever picks this up:** the framing flips from *close a hole* to *decide\nwhether to open one*. The oversell risk the description warns about is not currently reachable\nthrough the API — only by an operator writing allocations onto a seated pool directly. The \"before\nTKT-174 ships a picker\" deadline has passed and is moot; the real trigger is unchanged: **before any\nseated event is sold through a reseller channel.**\n\nThe product question itself is **unchanged and still open**, and so is the warning not to make\n`CreateSeatHold` subtract the reservation.\n"
    }
  ]
}
