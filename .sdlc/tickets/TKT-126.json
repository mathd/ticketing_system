{
  "key": "TKT-126",
  "type": "Task",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "url": "https://github.com/mathd/ticketing_system/pull/116",
    "number": 116,
    "state": "merged"
  },
  "classification": "complex",
  "summary": "One domain-event envelope in shared/go — the type and ADR-017's decode rules are re-implemented at 6+ sites",
  "description": "### Finding (architecture review R3, 2026-07-25)\n\nThe platform envelope is declared independently at:\n\n| Site | Form |\n|---|---|\n| `services/catalog/internal/events/events.go:32` | full `Envelope` struct |\n| `services/commerce/internal/events/events.go:17` | full `Envelope` struct |\n| `services/access/internal/consumer/consumer.go` | 3 partial decode structs (`:50`, `:159`, `:174`) |\n| `services/inventory/internal/consumer/consumer.go` | 2 partial decode structs (`:66`, `:178`) |\n| `services/access/internal/store/{scan,lifecycle,reconcile}.go` | inline anonymous structs |\n\nADR-017's two load-bearing rules — **dispatch on `schema` before decoding `data`**, and **`schema <= 0` is a broken envelope, not the future** — are therefore re-derived at each decode site. AGENTS.md already records the consequence: TKT-61 shipped the ordering bug **twice**, past a mutation-checked test suite and a full review pass. Duplication is the mechanism.\n\nThis is the one place where the review recommends *adding* to `shared/go` despite the thin-kernel rule. The rule is right; this and TKT-127 are what earned their way in.\n\n### The trap this ticket must not fall into\n\nADR-017 names it: **a fixture built from the type under test cannot fail** — it encodes the compatibility it claims to prove. The golden bytes proving the wire is unchanged must be captured from the **current** emitters *before* the refactor and pinned as literals. Regenerating them from the new shared type proves nothing.\n\n### Acceptance Criteria\n\n- [ ] One `Envelope` declaration remains repo-wide; per-subject payload types stay in their owning service.\n- [ ] `DecodeEnvelope` enforces `schema`-first dispatch and rejects `schema <= 0` as a broken envelope (terminate; must not touch readiness). Both have tests.\n- [ ] Golden literals captured from the pre-refactor emitters prove every published subject is byte-for-byte identical afterwards.\n- [ ] An ADR records the `shared/go` addition — the working agreement requires one, and it lands in this ticket, not a follow-up.\n- [ ] `make check` green with the existing event/consumer suites unmodified.",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "The domain-event envelope and ADR-017's decode rules are re-implemented at 6+ independent sites. TKT-61 shipped the same ordering bug twice past a mutation-checked suite and a full review pass; duplicated rules are why. Collapse them to one implementation."
    },
    "scope": {
      "state": "met",
      "note": "IN: an Envelope type and a DecodeEnvelope helper in shared/go that enforce ADR-017 §5b′ (dispatch on `schema` before decoding `data`) and the bottom end (`schema <= 0` is a broken envelope, terminate — do not let it touch readiness); replacing the local declarations in catalog/events, commerce/events, access/consumer (x3), inventory/consumer (x2) and the inline structs in access/store/{scan,lifecycle,reconcile}.go; the ADR recording the shared/go addition. OUT: the per-subject payload types (they stay in their owning service); the consumer run-loop skeleton (TKT-127); any wire change."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Whether the deterministic-id / dedup-key helpers move with the envelope or stay per-service. Catalog derives ids five different ways (live, backfill, closure, archive, seat-map) with domain-specific keys; the shared piece may be only the SHA1-over-key primitive. A plan-time call, no impact on the COS."
    },
    "dependencies": {
      "state": "met",
      "note": "None. Blocks TKT-127 (the consumer skeleton, which needs this type to exist first) — linked."
    },
    "approach": {
      "state": "met",
      "note": "The working agreement requires an ADR for any shared/go addition; write it as part of this ticket, not after. The critical safety property is that the wire is byte-identical afterwards — prove it with golden literals over real emitted bytes. ADR-017's own trap applies directly here: a fixture built from the type under test cannot fail. The golden bytes must be captured from the CURRENT emitters before the refactor and pinned as literals, not regenerated from the new shared type."
    },
    "first_slice": {
      "state": "met",
      "note": "The shared type + DecodeEnvelope + the ADR + migrating the two publisher sites (catalog, commerce) with byte-identical golden proof. The consumer-side decode sites can follow in the same PR if the diff stays inside the ~400-line budget; if not, split the consumer migration out."
    },
    "success": {
      "state": "met",
      "note": "COS: (1) one Envelope declaration remains in the repo — a grep for `json:\"schema\"` finds the shared type and per-subject payloads, not competing envelopes; (2) DecodeEnvelope refuses `schema <= 0` as a broken envelope and dispatches on `schema` before decoding `data`, with tests for both; (3) golden literals captured from the pre-refactor emitters prove the wire is byte-for-byte unchanged, and those literals are NOT generated from the new type; (4) an ADR records the shared/go addition per the working agreement; (5) make check green, including the existing event and consumer suites unmodified."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked: ADR-017 (the rules being centralized — §5b′ dispatch order, the `schema <= 0` bottom end, the fixture-built-from-the-type-under-test trap, and the TKT-61 history of shipping the ordering bug twice), ADR-009 (the envelope shape is contract, id/type/occurred_at/schema/data), ADR-002 (shared/go additions require an ADR — that requirement is why the ADR is in scope here, not a follow-up)."
    }
  },
  "context": {
    "binding": [
      {
        "id": "ADR-017",
        "date": "2026-07-12",
        "rule": "Domain event schema evolution: bump on consumer semantics, not parse compatibility. A consumer must dispatch on `schema` before decoding `data`. The poison/skew line has a bottom end (`schema <= 0` is a broken envelope, not the future). A fixture built from the type under test cannot fail."
      },
      {
        "id": "ADR-009",
        "date": "2026-07-12",
        "rule": "APIs are contract-first: the OpenAPI spec is the source of truth, served by the service at /openapi.yaml and checked for drift in the gate; domain events use a versioned platform envelope (id, type==subject, occurred_at, schema, data) published to JetStream with acks (never fire-and-forget), with a deterministic envelope id so retried/raced emissions de-duplicate."
      },
      {
        "id": "ADR-002",
        "date": "2026-07-12",
        "rule": "We build five Go services (catalog, inventory, commerce, payments, access) behind an API gateway, with an event bus for the domain event stream; every entity carries a tenant/organizer id from day one. Docker Compose only. The gateway is the only public surface, with explicit route registration."
      }
    ],
    "reference": [
      {
        "summary": "Architecture review R3 — the finding with every call site enumerated",
        "link": "docs/reviews/2026-07-25-architecture.md"
      },
      {
        "summary": "ADR-017 — the rules being centralized, and the traps",
        "link": "docs/adr/ADR-017-domain-event-schema-evolution.md"
      },
      {
        "summary": "The largest publisher site, with five id-derivation variants",
        "link": "services/catalog/internal/events/events.go"
      }
    ],
    "capturedAt": "2026-07-25"
  },
  "links": [
    {
      "type": "blocks",
      "key": "TKT-127"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\nBorn from the 2026-07-25 architecture review, recommendation **R3** ([docs/reviews/2026-07-25-architecture.md](docs/reviews/2026-07-25-architecture.md)). Blocks TKT-127 (the consumer skeleton). The ADR is IN scope — the working agreement gates shared/go additions on one. Code pointers are enumerated in the description table."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ DoR shaped — TKT-126 — all 8 items met/deferred (Gate 1 unblocked)\n\nShaped from the architecture review's code-seam survey; the call sites were enumerated by grep and the two publisher packages read in full. **No `open owner:human` items remain.**\n\n| DoR item | State |\n|---|---|\n| `objective` | met |\n| `scope` | met |\n| `uncertainties` | deferred |\n| `dependencies` | met |\n| `approach` | met |\n| `first_slice` | met |\n| `success` | met |\n| `context_memo` | met |\n\n**Deferred (safely resolved in Planning, no major redesign):**\n- `uncertainties` — Whether the deterministic-id / dedup-key helpers move with the envelope or stay per-service. Catalog derives ids five different ways (live, backfill, closure, archive, seat-map) with domain-specific keys; the shared piece may be only the SHA1-over-key primitive. A plan-time call, no impact on the COS.\n\n**Gate 1 granted by the owner on 2026-07-25** — the architecture review ([docs/reviews/2026-07-25-architecture.md](docs/reviews/2026-07-25-architecture.md)) was the prioritization pass; owner instruction was \"create new tickets and move them directly to Ready\".\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### 🤖 Claimed — TKT-126 — **gateless run (all four human gates waived)**\n\n**Backend:** local tracker (`config.tracker == \"local\"`), state on `sdlc-state`.\n**Branch:** `TKT-126-shared-event-envelope`, cut from `origin/main` @ `59c0295` (TKT-125).\n**Blockers:** none. The only link is outbound — TKT-126 *blocks* TKT-127 (consumer skeleton), which is correct ordering: TKT-127 needs this type to exist.\n\n#### ⚠️ Gate waiver — recorded for the board history\n\nThe repo owner is deliberately running this ticket **Ready → Done in one continuous autonomous\npass, with the four human gates waived**, as a **process experiment**: how far the pipeline's own\nadversarial machinery (cross-model plan critique, cross-model code review, TDD, the local gate)\ncarries a *complex*-classified ticket when no human is in the loop. This is the waiver record the\nowner asked for.\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human `Backlog → Ready` | **already granted** 2026-07-25 (architecture-review prioritization pass) |\n| 2 — plan approval | human `Planning → Building` | **waived** — the agent resolves open decisions itself: prefer the option the cross-model critique recommends; absent a recommendation, take the **most reversible** option. Every self-made decision + rationale is recorded in `kind=plan-final`. |\n| 3 — review + merge | human reviews PR, squash-merges | **waived** — the agent squash-merges and deletes the branch *after* the adversarial review converges under the normal triage / second-pass / churn-cap rules. The `kind=review-guide` still goes on the PR **first**, so the audit record exists before the merge. |\n| 4 — PO acceptance | PO validates COS | **waived** — the agent self-validates each COS against test evidence **on the merged code**, posts the validation note, then closes with the full `kind=metrics` comment. |\n\n**Everything else in the skill is unchanged and non-negotiable for this run:** `config.models`\nrouting with no substitutions (`plan` = `gpt-5.6-sol` via codex, `planReview` = `main-agent`,\n`implement` = `main-agent`, `aiReview` = `gpt-5.6-sol` via codex — both cross-model pairs hold),\nTDD with tests observed red first, `make check` green before every push *and* after the merge, a\nmarker comment at every stage, and no AI attribution anywhere in commits or the PR.\n\n**Stop-and-report conditions the owner set** (the run halts and hands back rather than improvising):\ngate/CI fails twice on the same cause with no clear fix; the work would touch auth, money paths,\ndata migrations or CI/deploy config in a way the ticket and plan did not anticipate; a blocking\nreview finding survives the churn cap; a configured model is unreachable after the documented\nescalation ladder; or a destructive/hard-to-reverse action where the right choice is ambiguous.\n\n#### Selection reason\n\nTop of the Ready queue and the **structural prerequisite for TKT-127** — the consumer skeleton\ncannot be lifted while six sites each declare their own envelope. The ticket also carries the\nsharpest evidence of cost in the repo: **TKT-61 shipped the ADR-017 dispatch-ordering bug twice**,\npast a mutation-checked suite and a full review pass, because the rule is re-derived at every\ndecode site. Collapsing the declaration is what makes that bug *unshippable* rather than\n*re-caught*.\n\n`risk:low` is **not** claimed and must not be — this is `classification: complex`, touches five\nservices' event paths, and adds to `shared/go` (ADR-002 gates that on an ADR, which is in scope\nhere).\n\n**Deferred DoR item carried into Planning:** `uncertainties` — whether the deterministic-id /\ndedup-key helpers move with the envelope or stay per-service (catalog derives ids five ways). A\nplan-time call with no COS impact; it is one of the decisions Gate 2's waiver hands to the agent.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### 📐 Plan draft — TKT-126\n\n**Drafted by `gpt-5.6-sol`** (config.models.plan) via the codex companion `task` command, read-only (`--effort high`, no `--write`). Briefed from the ticket, the context-mémo, the governing ADRs re-resolved at claim time, and eight verified code facts. Reproduced verbatim below; the critique and the amendments follow in `kind=plan-final`.\n\n---\n\n# TKT-126 execution plan — one domain-event envelope in `shared/go`\n\n## 1. Decision\n\nAdd package `domainevent` at `shared/go/domainevent`, containing one generic envelope declaration whose field order remains exactly `id`, `type`, `occurred_at`, `schema`, `data`:\n\n```go\ntype Envelope[T any] struct {\n\tID         uuid.UUID `json:\"id\"`\n\tType       string    `json:\"type\"`\n\tOccurredAt time.Time `json:\"occurred_at\"`\n\tSchema     int       `json:\"schema\"`\n\tData       T         `json:\"data\"`\n}\n\nvar ErrInvalidSchema error\n\nfunc DecodeEnvelope(data []byte) (Envelope[json.RawMessage], error)\n```\n\nThe generic provides typed `Data` to emitters and `json.RawMessage` to decoders without a second envelope declaration; `DecodeEnvelope` rejects omitted, zero, and negative schemas with `ErrInvalidSchema` while never decoding payload semantics. Use `uuid.UUID` for `ID`: every current ID is a UUID, this preserves commerce, access, inventory, alarm, and existing-test types, and only catalog’s string-returning helpers need trusted `uuid.MustParse` adaptation (`services/catalog/internal/events/events.go:135-143,190-211,228-239,323-329`). Replace second-pass structs with service-owned payload types plus aliases such as `type completed = domainevent.Envelope[completedData]`; do not embed or retain duplicated envelope fields (`services/access/internal/consumer/consumer.go:171-184`, `services/inventory/internal/consumer/consumer.go:64-75`). `FailureEvent` also migrates as an exported alias because it is an emitter envelope, not a consumer payload (`services/access/internal/consumer/consumer.go:46-60`).\n\n## 2. Definition of Done\n\n- [ ] Only `domainevent.Envelope[T]` declares the production wire fields; catalog, commerce, both consumers, the access policy projector, failure publisher, and three alarm writers use it directly or through aliases. A repo-wide inspection of `rg -n 'json:\"schema\"' --glob '*.go' services shared/go` finds no competing production envelope struct. **(COS-1)**\n- [ ] Per-subject payloads remain in their owning services: catalog payloads at `services/catalog/internal/events/events.go:40-122`, commerce at `services/commerce/internal/events/events.go:25-33`, consumer payloads beside their handlers, and alarm payloads at `scan.go:305-316`, `lifecycle.go:350-360`, and `reconcile.go:295-304`. **(COS-1)**\n- [ ] `DecodeEnvelope` returns `Envelope[json.RawMessage]`, rejects omitted/zero/negative schema through a recognizable sentinel, and never interprets `data`. **(COS-2)**\n- [ ] Hand-written future-schema fixtures incompatible with today’s payloads still park/quarantine, while `schema <= 0` terminates without changing readiness in access, the access policy projector, and inventory. **(COS-2)**\n- [ ] Literal byte comparisons cover every emitted subject and relevant schema fork, with expected bytes committed while all old local envelope structs still exist. **(COS-3)**\n- [ ] `docs/adr/ADR-033-shared-domain-event-envelope.md` records why this platform contract belongs in the deliberately thin shared kernel. ADR-033 is free: ADR-032 is the current highest entry. **(COS-4)**\n- [ ] Every pre-existing event and consumer test remains textually unmodified and green; new tests live in separate files. **(COS-5)**\n- [ ] `make check` passes from a committed tree, and no `openapi_gen.go` or `api-types.gen.ts` file changes because this ticket changes neither OpenAPI nor generation inputs. **(COS-5)**\n\n## 3. Files touched\n\n| Path | What changes | Rough lines |\n|---|---|---:|\n| `shared/go/domainevent/envelope.go` | Generic envelope, `ErrInvalidSchema`, raw decode helper | +35 |\n| `shared/go/domainevent/envelope_test.go` | Raw-data/schema-first and non-positive-schema tests | +45 |\n| `shared/go/go.mod` | Promote `github.com/google/uuid` from indirect to direct | ~2 |\n| `docs/adr/ADR-033-shared-domain-event-envelope.md` | Thin-kernel exception, generic design, UUID decision, decode contract and consequences | +60–70 |\n| `services/catalog/internal/events/events.go:30-38,146-166,280-320,341-400` | Remove local envelope; use shared generic; add/expose deterministic closure byte seam | ~25 |\n| `services/catalog/internal/events/wire_compat_test.go` | Literal goldens for all catalog subjects/schema forks | +40–45 |\n| `services/commerce/internal/events/events.go:17-23,52-63` | Remove local envelope; extract fixed-time marshal seam; use shared generic | ~15 |\n| `services/commerce/internal/events/events_test.go` | First commerce event test: literal order-completed wire golden | +20 |\n| `services/access/internal/consumer/consumer.go:46-52,149-184,277-302,341-381` | Alias failure/completed envelopes, use shared decoder, preserve disposition mapping | ~30 |\n| `services/access/internal/consumer/wire_compat_test.go` | Literal failure-event golden through the real marshal seam | +15–20 |\n| `services/access/internal/consumer/policy.go:64-113` | Service-owned payload + shared typed alias; shared raw decode | ~10 |\n| `services/inventory/internal/consumer/consumer.go:64-75,171-280` | Service-owned publication payload + shared alias; shared raw decode | ~25 |\n| `services/access/internal/store/event_envelope.go` | One package-local generic marshal seam backed by the shared envelope | +15 |\n| `services/access/internal/store/scan.go:364-384` | Replace policy-conflict anonymous wrapper only | ~8 |\n| `services/access/internal/store/lifecycle.go:365-386` | Replace lifecycle-integrity anonymous wrapper only | ~8 |\n| `services/access/internal/store/reconcile.go:306-329` | Replace admission-conflict anonymous wrapper only | ~8 |\n| `services/access/internal/store/event_envelope_test.go` | Literal goldens for all three alarm subjects | +35–40 |\n\nExpected review diff: approximately 370–395 changed lines after local envelope deletions. No service `go.mod`, `go.work`, generated API file, migration, or existing test file should change.\n\nThe service modules already import `ticketing/shared/*` without `require` or `replace` entries, for example access at `services/access/cmd/access/main.go:29-31` and inventory at `services/inventory/cmd/inventory/main.go:26-28`. `go.work:3-12` supplies all eight workspace modules, and the gate runs from directories beneath that workspace (`Makefile:44-71`), so adding another shared package does not require service-module metadata changes.\n\n## 4. Test plan\n\n### Pre-refactor wire capture\n\nThe expected literals must be committed in the first implementation commit, while the local envelope declarations at catalog `events.go:32-38`, commerce `events.go:17-23`, access consumer `consumer.go:46-52`, and the three anonymous alarm structs still exist.\n\nAt exact base `59c0295f22366bfbc1334d673cf79748b778cfc6`:\n\n1. Verify the base with `git rev-parse HEAD`.\n2. Extract only deterministic byte seams around the existing local structs where one is missing. This is not the shared-envelope refactor: the old declarations remain the code performing the marshal.\n3. Add temporary in-package capture tests that print `%q` of the real seam’s returned bytes.\n4. Run, per module:\n\n   ```text\n   (cd services/catalog && go test ./internal/events -run Capture -count=1 -v)\n   (cd services/commerce && go test ./internal/events -run Capture -count=1 -v)\n   (cd services/access && go test ./internal/consumer ./internal/store -run Capture -count=1 -v)\n   ```\n\n5. Paste those outputs as raw string literals, delete the printing/updating path, and make each test compare `string(got) == want`. Do not retain an `UPDATE_GOLDEN` mode or any generator.\n6. Commit and run the literal tests green before `shared/go/domainevent` exists.\n\nA reviewer can therefore inspect the first commit independently: `git show <wire-baseline-commit>` demonstrates that the literals pass against the old declarations, and the later shared-envelope commit only consumes already-pinned expectations. The expected side is never marshalled, normalized, or generated from `domainevent.Envelope`.\n\nCoverage matrix:\n\n- `platform.catalog.performance.published`: schema 2, grouped schema 3, seated schema 4, plus the distinct backfill-ID path.\n- `platform.catalog.performance.archived`: schemas 2 and 3.\n- `platform.catalog.performance.closed` and `.reopened`: both subjects through the closure seam.\n- `platform.catalog.seat_map.published`.\n- `platform.commerce.order.completed`.\n- `platform.access.ticket-issuance.failed`.\n- `platform.access.admission-policy-conflict.alarm`.\n- `platform.access.lifecycle-integrity.alarm`.\n- `platform.access.admission-conflict.alarm`.\n\n### Verified seams\n\nCatalog already has real pure-byte seams: `seatMapPublishedEnvelope` (`events.go:146-167`), `performancePublishedEnvelope`/`performancePublishedEnvelopeWithID` (`:272-320`), and `performanceArchivedEnvelope` (`:383-400`). Closure marshalling remains inline in `publishClosure` (`:341-363`), so the baseline commit extracts a fixed-time `closureEnvelope` used by that publisher.\n\nCommerce has no test file and marshals inline with `time.Now().UTC()` (`services/commerce/internal/events/events.go:56-62`); extract `orderCompletedEnvelope(data, occurredAt)` before capture and have `OrderCompleted` call it.\n\nAccess’s failure publisher currently marshals a supplied `FailureEvent` at `services/access/internal/consumer/consumer.go:277-285`, while `failureRecord` supplies `time.Now()` at `:288-302`; extract a body helper and test it with a constructed event carrying a fixed time. The three alarm paths currently marshal inline at `scan.go:366-379`, `lifecycle.go:368-380`, and `reconcile.go:311-323`; route them through the package-local marshal seam while their existing payload types remain unchanged.\n\n### Handling `occurred_at`\n\nEvery golden input uses a fixed UTC `time.Date`, including a non-zero nanosecond component to pin RFC3339Nano formatting. Production paths that currently call `time.Now()` continue to compute it once and pass it to the pure byte seam; access store paths already centralize clock access through `Postgres.now()` (`services/access/internal/store/lifecycle.go:115-121`).\n\nThe comparison remains over the complete byte slice, including `occurred_at`. Tests must not remove the field, compare decoded objects, or unmarshal/re-marshal either side; those approaches would fail to detect key-order and timestamp-format changes.\n\n### `DecodeEnvelope` and disposition tests\n\nNew shared tests:\n\n- `TestDecodeEnvelopeLeavesDataRawUntilSchemaDispatch`: pass a hand-written future envelope whose `data` uses renamed keys, changed types, and a non-object form; assert decoding succeeds, reports the future schema, and returns the exact raw `data` bytes.\n- `TestDecodeEnvelopeRejectsNonPositiveSchema`: table cases for omitted, zero, and negative schemas; assert `errors.Is(err, ErrInvalidSchema)`.\n- A malformed outer JSON case remains distinguishable from `ErrInvalidSchema`.\n\nThe real schema-first proof remains at the handler seams, which already drive fake `jetstream.Msg` instances:\n\n- Inventory `TestUnknownSchemaVersionSkewIsQuarantinedAndAcked` uses hand-written incompatible payloads (`services/inventory/internal/consumer/consumer_test.go:206-256`).\n- Access `TestUnknownSchemaVersionSkewIsParkedAndLatchesUnready` does the same (`services/access/internal/consumer/consumer_test.go:67-94`).\n- The access policy projector has an incompatible future fixture at `services/access/internal/consumer/policy_test.go:112-131`.\n\nReadiness on poison is already pinned by:\n\n- Inventory `TestEnvelopeWithoutUsableSchemaIsTerminatedAndStaysReady` (`consumer_test.go:332-360`).\n- Access `TestBrokenEnvelopeTerminatesAndStaysReady` (`consumer_test.go:110-135`).\n- Policy projection `TestPolicyConsumerPoisonTerminatesWithoutTouchingReadiness` (`policy_test.go:158-188`).\n\nThese tests must pass unchanged after `DecodeEnvelope` is introduced. In access, `ErrInvalidSchema` must continue down `ReasonInvalidContract`, not the malformed-JSON branch; otherwise the existing suite catches a disposition regression.\n\n### Existing suites that remain unmodified\n\nDo not edit any pre-existing `*_test.go` under:\n\n- `services/catalog/internal/events/`, especially the 11 envelope tests in `events_test.go:14-507`.\n- `services/access/internal/consumer/`, including `consumer_test.go`, `policy_test.go`, and `run_test.go`.\n- `services/inventory/internal/consumer/`, especially `consumer_test.go` and `offering_test.go`.\n\nThe new literal tests go into separate files so review can distinguish preserved behavioral coverage from TKT-126’s new compatibility evidence.\n\n## 5. Sequencing\n\n1. **Commit 1 — `test(events): pin pre-TKT-126 wire bytes`.** Starting at `59c0295`, mechanically extract the missing fixed-time marshal seams while retaining every old envelope declaration. Run the temporary capture commands, paste the outputs as literals, remove the capture path, and commit only once every literal comparison is green. This is the mandatory pre-refactor capture point.\n2. **Commit 2 — `feat(shared): define the platform domain-event envelope`.** Write the `domainevent` tests first; they are red because the package/helper does not exist. Add `Envelope[T]`, `ErrInvalidSchema`, and `DecodeEnvelope`, promote the UUID dependency, make the tests green, and land ADR-033 in the same commit.\n3. **Commit 3 — `refactor(events): migrate all envelope emitters`.** Replace catalog and commerce first, then `FailureEvent` and all three access alarm wrappers. During each replacement the relevant golden is the red/green tripwire: any field-order, UUID, timestamp, omission, or payload change fails exact equality.\n4. **Commit 4 — `refactor(consumers): centralize schema-first envelope decoding`.** Replace inventory and access local raw envelopes, migrate `completed`/`publication` to shared generic aliases with service-owned data types, and update the access policy projector. Run shared, access, and inventory suites; all existing handler tests must stay unmodified.\n5. **Final committed-tree gate.** Confirm `rg` finds no production competing envelope; confirm no generated file is modified; then run `make check`. Because no API specification changes, regeneration should produce a clean diff rather than files to commit.\n\n## 6. The open decision\n\nKeep all deterministic-ID helpers per service and add no shared SHA1 primitive. Catalog’s five helpers encode distinct, load-bearing domain keys (`events.go:135-143,190-211,228-239,323-329`), and commerce’s helper encodes its own subject/order identity (`services/commerce/internal/events/events.go:52-54`); the repeated `uuid.NewSHA1` call is incidental, while the key construction and return type are the actual policy.\n\nA shared `SHA1OverKey` would save roughly one line per helper but would neither enforce key domains nor prevent collisions between incorrectly composed keys. It would also broaden the thin kernel beyond what this ticket needs. Keep the derivations visible beside their subjects and payload rules; standardize them later only if a real cross-service invariant emerges, with pinned vectors and a separate ADR.\n\n## 7. Scope call\n\nUse one PR, including both publisher and consumer migrations. The generic type aliases let the second-pass `completed` and `publication` types retain their current field access and existing test construction, keeping the full change at approximately 370–395 lines after deletions—inside the shaped ~400-line budget.\n\nSplitting would leave local consumer envelopes in place, so TKT-126 could not truthfully meet COS-1 or centralize ADR-017’s load-bearing rule. `FailureEvent` and the three access alarm writers also belong in this PR because they are publishers at sites explicitly named by the finding; postponing them would make the “one envelope declaration” claim false.\n\n## 8. Risks / what could go wrong\n\n| Risk | Mitigation |\n|---|---|\n| **Code-review bounce:** goldens are introduced after the shared type, generated through an updater, or compare normalized JSON. | Commit literal tests first against local types at `59c0295`; retain no generator; compare entire byte slices. Point reviewers to the standalone baseline commit. |\n| Generic field order or a typed/raw substitution changes JSON key order or payload encoding. | Declare fields in ADR-009 order and run exact goldens for every emitter path. Never pre-marshal `Data` on emit. |\n| `ErrInvalidSchema` is treated as malformed JSON, changing access failure reason or letting poison touch readiness. | Use a sentinel, branch on `errors.Is`, and keep the existing access/inventory/policy poison tests unmodified. |\n| Migrating second-pass structs accidentally decodes future `data` before checking schema. | `DecodeEnvelope` returns `json.RawMessage`; handlers compare schema first, then decode either `env.Data` or a typed shared alias. Preserve incompatible hand-written skew fixtures. |\n| UUID choice causes catalog panics or changes its JSON. | Parse only strings returned by catalog’s own `uuid.NewSHA1(...).String()` helpers, at envelope construction; goldens prove the JSON remains identical. Do not accept external strings through this path. |\n| Workspace dependency behavior is changed unnecessarily, or shared’s UUID dependency remains incorrectly indirect. | Leave service `go.mod` files and `go.work` alone because that is the repo’s established shared-module wiring; make UUID direct only in `shared/go/go.mod`; exercise every module via `make check`. |\n| Alarm cleanup drifts into TKT-120 or changes integrity payloads. | Leave `policyConflictAlarmData`, `alarmData`, and `conflictAlarmData` byte-for-byte untouched; replace only their outer wrapper and pin all three literals. |\n\n## 9. Cons of this recommendation\n\n- `shared/go` gains a direct dependency on `google/uuid`, and the envelope cannot accept a future non-UUID identifier without another contract decision. That is acceptable because every current emitter and consumer already treats IDs as UUIDs, and choosing `string` would force broader boundary parsing plus modifications to existing tests.\n- Generics and service-local aliases are slightly more abstract than concrete structs. The benefit is that typed emit and raw decode views share one declaration without pre-marshalling payloads.\n- `DecodeEnvelope` cannot own subject-specific known-schema ranges, parking/quarantine behavior, or readiness: those remain service policy. It centralizes the safe outer decode and bottom-end rejection, while unchanged handler tests continue to enforce disposition.\n- One PR is near the shaped line budget and touches three modules plus shared code. The commits keep wire proof, shared policy, emit migration, and decode migration independently reviewable.\n- Literal goldens are deliberately brittle: a legitimate future wire change will require an explicit literal update. That friction is the intended contract-review gate, not a reason to weaken the comparison.\n\nCodex session ID: 019fa650-beff-7450-b272-03895fa6faad\nResume in Codex: codex resume 019fa650-beff-7450-b272-03895fa6faad\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### ✅ Plan final — TKT-126 — critique, amendments, and the self-made Gate-2 decisions\n\n**Critique by `main-agent`** (config.models.planReview) against the `gpt-5.6-sol` draft. Cross-model\npair holds: drafter ≠ critic. This is a **delta** — the `kind=plan` draft **as amended below is the\nplan**. Nothing restated.\n\n---\n\n#### 0. Cons read against the Decision first (the cheapest reject)\n\nPer `quality-practices.md` §1, before any code check: does a Con in §9 negate the Decision in §1?\n**No.** The five Cons are the honest cost of the chosen shape (a direct `uuid` dep in the kernel,\ngenerics over concrete structs, `DecodeEnvelope`'s deliberately narrow remit, a diff near budget,\nbrittle goldens) and each is argued as *accepted*, not as an unaddressed defect. §9's third Con —\n*\"`DecodeEnvelope` cannot own subject-specific known-schema ranges, parking/quarantine behavior, or\nreadiness\"* — reads at first like it refutes **COS-2** (*\"`DecodeEnvelope` enforces `schema`-first\ndispatch and rejects `schema <= 0` … (terminate; must not touch readiness)\"*). It does not: a\nlibrary function cannot call `msg.Term()` or touch a readiness latch, and it was never going to.\nCOS-2's *\"terminate\"* is a statement about the **disposition the rule produces at the handler**,\nwhich the existing, unmodified poison tests already pin. The split the draft draws — shared code\nreturns a **sentinel**, service code chooses the **disposition** — is the only coherent reading.\nRecorded, not held against it.\n\n#### 1. Seam and symbol verification (`git grep -n` at HEAD `59c0295`)\n\nEvery file, symbol, line range and test name the draft cites **exists and is where it says**. Spot\nchecks that mattered:\n\n| Draft's claim | Verdict |\n|---|---|\n| `seatMapPublishedEnvelope` `:146-167`, `performancePublishedEnvelope(WithID)` `:272-320`, `performanceArchivedEnvelope` `:383-400` are pure byte seams | ✅ confirmed — all three return `([]byte, error)` and take `occurred` as a parameter |\n| Closure marshalling is inline in `publishClosure` `:341-363`, so a seam must be extracted | ✅ confirmed |\n| Commerce has **no** test file and marshals inline with `time.Now().UTC()` `:56-62` | ✅ confirmed |\n| `policyConflictAlarmData` `scan.go:305`, `alarmData` `lifecycle.go:352`, `conflictAlarmData` `reconcile.go:297` | ✅ confirmed, exact lines |\n| ADR-033 is the next free number | ✅ confirmed — `docs/adr/` tops out at ADR-032 |\n| Service modules import `ticketing/shared/*` with no `require`/`replace`; `go.work` supplies all 8 | ✅ confirmed — `shared/go/go.mod` is module `ticketing/shared`; no service `go.mod` references it |\n| `google/uuid` is currently **indirect** in `shared/go/go.mod` | ✅ confirmed — promotion to direct is real and required |\n| The 11 catalog envelope tests + the named consumer poison/skew tests | ✅ all exist under the cited names |\n\n**No hallucinated seam.** Two citations drift harmlessly (`FailureEvent` is at `consumer.go:44-51`,\nnot `:46-60`; the gate target is `Makefile:17`, not `:44-71`) — noted, not amended.\n\n#### 2. Wire-level facts the draft asserts, verified against the handlers (not just the symbols)\n\n- **`ID uuid.UUID` is the right call, and for a reason the draft did not state.** I had independently\n  flagged the opposite risk before reading the draft: catalog's envelope is `ID string`, commerce's\n  is `ID uuid.UUID`, and flattening both to `string` is wire-identical but would **regress poison\n  detection** — today a malformed id (`\"not-a-uuid\"`) fails `json.Unmarshal` into `uuid.UUID` and\n  terminates; against a `string` field it parses clean and survives the `!= \"\"` check. The draft\n  picked `uuid.UUID` on typing grounds and landed on the safe side. **Accepted, with the reason\n  written down** so a later \"simplify to string\" cannot be argued as cosmetic.\n- **The existing catalog tests decode into hand-written local structs**, not the production\n  `Envelope` (`events_test.go:62,101,156,255,384,438,481` — `var got struct { ID string … }`,\n  deliberately, per the ADR-017 trap comment at `:83-88`). So changing the production envelope's\n  `ID` to `uuid.UUID` does **not** touch them, and `got.ID != EventID(tt.perf)` stays `string` vs\n  `string`. **COS-5 survives the type change.** ✅\n- **The inventory alias is safe.** All 9 test construction sites use **named** fields\n  (`publication{ID: uuid.New(), Schema: 3}` then `e.Data.X = …`), so\n  `type publication = domainevent.Envelope[publicationData]` compiles unchanged even though the\n  alias *adds* `Type` and `OccurredAt`. ✅\n- **The access fixtures are hand-written JSON strings**, never marshalled from the type under test\n  (`consumer_test.go:70-95`, `:110-135`) — the decode-side migration cannot quietly invalidate them. ✅\n\n#### 3. Pre-mortem lens — *assume this bounced at review; work backward*\n\nApplied to the draft (not to the code). Five credible causes; **four produced amendments**:\n\n**A1 — `uuid.MustParse` on the catalog publish path. REJECTED as written.** §1 proposes adapting\ncatalog's string-returning helpers with *\"trusted `uuid.MustParse`\"*. That introduces a **panic path\ninto an emitter**, and \"trusted\" is doing load-bearing work in a sentence about a production\npublish. It is also unnecessary: `EventID`/`BackfillEventID`/`ArchivedEventID`/`ClosureEventID`/\n`SeatMapPublishedEventID` each already compute `uuid.NewSHA1(…)` and *then* call `.String()`.\n**Amendment:** add an unexported sibling per helper returning the raw `uuid.UUID`, and make the\nexported string helper `return <sibling>(…).String()`. Production builds the envelope from the UUID\nform; the exported string API is untouched, so `events_test.go` and\n`cmd/catalog/reemit_test.go:99` keep compiling. **No `MustParse`, no panic path, no test edits.**\n(Changing the exported helpers to return `uuid.UUID` instead was considered and rejected — it breaks\n`got.ID != EventID(tt.perf)` at `events_test.go:73` and violates COS-5.)\n\n**A2 — the \"pre-refactor\" baseline commit contains production edits.** Commit 1 extracts four marshal\nseams (closure, commerce order-completed, the failure body, the store alarm wrapper) *before*\ncapturing goldens. A reviewer can fairly ask whether a commit that edits emitters is still\n\"pre-refactor\". It is — the extractions are pure moves of an unchanged struct literal — but the\nclaim must be **checkable, not asserted**. **Amendment:** commit 1 must contain **zero references to\n`shared/go/domainevent`** (the package does not exist yet at that commit), and the plan states this\nas the reviewer's one-command check: `git show <baseline> | grep domainevent` must return nothing.\nThat is what makes \"the literals were captured from the old declarations\" falsifiable.\n\n**A3 — the second-pass decode must keep decoding the whole message, not `env.Data`.** §8 says\nhandlers *\"decode either `env.Data` or a typed shared alias\"*. Those are **not** equivalent.\n`TestInvalidKnownSchemaStillTerminates` pins two different reasons at a known schema —\n`\"quantity wrong type\"` → `ReasonInvalidJSON`, `\"quantity zero\"` → `ReasonInvalidContract`\n(`consumer_test.go:137-166`). Both survive either route. But an envelope with a **missing or null\n`data`** does not: today `json.Unmarshal(msg.Data(), &event)` yields a zero payload that\n`validateCompleted` rejects as `ReasonInvalidContract`, whereas `json.Unmarshal(env.Data, &payload)`\non a nil `RawMessage` fails as *\"unexpected end of JSON input\"* → `ReasonInvalidJSON`. No existing\ntest covers it, so nothing would catch the drift. **Amendment:** the known-variant decode stays\n`json.Unmarshal(msg.Data(), &event)` into the typed alias at **every** migrated consumer. This is\nalso the smaller diff.\n\n**A4 — the per-subject `min` must not migrate into `DecodeEnvelope`.** Inventory's registry is\n`{subjectPublished: {1, max}, subjectArchived: {2, 3}, subjectClosed: {1,1}, subjectReopened: {1,1}}`\n(`consumer.go:164-169`) — **`archived` has `min = 2`**. A `schema:1` archived event is not `<= 0`, so\nthe shared bottom-end rule cannot see it; it must keep falling to inventory's `< spec.min` arm. The\ndraft preserves this, but never says *why* it must, which is exactly how a later \"tidy-up\" moves it.\n**Amendment:** the ADR and the shared package doc-comment state the boundary explicitly —\n**`DecodeEnvelope` owns only the universal bottom end (`schema <= 0`); per-subject `min`/`max`,\nparking vs quarantine, readiness and `Term` remain service policy.**\n\n**A5 — the COS-1 grep, as written, would license deleting the tests that make this ticket honest.**\nThe DoD's check is `rg -n 'json:\"schema\"' --glob '*.go' services shared/go` finding \"no competing\nproduction envelope struct\". That glob **includes `_test.go`**, and the hand-written decode structs\nin `events_test.go`, `smoke/catalog_publication_test.go` and the store smoke tests will still match.\nAn implementer reading COS-1 literally could \"finish the job\" by collapsing them onto the shared\ntype — which is precisely the ADR-017 trap the ticket exists to avoid (*a fixture built from the type\nunder test cannot fail*). **Amendment:** COS-1's grep is **production-only**\n(`--glob '!*_test.go'`), and the plan states affirmatively that **hand-written test decode structs\nmust survive and must not be migrated to the shared type**. Deleting them fails the ticket even\nthough it would make the naive grep cleaner.\n\n**Fifth cause, no amendment — diff overrun.** 370–395 *changed* lines can present as ≈+450/−200 in\n`gh pr view`. Accepted (see D2).\n\n**Lens result:** four amendments; A1 and A5 are the two that would most plausibly have bounced the PR.\n\n#### 4. Verdict per section\n\n| § | Verdict |\n|---|---|\n| 1 Decision | **Amended** — `Envelope[T any]`, `ErrInvalidSchema`, `DecodeEnvelope`, `uuid.UUID` id and the alias strategy all **accepted**; the `uuid.MustParse` adaptation **rejected** and replaced (A1) |\n| 2 DoD | **Amended** — COS-1 grep scoped to production, plus an explicit \"hand-written test fixtures survive\" line (A5) |\n| 3 Files touched | **Accepted** — every path and payload-type line verified |\n| 4 Test plan | **Amended** — baseline commit gets the falsifiable `grep domainevent` check (A2); `min` boundary pinned (A4) |\n| 5 Sequencing | **Accepted** — 4 commits, goldens first, TDD red on the `domainevent` tests in commit 2 |\n| 6 Open decision | **Accepted** (see D1) |\n| 7 Scope call | **Accepted** (see D2) |\n| 8 Risks | **Amended** — the `env.Data` alternative removed as unsafe (A3) |\n| 9 Cons | **Accepted** — none self-refuting (§0) |\n\n#### 5. Decisions I made myself — Gate 2 is waived on this run\n\nThe owner's waiver rule: *prefer the option the critique recommends; absent a recommendation, take\nthe most reversible.* Each decision, with which rule resolved it:\n\n- **D1 — the deferred DoR `uncertainties` item: id-helpers stay per-service; no shared SHA1\n  primitive.** *Critique-recommended **and** most reversible.* The repeated `uuid.NewSHA1` call is\n  incidental; the **key construction** is the policy, and it is genuinely different at all six sites\n  (catalog's backfill key even carries a deliberate `:backfill:<epoch>:` segment whose whole purpose\n  is to *not* collide with the live id — `events.go:188-204`). Extracting the primitive would save\n  ~1 line each and centralize nothing that matters. Adding a shared primitive later is trivial;\n  removing one from the kernel later is not. **DoR item `uncertainties` is now resolved, not deferred.**\n- **D2 — one PR, both publisher and consumer migrations.** *Critique-recommended.* A split leaves the\n  consumer envelopes in place, so COS-1 (\"one `Envelope` declaration remains repo-wide\") would be\n  **false at merge** — the ticket cannot pass its own acceptance criteria in halves. The shaped\n  ~400-line budget is a guideline; a COS is not. If the diff runs long the review-guide will say so\n  rather than the scope silently shrinking.\n- **D3 — `ID uuid.UUID` in the shared envelope, reached via unexported UUID helpers (A1).**\n  *Critique-recommended;* the alternative (`string`) is rejected on the poison-detection regression\n  in §2, which is a correctness argument, not a style one.\n- **D4 — known-variant decode reads `msg.Data()` into the typed alias (A3).** *Most reversible* —\n  it is the status-quo call shape, so it preserves the failure-reason mapping by construction rather\n  than by a test that does not exist.\n- **D5 — hand-written test decode structs are protected, not cleaned up (A5).** *Critique-recommended*\n  — ADR-017 §5b′ requires fixtures not built from the type under test; COS-1 must not be read as\n  overriding the ADR the ticket exists to enforce.\n\n**`risk:low` remains unset and must stay unset** — `classification: complex`, five services, a\n`shared/go` addition, ~14 files.\n\n#### 6. ⚠️ Introduced at plan-review, reviewed by no second model\n\n*(Required section. These reached the Gate-2 line — waived here — carrying only their author's\njudgment: the cross-model guarantee covers the draft and, later, the code, never plan-review's own\ninventions.)*\n\n1. **A1's unexported-UUID-sibling pattern in catalog** — my construction, not the draft's. It is the\n   one amendment that adds code the drafter never saw.\n2. **A3's \"decode `msg.Data()`, never `env.Data`\" rule** — my analysis of an *untested* behavior\n   difference (missing/null `data` → which failure reason). By definition no existing test proves me\n   right; if I am wrong the cost is a preserved status quo, which is why it is also the reversible call.\n3. **A5's production-only scoping of the COS-1 grep** — my reinterpretation of an acceptance\n   criterion the ticket author wrote. I am narrowing a COS check; a human PO might have wanted the\n   broader grep and a documented exception list instead.\n4. **A2's `git show <baseline> | grep domainevent` check** — my invention as the falsifiability\n   mechanism for COS-3.\n\nThese four are the first things the `gpt-5.6-sol` code review should be pointed at — and the code\nreview *will* see them as code, which is the compensating control for the missing plan gate.\n\n**Test seam (recorded):** the existing in-package byte seams in\n`services/catalog/internal/events` (+ one extracted `closureEnvelope`), a new\n`services/commerce/internal/events/events_test.go` over an extracted `orderCompletedEnvelope`, the\nextracted failure-body helper in `services/access/internal/consumer`, and one package-local marshal\nseam in `services/access/internal/store` — the **highest** seams that still isolate the change, all\n**existing** but two, and byte-level by construction. New goldens live in **separate files**\n(`wire_compat_test.go`, `event_envelope_test.go`) so review can tell preserved behavioral coverage\nfrom TKT-126's new compatibility evidence.\n\n**Lens applied:** pre-mortem (§3) — changed four things: A1, A2, A3, A4 (A5 came from the COS re-read).\n\n➡️ **Gate 2 waived by the owner for this run. Proceeding to `Building` / `agent:coding`.**\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### 🔨 Implementation — TKT-126 — PR [#116](https://github.com/mathd/ticketing_system/pull/116)\n\n**Implementer:** `main-agent` (config.models.implement). TDD, five commits, gate green on the\ncommitted tree.\n\n```yaml\nstage: coding\nimplementer: main-agent\npr: https://github.com/mathd/ticketing_system/pull/116\nbranch: TKT-126-shared-event-envelope\nbase: origin/main @ 59c0295\nhead: 974063f\ndiff: +1170/-173 (19 files)\ngate: make check — EXIT=0, judged from the log body (no `Error N` / `FAIL` / `drifted`)\ngate_runs: 2 (first failed on one lint finding; see below)\nexisting_tests_modified: 0\n```\n\n#### Commits\n\n| SHA | What |\n|---|---|\n| `34371bb` | **Baseline** — 13 golden literals captured from the pre-refactor emitters, plus the four marshal seams that had none |\n| `3cd0108` | `shared/go/domainevent` + ADR-033 (tests red first: package did not exist) |\n| `982f924` | Emitters migrated (catalog, commerce, `FailureEvent`, 3 alarms) |\n| `0d2ad40` | Consumers migrated (inventory, access order, access policy) |\n| `974063f` | Dead alias removed (the lint fix) |\n\n#### The golden capture, and why the ordering is checkable rather than asserted\n\nADR-017's trap is that *a fixture built from the type under test cannot fail*. So the goldens were\ncommitted **before** `shared/go/domainevent` existed, and the proof is a path, not prose:\n\n```\ngit show 34371bb:shared/go/domainevent/envelope.go\n→ fatal: path 'shared/go/domainevent/envelope.go' does not exist in '34371bb'\n```\n\nMy first version of this check was `git show <commit> | grep domainevent` — **it failed\nimmediately**, because the commit message and the test comments both contain the word. A\nfalsifiability check that prose can defeat is not one. Replaced with the path form above.\n\n**Three of the alarm goldens passed on first write.** A golden that has never been red proves\nnothing, so they were mutation-checked: swapping two fields in `integrityAlarmEnvelope` turns\n`TestWireGoldenIntegrityAlarm` red. Reverted, re-verified green.\n\nCoverage: `performance.published` (schemas 2/3/4 + the distinct backfill id), `performance.archived`\n(2/3), `.closed`, `.reopened`, `seat_map.published`, `order.completed`, the ticket-issuance failure\nrecord, and all three alarm classes. Whole-byte comparison; timestamps carry a non-zero nanosecond\ncomponent so an RFC3339Nano precision change cannot slip through.\n\n#### Two declaration sites neither the ticket nor the plan draft listed\n\n1. **`services/access/internal/consumer/policy.go`** — a fourth ADR-017 dispatch ladder (own\n   `maxKnownPublicationSchema`/`publicationSchemaMin`). Found at plan review, recorded in\n   `kind=plan-final`.\n2. **`store.completionEnvelope` in commerce** — built its own `events.Envelope`. Found by the\n   compiler during migration, because the finding's table was built by grepping for `json:\"schema\"`,\n   which sees *declarations* and not *usages*. **This is the copy that mattered most**: the outbox\n   freezes those bytes and publishes them verbatim, so it — not `OrderCompleted` — is what access\n   actually consumes. Now delegates to one exported `events.OrderCompletedEnvelope`, so the golden\n   covers the real production path.\n\nThe ticket said \"6+ sites\". It was **seven**.\n\n#### Plan-final amendments, as built\n\n- **A1 — no `uuid.MustParse` on a publish path.** The draft proposed adapting catalog's\n  string-returning id helpers with \"trusted `uuid.MustParse`\". Rejected: each helper gained an\n  unexported sibling returning the `uuid.UUID` the derivation already computes. The exported string\n  API is untouched, so no existing test changed. One genuine conversion remains — in\n  `performancePublishedEnvelopeWithID`, whose signature is fixed by its callers — and it returns an\n  error rather than panicking.\n- **A3 — the known-variant decode still reads `msg.Data()`, never `env.Data`.** These are not\n  equivalent: an envelope with missing or null `data` yields a zero payload that\n  `validateCompleted` rejects as `invalid_contract`, while unmarshalling a nil `RawMessage` fails\n  as \"unexpected end of JSON input\" and would report `invalid_json`. No test pins this; it would\n  have drifted silently.\n- **A4 — per-subject `min` stayed in the services.** Inventory's `archived` starts at schema 2, so\n  the shared bottom-end rule cannot see a schema-1 archived event. Also preserved inventory's\n  **question order** by holding the decode error rather than acting on it, so an unknown subject is\n  still answered before a broken schema.\n- **A5 — the five hand-written decode structs in test files survive.** Migrating them onto the\n  shared type would be the exact ADR-017 trap. COS-1's grep is production-only.\n\n#### COS-1 verified\n\n```\n$ git grep -n 'json:\"schema\"' -- 'services/**/*.go' 'shared/**/*.go' 'gateway/**/*.go' ':!*_test.go'\nshared/go/domainevent/envelope.go:64:  Schema int `json:\"schema\"`\n```\n\nOne production declaration repo-wide.\n\n#### Gate\n\nRun as the sole command in its shell call, redirected to a file, with an explicit `EXIT=$?`\nsentinel; PASS/FAIL judged from the **log body**, not the harness exit code.\n\n- **Run 1 — FAIL (`EXIT=2`).** `golangci-lint`: `type envelope is unused` in access. Real: both\n  call sites infer the type from `DecodeEnvelope`, so the alias had no users. Fixed by deleting it\n  (`974063f`).\n- **Run 2 — PASS (`EXIT=0`).** No `Error N` / `FAIL` / `drifted` in the log; the smoke suite ran\n  through `access verify-lifecycle`.\n\nNot two failures on the same cause — one failure, one clear fix.\n\n#### Scope call held\n\nDiff is **+1170/−173**, over the ~400-line shaping budget. Not split: COS-1 requires one envelope\ndeclaration repo-wide, which is **false at merge** if the consumers ship separately. The overrun is\nflagged in the review-guide rather than the scope quietly shrinking. Bulk is ADR-033 (~150 lines),\nthe goldens (~230), and the shared package plus its tests (~230) — roughly half the diff is\nevidence and decision record, not logic.\n\n➡️ `agent:coding` → `agent:ai-review`. Adversarial review by `gpt-5.6-sol` on the branch diff next.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### 🔬 Adversarial review — TKT-126 — two passes, `gpt-5.6-sol`\n\n**Reviewer:** `gpt-5.6-sol` (config.models.aiReview) via the codex companion `adversarial-review`,\nprimed for guilt, given the diff only — the plan and its reasoning were deliberately withheld.\nCross-model pair holds: implementer `main-agent` ≠ reviewer `gpt-5.6-sol`.\n\n```yaml\nstage: ai-review\nreviewer: gpt-5.6-sol\npasses: 2\npass_1: full branch diff vs origin/main  -> 1 high (blocking, correct)\npass_2: fix diff only (974063f..6339ed5) -> 1 high (split), 1 medium (accepted)\nsecond_pass_call: REQUIRED — pass 1's fix changed a type declaration and consumer\n  control flow across three consumers. Non-trivial by every clause of the rule.\nthird_pass: NO — cap reached; pass 2 refined pass 1's fix rather than refuting it\n  at the root, so the churn counter did not reset.\ngate_after_fixes: make check EXIT=0 (judged from log body)\nhead: dd4e121 (== origin/TKT-126-shared-event-envelope)\n```\n\n---\n\n#### Pass 1 — one finding, blocking, and it was right\n\n**F1 `[high]` — the shared decode view validated metadata before schema dispatch.** ✅ **ACCEPTED, FIXED** (`6339ed5`)\n\n`Raw` was `Envelope[json.RawMessage]` — the emit type reused — so it inherited\n`OccurredAt time.Time`. `time.Time` parsing is strict, so an envelope carrying a malformed timestamp\nfailed the first-pass decode, and a failed decode means *unreadable*, which terminates.\n\n**Verified empirically against the branch before changing anything**, not reasoned about:\n\n| Consumer | Input | Before this PR | On the PR as reviewed |\n|---|---|---|---|\n| inventory | schema 5, `\"occurred_at\":\"invalid\"` | quarantine + ack + unready | **terminate** |\n| access order | schema 2, same | park + unready | **`invalid_json` record + terminate** |\n| access policy | schema 9, same | park + unready | **terminate** |\n| all three | **known** schema, same | processed normally | **terminate** |\n\nThe future-variant rows are TKT-61 exactly — discarding a recoverable event on the authority of the\none binary that provably cannot read it — reintroduced by the refactor built to prevent it. The\nknown-schema rows are worse: valid events dropped over a field no consumer reads.\n\n**Fix:** emit and decode are now two declarations differing by exactly one field. `Envelope[T]` keeps\n`OccurredAt` (ADR-009 §5 requires emitters to write it); `Decoded[T]` omits it, because every field\nthe decode path parses is a field whose *format* can reject a message before `schema` has been\nlooked at. ADR-017 §5b′ had already named the minimal stable envelope as `{id, schema, data}` — this\nis why. **My first attempt at the fix was incomplete**: it corrected only the first pass, and the\ntyped second-pass aliases had the same defect, which the inventory known-schema parity test caught.\n\n`type` was kept on the decode side and the consequence pinned by a test: a string field cannot fail\nto parse the way a timestamp can, and a *non-string* `type` violates ADR-009 §5's envelope contract\nrather than being a schema variation. That is a tightening for inventory, explicit rather than\nimplicit.\n\n---\n\n#### Pass 2 — on the fix diff, which no one had reviewed\n\n**F2 `[high]` — inventory never validates `type` against the subject.** ⚖️ **SPLIT: rejected as a code change, accepted as a doc defect, deferred as a ticket**\n\n- **Rejected (code):** verified pre-existing — at `origin/main` inventory's envelope has no `type`\n  field at all and dispatches purely on `msg.Subject()`. Adding the check would change disposition\n  for messages that ship today, which is precisely the parity this ticket exists to preserve.\n- **Accepted (doc):** the reviewer was right that **ADR-033 and the `domainevent` package doc both\n  claimed \"every consumer makes them\"** about the `id`/`type` checks. That is false, and I wrote it.\n  All three check `id`; only access's two check `type`. Corrected in both places, stated plainly.\n- **Accepted (test):** my fixtures omitted `type` entirely, which blessed the gap rather than\n  testing the subject at hand. They now carry the correct type for the subject.\n- **Deferred:** **TKT-133** (Backlog), noting it is likely a slice of TKT-127.\n\n**F3 `[medium]` — the parity tests asserted less than they claimed to protect.** ✅ **ACCEPTED, FIXED** (`dd4e121`)\n\nCorrect, and against my own new tests. The future-schema case asserted only that termination did not\nhappen — a handler that quarantined and then NAK'd would have passed while leaving the event\noutstanding and stalling the ack window behind it (TKT-68). The known-schema case never asserted the\npublication was *provisioned*, so a handler that silently acked without doing the work would have\npassed. Both now assert the exact action sequence, and the known case asserts the `Provision` call\nwith its event id.\n\n---\n\n#### ⚠️ Unreviewed fix diff — stated, not buried\n\n**`dd4e121` has been reviewed by no model.** The cap is two passes per stable diff, and pass 2\nrefined pass 1's fix rather than proving it wrong at the root, so the counter did not reset. The\ncommit is test assertions and documentation with no production logic, and `make check` is green on\nit — but **that verification is mine, and it is labelled as mine** here and in the review-guide.\n\n#### Evidence the goldens could never have provided\n\nAll 13 byte-exact goldens were green through every one of F1's bugs and always would have been. **A\nfixture built from an emitter cannot express an input an emitter would never produce.** The goldens\nprove what this repo *writes*; they are structurally incapable of proving what it must tolerate\n*reading*. I built careful, mutation-checked evidence for one half of the claim and none for the\nother, and the review is what found the hole. `envelope_tolerance_test.go` (inventory + access) now\ncovers all three consumers at known and future schemas, mutation-checked — restoring `OccurredAt`\nturns all six groups red. ADR-033 records the rule for next time.\n\n**Review-guide posted on [PR #116](https://github.com/mathd/ticketing_system/pull/116#issuecomment-5099174086)** before the merge, per the waiver terms.\n\n➡️ Gate 3 waived by the owner. Squash-merging and deleting the branch next.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### ✅ Self-validation against the COS — TKT-126 — on the merged code\n\n**Gate 4 was waived by the owner for this run**, so this is the agent validating its own work. Read\nit as such. Every claim below is a command run against `origin/main` **after** the squash-merge\n(`ff89e69`), not against the branch.\n\n**Merged:** PR [#116](https://github.com/mathd/ticketing_system/pull/116) → `ff89e69`, branch deleted.\n**Post-merge gate:** `make check` — **EXIT=0** on the second run; the first failed on an unrelated\npre-existing flake (see below).\n\n---\n\n#### COS-1 — one `Envelope` declaration repo-wide ⚠️ **met in substance, not in its literal wording**\n\n```\n$ git grep -n 'json:\"schema\"' -- 'services/**/*.go' 'shared/**/*.go' 'gateway/**/*.go' ':!*_test.go'\nshared/go/domainevent/envelope.go:66    Schema int `json:\"schema\"`   # Envelope[T]  — emit\nshared/go/domainevent/envelope.go:113   Schema int `json:\"schema\"`   # Decoded[T]   — decode\n```\n\n**Zero declarations remain in any service** — that is the finding closed, and it is the part that\nmatters. But the COS says *one* declaration and there are **two**, both in the shared package. I am\nnot going to call that a clean pass.\n\nThe second exists because of the blocking ai-review finding: reusing the emit type as the decode\nview made a malformed `occurred_at` terminate events that should have been parked or quarantined.\nSplitting emit from decode was the fix, and the split is now the design — `Envelope[T]` carries\n`occurred_at` because ADR-009 §5 requires emitters to write it, `Decoded[T]` omits it because any\nfield the decode path parses can reject a message before `schema` has been dispatched on.\n\nSo the honest statement is: **one envelope, plus one deliberately narrower decode projection of it,\nboth in one package, zero in any service.** A PO who reads COS-1 strictly should register that as a\nscope amendment made under the waiver, not as a criterion silently met.\n\n#### COS-2 — `DecodeEnvelope` enforces schema-first dispatch and rejects `schema <= 0`, both tested ✅\n\n- Schema-first is **structural**, not asserted: `DecodeEnvelope` returns `Data json.RawMessage`, so\n  a caller has nothing typed to judge until it has read `schema`.\n- `schema <= 0` → `ErrInvalidSchema`, returned *with* the decoded fields so callers can still log\n  the id and offending schema. Consumers terminate it **without touching readiness**.\n- Evidence: 7 test functions in `shared/go/domainevent/envelope_test.go`, all fixtures hand-written\n  JSON (never marshalled from the type under test — ADR-017 §5b′), covering omitted / zero /\n  negative schema, future variants with renamed keys, changed types, array / scalar / null `data`,\n  and malformed bodies kept distinguishable from `ErrInvalidSchema`.\n- Readiness-untouched is pinned by the **pre-existing, unmodified** poison tests at all three\n  consumers.\n\n#### COS-3 — golden literals captured from the pre-refactor emitters, not regenerated ✅\n\n```\n$ git show 34371bb:shared/go/domainevent/envelope.go\nfatal: path 'shared/go/domainevent/envelope.go' exists on disk, but not in '34371bb'\n```\n\n13 whole-byte goldens over every published subject, committed at `34371bb` — a commit at which the\nshared package **does not exist**. The check is a path, not a grep: my first version was\n`git show <commit> | grep domainevent`, which the commit message and test comments defeated\nimmediately. Mutation-verified: reordering two fields in one envelope struct turns\n`TestWireGoldenIntegrityAlarm` red.\n\n#### COS-4 — an ADR records the `shared/go` addition, in this ticket ✅\n\n`docs/adr/ADR-033-shared-domain-event-envelope.md` on `main`. Records the thin-kernel argument, the\nemit/decode asymmetry and why it exists, the `uuid.UUID` id decision, the line the package does not\ncross, and — added after the review — that **the goldens are structurally incapable of proving the\nread side**, so any future change to the decode view's field set needs parity evidence instead.\n\n#### COS-5 — `make check` green with the existing event/consumer suites unmodified ✅\n\n```\n$ git diff --name-status 59c0295 origin/main -- '*_test.go'   # all 7 are A (added)\nmodified count = 0\n```\n\n**No pre-existing test file was touched.** All new evidence lives in separate files\n(`wire_compat_test.go`, `alarm_envelope_test.go`, `envelope_tolerance_test.go`) so preserved\nbehavioural coverage stays distinguishable from this ticket's new evidence. The five hand-written\ndecode structs in existing test files survive deliberately — migrating them onto the shared type is\nthe ADR-017 trap this ticket exists to avoid.\n\n---\n\n#### Post-merge gate: one failure, investigated, not ours\n\nThe first post-merge run failed on\n`TestScheduledReleaseIsLazyAndObservable` (`services/inventory/internal/store`). Established as a\npre-existing flake before re-running, not after:\n\n- `git diff dd4e121 ff89e69` is **empty** — the merged tree is byte-identical to the branch tree\n  that passed the gate pre-merge.\n- TKT-126 changed **no file** under `services/inventory/internal/store/`.\n- The test polls the DB clock against a 10-second deadline (*\"Cross release_at by DB time. Reads\n  only — no mutation, no sweeper.\"*).\n- Second run on the identical tree: `ok ticketing/services/inventory/internal/store`, gate EXIT=0.\n\nFiled as **TKT-134**, with the observation that the channel read and the public read disagreed about\nwhether the release boundary had been crossed *within one run* — which may be a real availability\nbug rather than a harness artifact, and the ticket says so rather than prescribing a longer sleep.\n\n#### One defect of mine, found and fixed after the merge\n\n`.plan-brief.126.md` — the codex prompt file for the plan stage — was swept into the branch by\n`git add -A` and merged to `main` as repo content. Removed in `07d1fd4`, with a `.gitignore` rule so\nthe next run cannot repeat it. Internal maintenance, direct to `main` per the skill.\n\n#### Deferred to Backlog (no finding silently dropped)\n\n- **TKT-133** — inventory never validates `type` against the subject (pre-existing; behaviour\n  deliberately unchanged here to preserve disposition parity; likely a slice of TKT-127).\n- **TKT-134** — the flake above.\n\n➡️ Gate 4 waived. Closing with the metrics comment.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### 📊 Closeout — TKT-126 — one domain-event envelope in `shared/go`\n\n**Merged:** PR [#116](https://github.com/mathd/ticketing_system/pull/116) → `ff89e69` (squash), branch deleted.\n**Ran gateless:** all four human gates waived by the owner as a process experiment (`kind=claim`).\n\n#### Durations\n\nFrom the `sdlc-state` commit log (`git log -- .sdlc/tickets/TKT-126.json`), not hand-tracked.\n\n| Stage | Duration |\n|---|---|\n| `Backlog → Ready` | n/a (shaped and prioritized 2026-07-25, before this run) |\n| `Ready` (queued) | 6h 38m — wall-clock queue time, not work |\n| `Planning` | 17m 32s |\n| ├ `agent:planning` (draft, `gpt-5.6-sol`) | 15m 54s |\n| └ `agent:plan-review` (critique + plan-final, `main-agent`) | 1m 38s |\n| `Building` | 1h 07m 33s |\n| ├ `agent:coding` (`main-agent`) | 19m 13s |\n| ├ `agent:ai-review` (2 passes, `gpt-5.6-sol`) | 48m 20s |\n| └ `needs:human` → merge | ~0 (gate 3 waived; agent merged) |\n| `PO Review` | ~0 (gate 4 waived; agent self-validated) |\n| **human-wait total** | **0s** — the point of the experiment. Gates 2/3/4 would normally sit here. |\n| **agent wall-clock, Ready→Done** | **≈1h 25m** |\n\n#### Diff\n\n`+1524 / −176 (21 files)` — 1 squashed commit on `main`, from 6 on the branch.\n**27 new test functions**, 0 pre-existing test files modified.\nRoughly half the diff is ADR-033, goldens and parity tests — evidence and decision record, not logic.\n\nFollow-up outside the ticket: `07d1fd4` (removed a stray codex brief I had merged, added an ignore rule).\n\n#### Findings triage — nothing silently dropped\n\n| Finding | Verdict |\n|---|---|\n| ai-review pass 1 `[high]` — decode view parsed `occurred_at`, terminating events that should park/quarantine | **fixed** (`6339ed5`) |\n| ai-review pass 2 `[high]` — inventory never validates `type` vs subject | **split**: pre-existing → **TKT-133**; the false ADR claim it exposed → **fixed** |\n| ai-review pass 2 `[medium]` — parity tests asserted less than they claimed | **fixed** (`dd4e121`) |\n| post-merge gate — `TestScheduledReleaseIsLazyAndObservable` failed once | **flake, not ours** → **TKT-134** |\n\n---\n\n#### learnings:\n\n1. **Golden tests prove the write side and are structurally incapable of proving the read side.**\n   *(house: technical → `docs/LEARNINGS.md` + `docs/learnings/`, and it is already in ADR-033.)*\n   13 byte-exact, mutation-checked goldens were green through a blocking bug that changed the\n   disposition of every consumer, and always would have been: **a fixture built from an emitter\n   cannot express an input an emitter would never produce.** Whenever a refactor touches both sides\n   of a wire, byte-goldens on the emit side need a *disposition-parity* counterpart on the read side.\n   This generalizes ADR-017's trap — that one warns against fixtures built from the type under test;\n   this is the case where the fixtures are honest and still cover only half the contract.\n\n2. **In a decode path, every parsed field is a field whose *format* can reject the message.**\n   *(house: technical → ADR-033, already recorded; worth a `docs/learnings/` note.)*\n   `occurred_at` as a `time.Time` turned a malformed timestamp into a termination *before* schema\n   dispatch. ADR-017 §5b′ already prescribed `{id, schema, data}` as the minimal stable envelope;\n   the reason is exactly this, and it was not written down as a *reason*. Rule of thumb: a decode\n   view should carry the fewest fields that dispatch needs, and prefer parse-total types (`string`)\n   over parse-partial ones (`time.Time`, enums, `uuid.UUID`) for anything not dispatched on.\n\n3. **A falsifiability check written in prose can be defeated by prose.**\n   *(house: process → this closeout; too small for a skill edit.)*\n   My check that the goldens predated the shared package was\n   `git show <commit> | grep domainevent` — defeated instantly by the commit message and the test\n   comments, which both contain the word. The working version checks a **path**\n   (`git show <commit>:<path>` must fail). Prefer structural checks over textual ones for any claim\n   about a commit.\n\n4. **A grep for declarations does not find usages.**\n   *(house: repo-local, no action — the cost was one compiler error.)*\n   The architecture review's site table was built by grepping `json:\"schema\"` and so listed 6 sites;\n   there were 7. The seventh (`store.completionEnvelope`) was the one that mattered most, because\n   the outbox freezes and replays its bytes. Enumerations built from one grep shape should say which\n   shape, so the next reader knows what it cannot have found.\n\n**Promotion status:** items 1 and 2 are recorded in ADR-033 (shipped). Items 1–3 are worth a\n`docs/learnings/` entry; **not written in this run** — that is a doc-only change and would itself be\na direct-to-main commit, deliberately left for the owner to accept or drop, since a gateless run is\na poor place to unilaterally add repo guidance.\n\n---\n\n#### retro:\n\n**(1) What would have made this ticket faster or better?**\n\n- **The context-mémo's site table was wrong in a way the DoR could not catch.** It was `met`, its\n  content was accurate for what it listed, and it still missed the highest-risk site — because it\n  was assembled from a single grep shape. *Concrete change:* `references/shaping.md`, in the\n  context-mémo section, require an enumeration to record **how it was derived** (`git grep\n  'json:\"schema\"'` — declarations only), so a plan reader knows the enumeration's blind spot instead\n  of trusting its completeness. One line in the mémo.\n- **The ~400-line first-slice budget was set before anyone knew the real shape** and was exceeded\n  ~3.8×, almost entirely by evidence (ADR + goldens + parity tests) rather than logic. *Concrete\n  change:* `references/shaping.md`, first_slice — express the budget as **logic lines**, explicitly\n  excluding ADRs and test evidence, or the number pressures exactly the wrong thing. I held the\n  scope, but a less stubborn run would have split the PR and shipped COS-1 false.\n- **Nothing about the plan stage would have caught the blocking bug.** The plan was verified against\n  seams, symbols, wire facts and a pre-mortem, and the bug was invisible to all of it because it\n  lived in a *type substitution's* side effect. Only the adversarial code review found it. That is\n  the cross-model review earning its cost, and it is the strongest single data point this run\n  produced about the pipeline.\n\n**(2) What should the humans change?**\n\n- **The gateless experiment worked, with one asterisk worth naming.** Zero human wait, Ready→Done in\n  ~1h 25m, and the machinery caught its own worst bug. But **I both wrote the code and judged\n  whether the COS were met**, and at COS-1 that judgment was genuinely load-bearing: the literal\n  criterion (\"one `Envelope` declaration\") is not met — there are two — and I decided that was an\n  acceptable amendment. A PO might have decided otherwise, or wanted the COS reworded first.\n  *Concrete change:* if gateless runs continue, `SKILL.md` § the waiver should require that any COS\n  the agent judges **partially met** be called out at the top of the validation note, not inside it.\n  I did that voluntarily here; it should not be voluntary.\n- **`gh pr merge --delete-branch` fails in a worktree setup** (`fatal: 'main' is already used by\n  worktree`) — *after* the server-side merge succeeds, so the merge lands and the branch deletion\n  silently does not. *Concrete change:* `SKILL.md` § step 4, in the merge instruction — when the\n  repo uses worktrees, verify branch deletion separately (`git ls-remote --heads origin <branch>`)\n  rather than trusting the flag. Cost here: one manual `git push --delete`.\n- **My own slip, worth a rule rather than an apology:** I committed the codex prompt file to `main`\n  via `git add -A`. `references/codex-runner.md` says to \"delete only what this run created\" but the\n  deletion is easy to skip when the run succeeds. *Concrete change:* the ignore rule shipped in\n  `07d1fd4` makes it structurally impossible, which is better than remembering.\n\n**Applied in this session:** the `.gitignore` rule (`07d1fd4`). The three `SKILL.md` /\n`shaping.md` edits above are **not applied** — they are skill edits, and the skill's own bar says\nthose need to change future agent behaviour *and* not be readable from the repo. They clear that\nbar, but applying them unilaterally at the end of a run whose whole point was removing human\njudgment seemed like the wrong instinct. **Offered for the owner to approve; say the word and I will\npatch them.**\n"
    }
  ]
}
