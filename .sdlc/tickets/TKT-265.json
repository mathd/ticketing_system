{
  "key": "TKT-265",
  "type": "Task",
  "summary": "Nothing checks whether a go.mod LAGS the version the workspace selects (TKT-264 ai-review)",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "number": 309,
    "url": "https://github.com/mathd/ticketing_system/pull/309",
    "state": "merged",
    "mergeCommit": "96c2da06"
  },
  "description": "Raised by TKT-264's adversarial ai-review (`gpt-5.6-luna`, finding 2, **medium**), verified by execution and carried out of that ticket as **pre-existing and wider than that ticket's diff**.\n\n## The gap\n\nADR-035's `scripts/check-go-dependency-drift.sh` compares **`require` declarations only**. That is its stated and deliberate scope -- the ADR is explicit that this is *declaration* drift, not runtime drift, and the checker reads manifests through `go mod edit -json` so it stays offline and fast.\n\nWhat nothing checks is the **other** direction: a manifest that lags the version the workspace actually **selects**. MVS can raise a selected version through a transitive requirement without any `go.mod` line changing, and when it does, every manifest in the repo still reads as internally consistent while describing a build that is not what happens.\n\n## Evidence it is real, and pre-existing\n\nRunning `go work sync` on **unmodified `origin/main`** (`4855c56e`) is **not** a no-op. It rewrites:\n\n- `services/access/go.mod` -- adds `kin-openapi`, `go-openapi/jsonpointer`, `gorilla/mux`, `oasdiff/yaml`, `oasdiff/yaml3`, `rogpeppe/go-internal`, `santhosh-tekuri/jsonschema/v6`\n- `services/commerce/go.mod` -- adds `cespare/xxhash/v2`, `go-logr/logr`, `go-logr/stdr`, `gorilla/mux`; removes `kr/pretty`\n\nNone of that is TKT-264's. It was already there and no gate noticed, which is precisely ADR-035's own framing of the original defect: *\"the repository was simultaneously fully tidy and fully drifted.\"* The ADR closed the horizontal case (two modules disagreeing with each other) and left the vertical case (manifests disagreeing with the build list) open.\n\n## What TKT-264 added to it\n\nThat ticket bumped `x/mod` and `x/tools` in `services/catalog` to close two HIGH CVEs. Resolution raised **three further dependencies in the selected build list that no `go.mod` line declares**:\n\n| dependency | before | after | declared? |\n|---|---|---|---|\n| `golang.org/x/net` | v0.56.0 | v0.58.0 | no (`shared/go` still declares v0.56.0) |\n| `golang.org/x/crypto` | v0.54.0 | v0.55.0 | no |\n| `golang.org/x/text` | v0.40.0 | v0.41.0 | no |\n\nAll three are **raises**, and `make check` is green -- the workspace is internally consistent and every checksum resolves (verified on a **cold module cache** with `-mod=readonly`: build and codegen both clean, generated output byte-identical). So this is a **legibility** defect, not a broken build: a reviewer reading `shared/go/go.mod` sees `x/net v0.56.0` and cannot see that the binary links v0.58.0.\n\n## Why it was not fixed in TKT-264\n\n`go work sync` there would have turned a 3-file security fix into a **14-file, 7-module** realignment that also sweeps in the unrelated pre-existing churn above -- scope creep on a CVE fix, and exactly the \"fixing incidental findings in the PR bloats the diff\" case. The CVE fix does not depend on it.\n\n## The open question this ticket must answer\n\nNot \"run `go work sync`\" -- that is one candidate answer. **Should the gate check the build list at all, and if so how, given ADR-035 explicitly rejected `go work sync && git diff --exit-code` as the implementation on three measured grounds** (~5 min cold, mutates the tree during the gate, and `git diff` cannot see untracked sum files)?\n\nOptions to weigh:\n1. Extend the gate with a read-only build-list comparison (`go list -m` per module vs declared) -- needs a cost measurement against TKT-42's gate budget.\n2. Adopt periodic `go work sync` as maintenance outside the gate, with an ADR amendment saying manifest lag is accepted between syncs.\n3. Accept it explicitly and document the limit in ADR-035's \"What this guarantees\" register, which already has the right shape for saying what is *not* claimed.\n4. Revisit ADR-035 Option 1 (collapse to one module), which makes both drift directions unrepresentable. The ADR says to reconsider if the check's maintenance cost exceeds the 61-path move.\n\n**Acceptance Criteria:**\n- [ ] A decision, recorded as an ADR amendment to ADR-035, on whether build-list lag is gated, maintained, or accepted.\n- [ ] If gated: the check runs inside TKT-42's budget, does not mutate the tree, and sees untracked sum files -- the three grounds ADR-035 rejected the naive version on.\n- [ ] The pre-existing `access`/`commerce` churn above is either committed or explicitly declared acceptable. It must stop being invisible either way.\n- [ ] Whatever is decided, ADR-035's guarantee register says which drift direction is closed and which is not, naming the adversary/case per ADR-021's house rule.\n\nQuality gates: `make check` green; if the gate changes, `scripts/gate-selftest.sh` gains a positive control **and** a seeded negative, per ADR-035's own precedent.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Shaped — DoR 8/8 `met`. The open question is costed; the decision itself is Planning's.\n\nThe ticket asks a genuine question rather than proposing a fix, so shaping's job here was to\n**measure the options**, not to pick one. All numbers below are from execution at `afa333f4` on a\nclean tree.\n\n**1. The defect reproduces.** A read-only probe (declared `require` vs `go list -m` selected)\nfinds **14 lagging pairs across 7 modules**, 3 distinct dependencies — exactly the ticket's table:\n\n| dependency | declared | selected | modules affected |\n|---|---|---|---|\n| `golang.org/x/crypto` | v0.54.0 | **v0.55.0** | 6 |\n| `golang.org/x/text` | v0.40.0 | **v0.41.0** | 7 |\n| `golang.org/x/net` | v0.56.0 | **v0.58.0** | `shared/go` |\n\n`make check` is green throughout. Legibility defect, as filed.\n\n**2. The cost question ADR-035 left open is now answered — and it does not go the way option 1 hopes.**\n\nADR-035 rejected `go work sync && git diff --exit-code` on three measured grounds. A read-only\n`go list -m` comparison clears two of them and **fails the third**:\n\n| ADR-035's ground | `go work sync && git diff` | read-only `go list -m` |\n|---|---|---|\n| ~5 min cold / may need network | ~5 min, may need network | **22s cold — but still needs the network** |\n| mutates the working tree | yes | **no — clears** |\n| `git diff` blind to untracked sum files | yes | **n/a — clears** |\n\nThe decisive measurement:\n\n```\nwarm:                     0.18s   ✅\ncold cache + network:     22s, 315MB, 35 zips\ncold cache + GOPROXY=off: FAILS — \"module lookup disabled by GOPROXY=off\"\n```\n\nThe shipped checker's `go mod edit -json` is **fully offline**; `go list -m all` is **not**, and\nnarrowing the query to the three paths does not help — it still walks the whole graph. So gating on\nthe build list would trade ADR-035's offline property for a check that closes a *legibility* defect.\nThat is the trade Planning has to rule on, and it is now a measured trade rather than a guess.\n\n**⚠️ Measurement trap, recorded because it cost a cycle.** Running `go list -m` under an isolated\n`GOMODCACHE` makes Go re-download the **toolchain** (`go.work` needs 1.26.6; local `go` is 1.26.0).\nTwo cold runs failed on `toolchain not available` and `requires go >= 1.26.6` — which reads exactly\nlike the module graph failing offline, and would have produced a confidently wrong conclusion in\neither direction. Seed the toolchain into the cold cache first. This is in the mémo's `gotchas`.\n\n**3. Recommendation carried into Planning — explicitly not a settled decision.**\n\nLean **Option 3-plus**: accept build-list lag, document it in ADR-035's guarantee register, and add\na **read-only reporting script that is not a gate**. Reasoning: the defect is that a reviewer\n*cannot see* what is linked; that is solved by something a reviewer can run, not by failing a build.\nMaking the gate network-dependent to fix a cosmetic problem spends a real property on a cosmetic\none.\n\nThe ticket's deliverable **is** the decision, so `plan-review` must be free to reject this and pick\n1, 2, or 4. `approach` is `met` because the option space is enumerated and costed — not because the\nanswer is pre-ordained. Recording it this way so the recommendation is visibly a hypothesis, per\nAGENTS.md's rule that a proposed remedy is a hypothesis until executed.\n\n**4. COS — filed set kept, third one sharpened.**\n\nThe pre-existing `access`/`commerce` churn (`go work sync` adding `kin-openapi`, `gorilla/mux`,\n`cespare/xxhash` etc.) must stop being invisible **either way**. Sharpened: if the decision is\n*accept*, that churn is declared acceptable **in the ADR amendment with the list named**, so a\nfuture reader sees it was seen — not merely left. Silence is what produced this ticket.\n\n**5. `risk:low` — NOT claimed.**\n\nThe deliverable is an architectural **decision** amending an accepted ADR, which is precisely the\ncase SKILL.md says must keep the plan gate (*\"if the change needs a decision or a plan, it's a\nticket\"*, and TKT-62's precedent: a wrong recommendation gets reversed at the plan gate). It may\nalso touch `Makefile`/`scripts/` — CI-adjacent surface, one of `risk:low`'s named carve-outs. The\nimplementation is small; the judgement is not, and the label gates the judgement.\n\n**Gate 1 taken by the human** (owner instruction, this session) → `Ready`.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — `TKT-265-go-build-list-lag` off `origin/main` @ `afa333f4`\n\n**Running gateless per `config.gates: \"autonomous\"`.** Gate 1 was taken by the owner in-session\n(explicit instruction to shape and promote); gates 2–4 are the agent's per § Autonomous mode.\n\n**Blockers:** none. The single link is `found-in: TKT-264`, which merged at `aaed8e95`.\n\n**Selection reason:** owner-directed. It was also the top unshaped item and its subject — a gap in\nthe gate itself — compounds while unaddressed: every dependency bump lands against manifests that\nalready misdescribe the build.\n\n**Risk verdict: `risk:low` NOT claimed.** Stated explicitly per the label's carve-outs rather than\nleft silent. Two independent reasons, either sufficient:\n\n1. The deliverable is an **architectural decision** amending an accepted ADR (ADR-035). SKILL.md\n   draws the line at *needs a decision* vs *needs writing down* — this is the former, and TKT-62 is\n   the precedent for why a wrong recommendation must meet the plan gate.\n2. It is likely to touch `Makefile` and `scripts/` — **CI/deploy config**, one of the four named\n   carve-outs.\n\nThe implementation will probably be small. The label gates the judgement, not the line count, so it\nstays off and Gate 2 runs.\n\n**Model routing** (`config.models`, declared not inferred):\n\n| stage | model |\n|---|---|\n| `plan` | `gpt-5.6-luna` (codex) |\n| `planReview` | `main-agent` |\n| `implement` | `main-agent` |\n| `aiReview` | `gpt-5.6-luna` (codex) |\n\nCross-model invariant checked both ways: `plan` (`gpt-5.6-luna`) ≠ `planReview` (`main-agent`) ✅,\nand `implement` (`main-agent`) ≠ `aiReview` (`gpt-5.6-luna`) ✅.\n"
    },
    {
      "stage": "planning",
      "kind": "blocker",
      "body": "<!-- sdlc:stage=planning kind=blocker -->\n### Mémo correction — ADR-035 filename (self-caught, non-blocking)\n\nThe `kind=readiness` mémo cited **`ADR-035-eight-go-modules-stay.md`**. That file does not exist.\nThe real path is **`docs/adr/ADR-035-go-module-dependency-declarations.md`** (verified with\n`git cat-file -e HEAD:...`). Corrected in `context.governingAdrs`.\n\nThis is exactly the failure `shaping.md` warns about — a mémo naming an ADR that does not resolve\nsends the drafter looking for a spec that isn't there. It reached the plan brief once. Impact was\nnil in practice: the drafter located the ADR by grepping content rather than by path. Recording it\nrather than silently fixing it, because the near-miss is the point — I asserted a filename from the\nADR's *subject matter* instead of from `ls`, and the shaping rule that says to verify exists\nprecisely because that reads as correct.\n\n### Additional measurement taken during Planning\n\n`go work sync` run on a clean tree at `afa333f4`, then reverted (tree verified clean after):\n\n- **0.6s warm**, 14 files across 7 modules, +49/−51.\n- **Every version change is a RAISE** to what the workspace already selects. Nothing is lowered.\n- **The build list is BYTE-IDENTICAL before and after** (`go list -m all` diffed) — the sync has\n  **zero runtime effect**. It only makes the manifests describe what was already being built.\n- Non-version churn is confined to two modules: `access` gains 7 requires, `commerce` gains\n  `gorilla/mux` and drops `kr/pretty`.\n\nThis matters for COS #3: the pre-existing churn is not a pending upgrade with risk attached, it is\nbookkeeping. Whichever option Planning picks, that fact belongs in the amendment.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — drafted by `gpt-5.6-luna` (codex `task`, effort high), per `config.models.plan`\n\nRead-only run, ~2m10s, conclusive. Verbatim below except the trailing codex session footer.\n\nIt independently caught the mémo's bad ADR filename (first line of its output) — an honest\ncross-check of the correction I had already posted as `kind=blocker`.\n\n**It disagrees with shaping's recommendation**, choosing **option 1 (gate it)** over shaping's\noption 3-plus (accept + report-only script), on the grounds that a report-only script \"would make\nthe defect visible only when someone elects to run it and would not change the guarantee register.\"\nThat disagreement is the point of the cross-model split and is adjudicated in the critique.\n\n---\n\n# TKT-265 execution plan\n\nVerified at `afa333f4` on `TKT-265-go-build-list-lag`, with a clean tree.\n\nThe brief names `docs/adr/ADR-035-eight-go-modules-stay.md`, but that file does not exist at HEAD. The governing ADR is [ADR-035-go-module-dependency-declarations.md](/home/mathieu/sources/ticketing_system/docs/adr/ADR-035-go-module-dependency-declarations.md:1).\n\n## Recommendation\n\nChoose option 1, with `go work sync` retained as the repair and maintenance command.\n\nAdd a separate, read-only build-list-lag check to `make check`. The supplied measurement makes the trade acceptable:\n\n- warm: 0.18s\n- cold cache with network: 22s and 315MB fetched\n- TKT-42's cited gate budget: roughly two minutes\n- current defect: 14 lagging pairs across seven modules\n\nThe check loses ADR-035's offline property. That cost is real. A clean offline environment with `GOPROXY=off` cannot produce a build-list verdict. However, the check closes a currently demonstrated legibility defect at the commit that introduces it. The existing horizontal checker remains manifest-only and offline.\n\nDo not choose a report-only script. It would make the defect visible only when someone elects to run it and would not change the guarantee register. Periodic `go work sync` remains useful, but it is remediation, not enforcement.\n\nOption 4 is not justified by the measured cost. ADR-035 already reserves module collapse for the case where checker maintenance exceeds the 61-path migration; this check has not reached that threshold.\n\n## Files to touch\n\n### Decision and documentation\n\n- [ADR-035](/home/mathieu/sources/ticketing_system/docs/adr/ADR-035-go-module-dependency-declarations.md:95)\n  - Amend the Decision section.\n  - Record the 22-second cold-cache measurement and the loss of offline operation.\n  - Add the vertical lag check to the consequences.\n  - Replace the guarantee register at lines 115–127 with the wording below.\n  - State that `go work sync` remains the repair command.\n\n- [docs/testing.md](/home/mathieu/sources/ticketing_system/docs/testing.md:3)\n  - Document the separate build-list-lag stage.\n  - Note that it resolves the workspace graph and may require the network.\n  - Keep the existing horizontal check described as offline.\n\n- [docs/technical-delivery-standards.md](/home/mathieu/sources/ticketing_system/docs/technical-delivery-standards.md:22)\n  - Change the dependency-declaration stage description from manifest-only horizontal checking to horizontal plus vertical checking.\n  - Update the gate-selftest description to mention both positive and seeded-negative controls.\n\n- [docs/conventions/dependencies-and-versions.md](/home/mathieu/sources/ticketing_system/docs/conventions/dependencies-and-versions.md:27)\n  - Document that `make check` rejects both conflicting declarations and declarations below the workspace-selected version.\n  - State that `go work sync` is still the repair operation.\n\n### Gate implementation\n\n- New `scripts/check-go-build-list-lag.sh`\n  - Read every module’s `require` declarations.\n  - Resolve the workspace-selected versions with `go list -m`.\n  - Fail when a declared version is lower than the selected version.\n  - Fail closed when a module or graph cannot be read or resolved.\n  - Never run `go work sync`, `go mod tidy`, `git diff`, or any other tree-mutating operation.\n  - Operate on filesystem paths directly, so untracked sum files are not invisible to the check.\n  - Keep this separate from [check-go-dependency-drift.sh](/home/mathieu/sources/ticketing_system/scripts/check-go-dependency-drift.sh:11), whose documented contract is offline manifest parsing.\n\n- [Makefile](/home/mathieu/sources/ticketing_system/Makefile:15)\n  - Add a `check-go-build-list-lag` phony target.\n  - Add it to `make check` beside `check-dep-drift`.\n  - Preserve the existing eight-module list at lines 4–6.\n\n- [scripts/gate-selftest.sh](/home/mathieu/sources/ticketing_system/scripts/gate-selftest.sh:100)\n  - Add a clean-baseline positive control.\n  - Add a seeded lagging declaration, for example lowering `services/access/go.mod`’s `golang.org/x/crypto` requirement below the selected `v0.55.0`.\n  - Add a failure case for an unreadable module if the new checker reads modules independently.\n  - Preserve the existing horizontal-drift positive, negative, and unreadable-module cases.\n\n### Manifest alignment\n\nCommit the complete expected `go work sync` result before adding the positive control. Otherwise the current 14 lagging pairs would make the clean baseline fail.\n\nThe affected module paths are the eight modules listed in [go.work](/home/mathieu/sources/ticketing_system/go.work:3):\n\n- `gateway/go.mod`, `gateway/go.sum`\n- `services/access/go.mod`, `services/access/go.sum`\n- `services/catalog/go.mod`, `services/catalog/go.sum`\n- `services/commerce/go.mod`, `services/commerce/go.sum`\n- `services/inventory/go.mod`, `services/inventory/go.sum`\n- `services/payments/go.mod`, `services/payments/go.sum`\n- `shared/go/go.mod`, `shared/go/go.sum`\n- `smoke/go.mod`, `smoke/go.sum`\n\nOnly files actually changed by `go work sync` should be staged. `go.work` and `go.work.sum` are not intended edits.\n\nThe pre-existing churn is committed, not accepted as invisible:\n\n- `services/access/go.mod` gains the listed OpenAPI/schema-related requirements.\n- `services/commerce/go.mod` gains `cespare/xxhash/v2`, `go-logr/logr`, `go-logr/stdr`, and `gorilla/mux`, and removes `kr/pretty`.\n- The corresponding sum changes are committed with the manifests.\n\n## Exact ADR guarantee-register amendment\n\nReplace the existing register with:\n\n```markdown\n### What this guarantees, and against whom\n\n- **Closed:** For an honest maintainer or Dependabot, `make check` rejects both forms of\n  accidental declaration drift covered by this ADR: two modules declaring different versions of\n  the same dependency, and a module declaring a version lower than the version selected by the\n  current `go.work` MVS build list. The latter is closed only when the workspace graph can be\n  resolved; inability to read a module or resolve the graph fails the check rather than producing\n  a clean result.\n\n- **Not closed, and not closeable here:** An actor who changes a `go.mod`, `go.work`, checker,\n  or dependency-resolution inputs together can make the repository accept its own chosen result.\n  These checks constrain accidental maintainer and Dependabot drift. They do not constrain\n  intentional edits to the repository or its build environment.\n\n- **Not claimed:** This does not create runtime version isolation or make the eight modules\n  independently reproducible outside the workspace. Supported builds already resolve one MVS list\n  through `go.work`. It also does not make the gate offline or guarantee that every selected\n  dependency appears in every module's manifest. The build-list check may require the module\n  cache or network access.\n```\n\n## Test plan\n\nThe decision itself is tested by document review. The implementation is testable at the gate boundary:\n\n1. After manifest alignment, `make check-go-build-list-lag` passes.\n2. The clean-baseline `expect_pass` catches an always-failing checker.\n3. Lower one declaration while another module keeps the selected version. The seeded negative must fail.\n4. Corrupt a non-first module manifest. The new checker must fail rather than report no lag.\n5. Run the checker with an untracked sum file present in the disposable worktree. Confirm it neither deletes nor ignores filesystem state through a `git diff`-based shortcut.\n6. Confirm the checker leaves the worktree unchanged.\n7. Run `make check`.\n8. Run `./scripts/gate-selftest.sh`.\n9. Confirm generated files are unchanged and the final tree is clean.\n\nNo test can make the new check offline. That limitation must remain explicit in ADR-035.\n\n## Risks\n\n- Offline developers lose the current guarantee that the dependency precheck works with `GOPROXY=off`.\n- A proxy outage or cold cache causes a gate failure unrelated to source correctness. The checker must report resolution failure distinctly and fail closed.\n- `go list -m` walks the full graph even for narrow queries, so future dependency growth may increase gate cost.\n- The checker closes accidental lag, not malicious or coordinated edits.\n- Future readers may assume all manifests exactly mirror the workspace build list. The ADR must explicitly say that only lower declared versions are rejected; absent declarations remain valid.\n- Committing `go work sync` output adds indirect-requirement churn, but leaving the access/commerce changes uncommitted would make future maintenance recreate an unexplained diff."
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan critique + finalization — critique by `main-agent` per `config.models.planReview`\n\nAdversarial pass on the `gpt-5.6-luna` draft. Every claim below was checked against the code at\n`afa333f4`; the two that mattered were settled by **running them**, not by reading. This is a\n**delta** — the `kind=plan` draft as amended here is the plan.\n\n---\n\n## Verdicts on the draft\n\n### ACCEPTED — the recommendation (option 1, gate it). My shaping recommendation is overturned.\n\nShaping leaned option 3-plus (accept + report-only script) on the grounds that gating would trade\nthe checker's **offline** property for a merely cosmetic gain. The draft rejected that. **The draft\nis right and shaping was wrong**, and the reason is a fact shaping did not check:\n\n`make check`'s first prerequisite is `deps: pnpm install --frozen-lockfile` (`Makefile:17,20-21`),\nwhich runs **before** `check-dep-drift`, and whose own comment reads *\"self-contained gate: clean\nclone needs nothing pre-installed\"*. `check-generate` then runs `go tool oapi-codegen`. **The gate\nalready requires the network on a cold machine.** The offline property belongs to *one script*, not\nto the gate, and no CI job, Make target, or developer doc anywhere in the repo sets `GOPROXY=off`\nor claims an air-gapped workflow — I grepped `.github/workflows/`, `Makefile`, `scripts/` and\n`docs/development.md`. \"Offline\" is asserted in two code comments and the ADR; it is not a tested\nor depended-upon invariant.\n\nSo the trade shaping feared is not the trade on offer. Gating costs the *new script's* offline\ncapability, which nothing consumes, and the existing horizontal checker keeps its own offline\nproperty untouched because the draft **correctly keeps the two scripts separate**. Recording this\nas a reversal rather than quietly adopting it: shaping's `approach` note is superseded, and the\nmémo should be read with that correction attached.\n\n### ACCEPTED — separate script, not an extension of `check-go-dependency-drift.sh`.\n\nRight for a reason the draft states only in passing: that script's contract is *manifest-only,\noffline, no workspace resolution* and it is documented as such in its header, the `Makefile:45`\ncomment, and ADR-035's Decision. Folding a graph-resolving check into it would falsify all three.\n\n### ACCEPTED — guarantee-register wording, with one amendment (below).\n\nIt follows ADR-021's closed / not-closed / not-claimed shape and correctly declines to overclaim.\n\n### REJECTED — the seeded negative. It cannot prove what it claims.\n\nThe draft proposes seeding by *\"lowering `services/access/go.mod`'s `golang.org/x/crypto`\nrequirement below the selected `v0.55.0`\"*. **Executed — it fires the OLD checker:**\n\n```\n$ (cd services/access && go mod edit -require=golang.org/x/crypto@v0.50.0)\n$ ./scripts/check-go-dependency-drift.sh <8 modules>\ngo dependency drift: the same dependency is declared at different versions\n  golang.org/x/crypto\n    services/access    v0.50.0\n    services/catalog   v0.54.0\n    ... (4 more)\n```\n\n`x/crypto` is declared by **six** modules, so lowering one diverges it horizontally. `check-dep-drift`\nruns **earlier in the `check` chain** than any new stage would. The seed would go red for the wrong\nreason and the new checker would never execute — AGENTS.md exactly: *\"a mutation caught by a lower\ntier proves the mechanism is live, not that your test caught it. The mutation that is evidence is\nthe one **only** the new tier can catch.\"*\n\n**Replacement, executed and verified isolated:** seed `shared/go`'s `golang.org/x/net`.\n\n```\n$ (cd shared/go && go mod edit -require=golang.org/x/net@v0.50.0)\n$ ./scripts/check-go-dependency-drift.sh <8 modules>\ngo dependency drift: none across 8 module(s)      <-- horizontal checker silent\n$ go list -m golang.org/x/net  ->  v0.58.0        <-- but declared v0.50.0: real lag\n```\n\n`x/net` is declared by exactly one module (`shared/go/go.mod:19`) — verified by grep across all\neight manifests — so it cannot diverge horizontally by construction. This is a mutation **only** the\nnew check can catch.\n\n### AMENDED — manifest alignment must be its own commit, and the ordering is load-bearing.\n\nThe draft says to commit the `go work sync` result \"before adding the positive control\". Correct,\nbut understated: with 14 lagging pairs live, a clean-baseline `expect_pass` would fail on the\n**current** tree, so the alignment is a **precondition of the selftest**, not housekeeping. Make it\ncommit 1, alone, with the measurement in the message.\n\nI re-measured the sync rather than inheriting the ticket's figures (run on a clean tree, then\nreverted; tree verified clean):\n\n- **0.6s warm**, 14 files / 7 modules, **+49 / −51**.\n- **Every version change is a RAISE** to what the workspace already selects. **Nothing is lowered.**\n- **The build list is BYTE-IDENTICAL before and after** — `go list -m all` diffed, zero difference.\n\nThat last point is the one the ADR amendment most needs and neither the ticket nor the draft states:\n**the alignment has zero runtime effect.** It is not a deferred upgrade carrying risk; it is\nbookkeeping that makes the manifests describe a build that is already happening. It also mirrors\nADR-035's own original realignment, which recorded the identical property.\n\n### AMENDED — `gateway/go.sum` is already tracked; drop it from the file list.\n\nThe draft lists `gateway/go.sum` among files to stage. It is **already tracked and empty** (`git\nls-files gateway/` confirms; 0 bytes) — ADR-035 committed it precisely so `go work sync` is\nidempotent. `go work sync` does not modify it now. Listing it invites a confusing empty entry.\nCorrespondingly, ADR-035's **third** rejection ground against the naive approach (*\"git diff cannot\nsee the untracked gateway/go.sum\"*) is **already neutralized** and should be recorded as historical\nin the amendment rather than restated as live.\n\n### REJECTED — test-plan item 5 (\"run the checker with an untracked sum file … confirm it neither\ndeletes nor ignores filesystem state through a `git diff`-based shortcut\").\n\nThis tests a shortcut the plan does not take. The new checker never invokes `git`, so there is no\nmechanism for it to be blind to untracked files — the assertion is unfalsifiable against the design\nand would be a test that cannot fail. Dropped. The genuine property (the checker does not consult\ngit at all) is better pinned by *reading* the script, or by the far cheaper invariant in item 6.\n\n### CAUTION — a `go mod edit` seed dirties `go.work.sum`.\n\nObserved during the executions above: after seeding and `git checkout -- shared/go/go.mod`, the tree\nstill showed `M go.work.sum`. `scripts/gate-selftest.sh` already ends each case with\n`git checkout -- . && git clean -fdq`, which covers it, but the new case must not assume the\nmanifest checkout alone restores the tree.\n\n---\n\n## Introduced at plan-review, reviewed by no second model\n\nPer the skill's required section — design elements *I* added, which reach Gate 2 reviewed by nobody\nbut their author. Named again in the Gate 3 hand-off.\n\n1. **The `x/net` / `shared/go` seed** replacing the draft's `x/crypto` seed. Executed both\n   directions (old checker silent; lag real), but the *choice* is mine.\n2. **The \"build list is byte-identical\" measurement** and the argument that alignment is\n   zero-risk bookkeeping. Measured by me, unreviewed by a second model.\n3. **The \"the gate already needs the network\" refutation** that overturned shaping's option 3-plus.\n   Grep-verified, but it is the load-bearing premise of the whole recommendation and one model\n   asserted it.\n4. **Dropping test-plan item 5** and the `gateway/go.sum` entry.\n\n## Decisions taken by the agent at Gate 2 (autonomous mode)\n\n`config.gates: \"autonomous\"`, so Gate 2 is mine. Open decisions and how each was resolved:\n\n| decision | resolution | rationale |\n|---|---|---|\n| Which of ADR-035's 4 options | **Option 1 — gate it** | Draft's recommendation; shaping's objection refuted by execution (gate is already networked). Also the most *reversible* option: deleting one script + one Make prerequisite undoes it, exactly as ADR-035 says of its own Option 2. |\n| Gate it vs report-only script | **Gate** | The critique's rule of preference is the more reversible option; both are, so the draft's argument decides — a report-only script does not change the guarantee register, and an unrun script is how this defect survived in the first place. |\n| Commit the churn vs declare it acceptable | **Commit it**, own commit, measurement in the message | COS #3 allows either, but \"byte-identical build list\" makes committing nearly free, and leaving it means every future `go work sync` regenerates an unexplained 14-file diff. |\n| Collapse to one module (Option 4) | **No** | ADR-035 reserves it for when checker maintenance exceeds the 61-path move. Two small scripts is not that. Out of this ticket's scope regardless. |\n\n## The plan, as amended\n\n**Commit 1 — manifest alignment.** `go work sync`; stage only the 14 files it changed\n(`go.work`/`go.work.sum` are not intended edits). Message records: 0.6s, +49/−51, every change a\nraise, **build list byte-identical**.\n\n**Commit 2 — the check.** New `scripts/check-go-build-list-lag.sh`: read each module's `require`\nset, resolve selected versions via `go list -m`, fail when declared < selected. **Fail closed** on\nan unreadable module or unresolvable graph, and reuse the shipped script's guards — no piped loop\n(`scripts/check-go-dependency-drift.sh:30-40` documents why), explicit per-module status checks,\ntruncation check. Never mutate the tree; never shell out to `git`. `Makefile`: `check-go-build-list-lag`\nphony target added to `check` beside `check-dep-drift`.\n\n**Commit 3 — selftest.** `expect_pass` clean-baseline positive control; `expect_fail` seeded with\n**`shared/go` `x/net` lowered to v0.50.0**; `expect_fail` unreadable-module case. All four existing\nhorizontal cases preserved and must stay green.\n\n**Commit 4 — docs + ADR.** ADR-035 amendment (register wording below), `docs/testing.md`,\n`docs/technical-delivery-standards.md`, `docs/conventions/dependencies-and-versions.md`.\n\n**Test plan (amended):** (1) after alignment, the new target passes; (2) positive control catches an\nalways-failing checker; (3) the `x/net` seed fails the **new** target while `check-dep-drift` stays\ngreen — *both* asserted, since that pairing is the whole proof of isolation; (4) corrupt a non-first\nmodule → fail closed, not \"no lag\"; (5) *[dropped — see above]*; (6) tree unchanged after a run;\n(7) `make check`; (8) `./scripts/gate-selftest.sh`; (9) generated files unchanged, tree clean.\n\n**Guarantee register:** the draft's wording, with one addition to *Not claimed* — that the\nalignment committed here changed **no selected version**, so the register is not asserting any\nruntime change — and *Not closed* noting ADR-035's untracked-sum ground is historical, since\n`gateway/go.sum` is now tracked.\n\n**DoD:** all four COS met; `make check` green; `gate-selftest.sh` green; tree clean; no version\nraised or lowered beyond what `go work sync` produces; ADR-035 amended in place (not superseded —\nthis refines one section, it does not reverse the decision).\n\n**Risk carried forward:** a proxy outage or genuinely cold cache now fails this stage for reasons\nunrelated to source correctness. Accepted because `deps` already has that property; the checker must\nreport resolution failure **distinctly** from lag so the failure is diagnosable.\n\n→ Gate 2 taken. `Planning` → `Building`.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation complete — PR [#309](https://github.com/mathd/ticketing_system/pull/309). Implemented by `main-agent` per `config.models.implement`.\n\nFour commits, TDD (selftest cases written and observed red before the checker existed).\n\n| commit | what |\n|---|---|\n| `75e1c367` | Align 14 manifests with the build list (`go work sync`) |\n| `3907c3c5` | `scripts/check-go-build-list-lag.sh` + `make check-build-list-lag` |\n| `c6209a73` | Selftest: a seed only the new stage can catch |\n| `a3e7157c` | ADR-035 amendment + 3 docs + seed-vacuity fix |\n\n**Diff:** +289 / −63 across 21 files (14 of them mechanical manifest realignment).\n\n### Mutation checks — run, not asserted\n\n| mutation | result |\n|---|---|\n| Delete the lag comparison, keep the seed | reports \"none\", **exit 0** — so the `expect_fail` observes the comparison |\n| Restore it | **exit 1** |\n| Seed `x/net`, run `check-dep-drift` | **green** — the two stages are provably distinct |\n| Remove the seed, run the presence guard | **fires** — the isolation assertion cannot pass vacuously |\n\nThe last row is a defect I introduced and then caught: `expect_pass \"seed does not disturb\ncheck-dep-drift\"` is equally true of a tree where the seed silently failed to apply. Added a\npresence guard and verified the guard itself can fail — otherwise it would have been one more\nassertion that cannot go red, which is this repo's most-repeated defect shape.\n\n### Gate\n\n`./scripts/gate-selftest.sh` → **all seeded errors caught**; four new cases green, all pre-existing\ncases still green.\n\n`make check` → **green through `build`**; `smoke` **could not run: `docker: command not found`.**\n\nReported as a limitation, not a pass. Judged from the sentinel (`EXIT=2`) and the log body\n(`make: *** [Makefile:159: smoke] Error 127`), **not** the harness's reported status — which said\nexit 0, exactly the divergence AGENTS.md documents. Docker is not installed on this box, so `smoke`\ncannot run here at all; `scripts/smoke.sh:35` calls `docker compose` unconditionally. The branch\ntouches no Go source, no TS, no compose file and no Dockerfile, so there is no mechanism by which it\ncould affect the smoke stack. CI runs the full gate.\n\nStages that did pass: `check-generate`, `check-dep-drift`, `check-build-list-lag`,\n`check-security-workflow-trigger`, `check-hermetic-workflow-trigger`, `check-adr-numbers`,\n`check-markdown-links`, `lint` (8 modules), `test` (7), `build` (8).\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### ai-review pass 1 — `gpt-5.6-luna` (codex `adversarial-review`, branch vs `origin/main`), per `config.models.aiReview`\n\nConclusive run, verdict **needs-attention**, three findings. Reviewer given the diff only — no plan,\nno reasoning — and primed to assume the diff is wrong.\n\n**All three verified against the revision under review by execution. All three are REAL. Two were\ndefects I introduced; one was a latent fail-open path. None were rejected.**\n\n---\n\n#### F1 [high] — the checker mutated `go.work.sum`. CONFIRMED, FIXED.\n\n> *`go list -m` runs without `-mod=readonly` … a cold or incomplete cache can leave modified\n> `go.sum` files … contradicts ADR-035's claim that the check never mutates the working tree.*\n\nReproduced on a tree seeded with a requirement whose checksum is not cached:\n\n```\n$ (cd shared/go && go mod edit -require=golang.org/x/net@v0.50.0)\n$ git checkout -- go.work.sum          # isolate the checker from the seed's own write\n$ md5sum go.work.sum > before\n$ ./scripts/check-go-build-list-lag.sh <8 modules>\n$ md5sum -c before\ngo.work.sum: FAILED\n```\n\nThis is **the exact property `go work sync && git diff` was rejected for** in ADR-035's original\nDecision — I reintroduced it in the check written to avoid it, and then asserted the opposite in the\nADR. The reviewer caught a false claim in a document whose entire purpose is to state what is\nguaranteed.\n\n**The reviewer's recommended fix does not work.** `-mod=readonly` — its specific suggestion — does\n**not** prevent the write: the target is `go.work.sum`, the *workspace* sum file, which that flag\ndoes not govern. Verified directly, and again via `GOFLAGS=-mod=readonly`. Recording this because\nadopting the recommendation as given would have produced a green test over a live defect.\n\n**Actual fix:** every `go list -m` now runs with `GOWORK` pointed at a **copy** of `go.work` in the\nscript's temp dir (with `use` paths absolutized), so any checksum write lands in a directory that is\ndiscarded. `-mod=readonly` kept as defence in depth.\n\nA second, subtler instance: my own `replace`-detection probe (added for F2) ran *before* the copy\nexisted and mutated the sum by itself. Found only by bisecting which `go list` call was responsible\nafter the first fix appeared not to work. The copy is now created before any `go list` runs.\n\nVerified fixed: same seeded tree, lag still detected, `go.work.sum: OK`.\n\n#### F2 [medium] — a `replace` makes the comparison a false guarantee. CONFIRMED, FIXED.\n\n> *A module can declare `example.com/foo v1.2.0` while `go.work` or a manifest replaces that with\n> … a local directory. The strings can match and the stage passes, while the build uses different\n> source.*\n\nConstructed it:\n\n```\n$ printf 'module golang.org/x/net\\n\\ngo 1.26\\n' > /tmp/fakenet/go.mod\n$ (cd shared/go && go mod edit -replace=golang.org/x/net=/tmp/fakenet)\n$ go list -m golang.org/x/net   ->  v0.58.0   (unchanged!)\n$ ./scripts/check-go-build-list-lag.sh <8>  ->  \"none across 8 module(s)\"\n```\n\nVersion matches, checker green, build links a stub. My ADR text said the manifests \"describe what\nis built\" — under a `replace` that is false.\n\n**Fix:** the check now **refuses to report a verdict** (exit 2) when any `replace` is in effect,\nnaming the offending module, rather than reporting a guarantee it cannot support. Chosen over\n\"account for replacements\" because the honest answer for a guard is to decline the question it\ncannot answer; the ADR register now scopes the guarantee to unreplaced modules explicitly. There are\nno `replace` directives in the workspace today (checked).\n\n#### F3 [medium] — an absent declaration was treated as safe. CONFIRMED (latent), FIXED.\n\n> *If a declared requirement is absent from `selected`, the checker silently continues … a\n> partial-but-successful resolver output … can make a lagging declaration disappear from comparison\n> and produce PASS.*\n\nLatent, not live: every declaration in all eight modules resolves today, so the `continue` never\nfires — verified by reproducing the comparison and counting skips (zero). But it is a **fail-open**\npath in a guard, and this ticket exists precisely because a guard failed silently. AGENTS.md's rule\napplies directly: *ask of any guard predicate what its unknown case does; if unknown means pass, the\nguard is a suggestion.*\n\n**Fix:** uncompared declarations are collected and the stage **refuses** rather than continuing.\nVerified the guard fires by feeding the comparison a truncated selected list (old code: pass; new\ncode: 1 uncompared entry, refuse). Duplicate paths in the build list now also refuse, since the\nfirst-match lookup would silently pick one.\n\n---\n\n### Fixes are non-trivial → a second pass is required\n\nPer the skill's rule: two blocking findings fixed, new control flow, a new refusal path, and a new\ntest. The fix diff is code no one has reviewed. Second pass will judge **the fix diff**, not the\noriginal.\n\n### Test coverage added with the fixes\n\nBoth new cases mutation-checked, because a test written while fixing a finding gets the same check\nas one written from the plan — and this is where that discipline usually lapses:\n\n- **8d — the stage leaves `go.work.sum` unchanged**, asserted on a tree with an incomplete cache\n  (the only state where it fails). **Mutation-checked:** removing the `GOWORK` export makes the\n  mutation return and 8d catches it.\n- **8e — a `replace` in effect must fail the stage.**\n\n### Note on the fix I did not adopt as written\n\nF1's stated remedy (`-mod=readonly` + a regression test) would have produced a passing regression\ntest over a still-mutating checker, because the flag does not govern the file being written. The\nfinding was right; its recommendation was not. Verifying the fix rather than the finding is what\nseparated them.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### ai-review passes 2–4 — `gpt-5.6-luna`, per `config.models.aiReview`\n\nFour passes total. **Findings 3 → 3 → 1 → 1, all real, none rejected.** Each pass judged the\nprevious pass's *fix diff*, not the original PR.\n\n#### Churn cap — why four passes were allowed\n\nThe rule caps at two per *stable* diff, resets when a pass invalidates the previous fix at the root,\nabsolute cap 4, exceeded only on demonstrated convergence. Four were run, and the cap now **holds**:\n\n| pass | findings | invalidated the previous fix at its root? |\n|---|---|---|\n| 1 | 3 | — |\n| 2 | 3 | **yes** — `sed` copy → Go's parser; `go list` replace-detection → declarations |\n| 3 | 1 | **yes** — JSON regeneration → verbatim copy + path-only edits |\n| 4 | 1 | **yes** — the verdict-based godebug assertion → inspect the copy |\n\nStrictly decreasing after pass 2, and no pass re-litigated settled ground: every round replaced a\n*mechanism* that the next round proved wrong, which is convergence, not churn. **No fifth pass.**\n\n---\n\n### Pass 2 — 3 findings (fixed in `45b85b6d`, `a9586420`)\n\n- **[high] `sed` rewrite of `use` paths handled only the `./x` form.** A bare `use gateway` left the\n  copy's paths relative. Consequence was fail-closed (the temp dir holds no modules, so resolution\n  errors) rather than a wrong verdict — but failing closed on a *valid* workspace is still broken.\n  Replaced with generation from `go work edit -json`.\n- **[medium] workspace-level `replace` was a FALSE NEGATIVE.** `go list -m all` reports a\n  replacement only for a module *already in the build list*, so `replace example.com/dep =>\n  ./localdep` for a module nothing requires was invisible and the stage reported \"none\". Detection\n  now reads the **declarations** (`go.work` + every `go.mod`). Verified against both forms.\n- **[high] case 8d discarded the run's exit status**, so any early failure looked identical to a\n  clean non-mutating run. Now requires exit **1 exactly** — 0 is \"no lag\" and 2 is \"refused\", both\n  reachable without the graph ever resolving.\n\n### Pass 3 — 1 finding (fixed in `6b37803c`). Found independently at the same time.\n\n**[medium] regenerating `go.work` from JSON drops directives.** `go work edit -json` exposes only\n`Go`, `Use`, `Replace` — so a `godebug` line vanished from the copy (original 1, copy 0) while the\nfidelity guard, comparing module *directories*, passed straight over it. A `godebug` changes build\nsemantics, so the copy would have described a different build while claiming fidelity.\n\nEnumerating directives to preserve is the same mistake one level up — the next Go release adds one\nand it silently breaks again. The copy is now the **original file** with only its filesystem paths\nredirected through `go work edit`. Nothing can be dropped because nothing is regenerated.\n\nTwo mechanical facts, each established by execution after a wrong guess, both now in the code:\n`-dropuse` matches the path **as written** (neither `./x` nor the absolute form removes a bare\n`use x`; both leave a duplicate), and edit arguments must travel through a **NUL-delimited file** —\nbash command substitution strips null bytes, re-splitting any path containing whitespace.\n\n### Pass 4 — 1 finding, **already fixed before the review returned** (`8d9d330f`)\n\n**[medium] case 8g was vacuous.** It asserted a `godebug`-bearing workspace still checks clean —\ntrue under **both** implementations, because dropping the directive does not stop the graph\nresolving. Green either way; the regression that motivated the entire commit was unprotected.\n\nI found this myself while probing my own diff and had fixed it before the pass reported it; the\nreviewer's recommended remedy — *\"reproduce the rewrite and assert the copied go.work still contains\nthe directive\"* — is what was implemented. Verified to **discriminate**: green under the\nverbatim-copy mechanism, red under the JSON-regeneration one it replaced.\n\nThe pass otherwise states the rewrite is *\"fail-closed for the reviewed edge cases\"* and\n*\"ADR-035 is not materially stale\"*. No blocking finding survives.\n\n---\n\n### Three vacuities found in my OWN tests, all written under fix-momentum\n\nRecording these because the pattern is the ticket's real lesson: every one names the right case, is\nwell-commented, and cannot fail. All three were in tests written *while fixing a finding* — exactly\nwhen AGENTS.md says the discipline lapses.\n\n1. **8d compared two MD5s that are both empty when `go.work.sum` is absent.** Empty equals empty →\n   passes while observing nothing. Presence now asserted first.\n2. **The fidelity guard compared two lists built with stderr discarded.** Both empty on failure,\n   `cmp` says equal, guard passes. Non-emptiness now checked first.\n3. **8g asserted a verdict blind to the property it pinned** (above).\n\n### Known limit, recorded rather than closed\n\nA hand-written bare `use gateway` (no `./`) cannot be dropped by any spelling `go work edit`\naccepts, so the stage **refuses** on such a workspace instead of checking it. Fail-closed and loud,\nnot a wrong verdict, and unreachable through Go's own tooling — `go work use m` writes `use ./m`\n(verified). Closing it would mean hand-parsing `go.work`, which is the class of mistake pass 3\nremoved. Named in the script and in `overrides:` at closeout.\n\n### Verification\n\n`./scripts/gate-selftest.sh` → **all seeded errors caught**, EXIT=0. Eight build-list-lag cases\ngreen; all pre-existing cases green.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### Merged as `96c2da06`. Gate 3 taken by the agent (`config.gates: \"autonomous\"`).\n\nReview-guide was posted on the PR **before** the merge, as the audit record.\nPre-merge assertion done: `git rev-parse HEAD` == `git rev-parse origin/<branch>` (`8d9d330f`),\ntree clean. Merge verified **by content on `origin/main`**, not by the PR badge — script present,\nMakefile target wired, ADR amendment landed, `shared/go` now declares `x/net v0.58.0`.\n\n### CI — the run that matters\n\n`check` **passed in 8m58s**, and that is the run carrying the `smoke` stage which could not execute\non the authoring machine (no docker). The one open verification gap is therefore closed by evidence\nrather than argument. `hermetic-smoke` 8m56s, `gate-selftest` 2m17s, 8 × `govulncheck`, all pass.\n\nPost-merge on `main`: `make check-build-list-lag` → *none across 8 module(s)*; `make check-dep-drift`\n→ *none across 8 module(s)*.\n\n### COS validation — each against evidence on the merged code\n\n**COS 1 — a decision recorded as an ADR-035 amendment.** ✅\n`docs/adr/ADR-035-go-module-dependency-declarations.md` §*Amendment (TKT-265)*. The decision is\n**gate it** (Option 1). Shaping had recommended Option 3-plus (accept + report-only script); the\nplan draft rejected that and was right — `make check`'s first prerequisite is\n`deps: pnpm install --frozen-lockfile`, so the gate already required the network on a cold clone.\nThe offline property belonged to one script, not the gate. Reversal recorded in `kind=plan-final`.\n\n**COS 2 — if gated, it must clear the three grounds ADR-035 rejected the naive version on.** ✅\n\n| ADR-035's ground | this check |\n|---|---|\n| ~5 min cold / needs network | 0.18s warm, 22s cold-with-network. Still not offline — **stated, not hidden**, and the trade is argued in the amendment |\n| mutates the working tree | **does not** — and this took a [high] finding and two attempts to actually achieve (see below) |\n| `git diff` blind to untracked sums | never consults `git`. That ground is also **historical**: `gateway/go.sum` has been tracked since ADR-035's own change |\n\n**COS 3 — the pre-existing churn stops being invisible.** ✅ Committed (`75e1c367`), not merely\ndeclared. 14 files / 7 modules, +49/−51. Every change is a **raise** to what the workspace already\nselected; nothing lowered, no major version moved; **the build list is byte-identical before and\nafter** (`go list -m all` diffed), so the realignment has zero runtime effect. The indirect churn is\nnamed in the commit and the amendment.\n\n**COS 4 — the register says which direction is closed, naming the adversary per ADR-021.** ✅\nBoth directions now named in *What this guarantees*. **Closed:** horizontal *and* vertical, for an\nhonest maintainer or Dependabot — the vertical half only when the graph resolves, since an\nunresolvable graph, unreadable manifest or truncated parse **refuses** (exit 2) rather than reporting\nno lag. **Not closed:** anyone editing a manifest and the checker together, or `go.work` and the\nchecker together; and **a `replace` is outside the guarantee entirely**. **Not claimed:** any runtime\nfix (there was none), that every selected dependency appears in every manifest, or that the gate is\noffline.\n\n**Quality gates.** `make check` green in CI including smoke; `gate-selftest` green with 8 new cases;\nmoney untouched (this branch changes no money path and no `.go` source at all).\n\n### What a PO should know, stated plainly\n\nThe ticket asked whether to gate, maintain, or accept. **It gates**, and the cost is real and\nrecorded: this stage needs the module cache or the network, so a proxy outage fails it for a reason\nunrelated to source correctness. That is why the failure is reported *distinctly* from lag.\n\nFour adversarial passes, eight findings, **all real, none rejected** — an unusually high hit rate,\nconcentrated in one place: every finding was my *guard* failing open or my *test* not observing what\nit named. The mechanism was replaced three times. Most notable: I reintroduced the exact\ntree-mutation property that got `go work sync && git diff` rejected in ADR-035, in the check written\nto avoid it, and asserted the opposite in the ADR — caught by review. The reviewer's recommended fix\n(`-mod=readonly`) **did not work**, because the write targets `go.work.sum`, which that flag does not\ngovern; adopting it verbatim would have shipped a passing regression test over a live defect.\n\nThree vacuous tests found in my own work, all written under fix-momentum. Residual risk is named in\n`overrides:` at closeout rather than smoothed over.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closed. Merged `96c2da06`, branch deleted. Gate 4 taken by the agent (`config.gates: \"autonomous\"`).\n\nDurations from the `sdlc-state` branch log (`git log -- .sdlc/tickets/TKT-265.json`).\n\n| stage | duration |\n|---|---|\n| Backlog (shaping) | ~20 min (filed 23:05 prev. day; shaped 22:25) |\n| Ready | ~1 min (22:25 → 22:26) |\n| Planning | 8 min (22:26 → 22:34) |\n| ├─ `agent:planning` (draft, `gpt-5.6-luna`) | ~2 min 10 s |\n| └─ `agent:plan-review` (critique, `main-agent`) | ~6 min |\n| Building | 43 min (22:34 → 23:17) |\n| ├─ `agent:coding` | ~16 min |\n| └─ `agent:ai-review` (4 passes) | ~27 min |\n| PO Review | ~2 min |\n| **human-wait total** | **n/a (autonomous)** |\n\n**diff:** +590 / −63 (21 files). 14 of them mechanical manifest realignment; the substance is one\nnew 190-line script, a Makefile target, 8 selftest cases and the ADR amendment.\n\n**CI:** `check` 8m58s ✅ (the only run where `smoke` executes), `hermetic-smoke` 8m56s ✅,\n`gate-selftest` 2m17s ✅, 8 × `govulncheck` ✅.\n\n---\n\n### overrides:\n\nMandatory on an autonomous run. Each point where a review pass stated a **blocking** objection and\nthe agent proceeded anyway, or where the agent took a decision a human gate would have held.\n\n1. **Gate 1 was taken by the owner in-session, not by the board's normal flow.** The skill reserves\n   Backlog → Ready for a human in both modes; the owner instructed it explicitly (\"shape and do\n   TKT-265, move to Ready by yourself\"). Recorded because the DoR was self-graded 8/8 by the same\n   agent that then claimed the ticket — no second party checked the readiness before work started.\n   *Residual risk carrier:* this ticket.\n\n2. **Shaping's recommendation was overturned by the agent, not by a human.** Shaping argued Option\n   3-plus (accept + report-only script); the plan draft argued Option 1 (gate it) and won on a fact\n   shaping had not checked. Gate 2 would normally be where a human adjudicates that disagreement.\n   The reversal is argued in `kind=plan-final`, but **one model proposed it and the same agent\n   accepted it**. *Residual risk carrier:* ADR-035's amendment, which is reversible by a follow-up.\n\n3. **A known limit was left open rather than closed.** A hand-written bare `use gateway` (no `./`)\n   cannot be dropped by any spelling `go work edit` accepts, so the stage **refuses** on such a\n   workspace instead of checking it. Fail-closed and loud, not a wrong verdict, and unreachable\n   through Go's own tooling (`go work use m` writes `use ./m` — verified). Closing it would require\n   hand-parsing `go.work`, the exact class of bug pass 3 removed. *Named in the script and the\n   review-guide. Residual risk carrier:* the script's `KNOWN LIMIT` comment.\n\n4. **The local gate never ran `smoke`.** Docker is not installed on the authoring machine, so\n   `make check` failed at `scripts/smoke.sh:35` (Error 127) on every local run. The agent proceeded\n   on the argument that the branch touches no Go/TS source, compose file or Dockerfile — then\n   **waited for CI rather than merging on that argument**, and CI's `check` passed. The gap is\n   closed by evidence; recorded because the decision to continue past a red local gate was the\n   agent's. *Residual risk carrier:* none — CI verified it.\n\n5. **No fifth review pass.** Passes ran 4 (the absolute cap), findings 3 → 3 → 1 → 1, each pass\n   invalidating the previous fix at its root. Pass 4's single finding was already fixed before it\n   returned, and the fix commit (`8d9d330f`) is therefore **reviewed by no model** — only by the\n   agent that wrote it. *Residual risk carrier:* this ticket. It is a 20-line test change, verified\n   to discriminate between the two implementations, but it is unreviewed and that is unquantified.\n\nNothing else: no reviewer objection was overridden. All eight findings across four passes were\naccepted as real and fixed.\n\n---\n\n### learnings:\n\n1. **`go work edit -json` is not a faithful representation of `go.work`** — it exposes only `Go`,\n   `Use` and `Replace`, so regenerating the file from it silently drops `godebug` (and anything a\n   future release adds). To copy a workspace, copy the *file* and redirect paths with `go work edit`;\n   never regenerate it. → **repo-local**, and already captured where it is discoverable: the\n   `scripts/check-go-build-list-lag.sh` header. No AGENTS.md edit proposed — it is one script's\n   concern, and AGENTS.md carries rules, not knowledge.\n\n2. **A guard's own guards need the fixture-that-cannot-fail question, recursively.** Three vacuities\n   on this ticket, all in tests *I* wrote, all under fix-momentum, all naming exactly the right case:\n   two MD5s that are both **empty** when the file is absent (empty == empty); two lists built with\n   stderr discarded, both **empty** on failure (`cmp` says equal); and an assertion on a **verdict\n   blind to the property** it pinned. The shared shape is new and worth naming: **comparing two\n   values that are equal because both producers failed.** AGENTS.md's existing rules cover a test\n   that cannot reach the failing state; this is narrower — the test reaches it and the *comparison*\n   is degenerate. → **candidate for `docs/learnings/`**, not the skill. Not written in this ticket:\n   documentation is a direct-commit class of work (SKILL.md § what this pipeline is for), and\n   inventing a learnings file mid-closeout would bundle unreviewed prose into a merged ticket.\n   **Proposed as a follow-up, not silently done.**\n\n3. **A review's finding and its recommended fix are separately falsifiable.** F1's finding was\n   correct and its remedy (`-mod=readonly`) was wrong — the write targets `go.work.sum`, which that\n   flag does not govern. Adopting it verbatim would have produced a *passing regression test over a\n   live defect*. → **skill-worthy**, and it is already there in substance: \"Verify reviewer findings\n   against the revision under review.\" The gap is that the rule says to verify the *finding*; this\n   ticket shows the *fix* needs the same treatment. → **retro item 1 below.**\n\n### retro:\n\n**(1) What would have made this ticket faster or better?**\n\nThe mémo cited **`ADR-035-eight-go-modules-stay.md`**, which does not exist — I wrote the filename\nfrom the ADR's *subject matter* instead of from `ls`. It reached the plan brief. Impact was nil only\nbecause the drafter grepped by content and found the real file, and it flagged the error in its first\nline. `shaping.md` already mandates `git cat-file -e` on every cited ADR; I did run that check on\nTKT-260 earlier in the same session and skipped it here.\n\n*Concrete change:* `references/shaping.md` § context_memo — the rule is present but stated as prose\ninside a long paragraph. Promote it to an explicit line: *\"Verify every `governingAdrs` entry with\n`git cat-file -e HEAD:<path>` and paste the command's output into the mémo note.\"* Making the\n**evidence** part of the deliverable is what stops the check being skipped when the filename looks\nobviously right.\n\n**(2) What should the humans change?**\n\n*Concrete change:* `SKILL.md` Hard rules, the bullet beginning *\"Verify reviewer findings against the\nrevision under review\"* — extend it to the fix: **\"and verify the FIX, not the finding. A correct\nfinding can carry an incorrect remedy; adopting the remedy verbatim can produce a green test over a\nlive defect (TKT-265 F1: `-mod=readonly` does not govern `go.work.sum`).\"** This ticket is the\nworked example and it cost one full fix-verify-refix cycle.\n\n*Second, smaller:* four review passes on a 190-line shell script is a signal about **where** this\npipeline's cross-model review is strongest and weakest. Every one of the eight findings was about a\nguard failing open or a test not observing — none about the ticket's actual *decision*, which is the\npart a wrong call would make expensive. Worth a human asking whether the review prompt should be\nsplit: one pass on *\"is this the right decision\"*, separate from *\"is this code wrong\"*.\n\n**V0 is interactive: both changes are offered, not applied.** They touch `SKILL.md` and\n`references/shaping.md`, which are process files owned by the human — and applying a skill edit\ninside the same autonomous run that produced the evidence for it would be the agent grading its own\nretro. Say the word and I will patch them.\n"
    }
  ],
  "parent": "TKT-9",
  "classification": "simple",
  "links": [
    {
      "type": "found-in",
      "key": "TKT-264"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "ADR-035's checker gates HORIZONTAL drift (two modules declaring different versions) and is silent on VERTICAL lag (a manifest declaring less than the workspace selects). Verified by execution at afa333f4: three dependencies lag across seven modules. Legibility defect, not a broken build."
    },
    "scope": {
      "state": "met",
      "note": "IN: a decision recorded as an ADR-035 amendment; whatever check or maintenance that decision implies; the guarantee register updated to name both drift directions. OUT: collapsing to one module (ADR-035 Option 1) -- a 61-path move is not this ticket's to take. OUT: bumping any dependency; this ticket changes no version."
    },
    "uncertainties": {
      "state": "met",
      "note": "The cost question that ADR-035 left open is now MEASURED -- see the kind=readiness comment. Key result: `go list -m all` is 0.18s warm but CANNOT run offline (cold cache + GOPROXY=off fails with 'module lookup disabled'), so option 1 clears two of ADR-035's three rejection grounds and NOT the third. That is the decision's crux and it is settled by measurement, not left open."
    },
    "dependencies": {
      "state": "met",
      "note": "No blockers. TKT-264 (found-in) is merged at aaed8e95."
    },
    "approach": {
      "state": "met",
      "note": "Recommend Option 3-plus: ACCEPT build-list lag explicitly and document the limit in ADR-035's guarantee register, plus a read-only reporting script that is NOT a gate. Rationale from measurement: a gating check cannot be offline (0.18s warm, but fails closed on a cold/air-gapped cache), and ADR-035's offline property is load-bearing for the checker it already ships. Making the gate network-dependent to close a LEGIBILITY defect trades a real property for a cosmetic one. The reviewer-facing problem -- 'I cannot see what is actually linked' -- is solved by a script a reviewer runs, not by a gate that fails a build. NOTE this is a RECOMMENDATION carried into Planning, not a settled decision: the ticket's deliverable is the decision itself, so plan-review must be free to reject it. approach is `met` because the option space is enumerated and costed, not because the answer is pre-ordained."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice: ADR-035 amendment + the reporting script + selftest coverage if the script becomes a make target. Fits a sprint comfortably."
    },
    "success": {
      "state": "met",
      "note": "The filed COS are already well-formed and are kept, with the third sharpened -- see the kind=readiness comment for the pre-existing-churn disposition."
    },
    "context_memo": {
      "state": "met",
      "note": "baked -- see `context` on the ticket"
    }
  },
  "context": {
    "area": "repo-level Go dependency hygiene: go.work, eight go.mod manifests, scripts/, docs/adr/",
    "entryPoints": [
      "scripts/check-go-dependency-drift.sh -- the shipped horizontal checker",
      "Makefile: `check-dep-drift` target, a `check` prerequisite",
      "scripts/gate-selftest.sh:103-113 -- the positive control + two seeded negatives",
      "docs/adr/ADR-035-eight-go-modules-stay.md -- the decision being amended"
    ],
    "governingAdrs": [
      {
        "adr": "ADR-035-go-module-dependency-declarations.md",
        "constrains": "THE ticket. Its Decision section rejects `go work sync && git diff --exit-code` on three MEASURED grounds: ~5min cold cache / may need network; mutates the tree during the gate; `git diff` cannot see untracked sum files. Any proposed gate must be measured against the same three, and the amendment lands in its 'What this guarantees' register. Note it also says to reconsider Option 1 (collapse) if the check's maintenance cost exceeds the 61-path move -- so 'the check is getting expensive' is an argument the ADR itself invites."
      },
      {
        "adr": "ADR-021-ticket-lifecycle-trail-integrity.md",
        "constrains": "The house rule for guarantee registers: name the adversary/case. ADR-035 already follows it (closed / not closed / not claimed). The amendment must say which drift DIRECTION is closed against WHOM. 'The gate enforces one version' is the exact overreach shape."
      },
      {
        "adr": "ADR-001-and-002 (architecture.md)",
        "constrains": "One repo, one deploy unit, eight modules under one go.work. This is WHY declaration drift is not runtime drift -- every supported build resolves one MVS list. Do not re-argue it; ADR-035 settled it."
      }
    ],
    "governingTests": [
      {
        "test": "scripts/gate-selftest.sh:103 expect_pass 'go dependency drift (clean baseline)'",
        "pins": "The POSITIVE CONTROL. ADR-035 added it because this stage's seed mutates an existing file, so an always-failing checker would satisfy its own expect_fail. If this ticket adds a gated check, it needs the same pair."
      },
      {
        "test": "scripts/gate-selftest.sh:112 expect_fail 'go dependency drift (unreadable non-final module)'",
        "pins": "A checker that cannot READ a module must not report 'none' and exit 0. The shipped script's comments (lines 30-40) explain the piped-loop trap that caused it. Any new script must not reintroduce it."
      },
      {
        "test": "make check-generate",
        "pins": "AGENTS.md: commit regenerated code BEFORE running the gate -- check-generate diffs against HEAD, so an uncommitted regen reads as drift. Relevant here because any go.mod realignment must be committed before the gate runs."
      }
    ],
    "gotchas": [
      "MEASUREMENT TRAP, cost a real cycle here: running `go list -m` under an isolated GOMODCACHE makes Go re-download the TOOLCHAIN (go.work requires 1.26.6; local go is 1.26.0). The first two cold-cache runs failed on 'toolchain not available' / 'requires go >= 1.26.6', which looks exactly like the module graph failing. Seed the toolchain into the cold cache before concluding anything about module lookups.",
      "`go work sync` MUTATES the tree. Never run it during a gate, and never leave it run in the working tree mid-ticket -- AGENTS.md forbids editing tracked files while the gate is running.",
      "The Makefile's per-module `cd $$m && go test` does NOT leave the workspace, so a per-module command still resolves the workspace build list. This is why lag is invisible to every existing gate stage."
    ],
    "measurements": {
      "at_commit": "afa333f4 (clean tree)",
      "lag_present": "14 (module, dependency) pairs across 7 modules; 3 distinct deps: x/crypto declares v0.54.0 selects v0.55.0 (6 modules); x/text declares v0.40.0 selects v0.41.0 (7 modules); x/net declares v0.56.0 selects v0.58.0 (shared/go)",
      "go_list_m_all_warm": "0.18s",
      "go_list_m_all_cold_with_network": "22s, 315MB fetched, 35 zips",
      "go_list_m_all_cold_offline": "FAILS -- 'module lookup disabled by GOPROXY=off'",
      "go_mod_edit_json_offline": "works (this is what the shipped checker uses, ~0.2s/8 modules)",
      "narrow_query_offline": "also FAILS -- `go list -m <3 paths>` still walks the full graph",
      "go_work_sync_effect": "MEASURED on clean afa333f4 then reverted: 0.6s warm, 14 files / 7 modules (+49/-51). Every version change is a RAISE to what the workspace already selects; nothing is lowered. Build list BYTE-IDENTICAL before vs after (go list -m all diffed) -- so the sync has zero runtime effect and is purely a legibility fix. Non-version churn: access gains 7 requires (kin-openapi, jsonpointer, gorilla/mux, oasdiff/yaml, oasdiff/yaml3, go-internal, jsonschema/v6); commerce gains gorilla/mux and drops kr/pretty."
    }
  }
}
