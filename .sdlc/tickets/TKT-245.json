{
  "key": "TKT-245",
  "type": "Task",
  "status": "Building",
  "labels": [
    "agent:ai-review"
  ],
  "assignee": "agent",
  "pr": {
    "number": 223,
    "url": "https://github.com/mathd/ticketing_system/pull/223",
    "state": "open"
  },
  "summary": "Catalog cannot verify WHICH organizer a back-office request is for (TKT-236 ai-review)",
  "description": "**Surfaced by TKT-236's ai-review.** Not a defect that ticket introduced — a property of the\ndeputy model that TKT-236 was the first to have to state out loud.\n\nCatalog authenticates the **calling process**, not the staff member behind it. Every back-office\nwrite takes `organizer_id` from the request body and catalog trusts it, because it has nothing else\nto go on. The back office passes its session's organizer and never one from a form — but that is a\ndiscipline in one codebase, not a boundary catalog can enforce.\n\nTKT-236 narrowed the blast radius where it could: `UpdateChannel` now carries an organizer predicate,\nso an id disclosed by the operator list is no longer sufficient to mutate another tenant's channel.\nWhat remains is that a compromised or buggy back office can still *author for*, and now *enumerate*,\nany organizer.\n\n**Conditions of Success:**\n- A way for catalog to verify which organizer a request is for, that does not rest on the caller\n  asserting it. Options to weigh: a per-organizer credential; a signed assertion the back office\n  mints from its session (commerce's customer-assertion key is the in-repo precedent); or catalog\n  resolving the organizer from the staff identity it already stores.\n- An ADR recording the choice and what it does *not* close — ADR-021's rule about naming the\n  adversary applies: this is about a compromised deputy, not a DB writer.\n- Applies to every catalog write the back office makes, not only channels. A fix that scoped one\n  endpoint would leave the model unchanged and the claim still unearned.\n\nQuality gates: `make check` green; `make browser` for any touched write form.\n\nNon-goals: multi-organizer staff sign-in (ADR-042 scopes that as future work); inventory's credential\n(TKT-244).",
  "context": {
    "capturedAt": "2026-08-15",
    "supersedes": "2026-08-10 bake (stale: ADR-056 and ADR-057 landed after it)",
    "governingAdrs": [
      {
        "id": "ADR-042",
        "why": "Staff identity lives in catalog (staff_accounts, migration 0015); the BACK OFFICE owns the session and it is in-process and non-persisted. Sub-decision 4 scopes multi-organizer sign-in as out of scope, and sub-decision 2 promises the session module can be replaced 'without moving the enforcement point' -- which is the hinge for mechanism 3."
      },
      {
        "id": "ADR-043",
        "why": "Where a service's auth guard lives: contract operation (declared security:, enforced by openapi3filter) vs internal route (inline header check). A new organizer-scoped credential or assertion must place itself on this line; catalog's writes are contract operations, so the guard belongs in the AuthenticationFunc seam."
      },
      {
        "id": "ADR-053",
        "why": "Records the enumeration capability this ticket closes, and states the assumption explicitly: 'the back office is not compromised.' Its own code comment names TKT-245 as the owner of the fix (internal/api/server.go:323-325). Its reasoning deliberately does NOT generalize -- read the WHY IT DOES NOT GENERALIZE block before citing it as precedent."
      },
      {
        "id": "ADR-056",
        "why": "NEW since the stale bake. The live in-repo template: a credential resolved to a scope {ResellerID, OrganizerID, ChannelCode} inside the AuthenticationFunc, handed to handlers through a per-request slot, with 'nothing in it ever comes from the request body'. It explicitly cites ADR-053's cross-tenant staff credential as the failure it avoids."
      },
      {
        "id": "ADR-057",
        "why": "NEW since the stale bake. The freshest precedent for adding a service-specific staff credential (TKT-244), and it answers the same-shaped question as ADR-053 while reaching the OPPOSITE conclusion -- the difference is the whole content of that ADR. Read both before arguing from either."
      },
      {
        "id": "ADR-049",
        "why": "Customer identity and storefront sessions -- the ADR behind commerce's customer assertion, which is mechanism 2's precedent. Its TKT-221 amendment records why a customer_id in the body and a service credential in the SSR process were BOTH refused; catalog's problem is the same shape with organizer_id."
      },
      {
        "id": "ADR-021",
        "why": "Name the adversary. This ticket is about a compromised or buggy DEPUTY PROCESS, not a writer with catalog DB access -- no mechanism here constrains the latter, and the ADR must not overclaim. Three of its clauses broke this rule across two review passes."
      },
      {
        "id": "ADR-002",
        "why": "Catalog owns organizers/tenants, which is why staff live there rather than in a sixth service or on the gateway."
      }
    ],
    "governingTests": [
      {
        "test": "TestCatalogSpecGuardsEveryUnsafeOperationAndOnlyThose",
        "path": "services/catalog/internal/api/write_credential_test.go:31",
        "why": "Derives the invariant from the SPEC in both directions over every operation: unsafe -> requires CatalogStaffWriteCredential AND declares 401; safe -> opts out with `security: []`. Adding a security scheme or a new operation without satisfying both fails the gate. This is why ADR-053 says catalog's contract cannot express a staff-authenticated GET."
      },
      {
        "test": "TestCatalogStaffWriteSchemeIsADeclaredHeaderKey",
        "path": "services/catalog/internal/api/write_credential_test.go:78",
        "why": "Pins the scheme name and header to what the document declares. A header renamed in the spec but not in code would silently read a header nobody sends."
      },
      {
        "test": "TestCatalogRefusalsAreIndistinguishable",
        "path": "services/catalog/internal/api/write_credential_test.go:137",
        "why": "Refusals must not become an oracle. Any new refusal path (wrong organizer, bad assertion) has to answer identically to the existing ones."
      },
      {
        "test": "TestCatalogPublicReadsStayOpenWithoutCredential",
        "path": "services/catalog/internal/api/write_credential_test.go:150",
        "why": "The storefront must keep working. A change that makes reads demand an organizer identity breaks the public surface."
      }
    ],
    "reference": [
      {
        "summary": "The corrected blast-radius reasoning and the residual it names -- it names TKT-245 as the owner of the fix. Read before anything else in the code.",
        "link": "services/catalog/internal/api/server.go:280-343"
      },
      {
        "summary": "Mechanism 2's precedent, in full: mint/verify an HMAC assertion `v1.<id>.<expiry>.<mac>`",
        "link": "services/commerce/internal/api/assertion.go"
      },
      {
        "summary": "Mechanism 1's precedent: credential -> scope in the AuthenticationFunc, per-request slot, never from the body",
        "link": "services/commerce/internal/api/partner_auth.go"
      },
      {
        "summary": "staff_accounts.organizer_id NOT NULL -- catalog already stores the fact it does not use",
        "link": "services/catalog/internal/store/migrations/0015_staff_accounts.sql:19-32"
      },
      {
        "summary": "Catalog already returns the organizer at sign-in (StaffPrincipal.OrganizerId)",
        "link": "services/catalog/internal/api/staff.go:76-80"
      },
      {
        "summary": "The back office reads staff.organizerId from its session and passes it as an argument",
        "link": "web/backoffice/src/pages/channels.astro:72,90,107,135"
      },
      {
        "summary": "The existing guard seam a fix would extend",
        "link": "services/catalog/internal/api/write_credential.go:29-46"
      },
      {
        "summary": "Browser-spec pattern for a back-office write form (make browser)",
        "link": "test/browser/channels.mjs"
      },
      {
        "summary": "TKT-236 PR and its review (the finding this ticket was filed from)",
        "link": "https://github.com/mathd/ticketing_system/pull/194"
      }
    ]
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Filed from TKT-236 ai-review — incidental, deferred\n\n**Finding:** [high] *\"Credential widening materially increases compromise impact despite claiming\nnone.\"* The reviewer was **right about the claim**: the code said the added blast radius was nil, and\nthat was false — a create could only probe one guessed code at a time and never yielded an id, while\nthe list read returns every channel, id and disabled row in one call.\n\n**Fixed in TKT-236:** the claim itself (corrected in the code and in ADR-053), and the chain it\nenabled — `UpdateChannel` now carries an organizer predicate, so a disclosed id no longer mutates\nacross tenants.\n\n**Deferred here:** the underlying model. Catalog cannot verify which organizer any back-office\nrequest is for, on any endpoint. Closing that is a new credential or assertion mechanism affecting\nevery catalog write — its own vertical slice, not a channels story's.\n\nNeeds shaping before Gate 1: the choice between a per-organizer credential, a signed session\nassertion, and catalog resolving the organizer from staff identity is the ticket's real content."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Shaping verdict — 6 of 8 met, 2 held for the human (Gate 1 blocked, correctly)\n\n**Backend:** local tracker. **Gates:** all four waived for this ticket (owner, confirmed at start of\nrun); agent takes them by judgement, `overrides:` required at closeout.\n\n#### The described defect survives verification — with one correction\n\nTKT-250's lesson was that a filed ticket can describe a defect the code does not have, so this was\nchecked against HEAD before anything was planned. **It holds**, and the code says so in its own\nwords: `services/catalog/internal/api/server.go:318-325` states the whole security property rests on\n*\"the back office is not compromised\"* and names TKT-245 as the owner of the fix.\n\n**The correction:** the ticket describes the problem as `organizer_id` **taken from the request\nbody**. That is 15 of catalog's 29 contract writes. The other 13 unsafe operations are\nstate transitions that take only a **path id** — `POST /series/{id}/publish`,\n`/festivals/{id}/archive`, `/performances/{id}/close` and so on — and they reach the store with **no\norganizer at any layer**: `PublishSeries(ctx, id uuid.UUID)` (`internal/store/store.go:696`,\n`postgres.go:1055`). There is no asserted field to distrust because there is no field at all. A fix\nshaped only around \"don't trust the body value\" would leave 13 writes untouched and the COS's\n*\"applies to every catalog write\"* unearned — the exact failure the COS warns about, one level\ndeeper than it states.\n\n#### What the read changed about the three mechanisms\n\nThe ticket presents three options as open. Reading the code narrows them:\n\n**Catalog already knows the answer it fails to remember.** `staff_accounts.organizer_id` is\n`NOT NULL REFERENCES organizers(id)` (migration `0015:21`) and `AuthenticateStaff` already returns it\nas `StaffPrincipal.OrganizerId` (`internal/api/staff.go:76-80`). The back office learns the organizer\n*from catalog* at sign-in, holds it in its session, and hands it back on every write — and catalog,\nhaving said it, no longer knows it. The gap is not missing data; it is a missing binding between the\nauthenticated staff member and the request.\n\n| # | Mechanism | Verified precedent | Assessment |\n|---|---|---|---|\n| 1 | Per-organizer credential | ADR-056 `partnerScope` — credential → `{ResellerID, OrganizerID, ChannelCode}` resolved in the `AuthenticationFunc`, *\"nothing in it ever comes from the request body\"* (`commerce/internal/api/partner_auth.go:36-43,103-115`) | Right shape, wrong caller. That credential authenticates a **machine** confined to one tenant. Catalog's is one env scalar (`CATALOG_STAFF_WRITE_TOKEN`, `cmd/catalog/main.go:104`) shared by one back office serving *all* staff; per-tenant provisioning and rotation buys a boundary the signed-in human already carries. |\n| 2 | Signed assertion minted from the session | commerce's customer assertion, `v1.<id>.<expiry>.<mac>`, HMAC-SHA256, minted on password presentation, kept server-side, forwarded on the write (`commerce/internal/api/assertion.go:57-72`); key already provisioned by `scripts/env-bootstrap.sh:72` | **Recommended.** Commerce hit the *identically shaped* problem in TKT-221 — session in one process, identity needed in another, `customer_id` in the body forgeable — and this is the answer it landed on, reviewed and shipped. Catalog's is the same with `organizer_id`. It also fits the existing seam: `authenticateStaffWrite` is already the `openapi3filter` `AuthenticationFunc` (`write_credential.go:29`), which is where ADR-056 proves a scope can be attached. |\n| 3 | Catalog resolves the organizer from staff identity | ADR-042 sub-decision 2: the session module can be replaced *\"without moving the enforcement point\"* | Cleanest **end state** and where ADR-042 points, but it means catalog owns sessions — a table, a migration, an expiry sweep, and moving enforcement out of the back office. Larger than this ticket; mechanism 2 is a strict step toward it, not a detour, since the mint/verify seam is the same one a session would later use. |\n\n⚠️ **ADR-053 must not be cited as precedent without reading its `WHY IT DOES NOT GENERALIZE` block**,\nand ADR-057 answers a same-shaped question while reaching the **opposite** conclusion. Both are in\nthe refreshed mémo with that warning attached.\n\n#### Scope is too large for one ticket — proposed slice\n\n28 writes (excluding `authenticateStaff` itself) across **two classes that do not share a remedy**.\nSPIDR, split on Rules/Interface:\n\n- **Slice 1** — the mechanism, the verification seam, and the **15 body-organizer writes**. Per\n  AGENTS.md the target is **unsubmittable, not validated**: delete `organizer_id` from the request\n  types rather than comparing it, or the trust boundary stays in the client and only moves.\n- **Slice 2** — the **13 path-id transitions**. A store-tier scoping change (organizer predicate on\n  lookup), not a credential change. Different tier, different tests, different failure mode.\n\nRecommending the split be confirmed **at Gate 1** rather than discovered mid-Planning.\n\n#### Readiness\n\n| Item | State | One-liner |\n|---|---|---|\n| `objective` | **met** | Verified at HEAD; the code names this ticket as its fix. |\n| `scope` | **met** | In/out drawn; multi-organizer sign-in and inventory explicitly out. |\n| `uncertainties` | **met** | Resolved by reading; surfaced the unstated 13-transition class. |\n| `dependencies` | **met** | No open blockers; precedents already shipped. |\n| `approach` | **open**, `owner: human` | Trust-boundary decision on an auth surface — `deferred` is barred by `shaping.md`. |\n| `first_slice` | **open**, `owner: human` | Follows from `approach`; the 15/13 split needs confirming. |\n| `success` | **met** | COS sharpened: unsubmittable, adversary named, omission enumerated both ways. |\n| `context_memo` | **met** | Re-resolved 2026-08-15; +ADR-056, +ADR-057, +4 governing tests; all 8 ADRs verified present at HEAD. |\n\n**Gate 1 is hard-blocked while `approach` and `first_slice` are open — as intended.** Both are\nquestions only the owner can answer, and both are in the agent's message alongside this comment.\n"
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Gate 1 decisions recorded + shaping addendum (2 findings from a second read)\n\n#### The owner's answers (both `open` items now `met` — Gate 1 unblocked)\n\n**`approach` → mechanism 2, the signed assertion.** Catalog mints an HMAC organizer assertion at\n`POST /staff/authenticate`, the back office holds it server-side in its existing session and forwards\nit as a header, catalog verifies it in the `openapi3filter` `AuthenticationFunc` seam and takes the\norganizer from the verified payload. **`organizer_id` is deleted from the 15 request types** —\n*unsubmittable, not validated*, per AGENTS.md and the three passes TKT-244 spent learning it.\n\n**`first_slice` → slice 1 = mechanism + seam + the 15 body writes.** The 13 path-id transitions\nbecome a follow-up ticket: a store-tier organizer predicate, not a credential change, sharing no code\nwith this slice.\n\n#### Addendum — two sites a second read found, both belonging to slice 1\n\nA follow-up pass over the whole surface (not just the contract) turned up two places the\n15-body-writes framing does not cover, and both would leave the claim half-true if missed:\n\n**1. `GET /internal/channels` takes a caller-supplied `organizer_id` in the QUERY STRING**, under the\nsame staff-write credential (`internal/api/channels.go:115`, guarded by `staffMayReadOperatorChannels`\nat `server.go:344`). This is the *exact* enumeration capability ADR-053 recorded and that this ticket\nwas filed to close — and it is not a body field, so a fix shaped only around request bodies sails\npast it. It is one route, it is in the same guard seam, and leaving it would mean shipping \"catalog\nverifies the organizer\" while the enumeration path that motivated the ticket still trusts the caller.\n**Recommend it joins slice 1.**\n\n**2. `DEFAULT_ORGANIZER_ID` is still a live fallback in the back office.**\n`web/backoffice/src/lib/catalog.ts:69` hardcodes `00000000-0000-0000-0000-000000000001` and it is a\n**default parameter** on six functions — `getVenues` (:72), `getVenue` (:85), `createSeatMap` (:131),\n`addSeatMapSection` (:143), `addSeatMapRow` (:154), `addSeatMapSeat` (:165), `updateVenueGaCapacity`\n(:233). Every current call site passes `staff.organizerId` explicitly, so nothing is broken today, but\nthe fallback is reachable by any caller that omits the argument, and its own comment says *\"When admin\nauth lands this comes from the session.\"* This ticket **is** that landing. These signatures have to\nchange anyway once catalog stops accepting a body organizer, so deleting the default belongs here\nrather than surviving as a silent tenant-defaulting path.\n\nContrast, already correct and worth preserving: the event and channel authoring functions take\n`organizerId` as a **required first parameter with no default**, for the reason stated at\n`catalog.ts:303-305` — *\"a default here is how one organizer's staff would author into another's\ntenant.\"*\n\n#### One count correction\n\nThe contract yields **15** body-carried writes (all with `organizer_id` **required**), enumerated by\nreading `services/catalog/api/openapi.yaml`: `createVenue`, `createSeatMap`, `addSeatMapSection`,\n`addSeatMapRow`, `addSeatMapSeat`, `editSeatMap`, `updateVenueGaCapacity`, `createEvent`,\n`createPerformance`, `createSeries`, `createSeason`, `createFestival`, `createTicketType`,\n`createChannel`, `updateChannel`. Slice 1 is therefore **15 writes + 1 guarded read + the back-office\ndefault** — not 14, and not 28.\n\n#### Note for the planner on the pricing/fees surface\n\n`CreatePriceRule`, `CreateFeeRule`, `CreatePayee` and `CreateSplitSchedule` exist in the store\n(`internal/store/store.go:599-629`) with **no HTTP endpoint and no caller**. They are not in scope and\nmust not be \"fixed\" — but a drafter grepping for organizer-carrying writes will find them, so they are\nnamed here as deliberately excluded rather than missed.\n\n#### Readiness: 8 of 8 met — Gate 1 clear\n\n`objective` · `scope` · `uncertainties` · `dependencies` · `approach` · `first_slice` · `success` ·\n`context_memo` — all **met**. No open blockers. Ready to claim.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed → Planning\n\n**Branch:** `TKT-245-organizer-assertion` off `origin/main` @ `be4bb5d7`.\n**Blockers:** none (no `links` on the ticket; readiness 8/8 `met`).\n\n**Why this one:** it is the ticket the code names as its own fix\n(`services/catalog/internal/api/server.go:323-325`), and it was shaped and decided in this session —\nmechanism and slice are settled, so Planning has no pending human call to stall on.\n\n#### Risk verdict — **`risk:low` NOT claimed**\n\nStated explicitly rather than left to inference, because the label's carve-outs are exactly the\ntickets whose risk is obvious to the claimer and invisible in the thread afterwards (TKT-190).\n\nThis ticket hits **three** of the four carve-outs:\n\n- **auth** — it changes a trust boundary. This is the entire content of the ticket.\n- **>5 files** — 15 request types, the mint site, the verify seam, the back-office session and client,\n  the contract, plus tests and an ADR.\n- **CI/deploy config** — a new signing key must be provisioned (`scripts/env-bootstrap.sh`,\n  `scripts/stack-env.sh`, `compose.yaml`), following `COMMERCE_CUSTOMER_ASSERTION_KEY`'s precedent.\n\nThe plan gate would therefore be mandatory. It is **waived for this run by the owner** (all four\ngates, agent's judgement, `overrides:` required at closeout) — waived, not inapplicable, and the\ndistinction is the point of writing it down.\n\n#### Gate-2 posture under the waiver\n\nThe plan draft goes to `gpt-5.6-luna` via the codex runner (`config.models.plan`), critiqued by\n`main-agent` (`config.models.planReview`) — the two differ, so the cross-model invariant holds and no\ndegraded flow is being run. I take Gate 2 on the strength of that critique.\n\n**One thing I will be watching for**, flagged now so it is on the record before the draft exists: the\nmechanism must not become *\"validate the submitted organizer\"*. If, after the change, the question\n*\"can the client influence which organizer catalog writes to?\"* still answers **\"yes, but only if it\nlies in a way I now check for\"**, it is not fixed (AGENTS.md; TKT-244 took three passes and a\ncapability deletion to learn it). The target is that `organizer_id` is **absent from the request\ntypes**, not compared.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — authored by `gpt-5.6-luna` (codex runner, read-only, `--effort high`)\n\n`config.models.plan = gpt-5.6-luna`. Run `task-msv74nsw-n033am`, ~9 min, `write: false`.\n**Judged a PASS on the runner's criteria**: it ran against the intended scope and returned a\nconclusive, code-cited plan (not a rubric template, not truncated — 21KB rendered). No skill hijack\nthis run.\n\nBriefed with: the verified problem statement, the settled approach and slice, the two precedents\n(`commerce/internal/api/assertion.go`, `partner_auth.go`), 8 governing ADRs, 5 governing tests, and\nthe repo's gate rules. Critique by `main-agent` follows as a separate comment — the two differ, so\nthe cross-model invariant holds.\n\n**Verbatim below.** My verification of its claims, including three places it corrected the brief,\nis in the `kind=plan-final` comment.\n\n---\n\nNo files were modified. The plan below is based on the current checkout.\n\n## Decision summary\n\nUse a second, operation-level OpenAPI security scheme:\n\n- Scheme: `CatalogOrganizerAssertion`\n- Header: `X-Catalog-Organizer-Assertion`\n- Key: `CATALOG_ORGANIZER_ASSERTION_KEY`\n- Payload: organizer UUID and expiry only\n- Format: `v1.<organizer uuid>.<unix expiry>.<HMAC>`\n- TTL: 8 hours\n\nThe assertion should carry the organizer, not the staff ID.\n\nThe enforcement target is tenant identity. Staff ID would add no authorization value because catalog does not enforce roles; role enforcement remains in back office authorization (`web/backoffice/src/lib/authorization.ts:8-12`, applied from `gate.ts:140-149`). Staff ID is already returned and retained for session/audit purposes (`services/catalog/internal/api/staff.go:76-80`).\n\nThe 15 affected operations should declare both schemes in one security requirement object:\n\n```yaml\nsecurity:\n  - CatalogStaffWriteCredential: []\n    CatalogOrganizerAssertion: []\n```\n\nOne object means AND; separate objects would mean OR.\n\nThis preserves the existing invariant that every unsafe contract operation requires `CatalogStaffWriteCredential` and a 401 response (`services/catalog/internal/api/write_credential_test.go:31-73`). A new derived test must additionally require the organizer assertion exactly for the 15 body-organizer operations.\n\n## DoD\n\n- The 15 request schemas no longer contain `organizer_id` or require it:\n\n  `createVenue`, `createSeatMap`, `addSeatMapSection`, `addSeatMapRow`, `addSeatMapSeat`, `editSeatMap`, `updateVenueGaCapacity`, `createEvent`, `createPerformance`, `createSeries`, `createSeason`, `createFestival`, `createTicketType`, `createChannel`, and `updateChannel` (`services/catalog/api/openapi.yaml:26-133`, `196-287`, `458-553`, `630-725`, `1274-1309`).\n\n- Those 15 operations require both the existing staff credential and the organizer assertion. The 13 path-ID transition operations remain unchanged and do not acquire this assertion.\n\n- `POST /staff/authenticate` returns the signed organizer assertion alongside `staff_id`, `organizer_id`, and `role`. The existing organizer source is the authenticated staff principal (`services/catalog/internal/api/staff.go:76-80`).\n\n- Catalog verifies the assertion before handlers run, fails closed on missing/empty configuration, checks the MAC before expiry, uses constant-time comparison, rejects nil UUIDs, and exposes one indistinguishable refusal.\n\n- Handlers take organizer identity only from verified request context. No handler compares a body organizer with a verified organizer.\n\n- `GET /internal/channels` has no caller-controlled organizer query. It requires the staff credential plus a valid organizer assertion and passes that verified organizer to `ListChannels` (`services/catalog/internal/store/store.go:643-650`).\n\n- The back office stores the assertion only in its existing server-side in-process session (`web/backoffice/src/lib/session.ts:1-7`, `53-58`). It never sends the assertion to browser JavaScript or renders it into a form.\n\n- Back-office catalog writes send the assertion header and no organizer body field. `DEFAULT_ORGANIZER_ID` and all seven default parameter occurrences are removed (`web/backoffice/src/lib/catalog.ts:67-69`, `72`, `85`, `131`, `143`, `154`, `165`, `233`).\n\n- Catalog requires a distinct assertion key at startup, separate from `INTERNAL_SERVICE_TOKEN` and `CATALOG_STAFF_WRITE_TOKEN`.\n\n- Generated files are regenerated and committed before `make check`.\n\n- `make check` passes, and `make browser` passes with a browser-submitting channel write test.\n\n## File-by-file changes\n\n### Contract and generated code\n\n- `services/catalog/api/openapi.yaml`\n\n  Add `CatalogOrganizerAssertion` as an `apiKey` header scheme, modelled on commerce’s assertion scheme (`services/commerce/internal/api/assertion.go:46-143`). Add an assertion field to `StaffPrincipal` (`services/catalog/api/openapi.yaml:1675-1690`).\n\n  Remove `organizer_id` from the 15 request schemas and add operation-level AND security requirements. Update stale descriptions that still describe organizer identity as caller-supplied, especially channel create/update (`services/catalog/api/openapi.yaml:2909-2947`).\n\n- `services/catalog/internal/api/openapi_gen.go`\n\n  Regenerate; affected request structs must no longer expose `OrganizerId`, and `StaffPrincipal` must expose the assertion.\n\n- `web/backoffice/src/lib/api-types.gen.ts`\n\n  Regenerate from the catalog contract.\n\n- `web/storefront/src/lib/api-types.gen.ts`\n\n  Regenerate because the shared catalog API generation command writes both frontends (`Makefile:23-42`, `package.json:6`).\n\n### Catalog assertion and verification\n\n- `services/catalog/internal/api/assertion.go` — new\n\n  Implement minting and verification using the commerce precedent:\n\n  - versioned four-part token\n  - HMAC-SHA256\n  - raw URL-safe base64\n  - MAC verification before expiry parsing\n  - constant-time comparison\n  - nil UUID rejection\n  - one `ErrOrganizerAssertionInvalid`\n  - empty key fails closed\n\n  The assertion should contain only the organizer UUID and expiry.\n\n- `services/catalog/internal/api/assertion_test.go` — new\n\n  Cover round-trip, malformed/truncated tokens, wrong key, tampering, expiry, nil UUID, empty key, and indistinguishable failures.\n\n- `services/catalog/internal/api/write_credential.go`\n\n  Extend the validator authentication function to dispatch on both declared schemes. The existing staff credential branch remains unchanged in behavior (`services/catalog/internal/api/write_credential.go:22-49`).\n\n  Add the context-slot pattern from commerce partner authentication (`services/commerce/internal/api/partner_auth.go:44-76`, `108-115`). The slot must be installed outside the validator so both validator-authenticated contract operations and hand-mounted internal routes can use it.\n\n- `services/catalog/internal/api/server.go`\n\n  Add the assertion key and organizer scope to `Server`. Install the organizer scope slot around the finished handler/guard.\n\n  For contract operations, the validator populates the slot through `AuthenticationFunc`.\n\n  For `/internal/channels`, modify `guardInternalSurface` and `staffMayReadOperatorChannels` so the shared internal token cannot bypass organizer scoping. The exact channel-list route should require the staff credential plus a valid assertion; other internal routes retain their existing internal-token behavior (`services/catalog/internal/api/server.go:228-356`).\n\n  Update the 15 handlers to obtain organizer identity from the verified scope instead of generated request fields. Examples include venue creation (`server.go:608-627`), event creation (`629-653`), series creation (`687-703`), ticket types (`1180-1207`), seat-map editing (`1493-1516`), and seat-map authoring (`1562-1612`).\n\n- `services/catalog/internal/api/channels.go`\n\n  Remove query-string organizer parsing from `listChannels` (`channels.go:100-120`). Read organizer identity from the verified context slot and refuse if the slot is absent.\n\n- `services/catalog/internal/api/staff.go`\n\n  Mint the assertion after successful staff authentication, where `principal.OrganizerID` is already available (`staff.go:76-80`), and include it in `StaffPrincipal`.\n\n### Catalog startup and tests\n\n- `services/catalog/cmd/catalog/main.go`\n\n  Require `CATALOG_ORGANIZER_ASSERTION_KEY` before dependency startup, and reject equality with the internal and staff-write credentials. Pass it into `api.NewServer`.\n\n- `services/catalog/cmd/catalog/credentials_test.go`\n\n  Add missing-key, empty-key, and key-collision startup tests. Do not echo secret values in errors, matching the existing credential tests (`credentials_test.go:24-50`).\n\n- `services/catalog/internal/api/write_credential_test.go`\n\n  Preserve the existing derived staff-security invariant and add:\n\n  - declared assertion scheme/header test\n  - derived assertion coverage for exactly the 15 operations\n  - refusal tests for missing, malformed, expired, and wrong assertions\n  - byte-identical refusal assertions for staff and organizer failures\n\n- `services/catalog/internal/api/staff_test.go`\n\n  Assert that successful authentication returns the assertion and that failed authentication does not expose it.\n\n- `services/catalog/internal/api/server_test.go`\n\n  Update server construction and request fixtures. Add an API-seam test proving that an assertion for organizer A causes the store input to use A, regardless of any raw body value supplied by a test client.\n\n- `services/catalog/internal/api/channels_test.go`\n\n  Replace query-based operator-read tests with assertion-based tests. Assert that a malicious query value cannot influence the organizer passed to the store. Add tests for:\n\n  - missing/invalid assertion\n  - internal token alone being refused for `/internal/channels`\n  - sibling `/internal/channels/{id}` remaining closed to the staff allowance\n  - exact method/path behavior\n\n  The existing store predicate remains the real database enforcement seam; the API test only proves request-to-store binding.\n\n- `services/catalog/internal/api/seatmap_test.go`\n- `services/catalog/internal/api/seatmap_edit_test.go`\n\n  Remove `OrganizerId` from generated request fixtures and add assertion context to router-level tests.\n\n### Back office\n\n- `web/backoffice/src/lib/session.ts`\n\n  Add `organizerAssertion` to `StaffPrincipal`. Preserve it in `Entry`; sessions remain server-side and opaque. The existing eight-hour session TTL is `SESSION_TTL_MS` (`session.ts:32-38`).\n\n- `web/backoffice/src/lib/catalog.ts`\n\n  Extend the sign-in response type and parse the assertion.\n\n  Split or parameterize write headers so catalog writes send:\n\n  - `X-Catalog-Staff-Write-Token`\n  - `X-Catalog-Organizer-Assertion`\n\n  Keep `/staff/authenticate` using only the staff-write credential because it is the minting operation.\n\n  Remove organizer body fields from all affected write payloads. Require an assertion argument for server-side write helpers. Remove `DEFAULT_ORGANIZER_ID` and all default organizer parameters.\n\n  Change `listChannelsForOperator` to send no organizer query and send the assertion header instead (`catalog.ts:380-409`).\n\n  Keep explicit organizer IDs for organizer-scoped reads such as `getVenues` and `getVenue`; those are read parameters, not write authorization.\n\n- `web/backoffice/src/pages/login.astro`\n\n  Store the returned principal, including its assertion, in the existing session (`login.astro:27-50`).\n\n- `web/backoffice/src/pages/channels.astro`\n\n  Pass the session assertion to create/update/list operations. Do not introduce hidden organizer fields; the current session lookup is at `channels.astro:41-42`, and current organizer usage is at `72`, `90`, `107`, and `135`.\n\n- `web/backoffice/src/pages/venues/[id].astro`\n\n  Pass the session assertion to seat-map creation, section/row/seat authoring, map edit, and GA-capacity writes (`venues/[id].astro:50-89`). Continue passing `staff.organizerId` only to organizer-scoped reads.\n\n- `web/backoffice/src/pages/events/new.astro`\n\n  Change event, performance, and ticket-type writes to use the assertion. Existing organizer IDs remain valid for the venue read (`events/new.astro:56`, `89`, `103`, `123`).\n\n- `web/backoffice/src/components/SeatMapEditor.tsx`\n\n  Remove `organizerId` from props and from `toEdit`. The current component actually serializes `organizer_id` into the hidden `geometry` form field (`SeatMapEditor.tsx:59-73`), so this is an important in-scope removal.\n\n- `web/backoffice/test/api.test.ts`\n\n  Replace default-organizer expectations with explicit read organizers. Assert that writes include the assertion header and omit `organizer_id`. Assert operator channel reads omit the query parameter. Preserve the existing invariant that the back office never sends `X-Internal-Token`.\n\n- `web/backoffice/test/session.test.ts`\n\n  Test assertion round-tripping, expiry behavior, and session storage.\n\n- `web/backoffice/test/SeatMapEditor.test.tsx`\n\n  Assert the serialized edit body contains sections only and cannot contain `organizer_id`.\n\n### Configuration and integration\n\n- `compose.yaml`\n\n  Add required `CATALOG_ORGANIZER_ASSERTION_KEY` to the catalog service only. The back office must not receive the signing key; it receives minted assertions through the server-side sign-in response.\n\n- `scripts/env-bootstrap.sh`\n\n  Generate a distinct local assertion key alongside the existing credentials (`env-bootstrap.sh:72-76`).\n\n- `scripts/stack-env.sh`\n\n  Generate and export an independent smoke-stack assertion key, following the existing credential-separation pattern (`stack-env.sh:50-77`).\n\n- `scripts/smoke.sh`\n\n  Pass the assertion key to the catalog service and the catalog startup probe. The key must be provisioned before the new catalog binary starts.\n\n- `smoke/smoke_test.go`\n\n  Supply the required assertion key when launching the unmigrated catalog server probe (`smoke/smoke_test.go:650-660`).\n\n- `smoke/catalog_publication_test.go`\n\n  Extend the shared catalog HTTP helper to authenticate the provisioned staff account once through `/staff/authenticate`, cache the returned assertion, and send it on catalog requests. Remove `organizer_id` from catalog write bodies.\n\n- Update catalog fixture bodies in:\n\n  - `smoke/catalog_orphan_prevention_test.go`\n  - `smoke/catalog_policy_reemit_test.go`\n  - `smoke/catalog_publication_test.go`\n  - `smoke/checkout_test.go`\n  - `smoke/inventory_contention_test.go`\n  - `smoke/multi_event_admission_test.go`\n  - `smoke/operational_holds_test.go`\n\n  Retain organizer fields in inventory, commerce, and other service payloads; remove them only from the affected catalog contract writes.\n\n- `test/browser/channels.mjs`\n\n  Extend the existing real form-submit spec. It must submit the channel form, then assert the redirect/rendered result and re-read the exact affected database row. Add a cross-organizer case that submits a form targeting another organizer’s channel and verifies that the row’s exact `display_name`, `kind`, `enabled`, and `organizer_id` remain unchanged.\n\n  Do not count upstream requests from Playwright: the catalog call occurs inside the Astro SSR handler and is invisible to the browser.\n\n- `docs/adr/ADR-[next].md`\n\n  Add an ADR; the next number should be assigned from the existing ADR registry. It should document:\n\n  - organizer-only assertion payload\n  - validator enforcement for contract operations\n  - inline assertion enforcement for `/internal/channels`\n  - the loss of internal-token-only access to that route\n  - eight-hour TTL convention\n  - exact adversary and non-goals\n\n## Test plan and seams\n\n| Assertion | Test seam | Tier |\n|---|---|---|\n| Token format, HMAC, expiry, nil UUID, empty key | `assertion.go` mint/verify functions | Catalog unit |\n| Contract declares both schemes | Parsed `apispec.Spec`, like `TestCatalogSpecGuardsEveryUnsafeOperationAndOnlyThose` | Catalog contract/invariant |\n| Header name matches contract | Parsed security scheme versus constant | Catalog contract |\n| MAC checked before expiry | Assertion verifier code path plus forged/expired refusal cases | Catalog unit/code review |\n| Body cannot choose organizer | Validator/router request with assertion A and attempted body organizer B; fake store records organizer A | Catalog API seam |\n| Handler refuses absent scope | Direct handler/router request without populated scope | Catalog API seam |\n| `/internal/channels` uses assertion organizer | Hand-mounted route with assertion A and malicious query B; store receives A | Internal-route API seam |\n| SQL still scopes channel data | Real Postgres/browser write and exact reread | Database/browser tier |\n| Session retains assertion server-side | `createSession`/`lookup` round-trip | Back-office session tier |\n| Every client write omits organizer body | `spyFetch` request-body assertions | Back-office client tier |\n| Browser SSR path actually works | `test/browser/channels.mjs` submits forms and checks redirect/database result | Browser tier |\n| Full integration | Shared smoke catalog helper and catalog fixtures | Gateway smoke tier |\n\nTests that should be red before the implementation:\n\n1. The new derived contract test requiring `CatalogOrganizerAssertion` on the 15 operations.\n2. Assertion round-trip and refusal tests.\n3. The API mapping test proving organizer A, not body organizer B, reaches the store.\n4. The `/internal/channels` test with no query and a verified assertion.\n5. Back-office tests asserting no organizer body field and an assertion header.\n6. The browser cross-organizer channel submission test.\n7. Updated smoke catalog writes, once the new contract requires the assertion.\n\nDelete-the-mechanism checks:\n\n- Remove the scope-slot assignment in the verifier and rerun the valid assertion/API mapping tests; they must refuse or fail the expected organizer assertion.\n- Temporarily make a handler use a submitted organizer value; the API mapping test with assertion A/body B must fail.\n- Temporarily restore query-based channel scoping; the internal-route test with query B/assertion A must fail.\n- Remove the store organizer predicate in a real database test and rerun the browser/database reread. It must observe the cross-organizer mutation. A fake-store API test is insufficient for this claim because the fake can enforce scoping in Go.\n- Reintroduce the `SeatMapEditor` hidden organizer field; its exact-body test must fail.\n\nRun generation first:\n\n```text\nmake generate\n```\n\nCommit the generated files, then run:\n\n```text\nmake check\nmake browser\n```\n\n`make check` compares generated output with `HEAD` (`Makefile:23-42`), so running it before committing generated files will report drift.\n\n## Migration and rollout\n\nThere is no database migration.\n\n1. Provision a new, independent `CATALOG_ORGANIZER_ASSERTION_KEY`.\n2. Add it to the catalog deployment environment before starting the new catalog binary.\n3. Deploy catalog and back office as a coordinated cutover.\n4. Restart the back office so its in-process sessions are discarded. Existing cookies refer to entries that no longer exist; users sign in again and receive assertions.\n5. Run the browser-submit and smoke checks after both services are on the new contract.\n\nA phased deployment without compatibility code has a failure window:\n\n- New catalog first: old back-office sessions send no assertion, so writes receive 401.\n- New back office first: old catalog authentication responses contain no assertion, so new sessions cannot perform writes.\n\nThe settled “delete the field” approach therefore requires coordinated deployment or a maintenance window. Adding temporary dual acceptance would be a separate compatibility design and should not be silently introduced.\n\nKey rotation also invalidates existing assertions. Because the key is catalog-only and there is no overlap mechanism in this slice, rotate by restarting catalog and forcing back-office sign-in again.\n\n## Risks and open questions\n\n- `/internal/channels` currently accepts the shared internal token as an alternative to the staff credential (`server.go:228-267`). That token carries no organizer identity. The proposed fix must intentionally remove internal-token-only access to this exact route or introduce a separate organizer-bound service credential. No in-repo production caller for this list route was found; external callers remain an operational risk.\n\n- Schemas such as channel create/update already use `additionalProperties: false` (`openapi.yaml:2909-2947`), but several other affected schemas do not. Removing `organizer_id` from the schema makes it non-load-bearing, but raw clients may still submit an ignored extra property unless those schemas are also tightened. Adding `additionalProperties: false` everywhere would be an additional contract-hardening decision.\n\n- The brief says `DEFAULT_ORGANIZER_ID` affects six functions, but the code has seven default parameter occurrences across seven functions: `catalog.ts:72`, `85`, `131`, `143`, `154`, `165`, and `233`.\n\n- The broad statement that the back office never puts organizer identity in a form is not true for the seat-map editor: `SeatMapEditor.tsx:59-73` serializes it into a hidden geometry field. The plan removes it.\n\n- The assertion does not enforce staff roles. Role checks remain in the back office (`gate.ts:140-149`), while catalog authenticates the calling process and does not independently authorize `admin`, `box_office`, or `finance`.\n\n- The assertion does not close the 13 path-ID transitions, which have no organizer parameter at the store boundary (`services/catalog/internal/store/store.go:696-699`; `postgres.go:1055-1056`). Those need the separate store-tier organizer predicate follow-up.\n\n- The assertion does not provide tamper-evidence against a writer with catalog database access, nor protect a compromised catalog process holding the signing key or a live valid assertion. The ADR should name the adversary precisely: this closes organizer selection by a compromised or buggy deputy process that lacks a valid organizer assertion; it is not a database-integrity mechanism or staff-role authorization mechanism.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan review + final — critique by `main-agent`, Gate 2 taken by agent judgement\n\n`config.models.planReview = main-agent`; drafter was `gpt-5.6-luna`. Different models, so the\ncross-model invariant holds and no degraded flow was run.\n\n**This is a DELTA, not a restatement.** The `kind=plan` draft as amended below is the plan.\n\nMethod note: before reading the draft I wrote my own answers to the brief's five hard questions, so\nthis compares two independently-derived positions rather than rationalizing the draft's. We converged\non the two load-bearing calls (AND-form security requirement; organizer taken from verified context,\nnever compared). Where we differ is recorded below with the evidence, not the opinion.\n\n---\n\n#### Seam verification — every named file and symbol checked at HEAD\n\n`shaping.md` requires this: a drafter that cannot run the gate will hallucinate seams. **All 14 test\nfiles it names exist** (`credentials_test.go`, `seatmap_test.go`, `seatmap_edit_test.go`,\n`channels_test.go`, `staff_test.go`, `session.test.ts`, `SeatMapEditor.test.tsx`, `smoke_test.go`,\nand the six smoke fixtures). Line citations spot-checked and accurate. **Zero hallucinated seams** —\nnotable, and it is why the rest of this critique is about judgement rather than existence.\n\n#### ✅ Accepted — the load-bearing calls\n\n1. **Second declared security scheme, AND-form, in one requirement object.** Correct, and its stated\n   reason (\"one object means AND; separate objects would mean OR\") is the exact trap. Verified\n   independently: `securityRequires` (`write_credential_test.go:181-192`) tests **membership** across\n   the requirement list, so adding a second scheme to the same object keeps\n   `TestCatalogSpecGuardsEveryUnsafeOperationAndOnlyThose` green **with no amendment**.\n2. **Organizer taken from a verified context slot; no handler compares body vs verified.** This is\n   the ticket's whole point (unsubmittable, not validated) and the draft states it as a DoD item.\n3. **The commerce assertion mechanics, reproduced exactly** — MAC before expiry, constant-time,\n   nil-UUID rejection, one indistinguishable error, empty key fails closed. All five are the\n   hard-won details of `assertion.go`; none was dropped.\n4. **Regenerating `web/storefront/src/lib/api-types.gen.ts`.** I checked this because it looked like\n   over-reach: it is correct. `Makefile:33` runs `pnpm run generate:api` from the root, and\n   `check-generate` (`Makefile:42`) diffs that exact path. Missing it fails the gate.\n5. **Delete-the-mechanism checks, including the right one at the right tier** — \"remove the store\n   organizer predicate in a real database test... a fake-store API test is insufficient because the\n   fake can enforce scoping in Go.\" That is AGENTS.md's TKT-236 lesson, applied unprompted.\n6. **Coordinated cutover, no dual-acceptance compatibility shim.** A grace window accepting a missing\n   assertion is the hole this ticket closes, kept open on purpose. Correctly refused.\n\n#### ✅ Three corrections the draft made to MY brief — all verified, all mine to own\n\n1. **`SeatMapEditor.tsx` puts the organizer in a hidden form field.** My shaping said the back office\n   \"passes its session's organizer and never a form value.\" **That is false**, and it is the ticket's\n   own language which I inherited without checking this path. `toEdit(organizerId, sections)`\n   (`SeatMapEditor.tsx:61-63`) serializes `organizer_id` into JSON that is stringified at `:86` into\n   a hidden field. The organizer round-trips **through the browser** and comes back as form input —\n   AGENTS.md's *\"a hidden input is not a checkbox\"* trap, live in the tree. Good catch; in scope.\n2. **Seven default-parameter occurrences, not six** (`catalog.ts:72,85,131,143,154,165,233`). I\n   miscounted from a truncated grep. Verified: `grep -c` returns 8 (the declaration + 7 uses).\n3. **The smoke suite is in scope and my brief never mentioned it.** `smoke/catalog_publication_test.go`\n   authenticates with the staff-write token only (`:38-40,:66`) and sends `organizer_id` in catalog\n   write bodies (`:118,:121,:126,:130`). It has no staff sign-in, so once the field is deleted it must\n   acquire an assertion. Seven smoke files affected. Missing this would have failed `make check` at\n   the smoke stage after the contract change — an expensive, late discovery.\n\n#### 🔶 Amended — four changes to the draft\n\n**A1. The assertion must carry the staff id as well as the organizer. [amend]**\nDraft: organizer + expiry only, arguing staff id \"would add no authorization value because catalog\ndoes not enforce roles.\"\n\nThe premise is true, the conclusion does not follow. `v1.<staff>.<organizer>.<expiry>.<mac>` costs\n36 signed bytes and avoids a **format migration** the moment anything wants the principal — and two\nsuch things are already visible: the follow-up transitions ticket, and catalog's total absence of\nrole enforcement, which the draft itself names as a residual. The version prefix makes migration\nsurvivable, but avoidable beats survivable. Both fields are immutable per staff row\n(`0015_staff_accounts.sql:21`), so neither can go stale.\n\n⚠️ **And the corollary, which the draft did not state and which matters more: do NOT sign the role.**\nADR-042 snapshots `role` at sign-in and explicitly warns it goes stale when a role-change surface\nlands. A signed role would make catalog authoritative about something it cannot refresh. Organizer\nand staff id: safe. Role: not. This constraint must survive into the ADR.\n\n**A2. `POST /staff/authenticate` needs an explicit per-operation security override. [amend — this is\nthe one most likely to ship broken]**\nThe draft says \"keep `/staff/authenticate` using only the staff-write credential\" in the *back-office*\nsection (`catalog.ts`), but never says it in the **contract** section — and that is where it must\nhappen. Verified: the operation has **no `security:` key of its own** and inherits the document-level\nrequirement. If the assertion is added at document level, or the override is forgotten, **the\noperation that issues assertions would itself require one**: an unreachable sign-in, chicken-and-egg\n401, nobody can ever obtain a first assertion.\n\nThe draft's own decision (\"operation-level\" scheme on the 15) makes this survivable, but the\nrequirement must be written down as a DoD item, not left implicit in a client-side sentence. **Add\na test that pins it**: `authenticateStaff` must NOT require `CatalogOrganizerAssertion`.\n\n**A3. Add a test pinning the AND-ness — the invariant cannot catch an OR. [amend, and this is my\nmain addition]**\nThe draft correctly writes the AND form and adds \"derived assertion coverage for exactly the 15\noperations\". Necessary, **not sufficient**. Because `securityRequires` returns true if **any**\nrequirement object contains the scheme, the OR form `[{Staff:[]},{Assertion:[]}]` — one bracket's\ndifference — **passes both the existing invariant and a naive new coverage test**, while meaning\n*either credential alone suffices*. That is a silent auth **downgrade in the exact property this\nticket exists to add**, and nothing local catches it: not the invariant, not `make check`, not\nmutation testing (the mutant flips the mechanism; the assertion was never about co-occurrence).\n\nRequired: assert that for each of the 15, **one requirement object contains BOTH schemes** —\nequivalently, no requirement object names the assertion without also naming the staff credential.\nDerive it from the spec in both directions, like the test it sits beside.\n\n**A4. TTL: equal is not enough — clamp the session to the assertion. [amend]**\nDraft says 8h, matching the session, and stops. Commerce already found and fixed the residual:\nit mints at T1 on catalog's clock, the back office stamps its session at T2, so a session created at\nT2 holding an 8h assertion **outlives it** by the round trip plus clock skew — surfacing near the\nboundary as a mystery 401 on a session the back office still thinks is valid. The storefront parses\nthe expiry back out and clamps to `min(SESSION_TTL_MS, assertionLifetimeMs)`\n(`web/storefront/src/lib/session.ts:230-257, :319-323`), and an unparseable assertion falls back to\nthe session TTL rather than being pre-judged (`:253`). Reproduce that, and write the coupling down in\nboth places as commerce did — nothing enforces it.\n\n#### 🔷 Softened — one risk the draft overstates\n\n**`/internal/channels` losing internal-token access.** The draft calls external callers \"an\noperational risk\". I checked: `git grep \"internal/channels\"` outside catalog returns **exactly one**\ncaller repo-wide — the back office (`web/backoffice/src/lib/catalog.ts:396`, plus its test at\n`web/backoffice/test/api.test.ts:596`). No Go service, no smoke test. The only other users are\ncatalog's own tests via `operatorGet`. The route is hand-mounted, undeclared, and gateway-denied at\nthe edge, so there is no external surface to break. The draft's **conclusion is right and I am\nadopting it unchanged**; only the risk framing is softened, so the implementer does not invent a\ncompatibility shim for a caller that does not exist.\n\n#### ❌ Rejected — nothing\n\nNo part of the draft was rejected. Two items are explicitly deferred rather than rejected:\n- `additionalProperties: false` on the schemas that lack it — the draft correctly flags it as a\n  *separate contract-hardening decision*. Out of scope here. **Note for the implementer:** with\n  `organizer_id` deleted from the schema, a raw client may still submit it as an ignored extra\n  property on the schemas that permit extras. That is harmless *only because* nothing reads it —\n  which is exactly the unsubmittable property, achieved by deletion rather than by rejection. Say so\n  in the ADR so a later reader does not mistake it for a gap.\n- Per-organizer scoping on the 13 path-id transitions — the agreed slice-2 follow-up.\n\n#### 📌 Introduced at plan-review, reviewed by no second model\n\nRequired section. These reached Gate 2 reviewed by nobody but their author (me) — the cross-model\nguarantee covers the draft and the code, not plan-review's own inventions (TKT-57). **Three items:**\n\n1. **A3, the AND-ness test.** A new test requirement, my invention, unreviewed. It is the highest-value\n   item in this critique and it has had exactly one pair of eyes. **ai-review must attack it\n   specifically**, including whether my reading of `securityRequires` is right.\n2. **A1, signing the staff id and the prohibition on signing the role.** A payload-shape decision that\n   is expensive to reverse after tokens exist in the wild.\n3. **A4's clamp requirement.** Copied from a working in-repo precedent, so lower risk, but it is\n   still a design element no second model has seen.\n\n#### Human decisions\n\nNone outstanding. `approach` and `first_slice` were decided by the owner at Gate 1 and the draft\nrespects both. No new question arose that needs a human.\n\n#### Gate 2 verdict — proceeding to Building\n\n**Approved with amendments A1–A4.** Basis: the draft is code-grounded with zero hallucinated seams,\nit independently reproduced every subtle property of the precedent, and it corrected three errors in\nmy own brief — including one (the hidden organizer field) that would have left a live instance of the\nvery trap this ticket closes. The amendments are additive; none reverses a draft decision.\n\nGate 2 is the owner's and is **waived for this run**. `overrides:` at closeout will record that this\napproval was taken by agent judgement, and that A1–A4 plus the three §📌 items entered the plan with\nsingle-model review.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation complete — TDD, by `main-agent` (`config.models.implement`)\n\nSix commits on `TKT-245-organizer-assertion`. Tests first at every step; every mechanism\nmutation-checked, and **three mutation findings changed the code or the tests**.\n\n| # | Commit | What |\n|---|---|---|\n| 1 | `01067c0f` | the mint/verify mechanism |\n| 2 | `7faa8a14` | sign-in hands back the assertion |\n| 3 | `ab798326` | the organizer becomes unsubmittable on the 15 writes |\n| 4 | `6bcec23c` | the back office forwards it; `DEFAULT_ORGANIZER_ID` deleted |\n| 5 | `df6aff26` | signing key provisioned; smoke suite converted |\n| 6 | `b396c24b` | ADR-058 + the browser-submit spec |\n\n#### The plan's amendments, as built\n\n- **A1** — payload is `v1.<staff>.<organizer>.<expiry>.<mac>`; the **role is deliberately not signed**\n  (ADR-042 snapshots it and warns it goes stale), and a test pins the five-part shape so a future\n  field is a deliberate canonical-format change.\n- **A2** — the assertion is required **per-operation on the 15**, never at document level, so\n  `POST /staff/authenticate` does not require the thing it issues. Verified in the contract.\n- **A3** — the AND-ness test exists, and **is demonstrably the only thing that catches its defect**\n  (below).\n- **A4** — the session is clamped to `min(SESSION_TTL, assertion lifetime)`, copying the storefront's\n  fix for the same skew.\n\n#### Three mutation findings\n\n**1. A test of mine could not fail, and I replaced it.** I wrote\n`TestOrganizerAssertionJudgesTheMacBeforeTheExpiry`; it passed. Then I reordered exactly those two\nchecks in the implementation and **it still passed**. The MAC-before-expiry ordering is real and\ndeliberate, but it is not observable from outside — both orders return the same single error for\nevery bad token, by construction. The assertion was pinning the mechanism, not the rule. Replaced\nwith what a holder actually cannot do: revive an expired token by rewriting its expiry, which dies\nwhen the MAC stops covering that field.\n\n**2. The A3 test earns its place, executed rather than argued.** Flipping all 15 requirements from\nthe AND form to the OR form — one bracket, meaning *either credential alone suffices* — leaves\n**every other invariant green**, including the pre-existing `TestCatalogSpecGuardsEveryUnsafe‑\nOperationAndOnlyThose` and my own coverage test. Only the AND-ness test fails. That is the silent\nauth downgrade I flagged at plan-review as reviewed by no second model; it is now demonstrated.\n\n**3. A guard was resting on a fixture that could not reach its failing state.** Deleting the refusal\nfrom `organizerFor` — so it returns the zero scope and `true` — left the **entire package green**.\nEvery test drives the router, where the validator always fills the slot, so nothing could reach the\nunfilled state. Added `TestConvertedHandlerRefusesWhenNoScopeWasVerified`, which calls the handler\ndirectly with a bare context; it now catches a venue created for\n`organizer_id = 00000000-0000-0000-0000-000000000000`.\n\nOther mutants killed by their intended tests: expiry outside the MAC, nil-UUID accepted, empty key\nverifies everything, minting from the wrong organizer, minting on the refusal path, and removing the\nsession clamp. **Constant-time comparison is NOT test-observable** — removing it fails the build on\nan unused import, not an assertion — so it stands on review and the comment, and I am not claiming\ntest coverage it does not have.\n\n#### Two things the plan draft caught that my brief had wrong\n\nBoth were mine, both verified against the code, both in scope:\n\n- **`SeatMapEditor` serialized the organizer into a hidden form field.** My shaping said the back\n  office \"never a form value\" — inherited from the ticket text without checking this path. The tenant\n  made a round trip through the browser and came back as client input: AGENTS.md's *\"a hidden input is\n  not a checkbox\"*, live in the tree. Removed.\n- **The smoke suite was in scope and my brief never mentioned it.** 65 catalog write bodies carried\n  `organizer_id`. Inventory, commerce and reservation bodies keep theirs — those services take an\n  organizer legitimately.\n\n#### A test that asserted the gap, inverted rather than deleted\n\n`TestStaffCredentialCanStillEnumerateAndMutateAcrossTenants` pinned the chain **as present** and its\nfailure message named the condition for changing it: *\"If this now REFUSES… change this test to assert\nthe refusal. Do not simply delete it: it is the record of what was open.\"* Done — renamed, inverted,\nand the history kept in the comment.\n\n#### A reversal worth flagging for review\n\n`GET /internal/channels` now requires an assertion **whatever credential opened the door, including\nthe shared internal token** — reversing what a test asserted. The token names no tenant, so keeping a\nquery-parameter path \"for services\" would leave the enumeration open to the widest credential in the\nsystem while closing it for the narrowest. Verified first: the back office is the route's **only**\ncaller repo-wide. Recorded in ADR-058 § 5.\n\n#### Scope honesty\n\nThe **13 path-id transitions are NOT closed** and are the agreed follow-up slice. ADR-058 says so in\nits own words rather than leaving a reader to infer it: *\"catalog can verify the organizer\" is true of\nthe 15 writes and the channel read, and **false of the transitions***.\n\nGate status and the PR follow in the next comment.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### ai-review pass 1 — `gpt-5.6-luna` (codex `adversarial-review`, branch diff vs `origin/main`)\n\n**Run judged a PASS on the runner's criteria**: correct scope, conclusive verdict, three code-cited\nfindings, no rubric template, no skill hijack. Verdict `needs-attention`, *\"Do not ship\"*.\n\nThe reviewer was given the **diff only** — no plan, no ADR, no reasoning — deliberately, so it judged\nthe code against reality rather than against my intent.\n\n**All three findings are factually correct.** I verified each by executing it, not by reading it. One\nis mine and is fixed; two are scope disagreements I am overriding with reasons, and both are recorded\nin `overrides:` at closeout.\n\n---\n\n#### F3 [medium] — **CONFIRMED, FIXED.** The AND-ness test reproduced the bug it was fixing\n\n*\"The test examines only requirement objects that contain `CatalogOrganizerAssertion`… it ignores\nsibling requirement objects without the assertion.\"*\n\n**Correct, and it is the most valuable finding of the three.** My test — the one I flagged at\nplan-review as *introduced at plan-review, reviewed by no second model*, and the one I told this\nthread was \"demonstrably the only thing that catches its defect\" — had the same shape of hole as the\ninvariant it was written to reinforce.\n\nVerified by executing it: adding `- CatalogStaffWriteCredential: []` as a second alternative to one\noperation left **every test green**, including the AND-ness test, the staff-credential invariant and\nthe coverage test.\n\n**One correction to the finding.** It predicted runtime would then *accept* a staff-credential-only\nrequest. It does not: `kin-openapi` requires **every** declared alternative to pass rather than any\none of them, so a probe returned **401**. I only know that because I ran it — the reviewer's argument\nand my own were both plausible. The finding still stands on its own terms: the **contract** would have\ndeclared a weaker way in, and the contract is what the next reader and every other client believe.\n\nFixed in `3e488c59`: the rule is now stated over **all** alternatives of an assertion-protected\noperation. Re-applying the mutant now fails, naming the operation and the alternative index. A new\nruntime test (`TestConvertedWriteRefusesEitherCredentialAlone`) pins that neither credential alone\nopens a converted write — because \"the declaration is right\" and \"the guard refuses\" are two claims\nand this ticket needs both.\n\n---\n\n#### F1 [high] — **CONFIRMED as fact, OVERRIDDEN as scope.** Path-id writes remain cross-tenant\n\n*\"A caller holding the process-wide staff credential can choose another tenant's series and\nperformance IDs and mutate that series.\"*\n\n**Every word is true**, and I verified it at the store tier rather than taking it on trust:\n`AttachPerformanceToSeries` (`postgres.go:184-202`) reads `organizer_id` from **both** rows and\ncompares them **to each other** — never to a caller. Two resources of the same victim tenant satisfy\nit. `AttachPerformanceToSeries` (`server.go:747`) passes both caller-chosen ids straight through.\n\n**Why I am shipping anyway.** This is the **13 path-id transitions**, which are the *agreed* slice\nboundary — the owner chose it at Gate 1 after I enumerated both classes — and they are named as an\nopen residual in **ADR-058 § what this does NOT close, item 5**, in these words:\n\n> *\"catalog can verify the organizer\" is true of the 15 writes and the channel read, and **false of\n> the transitions** — say it that way.*\n\nThe reviewer's own sentence concedes the point: *\"ADR-058 acknowledges 13 such operations, but that\nmeans the claimed tenant boundary is incomplete.\"* The boundary **is** incomplete, the ADR says so\nbefore anyone asks, and the PR description says so too. The remedy is also genuinely different — a\nstore-tier organizer predicate on lookup, not a credential — so folding it in would mean a second\nimplementation reviewed by nobody, inside a diff that is already 43 files.\n\n**What would change my mind:** if this were the *only* record of the gap, shipping would be\nindefensible. It is not — it is in the ADR, the PR, and the follow-up ticket I am filing at closeout.\nRecorded in `overrides:` so the owner can reverse it.\n\n---\n\n#### F2 [high] — **CONFIRMED as fact, OVERRIDDEN as scope.** No entropy floor on the signing key\n\n*\"Startup checks only that `CATALOG_ORGANIZER_ASSERTION_KEY` is non-empty and differs from two\ncredentials… a deployment configured with a value such as `x` starts successfully.\"*\n\n**True**, verified: `runtimecfg.RequiredCredential` (`runtimecfg.go:123-158`) validates *transport\ncharacters* — emptiness, retired literals, edge whitespace, header-safety — and never entropy.\n\n**But this is not this ticket's defect, and fixing it here would be the wrong shape.** The same\nfunction guards **every credential in the system**, including `COMMERCE_CUSTOMER_ASSERTION_KEY`\n(`commerce/cmd/commerce/main.go:145`) — the *direct precedent* for this mechanism, with an identical\nthreat model. No service anywhere enforces a key length. So this ticket **inherits** a system-wide\nproperty rather than introducing one, and a catalog-only entropy floor would leave the identical hole\nopen one service over while looking like it had been closed — which is precisely the \"configured-\nlooking and wrong\" failure ADR-042's own credential guard exists to prevent.\n\nThe right fix is a `runtimecfg.RequiredSigningKey` used by **both** assertion keys at once, and that\nis a shared-package change touching two services. Filed as its own ticket at closeout, and recorded\nin `overrides:`.\n\nMitigations that already hold: `make up` and `scripts/env-bootstrap.sh` generate 32 bytes from\n`/dev/urandom`, compose ships no default and refuses to start without the variable, and the key is\nnever sent to the back office.\n\n---\n\n#### Verdicts\n\n| # | Sev | Verdict | Disposition |\n|---|---|---|---|\n| F3 | medium | **confirmed** | **fixed** in `3e488c59` + a new runtime test |\n| F1 | high | **confirmed** | **overridden** — agreed slice boundary, named in ADR-058 §5, follow-up ticket |\n| F2 | high | **confirmed** | **overridden** — pre-existing and system-wide, fix belongs in `runtimecfg`, follow-up ticket |\n\n#### Second pass: REQUIRED\n\nThe fix diff is a **new test that changes what the suite refuses**, on an auth path — non-trivial by\nthe skill's rule (*\"a blocking finding was fixed\"*, *\"a new or rewritten test\"*). The fix diff is also\ncode no one has reviewed. Running pass 2 against it now.\n"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Catalog authenticates the calling PROCESS, not the staff member behind it. Verified at HEAD: 15 of 29 contract writes take organizer_id from the request body and catalog trusts it (services/catalog/internal/api/server.go, channels.go); the back office passes staff.organizerId from its session (web/backoffice/src/pages/channels.astro:72,90,107,135) and never a form value -- a discipline in one codebase, not a boundary catalog enforces."
    },
    "scope": {
      "state": "met",
      "note": "IN: an organizer identity catalog verifies independently of the request body, applied across the whole back-office write surface; the ADR recording the choice and its residual. OUT: multi-organizer staff sign-in (ADR-042 sub-decision 4 scopes it as future work); inventory's credential (TKT-244/ADR-057); commerce and access surfaces. See first_slice -- the surface splits into two classes that do not have the same fix."
    },
    "uncertainties": {
      "state": "met",
      "note": "Resolved by reading, not asking. (1) staff_accounts ALREADY carries organizer_id NOT NULL REFERENCES organizers(id) (migration 0015:21), and AuthenticateStaff already returns it as StaffPrincipal.OrganizerId (internal/api/staff.go:76-80) -- catalog already knows the fact it fails to remember. (2) The back-office session is in-process and its token is opaque (ADR-042 sub-decision 2; web/backoffice/src/lib/session.ts:10-11), so catalog cannot verify a session cookie -- any fix must carry the organizer in something catalog can check itself. (3) A second, UNSTATED exposure class: 13 state-transition writes take only a path id and reach the store with no organizer at any layer (store.PublishSeries(ctx, id) -- internal/store/store.go:696, postgres.go:1055). Those have no field to compare, so the body-field framing in the ticket text does not cover them."
    },
    "dependencies": {
      "state": "met",
      "note": "No open blockers. Depends on decisions already landed, not on unshipped work: ADR-056's partnerScope is a working in-repo template for exactly this (credential -> scope resolved in the AuthenticationFunc, 'nothing in it ever comes from the request body' -- services/commerce/internal/api/partner_auth.go:36-43,103-115), and it names ADR-053's staff credential reaching across tenants as the anti-pattern it was written to avoid (:105-107)."
    },
    "approach": {
      "state": "met",
      "note": "DECIDED by the owner at Gate 1 (2026-08-15): mechanism 2, an HMAC-signed organizer assertion. Catalog mints it at POST /staff/authenticate (it already holds the organizer there -- internal/api/staff.go:78); the back office keeps it server-side in its existing in-process session (web/backoffice/src/lib/session.ts:53-56, the Entry interface gains a field exactly as the storefront's did) and forwards it as a header on writes; catalog verifies it in the openapi3filter AuthenticationFunc seam and takes the organizer from the verified payload. organizer_id is DELETED from the 15 request types -- unsubmittable, not validated (AGENTS.md). Modelled on commerce's customer assertion (services/commerce/internal/api/assertion.go), which solved the identically-shaped problem in TKT-221."
    },
    "first_slice": {
      "state": "met",
      "note": "DECIDED by the owner at Gate 1 (2026-08-15): slice 1 = the mint/verify mechanism, the verification seam, and the 15 body-organizer writes. The 13 path-id transitions become a follow-up ticket -- they need a store-tier organizer predicate, not a credential change, and share no code with this slice. See the kind=readiness addendum for the two extra sites that belong to slice 1 for consistency (GET /internal/channels' query-string organizer, and the back office's DEFAULT_ORGANIZER_ID fallback)."
    },
    "success": {
      "state": "met",
      "note": "From the ticket, sharpened by the read: (1) catalog determines the organizer for a back-office write without the caller asserting it -- make it UNSUBMITTABLE rather than validated (AGENTS.md; TKT-244 took three passes to learn this); (2) an ADR naming the adversary per ADR-021 -- a compromised deputy PROCESS, not a DB writer -- and stating what stays open; (3) coverage across the sliced surface, with omission enumerated in both directions. Gates: make check green; make browser for any touched write form (test/browser/channels.mjs is the pattern)."
    },
    "context_memo": {
      "state": "met",
      "note": "Re-resolved at claim time. The 2026-08-10 bake was stale: ADR-056 and ADR-057 both landed after it and both bear on the central decision. All 8 cited ADRs verified to resolve to real files at HEAD. governingTests added -- catalog's contract invariant is enforced by a TEST, not an ADR, and is discoverable only by failing the gate."
    }
  }
}
