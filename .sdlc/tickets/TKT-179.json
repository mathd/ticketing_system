{
  "key": "TKT-179",
  "type": "Story",
  "parent": "TKT-35",
  "classification": "complex",
  "summary": "US-023a: A seat map can be marked orphan-preventing (setting only, nothing enforces it yet)",
  "status": "Building",
  "labels": [
    "agent:coding"
  ],
  "assignee": "agent (orchestrator: main-agent / Claude Opus 5)",
  "pr": null,
  "links": [],
  "description": "**First of four slices carved out of TKT-175** when its plan showed the ticket to be ~50 files across five services. The architecture is decided — see TKT-175's `kind=plan-final`.\n\nShips the **decision** and the **setting**, and nothing that acts on either.\n\n**Acceptance criteria:**\n- [ ] **ADR-041** records the arbitration decision: geometry fetched once at provisioning for the exact immutable published version (ADR-029), projected into inventory, decided under the pool lock. It states why a claim-path catalog call (ADR-010), a new `seat_map.published` subscription, and a commerce pre-check were each rejected — the last because an advisory check cannot satisfy a contention AC.\n- [ ] `seat_maps` gains `orphan_prevention_enabled boolean NOT NULL DEFAULT false`; a guarded Down refuses while any map has it on.\n- [ ] Catalog's create/read/edit contract carries it. An edit that omits it **inherits the predecessor's value**; an explicit value applies only to the newly minted version (ADR-029: versions are immutable).\n- [ ] The back-office venue page can set it on create and on edit.\n- [ ] **Nothing reads it.** No event payload changes, no inventory change, no claim behaviour changes. Every existing map and every existing test behaves exactly as before.\n- [ ] Spec declared, generated code committed before the gate, browser evidence for the back-office form submission (the repo's rule: an unsubmitted Astro form is unverified).\n\nQuality gates: `make check` green; browser submit evidence.",
  "context": {
    "capturedAt": "2026-08-02",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-08-02",
        "rule": "Local gate: `make check` ... Commit regenerated code (`openapi_gen.go`, `api-types.gen.ts`) before running the gate — `check-generate` diffs against HEAD, so an uncommitted regen reads as drift and fails the run."
      }
    ],
    "reference": [
      {
        "summary": "The architecture, every design decision and the full test plan live in TKT-175's kind=plan and kind=plan-final — read those first, they are not repeated here",
        "link": "board:TKT-175"
      },
      {
        "summary": "Epic lineage",
        "link": "docs/product/prd-v1.md"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-029",
        "why": "A published seat-map version is immutable and an edit mints a new one. That is why the setting is per VERSION and why an edit's omission must inherit rather than reset — and it is the property the whole TKT-175 architecture rests on."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first: the catalog spec changes and openapi_gen.go plus api-types.gen.ts are committed before the gate."
      },
      {
        "id": "ADR-022",
        "why": "The migration runs out-of-band as the service's migrate subcommand; the server path never migrates."
      }
    ]
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ Readiness — PASS\n\nSmallest possible slice: a decision and a flag nobody reads. Its whole risk surface is the migration default and the edit-inheritance rule, both of which are directly testable.\n\nShaping is inherited: TKT-175's `kind=plan` (gpt-5.6-sol) and `kind=plan-final` (main-agent) decided the architecture, the design, the test plan and the risks before this ticket existed. This child re-scopes delivery, not thinking."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-179\n\n**Selection reason.** First of the four slices TKT-175 was split into, and the only one with no blockers. It carries **ADR-041**, which the other three depend on.\n\n**Branch.** `TKT-179-orphan-prevention-setting`, cut from `origin/main` at `762ee6fc`.\n\n**⚠️ Gate waiver.** Gateless under the standing owner waiver for TKT-35 and its descendants.\n\n**Planning inherited, deliberately.** TKT-175's `kind=plan` (`gpt-5.6-sol`) and `kind=plan-final` (`main-agent`) decided the architecture, the design and the test plan for all four children before this ticket existed. Re-planning each slice would re-litigate a decision already taken and cross-model reviewed. The plan-review that produced the split is the plan for this ticket.\n\n**Stop-condition pre-check.** One additive migration (`seat_maps.orphan_prevention_enabled boolean NOT NULL DEFAULT false`, guarded Down) — anticipated by the ticket's own AC. No auth, no money, no CI config. Nothing reads the column, so no behaviour changes anywhere.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation — TKT-179\n\n**Implemented by `main-agent` (Claude Opus 5)**, per `config.models.implement`. Branch `TKT-179-orphan-prevention-setting`, commit `268a6ab6`.\n\n#### What shipped\n\n- **`docs/adr/ADR-041-orphan-seat-arbitration.md`** — the decision, with the three rejected shapes and *why* each fails: a claim-path catalog call violates ADR-010, a new `seat_map.published` subscription buys drift, and a commerce pre-check is advisory and so cannot satisfy a contention requirement even in principle. It also fixes the rules the later slices implement — adjacency is `position` order, only *newly* orphaned seats are refused, a row end has one neighbour, liveness is `released_at IS NULL` after `sweepExpired`, and `orphaned_seats` must be a distinct wire code from `seat_taken` because the identities are seats the buyer never requested.\n- **Migration `0014`** — `seat_maps.orphan_prevention_enabled boolean NOT NULL DEFAULT false`, Down guarded against discarding enabled maps.\n- **Store** — the field on `SeatMap`, `SeatMapInput`, and `EditSeatMapInput` (as a **pointer**), carried by create, the geometry read, the venue list and the version history.\n- **Contract** — optional on `SeatMapCreate` (default false), **required** on `SeatMap` responses, optional on `SeatMapEdit` where absent means inherit.\n- **Back office** — a checkbox on the create-map form.\n\n#### The one design point worth re-reading\n\n`EditSeatMapInput.OrphanPreventionEnabled` is a `*bool`, and the pointer survives the wire-to-store mapping. Collapsing it to a `bool` at the boundary would turn *\"the staffer said nothing\"* into *\"the staffer said off\"* — an edit is a geometry change, and silently clearing a revenue rule on every edit is the kind of defect that is invisible until an on-sale ends with a gap-filled house. `COALESCE($3, orphan_prevention_enabled)` in the version-copy is where inheritance actually happens.\n\n#### TDD\n\n`TestSeatMapOrphanPreventionSetting` was written first and observed red (`SeatMap has no field or method OrphanPreventionEnabled`). `TestEditSeatMapInheritsOrphanPrevention` covers the three cases that matter: omitted inherits, explicit applies to the new version, and the predecessor is untouched (ADR-029).\n\n**The contract change also proved itself**: making the field required on responses broke the back-office test fixture at compile time rather than letting it pass with a silent default. That is ADR-028's discipline reaching TypeScript.\n\n#### Nothing reads it\n\nNo event payload changed, no inventory code changed, no claim behaviour changed. The schema-4 golden is untouched. That is the point of the slice.\n"
    }
  ]
}
