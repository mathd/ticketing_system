{
  "key": "TKT-77",
  "type": "Story",
  "summary": "US-014: Operational holds — house, artist and kills",
  "status": "Done",
  "labels": [],
  "assignee": "claude",
  "pr": {
    "number": 53,
    "url": "https://github.com/mathd/ticketing_system/pull/53",
    "state": "merged"
  },
  "description": "### US-014: Operational holds — house, artist and kills\n**As** box-office staff, **I want** to place named operational holds (house seats, artist\nallotment, kills) that sit outside public sale, and release or convert them without racing the\npublic. **Priority:** P0 **Depends on:** —\n**Acceptance Criteria:**\n- [ ] Claims gain an operational type with a named purpose (`house | artist | kill | other` +\n      label); operational holds have **no TTL**, count against pool capacity, and are placeable/\n      releasable via staff/internal API only (gateway keeps them off the public edge).\n- [ ] **Convert without a gap:** converting an operational hold (whole or partial quantity) into\n      a buyer-purchasable claim happens atomically under the pool lock — released-to-order\n      capacity is never observable as publicly available in between.\n- [ ] Partial release/convert supported (release 3 of a 10-seat house hold); the remainder stays\n      held; all transitions journal-style auditable (who/when/why on the claim history).\n- [ ] Availability reads distinguish sellable from operationally-held capacity for staff, while\n      the public read keeps a single `available` number.\n\n**Quality gates (every story):**\n- `make check` green (lint + unit + integration + smoke; mirrors CI)\n- Contract tests pass for any touched service boundary (ADR-009 drift check)\n- Money/ticket-path: audit-trail invariants hold (ADR-003)\n- Read-endpoint changes: ADR-004 TTL tier declared with rationale\n\nParent epic: TKT-4. Full lineage: docs/product/prd-v1.md §TKT-4.",
  "links": [],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Epic COS 2: staff place/release/convert named house/artist/kill holds outside public sale; today every claim is a buyer hold."
    },
    "scope": {
      "state": "met",
      "note": "IN: typed claims with purpose+label, no TTL, staff/internal API, atomic partial convert under the pool lock, staff vs public availability split. OUT: box-office UI (TKT-16/22), pricing of converted seats (TKT-5)."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Whether convert lands as buyer-hold or direct-confirm is settled in Planning against the commerce checkout seam — the no-gap property is the requirement either way."
    },
    "dependencies": {
      "state": "met",
      "note": "None."
    },
    "approach": {
      "state": "met",
      "note": "Claim type + purpose columns; convert = single transaction re-typing quantity under the pool FOR UPDATE; extend availability SQL; contention test gains a convert-vs-public race."
    },
    "first_slice": {
      "state": "met",
      "note": "Place a 10-seat house hold, release 3, convert 2 to a buyer claim — public availability never shows the converted 2 in between."
    },
    "success": {
      "state": "met",
      "note": "The 4 ACs + quality gates; the no-gap convert race test is the load-bearing signal."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked."
    }
  },
  "context": {
    "binding": [
      {
        "id": "ADR-010",
        "rule": "The inventory database is the sole write-side correctness boundary. Each mutation locks the inventory pool row first, then its claim rows; operations addressed by hold ID discover the pool with a non-locking read, then re-read the claim after locking the pool — every path has the same pool-to-claim lock order. A claim is held, confirmed, released, or expired; the last three are terminal. PostgreSQL time decides expiry: a transaction expires due holds before capacity accounting, so cleanup jobs are optional optimizations. Confirmed plus unexpired-held quantity may never exceed pool capacity. Hold creation requires an organizer-scoped idempotency key, single-use across the claim's full lifecycle. Reserved seats, date ranges, and entitlements reuse the lifecycle and lock ordering but add their own resource constraints.",
        "date": "2026-07-12"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-010",
        "why": "Operational holds are claims: same lifecycle, same pool→claim lock order; no TTL means they are exempt from the expiry sweep, not from the invariant."
      },
      {
        "id": "ADR-009",
        "why": "New staff endpoints are contract-first; the gateway must keep them off the public edge like the existing transition routes."
      },
      {
        "id": "ADR-022",
        "why": "Migrations out-of-band."
      }
    ],
    "reference": [
      {
        "summary": "PRD v1 §TKT-4 decomposition — the story text is the source of truth",
        "link": "docs/product/prd-v1.md"
      }
    ],
    "capturedAt": "2026-07-16"
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\nShaped at TKT-4 decomposition (2026-07-16). Classification: complex. All 8 DoR items met/deferred — claimable once prioritized (and blockers Done, where wired)."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\nClaimed by the agent (session model orchestrating; plan draft routes to gpt-5.6-sol per config.models).\n\nSelection reason: only P0 in Ready; zero open blockers; the epic verdict's soft-ordering recommendation lands US-014 before US-015/US-016 (shared claim-type surface). Branch: TKT-77-operational-holds off origin/main."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n**Draft by gpt-5.6-sol** (codex task task-mrnht5gw-9vxdxs, read-only, effort high). Verbatim below; critique follows in plan-review.\n\n---\n\n# TKT-77 / US-014 — Execution plan: Operational holds\n\n## Definition of Done\n\n- [ ] **AC1 — Operational claims:** Inventory claims support `buyer | operational`; operational claims require `purpose ∈ house | artist | kill | other` and a non-blank label, have `expires_at = NULL`, survive expiry sweeps, and count in the existing pool-capacity invariant.\n- [ ] **AC1 — Internal-only:** create, release, convert, history, and staff availability endpoints require `X-Internal-Token` and live under `/internal/`; gateway requests to every operational route return 404.\n- [ ] **AC2 — Gapless conversion:** whole or partial conversion replaces operational quantity with a buyer `held` claim in one transaction while holding the pool row lock. No committed state exposes the converted quantity as public availability.\n- [ ] **AC3 — Partial operations:** release/convert validate `0 < quantity <= remaining`; partial operations decrement the source claim, while whole operations retain its original quantity and mark it `released`.\n- [ ] **AC3 — Idempotency/audit:** every mutation has an organizer-scoped idempotency key and fingerprint; replay returns the original outcome, changed input conflicts, and claim history records actor, database time, reason, before/after quantity and status, plus the converted claim link.\n- [ ] **AC4 — Availability:** the public response shape remains unchanged and subtracts both buyer and operational holds; a separate staff response exposes `buyer_held`, `operational_held`, `confirmed`, and `sellable_available`.\n- [ ] Public availability remains seconds-tier: `public, max-age=5, s-maxage=5`. Staff availability and history declare the zero-TTL tier: `Cache-Control: no-store`.\n- [ ] Inventory and Commerce OpenAPI contracts, generated Go models, runtime validation, and boundary tests agree.\n- [ ] `make generate` produces no uncommitted drift, then `make check` passes unchanged as the local/CI gate.\n\n## Design decisions\n\n1. **Claim representation — extend `claims`, do not add parallel inventory.**  \n   Add `claim_kind`, nullable `operational_purpose`, nullable `operational_label`, and make `expires_at` nullable. A database check requires buyer claims to have expiry and no operational metadata, and operational claims to have valid purpose/label and no expiry. This preserves ADR-005/010’s single claim primitive.\n\n2. **Live-capacity predicate — centralize it.**  \n   Capacity accounting treats `finalizing` as live, buyer `held` as live only while `expires_at > now()`, and operational `held` as live regardless of time. Every sweep explicitly targets buyer claims; `NULL` must never accidentally mean expired or free.\n\n3. **Partial mechanics — decrement the source, create a row only for conversion.**  \n   Partial release changes operational quantity from `N` to `N-q`; partial conversion does the same and inserts one buyer claim for `q`. This preserves the staff-facing hold ID for the remainder and avoids chains of replacement operational rows.\n\n4. **Idempotency — use claim history as the mutation registry.**  \n   Give request-backed history rows a unique `(organizer_id, idempotency_key)` and persisted fingerprint. All create/release/convert paths consult it after locking the pool; same operation/fingerprint replays, any other reuse conflicts. Store immutable result quantities and related claim ID so replay does not report a later source state.\n\n5. **Conversion result — a normal buyer `held` claim with TTL.**  \n   Do not confirm directly: payment has not happened. The child receives the existing Inventory TTL, ticket type, integer unit amount and currency, then follows today’s `finalizing → confirmed/released` checkout path.\n\n6. **Conversion orchestration — staff calls Commerce.**  \n   Add an internal Commerce conversion endpoint. Commerce resolves the requested ticket type through the existing Catalog offer seam—no price override or new pricing policy—then calls Inventory’s internal conversion endpoint and persists the returned child as the existing `held` reservation shape. Deterministic reservation/buyer IDs and the forwarded idempotency key repair a crash after Inventory commits but before Commerce persists.\n\n7. **No-gap transaction — swap under one pool lock.**  \n   Inventory discovers the source pool without locking, begins a transaction, locks the pool, re-reads/locks the operational claim, sweeps expired buyer holds, validates replay and remaining quantity, inserts the TTL buyer claim, decrements/releases the operational source, appends history, and commits. MVCC readers see either the old operational quantity or the new operational-plus-buyer quantities; both reserve the same total.\n\n8. **Audit trail — append-only local claim history, not domain events.**  \n   Add `claim_history` rather than last-actor columns because a claim has many partial mutations. Record action, actor, reason, DB timestamp, before/after status and quantity, related claim, idempotency key, and fingerprint. Add an update/delete-rejecting trigger for application-level append-only behavior, but make no tamper-evidence claim against a database writer. Inventory still emits no domain events.\n\n9. **Audit coverage — route every claim mutation through the append path.**  \n   Operational actions use the supplied staff actor/reason; Commerce transitions identify the service and transition reason; automatic expiry records `system / ttl_elapsed`. Migration creates an explicit `migrated_baseline` for existing claims rather than inventing historical transitions.\n\n10. **Availability shapes — separate endpoints.**  \n    Keep `/slots/{id}/availability` and its schema unchanged: `held` is total live held quantity and `available` is sellable capacity. Add `/internal/slots/{id}/availability` with the staff-only breakdown; never add operational fields to the public schema.\n\n11. **Decision record.**  \n    Capture representation, decrement semantics, Commerce orchestration, transaction ordering, audit scope, and cache tiers in `docs/adr/ADR-023-operational-claim-conversion.md` **(NEW)**.\n\n## API surface\n\n- Inventory: `POST /internal/operational-holds`\n- Inventory: `POST /internal/operational-holds/{id}/release`\n- Inventory: `POST /internal/operational-holds/{id}/convert` — Commerce-to-Inventory\n- Inventory: `GET /internal/operational-holds/{id}/history`\n- Inventory: `GET /internal/slots/{id}/availability`\n- Commerce: `POST /internal/operational-holds/{id}/convert` — staff orchestration entry point\n\nMutation bodies require `organizer_id`, positive `quantity`, `actor`, and non-blank `reason`; create also requires purpose/label, while Commerce conversion requires `ticket_type_id`. All mutations require `Idempotency-Key`.\n\n## File-level change list\n\n- `docs/adr/ADR-023-operational-claim-conversion.md` **(NEW)** — decision record.\n- `services/inventory/internal/store/migrations/0003_operational_holds.sql` **(NEW)** — schema, checks, history, baseline, indexes and append-only trigger.\n- `services/inventory/internal/store/store.go` — nullable expiry, common live predicate, audited existing creation/transition/expiry paths, split public/staff availability accounting.\n- `services/inventory/internal/store/operational.go` **(NEW)** — place, partial release and atomic conversion transactions.\n- `services/inventory/internal/store/operational_holds_smoke_test.go` **(NEW)** — PostgreSQL-backed store tests.\n- `services/inventory/internal/api/server.go` — register internal routes and shared credential guard.\n- `services/inventory/internal/api/operational.go` **(NEW)** — staff handlers and validation.\n- `services/inventory/internal/api/server_test.go` — auth, strict request, response-shape and cache-policy tests.\n- `services/inventory/api/openapi.yaml` — operational/staff contracts and conditional claim schemas.\n- `services/inventory/internal/api/openapi_gen.go` — regenerated Inventory models.\n- `services/commerce/internal/api/server.go` — register authenticated conversion orchestration.\n- `services/commerce/internal/api/server_test.go` — Catalog/Inventory seam, replay and failure-window tests.\n- `services/commerce/api/openapi.yaml` — internal conversion contract.\n- `services/commerce/internal/api/openapi_gen.go` — regenerated Commerce models.\n- `smoke/smoke_test.go` — authenticated internal-service request helper over the Compose network.\n- `smoke/inventory_contention_test.go` — convert-vs-public adversarial race.\n- `smoke/checkout_test.go` — converted reservation completes through existing checkout.\n- `smoke/contract_validation_test.go` — operational gateway routes are 404 and touched responses conform.\n- `scripts/smoke.sh` — run all Inventory store smoke tests, removing the current single-test `-run` allowlist.\n\n`gateway/cmd/gateway/main.go` needs no code change: its verified generic `/api/<service>/internal/` 404 boundary already covers the new routes.\n\n## Test plan by seam\n\n- **Inventory store/PostgreSQL seam:** prove no-TTL survival across sweeps; capacity rejection; purpose/label checks; partial and whole release/convert; TTL child creation; exact history; replay and changed-fingerprint conflict; wrong organizer; terminal and over-quantity conflicts.\n- **Inventory `server_test.go` contract seam:** prove missing/wrong token is rejected before store access, strict JSON, required actor/reason, public shape unchanged, staff breakdown present, public 5-second cache and staff/history `no-store`.\n- **Commerce `server_test.go` service-boundary seam:** prove offer resolution, pool/ticket-type match, integer pricing propagation, internal token, forwarded idempotency, deterministic reservation identity, retry repair after simulated post-conversion persistence failure, and no direct confirmation.\n- **Contention smoke seam:** fill a capacity-10 pool with one operational hold, race a partial conversion against many public one-seat hold attempts, and assert zero public grants plus final accounting `operational_held=7`, `buyer_held=3`, `sellable_available=0`. This specifically fails an implementation that commits release before buyer-claim creation.\n- **Checkout smoke seam:** place operational hold internally, convert through Commerce, then submit the returned reservation through the existing public checkout and verify normal finalize/payment/confirm completion.\n- **Gateway/contract seam:** assert Inventory and Commerce operational paths return 404 through the gateway; validate direct internal responses against committed OpenAPI documents and served contracts against disk.\n\n## Migration plan\n\n1. Add transactional `0003_operational_holds.sql`; use plain `CREATE INDEX`, never `CONCURRENTLY`.\n2. Backfill existing rows as `claim_kind='buyer'`; relax `expires_at` only after the kind/metadata check is ready.\n3. Create `claim_history`, its organizer/claim/related-claim indexes, unique request-key index, immutable trigger, and explicit current-state baseline rows.\n4. Supply a reversible down migration that removes the new trail and constraints only after rejecting any remaining operational rows.\n5. Rely on the existing embedded `store.Migrate`, Inventory `migrate` subcommand, `inventory-migrate` one-shot job, and `service_completed_successfully` dependency. The server path remains migration-free.\n6. The existing migration smoke test derives the latest checked-in version, so it will prove `0003` ran out-of-band before Inventory started.\n\n## Risks / pre-mortem\n\n- **A missed expiry predicate treats `NULL` as free capacity.** Centralize the live-claim SQL and test every mutation plus both availability reads against an aged operational hold.\n- **Commerce crashes after conversion and strands a TTL claim.** Deterministic IDs and operation replay repair it before expiry; document that an expired child requires a new staff operation rather than silently reconverting.\n- **Audit “who” is overstated.** Until staff identity exists, `actor` is an assertion from an authenticated internal caller, not cryptographically verified human identity; say so in the ADR and contract.\n- **Public contract leaks operational composition or changes cache behavior.** Pin the old response schema and headers in API and smoke tests; expose breakdown only under `/internal/`.\n- **Idempotent partial replay reapplies quantity or reports a later remainder.** Return the immutable history outcome, not the source claim’s current mutable quantity, and race duplicate requests in the store test.\n\n_Read-only drafting only; no files were changed and no builds or tests were run._"
    },
    {
      "stage": "plan-review",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n**Final plan** — draft by gpt-5.6-sol, critiqued by main-agent (config.models: plan=gpt-5.6-sol, planReview=main-agent — distinct, invariant holds; implement=main-agent vs aiReview=gpt-5.6-sol also distinct).\n\n### Critique verdicts on the draft\n\n**Accepted (verified against HEAD):**\n- All 19 named files exist or are marked NEW — verified (`smoke/checkout_test.go`, `smoke/contract_validation_test.go`, `smoke/smoke_test.go`, gateway generic `/api/*/internal/` 404 at `gateway/cmd/gateway/main.go:103`, inventory transition blocks at `:120-130`).\n- The `scripts/smoke.sh` finding is real and sharp: line 90 still runs inventory store tests with `-run TestGroupedDaysConvergeOnOneInventoryPool` — the allowlist defect the commerce/catalog/access blocks document having fixed (it stranded tests twice before). Removing it is in scope and the right call.\n- Decision 5 (convert → buyer `held` with TTL, never direct-confirm): correct and load-bearing — direct confirm would mint admissions with no order journal entry, violating ADR-003/ADR-011.\n- Decision 6 (commerce orchestrates conversion): **verified load-bearing, not scope creep** — checkout accepts only a commerce `reservations` row (`services/commerce/internal/api/server.go` reserve/checkout seam), and reservations are only minted by the public reserve path with a fresh hold. An inventory-only convert would strand the child claim unpurchasable → AC2 unmet. Deterministic-ID crash repair mirrors the existing reserve idiom (`uuid.NewSHA1(\"reservation:\"+org+\":\"+key)`) — same pattern, verified at HEAD.\n- Decision 2 (centralized live-capacity predicate): the NULL-expiry trap is real — today's predicates (`store.go:134,138,216`) all read `expires_at>now()`, which is false for NULL, so an un-centralized port silently frees every operational hold. The draft names it as its own top risk; DoD keeps it explicit.\n- Decision 8 (local `claim_history`, no domain events, **no tamper-evidence claim**): correctly scoped per ADR-021 — state inside the database cannot constrain an adversary who writes to the database; the draft says so instead of overclaiming.\n\n**Amended (introduced by the critic — see next section):**\n1. **Child-claim idempotency key namespace.** The draft forwards the staff `Idempotency-Key` into claim_history *and* uses it for the child buyer claim, but `claims(organizer_id, idempotency_key)` is UNIQUE across ALL claims — a raw forward can collide with an unrelated public hold using the same key. Derive the child key deterministically with a namespace prefix (e.g. `convert:<source-claim-id>:<key>`), keeping replay-repair intact.\n2. **Cut the `migrated_baseline` history rows.** History starts empty at 0003; the migration documents that history begins at migration time. Baseline rows add migration surface for no consumer — no verifier asserts coverage over this trail (unlike access's).\n3. **Documented behavior, promoted from risk to DoD:** an expired converted child returns capacity to the *public* pool, not the source operational hold; re-carving requires a new staff operation. Goes in the ADR + operator docs sentence.\n\n**Rejected:** none.\n\n### Introduced at plan-review, reviewed by no second model\nAmendments 1–3 above are the critic's own inventions — the cross-model guarantee covers the draft and the code, not these. Flagging them again in the Gate 3 review-guide.\n\n### Test seam (verified)\nStore-level Postgres seam (`services/inventory/internal/store/*_smoke_test.go`, runs in `smoke.sh` once the `-run` allowlist is removed) + `server_test.go` contract seam + the smoke contention/checkout/contract suites — all existing seams, no new ones. The convert-vs-public race lands in `smoke/inventory_contention_test.go` beside the ≫C test.\n\n### Pre-mortem (named lens, one pass)\nAssume Gate 3 bounced: (1) a NULL-expiry predicate missed in one query → mitigated by the single centralized predicate + aged-operational-hold tests on every mutation and both availability reads; (2) diff exceeds the one-sitting review target (2 services + ADR) → trimmed by amendments 2 and keeping ADR-023 to the decision table; walkthrough will order commerce-orchestration first; (3) flaky race test → the contention scenario pins deterministic fill-then-race sequencing; (4) OpenAPI conditional schemas fight the request validator → public `/holds` schema stays byte-identical, new shapes live only under `/internal/`. Changes applied: amendments 2–3 came out of this pass.\n\n**DoD, API surface, file list, migration plan: per the draft as amended above.**\n\n⛔ Gate 2 — awaiting human approval (Planning → Building)."
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n**Implementation complete** — by main-agent (config.models.implement), per the approved plan + amendments. PR #53 open.\n\nstage:\n  implementer: main-agent\n  diff: 19 files, ~1780 insertions (migration 0003, store operational core, internal API both services, ADR-023, tests)\n  gate: make check green on the committed tree (two runs — first run caught 2 NULL-scan bugs via the store smoke tests that the removed -run allowlist let run for the first time; fixed with COALESCE, re-run green)\n  plan deviations: none — amendments 1–3 from plan-final implemented as specified (namespaced child key, no baseline history rows, expired-child behavior documented in ADR-023)\n\nNext: adversarial review by gpt-5.6-sol (config.models.aiReview)."
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n**Adversarial review complete** — reviewer: gpt-5.6-sol (codex adversarial-review, diff-only, primed for guilt). Three passes; every finding verified against the revision under review before acting.\n\n### Pass 1 (full branch diff) — 4 findings\n| # | Severity | Verdict | Disposition |\n|---|---|---|---|\n| 1 | critical — ticket-type/slot check ran AFTER inventory committed the carve | CONFIRMED | **Fixed**: expected slot is now a precondition inside inventory's locked transaction; mismatch rejects with the hold untouched. Store test pins that a rejected convert leaves quantity/claims/history unchanged. |\n| 2 | high — crash replay not durable (saga recommended) | PARTIAL | **Fixed the real slice** (replay of a dead child no longer persists a dead reservation), **rejected the saga** — the plan chose deterministic-ID replay; an expired child fails checkout with a clean 409 and its capacity-to-public behavior is documented in ADR-023. |\n| 3 | high — staff ports bound on all interfaces + default token | CONFIRMED (bind) / INCIDENTAL (token) | **Fixed** the bind (127.0.0.1 only). The checked-in default `INTERNAL_SERVICE_TOKEN` predates this diff and is repo-wide → **TKT-83** created. |\n| 4 | medium — int32 capacity math can wrap | CONFIRMED | **Fixed**: int64 admission math in both paths + an int32-boundary oversell test. |\n\n### Pass 2 (fix diff) — 1 finding, CONFIRMED, invalidated pass 1's finding-2 fix at the root\nThe expired-replay guard keyed on timestamp, not lifecycle: a confirmed child keeps its elapsed expires_at forever, so a legitimate post-checkout replay would 409 and instruct a double carve; a released child with a future timestamp slipped through. **Fixed**: replays judged by child status (confirmed/finalizing accepted; expired/released 409; held 409 only past deadline). Counter reset per the convergence rule → pass 3.\n\n### Pass 3 (guard rewrite) — 1 finding, CONFIRMED\nThe switch default treated unknown/version-skew statuses as terminal and advised re-conversion (double-carve risk on a still-live child). **Fixed**: expired/released enumerated; anything else is 502 invalid-response (ADR-017's never-judge-the-future rule).\n\n### Stopping call\nThree passes, findings narrowing each round (critical → high → one switch branch). The final fix (6-line case enumeration + 2 unit cases) is **unreviewed by codex** — stopping per the churn rule at the cap. My own verification of it: unit table covers held-elapsed/released-future/expired/empty/unknown; the smoke checkout test replays after completion and pins the confirmed-accept path against the real stack; full gate green. That verification is mine, labeled as such.\n\nNo finding silently dropped: 6 fixed, 1 rejected with reason (saga), 1 deferred to TKT-83.\nGate: `make check` green on every fix commit (committed tree, run by the orchestrator, not delegated)."
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n**PO validation note — how to see the COS met** (no staff UI yet — TKT-22 owns it; the demoable surface is the staff API, per the epic decomposition note).\n\nBring the stack up (`docker compose up -d`), then with `TOK='X-Internal-Token: local-service-token'`:\n\n1. **Place a named hold outside public sale** (AC1): create+publish a performance (or reuse one), then\n   `curl -s -X POST localhost:8091/internal/operational-holds -H \"$TOK\" -H 'Idempotency-Key: demo-1' -H 'Content-Type: application/json' -d '{\"organizer_id\":\"<org>\",\"slot_id\":\"<slot>\",\"quantity\":10,\"purpose\":\"house\",\"label\":\"FOH picks\",\"actor\":\"staff:you\",\"reason\":\"demo\"}'`\n   → public availability (`/api/inventory/slots/<slot>/availability` through the gateway) drops by 10 with a single `available` number; the staff read (`localhost:8091/internal/slots/<slot>/availability`) shows the `operational_held` breakdown (AC4). The same URL through the gateway is 404 — off the public edge (AC1).\n2. **Partial release** (AC3): `POST localhost:8091/internal/operational-holds/<id>/release` with `{\"quantity\":3,...}` → remainder stays held; `GET .../history` shows place → release with who/when/why.\n3. **Convert without a gap** (AC2): `POST localhost:8092/internal/operational-holds/<id>/convert` with a ticket type → returns a normal reservation; complete it through the public storefront checkout (`/api/commerce/orders`). The no-gap property is machine-proven in the gate: `TestOperationalConvertNeverLeaksCapacityToPublicHolds` fires 30 public holds against a full pool mid-conversion — zero grants.\n4. **No oversell including operational quantity**: the ≫C contention test extended; int32-boundary case pinned.\n\nEvery AC is also pinned by tests that run in `make check` (the merged main is green). Deferred out with reasons: staff UI (TKT-22), pricing overrides (TKT-5), default-token removal (TKT-83).\n\n⛔ Gate 4 — PO validates and moves to Done. Changes requested → I swap back to agent:coding."
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-77\n\n| stage | duration |\n|---|---|\n| Backlog (shaping, at epic decomposition) | 18m |\n| Ready | 2m39s |\n| Planning | 21m |\n| — agent:planning (gpt-5.6-sol draft) | ~9m |\n| — agent:plan-review (main-agent critique + finalize) | ~7m |\n| — needs:human (Gate 2 wait) | 4m20s |\n| Building | 56m43s |\n| — agent:coding | ~23m |\n| — agent:ai-review (3 codex passes + fixes) | ~23m |\n| — needs:human (Gate 3 wait) | 10m30s |\n| PO Review (Gate 4 wait) | ~25m |\n\n**human-wait total:** ~40m (Gates 2+3+4) — vs ~62m agent work. Wait dominated by nothing; healthy ratio for a P0.\n**diff:** +1906/−22 (17 files) — above the ~400-line target; the plan called it at Gate 2 (commerce orchestration is load-bearing for AC2) and the walkthrough ordered review by risk to compensate.\n\n**learnings:**\n1. *Judge idempotent replays by lifecycle state, never by timestamp — and an unknown state is an invalid response, not a terminal one.* Three review passes converged on this one guard; it generalizes ADR-017's dispatch rule to synchronous HTTP replies. → house: repo docs (`docs/learnings/` + LEARNINGS.md line) — **applied this session**.\n2. *The smoke `-run` allowlist defect recurred a 4th time and is now extinct* — already documented in smoke.sh itself; no new promotion (cite, don't restate).\n\n**retro:**\n1. *What would have made this faster:* shaping never asked \"how is a staff-only surface reached by tests and the PO demo?\" — the port-exposure decision (compose, loopback bind, smoke env plumbing) had to be made mid-Building, and pass 1 of review caught its first draft binding 0.0.0.0. Appliable change: one line in `shaping.md` §DoR `approach` — \"internal/staff surfaces: name the access path for tests + Gate 4 demo\". **Offered, not applied** (skill edits carry the high bar — say the word and I patch it).\n2. *What the humans should change:* TKT-82 (load proof) should explicitly measure the per-mutation `claim_history` write overhead this ticket added to the hot path (ADR-023 §Consequences names it). **Applied**: note added to TKT-82's description this session.\n\nNewly unblocked: none were hard-blocked on TKT-77; US-015/US-016 (TKT-78/79) had a soft ordering on it and are now cleanly claimable."
    }
  ],
  "parent": "TKT-4"
}
