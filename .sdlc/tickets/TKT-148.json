{
  "key": "TKT-148",
  "type": "Task",
  "summary": "Buyer hold TTLs anchor to transaction-start now(), so a lock-delayed hold can be born already expired (TKT-134 investigation)",
  "status": "Building",
  "labels": [
    "agent:ai-review"
  ],
  "assignee": "agent",
  "pr": {
    "number": 316,
    "url": "https://github.com/mathd/ticketing_system/pull/316",
    "state": "open"
  },
  "links": [
    {
      "type": "relates",
      "key": "TKT-134"
    }
  ],
  "description": "Found while root-causing TKT-134's flake. **Not** what that flake hit — TKT-134 needed a whole minute to elapse and this mechanism cannot supply that on its own — and deliberately filed separately rather than claimed as its cause.\n\nEvery buyer-claim creation path sets the hold's expiry as `now() + ttl`:\n\n- `services/inventory/internal/store/store.go:286` (GA `CreateHold`)\n- `services/inventory/internal/store/operational.go:309` (operational conversion)\n- `services/inventory/internal/store/reservations.go:218` (reservation draw-down)\n- `services/inventory/internal/store/seat_claims.go:228` (seated claims)\n\nIn PostgreSQL `now()` is **transaction start** time, not insert time. These inserts happen inside a transaction that first takes the pool row lock (ADR-010). Under contention — an on-sale, a long sweep, a slow catalog call inside the same transaction — that lock wait sits **between** the transaction's start and the insert.\n\nSo the TTL is consumed by the wait. A hold created after a wait longer than the TTL is returned to the buyer **already expired**: `liveClaims` (`store.go:62`) evaluates `expires_at > now()` and will not count it, the buyer holds nothing, and the seat they think they have is available to someone else. The window is real on the exact path that matters most — high contention is when holds matter.\n\n`clock_timestamp()` (real time, advancing within the transaction) is the correct anchor for a *duration granted to a buyer*. Note the codebase already uses it where decision time is meant: `activeAllocation` (`channel_allocations.go:24`).\n\n**Scope:**\n- [ ] Anchor new buyer TTLs to insert time (`clock_timestamp() + ttl`) on all four creation paths\n- [ ] Deterministic proof: force a pool-lock wait longer than a short TTL using the existing lock-wait handshake in `channel_allocations_smoke_test.go`, then assert the returned hold still carries a full TTL post-commit\n- [ ] Re-check ADR-024's mixed-clock independence argument before changing semantics — the two clocks are used deliberately in places and this must not flatten that\n- [ ] Consider whether `liveClaims`' own `now()` should stay transaction-start (it probably should: a *read* wants a consistent snapshot; a *grant* wants real time)\n\n**Severity:** availability-path, contention-triggered. A buyer can be issued a dead hold and the seat resold under them.\n\n**Source:** TKT-134's plan draft (`gpt-5.6-sol`), confirmed at plan review. Not fixed in TKT-134 because that ticket's fix is test-only and folding a four-path production change into it would have hidden a real defect inside a flake repair.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n**Verdict: READY.** All eight DoR items `met`, no `open`, no `deferred`, no open blockers (TKT-134 is `relates` and is Done). No spike needed -- every unknown resolved by reading the code.\n\n**What the shaping read changed about the ticket.** Two corrections, both from the code rather than the ticket text:\n\n1. **Five grant sites, not four.** `seat_claims.go` has two `now()+$N::interval` buyer inserts -- explicit-seat selection (805) and best-available (1708). The ticket lists one seated path. Its line numbers are also stale across the board; the mechanism is exactly as described.\n\n2. **The `RETURNING` clause is in scope and the ticket does not mention it.** Every grant site returns `now()` into `Claim.ServerTime`, and that value is load-bearing: the storefront's buyer countdown is `expires_at - server_time` (pinned by `web/storefront/test/hold-picker.test.ts`), commerce's conversion gate is `!ExpiresAt.After(ServerTime)` (`server.go:1821`), and `Claim.expired()` is the same comparison. Moving `expires_at` to `clock_timestamp()` while leaving `RETURNING now()` would convert a hold that is born dead into a hold that **lies about how much time it has** -- over-reporting by exactly the lock wait. The anchor and its returned reference move together, or the fix trades one defect for a quieter one.\n\n**Scope boundary that is easy to get wrong.** `reservations.go:210` also writes an expiry but is **out**: it stores a staff-supplied *absolute* timestamp and already validates it against `clock_timestamp()` at line 99. Only durations granted to a buyer are in scope. Likewise the `lease_until=now()+...` sites in commerce/payments/access are worker retry fences, not buyer grants.\n\n**ADR-024 re-check (a ticket scope item), answered now:** the mixed-clock independence argument holds. `clock_timestamp() >= now()` within a transaction, so this change can only move an expiry *later*; capacity is never derived from the release predicate; and `liveClaims` stays on `now()` deliberately -- a *read* wants one consistent snapshot, a *grant* wants real time. That is the same split the ticket proposes and the code already applies it at `channel_allocations.go:24`.\n\n**COS**\n\n1. All five buyer-grant inserts anchor `expires_at` to `clock_timestamp()+$ttl` and return `clock_timestamp()` as `server_time`, so the returned pair describes the same instant.\n2. A deterministic test proves it: with a TTL shorter than an enforced pool-lock wait, the hold returned after the wait still carries a full TTL and is live. **The test must be able to fail** -- reverting either the anchor or the `RETURNING` change makes it red, demonstrated by mutation, not asserted.\n3. `liveClaims`, `sweepExpired` and every capacity read still use `now()`; the change does not flatten the deliberate two-clock split.\n4. No behaviour change on the uncontended path: an ordinary hold still gets its full TTL and every existing inventory/commerce test stays green.\n5. The decision is recorded as an ADR amendment naming which clock anchors a grant, which anchors a read, and why.\n\n**risk verdict (restated at claim):** NOT `risk:low`. Five production call sites on the availability path, a change to a value returned to buyers, and a concurrency-timing semantic. The plan gate stays."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n**Claimed.** Running **gateless per `config.gates: autonomous`** -- gates 2-4 are taken by agent judgement; Gate 1 was the human's and was given explicitly. Backend: **local tracker** (`config.tracker: \"local\"`), state on `sdlc-state`.\n\n**Selection reason:** requested by name. It arrives fully shaped (all eight DoR items `met`) and its blocker set is empty -- TKT-134 is `relates` and Done.\n\n**risk verdict: `risk:low` NOT claimed.** Stated explicitly rather than left silent, per the claim rule. Three of the label's carve-outs are in play at once: five production call sites on the **availability path**; a change to a value **returned to buyers** (`server_time`, which drives the storefront countdown); and a **concurrency-timing semantic** whose whole point is what happens under lock contention. The plan gate stays -- and it is earning its keep here, because the ticket's central question is the one shaping flagged as wrong-by-default: how to construct a contention test that can actually fail.\n\n**Branch:** `TKT-148-clock-timestamp-buyer-ttl` off `origin/main`."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n**Plan draft — authored by `gpt-5.6-luna`** (via codex `task`, effort high, read-only), per `config.models.plan`. Posted as written; my critique follows under `agent:plan-review`.\n\nRun notes for the audit trail: the run announced the local `unslop` skill (the documented hijack shape -- survivable, judged on output). Output was conclusive and non-empty (7.9 KB), read all five grant sites, both seated paths, the smoke-test fixtures and ADR-024, and cites real line numbers. One attempt, rung 1, no escalation.\n\n---\n\n# Execution plan for TKT-148\n\n## DoD\n\nThe five buyer-grant inserts use `clock_timestamp() + $ttl::interval` for `expires_at` and return `clock_timestamp()` as `ServerTime`.\n\nA database-backed contention test covers all five paths. Each case holds the pool row lock longer than the configured TTL, proves the grant transaction was waiting before the TTL boundary, then verifies that the returned hold is still live after release.\n\n`liveClaims`, `sweepExpired`, capacity reads, reservation placement, lease fences, commerce, payments, and access remain unchanged.\n\nADR-024 records the distinction:\n\n- grants use advancing database time;\n- reads use transaction-start `now()` for a consistent snapshot;\n- the mixed clocks remain safe because `clock_timestamp() >= now()`, so the change can extend a hold but cannot cause an oversell.\n\n## Files and edits\n\n- [`services/inventory/internal/store/store.go:614`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/store.go:614)  \n  Change the GA `CreateHold` insert from `now()+$8::interval` and `RETURNING expires_at,now()` to `clock_timestamp()+$8::interval` and `RETURNING expires_at,clock_timestamp()`.\n\n- [`services/inventory/internal/store/operational.go:334`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/operational.go:334)  \n  Make the operational-hold conversion insert use the same two advancing-clock expressions.\n\n- [`services/inventory/internal/store/reservations.go:317`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/reservations.go:317)  \n  Change only the draw-down child grant. Leave reservation placement at [`reservations.go:210`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/reservations.go:210), because it stores an absolute staff-supplied expiry.\n\n- [`services/inventory/internal/store/seat_claims.go:805`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/seat_claims.go:805) and [`seat_claims.go:1708`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/seat_claims.go:1708)  \n  Update explicit-seat and best-available inserts together.\n\n- Add [`services/inventory/internal/store/clock_timestamp_smoke_test.go`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/clock_timestamp_smoke_test.go) with a shared database-time lock-wait helper and five serial subtests:\n  `CreateHold`, `ConvertOperational`, `DrawDownGroupReservation`, `CreateSeatHold`, and `CreateBestAvailableSeatHold`.\n\n- Amend [`docs/adr/ADR-024-channel-allocations.md`](/home/mathieu/sources/ticketing_system/docs/adr/ADR-024-channel-allocations.md) in the decision section. State that buyer duration grants use `clock_timestamp()`, while `liveClaims` and capacity reads retain `now()`. Recheck and document the no-oversell argument from lines 40-44 against the later grant expiry.\n\nNo API, schema, generated-code, storefront, or commerce edits are needed.\n\n## Test plan\n\nThe proof should cover all five sites with deep contention cases. One deep test plus four uncontended assertions is insufficient. An uncontended test cannot distinguish the old implementation: `now()+TTL` and `RETURNING now()` still produce a self-consistent pair. The existing storefront and commerce tests verify pair semantics, not the pair’s relationship to the lock wait.\n\nUse one shared helper to reduce duplication, but give each SQL site its own subtest and mutation run.\n\nEach subtest should:\n\n1. Create a short-TTL store with [`storeForTest(t, ttl)`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/operational_smoke_test.go:19), using a TTL of about 500ms.\n2. Seed the real prerequisite through the store API.\n3. Start a blocker transaction and take `inventory_pools FOR UPDATE`.\n4. Start the operation in a goroutine.\n5. Poll `pg_stat_activity` until that operation’s exact pool-lock query is observed waiting.\n6. Require that observation while database time is still before a boundary roughly one second after the test’s initial DB timestamp.\n7. Keep the blocker held until database time is roughly three seconds after the initial timestamp. This is longer than the TTL and longer than the maximum old expiry implied by the transaction-start timestamp.\n8. Release the blocker and wait for the operation.\n9. Read database time immediately afterward and assert the returned contract:\n   - `ServerTime` is after the pre-wait boundary;\n   - `ExpiresAt` is still after current database time;\n   - `ExpiresAt` is after `ServerTime`;\n   - `ExpiresAt - ServerTime` remains close to the configured TTL;\n   - the persisted claim is still `held` and live.\n\nAll timing boundaries come from `clock_timestamp()` queries. `time.Sleep` may only provide short polling backoff. It must not decide whether the test crossed a boundary.\n\nFixture coverage:\n\n- GA hold: seed only `provisioned(...)`. Deleting that seed removes the pool and makes `CreateHold` fail before the assertion.\n- Operational conversion: seed `PlaceOperationalHold(...)`. Deleting it makes conversion fail because there is no source hold.\n- Group draw-down: seed `PlaceGroupReservation(...)` with an expiry obtained from the existing DB-clock helper. Deleting it makes draw-down fail because there is no reservation source.\n- Explicit seat selection: seed `provisionedSeated(...)` and use a valid seat. Deleting the seated pool makes the operation fail before insertion.\n- Best available: use `seededBestAvailablePool(...)` from [`best_available_smoke_test.go:26`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/best_available_smoke_test.go:26). Deleting the seat projection makes no valid best-available selection possible.\n\nThe blocker itself is also load-bearing. If it is removed, the operation will not wait, the `pg_stat_activity` handshake will fail, and the test will stop instead of passing vacuously.\n\nFor mutation verification, revert exactly one grant site at a time to its pre-fix `now()+$ttl` and `RETURNING expires_at,now()` expression. Run only the matching new subtest and observe it go red on the database-time assertion. Restore the site before testing the next one. A mutation killed by an unrelated existing test does not count as proof for this ticket.\n\nExisting tests remain useful but are not substitutes for this proof:\n\n- [`Claim.expired()` and `liveClaims`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/store.go:144) must retain their `now()` behavior.\n- The closure test at [`offering_smoke_test.go:154`](/home/mathieu/sources/ticketing_system/services/inventory/internal/store/offering_smoke_test.go:154) remains anchor-agnostic.\n- The storefront countdown and commerce pair tests continue to pin the returned pair’s meaning.\n\n## Risks\n\nThe main testing risk is a false green caused by observing only that the callback ran or that a row was inserted. The proposed assertions use returned timestamps and current database time, so the old implementation must fail.\n\n`clock_timestamp()` advances within one SQL statement. The returned interval may therefore be slightly less than the configured TTL. The test should allow a bounded statement-time difference rather than assert exact equality.\n\nThe five contention cases add several seconds to the smoke suite. That cost is justified because the five SQL statements are independent regression points. A single generic uncontended assertion would leave four sites effectively unprotected.\n\nThe ADR-024 mixed-clock argument still holds. A later grant expiry can keep a claim counted longer, causing conservative rejection and possible undersell, but cannot make a claim disappear earlier or reduce counted demand. The global capacity check also does not depend on the channel-release predicate."
    },
    {
      "stage": "plan-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=plan-review kind=summary -->\n**Plan critique — `main-agent`** (`config.models.planReview`; the drafter was `gpt-5.6-luna`, so the cross-model pair holds). Adversarial pass, not a rubber stamp. Every file, symbol and helper the draft names was verified to exist at HEAD before I judged the reasoning.\n\n**Overall: ACCEPT the draft's structure and its two judgement calls; AMEND the test design on one point that makes it unimplementable as written.**\n\n### Symbol verification (all pass)\n\n`provisionedSeated` (`seat_claims_smoke_test.go:17`), `seededBestAvailablePool` (`best_available_smoke_test.go:26`), `storeForTest` (`operational_smoke_test.go:19`), `PlaceOperationalHold`, `PlaceGroupReservation`, `ConvertOperational`, `DrawDownGroupReservation`, `CreateSeatHold`, `CreateBestAvailableSeatHold` — all resolve. The five grant sites and the five line numbers are correct. No hallucinated seams.\n\n### ACCEPTED — the draft was right and I was wrong\n\n**1. Five deep contention subtests, not one deep plus four cheap.** My brief floated \"one deep contention test plus a cheaper uniform assertion over the other four\" and invited an argument. The draft refused it and the refusal is correct: *an uncontended test cannot distinguish the old implementation, because `now()+TTL` with `RETURNING now()` is a self-consistent pair.* That is exactly the repo's fixture-that-cannot-fail rule and it kills my suggestion outright. Each of the five is an independent SQL literal with no shared helper between them — nothing propagates a fix from one to another — so four sites would have been protected by a test structurally incapable of failing. Adopted as drafted.\n\n**2. `ExpiresAt - ServerTime` asserted as a bounded interval, not exact equality.** Correct, and for a sharper reason than the draft gives. I verified against a real PostgreSQL 18: inside a transaction after a 300ms sleep, `now() < clock_timestamp()` is true with drift 307ms. But the two `clock_timestamp()` calls in `clock_timestamp()+$ttl` and `RETURNING clock_timestamp()` are separate evaluations in one statement, so the returned interval is TTL minus a sub-millisecond delta, never exactly TTL. A tolerance is required, not stylistic.\n\n### AMENDED — one blocking design defect\n\n**3. \"Poll `pg_stat_activity` until that operation's *exact* pool-lock query is observed waiting\" is not implementable for two of the five sites, and the draft does not notice.**\n\nThe instruction is right in principle and is the repo's documented rule (`docs/learnings/2026-07-16-lock-handshakes-pin-the-exact-statement.md`, TKT-76 ai-review pass 3: a handshake matching a table-name substring latched onto the wrong waiter and the test passed with the statement under test deleted). But the draft assumes the five lock statements are distinguishable by text. **They are not.** I extracted all of them:\n\n- `CreateHold` — `SELECT capacity,confirmed_quantity,target_capacity,lifecycle_status,inventory_kind,closure_status FROM inventory_pools WHERE slot_id=$1 AND organizer_id=$2 FOR UPDATE` (`store.go:422`)\n- `ConvertOperational` — `SELECT 1 FROM inventory_pools WHERE slot_id=$1 FOR UPDATE` (`operational.go:303`)\n- `DrawDownGroupReservation` — `SELECT 1 /* grp-draw pool lock */ FROM inventory_pools WHERE slot_id=$1 FOR UPDATE` (`reservations.go:244`)\n- `CreateSeatHold` — `SELECT ...,seat_map_id,orphan_prevention_enabled,closure_status FROM inventory_pools WHERE slot_id=$1 AND organizer_id=$2 FOR UPDATE` (`seat_claims.go:676`)\n- `CreateBestAvailableSeatHold` — **byte-identical to the line above** (`seat_claims.go:1554`)\n\nTwo collisions. The seated pair cannot be told apart by query text at all. And `ConvertOperational`'s text is a substring-prefix of the draw-down's, so a `LIKE '%SELECT 1 FROM inventory_pools%FOR UPDATE%'` matcher catches either. Note the `/* grp-draw pool lock */` tag already in `reservations.go:244`: someone hit this exact problem before and solved it by tagging one statement. That is evidence the collision is real, not theoretical.\n\n**Amendment — discriminate by TARGET ROW, not by statement text.** The property the handshake actually needs is *\"the statement under test is the waiter\"*, and text was only ever a proxy for it. Each subtest already gets a unique schema from `storeForTest` and a fresh `slot_id` UUID, and only one goroutine is in flight per subtest. So pin on all three together:\n\n1. the exact query text for that site (still — it is free and it kills the cross-site prefix collision for the three non-seated sites);\n2. `pg_locks.granted = false` for that backend, joined on `pid` — proving it is *blocked*, not merely running; and\n3. the backend's `datname` plus `pid <> pg_backend_pid()`, as the existing handshake already does.\n\nFor the seated pair specifically, (1) is not discriminating, so the load-bearing discriminator is that **the two subtests never run concurrently and each holds a distinct pool row** — which `t.Run` serial execution and per-subtest UUIDs already guarantee. **Say that in a comment**, because it is a real precondition: if anyone later adds `t.Parallel()` to these subtests, the seated pair's handshake silently starts matching each other's waiter and both tests become vacuous. That is precisely the TKT-76 shape, one level removed.\n\n**And mutation-check the handshake itself, per rule 3 of that learning** — not only the anchor. Delete the blocker transaction and the subtest must FAIL (time out observing no waiter), not pass. The draft says this for the blocker seed in prose (\"the blocker itself is also load-bearing\") but does not put it in the mutation list; it belongs there as an executed run, not an argument.\n\n### AMENDED — timing budget\n\n**4. The draft's ~3s hold x 5 serial subtests does not fit `storeForTest`'s context.** `storeForTest` installs a **30-second** `context.WithTimeout` (`operational_smoke_test.go:25`) and every store call in the subtest takes that ctx. Five serial subtests at ~3s of held lock, plus per-subtest schema creation and `Migrate`, plus best-available's seat-projection seeding, is uncomfortably close. Migration alone is not free.\n\nAmendment: **each subtest calls `storeForTest` itself** so each gets its own 30s budget and its own schema, rather than sharing one store across five subtests. Then tighten the drafted margins: TTL **500ms**, hold the blocker until DB time is **~2s** past the pre-wait mark. 2s is 4x the TTL — ample to prove the point — and it keeps each subtest's whole lifetime well inside its own context. The draft's \"roughly one second\" boundary for the waiter observation stays.\n\n### CHALLENGED — a scope question the draft did not raise, and my ruling\n\n**5. The idempotency REPLAY paths return `now()` as `ServerTime` too, and the draft is silent on them.** `store.go:447`, `seat_claims.go:712` and `:1580`, `operational.go:312`, `reservations.go:253` all read an existing claim with `...,expires_at,now(),...` and then call `Claim.expired()`, which is `!ExpiresAt.After(ServerTime)` (`store.go:152`). Two consequences under a long lock wait:\n\n- the returned pair over-reports the buyer's remaining time by the wait, exactly the inconsistency this ticket exists to remove; and\n- `expired()` judges with stale-early time, so a claim that has really lapsed can be replayed as live.\n\n**Ruling: OUT of scope for this ticket, and deliberately so — but recorded, not ignored.** The second bullet is a *decision* about when a claim dies, and `docs/learnings/2026-07-16-lock-queue-time-cutoffs.md` plus `ReconcileSeatClaimStates` (`seat_claims.go:1103`) show the codebase has already reasoned about this direction and concluded the stale-early read **errs toward keeping the claim**, which is the safe direction for a buyer. Changing it is a liveness-semantics change with its own blast radius and belongs in its own ticket with its own gate. Folding it in here would repeat exactly the mistake TKT-134 avoided when it filed this ticket separately rather than bundling a four-path production change into a flake repair.\n\nAction: **file a follow-up backlog ticket** at closeout rather than widening this one. This ticket's COS 3 (\"reads still use `now()`\") already commits us to not touching it, so the two are consistent.\n\n### CONFIRMED — the ADR-024 re-check\n\nBoth the draft and shaping conclude the mixed-clock independence argument survives, and I agree on the merits: `clock_timestamp() >= now()` within a transaction, so a grant can only move an expiry **later**; a later expiry keeps a claim counted as live for longer, which is conservative (possible undersell, never oversell); and the global capacity check does not depend on the channel-release predicate. The draft states the undersell direction explicitly, which is the honest form of the claim. Accepted as drafted, and it must appear in the ADR amendment in those words rather than as a bare \"still safe\".\n\n### Introduced at plan-review, reviewed by no second model\n\nPer the cross-model rule, these are mine and no second model has seen them:\n\n- **the target-row/`pg_locks.granted` handshake discriminator and the seated-pair collision finding (§3)**;\n- **the per-subtest `storeForTest` restructuring and the 500ms/2s margins (§4)**;\n- **the replay-path scope ruling and the follow-up ticket (§5)**.\n\n§3 is the one carrying real risk: it is a test-mechanism change and a wrong handshake produces a *green* test, which is the failure mode this repo most distrusts. It is mitigated by rule 3 of the TKT-76 learning — the blocker-deletion mutation must be **run and observed red**, not argued — and I have made that an explicit deliverable rather than a note.\n\n### Pre-mortem — it is three weeks later and this shipped broken. What happened?\n\n1. **The five subtests are green and the defect is live**, because the handshake latched onto the wrong waiter. Countered by §3 plus the mandatory blocker-deletion mutation per site.\n2. **The anchor moved but `RETURNING` did not** on one of five sites — a copy-paste miss. This is why the assertion must include `ExpiresAt - ServerTime ~= TTL`: with a moved anchor and a stale `RETURNING`, that interval reads as TTL **plus the lock wait**, so the assertion catches the half-fix. Confirm the tolerance is a two-sided band, not a floor.\n3. **Someone adds `t.Parallel()`** and the seated pair goes vacuous. Countered by the required comment in §3.\n4. **A flake on a loaded CI box** — the blocker is released before the waiter is observed. Countered by the existing handshake's own discipline: fail the *setup* with a clear message when no waiter is ever observed, never fall through to the assertion.\n\n### Verdict\n\n**Approved with amendments 3, 4 and 5.** Items 1, 2 and the ADR conclusion stand as drafted. The draft as amended above is the plan."
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n**Plan final — delta on the `kind=plan` draft.** The draft as amended below is the plan. Gate 2 taken by agent judgement per `config.gates: autonomous`; nothing here waited on a human.\n\n### Decisions I made, and why\n\nEvery open point came from my own plan-review, so the rule \"prefer what the critique recommends\" resolves them directly. Where the critique offered a choice I took the **more reversible** option, as the autonomous-mode rule requires.\n\n1. **Five deep contention subtests** (draft §Test plan, critique §1) — the drafter's position over my brief's suggestion. My \"one deep + four cheap\" would have left four sites covered by an assertion that cannot fail, since `now()+TTL` with `RETURNING now()` is a self-consistent pair. Decided: as drafted.\n2. **Handshake discriminates by target row, not statement text** (critique §3) — forced, not preferred: `CreateSeatHold` and `CreateBestAvailableSeatHold` issue byte-identical lock statements, so the drafted \"exact query\" rule is unimplementable for that pair. Pin on query text AND `pg_locks.granted=false` AND per-subtest unique schema/slot, with a comment recording that serial execution is a precondition.\n3. **Per-subtest `storeForTest`, TTL 500ms, blocker held ~2s past the mark** (critique §4) — each subtest gets its own 30s context instead of five sharing one. More reversible than raising a timeout: it changes only the new test's structure and touches no shared fixture.\n4. **Replay paths stay out of scope** (critique §5) — a follow-up ticket at closeout. Reversible by construction: the finding is written down and costs nothing to act on later, whereas folding a liveness-semantics change into this ticket is exactly what TKT-134 declined to do when it filed this one.\n5. **ADR-024 amended in place** rather than a new ADR — the ticket's COS says amendment, ADR-024 already carries three amendments in that idiom (TKT-238, TKT-250, TKT-246), and the mixed-clock argument being revisited is that ADR's own. The undersell-not-oversell direction goes in explicitly, not as \"still safe\".\n\n### Required amendments to the draft\n\n- **§Test plan step 5** — replace \"poll until that operation's exact pool-lock query is observed waiting\" with the three-part predicate in critique §3, and add the `t.Parallel()` precondition comment.\n- **§Test plan mutation list** — add the **blocker-deletion** mutation per site (delete the blocker transaction; the subtest must fail by observing no waiter, not pass). Rule 3 of `docs/learnings/2026-07-16-lock-handshakes-pin-the-exact-statement.md`, and it must be a run, not an argument.\n- **§Test plan step 1** — `storeForTest` per subtest; TTL 500ms; blocker released ~2s past the pre-wait mark.\n- **§Test plan step 9** — the `ExpiresAt - ServerTime ~= TTL` assertion is a **two-sided band**. This is what catches the half-fix where the anchor moved but `RETURNING` did not: that combination reads as TTL *plus* the lock wait, which a one-sided floor would pass.\n\nEverything else in the draft stands, including its file list, its five line numbers, its per-seed \"what would go red if deleted\" analysis, and its ADR-024 conclusion.\n\n### Introduced at plan-review, reviewed by no second model\n\nCarried forward from the critique and named again here for the Gate 2 record, per the cross-model rule:\n\n- the target-row / `pg_locks.granted` handshake discriminator, and the seated-pair statement collision that forces it (§3);\n- the per-subtest `storeForTest` restructuring and the 500ms / 2s margins (§4);\n- the replay-path scope ruling and its follow-up ticket (§5).\n\n`gpt-5.6-luna` wrote the draft; these three are mine and no second model has seen them. **§3 carries the real residual risk**, because a wrong handshake yields a *green* test — the failure mode this repo distrusts most. The mitigation is executed, not argued: the blocker-deletion mutation must be run and observed red for each of the five sites, and the result recorded in the coding stage comment. If any site cannot be made to fail that way, that is a stop-and-report, not a note.\n\n### Verified during planning, not assumed\n\n- Against a real PostgreSQL 18: inside a transaction after a 300ms sleep, `now() < clock_timestamp()` is true with drift 307ms. The mechanism is real.\n- Incidental: the comment at `channel_allocations_smoke_test.go:595` claims `SELECT clock_timestamp() = clock_timestamp()` is false. On PG 18 it returns **true** — adjacent calls in one expression fold to the same value; it advances across rows and statements. Does not affect this plan (the fact this ticket relies on is the transaction-level one, which holds). Filed as an incidental at closeout, not fixed here.\n\n**→ Transitioning Planning → Building.**"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n**Implemented by `main-agent`** per `config.models.implement`. TDD, tests observed red first. PR **#316**, commit `1a9f1ad4`.\n\n```yaml\nstage: coding\nimplementer: main-agent\npr: 316\ncommit: 1a9f1ad4\ngate: make check -> sentinel EXIT=0, 0 failure signatures in log body\nproduction_diff: 5 lines / 4 files\n```\n\n### Sequence\n\n1. Wrote `buyer_ttl_clock_smoke_test.go` first and ran it against **unmodified** production code. Four of five went red on the ticket's exact defect; the fifth failed on a fixture bug of mine (`PlaceOperationalHold` purpose must be one of `house|artist|kill|other`, per `claims_kind_shape` in migration 0004). Fixed the fixture, re-ran: **all five red**, each having waited the full ~2.2s, which means the handshake observed a real waiter every time.\n2. Applied the anchor + `RETURNING` change to the five sites.\n3. All five green. Full inventory suite (`./internal/...`) green in 64s — no regressions.\n4. ADR-024 amended.\n5. `make check` green.\n\n### Mutations — run and observed, not argued\n\nThe plan made these a deliverable with a stop-and-report attached. All passed.\n\n| Mutation | Expected | Observed |\n|---|---|---|\n| Each of the 5 sites reverted **alone** | only its own subtest red | 5/5, each killed by its own subtest |\n| **Blocker transaction deleted** | fail at *setup*, not vacuous pass | all 5: `grant transaction never blocked on the pool lock before the boundary` |\n| Half-fix: anchor moved, `RETURNING` stale | red | `server_time … is not after the enforced wait boundary: the RETURNING clause is still anchored to transaction start` |\n| Half-fix: `RETURNING` moved, anchor stale | red | `hold was born expired: expires_at … is not after database time …` |\n\nThe blocker-deletion result is the one that matters most: it proves the handshake is the mechanism and not decoration. The two half-fix mutants are caught by **different** assertions, which is what justifies the two-sided band from plan-review §4 — a one-sided \"is it still alive\" floor would pass the first of them, shipping a hold that lies to the buyer about its remaining time.\n\n### Deviation from the plan, and why\n\nMy first `perl` pass also rewrote `reservations.go:210` — group-reservation **placement**, which plan-final explicitly ruled out of scope. Only its `RETURNING` matched (its anchor is the absolute `$5`), so the change looked harmless. **Reverted.** Placement grants no duration, and quietly widening a diff past its approved plan is how the plan stops describing the code. Verified by reading the final `git diff`: five changed lines, all five at the intended sites.\n\n### Deliberately not done\n\n- The **idempotency replay** paths return transaction-start `now()` as `server_time` and judge `expired()` against it — the read-side mirror of this defect. Ruled out at plan-review §5 and still out: it is a liveness-semantics change, and the codebase's existing reasoning (`ReconcileSeatClaimStates`, `seat_claims.go:1103`) is that stale-early time errs toward *keeping* the claim, which is the buyer-safe direction. Follow-up ticket at closeout.\n- No fix for the incorrect `clock_timestamp() = clock_timestamp()` comment at `channel_allocations_smoke_test.go:595`. Incidental, filed at closeout.\n\n### Note for the reviewer\n\nThe five subtests must stay **serial**. `CreateSeatHold` and `CreateBestAvailableSeatHold` issue byte-identical pool-lock statements, so `t.Parallel()` would let them observe each other's waiter and both would go quietly vacuous — the TKT-76 shape one level removed. This is commented in the test, but it is the kind of precondition a later refactor deletes without noticing."
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "A buyer TTL anchored to now() (transaction start) is consumed by the pool-lock wait that precedes the INSERT, so under contention a hold can be returned already expired and the seat resold under the buyer. Verified in code, not inherited from the ticket text."
    },
    "scope": {
      "state": "met",
      "note": "IN: the buyer-TTL grant sites in inventory that write `now()+$N::interval` -- there are FIVE, not the four the ticket lists (seat_claims.go has two: explicit-seat and best-available). OUT: worker/outbox leases in commerce, payments and access (`lease_until=now()+...`) -- those are internal retry fences, not durations granted to a buyer, and moving them changes lease semantics for no user-visible benefit. OUT: liveClaims, sweepExpired and every capacity read -- see the `approach` note."
    },
    "uncertainties": {
      "state": "met",
      "note": "Settled by reading, no spike needed. (1) Does the fix widen an oversell window? No: clock_timestamp() >= now() inside a transaction, so an expiry can only move LATER, and ADR-024's independence argument (global capacity never depends on the release predicate) is untouched. (2) Is any current caller relying on hold expiry being frozen at transaction start? offering_smoke_test.go:154 asserts a closure does not REWRITE a TTL -- it compares two reads of the same row and is indifferent to the anchor."
    },
    "dependencies": {
      "state": "met",
      "note": "No open blockers. TKT-134 is `relates` only and is Done."
    },
    "approach": {
      "state": "met",
      "note": "Anchor the five buyer-grant INSERTs to `clock_timestamp()+$N::interval`. THE TICKET'S SCOPE IS INCOMPLETE ON ONE POINT, found by reading: every one of these sites also does `RETURNING expires_at, now()` into `Claim.ServerTime`, and ServerTime is not decoration -- the storefront computes the buyer's countdown as `expires_at - server_time` (web/storefront/src/components/HoldPicker.tsx:21, pinned by hold-picker.test.ts) and commerce gates on `!Hold.ExpiresAt.After(Hold.ServerTime)` (server.go:1821) and Claim.expired() does the same (store.go:152). Moving expires_at to clock_timestamp() while leaving RETURNING now() would make the returned pair inconsistent: server_time would be transaction-start, so the countdown would over-report the buyer's remaining time by exactly the lock wait -- turning a born-dead hold into a hold that lies about how long it has. The anchor and its returned reference must move TOGETHER: `RETURNING expires_at, clock_timestamp()`. GUARD PREDICATE COUNT: none added -- this is an anchor change, not a new guard, so the N-tests-per-predicate rule has nothing to bind. Reservation PLACEMENT (reservations.go:210) is deliberately excluded: it stores a staff-supplied ABSOLUTE expires_at and already validates it against clock_timestamp() at line 99; only the DRAW-DOWN (line 317) grants a duration."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice: five call sites plus their RETURNING clause, one deterministic contention test, one ADR amendment. No migration, no API shape change."
    },
    "success": {
      "state": "met",
      "note": "COS below."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked -- see the `context` object on this ticket. governingAdrs and governingTests both populated and each ADR verified to resolve at HEAD."
    }
  },
  "context": {
    "governingAdrs": [
      {
        "adr": "ADR-010-postgres-claim-transaction.md",
        "constrains": "The pool row FOR UPDATE is the serialization point and expiry is a lazy predicate, never an event. This ADR is WHY the defect exists: the lock wait it mandates sits between transaction start and the INSERT. The fix must not add a sweeper or an event -- expiry stays lazy."
      },
      {
        "adr": "ADR-024-channel-allocations.md",
        "constrains": "Carries the repo's mixed-clock argument: allocation release uses clock_timestamp() precisely because now() freezes at transaction start, while liveClaims deliberately stays on now(). The ticket asks to re-check this independence before changing semantics -- it holds, because a grant moving to clock_timestamp() can only lengthen an expiry relative to now(), and the global capacity check never depends on the release predicate."
      },
      {
        "adr": "ADR-027-group-agency-reservations.md",
        "constrains": "Reservation placement takes an absolute staff-chosen expiry; only the draw-down grants a buyer duration. Keeps placement out of scope."
      },
      {
        "adr": "ADR-054-per-channel-sales-windows.md",
        "constrains": "Extends ADR-024's clock discipline to sales windows; same reasoning, already on clock_timestamp(). Nothing to change, cited so the drafter does not 'fix' it."
      },
      {
        "adr": "ADR-064",
        "constrains": "Presale-code redemption accounting on the reservation draw-down path -- the draw-down INSERT being edited is the one whose children deliberately omit the code. Do not touch its columns."
      }
    ],
    "governingTests": [
      {
        "test": "services/inventory/internal/store/channel_allocations_smoke_test.go:TestReleaseCutoffHoldsUnderPoolLockContention",
        "note": "The lock-wait handshake this ticket must reuse: blocker transaction takes the pool FOR UPDATE, a goroutine calls the store, then pg_stat_activity is polled until the waiter is OBSERVED blocked while DB time is still before the boundary. It is a handshake, not a sleep, and all bounds are DATABASE time -- copy that discipline exactly."
      },
      {
        "test": "services/inventory/internal/store/channel_allocations_smoke_test.go (comment block ~line 593)",
        "note": "Pins a verified fact about this database: clock_timestamp() ADVANCES WITHIN A STATEMENT (`SELECT clock_timestamp() = clock_timestamp()` is false), and after a 50ms sleep inside a transaction `now() < clock_timestamp()` is true. That second fact is the mechanism this ticket exploits -- and the first is why a bound written as bare clock_timestamp() is already in the past by the time it is compared."
      },
      {
        "test": "web/storefront/test/hold-picker.test.ts",
        "note": "Pins `remainingMilliseconds = expires_at - server_time`. This is the tripwire that makes ServerTime load-bearing: it is why the RETURNING clause must move with the anchor, and it is discoverable only by reading, since no inventory test asserts on ServerTime at all."
      },
      {
        "test": "services/inventory/internal/store/offering_smoke_test.go:154",
        "note": "Asserts an offering closure does not rewrite a live hold's TTL. Compares two reads of one row, so it is anchor-agnostic -- named so the drafter does not mistake it for a constraint on the anchor."
      },
      {
        "test": "services/commerce/internal/api/server_test.go:235-237,320-326",
        "note": "Table tests feeding literal expires_at/server_time pairs to commerce's hold gate. They pin the PAIR's semantics (held-past-deadline is a 409); they use fixed literals so the anchor change cannot move them, but a change to the pair's meaning would."
      }
    ],
    "notes": "Five grant sites, not four: store.go:614 (GA CreateHold), operational.go:334 (operational conversion), reservations.go:317 (reservation draw-down), seat_claims.go:805 (explicit seats) and seat_claims.go:1708 (best-available). The ticket's line numbers are stale; its file list omits the second seated site."
  }
}
