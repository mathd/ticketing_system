{
  "key": "TKT-101",
  "type": "Story",
  "status": "Planning",
  "labels": [
    "needs:human"
  ],
  "assignee": "autonomous-agent",
  "pr": null,
  "summary": "US-018: Back-office app shell — staff see the venue list",
  "description": "### US-018: Back-office app shell — staff see the venue list\n**As** staff, **I want** a back-office web app where I can see the organizer's venues and their GA capacity, **so that** later slices have a surface to author seat maps on. **Priority:** P2 **Depends on:** —\n\nThis is the prefactor slice: it stands up a new back-office web app and its gateway route, de-risking the new-app plumbing (framework, pnpm workspace, OpenAPI codegen, gateway routing, Compose wiring, healthcheck) **separately** from any seat-map domain logic. No seat-map code here — it renders venues that already exist.\n\n**Acceptance Criteria:**\n- [ ] A new `web/backoffice` app is added to the pnpm workspace and starts under `docker compose up` with a `/healthz` (or equivalent liveness) that the Compose healthcheck gates on; stack matches an existing precedent (storefront Astro+React **or** scanner React+Vite) — the plan picks one with a one-line rationale.\n- [ ] The gateway route table gains a back-office prefix (e.g. `/admin/`) proxying to the new app, added alongside the existing `/api/catalog/` and `/` routes without breaking longest-prefix matching; the storefront catch-all `/` still resolves.\n- [ ] Catalog exposes a venue **read** the back-office consumes: `GET /venues` (list) and/or `GET /venues/{id}` returning at least `{id, name, ga_capacity}`, `organizer_id`-scoped, with an ADR-004 `Cache-Control` tier declared (hours tier — venue geometry is long-lived) and the OpenAPI contract published (ADR-009).\n- [ ] The back-office renders the venue list from that endpoint via generated types; opening `/admin/` in a browser shows the seeded organizer's venues with GA capacity (screenshot in the PR).\n- [ ] The M1 storefront and scanner paths are untouched (their smoke tests stay green).\n\n**Quality gates (every story):**\n- `make check` green (lint + unit + integration + smoke; mirrors CI)\n- Contract tests pass for any touched service boundary (ADR-009 drift check)\n- New/changed public read endpoints declare an ADR-004 TTL tier with rationale\n- New migrations run out-of-band via the service `migrate` subcommand (ADR-022) and build within the 30s startup budget with plain `CREATE INDEX` (ADR-020)\n- Every owned entity is `organizer_id`-scoped (ADR-002)\n\nParent epic: TKT-3. Full lineage: docs/product/prd-v1.md §Capability map (TKT-3).",
  "context": {
    "binding": [
      {
        "id": "ADR-004",
        "rule": "Every public read endpoint declares a volatility-tiered `Cache-Control`/`s-maxage`. Venue/seat-map geometry is the 'hours' tier (longer-lived than the 'minutes' event tier); availability is 'seconds'; hold/order state is never cached. Writes are never cached.",
        "date": "2026-07-12"
      },
      {
        "id": "ADR-002",
        "rule": "Every owned entity carries an `organizer_id` (tenant-aware) even though v1 has one organizer.",
        "date": "2026-07-12"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-004",
        "why": "The new venue read must declare the 'hours' geometry TTL tier, not the events 'minutes' tier."
      },
      {
        "id": "ADR-006",
        "why": "Storefront-shell precedent (Astro) — reuse its scaffolding shape if the plan picks Astro; scanner is the React+Vite alternative."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first: the new venue GET is an OpenAPI addition, codegen'd into the back-office types."
      },
      {
        "id": "ADR-002",
        "why": "Venue reads and the app are organizer-scoped from birth."
      }
    ],
    "reference": [
      {
        "summary": "PRD v1 §Capability map — TKT-3 row is the source of truth for scope",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "TKT-3 epic — COS: authoring UI, versioning safe against sold inventory, GA+seated coexist",
        "link": ".sdlc board TKT-3"
      },
      {
        "summary": "Gateway route table — add the /admin prefix here; longest-prefix wins, catch-all `/` last",
        "link": "gateway/cmd/gateway/main.go"
      },
      {
        "summary": "Storefront = Astro+React public reads; scanner = React+Vite SPA — the two stack precedents",
        "link": "web/storefront, web/scanner"
      }
    ],
    "capturedAt": "2026-07-20"
  },
  "links": [
    {
      "type": "blocks",
      "key": "TKT-102"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ✅ DoR shaped — TKT-101 — all 8 items met/deferred (Gate 1 unblocked)\n\nShaped from the code-seam survey + the TKT-3 decomposition + owner decisions (2026-07-20: back-office = Astro+React; versioning proof is catalog-side via a pinning contract). **No `open owner:human` items remain** — the board hard-block is cleared.\n\n| DoR item | State |\n|---|---|\n| `objective` | met |\n| `scope` | met |\n| `uncertainties` | deferred |\n| `dependencies` | met |\n| `approach` | met |\n| `first_slice` | met |\n| `success` | met |\n| `context_memo` | met |\n\n**Deferred (safely resolved in Planning, no major redesign):**\n- `uncertainties` — Gateway longest-prefix interaction with the /admin route and the catch-all '/' — verified pattern exists in gateway/cmd/gateway/main.go; plan confirms ordering. No product unknowns.\n\n**Ready to prioritize (Gate 1).**"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### 🎯 Claimed TKT-101 — Ready → Planning\n\n**⚠️ GATELESS RUN — human gates waived by the repo owner (process experiment).**\nThe owner is running this ticket Ready→Done in one continuous autonomous pass and has\n**deliberately waived all four human gates** for this run. Recorded here so the board history\nshows this ticket ran gateless:\n- **Gate 1 (priority):** already given — ticket entered in Ready.\n- **Gate 2 (plan approval):** agent self-resolves open decisions after the cross-model critique\n  (prefer the critique's recommendation; else the most reversible option). Decisions recorded in `kind=plan-final`.\n- **Gate 3 (review+merge):** agent squash-merges the PR itself after ai-review converges; review-guide still posted first as the audit record.\n- **Gate 4 (PO acceptance):** agent self-validates each COS against test evidence, then closes with the full metrics comment.\n\nEverything else in the sdlc-ticket skill applies unchanged: config.models routing (plan/aiReview = `kimi-k3:medium@claudex`, planReview/implement = `main-agent`), cross-model review for plan and code, TDD with tests observed red first, `make check` green before every push and after merge, all marker comments, no AI attribution.\n\n**Selection reason:** TKT-101 is the sole ticket in Ready, has zero inbound blockers (it *blocks* TKT-102, outbound), and Gate-1/DoR is already satisfied (all 8 items met/deferred). It is the prefactor slice de-risking new-app plumbing (framework, workspace, OpenAPI codegen, gateway route, Compose wiring, healthcheck) ahead of the seat-map slices.\n\n**Governing ADRs re-resolved at claim time** (mémo snapshot was 2026-07-20; re-checked `docs/adr/` — ADR-023…028 have since landed):\n- **ADR-004** — the new venue read declares the **hours** geometry tier (not the events minutes tier). *(carried from mémo)*\n- **ADR-006** — back-office = Astro+React reusing the **real** storefront shape (`src/middleware.ts` + `@astrojs/node` SSR + `i18n.routing`; the ADR's `Astro.cache`/`src/fetch.ts` names are fictional per its own Status correction). *(carried, corrected)*\n- **ADR-009** — contract-first: the venue GET is an OpenAPI addition, `make generate` regenerates catalog's `openapi_gen.go` + storefront types (back-office consumes generated types). *(carried)*\n- **ADR-002** — venue reads + the app are organizer-scoped from birth. *(carried)*\n- **ADR-028** *(NEW since shaping — added at claim)* — **every service response is runtime-validated against the committed OpenAPI doc and fails closed with 500 on drift** (`shared/go/contract`). The new `GET /venues` handler payload must match the published `Venue` schema exactly or it 500s at runtime; a naming/shape mismatch is invisible to a code-only drafter. This constrains the implementation directly.\n- **ADR-022 / ADR-020** — venue **seed data** is needed (no venue is seeded today, only the default organizer): add it as an out-of-band goose migration run via the catalog `migrate` subcommand. Plain INSERTs, no index concurrency concern here.\n\nBranch: `TKT-101-backoffice-venue-list` off `origin/main`. Assigned to the autonomous agent.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### 📐 Plan draft — drafted by `kimi-k3:medium@claudex` (config.models.plan)\n\nRead-only claudex run (allowlist Read,Grep,Glob; medium effort). Every seam verified by the drafter and independently spot-checked by me before posting (check-generate list, PublicEventList wrapper shape, gateway /internal 404, Cache-Control header ref — all confirmed at HEAD).\n\n---\n\nI have every seam verified. Writing the plan now.\n\n---\n\n# TKT-101 execution plan — back-office shell + venue list\n\n## 1. DoD (maps to COS)\n\n| COS | Done condition |\n|---|---|\n| 1 | `web/backoffice` in `pnpm-workspace.yaml`; `backoffice` compose service builds, `/healthz` returns 200, healthcheck gates; `make check` green. |\n| 2 | Gateway `routes` gains `\"/admin/\": \"BACKOFFICE_URL\"` (NOT path-stripped, like `/scanner/`); `GET /admin/` proxies to the app; `/` still resolves to storefront. |\n| 3 | Catalog `GET /public/venues` returns `[{id, organizer_id, name, ga_capacity, created_at}]` scoped by `organizer_id`, `Cache-Control` hours tier, in `openapi.yaml`, codegen clean. |\n| 4 | `/admin/` SSR-renders the seeded venues via `api-types.gen.ts` types; screenshot in PR. |\n| 5 | Storefront + scanner smoke green. |\n\n## 2. Chosen stack + rationale\n\n**Astro 7 SSR + React** (`@astrojs/node` standalone), copying `web/storefront`'s real scaffolding — **minus i18n** (staff tool, single locale; drop `i18n.routing`, `redirects`, `src/locales`). Confirmed owner's shaping decision. The scanner React+Vite+nginx alternative is viable but loses: the back-office's next stories (seat-map authoring) are SSR/data-loading heavy, and reusing the storefront's `api.ts`/`cache.ts`/`middleware.ts` pattern beats re-deriving it on Vite. One-line: storefront shape reused, i18n stripped.\n\n## 3. Read-path design decision — **(a) `/public/venues`**\n\nRecommend **(a)**, not the ticket's literal bare `GET /venues`.\n\n- **Why not (b)** bare `/venues` GET: the codebase has exactly two read conventions — `/public/...` (cached, unauthenticated) and `/internal/...` (`X-Internal-Token`). A third bare-read convention is a new precedent for no gain, and `/venues` already hosts `createVenue` POST; adding GET there mixes the trusted-write and cached-read surfaces on one path.\n- **Why not (c)** `/internal/venues` + gateway token injection: the gateway currently injects no credentials and `/internal/` is explicitly 404'd at the gateway (`main.go:103`). Wiring token injection is new gateway machinery this prefactor slice shouldn't own.\n- **Why (a):** matches the only existing cached-read convention; the back-office is unauthenticated today anyway (no admin auth in scope); the gateway already proxies `/api/catalog/public/...` to catalog with zero new machinery. When admin auth lands, `/public/venues` can be re-scoped or the back-office moved behind a token — a documented follow-up, not a blocker.\n\n**Exact contract:**\n- Route: `GET /public/venues` → operationId `listPublicVenues`, query param `organizer_id` (uuid, required) — scoping is explicit, not inferred (ADR-002), and there's no session to infer from.\n- Response `200`: array of `Venue` (reuse the existing schema verbatim — all five required fields). New schema `PublicVenueList` = `{ venues: [Venue] }` or bare `[Venue]`; pick **bare array** to match `PublicEventList` style only if that's an array — check: `PublicEventList` is an object; follow it with `{ venues: [...] }` for forward-compat (pagination later). **Decide: object wrapper `{ venues: [Venue] }`.**\n- Cache-Control: **new constant** in `server.go`: `CacheControlPublicVenueReads = \"public, max-age=3600, s-maxage=3600\"` (hours tier, ADR-004 — venue geometry is long-lived). Declare the `Cache-Control` response header in the spec (mirror line 450-452 pattern).\n- **ADR-028:** handler must emit `organizer_id`, `created_at` (RFC3339), all required fields, exact field names — response is validated against the spec and 500s on drift. The codegen'd `Venue` model is the source of truth for the payload.\n\n## 4. Files to add/change\n\n**Catalog spec + gen:**\n- `services/catalog/api/openapi.yaml` — add `/public/venues` GET (after `/public/festivals/{id}` block, ~line 540); add `PublicVenueList` schema; declare Cache-Control header. Add `OrganizerId` query param component.\n- `services/catalog/internal/api/openapi_gen.go` — regenerated via `make generate` (never hand-edit).\n\n**Catalog handler + store:**\n- `services/catalog/internal/api/server.go` — add `CacheControlPublicVenueReads` const (~line 39); implement generated `ListPublicVenues` handler setting the hours-tier header, scoping by `organizer_id`.\n- `services/catalog/internal/store/store.go` — add `ListVenues(ctx, organizerID uuid.UUID) ([]Venue, error)` to `Store` interface (~line 285).\n- `services/catalog/internal/store/postgres.go` — implement `ListVenues` (query style per `CreateVenue`: `SELECT id, organizer_id, name, ga_capacity, created_at FROM venues WHERE organizer_id=$1 ORDER BY name`).\n\n**Migration:**\n- `services/catalog/internal/store/migrations/0003_seed_default_venues.sql` — plain `INSERT INTO venues (organizer_id, name, ga_capacity) VALUES ('00000000-...-0001', ...)` ×2-3 rows + matching `Down` DELETE. No index (ADR-020 N/A). Runs via existing `catalog-migrate` compose job (ADR-022).\n\n**Gateway:**\n- `gateway/cmd/gateway/main.go` — add `\"/admin/\": \"BACKOFFICE_URL\"` to `routes` map (:30); keep `/` last. No strip logic change (non-`/api/` so not stripped — correct).\n\n**New app `web/backoffice`:**\n- `package.json` — name `backoffice`, scripts mirror storefront (`generate:api` → `src/lib/api-types.gen.ts` from catalog spec), deps: astro/@astrojs/node/@astrojs/react/react/openapi-typescript/vitest/oxlint/typescript.\n- `astro.config.mjs` — `output:'server'`, node standalone adapter, `ssr.noExternal:true`, `base:'/admin'` (critical: serves under `/admin/`), **no i18n**, react integration.\n- `src/pages/index.astro` — venue list page (SSR fetch + render).\n- `src/pages/healthz.ts` — liveness `{status:'ok'}` no-store (copy storefront).\n- `src/lib/api.ts` — `getVenues()` via `GATEWAY_URL/api/catalog/public/venues?organizer_id=...`.\n- `src/lib/api-types.gen.ts` — generated.\n- `Dockerfile` — copy storefront's (root context, `npm i -g pnpm@11.12.0`, `--filter backoffice`).\n- `src/App.test.tsx` or `src/pages/index.test.tsx` — vitest render test (scanner precedent).\n\n**Wiring:**\n- `pnpm-workspace.yaml` — add `web/backoffice` to `packages`.\n- `compose.yaml` — add `backoffice` service (root context build, wget `/healthz` healthcheck, `GATEWAY_URL: http://gateway:8080`); add `BACKOFFICE_URL: http://backoffice:8080` to gateway env + `backoffice: {condition: service_healthy}` to gateway `depends_on`.\n- `Makefile` `check-generate` (:34) — add `web/backoffice/src/lib/api-types.gen.ts` to the drift-check path list.\n\n## 5. Test plan (TDD — RED first)\n\n1. **Catalog handler test** (`server_test.go`): `TestListPublicVenues` — seed 2 venues for organizer A + 1 for organizer B via store, `GET /public/venues?organizer_id=A` returns exactly A's 2 with all five fields, correct `Cache-Control: public, max-age=3600, s-maxage=3600`. Also: missing/invalid `organizer_id` → 400. RED: 404 (route unregistered).\n2. **Store test** (wherever `CreateVenue` is tested — `postgres` integration test seam): `ListVenues` returns only the scoped organizer's rows. RED: method undefined (compile fail).\n3. **Gateway route**: no `gateway/*_test.go` exists. **Decision: cover via smoke** — add a smoke assertion rather than introduce a new gateway unit-test harness in a prefactor slice. (Flag to reviewer: cheap to add a `main_test.go` asserting `routes[\"/admin/\"] == \"BACKOFFICE_URL\"`; optional.)\n4. **Back-office render test** (vitest, scanner `App.test.tsx` precedent): mock `api.ts`, render list, assert venue names + GA capacities appear. RED: page/component doesn't exist.\n5. **Smoke (end-to-end proof)**: extend `smoke/` — `GET gateway:8080/api/catalog/public/venues?organizer_id=<default>` returns seeded venues (proves migration + handler + contract), and `GET /admin/` returns 200 HTML containing a seeded venue name (proves gateway proxy + SSR render through the real stack). ADR-028 drift is caught here, not in unit tests.\n\n## 6. Risks / traps\n\n- **ADR-028 drift 500**: `created_at` must serialize RFC3339; `organizer_id` required and present; field names exact. A mismatch passes unit tests (if the test hand-builds expected JSON loosely) but 500s live — the smoke contract test is the real guard. Build the response from the codegen'd `Venue` type, not a hand-rolled map.\n- **`check-generate` fails** if the spec is edited without rerunning `make generate` — and the Makefile drift list (:34) must include the new backoffice gen file or backoffice drift is unchecked.\n- **`base: '/admin'`**: Astro must serve under `/admin/` since the gateway doesn't strip; a missing `base` renders the app at `/` and every asset link 404s behind the proxy. Asset paths must respect `base`.\n- **Longest-prefix**: `/admin/` vs `/` — `http.ServeMux` handles it; keep `/` last in the map literal for readability (map iteration is random, ServeMux ordering is what actually governs).\n- **Node 25+ corepack**: Dockerfile must `npm i -g pnpm@11.12.0`, not `corepack enable` (storefront already encodes this — copy it).\n- **Hours-tier value**: don't reuse `CacheControlPublicReads` (minutes); new constant, exact value `public, max-age=3600, s-maxage=3600`.\n- **Migration ordering**: `0003` must sort after `0002` (organizer FK). Goose runs in filename order; `ORDER BY name` in `ListVenues` keeps output deterministic for tests/screenshot.\n- **`PublicVenueList` wrapper vs array**: pick wrapper now; changing later is a contract revision.\n\n## 7. Sequencing (gate stays green incrementally)\n\n1. **Migration** `0003_seed_default_venues.sql` (green: existing migrate job runs it).\n2. **Spec + codegen**: add `/public/venues` to `openapi.yaml`, `make generate`, add backoffice gen path to `check-generate` — but backoffice gen file doesn't exist yet, so **defer the Makefile edit to step 5** or create the app skeleton first. Recommended: app skeleton (step 5) codegen target created before wiring `check-generate`.\n3. **Store** `ListVenues` (RED store test → implement → green).\n4. **Handler** `ListPublicVenues` + hours-tier const (RED handler test → implement → green). Catalog fully green here.\n5. **`web/backoffice` skeleton**: package.json, astro.config (`base:'/admin'`), healthz.ts, empty index.astro, Dockerfile, `generate:api`; add to `pnpm-workspace.yaml`; `pnpm install`; now add gen file to `check-generate` and run `make generate`. Lint/build green.\n6. **Back-office feature**: `api.ts` + `index.astro` + render test (RED → green).\n7. **Gateway route** + **compose** service + `depends_on`/`BACKOFFICE_URL`.\n8. **Smoke assertions** (venues endpoint + `/admin/` 200 with venue name).\n9. `make check` full green; manual browser screenshot of `/admin/` for the PR.\n\nSteps 1–4 land catalog independently; 5–6 land the app independently; 7–8 wire them; nothing breaks mid-sequence because the gateway route and compose service only appear once both ends exist.\n\n---\n\n**Open questions for the reviewer:** (i) `/public/venues` vs the ticket's literal `/venues` — I chose (a), overrule if the ticket text is binding; (ii) `PublicVenueList` object wrapper vs bare array; (iii) gateway unit test vs smoke-only coverage.\n"
    },
    {
      "stage": "plan-review",
      "kind": "plan",
      "body": "<!-- sdlc:stage=plan-review kind=plan -->\n### 🔎 Plan critique — by `main-agent` (config.models.planReview), adversarial + pre-mortem\n\nCross-model: draft by `kimi-k3@claudex`, critique by `main-agent`. Reviewer ≠ author ✓. I verified every file/symbol/line the draft names against HEAD (`git grep`/`sed`); all cited seams exist and the numbers are accurate. Verdicts below.\n\n**ACCEPT — the spine is right:**\n- **Read path `(a) /public/venues`** — accepted. The reasoning holds against the code: two read conventions exist (`/public/*` cached, `/internal/*` token-guarded), the back-office is unauthenticated today, and the gateway already proxies `/api/catalog/public/*` with zero new machinery. There's even a codebase precedent that *staff surfaces stay off the public gateway* (smoke/group_reservations_test.go:19) — but that's for token-guarded write/admin surfaces, whereas a cached read of long-lived geometry is genuinely public-shaped. Overruling the ticket's literal `/venues` is the correct call and I'm recording it as a self-made Gate-2 decision (below).\n- **`{ venues: [Venue] }` object wrapper** — accepted, and now *confirmed* against the code: `PublicEventList` is `type: object, required: [events]` (openapi.yaml:970). Bare array would break the house style; wrapper matches. Reuse the existing `Venue` schema verbatim.\n- **Hours tier `public, max-age=3600, s-maxage=3600`, new constant** — accepted. Do NOT reuse `CacheControlPublicReads` (300s minutes). ADR-004 names venue/seat-map geometry as the hours tier explicitly.\n- **`base: '/admin'` in astro.config** — accepted and load-bearing: the gateway does not strip web-shell prefixes (main.go), so the app must serve under `/admin/` or every asset 404s behind the proxy. Good catch.\n- **ADR-028 framing** — accepted. Build the response from the codegen'd `Venue` type; the smoke contract guard (smoke/contract_validation_test.go) is the real net, unit tests aren't.\n- **Astro+React minus i18n** — accepted. Owner pre-decided Astro (readiness.approach). Dropping i18n for a single-locale staff tool is a sound simplification, not a deviation.\n\n**AMEND — three corrections before this is implementable:**\n1. **`make generate` must gain a backoffice line, not just `check-generate`.** The draft (§4, §5-step-2) is fuzzy here. `check-generate` (Makefile:33) runs `generate` then `git diff --exit-code` over a *fixed file list* that currently ends at `web/storefront/src/lib/api-types.gen.ts`. TWO edits are required, both mandatory: (i) add `pnpm --filter backoffice generate:api` to the `generate` target (Makefile:30, after the storefront line) so the file is actually produced; (ii) add `web/backoffice/src/lib/api-types.gen.ts` to the `check-generate` diff list (Makefile:34). Miss (i) and the committed types never regenerate; miss (ii) and backoffice contract drift is unchecked. Name both in the plan-final.\n2. **Smoke has a concrete precedent to copy, not invent.** `smoke/catalog_publication_test.go` already hits storefront HTML through the gateway and asserts ADR-004 cache headers + rendered content (`X-Cache-Status` MISS→HIT, localized names). The `/admin/` assertion and the `/api/catalog/public/venues` hours-tier `Cache-Control` assertion should follow that file's shape (helpers `get(t,url,hdr)` at smoke/smoke_test.go:60, `gatewayURL` at :39). Concretely: assert (a) `GET {gateway}/api/catalog/public/venues?organizer_id=<default>` → 200, body contains the seeded venue names, `Cache-Control: public, max-age=3600, s-maxage=3600`; (b) `GET {gateway}/admin/` → 200 `text/html` containing a seeded venue name. This proves migration + handler + contract + gateway proxy + SSR in one pass.\n3. **Gateway coverage: smoke-only is acceptable, but add the one-line unit test too — cheap insurance.** The draft left this open. Decision: do BOTH — the smoke assertion (item 2) is the real end-to-end proof, AND add a trivial `gateway/cmd/gateway/main_test.go` asserting `routes[\"/admin/\"] == \"BACKOFFICE_URL\"` and that `/` remains the catch-all. A gateway unit test is a *new* test file (introduced at plan-review — flagged below); it's 15 lines and guards the route table against a careless future edit. If it proves awkward (the `routes` var is package-private but the test is same-package, so it won't), fall back to smoke-only and say so.\n\n**PRE-MORTEM — what kills this in Building:**\n- **`pnpm install` + lockfile churn.** Adding `web/backoffice` mutates `pnpm-lock.yaml`. The Dockerfiles use `--frozen-lockfile`; the lockfile MUST be committed or the hermetic build fails. Also honor `minimumReleaseAgeExclude` in pnpm-workspace.yaml (astro@7.0.9 is already excluded — reuse the SAME pinned versions as storefront so no new exclusion is needed).\n- **`astro check` in the build script needs @astrojs/check + a tsconfig.** Storefront's `build` is `astro check && astro build`. Copy its tsconfig.json and devDeps or the backoffice `build` fails on a missing checker.\n- **Migration determinism.** `0003_seed_default_venues.sql` INSERTs need stable, ORDER-BY-able names so the smoke assertion and screenshot are deterministic. `ListVenues` must `ORDER BY name`.\n- **Compose `depends_on` cycle risk — none, but verify.** backoffice depends on nothing at startup (it fetches venues lazily at request time via GATEWAY_URL, like storefront); gateway depends on backoffice being healthy. Healthchecks are self-only (`/healthz`), so no deadlock — but the backoffice `/healthz` must be reachable at the container root `127.0.0.1:8080/healthz`, NOT under `/admin/` (the compose healthcheck hits the container directly, pre-proxy). With `base:'/admin'`, confirm Astro still serves `src/pages/healthz.ts` at `/healthz` or move the healthcheck to `/admin/healthz`. **This is the single most likely Building failure — resolve it explicitly in implementation:** either set the healthcheck to `/admin/healthz` (matches base) or serve healthz outside the base. Simplest: healthcheck hits `/admin/healthz` to match `base`.\n- **`check-generate` ordering in the sequence.** Draft §7 step 2 correctly defers the Makefile `check-generate` edit until the backoffice codegen target+file exist (step 5). Keep that ordering or an intermediate `make check` goes red on a missing file.\n\n**Verdict:** plan is sound; accept with the three amendments and the healthz-under-base resolution folded in. No rejects. Proceeding to finalize (self-resolving the open decisions per the gateless-run waiver)."
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### ✅ Plan finalized — GATELESS (Gate 2 self-resolved by the agent per the owner's waiver)\n\nThis is a **delta** on the `kind=plan` draft (kimi-k3) as amended by the `kind=plan-review` critique (main-agent). Read the three comments together — the draft + critique amendments **are** the plan. Below: only the self-made Gate-2 decisions and the required audit sections.\n\n#### Self-made decisions (Gate 2 was waived; I resolved each — rule: prefer the critique's recommendation, else the most reversible option)\n\n1. **Read path → `GET /public/venues`** (not the ticket's literal `/venues`). *Rationale:* critique recommended it and it matches the sole existing cached-read convention; overruling the literal ticket text because the ticket itself says \"`GET /venues` (list) **and/or** `GET /venues/{id}`\" — the shape is illustrative, the requirement is \"a venue read the back-office consumes, organizer-scoped, hours-tier, contract-published.\" Reversible: renaming a route pre-release is a one-line spec change. **Scope note:** shipping the **list** (`/public/venues`) only — the back-office needs the list to render; `/{id}` has no consumer in this slice (YAGNI, and the ticket's \"and/or\" permits list-only).\n2. **Response shape → `{ venues: [Venue] }` object wrapper.** *Rationale:* matches `PublicEventList` (`type:object, required:[events]`, confirmed at openapi.yaml:970); forward-compatible with pagination. Most reversible-with-the-grain choice.\n3. **Gateway route coverage → BOTH smoke + a 15-line unit test.** *Rationale:* critique recommended both; the smoke assertion is the real end-to-end proof, the unit test cheaply guards the route table. If the unit test proves awkward it falls back to smoke-only (noted in code).\n4. **Compose healthcheck path → `/admin/healthz`** (matches `base:'/admin'`). *Rationale:* with Astro `base:'/admin'`, `src/pages/healthz.ts` serves at `/admin/healthz`, not `/healthz`. The compose healthcheck hits the container directly (pre-proxy), so it must target the base-prefixed path. Most reversible and least surprising: one path aligns with the app's base rather than special-casing healthz outside it. This was the pre-mortem's single most likely Building failure — resolved up front.\n\n#### REQUIRED — introduced at plan-review, reviewed by no second model\nThe cross-model guarantee covers the draft (kimi-k3) and the code (ai-review by kimi-k3), **not** these elements the critique (main-agent) *added* rather than accepted/amended from the draft. They reach Gate 2 authored and reviewed only by me. Named again for the Gate-3 hand-off:\n- **The `make generate` two-edit correction** (add `pnpm --filter backoffice generate:api` to the `generate` target **and** the gen path to the `check-generate` diff list). New requirement, not in the draft.\n- **The `/admin/healthz` healthcheck resolution** (decision #4). Introduced by the critique's pre-mortem; not in the draft.\n- **The gateway `main_test.go` unit test** (decision #3). A new test file the draft left as an open question.\n- **The specific smoke assertions** (hours-tier `Cache-Control` on the venues endpoint + `/admin/` HTML containing a seeded venue name), modeled on `smoke/catalog_publication_test.go`. The draft named smoke coverage generically; the concrete assertions are the critique's.\n\n#### DoD (consolidated, maps to COS)\n- **COS1:** `web/backoffice` (Astro7 SSR + React, no i18n) in `pnpm-workspace.yaml`; `backoffice` compose service builds under `docker compose up`, `/admin/healthz` returns 200, gateway `depends_on` gates on it. `make check` green.\n- **COS2:** gateway `routes` gains `\"/admin/\": \"BACKOFFICE_URL\"` (non-`/api/`, not stripped, like `/scanner/`); `/admin/` proxies to the app; `/` still resolves to storefront. Guarded by unit test + smoke.\n- **COS3:** catalog `GET /public/venues?organizer_id=<uuid>` → `{venues:[Venue]}` (all 5 required fields), organizer-scoped, `Cache-Control: public, max-age=3600, s-maxage=3600` (hours tier, ADR-004), spec published + codegen clean (ADR-009), contract-validated (ADR-028).\n- **COS4:** `/admin/` SSR-renders the seeded venues via generated types (`api-types.gen.ts`); PR carries a browser screenshot.\n- **COS5:** storefront + scanner smoke stay green (untouched).\n\n#### Test plan (TDD, RED first) — the order tests are written and observed failing\n1. `server_test.go::TestListPublicVenues` — organizer-scoped list, all 5 fields, hours-tier header, `env.validateResponse` (ADR-028) passes; bad/missing `organizer_id` → 400. RED: route 404.\n2. store `ListVenues(ctx, organizerID)` — scoped rows only, `ORDER BY name`. RED: compile fail (undefined).\n3. `gateway/cmd/gateway/main_test.go` — `routes[\"/admin/\"]==\"BACKOFFICE_URL\"`, `/` catch-all intact. RED: key absent.\n4. `web/backoffice` vitest render test — mock `api.ts`, assert venue names + GA capacities render. RED: page absent.\n5. `smoke/` — venues endpoint (200, seeded names, hours-tier header) + `/admin/` (200 text/html, seeded name). RED until full stack wired.\n\n#### Sequencing (gate green incrementally): migration → spec+codegen → store → handler → backoffice skeleton (+ Makefile generate/check-generate edits) → backoffice feature → gateway route+compose → smoke. Catalog (1-4) lands independently of the app (5-6); wiring (7-8) only appears once both ends exist.\n\n#### Governing ADRs (Gate-3 hand-off reminder): ADR-004 (hours tier), ADR-006 (Astro real shape, no fictional APIs), ADR-009 (contract-first, `make generate`), ADR-002 (organizer-scoped), **ADR-028 (response drift fails closed — build response from codegen'd type)**, ADR-022/ADR-020 (seed migration out-of-band, plain SQL).\n\n**risk:** NOT low — touches a service boundary (new catalog endpoint), a data migration (seed), CI/gate config (Makefile), gateway routing, and >5 files. The plan gate applies (and here is self-resolved under the explicit waiver, not bypassed silently).\n\nMoving to `needs:human` per the label machine; under the waiver I proceed to Building without waiting.\n"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Staff have no surface to view/author venues; this stands up the back-office app so later seat-map slices have a home. Prefactor that de-risks new-app plumbing away from domain logic."
    },
    "scope": {
      "state": "met",
      "note": "IN: new web/backoffice app (Astro+React) in the pnpm workspace + Compose + healthcheck; gateway /admin route; catalog venue read (GET /venues[/{id}]); render the venue list. OUT: any seat-map model/authoring (TKT-102+), auth/RBAC beyond what M1 has, editing."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Gateway longest-prefix interaction with the /admin route and the catch-all '/' — verified pattern exists in gateway/cmd/gateway/main.go; plan confirms ordering. No product unknowns."
    },
    "dependencies": {
      "state": "met",
      "note": "None — first slice in the chain. Extends existing catalog venues + gateway route table; no blocked-by links."
    },
    "approach": {
      "state": "met",
      "note": "OWNER DECISION 2026-07-20: back-office = Astro+React, matching the storefront (ADR-006) precedent. Add /admin to the gateway route table; catalog gains an organizer-scoped venue GET (OpenAPI/ADR-009) at the ADR-004 hours tier; back-office renders via generated types."
    },
    "first_slice": {
      "state": "met",
      "note": "The whole ticket IS the first slice: shell + one working read-only page (venue list). Demoable via a browser screenshot at /admin."
    },
    "success": {
      "state": "met",
      "note": "COS: `docker compose up` brings up web/backoffice with a passing healthcheck; /admin proxies to it; the seeded organizer's venues+GA capacity render from the catalog read; storefront/scanner smoke green."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked at decomposition: ADR-004 (hours tier), ADR-006 (Astro precedent), ADR-009 (contract-first), ADR-002 (tenancy). governingAdrs present."
    }
  }
}
