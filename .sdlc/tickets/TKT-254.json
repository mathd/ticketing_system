{
  "key": "TKT-254",
  "type": "Task",
  "summary": "Flaky: payments journal verification fails with `broken chain` during the smoke stage (TKT-243 ai-review incidental)",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "number": 240,
    "url": "https://github.com/mathd/ticketing_system/pull/240",
    "state": "merged"
  },
  "parent": null,
  "description": "**Observed during TKT-243's local gate, on a catalog-only change that cannot touch payments.**\n\n`make check` failed in the smoke stage with:\n\n```\nbroken chain organizer=00000000-0000-0000-0000-000000000001 sequence=167\nmake: *** [Makefile:123: smoke] Error 1\n```\n\nThe message comes from `services/payments/internal/store/store.go:327`, the journal chain verifier:\n\n```go\nif e.Sequence != seqByOrg[e.OrganizerID]+1 || !hmac.Equal(prev, e.PreviousHash) {\n    return fmt.Errorf(\"broken chain organizer=%s sequence=%d\", e.OrganizerID, e.Sequence)\n}\n```\n\n**Re-running `make check` on the identical commit (029b919d) passed with zero failures**, which is\nwhat makes this a flake rather than a defect in the change under test. TKT-243's diff touches only\n`services/catalog/**` and `docs/development.md`; it adds no payments code, no migration and no event.\n\n**Why it matters more than an ordinary flake.** The predicate that fired is a **sequence-gap** check\n(`e.Sequence != prev+1`), not a hash mismatch — the chain's integrity math was fine, the *ordering*\nof what the verifier read was not. Two candidate explanations, and they have very different\nconsequences:\n\n1. **Test-harness only** — the verifier reads the journal while the smoke stack is still writing to\n   it, so it observes a row whose predecessor has not landed yet. Harmless in production; the fix is\n   to quiesce or snapshot before verifying.\n2. **A real ordering weakness in the journal read** — in which case an operator running verification\n   against a live database gets a false integrity alarm, which is expensive: ADR-021's whole point is\n   that an integrity claim must not cry wolf.\n\nWorth noting this is the same *shape* as TKT-234 (append-only trails ordering by a non-monotonic wall\nclock) — that ticket is about `claim_history` and access, this one about the payments journal, and\nthey may share a root cause.\n\n**Conditions of Success:**\n- Determine which of the two explanations above is true, with evidence — reproduce it, do not argue\n  it. A `broken chain` alarm that turns out to be a harness artifact must be *shown* to be one.\n- If (1): make the smoke stage's verification deterministic — quiesce writers, or verify inside a\n  snapshot — so a green gate means the journal verified and a red one means something is wrong.\n- If (2): fix the read ordering, and pin it with a test that fails against the current code.\n- Either way, the sequence-gap branch should say enough to tell the two apart from the log alone —\n  today it prints the offending sequence but not the previous one it expected.\n\nQuality gates: `make check` green, and the fix demonstrated against a run that previously failed.\n\nNon-goals: changing the chain construction or the hash discipline (ADR-016/ADR-021) — this is about\nwhat the verifier READS and when, not what it computes.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Filed from TKT-243's gate run — incidental, not that ticket's to fix\n\nTriaged as **incidental** under the pipeline's rule: it is real, it is reproducible-in-principle, and\nit is entirely outside TKT-243's diff (catalog only). Fixing it there would have meant an unrelated\npayments change riding a catalog PR.\n\n**Evidence it is not TKT-243's:** the identical commit passed `make check` on the immediately\nfollowing run, and the ticket's diff contains no payments code.\n\nNeeds shaping before Gate 1 — in particular, whether this reproduces under deliberate load\n(`compose.onsale-load.yaml` is already part of every smoke stack) and whether it shares a root cause\nwith TKT-234."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Shaped. COS 1 is discharged here: it is explanation **(1), harness-only** — with a sharper cause than the ticket assumed, and one twist that changes the fix.\n\nThe ticket asks to determine which of two explanations is true **with evidence, by reproduction not\nargument**. The reproduction is not available on demand — the flake is timing-dependent and the\nidentical commit passed on re-run. What *is* available, and is stronger than a lucky re-trigger, is\na closed mechanical chain read at HEAD in which every link is a file and a line. It is below. The\nimplementation still owes a **deterministic test**; that is where the \"executed, not argued\" rule\ngets paid.\n\n#### Why a correctly-written journal cannot hold a sequence gap\n\nEvery append funnels through `AppendWithSettlement`, which takes `SELECT last_sequence,last_hash\nFROM journal_heads WHERE organizer_id=$1 **FOR UPDATE**` (`store.go:262`), derives `seq++` under that\nlock, and writes the entry and the head update **in one transaction** (`store.go:283-287`). A\nconcurrent appender cannot even read the head until the first commits, and a rollback undoes both\nrows. So per organizer, sequences are allocated serially and commit in order. **No correct write\npath can produce a gap** — which means the gap was created from outside the write path.\n\n#### Where it is created\n\n`scripts/smoke.sh`'s corruption block, and it is worse than a race — it is **durable corruption**:\n\n| line | what happens |\n|---|---|\n| `:338` | `CREATE TABLE journal_entries_smoke_backup AS TABLE journal_entries` — snapshot taken. Same statement runs `ALTER TABLE journal_entries **DISABLE TRIGGER ALL**`, so the append-only guard is off for the whole block. |\n| `:340-345` | corruption + `expect_verify_failure` — **seconds** of wall time, each `psql -c` its own transaction (`:325`). |\n| `:342`, `:346` | `DELETE FROM journal_entries; INSERT INTO journal_entries SELECT * FROM journal_entries_smoke_backup;` |\n\nAn append that commits **after** `:338` is not in the backup. The restore's `DELETE` removes it and\nthe `INSERT ... SELECT` does not put it back — but its `journal_heads` update survives until `:350`.\nThe next append then takes `seq++` from the surviving head and chains onto a row **that no longer\nexists**. That is a permanent, self-perpetuating gap, and `:351`'s bare `verify-journal` reports it\nas `broken chain organizer=… sequence=N`. Under `set -euo pipefail` (`:8`) its non-zero exit aborts\nthe stage — matching the ticket's log, which shows no `smoke:` prefix line and therefore came from a\n**bare** invocation (`:321` or `:351`), not from `expect_verify_failure`, which swallows output.\n\n#### Who is writing\n\nThe stack is still serving HTTP — teardown is only the `EXIT` trap (`:37`). Commerce runs a recovery\nticker (`recovery/runner.go:218-231`) whose `JournalFact.OrderFailed` POSTs payments\n`/internal/facts` (`recovery/clients.go:341-361`) → `api/server.go:179` → `journal.Append`, for\norganizer `…0001`. And it is fast: `scripts/smoke.sh:32` adds `compose.smoke.yaml` on the default\nnon-hermetic path, which sets **`RECOVERY_INTERVAL: 2s`** (`compose.smoke.yaml:16`). A writer fires\nevery two seconds for the entire block.\n\nIt also always has work. `smoke/psp_recovery_test.go:64,382-384` deliberately backdate `updated_at`\nby 10 minutes and NULL the lease — defeating `ClaimStuckOrders`' 2-minute grace\n(`store/recovery.go:81`) on purpose, so claimable residue is guaranteed rather than incidental. The\nintermittency is just whether a 2-second tick lands inside the window on a given run.\n\n#### The twist, and why the obvious fix is the wrong one\n\nThe instinctive remedy is to make the read consistent — wrap `Journal.Verify` in a `REPEATABLE READ`\ntransaction. **That does not fix this.** The corruption is in the table, not in the read: a\nconsistent snapshot of a journal that really is missing row 166 still reports the gap, correctly. The\nonly remedy that removes the cause is to **stop the writer** for the duration of the corruption\nblock. Scope it to **commerce**, not to one runner — `bulkrefund/runner.go:465-473` reaches payments\ntoo — and place the quiesce **after** the Go smoke suite, since `smoke/psp_recovery_test.go` requires\na live runner claiming its orders within 45s.\n\n#### A second, separate defect — same function, not this flake\n\n`Journal.Verify` runs its `journal_entries` scan (`store.go:306`) and its `journal_heads` scan\n(`store.go:347`) as two statements on a pooled `*sql.DB` with **no `BeginTx`** — two snapshots,\npossibly two connections. Against a live database that is a real false-alarm source, and ADR-003 puts\n`verify-journal` in the gate precisely so it does not cry wolf. It is in scope for this ticket\nbecause it is four lines away and cheap. **It must be fixed and tested as its own thing**: it\nproduces `journal head mismatch`, *never* `broken chain`, so folding it into the flake's narrative\nwould attach the wrong evidence to the wrong claim.\n\n#### Corrections to the ticket text, recorded rather than inherited\n\n- **\"Same shape as TKT-234 … may share a root cause\" — does not survive the read.** TKT-234 is about\n  trails whose primary sort key is a non-monotonic wall clock (`ORDER BY occurred_at`). The payments\n  journal orders by `sequence` (`store.go:306`). Different mechanism; no shared cause.\n- **\"reproduce it, do not argue it\"** — honoured in the only form available. The flake cannot be\n  replayed on demand, so the deliverable is a test that constructs the gap **deterministically**, and\n  the closeout must say exactly that rather than claim a reproduction it did not get.\n- The ticket's own last COS (\"the message should tell the two apart\") is **half the diagnosis cost\n  here**: `e.Sequence != seqByOrg+1 || !hmac.Equal(prev, e.PreviousHash)` short-circuits, so the log\n  cannot say which condition fired. Splitting it is cheap and independently worth doing.\n\n#### DoR\n\nAll 8 items **met**; context-mémo baked (`governingAdrs` ×4, all resolving at HEAD; `governingTests`\n×4, including `TestSmokeJournalKeyLiteralsMatchScript`, which **reads `scripts/smoke.sh`** — the file\nthis ticket edits).\n\n**`risk:low` is NOT claimed.** The change touches `scripts/smoke.sh`, which *is* the local gate's\nsmoke stage (CI config by the label's own carve-out), and a money-path audit surface. The plan gate\nstands.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed. Running **gateless** per `config.gates: \"autonomous\"`.\n\n**Gate 1 was granted explicitly by the owner** (\"Run TKT-254 completely autonomous. Take from backlog\nand start the work until done\"), not self-taken — the agent never promotes itself out of Backlog. The\nticket was found genuinely in `Backlog` with `readiness: null`, so it was **shaped first** rather than\nentered at `Ready`; that shaping is the preceding `kind=readiness` comment and it is what discharged\nCOS 1.\n\n**Selection reason:** the owner named it. It is also well-posed for a gateless run — the expensive,\njudgement-heavy half (which of two explanations is true) resolved to a closed chain of file:line\nevidence at shaping, so what remains is mechanical and testable.\n\n**Blockers:** none. `links` is empty and TKT-234 was checked and is explicitly *not* a dependency —\ndifferent mechanism (wall-clock ordering vs. `sequence` ordering).\n\n**Risk verdict — `risk:low` is NOT claimed, stated explicitly rather than left silent.** Two of the\nlabel's own carve-outs are hit:\n- **CI/deploy config** — `scripts/smoke.sh` *is* the local gate's smoke stage, and `make check` mirrors\n  CI. A change there that is wrong does not fail loudly in one test; it changes what the gate proves.\n- **Money path** — `verify-journal` is the payments audit surface ADR-003 put in the gate.\n\nSo the plan gate stands (taken by the agent per § Autonomous mode, with every decision recorded in\n`kind=plan-final`). Branch: `TKT-254-journal-verify-determinism`, off `origin/main`.\n\n**One thing the planner must not inherit:** the instinctive fix here — snapshot `Journal.Verify` in a\n`REPEATABLE READ` transaction — is *wrong for this flake*. The corruption is durable, in the table, not\na read-skew artifact. Shaping argues this in full; the plan must quiesce the writer, and treat the\n(real, separate) two-snapshot defect as its own item with its own test.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — by `gpt-5.6-luna` (codex, effort high), per `config.models.plan`\n\nRead-only run; the drafter modified nothing. It read the code independently (~15 tool calls across\n`store.go`, the recovery runner, `scripts/smoke.sh`, the journal smoke tests and all four governing\nADRs) rather than taking the brief on trust, and it pushed back on one shaping claim (recorded at the\nend of its draft). Verbatim below.\n\n---\n\n# Execution plan\n\nThe shaping is sound. The smoke flake is a harness race, while `Journal.Verify` also has a separate live-read consistency defect.\n\n## Definition of done\n\n### COS A: deterministic smoke verification\n\n- Stop the entire `commerce` service after the Go smoke suite completes.\n- Keep it stopped for the full interval covering `scripts/smoke.sh:319-351`.\n- Preserve the existing PostgreSQL corruption and restore sequence, including `DELETE` instead of `TRUNCATE`.\n- The final `verify-journal` runs against a database with no commerce writers active.\n- No change is made to recovery predicates or production intervals.\n- Repeated smoke runs no longer produce a false `broken chain` failure.\n\n### COS B: diagnosable chain errors\n\n- A sequence error reports expected and observed sequence numbers.\n- A previous-hash link error reports expected and observed hashes.\n- The two errors have different messages without requiring database inspection.\n- A test fails against the current `broken chain` message before the implementation is changed.\n\n### COS C: consistent journal/head verification\n\n- `journal_entries` and `journal_heads` are read from one repeatable snapshot.\n- A live append between the two logical reads cannot produce a false `journal head mismatch`.\n- A test deterministically creates that interleaving.\n- The test fails against HEAD with `journal head mismatch`, never `broken chain`.\n- Existing durable head-corruption tests continue to report `journal head mismatch`.\n\n## Files to touch\n\n### `scripts/smoke.sh`\n\nAdd the commerce quiesce immediately after the Go smoke suite at line 317 and before the first packaged journal verification at line 319:\n\n```sh\ncompose stop --timeout 45 commerce\n```\n\nDo not restart commerce later in the script. The remaining access checks do not need it, and the EXIT trap will remove the stopped stack.\n\nDo not add, move, rename, or reflow any `JOURNAL_*` assignment. This preserves `TestSmokeJournalKeyLiteralsMatchScript` and the mixed-key fixture ordering.\n\n### `services/payments/internal/store/store.go`\n\nRefactor `Verify` to:\n\n1. Begin a read-only `REPEATABLE READ` transaction.\n2. Run both journal queries through that transaction.\n3. Close the entry rows before issuing the head query.\n4. Roll back on errors and commit the read-only transaction on success.\n5. Split the current compound condition into separate checks.\n\nThe diagnostic shape should be equivalent to:\n\n```text\njournal sequence gap organizer=<id> expected_sequence=<n> observed_sequence=<n>\njournal previous hash mismatch organizer=<id> sequence=<n> expected_previous_hash=<hex> observed_previous_hash=<hex>\n```\n\nKeep the existing `invalid hash`, signature, unknown-key, missing-head, and durable head-mismatch checks distinct.\n\nThe transaction fixes only the second defect. It must not be presented as a fix for the smoke corruption race. A consistent snapshot correctly reports a row that the harness permanently removed.\n\nA small private verifier seam may be added so the test can run an append exactly between the entry scan and head scan without introducing a public API or dependency. The normal `Verify` path passes no callback and retains the production flow.\n\n### `services/payments/internal/store/journal_smoke_test.go`\n\nAdd:\n\n- A diagnostic test covering a sequence gap and a previous-hash mismatch as separate mutations.\n- A cross-statement snapshot test for the journal/head race.\n- Any narrowly scoped helper needed to mutate and restore a row or invoke the private verifier seam.\n\nKeep `TestJournalVerifyRejectsHeadEntryDesync` as the direct durable-corruption test. It proves a deliberately inconsistent head still fails with `journal head mismatch`.\n\n## Test plan and seams\n\n### B. Diagnostic test\n\nUse the existing real PostgreSQL smoke database and `fullRing`.\n\n1. Append two valid facts for one organizer.\n2. Verify the clean fixture first.\n3. Disable user triggers temporarily.\n4. Change the second entry’s sequence from `n+1` to `n+2`, leaving its previous hash unchanged.\n5. Run verification.\n6. Assert the error names the expected and observed sequence values.\n7. Restore the sequence.\n8. Change only the second entry’s `previous_hash`.\n9. Run verification again.\n10. Assert the error names the expected and observed hashes.\n11. Restore the row and verify the journal is clean.\n\nThe sequence mutation reaches the sequence predicate while the previous hash still matches. The hash-link mutation reaches the previous-hash predicate while the sequence remains correct. Each case therefore proves one branch rather than relying on a fixture that trips both.\n\nAgainst HEAD, both subtests receive:\n\n```text\nbroken chain organizer=... sequence=...\n```\n\nThey fail because that message contains neither the required diagnosis nor expected/observed values.\n\n### C. Snapshot consistency test\n\nUse the real PostgreSQL database and a private helper shaped like:\n\n```go\nfunc (j *Journal) verify(ctx context.Context, afterEntries func()) error\n```\n\nThe public `Verify` calls it with no callback.\n\nThe test should:\n\n1. Append one valid fact for a fresh organizer.\n2. Invoke the private verifier with a callback that appends a second fact for the same organizer.\n3. Ensure the entry rows have been closed before the callback runs and allow more than one database connection.\n4. Let verification continue to the head query.\n5. Assert that the result is clean after the implementation.\n\nAgainst HEAD:\n\n- The first query sees only the first entry.\n- The callback commits the second entry and advances the head.\n- The second query sees the new head.\n- The in-memory entry map still ends at the first sequence.\n- HEAD returns `journal head mismatch`.\n\nIt cannot return `broken chain`, because the first query sees a complete chain and the second query does not scan entries at all. The append itself is valid and commits the entry and head together.\n\nAfter changing `Verify` to one repeatable-read transaction, both reads use the original snapshot, so the second entry and the advanced head are both invisible to that verification. The test passes without masking a real database inconsistency.\n\nThe existing direct mutation test remains the separate control:\n\n- Rewrite only `journal_heads.last_hash`.\n- Advance only `journal_heads.last_sequence`.\n- Delete a head row.\n- Assert the appropriate durable mismatch or missing-head error.\n\n### Red-first sequence\n\n1. Add the B test and run it against current code. It must fail on `broken chain`.\n2. Add the private C seam without changing snapshot semantics and run the C test. It must fail on `journal head mismatch`.\n3. Implement the diagnostic split.\n4. Implement the repeatable-read transaction.\n5. Re-run the focused payments store tests.\n6. Run the full gate only after all tracked edits are complete.\n\nNo browser test is needed. No generated code should change.\n\n## Commerce quiesce options\n\n### Recommended: graceful `compose stop`\n\nUse:\n\n```sh\ncompose stop --timeout 45 commerce\n```\n\nThis stops the HTTP server and all commerce workers:\n\n- recovery\n- bulk refund\n- reversal\n- exchange sweep\n- outbox and mail drainers\n\nCommerce’s shutdown path first drains HTTP handlers, then stops workers. The longer timeout covers the service’s ten-second HTTP shutdown and worker grace periods.\n\nThis is preferable because it prevents new commerce work and gives already accepted commerce requests a chance to finish. It still cannot undo a request that has already reached payments. The payments request may complete after the commerce client has disconnected, so the backup must be created only after the commerce container has fully stopped.\n\nIf the stop command fails, `set -e` aborts the smoke stage and the existing EXIT trap tears down the isolated stack. That is the correct failure mode. Continuing into corruption tests after an unsuccessful quiesce would make the gate’s result untrustworthy.\n\n### `compose pause` / `unpause`\n\nDo not use this.\n\nPause freezes workers and handlers in place. It does not drain accepted HTTP requests, and a payment request already received by payments can continue independently. It also leaves the process suspended rather than shut down cleanly.\n\n### Scaling commerce to zero\n\nDo not use this as the primary mechanism.\n\nScaling to zero is less explicit about graceful shutdown and can remove the container before in-flight work has drained. Restarting it later is also unsafe near the restore block because `Runner.Run` calls `RunOnce` before the first ticker. A restart can append immediately.\n\nThere is no reason to restart commerce in this script. If a future change requires that, it must happen only after line 351 and after the final journal verification.\n\n## Risks and pre-mortem\n\nIf the flake returns after this change, likely causes are:\n\n- A new payments writer was missed. The current repository audit shows commerce owns the background paths that append to payments, including recovery, bulk refunds, reversals, exchange-related flows, and commerce HTTP handlers. Payments itself has no comparable periodic writer. Direct callers of the payments internal API would still bypass the commerce stop, so any future service or test that calls `/internal/facts`, PSP compensation endpoints, or charge endpoints must be included in the writer inventory.\n- The quiesce was placed too late. It must precede line 319. Putting it before only the corruption statements at lines 338-350 leaves the initial verification exposed and does not protect the whole restore sequence.\n- Commerce was restarted before line 351. Its immediate `RunOnce` could append while the backup is being restored.\n- `compose stop` timed out and forcibly terminated commerce while a request was already accepted by payments. The script should retain the 45-second grace period and treat stop failure as a hard smoke failure.\n- A future script edit adds another `psql_payments` statement outside the quiesced interval. The protected range should remain visibly bounded from the first canonical verification through the final restored verification.\n- The C test fixture stops distinguishing the two snapshots. The test must append after the entry scan and before the head scan, with the rows closed and a second database connection available.\n- A diagnostic test mutates both predicates at once. Each B subcase must change only the field needed for its claimed error, then restore it before the next case.\n- The verifier transaction is added only around one query, or uses `READ COMMITTED`. Both statements must run through the same `REPEATABLE READ` transaction.\n\n## Assessment of the shaping\n\nThe main conclusions are supported.\n\nOne nuance is worth recording. The permanent gap is not caused by an append transaction producing a bad sequence. It comes from separate corruption and restore transactions interleaving with a valid append. The fact that `journal_heads` is restored at line 350 does not eliminate the race, because an append can insert an entry or advance the head across the restore’s transaction boundaries. The harness can therefore leave an entry/head combination that the writer itself never created incorrectly.\n\nThe claim that commerce is the live writer is accurate for the smoke stack’s background activity. It should be read as “the only known live process with these writers,” not as protection against a new direct caller of the payments API.\n\nThe repeatable-read change is required for COS C, but it is not a solution for COS A. These remain separate fixes and separate tests.\n\nCodex session ID: 01a01c5b-0f3e-7e93-b488-a1a11707b11c\nResume in Codex: codex resume 01a01c5b-0f3e-7e93-b488-a1a11707b11c\n"
    },
    {
      "stage": "plan-review",
      "kind": "plan",
      "body": "<!-- sdlc:stage=plan-review kind=plan -->\n### Plan critique — by `main-agent`, per `config.models.planReview` (cross-model: the draft is `gpt-5.6-luna`)\n\nAdversarial pass plus a pre-mortem. **Every file, symbol and seam the draft names was verified to\nexist at HEAD** — the drafter could not run the gate, so seams were checked with `git grep` rather\nthan trusted: `journalDB:69`, `fullRing:109`, `exec:352`, `assertVerifyFails:401`,\n`TestJournalVerifyRejectsHeadEntryDesync:364`, `installDelayTrigger:641`, `store.go:244/262/306/347`,\n`scripts/smoke.sh:317/319/325/338/342/346/350/351`. All present, all as described.\n\n**Verdict: ACCEPT the draft's structure and its two headline calls, with four amendments — one of\nwhich reverses a specific instruction — plus one carve-out and one finding the draft missed.**\n\n---\n\n#### ACCEPTED, and independently verified rather than waved through\n\n**A1 — `compose stop commerce`, no restart.** Accepted, and the two load-bearing facts under it were\nchecked rather than assumed.\n\n*The draft asserted commerce's shutdown drains HTTP before stopping workers.* **Verified true** —\n`services/commerce/cmd/commerce/main.go:446-457`: on `ctx.Done()` it runs `srv.Shutdown(sctx)` with a\n10s timeout and only then `stopWorkers()`; `start()` at `:465-476` gives each worker a 5s grace. So\nthe shutdown is bounded well inside `--timeout 45`, and the draft's insistence that the backup be\ntaken only after `stop` **returns** is correct and necessary: a worker mid-`RunOnce` keeps appending\nduring its grace window.\n\n*The draft asserted nothing after line 319 needs commerce.* **Verified true, and this is the fact\nthat makes \"never restart\" safe rather than merely convenient** — `awk 'NR>=319' scripts/smoke.sh |\ngrep -i commerce` returns **zero matches** across the remaining 181 lines. Not restarting is also the\nmore reversible option (no restart-triggers-`RunOnce` hazard at all), which is the tie-breaker this\nrun is required to apply.\n\n**A2 — the transaction is NOT the fix for the flake.** Accepted and emphasised. The draft states it\nthree times; keep every one of them. This is the single most likely thing for a later reader to get\nbackwards, and getting it backwards means shipping a change that leaves the flake live while looking\nlike a fix.\n\n**A3 — one predicate per test case (COS B).** Accepted. Mutating `sequence` while leaving\n`previous_hash` valid, then the reverse, is exactly the discipline AGENTS.md requires for a guard\nwith N predicates: an earlier refusal short-circuits the rest, so a fixture that trips both proves\none branch and is silent about the other.\n\n---\n\n#### AMENDMENT 1 — REVERSES a draft instruction: `compose stop --timeout 45` will not parse\n\nThe draft writes:\n\n```sh\ncompose stop --timeout 45 commerce\n```\n\n`docker compose stop` takes `-t` / `--timeout`, but the repo's `compose()` wrapper\n(`scripts/smoke.sh:35`) interposes `-p \"$PROJECT\"` and the `-f` list, and flag placement after the\nsubcommand is the fragile form. **Verify the exact invocation against the installed Compose version\nbefore committing it, and if there is any doubt use the unambiguous form:**\n\n```sh\ncompose stop -t 45 commerce\n```\n\nThis matters more than a style nit because of where it sits: under `set -euo pipefail` a\nmis-parsed flag aborts the smoke stage with a message about argument parsing, three hundred lines\naway from anything to do with journals — the TKT-71 shape, where a gate that never ran looks like a\ngate that failed. **Implementation must run the command once against a real stack and read its exit\ncode before relying on it.**\n\n#### AMENDMENT 2 — the draft's COS C seam is right, but its stated red-first evidence needs one more guard\n\nThe private-seam idea (`func (j *Journal) verify(ctx, afterEntries func()) error`, public `Verify`\npassing `nil`) is sound and is the minimum-surface way to force the interleaving. But the draft's\nclaim that HEAD returns `journal head mismatch` depends on a fact it never checked:\n\n**`Verify` scans the WHOLE table across ALL organizers** — this is explicit and load-bearing at\n`journal_smoke_test.go:96-108`, which records that total verification is the only reason these tests\nare order-independent. So the C test's callback append is evaluated against a `journal_heads` scan\nthat also covers every other test's organizers.\n\nThat does not break the test, but it does mean **`assertVerifyFails(..., \"journal head mismatch\")` is\nnot sufficient evidence on its own**: the same message is produced by three existing subtests in\n`TestJournalVerifyRejectsHeadEntryDesync` and by any unrelated desync. **Amend the C test to assert\nthe message names the C test's own organizer id**, not merely the message class. Without that, the\ntest passes for reasons that have nothing to do with the snapshot, which is precisely the\n\"green test that cannot reach the failing state\" failure mode.\n\n#### AMENDMENT 3 — make the transaction's read-only intent explicit, and note it is a NEW idiom\n\n`git grep BeginTx` shows **every existing call in payments and access passes `nil` options** — there\nis no `REPEATABLE READ` anywhere in this codebase today. This change introduces the idiom. Use:\n\n```go\ntx, err := j.db.BeginTx(ctx, &sql.TxOptions{Isolation: sql.LevelRepeatableRead, ReadOnly: true})\n```\n\n`ReadOnly: true` is not decoration: it makes \"this transaction exists to take one snapshot, not to\nwrite\" enforced by PostgreSQL rather than asserted in a comment, and it is the difference between a\nfuture edit adding a write here failing loudly and failing silently. Being the first such call in the\nrepo, it needs a comment saying *why* the isolation is raised — a bare `LevelRepeatableRead` reads as\ncargo-culting to the next person.\n\n#### AMENDMENT 4 — say, in the code, which defect each change closes\n\nThe two changes land in the same function and close different defects; the draft understands this but\nthe *code* will not. Require a comment at the transaction naming it as the fix for the two-snapshot\nfalse alarm **and explicitly disclaiming the smoke flake**. This is ADR-021's naming discipline\napplied to a diff: a reader six months out who sees a `REPEATABLE READ` next to a ticket titled\n\"flaky broken chain\" will conclude the transaction fixed the flake, and will then delete the smoke\nquiesce as redundant.\n\n---\n\n#### CARVE-OUT — an identical defect 30 lines below, deliberately NOT fixed here\n\nFound during this pass, not in the draft or the shaping: **`scripts/smoke.sh:352-410`, the access\nlifecycle block, has the same durable-corruption exposure and is not closed by this ticket.**\n\n- Same pattern: backup (`:373-375`), `DISABLE TRIGGER`, corrupt, restore by\n  `TRUNCATE`/`INSERT ... SELECT` (`:381`, `:384`, `:390`) — and restores happen across separate\n  `psql -c` transactions (`:363`), exactly as in the payments block.\n- Same live writer shape: `services/access/internal/lifecyclejob/runner.go:87-98` runs a\n  `Checkpointer` ticker, mounted for real at `services/access/cmd/access/main.go:280`, writing\n  `lifecycle_checkpoints` — **one of the tables that block backs up and restores** — every\n  `ACCESS_LIFECYCLE_CHECKPOINT_INTERVAL`, default **60s** (`compose.yaml:382`). `AlarmDrainer`\n  (`runner.go:249`) is a second ticker.\n\n**It is out of scope, and the reason is structural rather than a scoping preference: the same remedy\ncannot be applied.** The payments block can stop its writer because commerce is a *different*\ncontainer from the one being exec'd into. The access block runs `compose exec -T access\n/app verify-lifecycle` — **access is both the writer and the container under test**, so stopping it\nmakes the block unrunnable. That needs a different mechanism and its own design, which is a ticket,\nnot an amendment.\n\nIts window is also ~30× narrower (60s tick vs commerce's 2s over a comparable block), which is\nconsistent with this having surfaced on payments first and not yet on access. **Consistent with, not\nevidence for** — nobody has looked for a lifecycle flake, and absence of a report is not absence of\nthe defect. File it as an incidental ticket at `agent:ai-review` triage.\n\n---\n\n#### PRE-MORTEM — it shipped, the flake came back\n\nThe draft's list is good. Three additions:\n\n1. **The quiesce landed after `verify-concurrent-append` (`:320`) instead of before `:319`.** Both\n   verifies are bare invocations under `set -e`, so `:321` is exposed too — and `:321` is *more*\n   likely to be the one that fired historically, since it runs seconds after a Go suite that leaves\n   claimable stuck orders by design. Place the stop **before `:319`**, as the draft says, and treat\n   the protected range as `:319`–`:351` inclusive.\n2. **Someone added a `psql_payments` statement after `:351`.** The protected range must be visibly\n   bounded by a comment saying commerce is stopped and why, or the next editor extends the block past\n   the protection without noticing there was any.\n3. **The stop succeeded but a payments request was already in flight.** `compose stop` cannot recall a\n   request payments has already accepted; the draft says this and is right. The mitigation is the\n   ordering it already specifies — backup only after `stop` returns — and there is no way to make it\n   airtight from the script. **Say so in the closeout rather than claiming determinism absolutely:**\n   the honest claim is \"the writer is stopped before the window opens\", not \"no write can occur\".\n\n---\n\n#### On the drafter's own pushback\n\nIt challenged the shaping's framing of the permanent gap, arguing the corruption comes from\n\"separate corruption and restore transactions interleaving with a valid append\" rather than from an\nappend producing a bad sequence. **It is right, and shaping said the same thing** — no correct write\ncan gap a sequence; the harness creates the gap. Recording the agreement because it is the claim most\nlikely to be paraphrased into something false later.\n\nIt also narrowed \"commerce is the live writer\" to \"the only *known* live process with these writers,\nnot protection against a new direct caller of the payments API\". **Accepted as stated** and folded\ninto Amendment 4's documentation requirement.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan final — DELTA on the `kind=plan` draft. Gate 2 taken by the agent (`config.gates: autonomous`).\n\n**The `kind=plan` draft as amended below is the plan.** Not restated — resume reads draft + this delta\ntogether.\n\n#### Amendments carried from the critique\n\n- **D1 — `compose stop -t 45 commerce`, placed immediately before `scripts/smoke.sh:319`.** Resolved\n  against the installed Compose: `docker compose stop --help` lists **`-t, --timeout int`** only, so\n  the draft's `--timeout 45` spelling is not the documented one. Use `-t 45`. **Still run it once\n  against a real stack and read the exit code** before relying on it — under `set -euo pipefail` a\n  mis-parsed flag aborts the stage with an argument-parsing error far from anything journal-related.\n- **D2 — the COS C test asserts the failing organizer id, not just the message class.** `Verify`\n  scans all organizers (`journal_smoke_test.go:96-108`), and three existing subtests already produce\n  `journal head mismatch`. Asserting only the substring would let the test pass for an unrelated\n  desync.\n- **D3 — `BeginTx(ctx, &sql.TxOptions{Isolation: sql.LevelRepeatableRead, ReadOnly: true})`**, with a\n  comment saying why the isolation is raised. First `REPEATABLE READ` in the repo — every existing\n  `BeginTx` passes `nil`.\n- **D4 — each change carries a comment naming the defect it closes, and the transaction explicitly\n  disclaims the smoke flake.** Otherwise a later reader deletes the smoke quiesce as redundant.\n- **D5 — no restart of commerce.** Verified: zero references to commerce in `scripts/smoke.sh` after\n  line 319. Also the more reversible option.\n\n#### Decisions self-made at Gate 2, with rationale (autonomous mode)\n\n1. **Quiesce by stopping commerce, not by snapshotting the read.** The critique's recommendation, and\n   the only option that removes the cause: the corruption is durable, in the table.\n2. **`stop` over `pause` over `scale 0`.** `stop` is the only one that drains accepted HTTP requests\n   before cancelling workers (verified: `commerce/main.go:446-457`). Where the critique made no\n   recommendation the rule is most-reversible; here the critique did recommend, and it agrees.\n3. **Fix the two-snapshot defect (COS C) in this ticket rather than carving it out.** It is four lines\n   from the change, cheap, and genuinely a live-verification false-alarm source that ADR-003's\n   gate placement argues against. Carved out it would likely never be done. **Constraint honoured:\n   its own test, its own message, no shared narrative with the flake.**\n4. **The access lifecycle block is carved OUT, as an incidental ticket.** Structural reason, not\n   preference: access is both the writer and the container exec'd into, so the same remedy cannot\n   apply. Filed at ai-review triage.\n5. **`risk:low` remains NOT claimed.** Unchanged from the claim comment.\n\n#### Introduced at plan-review, reviewed by no second model\n\nPer SKILL.md this must be named rather than left implicit. Three items reached this point authored by\nthe critique alone:\n\n- **D2** (assert the organizer id in the C test) — added by the critique; the draft asserted only the\n  message class. Mitigated by being a *strengthening* of an assertion: if it is wrong, the test is\n  merely stricter than needed.\n- **D3's `ReadOnly: true`** — the draft asked only for `REPEATABLE READ`. Low risk: PostgreSQL\n  enforces it, and a violation fails loudly at the first write.\n- **The access-block carve-out** — found in this pass, in neither the shaping nor the draft. It\n  changes no code in this ticket; its only output is a backlog ticket, so the residual risk is that\n  the ticket is wrong, not that this diff is.\n\nThese three get **explicit attention at ai-review**: the reviewer is told the C test's assertion and\nthe transaction options were authored by the plan-reviewer, so it attacks them as unreviewed rather\nthan as settled.\n\n#### Definition of done (unchanged from the draft, restated only as the acceptance checklist)\n\n- **A** — `scripts/smoke.sh` stops commerce before `:319`; the protected range `:319`–`:351` is\n  bounded by a comment; no restart; `TestSmokeJournalKeyLiteralsMatchScript` still green (no\n  `JOURNAL_*` assignment added, moved, renamed or reflowed).\n- **B** — the sequence-gap and previous-hash branches report distinct messages naming expected vs\n  observed; one test per predicate, each mutating only its own field; both observed **red** against\n  HEAD's single `broken chain`.\n- **C** — one `REPEATABLE READ` read-only transaction spans both scans; a test forces an append\n  between them via a private seam, observed **red** against HEAD with `journal head mismatch` **naming\n  its own organizer**; `TestJournalVerifyRejectsHeadEntryDesync` stays green as the durable-corruption\n  control.\n- **Gate** — `make check` green. `make browser` not required (no write form touched).\n- **Honesty constraint on the closeout** — the flake is timing-dependent and cannot be replayed on\n  demand. The claim is \"the writer is stopped before the window opens\", **not** \"no write can occur\":\n  `compose stop` cannot recall a request payments has already accepted. Say that plainly rather than\n  claiming absolute determinism.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation — by `main-agent`, per `config.models.implement`\n\nPR **#240** · branch `TKT-254-journal-verify-determinism` · commit `419cdb6b`\n\n```yaml\nstage: coding\nimplementer: main-agent\ngate: make check\ngate_runs: 1 (green on the first run)\ngate_verdict: \"EXIT=0 from the sentinel; log body grepped for FAIL / Error N / smoke: / drifted — none\"\ndiff: 3 files, +253/-4\nbrowser: not required (no write form touched)\ntests_red_first: yes, both, for the predicted reasons\nmutations_checked: 4\nmutations_caught: 4\n```\n\n#### What landed, and which COS each closes\n\n**A — determinism (`scripts/smoke.sh`).** `compose stop -t 45 commerce` immediately before line 319,\nnever restarted, with a comment block that states the mechanism, why `stop` rather than `pause` or\n`scale 0`, why there is no restart, and the exact limit of the claim.\n\n*Amendment D1 discharged by execution, not argument.* The critique flagged that the draft's\n`--timeout 45` spelling might not parse. `docker compose stop --help` lists only `-t, --timeout int`,\nand the invocation was dry-run against the real four-file `-f` set the wrapper uses\n(`stop -t 45 --dry-run commerce`) → exit 0, before being committed.\n\n**B — diagnosability (`store.go:357-369`).** The short-circuiting disjunction became two branches:\n\n```\njournal sequence gap organizer=%s expected_sequence=%d observed_sequence=%d\njournal previous hash mismatch organizer=%s sequence=%d expected_previous_hash=%s observed_previous_hash=%s\n```\n\n**C — the separate two-snapshot defect (`store.go:306-333, 389-395`).** Both scans now run inside one\n`BeginTx(ctx, &sql.TxOptions{Isolation: sql.LevelRepeatableRead, ReadOnly: true})`. Per D3 this is the\nfirst `REPEATABLE READ` in the repo (every other `BeginTx` passes `nil`), so the comment says why the\nisolation is raised; `ReadOnly` per the plan-reviewer's amendment.\n\nPer **D4**, both changes carry comments naming the defect they close **and disclaiming the other** —\nthe transaction explicitly says it is not the flake's fix, and points at the smoke quiesce. Without\nthat, the next reader deletes one as redundant.\n\n#### The seam for C\n\n`Verify` delegates to a private `verify(ctx, afterEntries func())`; production passes `nil`. The\ncallback runs after the entry scan is consumed **and explicitly closed**, before the head scan. A\nhand-written two-query test would have proved only that the test's own queries are two queries; this\nforces the interleaving through the real code path.\n\n#### Red first — observed, not assumed\n\n```\n--- FAIL: TestJournalVerifyNamesWhichChainCheckFailed/a_sequence_gap_...\n    error \"broken chain organizer=c43c7c3c-… sequence=3\" does not diagnose a sequence gap\n--- FAIL: TestJournalVerifyNamesWhichChainCheckFailed/a_rewritten_link_...\n    error \"broken chain organizer=c43c7c3c-… sequence=2\" does not diagnose a previous-hash mismatch\n--- FAIL: TestJournalVerifyReadsEntriesAndHeadsFromOneSnapshot\n    a legal append between the two reads was reported as corruption: journal head mismatch organizer=c2ec6ccb-…\n```\n\nThe C test failed with **`journal head mismatch`, never `broken chain`** — the plan's stated\nprediction, and the thing that keeps the two defects from being conflated. It also passed its own\n`appended.Sequence != 2` guard before failing on the verdict, so the interleaving demonstrably\nhappened rather than the callback quietly doing nothing.\n\n#### Mutation testing — 4 run, 4 caught\n\n| # | mutation | caught by | why it matters |\n|---|---|---|---|\n| 1 | head scan reverted to `j.db.QueryContext` (outside the tx) | C test | the shared transaction is load-bearing |\n| 2 | `LevelRepeatableRead` → `LevelReadCommitted` | C test | the **isolation level** is load-bearing, not merely \"one transaction\" |\n| 3 | expected/observed swapped in the sequence message | B, gap subtest | positions are distinguishable (2 vs 3), so a swap is observable |\n| 4 | expected/observed swapped in the hash message | B, link subtest | same, on hex values |\n\nMutation 2 is the one worth naming: without it, \"we wrapped it in a transaction\" would have passed\nwhile a `READ COMMITTED` transaction re-took a snapshot per statement and left the defect live.\n\n#### Governing tests — checked, not assumed\n\n`TestSmokeJournalKeyLiteralsMatchScript` **reads `scripts/smoke.sh`** and was the mémo's top hazard for\nthis ticket. Run directly after the edit: green. No `JOURNAL_*` assignment was added, moved, renamed\nor reflowed. Also grepped the tree for surviving dependencies on the old `broken chain` string —\nevery remaining hit is prose, or belongs to the **access** lifecycle verifier, which is a different\nfunction this diff does not touch.\n\n#### COS A demonstrated on a real run\n\n`make check` green, and the gate log shows commerce stopping **before** the verify block, with\neverything downstream completing without it:\n\n```\n1289  Container ticketing-smoke-0-commerce-1 Stopping\n1290  Container ticketing-smoke-0-commerce-1 Stopped\n```\n\nboth `verify-journal` invocations, the corruption matrix, the access lifecycle block and\n`reconcile-pins` all ran after that point and passed.\n\n#### Honesty note, carried forward to the closeout deliberately\n\nThe flake is timing-dependent; the identical commit passed on re-run. **\"Demonstrated against a run\nthat previously failed\" was never available and is not being claimed.** What is claimed: a\ndeterministic test per defect, four mutations, and one green gate in which the quiesce is visible in\nthe log. The residual is a request payments accepted before `stop` returned — narrowed by taking the\nbackup only after the stop, not eliminated.\n\n#### Filed, not fixed\n\n**TKT-261** — the access lifecycle block has the identical durable-corruption exposure\n(`Checkpointer` writes `lifecycle_checkpoints`, a table that block backs up and restores). Carved out\nbecause access is both the writer and the container `compose exec`'d into, so `compose stop` cannot\nbe the remedy there. Its description names three candidate approaches, one of which (make the restore\natomic in a single transaction) would generalize back here and is worth checking first.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review — by `gpt-5.6-luna` (codex), per `config.models.aiReview`\n\n**Three passes.** Findings **2 → 1 → 2**. Every pass found a defect in the *previous pass's fix*,\nat the root — so the churn cap reset twice under SKILL.md's convergence rule rather than being\nexceeded. It stops here: the third pass broke the strictly-decreasing condition, so pass 4 is not\njustified and the remaining residual is recorded rather than chased.\n\nCross-model invariant held: `implement: main-agent`, `aiReview: gpt-5.6-luna`.\n\n**The thing worth reading first:** every finding was about *a check that could not refuse* — a test\nthat could not fail, a barrier that could not observe, a predicate that discarded its own NULL case,\nand a comment that asserted a bound that did not exist. Not one was about the fix's logic. That is\nthe same shape as the ticket itself, which was a verifier crying wolf.\n\n---\n\n#### Pass 1 — 2 findings, both accepted\n\n**[medium] The snapshot test could not fail.** It took a bare `afterEntries func()` seam and asserted\nthe interleaved append *happened* — constraining where the callback ran, not what the verifier read.\nThe reviewer claimed a coordinated reversion (transaction removed **and** callback moved above the\nentry scan) would leave it green.\n\n**Verdict: CONFIRMED by running it, not by agreeing with it.** The reversion was applied and the test\npassed with the defect live. This is `docs/learnings/2026-08-10-a-green-test-that-cannot-reach-the-failing-state.md`\none level up: not a fixture that cannot reach the state, but an *assertion about the harness* standing\nin for an assertion about the contract.\n\n*Fix:* the seam became a **probe reporting what each scan observed** — the entry scan's highest\nsequence per organizer, the head scan's `last_sequence` — and the test asserts they **agree**. A shared\nsnapshot makes the head scan blind to an append committing after the entry scan; two snapshots make\nthem differ by exactly that append. No callback placement can manufacture agreement out of two\nsnapshots, because the divergence comes from the commit, not the call. Both mutations now fail on the\ncontract assertion naming the divergence: *\"entries saw sequence 1, heads saw 2 — they did not read one\nsnapshot\"*.\n\n**[medium] `compose stop commerce` does not prove payments drained.** Correct: it closes the door on new\ncalls but cannot recall one payments already accepted. The original comment *admitted* this as a\nresidual — and admitting is not closing.\n\n*Fix:* a payments-side barrier polling `pg_stat_activity` until no busy backend remains, failing loudly\nafter 30s rather than opening the window anyway.\n\n#### Pass 2 — 1 finding, accepted; the probe fix cleared\n\nConfirmed the probe fix: `afterHeads` cannot run twice, the test is organizer-isolated, the second\nconnection cannot deadlock against an MVCC read.\n\n**[medium] The barrier's predicate discarded its own failure case.** `state <> 'idle'` reads as\n\"anything not idle\", but `state` is nullable and `NULL <> 'idle'` is **NULL**, which `WHERE` drops. An\nunknown-state backend was counted as *drained* by a guard whose entire purpose is to fail closed.\n\n**Verdict: CONFIRMED by execution** against the same PostgreSQL version — `SELECT (NULL <> 'idle') IS\nNULL` → true; `SELECT (NULL IS DISTINCT FROM 'idle')` → true. Fixed with `IS DISTINCT FROM`.\n\n*Also raised and DECLINED:* quiesce payments itself and convert the four verifier calls to one-off\ncontainers. Recorded in `overrides:` at closeout with reasoning.\n\n**Unprompted improvement, not a finding:** `afterHeads` had two call sites with a `return` between\nthem — correct once traced, but a later added return would silently drop the report. Collapsed to one\n`defer`. Behaviour re-verified against all three mutations.\n\n#### Pass 3 — 2 findings, both accepted; one refutes a claim the *agent* wrote\n\n**[high] The stated bound was false.** The comment claimed commerce's shutdown bounded the residual\nbecause it \"drains HTTP for up to 10s BEFORE cancelling workers\".\n\n**Verdict: CONFIRMED, and this one is the agent's own error.** `srv.Shutdown` drains commerce's\n**inbound** handlers; with no inbound traffic it returns *at once*, not after 10s. Workers run on\nindependent contexts cancelled only afterwards — and cancelling a worker aborts the **client** side of\na request payments already accepted, not the server handler. The ordering bounds inbound drain, not\nthe outbound call that appends.\n\n*Fix:* the claim is **retracted in the code**, with the retraction written out so nobody re-derives it,\nand replaced by what is actually true: commerce is the only caller (four call sites, all under\n`services/commerce`; the one smoke test POSTing `/api/payments/internal/facts` is refused at the\ngateway edge and never reaches payments — verified); `stopWorkers` blocks up to 5s per worker, so none\nis still starting requests once `stop` returns; the poll then requires no busy client backend. The\nuncovered case is named exactly: *a request accepted in the instant before its producer was cancelled,\nstill ahead of `BeginTx` when the poll samples zero.*\n\nA false reassurance in a comment is worse than an admitted gap — it is the sentence that stops the next\nperson looking.\n\n**[medium] The predicate counted processes that cannot append.** `datname='payments'` already excludes\nmost server processes — checkpointer, walwriter, background writer, io workers and both launchers all\nreport `datname` NULL (**verified** against the same PostgreSQL version). An **autovacuum worker** does\nnot: it carries the database it is vacuuming and a non-idle state. It cannot write a journal fact, has\nno completion bound, and a wraparound vacuum resists interruption — so it could hold the loop past its\ntimeout and **fail the whole smoke stage for unrelated maintenance**. That is a red gate with no defect\nbehind it: worse than the flake being replaced.\n\n*Fix:* `backend_type='client backend'`.\n\n---\n\n#### Why this stopped at three passes\n\nSKILL.md permits exceeding two only while **both** conditions hold: every pass invalidated the previous\nfix at the root (true here — 1→2→3 each did), **and** findings strictly decrease (2 → 1 → 2: **false**).\nThe second condition broke, so the cap holds. The fourth-pass candidates are the two residuals below,\nand both are better served by being written down than by another round.\n\n#### Residuals, stated rather than closed\n\n1. **The pre-`BeginTx` race.** A request accepted by payments in the instant before its producer was\n   cancelled, still ahead of `BeginTx` when the poll samples zero. Closing it requires quiescing\n   payments, which cannot be done here — payments is the container the four `verify-journal` calls\n   `compose exec` into. **Unquantified**, and named in the script as the first thing to suspect if a\n   journal flake recurs.\n2. **The fix diff for pass 3 has had no adversarial pass.** It is one SQL clause plus comment\n   retractions, verified by the gate and by direct execution of both PostgreSQL claims — but it is\n   reviewed by its author only, and that is labelled as such rather than implied to be reviewed.\n\n#### Gate\n\n`make check` green on the frozen tree at each stage (`EXIT=0` from the sentinel; log body grepped for\n`FAIL` / `Error N` / `smoke:` / `make: ***`). The barrier ran without tripping its 30s timeout, and the\nlog shows commerce stopping before the verify block:\n\n```\n1281  Container ticketing-smoke-0-commerce-1 Stopping\n1282  Container ticketing-smoke-0-commerce-1 Stopped\n```\n\n**Process failure to record:** one gate run was started and then a tracked file was edited while it ran\n— the exact TKT-240 shape AGENTS.md warns about. The run was **killed rather than trusted**, the\norphaned compose stack torn down, and the gate restarted on a frozen tree. Cost: one full gate cycle.\nRetro item.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### PO validation — self-taken (Gate 4, `config.gates: autonomous`)\n\nMerged as `b879cce9` (squash of PR #240). **Content verified on `origin/main` rather than trusting\nthe merge badge** (the stacked-PR/squash trap): `backend_type='client backend'` ×2 in\n`scripts/smoke.sh`, `LevelRepeatableRead` ×1 and `verifyProbe` ×3 in `store.go`.\n\nEach COS against evidence on the merged code.\n\n---\n\n**COS 1 — \"Determine which of the two explanations is true, with evidence. Reproduce it, do not argue\nit. A `broken chain` alarm that turns out to be a harness artifact must be *shown* to be one.\"**\n\n**MET, with one honest qualification the ticket's own wording invites.**\n\nThe verdict is **(1), harness-only**, established as a closed mechanical chain read at HEAD, every\nlink a file and a line:\n\n- no correct write can gap a sequence — `store.go:262` takes `FOR UPDATE` on the head row and\n  commits entry + head in one transaction;\n- the gap is therefore created outside the write path — `scripts/smoke.sh` backs up\n  `journal_entries`, restores it with `DELETE` + `INSERT ... SELECT` seconds later, triggers disabled\n  for the whole block;\n- the stack is still serving (teardown is only the `EXIT` trap) and commerce's recovery ticker fires\n  every 2s (`compose.smoke.yaml:16`), appending via `/internal/facts` → `api/server.go:179` →\n  `journal.Append`;\n- it always has work — `smoke/psp_recovery_test.go` backdates orders past the claim grace period on\n  purpose.\n\n**The qualification, stated because the COS asked for reproduction and reproduction was not\navailable:** the flake is timing-dependent and the identical commit passed on re-run, so \"run the\nfailing case, apply the fix, watch it stop\" could not be performed. **This is not claimed.** What\nreplaced it: a deterministic test per defect, four mutations checked and caught, and a gate log in\nwhich the quiesce is visible. A PO who considers \"shown to be one\" to require a live reproduction\nshould read this COS as *partially* met and TKT-261 as where that capability would be built.\n\n**COS 2 — \"If (1): make the smoke stage's verification deterministic — quiesce writers, or verify\ninside a snapshot — so a green gate means the journal verified and a red one means something is\nwrong.\"**\n\n**MET.** `compose stop -t 45 commerce` before the verify block (never restarted — nothing after that\nline uses commerce, verified: zero references in the remaining 181 lines), plus a payments-side drain\nbarrier polling `pg_stat_activity` until no busy client backend remains, failing loudly at 30s rather\nthan opening the window anyway.\n\nEvidence, from the gate log rather than from argument:\n\n```\n1272  Container ticketing-smoke-0-commerce-1 Stopping\n1273  Container ticketing-smoke-0-commerce-1 Stopped\n```\n\nBoth `verify-journal` invocations, the corruption matrix, the access lifecycle block and\n`reconcile-pins` all ran after that point and passed. The barrier never tripped its timeout across\nthree full gate runs.\n\n*Note the ticket offered \"or verify inside a snapshot\" — that option was deliberately **not** taken as\nthe fix, because the corruption is durable and a consistent snapshot reports it correctly. The\nsnapshot change was made anyway, for a different defect. See COS 4.*\n\n**COS 3 — \"If (2): fix the read ordering, and pin it with a test that fails against the current\ncode.\"**\n\n**N/A by construction** — the branch resolved to (1). But the ordering weakness the COS anticipated\nturned out to exist *separately*: `Verify` read `journal_entries` and `journal_heads` as two\nstatements on a pooled `*sql.DB`, so a legal append between them reported `journal head mismatch`\nabout an intact journal. Fixed under one `REPEATABLE READ, ReadOnly` transaction, pinned by\n`TestJournalVerifyReadsEntriesAndHeadsFromOneSnapshot`, which **was observed red** against HEAD.\n\n**COS 4 — \"Either way, the sequence-gap branch should say enough to tell the two apart from the log\nalone — today it prints the offending sequence but not the previous one it expected.\"**\n\n**MET, and slightly exceeded.** The disjunction was split; both branches now name expected *and*\nobserved:\n\n```\njournal sequence gap organizer=%s expected_sequence=%d observed_sequence=%d\njournal previous hash mismatch organizer=%s sequence=%d expected_previous_hash=%s observed_previous_hash=%s\n```\n\nPinned by `TestJournalVerifyNamesWhichChainCheckFailed`, one subtest per predicate, each mutating only\nits own field — because `||` short-circuits and a fixture tripping both would prove one branch and be\nsilent about the other.\n\n**Quality gate — \"`make check` green, and the fix demonstrated against a run that previously\nfailed.\"**\n\nGreen (`EXIT=0`, log body grepped, not the harness status). The second half **is not claimed** — see\nCOS 1.\n\n**Non-goals respected.** Chain construction and hash discipline (ADR-016/ADR-021) untouched: the diff\nchanges what the verifier *reads and when*, and what it *says*, never what it computes. The\n`invalid hash`, signature, unknown-key, missing-head and head-mismatch messages are unchanged.\n\n---\n\n#### What a PO should be uneasy about\n\n1. **The pre-`BeginTx` residual.** A request accepted by payments in the instant before its producer\n   was cancelled, still ahead of `BeginTx` when the poll samples zero, is uncovered. **Unquantified.**\n   Named in the script as the first thing to suspect if the flake recurs.\n2. **Pass 3's fix diff had no adversarial pass** (churn cap). One SQL clause plus comment retractions,\n   gate-verified, author-reviewed only — labelled as such rather than implied reviewed.\n3. **One load-bearing fact was argued, not executed:** that an autovacuum worker carries `datname`.\n   The complementary fact (background processes carry NULL) *was* executed.\n4. **TKT-261 is open** and is the same defect class one block below, unfixed here for a structural\n   reason.\n\nRecommending acceptance on COS 2, 3 and 4 unambiguously, and on COS 1 with the reproduction caveat\nstated above rather than buried.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-254\n\nMerged `b879cce9` (squash of PR #240), content verified on `origin/main`. Branch deleted.\n\n#### Durations — from the `sdlc-state` commit log, not hand-tracked\n\n| stage | duration |\n|---|---|\n| Backlog (created 2026-08-18 22:57 → shaped 19:27) | ~20h 30m wall (queue time, not work) |\n| Backlog → Planning (shaping + claim) | 0m 23s |\n| `agent:planning` (draft by `gpt-5.6-luna`) | 12m 58s |\n| `agent:plan-review` (critique by `main-agent`) | 1m 15s |\n| Planning → Building (plan-final + Gate 2) | 0m 36s |\n| `agent:coding` (TDD + gate + PR) | 12m 07s |\n| `agent:ai-review` (3 passes + fixes + 4 gate runs) | 43m 41s |\n| Building → PO Review (Gate 3: guide, merge, verify) | n/a (folded into ai-review above) |\n| PO Review → Done | ~5m |\n| **human-wait total** | **n/a (autonomous)** |\n\nActive agent time ≈ **1h 16m**. The ai-review stage is 57% of it, and four of the six `make check`\nruns happened inside it — the gate, not the model, is the wall-clock cost here.\n\n#### Diff\n\n`+414 / −4` across **3 files**, 5 commits (squashed).\n\n#### overrides: — blocking objections the agent proceeded past\n\n**Two.** Both from `aiReview` (`gpt-5.6-luna`), both rated at least [medium], both recorded because a\nfixed finding and an overridden one look identical in a merged diff.\n\n**1. [medium→high, raised in passes 2 and 3] \"Quiesce payments itself; run the verifier commands in\none-off containers.\"**\n- *Reviewer's reason:* no database-level poll can see a handler payments has accepted that has not yet\n  reached `BeginTx`. It holds no backend, so a zero sample does not prove it is gone. The reviewer\n  said plainly: *\"Do not treat one zero `pg_stat_activity` sample as a drain guarantee.\"*\n- *Agent's reason for proceeding:* payments is the container the four `verify-journal` calls\n  `compose exec` into, so it cannot be stopped here. Converting them to one-off containers changes\n  **what they prove** — a fresh container is not the running service, which is the entire point of the\n  ADR-016 §D8 multi-key check. That is a redesign, and TKT-261 is the ticket where this class of\n  problem (writer and verifier in the same container) is being taken up.\n- *Residual risk carried by:* **TKT-261**, and by the named gap in `scripts/smoke.sh` itself.\n- *Honest bound:* **unquantified.** The producer is removed and the poll adds a second condition, but\n  the uncovered instant is real and is not measured.\n\n**2. [medium, pass 3] \"Add a smoke regression that holds an accepted payments request before\n`BeginTx`, lets the poll observe zero, then releases it.\"**\n- *Reviewer's reason:* without it, the barrier's adequacy is asserted rather than demonstrated.\n- *Agent's reason for proceeding:* the test requires a fault-injection seam in the payments *request\n  path* (a delay between accept and `BeginTx`) that does not exist and would be production code\n  serving only this test. Building it is TKT-261-shaped work. The churn cap had also been reached —\n  findings went 2 → 1 → 2, so the strictly-decreasing condition for a fourth pass failed.\n- *Residual risk carried by:* **TKT-261**.\n\n**Also not reviewed, and not an override but a gap a reader deserves:** the pass-3 fix diff\n(`01de2142`) had **no adversarial pass** — the cap stopped the run. One SQL clause plus comment\nretractions, gate-verified and author-reviewed only. Stated in the review-guide and the PR.\n\n#### learnings: — three, one already promotable\n\n**1. `docs/learnings/` — \"a green test can pin the harness instead of the contract.\"** The snapshot\ntest asserted *the callback ran*, which is a fact about the test harness, and stayed green under a\ncoordinated reversion that restored the defect. The existing learning\n`2026-08-10-a-green-test-that-cannot-reach-the-failing-state.md` covers fixtures that cannot reach a\nstate; this is one level up — an assertion about the *harness* standing in for one about the\n*contract*, where the fix is to assert what the code **observed**, not what the test **did**.\n**Target house: repo-local `docs/learnings/`.** Worth writing: it cost a review round and the failure\nwas invisible to mutation testing of the production code.\n\n**2. `AGENTS.md` — \"a guard's predicate must be NULL-safe, or it fails open.\"** `state <> 'idle'`\ncounted an unknown backend as drained because `NULL <> 'idle'` is NULL and `WHERE` discards it. This\nis the \"precondition that cannot fail\" rule in a new dress: not a value compared with itself, but a\ncomparison whose *unknown* case silently means *pass*. **Target house: repo-local. APPLIED** — added\nunder the existing precondition rule in `AGENTS.md`, together with its sibling (scope a guard to\nwhat can actually cause the thing you are guarding against: the same barrier counted autovacuum\nworkers, which cannot append, so unrelated maintenance could have failed the whole gate).\n\n**3. Process, already in `AGENTS.md` and violated anyway — \"never edit a tracked file while the gate\nis running.\"** Did it once (details in `retro:`). The rule needs no edit; the incident is evidence\nthat a *stated* rule is not a *followed* rule when a review finding arrives mid-gate. See retro item 2.\n\n#### retro:\n\n**(1) What would have made this ticket faster or better?**\n\n**The shaping was worth more than the plan.** COS 1 — the ticket's hardest question — was fully\ndischarged in Backlog by reading, before any model drafted anything. The planner then had a settled\nmechanism and spent its effort on *how to fix* rather than *what is true*. Concretely appliable:\n`references/shaping.md` already says a proposed remedy is a hypothesis to verify; it does **not** say\nthat for a ticket framed as *\"determine which explanation is true\"*, discharging that question **is**\nthe shaping. Suggested edit: add to the shaping pass, step 1 — *\"If the ticket's deliverable is a\ndetermination rather than a change, the determination is shaping's output, not Planning's. Record it\nin `readiness.uncertainties` with the evidence chain, and let the plan brief inherit it as given.\"*\n\n**The ticket's own precedent guess was wrong and cost nothing only because shaping checked it.**\n\"Same shape as TKT-234, may share a root cause\" — false (wall-clock ordering vs `sequence` ordering).\nThis is exactly the TKT-233 pattern the shaping doc already records. Working as intended; no edit.\n\n**(2) What should the humans change?**\n\n**A. `config.models.aiReview` earned its keep three times over — protect the budget for it.** Every\none of the seven findings was a check that could not refuse, and **none** was findable by the gate,\nby types, or by mutation testing of production code. Two of them refuted claims the implementing\nagent had written *in prose, in the code*. If review passes are ever trimmed for cost, this ticket is\nthe argument against trimming them on defect-fix work specifically.\n\n**B. The gate is the wall-clock bottleneck, and nothing in the flow acknowledges it.** Six `make check`\nruns, ~9 minutes each, ≈54 minutes of the ~76-minute total. Each review round forces a full gate even\nwhen the diff is one SQL clause. Suggested concrete change to `AGENTS.md`: document a **narrow\nverification path** for review-fix rounds — the affected package's smoke tests plus `bash -n` plus the\ntripwire test — with a full `make check` required only before push and before merge. Roughly halves\nthe cycle on tickets with three review rounds, and the full gate still gates the merge.\n\n**C. The agent edited a tracked file while a gate was running — the exact TKT-240 shape AGENTS.md\nwarns about, one bullet long, and it happened anyway.** A review finding arrived while gate2 was in\nits smoke stage; the fix was applied immediately instead of after the run. The run was killed rather\nthan trusted (a gate that reads a half-written tree fails for a reason that does not exist), the\norphaned compose stack was torn down by hand, and the gate restarted on a frozen tree. **Cost: one\nfull gate cycle, ~9 minutes.**\n\nWhy the existing rule did not prevent it: it is written as a property of *editing* (\"never edit while\nthe gate runs\"), and the trigger here was *receiving a finding* — a moment at which the natural next\naction is to fix. Suggested concrete edit to `AGENTS.md`, beside the existing rule: *\"A review finding\narriving mid-gate is the highest-risk moment for this: the natural next action is to fix, and the gate\nis invisible. Read the sentinel before touching the tree — or start the fix in a scratch copy and apply\nit after.\"* One sentence, at the point of the mistake rather than the point of the rule.\n\n**D. Applied already:** the `--timeout`/`-t` flag check (plan critique Amendment 1) was discharged by\n`--dry-run` against the real compose file set before committing. That habit — *execute the invocation,\ndo not reason about the flag* — is worth keeping; it is cheap and it caught a real spelling issue.\n"
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "make check's smoke stage aborts intermittently on `broken chain organizer=...0001 sequence=N` from payments Journal.Verify. Root cause found by reading (see kind=readiness): a LIVE commerce recovery runner appends to the journal while smoke.sh's corruption/restore block non-atomically snapshots and restores journal_entries, so an append landing in that window is deleted and never restored -- a PERMANENT gap, not a transient read skew."
    },
    "scope": {
      "state": "met",
      "note": "IN: quiesce the live journal writer (commerce) around scripts/smoke.sh's journal corruption/restore block so the verify block is deterministic; split the two-condition `broken chain` message so a gap and a hash mismatch are distinguishable from the log alone; a test that fails against today's message. OUT (non-goals, per ticket): chain construction and hash discipline (ADR-016/ADR-021). ALSO OUT, deliberately: wrapping Journal.Verify in a REPEATABLE READ transaction -- see approach."
    },
    "uncertainties": {
      "state": "met",
      "note": "The two candidate explanations in the ticket body are now settled: it is (1) test-harness only. Evidence chain, all read at HEAD: (a) every journal append funnels through AppendWithSettlement's `SELECT ... FOR UPDATE` on journal_heads (store.go:262), so no correctly-written journal can hold a sequence gap; (b) the gap is therefore externally created -- scripts/smoke.sh:338 backs up journal_entries, :342/:346 restore it with `DELETE ... ; INSERT ... SELECT backup`, and the block runs `ALTER TABLE journal_entries DISABLE TRIGGER ALL` (:338) so the append-only guard is OFF throughout; (c) the stack is still serving HTTP -- teardown is only the EXIT trap (:37); (d) commerce's recovery runner ticks on a timer (recovery/runner.go:218-231) and its JournalFact.OrderFailed POSTs payments /internal/facts (recovery/clients.go:341-361) -> server.go:179 -> journal.Append, for organizer ...0001; (e) scripts/smoke.sh:32 adds compose.smoke.yaml on the default non-hermetic path, which sets RECOVERY_INTERVAL: 2s (compose.smoke.yaml:16), so the writer fires every 2s for the whole block; (f) smoke/psp_recovery_test.go:64,382-384 deliberately backdate updated_at by 10 minutes and NULL the lease, defeating ClaimStuckOrders' 2-minute grace (store/recovery.go:81) and guaranteeing claimable residue. Remaining uncertainty is only WHICH of the block's windows lost the append, which the fix does not need to distinguish."
    },
    "dependencies": {
      "state": "met",
      "note": "No blockers. TKT-234 is explicitly NOT a shared root cause -- it is about trails ordered by a non-monotonic wall clock (ORDER BY occurred_at); the payments journal orders by `sequence` (store.go:306), so the ticket body's 'same shape as TKT-234' guess does not survive the read. Record that, do not inherit it."
    },
    "approach": {
      "state": "met",
      "note": "Quiesce the writer for the corruption/restore block -- NOT a snapshot read. Reasoning, since the obvious remedy is the wrong one: the corruption is DURABLE. An append lost between the :338 backup and the :342 restore is gone from journal_entries while journal_heads keeps its sequence, so every later append chains onto a row that no longer exists. Wrapping Journal.Verify in REPEATABLE READ makes the read consistent and still reports the gap, because the gap is really in the table. Quiescing commerce is the only remedy that removes the cause. Scope note: commerce is the unit to stop, not one runner -- recovery/runner.go reaches payments via OrderFailed/Void/Refund AND bulkrefund/runner.go reaches it too (ErrPaymentsRefused/ErrJournalUnavailable, runner.go:465-473). SECOND, SEPARATE defect found while reading, worth fixing in the same ticket because it is in the same function and cheap: Journal.Verify runs its journal_entries scan (store.go:306) and its journal_heads scan (store.go:347) as two statements on a pooled *sql.DB with no BeginTx, so they take two different snapshots on possibly two different connections. That one produces `journal head mismatch`, never `broken chain` -- so it is NOT this flake, and it must be fixed and tested as its own thing rather than folded into the flake's narrative."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice, all three COS: (1) smoke.sh stops commerce before the journal corruption/restore block and restarts it after; (2) the `broken chain` predicate splits into two messages naming the expected and observed sequence, and the expected vs observed previous_hash; (3) a Go test that fails against today's single message. Small and self-contained -- one shell file, one Go file, one test file."
    },
    "success": {
      "state": "met",
      "note": "COS as written on the ticket, with the first one now discharged AT SHAPING (explanation (1), by reading, with the file:line chain above). What implementation still owes: the determinism fix, the message split, and a test that goes red against HEAD. Note the demonstration constraint honestly -- the flake is timing-dependent, so 'a run that previously failed' cannot be replayed on demand; the honest evidence is a test that constructs the gap deterministically plus a green make check, and the closeout must say that rather than claim a reproduction it did not get."
    },
    "context_memo": {
      "state": "met",
      "note": "baked below"
    }
  },
  "context": {
    "governingAdrs": [
      {
        "adr": "ADR-003-append-only-audit-trail.md",
        "constrains": "Why verify-journal runs in the gate at all, and the exact scope of the integrity claim: modification and insertion are evident, coordinated rollback is NOT. This ticket must not widen or narrow that claim -- it only makes the CHECK deterministic."
      },
      {
        "adr": "ADR-016-checkout-recovery-state-machine.md",
        "constrains": "Owns the recovery runner that is the live writer here (Decision 3: the runner resolves against payments rather than guessing). Quiescing commerce during the smoke verify block must not change the runner's own predicates or its production interval -- only when the smoke script lets it run."
      },
      {
        "adr": "ADR-021-ticket-lifecycle-trail-integrity.md",
        "constrains": "The naming discipline for any integrity claim: say which adversary you mean. This ticket's write-up must call the failure a HARNESS artifact, not an integrity finding, and must not let 'the verifier cried wolf' drift into 'the verifier is unsound'."
      },
      {
        "adr": "ADR-048-settlement-ledger.md",
        "constrains": "settlement_entries carries an FK to journal_entries, which is why smoke.sh restores with DELETE rather than TRUNCATE (:332-337). Any change to the restore block must preserve that, or the restore starts failing on referential integrity."
      }
    ],
    "governingTests": [
      {
        "test": "services/payments/internal/store/journal_smoke_test.go:701 TestSmokeJournalKeyLiteralsMatchScript",
        "why": "READS scripts/smoke.sh and fails if the JOURNAL_KEY_ID / JOURNAL_SIGNING_KEY / JOURNAL_HISTORICAL_KEYS values there drift from the Go constants. This ticket edits smoke.sh. Safe as long as no journal-key assignment is added, moved into a variable, or reformatted across a line continuation -- the extractor is a regexp over `NAME=([^\\s\\\\]*)`."
      },
      {
        "test": "services/payments/internal/store/journal_smoke_test.go:421 TestJournalRotationKeepsHistoryVerifiable",
        "why": "Leaves the mixed-key journal that smoke.sh:190-211 asserts on. Do not reorder the payments store-suite block relative to the packaged verify-journal checks."
      },
      {
        "test": "scripts/smoke.sh:187-199 the mixed_kids fixture guard",
        "why": "Fails closed if payments_store_smoke holds fewer than 2 distinct key ids. Unrelated to the fix but sits in the same file -- do not disturb."
      },
      {
        "test": "smoke/psp_recovery_test.go",
        "why": "Depends on a LIVE recovery runner claiming its backdated orders within 45s. If the fix stops commerce, it must restart it and it must not run while these tests are running -- the quiesce belongs AFTER the Go smoke suite, around the corruption block only."
      }
    ],
    "keyFiles": [
      "services/payments/internal/store/store.go:305-370 (Journal.Verify -- the two-condition message at :327, the two unsynchronized queries at :306 and :347)",
      "scripts/smoke.sh:319-351 (the verify + corruption/restore block)",
      "services/commerce/internal/recovery/runner.go:218-231 (the ticker), clients.go:341-361 (the journal POST)",
      "compose.smoke.yaml:16 (RECOVERY_INTERVAL: 2s), scripts/smoke.sh:32 (where that file is applied)"
    ],
    "risk": "not-low: touches CI/gate config (scripts/smoke.sh is the local gate's smoke stage) and a money-path audit surface. The plan gate is NOT skipped."
  }
}
