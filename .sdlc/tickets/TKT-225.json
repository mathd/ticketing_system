{
  "key": "TKT-225",
  "type": "Story",
  "parent": "TKT-21",
  "classification": "complex",
  "summary": "An operator path to undo a guest-order claim (TKT-223 follow-up)",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "number": 187,
    "url": "https://github.com/mathd/ticketing_system/pull/187",
    "state": "merged"
  },
  "links": [
    {
      "type": "blocked-by",
      "key": "TKT-223"
    }
  ],
  "description": "Filed by TKT-223, whose ACs required the ownership decision to be argued rather than assumed. It was\nargued, and this is the part the argument left open.\n\n**The problem, in ADR-049 § TKT-223's own words:** *\"A claim is destructive, exclusive, permanent and\nunrecoverable. The first holder to claim takes the order away from its buyer, and there is no\nun-claim path — no endpoint, no CLI, no support tool. The buyer's recourse is nothing.\"*\n\nThe proof of ownership is the order reference alone, deliberately: matching the email would refuse a\nbuyer who signed up with a different address. The cost of that choice is that anyone holding a leaked\nreference can permanently take an order — and references **do** leak, because the gateway logs them\nvia the URL path (**TKT-202**, open).\n\nToday the only remedy is a manual `UPDATE` against the commerce database.\n\n**Acceptance Criteria:**\n- [ ] An operator can detach an order from the customer it is attributed to, restoring it to\n  unattributed so the real buyer can claim it.\n- [ ] The plan states **who may do this and how they are authenticated**. It is a support action on\n  someone else's purchase, so \"an internal endpoint\" is the start of the answer and not the whole of\n  it — ADR-043 decides the guard, and the back office already has a staff credential and a role\n  matrix (ADR-042/TKT-197).\n- [ ] The action is **recorded**, with who did it and why. An un-claim that leaves no trace turns a\n  support tool into a way to move purchases quietly.\n- [ ] It does **not** become a transfer. Detaching restores NULL; it never repoints an order at a\n  different customer in one step. (Transfers are TKT-9/TKT-160 and have a different adversary.)\n- [ ] `TestNoProductionCodeUpdatesOrderAttribution`'s allowlist grows to **exactly two** statements,\n  and `TestTheAllowlistCannotBeWidened` covers the new one. That invariant is the thing keeping\n  attribution honest; widening it is part of the work, not a side effect.\n- [ ] The plan says whether an un-claimed order should be re-claimable by the same attacker\n  immediately, and if not, what stops it.\n- [ ] `make check` green.\n\n**Non-goals:** transfers between accounts (TKT-9/TKT-160) · rate limiting the claim (TKT-224) ·\nfixing the reference leak (TKT-202).",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "A wrongly-claimed order can be detached, so 'someone else claimed my order' stops being a support request with no answer."
    },
    "scope": {
      "state": "met",
      "note": "Detach only — restore NULL. Never repoint at another customer in one step; that is a transfer (TKT-9/TKT-160) with a different adversary."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "One genuinely open: whether an un-claimed order needs protecting from immediate re-claim by the same actor. Detaching restores it to the exact state the attacker claimed it from, so 'undo' without more is a loop. Options the plan must weigh: accept it (the operator can repeat), a cooldown, or a short-lived reservation for the rightful buyer. Deferred rather than open because it does not block starting and the ADR-021 framing is already written down."
    },
    "dependencies": {
      "state": "met",
      "note": "TKT-223 is Done - it creates the state this undoes. TKT-202 (references leak into gateway logs) is the CAUSE and is neither a blocker nor a substitute: this is recovery, that is prevention."
    },
    "approach": {
      "state": "met",
      "note": "Narrowed during shaping: a commerce /internal/ operation, reached by the back office with COMMERCE_STAFF_WRITE_TOKEN - the precedent is TKT-194's staff refund, which is the same shape (a support action on somebody else's order, guarded by an inline check per ADR-043, edge-denied at the gateway by construction). Authorization by role is ADR-042/TKT-197's matrix; the plan says which roles, and `finance` is probably not one of them."
    },
    "first_slice": {
      "state": "met",
      "note": "Detach + its audit record + the invariant amendment. The back-office surface can follow, or the first slice can be the internal operation alone if the plan argues it is demonstrable."
    },
    "success": {
      "state": "met",
      "note": "Deterministic: an attributed order becomes unattributed, the rightful buyer can then claim it, the action leaves a record, and TestTheAllowlistCannotBeWidened covers the SECOND allowlisted statement."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked at creation; re-resolved 2026-08-06 when the ticket was shaped."
    }
  },
  "context": {
    "capturedAt": "2026-08-06",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-08-06",
        "rule": "Local gate: `make check` (lint + test + build, Go & TS, plus the gateway smoke suite). CI runs the same gate - keep them mirrored. Commit regenerated code before running the gate."
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-049",
        "why": "The TKT-223 amendment states the gap this closes, in the words this ticket quotes. Its 'one exception to attribution never changes' section is the rule the un-claim becomes a SECOND exception to - read why the first one was allowed before adding another."
      },
      {
        "id": "ADR-042",
        "why": "Staff identity and the back-office session; TKT-197's role matrix is what would decide who may un-claim, if the back office is where this lands."
      },
      {
        "id": "ADR-043",
        "why": "Declared `security:` guards public contract operations; an inline X-Internal-Token check guards the internal surface. This is an internal support action, so that rule decides its guard."
      },
      {
        "id": "ADR-021",
        "why": "Name the adversary. An audit record inside the same database it audits constrains a careless operator, not a hostile one."
      },
      {
        "id": "ADR-016",
        "why": "Do not add an order status. Attribution is its own dimension."
      }
    ],
    "reference": [
      {
        "summary": "The statement to mirror and the guard to widen: `claimGuestOrderStatement` in services/commerce/internal/store/customer.go, allowlisted BY TEXT AND BY COUNT in attribution_invariant_test.go. Exactly one is permitted today; this ticket makes it two, and TestTheAllowlistCannotBeWidened must cover the second.",
        "link": "services/commerce/internal/store/attribution_invariant_test.go"
      },
      {
        "summary": "PREREQUISITE, not a coincidence: TKT-202 (the gateway logs guest_order_ref via the URL path). ADR-049 calls it a safety issue for claiming rather than logging hygiene - a leaked reference is what makes a hostile claim possible in the first place.",
        "link": "TKT-202"
      }
    ]
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\nFiled by TKT-223 (ai-review stage, gateless run 2026-08-06). Not shaped: `readiness` is mostly `open` and it must go through `shaping.md` before Gate 1. The three open questions are who may un-claim, what is recorded, and whether an un-claimed order needs protection from immediate re-claim by the same actor."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Implementation readiness — CONCERNS\n\nShaped 2026-08-06, after TKT-21 closed. Gate 1 granted by the owner in the same instruction.\n\n| Criterion | Status | Note |\n|---|---|---|\n| Objective | OK | \"Someone else claimed my order\" gets an answer |\n| Scope | OK | Detach only — never a transfer |\n| Uncertainties | WARN | Re-claim protection, deferred to the plan |\n| Dependencies | OK | TKT-223 Done; TKT-202 is the cause, not a blocker |\n| Approach | OK | **Narrowed during shaping** — see below |\n| AC verifiable | OK | Including the allowlist growing to exactly two |\n| Context-mémo | OK | Re-resolved today |\n\n**What shaping decided:** a commerce `/internal/` operation reached by the back office with\n`COMMERCE_STAFF_WRITE_TOKEN`. The precedent is **TKT-194's staff refund** — the same shape exactly: a\nsupport action on somebody else's order, guarded by an inline check (ADR-043), edge-denied at the\ngateway by construction, and driven from a back office that already holds that credential and has a\nrole matrix (ADR-042/TKT-197). This is not a new pattern; it is an existing one applied again.\n\n**Concerns:**\n\n1. **Undo without more is a loop.** Detaching restores the order to precisely the state the attacker\n   claimed it from, so they can claim it again a second later. The plan must decide between accepting\n   that, a cooldown, or a short-lived reservation for the rightful buyer — and each has a different\n   adversary story (ADR-021).\n2. **The audit record's adversary needs naming.** A row in the same database the operator can write\n   constrains a *careless* operator, not a hostile one. ADR-021 is explicit that state inside the\n   database cannot constrain someone who writes to the database — say which is meant before writing\n   \"audited\".\n3. **TKT-202 is the cause and is still open.** This ticket is recovery; that one is prevention.\n   Shipping only this leaves the leak; shipping only that leaves today's wrongly-claimed orders stuck.\n   They are complementary and neither substitutes.\n\n**Verdict:** CONCERNS — proceedable. The mechanism is decided from precedent; what is left is one\ngenuine product question and one honesty question about the audit claim."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claim — TKT-225\n\n**⚠ GATELESS RUN — all four human gates waived by the repo owner**, same terms as TKT-198 and\nTKT-229, extended to this ticket by the owner's instruction on 2026-08-08 (\"now do tkt-225\"). Gate 1\nwas granted at shaping; Gate 2 self-resolved after the cross-model critique, preferring the\ncritique's recommendation and otherwise the most reversible option; Gate 3 agent-merged after the\nadversarial review converges; Gate 4 self-validated against test evidence on merged code.\n\n**Blocker verified clear:** the `blocked-by TKT-223` link is satisfied — TKT-223 is **Done**. The\nstatus is priority, not feasibility, so this was checked rather than assumed.\n\n**Selection reason.** The owner asked for it, and it is the only remaining shaped `Ready` ticket. It\nalso closes a real gap: ADR-049 § TKT-223 states in its own words that a claim is *\"destructive,\nexclusive, permanent and unrecoverable… The buyer's recourse is nothing.\"* Today the only remedy is\na manual `UPDATE` against the commerce database.\n\n**Risk verdict: `risk:low` NOT claimed.** Explicitly refused — it fails three carve-outs:\n- **auth** — the operation needs a guard decision (ADR-043), and the back office's staff credential\n  is currently accepted by *exactly one* internal route, enumerated by a test;\n- **data migrations** — an audit record almost certainly means a new table and migration 0019;\n- **>5 files** likely, across store, api, migrations, the OpenAPI contract and the invariant test.\n\nMore to the point: this ticket **deliberately widens a security invariant**\n(`TestNoProductionCodeUpdatesOrderAttribution` allows exactly one attribution write today, and this\nmakes it two). Widening a guard is the last thing that should ride a low-risk label, and on a\ngateless run there is no human downstream to catch it.\n\n**Governing ADRs, re-resolved at claim time** (the mémo is a 2026-08-06 snapshot):\n- **ADR-049** — creates the gap, in the words the ticket quotes. Its \"attribution never changes,\n  with one exception\" section is the rule this becomes a **second** exception to.\n- **ADR-043** — decides where the guard lives: `security:` for public contract operations, inline\n  token checks for `/internal/`.\n- **ADR-042** + TKT-197's role matrix — who may do this, if the back office is the surface.\n- **ADR-021** — **name the adversary.** An audit row inside the database it audits constrains a\n  careless operator, not a hostile one. Any \"recorded\" claim must say which.\n- **ADR-016** — do not add an order status; attribution is its own dimension.\n- **ADR-051 — NEW since shaping (TKT-224, merged 2026-08-07), and worth checking.** It rate-limits\n  `/orders/claim` per subject and per source. It does not obviously govern an internal un-claim, but\n  the *interaction* matters for one of this ticket's own ACs: \"should an un-claimed order be\n  re-claimable by the same attacker immediately?\" The limiter is now part of that answer and did not\n  exist when the question was written. Flagged for the plan.\n\n**Two constraints found in the code before planning, so the plan does not rediscover them:**\n1. `claimGuestOrderStatement` (`store/customer.go:451`) is allowlisted **by text and by count** —\n   `allowedAttributionUpdates` matches the exact normalized string `customer_id = $2`, and\n   `TestNoProductionCodeUpdatesOrderAttribution` asserts `allowed == 1`. A detach sets\n   `customer_id = NULL`, which that function will **refuse**. Both must change, deliberately.\n2. `staff_credential_test.go` enumerates the internal routes and asserts the staff credential is\n   refused by all of them **except** the refund (`staffOrInternal`, `refunds.go:69`). Making the\n   un-claim staff-reachable means updating that enumeration — which is the test's whole point.\n\n**Branch:** `TKT-225-unclaim-guest-order` off `origin/main` at `a0a596d`."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — drafted by `gpt-5.6-luna` (config.models.plan)\n\nRun via the codex companion, `task --model gpt-5.6-luna --effort high --background`, READ-ONLY (no\n`--write`). The drafter read the real tree. Clean run: no foreign-skill hijack, no \"No job found\"\nfalse negative.\n\n---\n\n## 1. Decision\n\nBuild `POST /internal/orders/{id}/unclaim`, guarded by existing `staffOrInternal`, with a back-office action on `/admin/orders` for `admin` and `box_office`. Commerce will lock the completed attributed order, set `customer_id = NULL`, and insert an audit/block record in one transaction. The detached customer cannot immediately reclaim that order; a different customer can. This is an attribution recovery action, not a transfer, status change, lifecycle event, or reference-leak fix.\n\nA. Recommend `staffOrInternal`. This preserves the existing internal-token operator path while making the action usable by support staff through the existing `X-Commerce-Staff-Write-Token`. The back office derives `actor` from the authenticated staff session; the form cannot supply it. `staff_credential_test.go` must be updated so exactly refund and unclaim accept the staff credential; every other internal route remains 404. Internal-token-only would be simpler but would leave support agents without a supported back-office workflow and would not provide individually identified staff actions.\n\nB. Add a commerce-owned `order_attribution_detachments` table containing order ID, detached customer ID, actor, reason, and timestamp. The audit insert and attribution update commit together. This provides accountability against careless operators or application omissions, not tamper evidence against a commerce-database writer. The access lifecycle chain is rejected because it belongs to another service/domain and would not protect commerce database state.\n\nC. Permanently block the detached customer ID from reclaiming that order. ADR-051 rate limiting only slows an attacker and does not prevent immediate reclaim within the remaining budget. A cooldown would also block the legitimate buyer. The block allows another customer to claim the restored guest order, while attacks using another account remain outside this ticket’s guarantee.\n\nD. Extend the attribution scanner with exactly two statement-bound cases: normalized `customer_id = $2` with claim predicates, and normalized `customer_id = NULL` with detach predicates (`id = $1`, completed status, and non-NULL attribution). `scanAttributionUpdates` remains the complete match/recognize/authorize decision. The production count becomes exactly two, and `TestTheAllowlistCannotBeWidened` drives synthetic source through the scanner to reject a third statement or weakened predicates.\n\n## 2. Definition of Done\n\n- AC1: An operator can detach a completed attributed order; `customer_id` becomes NULL and another customer can claim it.\n- AC2: Authentication uses `staffOrInternal`; the back office permits only existing `admin` and `box_office` roles, while finance remains denied.\n- AC3: Every successful detach records actor, reason, previous customer, order, and timestamp atomically.\n- AC4: The action only clears attribution; it never assigns another customer or changes order status, money, tickets, or `updated_at`.\n- AC5: The source invariant allows exactly two attribution-update statements, and rejects a third or widened statement.\n- AC6: The detached customer is blocked from immediately reclaiming; a different customer remains eligible.\n- AC7: Generated code is committed before `make check`; `make check` passes. `make browser` submits the back-office form and verifies the resulting database state.\n\n## 3. Files to change\n\n- `docs/adr/ADR-052-order-attribution-detachment.md` — NEW; record guard, audit threat model, permanent reclaim block, atomicity, and non-goals.\n- `docs/development.md` — document operator procedure, required reason/actor, refusal behavior, and audit guarantee boundaries.\n- `services/commerce/api/openapi.yaml` — add unclaim operation and request/response schemas.\n- `services/commerce/internal/api/openapi_gen.go` — GENERATED; regenerate and commit.\n- `services/commerce/internal/api/server.go` — register `/internal/orders/{id}/unclaim`.\n- `services/commerce/internal/api/staff_credential.go` — update comments for two staff-reachable operations.\n- `services/commerce/internal/api/staff_credential_test.go` — enumerate unclaim as the second staff-credential exception.\n- `services/commerce/internal/api/unclaim.go` — NEW; authentication, validation, store call, and response mapping.\n- `services/commerce/internal/api/unclaim_test.go` — NEW; auth, validation, success, and refusal tests.\n- `services/commerce/internal/store/customer.go` — add detached-customer block predicate to guest claiming.\n- `services/commerce/internal/store/unclaim.go` — NEW; row-locking transaction, conditional NULL update, and audit insert.\n- `services/commerce/internal/store/migrations/0019_order_attribution_detachments.sql` — NEW; audit/block table, constraints, index, and safe down migration.\n- `services/commerce/internal/store/attribution_invariant_test.go` — widen scanner/count from one to exactly two statements.\n- `services/commerce/internal/store/claim_smoke_test.go` — prove detached-customer refusal and different-customer reclaim.\n- `services/commerce/internal/store/attribution_smoke_test.go` — prove atomic detach, audit contents, preserved timestamps/status, and rollback.\n- `services/commerce/internal/store/migration_smoke_test.go` — verify migration schema and rollback refusal.\n- `services/commerce/cmd/commerce/main.go` — update credential-scope comments.\n- `services/commerce/cmd/commerce/credentials_test.go` — update credential-scope comments.\n- `web/backoffice/src/lib/commerce.ts` — add direct commerce `unclaimOrder`.\n- `web/backoffice/src/lib/order-console.ts` — parse nullable attribution and unclaim reason.\n- `web/backoffice/src/pages/orders.astro` — add the authenticated unclaim form/action.\n- `web/backoffice/test/api.test.ts` — test URL, credential, body, response, and failure mapping.\n- `web/backoffice/test/order-console.test.ts` — test parsing and negative fixtures.\n- `compose.yaml` — update staff-credential comments.\n- `scripts/stack-env.sh` — update credential-scope comments.\n- `scripts/browser.sh` — provision/pass a temporary box-office staff account for browser tests.\n- `test/browser/unclaim-order.mjs` — NEW; submit the real back-office form and verify database/audit results.\n\n`web/backoffice/src/lib/authorization.ts` and its existing tests require no route change because `/admin/orders` already admits `admin` and `box_office` and refuses `finance`.\n\n## 4. Test plan\n\n1. `TestNoProductionCodeUpdatesOrderAttribution`  \n   RED once the new detach SQL exists but the scanner still allows only one statement. GREEN with exactly two allowed statements.\n\n2. `TestTheAllowlistCannotBeWidened`  \n   RED because the detach statement is initially refused. GREEN when both approved statements pass through `scanAttributionUpdates`, while a third assignment and weakened predicates remain refused.\n\n3. `TestPredicatesAreReadFromTheStatementAndNotTheFile`  \n   RED if the new implementation checks predicates at file scope. GREEN when claim and detach predicates are checked against their own statements.\n\n4. Attribution smoke tests  \n   RED before migration/store implementation. GREEN when a locked completed attributed order becomes NULL, exactly one audit row is written, and status/`updated_at` remain unchanged.\n\n5. Atomicity/concurrency smoke test  \n   RED before transactional implementation. GREEN when a failed audit insert rolls back the attribution update and concurrent claim/detach operations serialize without losing audit evidence.\n\n6. Claim smoke tests  \n   RED because the detached customer can currently reclaim. GREEN when that customer receives `ErrOrderNotClaimable`, while another customer can claim the restored order.\n\n7. Migration smoke tests  \n   RED because migration `0019` and its table do not exist. GREEN when constraints/indexes are present and rollback refuses while evidence exists.\n\n8. `unclaim_test.go`  \n   RED because no route/handler exists. GREEN when missing or wrong credentials return 404, valid staff/internal credentials pass, malformed inputs return 400, and store errors do not leak detail.\n\n9. `staff_credential_test.go`  \n   RED because the real router gains an unaccounted route and the old “refund only” assumption fails. GREEN when only refund and unclaim accept the staff credential.\n\n10. Back-office unit tests  \n    RED because no client/parser/form exists. GREEN when the exact direct request is sent, actor comes from the session, target-customer fields are ignored/refused, and NULL responses are validated.\n\n11. `test/browser/unclaim-order.mjs` via `make browser`  \n    RED because the form submission path does not exist or leaves attribution intact. GREEN only after real Chrome signs in, submits lookup and unclaim, and the database shows NULL attribution plus one audit record.\n\n12. `make generate` followed by `make check`  \n    RED on generated-code drift or any Go/TypeScript/lint/build/smoke failure. GREEN after generated output is committed and the out-of-band migration job applies `0019`.\n\nNo test can prove tamper evidence against a hostile commerce-database writer; that guarantee is explicitly not claimed.\n\n## 5. Risks\n\n- The shared staff credential authenticates the back office as a deputy, not an individual. Session-derived actor attribution can still be forged by someone holding the bearer credential.\n- The permanent customer/order block can hinder recovery if support detaches the wrong customer. Undoing that block is outside this ticket.\n- ADR-051 does not stop reclaim through another account, source rotation, replica restart, or direct service access.\n- Audit rows are ordinary commerce state and can be altered or deleted by a database writer.\n- Claim and detach must be reviewed together: both need completed-order predicates, detach must require non-NULL attribution, and claim must reject only the detached customer while preserving eligibility for others.\n- The update, audit insert, claim block, generated contract, back-office client, and Astro form can each be correct while composing incorrectly; the smoke, unit, generated-code, and browser gates must all pass.\n- This does not implement transfers, cooldowns, rate-limit changes, gateway log redaction, lifecycle-chain integration, or database-adversary tamper evidence."
    },
    {
      "stage": "plan-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=plan-review kind=summary -->\n### Plan critique — by `main-agent` (config.models.planReview)\n\nCross-model pair satisfied: drafted by `gpt-5.6-luna`, critiqued by `main-agent`. Every file, symbol\nand role the draft names was verified at HEAD before judging.\n\n#### Existence checks — all PASS\n\n| Draft claim | Verdict |\n|---|---|\n| `/admin/orders` admits `admin` + `box_office`, refuses `finance` | **PASS** — `authorization.ts:80`, exactly as stated |\n| `staffOrInternal` at `staff_credential.go:43`; refund is the only user | **PASS** — `refunds.go:69` |\n| Migrations run to `0018`, so a new one is `0019` | **PASS** |\n| `allowedAttributionUpdates` is an exact-string match on `customer_id = $2` | **PASS** — a detach *will* be refused as written |\n| `web/backoffice/src/pages/orders.astro` exists | **PASS** |\n\n#### ⛔ REJECT — decision C, the permanent re-claim block\n\nThis is the draft's weakest decision and it is load-bearing. It proposes a **permanent** block on\nthe detached customer/order pair, arguing ADR-051's limiter \"only slows an attacker\".\n\nThree problems:\n\n1. **It gets the primary case backwards.** The overwhelmingly likely detach is *support fixing a\n   mistake* — wrong order detached, or a buyer who claimed with the wrong account and wants to redo\n   it with the right one. A permanent block means **the legitimate buyer can never claim their own\n   order again**, and the draft's own risk section admits \"undoing that block is outside this\n   ticket\". A support tool whose main action is irreversible is the exact defect this ticket exists\n   to fix — it would recreate TKT-223's problem one level up.\n2. **It adds a second irreversible state to a ticket about reversing state.** Scope-wise it is\n   backwards.\n3. **It does not stop the attacker it names.** The draft concedes \"attacks using another account\n   remain outside this ticket's guarantee\". An attacker who can mint accounts (registration is\n   public — ADR-049) simply claims from a second account. So the block reliably stops only the\n   *honest* re-claimer, i.e. the buyer.\n\n**Amended:** answer AC 6 **explicitly and negatively** — an un-claimed order *is* immediately\nre-claimable, because (a) making it otherwise blocks the legitimate buyer, (b) ADR-051 now\nrate-limits the claim path per subject and per source, and (c) the real fix for reference leakage is\nTKT-202, which is this ticket's stated non-goal. AC 6 asks the plan to *say whether* and *what stops\nit*, not to build a block. Answering \"no, and here is why\" satisfies it.\n\n#### ⛔ REJECT — the scope. 26 files for a first slice.\n\nThe draft lists **26 files**, including `compose.yaml`, `scripts/stack-env.sh`, `scripts/browser.sh`,\n`cmd/commerce/credentials_test.go` and `main.go` comment updates, a new ADR, a back-office Astro\nform, two back-office test files, a browser spec, and provisioning a temporary box-office account in\nthe browser harness.\n\nThe ticket's own `first_slice` readiness says: **\"Detach + its audit record + the invariant\namendment. The back-office surface can follow.\"** The draft ignored that and planned the whole\nepic. On a **gateless** run this matters more than usual: a 26-file diff with no human gate is the\nshape most likely to hide a defect, and `scripts/browser.sh` provisioning credentials is\ninfrastructure work nobody asked for.\n\n**Amended: the slice is commerce-only.** Internal endpoint, store operation, migration, audit table,\ninvariant widening, ADR. **No back-office form, no browser spec, no compose/script changes** — and\ntherefore, per the working agreement, `make browser` is *not required*, because this slice adds no\nweb-UI write form. The back-office surface becomes a follow-up ticket.\n\n#### AMEND — decision A, the guard\n\n`staffOrInternal` is right *eventually*, but with no back-office form in this slice it buys nothing\nand costs the `staff_credential_test.go` enumeration change — a security test that exists precisely\nto make that list deliberate. **Internal-token only for this slice**, and the follow-up that adds\nthe form does the enumeration change with its own review. This is also strictly more reversible:\nwidening a credential later is easy; narrowing one after something depends on it is not.\n\n#### ACCEPT — decisions B and D\n\n**B (audit table + honest threat model)** is right, and the draft states the ADR-021 boundary\ncorrectly and unprompted: accountability against a careless operator, **not** tamper evidence\nagainst a database writer. It also correctly rejects the access-service lifecycle chain as\ncross-domain. Adopt as drafted.\n\n**D (two statement-bound cases)** is right. Note the detail the draft got right: keeping\n`scanAttributionUpdates` as the single match/recognise/authorize decision, because a previous review\npass caught a version whose test called helpers directly and therefore could not fail.\n\n#### CONCUR — the details worth keeping\n\n- Atomic detach + audit insert in one transaction; a failed audit rolls back the attribution change.\n- `updated_at` untouched — it means \"when did this order's CHECKOUT last move\" (`customer.go`), and a\n  support action months later is not checkout activity. The draft caught this unprompted.\n- No new order status (ADR-016).\n- Never repoint at another customer in one step (AC 4).\n\n#### Pre-mortem — how this ships and is still wrong\n\n1. **The widened allowlist admits a third statement.** The whole point of AC 5. The new case must be\n   bound to the detach's *predicates*, not merely to the string `customer_id = NULL` — otherwise any\n   future `UPDATE orders SET customer_id = NULL WHERE <anything>` passes.\n2. **The detach has no `WHERE customer_id IS NOT NULL`**, so detaching an already-unattributed order\n   silently \"succeeds\" and writes a misleading audit row claiming a detachment that did not happen.\n3. **The audit row records the actor the caller *claims*.** With internal-token-only there is no\n   session to derive it from, so `actor` is caller-supplied text. That must be *stated* in the ADR\n   as what it is — an operator-supplied label, not an authenticated identity — or the audit trail\n   overclaims."
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan final — delta on the `kind=plan` draft\n\n**The `kind=plan` draft as amended below is the plan.**\n\n#### ⚠ Gate 2 WAIVED — decisions made by the agent\n\n**Decision 1 — scope cut to the ticket's own first slice: commerce only.**\nInternal endpoint + store operation + migration + audit table + invariant widening + ADR.\n**Dropped from the draft:** the back-office Astro form, `web/backoffice/**` client/parser changes and\ntheir tests, `test/browser/unclaim-order.mjs`, `scripts/browser.sh` credential provisioning,\n`compose.yaml` and `scripts/stack-env.sh` comment edits, `cmd/commerce` comment edits. 26 files → ~8.\n*Rationale:* the ticket's `first_slice` readiness says exactly this, and the draft overrode it\nwithout argument. Following the critique. Also the most reversible: adding a form later is additive.\n**Consequence recorded:** `make browser` is **not required** for this slice, because it adds no\nweb-UI write form. The follow-up that adds the form must run it.\n\n**Decision 2 — guard is internal-token only, NOT `staffOrInternal`.**\n*Rationale:* with no back-office form in this slice, `staffOrInternal` buys nothing and costs a\nchange to `staff_credential_test.go`, whose entire purpose is to make that list deliberate. Widening\na credential later is easy; narrowing one after a dependency exists is not. Following the critique.\n\n**Decision 3 — AC 6 answered \"yes, immediately re-claimable\", and the permanent block REJECTED.**\n*Rationale:* the block's primary victim is the legitimate buyer — the most likely detach is support\nfixing a mistake, and a permanent block means the real buyer can never claim their order. The draft\nconceded that undoing the block was out of scope, i.e. it proposed a new irreversible action inside a\nticket whose purpose is reversing one. It also fails against the attacker it names, who can mint a\nsecond account (registration is public, ADR-049). AC 6 asks the plan to *say whether*, and this says\nno, with reasons: ADR-051 rate-limits the claim path, and the leak itself is TKT-202's.\n\n**Decision 4 — decisions B and D adopted as drafted.** Audit table with an honestly-stated threat\nmodel (accountability vs a careless operator; **not** tamper evidence vs a database writer, ADR-021),\nand two statement-bound allowlist cases with `scanAttributionUpdates` remaining the whole decision.\n\n**Decision 5 — the detach requires `customer_id IS NOT NULL`.** From pre-mortem #2: without it,\ndetaching an unattributed order \"succeeds\" and writes an audit row for a detachment that never\nhappened.\n\n**Decision 6 — `actor` is documented as an operator-supplied label, not an authenticated identity.**\nFrom pre-mortem #3. With internal-token-only there is no session to derive it from. The ADR says so\nplainly rather than letting the audit trail overclaim.\n\n#### Files to change — amended (~8, not 26)\n\n| File | Change |\n|---|---|\n| `docs/adr/ADR-052-order-attribution-detachment.md` | NEW — guard, threat model, the re-claim answer, why a second attribution write is sanctioned |\n| `services/commerce/api/openapi.yaml` | The unclaim operation |\n| `services/commerce/internal/api/openapi_gen.go` | GENERATED — regenerate and **commit before the gate** |\n| `services/commerce/internal/api/server.go` | Register `POST /internal/orders/{id}/unclaim` |\n| `services/commerce/internal/api/unclaim.go` + `_test.go` | NEW — inline internal-token check, validation, refusals |\n| `services/commerce/internal/store/unclaim.go` | NEW — the transaction: locked conditional UPDATE + audit insert |\n| `services/commerce/internal/store/migrations/0019_order_attribution_detachments.sql` | NEW |\n| `services/commerce/internal/store/attribution_invariant_test.go` | Widen to exactly two, bound by predicate |\n| `services/commerce/internal/store/*_smoke_test.go` | Detach behaviour, atomicity, re-claim after detach |\n| `docs/development.md` | The operator procedure |\n\n#### Test plan — the parts that matter\n\n1. **`TestNoProductionCodeUpdatesOrderAttribution`** — RED when the detach SQL exists and the count\n   is still 1; GREEN at exactly 2.\n2. **`TestTheAllowlistCannotBeWidened`** — the AC-5 test. Must assert a **third** statement is still\n   refused, and that a detach with *weakened predicates* is refused. Not merely that two pass.\n3. **Detach smoke** — attributed → NULL, exactly one audit row, `status` and `updated_at` unchanged.\n4. **Detaching an unattributed order is refused** (Decision 5), and writes **no** audit row.\n5. **Atomicity** — a failing audit insert rolls back the attribution change.\n6. **Re-claim after detach succeeds** — pins Decision 3 as behaviour, so a later \"hardening\" that\n   adds a block has to argue with a test.\n7. Every test written during the work gets a **mutation check**; results recorded in the stage\n   comment.\n\n#### Introduced at plan-review, reviewed by no second model\n\nPer the required section — these reached the (waived) Gate 2 reviewed only by `main-agent`:\n\n1. **Decision 3 (no re-claim block).** The most consequential. If the reasoning is wrong, this ships\n   a support tool that lets an attacker immediately re-take an order — mitigated only by ADR-051's\n   limiter. I judge the alternative strictly worse, but one model judged it.\n2. **Decision 1's scope cut**, including the conclusion that `make browser` is not required here.\n   If the back-office surface is what makes this ticket *useful*, then shipping commerce-only is a\n   half-feature — defensible per `first_slice`, but it is my reading of it.\n3. **Decision 5's `IS NOT NULL` predicate** — mine, not the draft's.\n\nNamed again for the ai-review stage to attack."
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation — by `main-agent` (config.models.implement), commits `0e8cd1a` + `179aecc`\n\n```yaml\nstage: coding\nimplementer: main-agent\ncommits: [0e8cd1a, 179aecc]\nbranch: TKT-225-unclaim-guest-order\ndiff: +714 / -13 (11 files)\ntests_added: 12 (8 store smoke, 4 handler) + 6 allowlist cases + 1 gateway smoke\ngate_runs: 7 (1 green; 6 red, every one diagnosed)\n```\n\n#### TDD — RED observed first, twice, on the guards that matter\n\n| Guard | Observed RED | Why it matters |\n|---|---|---|\n| `TestNoProductionCodeUpdatesOrderAttribution` | **RED** on the new detach statement before the allowlist was widened | The invariant caught my own new statement — it works |\n| `TestTheEnumerationCoversEveryInternalRouteCommerceServes` | **RED** on the new route before it was enumerated | *(An earlier `ok` was Go's test cache, not a pass — worth knowing)* |\n\n#### Mutations — five, one at a time, each restored after\n\n| Mutation | Caught by |\n|---|---|\n| Drop `customer_id IS NOT NULL` from the detach | invariant count 2 → 1 |\n| Add a **third** attribution statement (transfer-shaped) | invariant refuses it by name |\n| Remove the handler's credential guard | route enumeration + my own refusal test |\n| Swap the guard for `staffOrInternal` | route enumeration |\n| Answer 401 instead of 404 | refusal test |\n\n#### Three defects the tests found that reading did not\n\n1. **`RETURNING customer_id` returns the NEW value.** Every audit row would have recorded a\n   detachment *from nobody*. I then assumed the table-qualified `RETURNING orders.customer_id`\n   reached the old row, shipped that, and watched it fail identically. Only on the third attempt did\n   I verify against a real PostgreSQL 18 instead of reasoning: `RETURNING OLD.column` is the only\n   spelling that works, and it is **new in 18**. `compose.yaml` pins `postgres:18.4`.\n   **The failure mode is the lucky part:** the FK to `customer_accounts` rejected the NULL, so this\n   surfaced as a constraint violation. A nullable audit column would have accepted the row and the\n   trail would have been quietly worthless.\n2. **The not-completed fixture used `payment_pending`, which this schema does not allow.** The CHECK\n   constraint refused it. Replaced with the two *real* states either side of completed —\n   `confirmation_pending` and `refunded` — because one fixture on one side passes against a\n   predicate that tests the wrong bound.\n3. **The smoke coverage guard rejected a documented 2xx operation with no happy path.** Correct, and\n   it forced the end-to-end test that turned out to be the most valuable one written here.\n\n#### What the end-to-end test proves that no handler test could\n\n`smoke/unclaim_test.go` drives the whole story through the live stack: a guest buys, the **wrong**\naccount claims, the rightful buyer is **refused**, the operator detaches, the rightful buyer claims.\nPlus: the route is **not** reachable through the gateway (edge-denied), a second detach reports the\n**current** owner rather than a remembered one, and a third finds nothing to detach.\n\n#### Gate\n\n**`make check` exit 0** on run 7. Runs 1–6 were all diagnosed: 1 lint (a helper name collision with\nan existing `attributionOf`), 4 the defects above, and **1 an unrelated intermittent mailer failure\n(`TestAFailedSendLeavesTheRowUnsentAndRetryable`) that did not recur** — the TKT-230 pattern, on a\npackage this diff does not touch.\n\n`make browser` **not run, and not required**: this slice ships no web-UI write form, per Decision 1."
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review — by `gpt-5.6-luna` (config.models.aiReview), 3 passes\n\nCross-model pair satisfied: implemented by `main-agent`, reviewed by `gpt-5.6-luna`, primed for\nguilt, diff only. All three passes returned `needs-attention`. **Every finding was reproduced before\nbeing fixed** — mutation applied, gate observed *green* (the hole was real), then fixed.\n\n| # | Pass | Finding | Verdict | Disposition |\n|---|---|---|---|---|\n| 1 | 1 | A retry can detach a **newly-claimed** customer — the operation is not naturally idempotent because a detached order is immediately re-claimable | **CONFIRMED** | Fixed → `4fdc778`: required `Idempotency-Key`, unique per order, replay returns the original customer |\n| 2 | 1 | The invariant guard accepts `... OR $1 = $1` — every predicate present, matches every row | **CONFIRMED**, and **pre-existing on `main`** | Fixed → `4fdc778`: whole-statement equality |\n| 3 | 2 | Concurrent first attempts are not linearized into a replay: the loser answers 404 or 500 | **CONFIRMED** | Fixed → `b1f5e21`-range: both paths roll back and re-read on a fresh connection |\n| 4 | 2 | The scanner is blind to `\"orders\"` / `\"public\".\"orders\"` | **CONFIRMED** — a quoted blanket write was *neither refused nor counted* | Fixed: one `ordersTable` pattern, each spelling pinned |\n| 5 | 3 | The concurrency test does not force the race it claims to prove | **CONFIRMED** | Partially fixed → `b576295`; see below |\n| — | 1,2 | *Parse the SQL into an AST* | **REJECTED, stated** | Two statements that change roughly never do not justify a SQL parser in a test guard |\n\n#### Finding 2 is worth separating out\n\n`UPDATE orders SET customer_id = NULL WHERE id = $1 AND status = 'completed' AND customer_id IS NOT\nNULL OR $1 = $1` passed the guard and would have **cleared attribution from every row in the table**.\nIt was **not introduced by this ticket** — the one-statement version on `main` admits the identical\nappend, verified on a detached checkout. Fixed here because this ticket doubles the number of\nstatements to append to.\n\nFinding 4 is the sharper lesson: hardening the *authorization* while leaving the *recognition* weak\nmoves the hole rather than closing it. The whole-statement equality from finding 2 is worth nothing\non a statement the scanner never hands it.\n\n#### Finding 5 — fixed by admitting a limit rather than faking coverage\n\nThe goroutine test passed with the ErrNoRows recovery **deleted**. I rewrote it twice to force the\nbranch deterministically; **both rewrites also passed against the mutated code.** The cause is\nstructural: the fast replay read catches the same-key case before the UPDATE, so the recovery is\nunreachable unless two transactions both pass that read before either commits — which a sequential\ntest cannot arrange and a goroutine race does not reliably produce.\n\nSo both concurrent branches now **state in the code** that they are uncovered, why, and that\ndeleting them leaves the gate green. They rest on READ COMMITTED semantics rather than a red test,\nand the file says so. Shipping a test that *looks* like coverage would have been worse than the\nhonest comment — that is this ticket's own fixture-too-small trap, one level up.\n\nWhat **is** covered got sharper: same-key replay, a different key as a new decision, and a key that\ndid no work getting `ErrOrderNotDetachable` rather than an invented replay. A recovery that always\nreplays and one that never does are equally wrong; only asserting both directions tells them apart.\n\n#### Churn cap\n\nThree passes. **Convergence, not churn** — each pass falsified the previous pass's fix at the root\n(pass 2's finding 4 existed *because* pass 1's fix hardened only authorization; pass 3's finding\nexisted *because* pass 2 added branches). A fourth is permitted under the reset rule; stopping at\nthree, and **the fix diff from `b576295` is reviewed by nobody** — stated here and in the PR rather\nthan implied."
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### Validation — each AC checked against evidence on the MERGED code\n\nMerged as **#187** (squash), `4c995e8` on `main`; branch deleted. CI green (`check` 7m47s,\n`gate-selftest` 44s). Verified by **content** on `origin/main`, not the PR badge:\n`RETURNING OLD.customer_id` present, migration `0019` present.\n\n| AC | Verdict | Evidence |\n|---|---|---|\n| 1. An operator can detach, restoring the order so the real buyer can claim | **MET** | `smoke/unclaim_test.go` drives it end to end through the live stack: guest buys → wrong account claims → rightful buyer **refused** → operator detaches → rightful buyer claims |\n| 2. The plan states who may do this and how they authenticate | **MET** | Internal token, compared inline (ADR-043). ADR-052 § 2 states the consequence plainly: the operator needs the **service credential**, not a support login |\n| 3. Recorded, with who and why | **MET** | `order_attribution_detachments`, written in the same transaction; `actor`/`reason` `NOT NULL` **and** `CHECK (btrim(...) <> '')` |\n| 4. Not a transfer | **MET** | `SET customer_id = NULL` is a literal — no caller-supplied customer can reach the column through this statement |\n| 5. Allowlist grows to exactly two, `TestTheAllowlistCannotBeWidened` covers the new one | **MET, and exceeded** | Count asserted at 2; the widening test grew from 5 cases to **17**, including the `OR $1 = $1` bypass that **pre-dated this ticket** |\n| 6. The plan says whether an un-claimed order is immediately re-claimable | **MET** | Answered **yes**, argued in ADR-052 § 4, and pinned by tests in both the store and the live stack |\n| 7. `make check` green | **MET** | Exit 0 locally and on CI |\n\n#### What I am NOT claiming\n\n**The two concurrent recovery branches are untested.** Deleting either leaves the whole gate green —\nverified, not assumed. They rest on READ COMMITTED semantics rather than a red test, and both say so\nin the code. A PO who wanted every path covered before acceptance would be right to push back; that\nis a decision this gateless run took.\n\n**`make browser` was not run**, and per Decision 1 does not apply: this slice ships no web-UI write\nform. The follow-up that adds the back-office surface must run it."
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-225\n\n| Stage | Duration |\n|---|---|\n| Ready → Planning (claim) | ~6 min — Gate 1 pre-granted at shaping |\n| `agent:planning` (draft, `gpt-5.6-luna`) | ~9 min |\n| `agent:plan-review` (critique, `main-agent`) | ~10 min |\n| Planning → Building | 0 — Gate 2 waived, self-resolved |\n| `agent:coding` (TDD, `main-agent`) | ~70 min, 9 gate runs |\n| `agent:ai-review` (3 passes, `gpt-5.6-luna`) | ~85 min, 5 findings fixed, 4 more gate runs |\n| Building → PO Review → Done | 0 — Gates 3 and 4 waived |\n| **human-wait total** | **0 min** |\n| **Total** | **~3h** Ready → Done |\n\n**diff:** `+1,046 / −24 (12 files)` merged as `4c995e8` (#187).\n**gate runs:** 13 local (2 green) + 2 CI (both green). Every red diagnosed; none was a re-run-and-hope.\n**review:** 3 passes, 6 findings — **5 confirmed and fixed, 1 rejected with a stated reason**.\n\n---\n\n### overrides — blocking objections the agent proceeded past\n\n**1. \"Do not ship\" ×3.** Every pass opened with it. Each pass's findings were **fixed**, not\noverridden, and the next pass ran on the fix. What was not done is a **fourth** pass on the final\nfix diff — the churn cap, applied deliberately, leaving `b576295` reviewed by nobody.\n\n**2. \"Parse the SQL into an AST.\" [high/medium], passes 1 and 2. REJECTED.**\n- *Reviewer:* substring and even whole-string matching are brittle; parse the statement.\n- *Agent:* two statements that change roughly never do not justify a SQL parser inside a test guard,\n  with its own dependency and failure modes. The recognition gap was closed at the regex, where it\n  was.\n- *What an owner might decide differently:* that a guard protecting a security invariant deserves a\n  real parser, and that \"it changes roughly never\" is exactly what people say before it changes.\n\n**3. The draft plan's 26-file scope, REJECTED at plan-review.** Cut to ~8, commerce-only, per the\nticket's own `first_slice`. **Consequence:** the feature has no back-office surface, so an operator\nneeds the service credential. A PO might reasonably call that not-yet-useful.\n\n**4. Accepted with two untested concurrency branches.** See the PO note. The alternative on offer was\na test that *looked* like coverage and was not — I rewrote it twice before concluding the branch is\nunreachable sequentially.\n\n---\n\n**learnings:**\n1. **Hardening authorization while leaving recognition weak moves the hole rather than closing it.**\n   Pass 1 fixed the guard to match whole statements; pass 2 found the scanner never *sees*\n   `UPDATE \"orders\"`, so the stronger check was worth nothing on the statement that mattered. A\n   guard is `recognize → authorize`, and both halves need attacking.\n   → `docs/learnings/`.\n2. **`RETURNING column` in an UPDATE is the NEW value — and so is `RETURNING table.column`.** Only\n   `RETURNING OLD.column` (PostgreSQL 18) gives the prior value. I shipped both wrong spellings\n   before verifying against a live engine. The FK on the audit table is what made it loud; a\n   nullable column would have accepted the row and the trail would have been quietly worthless.\n   → `docs/learnings/`; worth an `AGENTS.md` line given how many audit paths this repo has.\n3. **A test can be unable to reach the branch it is named for.** Three attempts at forcing a\n   same-key race all passed against the mutated code, because the fast replay path shadows the\n   recovery. The honest end state is a comment saying so, not a green test.\n   → `docs/learnings/`, and it strengthens the existing \"fixture too small\" entry.\n4. **A pre-existing bypass found while widening a guard should be fixed in the widening ticket.**\n   `OR $1 = $1` defeated the one-statement version on `main` too. Filing it would have left the\n   ticket that doubled the attack surface pointing at a backlog item.\n\n**retro:**\n1. *What would have made this faster:* the plan draft ignored the ticket's own `first_slice` field\n   and planned 26 files including browser-harness credential provisioning. **Concrete change:** the\n   plan brief template in the skill should quote the ticket's `first_slice` verbatim as a\n   **constraint**, not leave it to be discovered in the readiness object — a drafter that reads the\n   description but not the shaping produces an epic.\n2. *What the humans should change:* three tickets in a row (TKT-198, TKT-229, this) found defects\n   whose common shape is **a guard or fixture that cannot fail**. `AGENTS.md` names the class\n   already. **Concrete change:** make the mutation check an explicit, named step in\n   `quality-practices.md` §2 — \"for each test written, state the mutation, the observed red, and the\n   restore\" — rather than a rule mentioned in the Hard rules that is honoured when remembered.\n3. *Process:* this run's most valuable output was arguably the *rejections* — the 26-file scope, the\n   permanent re-claim block, the AST parser. All three are recorded in `overrides:`. That list is\n   doing real work on gateless runs and should stay mandatory."
    }
  ]
}
