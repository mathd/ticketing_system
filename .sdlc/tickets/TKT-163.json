{
  "key": "TKT-163",
  "type": "Task",
  "parent": "TKT-9",
  "classification": "complex",
  "summary": "Outstanding refund reversals are never retried automatically (TKT-157 follow-up)",
  "description": "Found by the TKT-157 adversarial review (finding F3). Recorded rather than fixed there, for the reasons in ADR-038 §7.\n\nA refund whose money moved but whose tickets were not voided — access down, `ACCESS_URL` unset, or issuance not caught up (503) — is **visible** (`order_refunds.tickets_voided_at IS NULL`) and **retryable** (replay the same idempotency key). Nothing retries it. The reviewer's sharpest point: the caller receives an HTTP **200**, so there is no signal telling it to come back, and the outstanding obligation depends on someone noticing `tickets_voided:false`.\n\nTKT-157's AC asked for *visible and retryable*, which is met. Automatic retry is a different requirement, and a second recovery state machine in commerce is a large cost to add on the strength of a review finding rather than a stated need — so it was deferred here, not dismissed. TKT-161 adds `capacity_returned_at` as a second outstanding obligation of the same shape, which is the natural moment to decide whether one runner should drive both.\n\nRelated in kind: **TKT-146** (no operator path to resolve or unpark a parked recovery order).\n\n**Acceptance Criteria:**\n- [ ] An outstanding reversal is driven to completion without a human replaying the request, or there is an explicitly monitored, documented reconciler that is part of the deployment contract.\n- [ ] Whatever drives it is idempotent against the downstream operations, which already are.\n- [ ] A test drives a downstream outage and proves the obligation completes afterwards.\n- [ ] Decide, and record, whether missing `ACCESS_URL` should become a readiness failure — ADR-038 §7 currently makes it a deliberate degradation.\n\nQuality gates: `make check` green; contract tests; money untouched.",
  "context": {
    "capturedAt": "2026-07-31",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-07-31",
        "rule": "Money is integer minor units + ISO currency code — floats are banned on money paths."
      },
      {
        "id": "PRD-v1",
        "date": "2026-07-31",
        "rule": "Traceability is a must, for money and tickets: money-relevant records land as immutable, hash-chained, signed journal entries from the first order (corrections = compensating entries; read models = projections), and every ticket/entitlement state change (issued, delivered, transferred, resold, exchanged, redeemed, reissued, invalidated) is an append-only lifecycle event linked to its order."
      }
    ],
    "reference": [
      {
        "summary": "Epic lineage and capability map",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "Service layout and ownership",
        "link": "docs/architecture.md"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-021",
        "why": "Every lifecycle event goes through store.appendLifecycle — the verifier asserts one-to-one coverage, so a direct INSERT into lifecycle_events reads as tampering and fails `access verify-lifecycle` in the gate. Canonical forms are pinned by golden literals; changing a byte is a canonical-version migration, not a test update. Also: name the adversary before writing 'tamper-evident'."
      },
      {
        "id": "ADR-010",
        "why": "Inventory's claim transaction is the contention path. Giving capacity back must go through the same row-locked transaction discipline; a plain UPDATE outside it can drive confirmed_quantity negative or double-return under concurrency."
      },
      {
        "id": "ADR-026",
        "why": "Capacity adjustments clamp rather than reject. Returning capacity must not let availability exceed the pool's capacity, and the clamp reasoning is the precedent for what to do when it would."
      },
      {
        "id": "ADR-003",
        "why": "Corrections are COMPENSATING ENTRIES, never mutations. A refund/exchange appends a new signed fact; the original sale fact and its hash link are untouched. The payments journal has a DB trigger that raises on UPDATE/DELETE — any design that 'adjusts' a fact is rejected by the database, not by review."
      },
      {
        "id": "ADR-017",
        "why": "If any domain event payload changes or a new one is published: bump on CONSUMER SEMANTICS, not parse compatibility, and consumers dispatch on `schema` BEFORE decoding `data`. A fixture built from the type under test cannot fail."
      },
      {
        "id": "ADR-028",
        "why": "Fail closed. Responses are validated against the spec; an undeclared status code becomes a 500. Every new endpoint must declare every status it can answer, and a downstream answer commerce cannot validate aborts rather than degrades."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first. A new operation is written in the service's openapi.yaml first, then `openapi_gen.go` / `api-types.gen.ts` are regenerated and COMMITTED before the gate runs — check-generate diffs against HEAD, so an uncommitted regen reads as drift."
      },
      {
        "id": "ADR-030",
        "why": "Commerce and inventory ARE inside the smoke happy-path coverage gate. A new documented 2xx operation must be exercised through the running stack or the suite fails."
      },
      {
        "id": "ADR-022",
        "why": "Migrations run out-of-band via each binary's `migrate` subcommand as a one-shot Compose job. Add the SQL under the service's internal/store/migrations/ with the next number; the server path never migrates."
      }
    ]
  },
  "readiness": {
    "objective": {
      "state": "met",
      "note": "A transient access outage currently leaves refunded tickets valid until a human replays the refund."
    },
    "scope": {
      "state": "met",
      "note": "IN: driving outstanding reversal obligations to completion. OUT: the money path, which is already durable and already recovered."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "Runner vs outbox vs external reconciler is a Planning-time call. Best taken after TKT-161, when both obligations exist and one mechanism can cover both."
    },
    "dependencies": {
      "state": "deferred",
      "note": "Not blocked, but worth sequencing after TKT-161 so the design covers both obligations rather than being retrofitted."
    },
    "approach": {
      "state": "deferred",
      "note": "See uncertainties."
    },
    "first_slice": {
      "state": "met",
      "note": "Drive the ticket-voiding obligation after a simulated access outage."
    },
    "success": {
      "state": "met",
      "note": "Demoable: stop access, refund, restart access, watch the reversal complete with no human action."
    },
    "context_memo": {
      "state": "met",
      "note": "Inherited from TKT-157; ADR-038 §7 records the rejected design and why."
    }
  },
  "links": [
    {
      "type": "found-in",
      "key": "TKT-157"
    }
  ],
  "status": "Building",
  "labels": [
    "agent:coding"
  ],
  "assignee": "agent",
  "pr": null,
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ⚠️ Implementation readiness — CONCERNS\n\nShapeable now, better after TKT-161. Two obligations of identical shape will exist then, and deciding the mechanism against one of them risks a design that has to be retrofitted for the second.\n\n**Concerns:** (1) sequencing, above. (2) The `ACCESS_URL`-as-readiness question is a deployment-contract decision, not purely engineering.\n\n**Verdict:** CONCERNS — proceedable, but hold for TKT-161."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Gate 1 — promoted to Ready (owner, 2026-08-17)\n\n**Outstanding refund reversals are never retried automatically.**\n\nIts deferral has now been *satisfied*, which is why it is promotable. `uncertainties`,\n`dependencies` and `approach` were all deferred with one shared reason: *\"best taken after TKT-161,\nwhen both obligations exist and one mechanism can cover both.\"* **TKT-161 is Done.** The condition\nthe deferral named has been met, so the Planning-time call (runner vs outbox vs external\nreconciler) can now be taken against both obligations rather than retrofitted to one.\n\n**On `approach: deferred` and the money-path carve-out** — `shaping.md` says a deferred approach\ndoes *not* pass when a ticket touches money. Called out rather than glossed: the exception does not\nbite here, because what is deferred is a **mechanism choice among three known options**, not a\ndecision about who may spend or what a trust boundary is. The obligation is already visible\n(`order_refunds.tickets_voided_at IS NULL`) and already retryable by replaying the idempotency key.\nIf Planning finds the choice is actually about authorization, that is a signal to stop and\nre-shape.\n\n**Verified before promoting, not assumed:** all eight DoR items are `met` or `deferred` with none\n`open`, and every `blocked-by` link resolves to a **Done** ticket. Promoted as part of a sweep of\nevery fully-shaped Backlog ticket, on the owner's instruction.\n"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — Ready → Planning\n\nRunning **gateless per `config.gates: autonomous`** (local tracker backend, `config.tracker: local`).\nGate 1 was taken by the owner on 2026-08-17; gates 2-4 are the agent's on this run.\n\n**Selection reason.** Requested by name for an autonomous end-to-end run. Its Gate 1 promotion note\nrecords that the deferral condition — *\"best taken after TKT-161, when both obligations exist and one\nmechanism can cover both\"* — is now satisfied: **TKT-161 is Done**, and `order_refunds` carries both\n`tickets_voided_at` and `capacity_returned_at`. Verified on the board, not assumed.\n\n**Blockers.** Zero open. The only link is `found-in: TKT-157` (**Done**), which is a provenance link,\nnot a dependency. TKT-146 and TKT-165, named as related-in-kind in the description, are Backlog and\nare neither blockers nor in scope.\n\n**Status verified against the board before entering, not taken from the request:** `status: Ready`,\n`labels: []`, `readiness` fully populated with no `open` item. Entering at step 3 (claim) is therefore\ncorrect rather than a skipped shaping.\n\n**Risk verdict — `risk:low` NOT claimed.** Stated explicitly rather than left silent. The work adds a\nrecovery mechanism on the **refund** path; even though ADR-038 §7 and the ticket both scope it to the\nalready-durable reversal obligations and explicitly *not* to the money leg, the ticket's own quality\ngate reads *\"money untouched\"* — a claim that has to be demonstrated, not assumed. Expected to touch a\nservice's startup wiring and plausibly a migration and/or deployment contract (the `ACCESS_URL`\nreadiness question is an explicit AC). Two of the label's four carve-outs are in play. The plan gate\nstands.\n\n**Branch:** `TKT-163-refund-reversal-reconciler` off `origin/main` @ `bf6c054a`.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — `gpt-5.6-luna` (config.models.plan)\n\n**Drafted by `gpt-5.6-luna` via the codex companion**, effort high, read-only, against the real\ntree at `origin/main` @ `bf6c054a`. Verified read-only after the run: `git status --short` was\nempty, so no sandbox breach (the TKT-104 shape). Posted verbatim below, apart from the codex\nsession trailer.\n\nThe critique (`config.models.planReview: main-agent`) follows in the next comment; this is the\ndraft as received, unamended, so the delta is auditable.\n\n---\n\n# Approach\n\nAdd a dedicated `internal/reversal` runner for completed refunds whose `tickets_voided_at` or `capacity_returned_at` remains `NULL`.\n\nThe runner will:\n\n1. Select outstanding, retryable refund rows with a joined `orders → reservations` read so `OrganizerID` and `HoldID` are populated. `LookupRefundByID` cannot be reused because it intentionally omits `HoldID` (`services/commerce/internal/store/refunds.go:242-256`), while `BindOrderRefund` shows the required join (`services/commerce/internal/store/refunds.go:106-136`).\n2. Claim rows with a lease, claim token, `SKIP LOCKED`, next-attempt time, and attempt counter.\n3. Drive each row sequentially through the existing `refunds.Service.DriveReversal`, preserving its ticket-before-capacity ordering (`services/commerce/internal/refunds/service.go:110-126`).\n4. Requeue incomplete transient work with capped exponential backoff.\n5. Exclude known permanently undischargeable partial seated refunds from repeated claims while exposing them through metrics and documentation. The reservation’s `seat_identities` presence is the seated marker (`services/commerce/internal/store/migrations/0013_reservation_seats.sql:22-25`), and inventory explicitly refuses partial seated returns (`services/inventory/internal/store/refund_returns.go:113-133`).\n6. Add a database guard preventing `capacity_returned_at` from being recorded before `tickets_voided_at`.\n\nThis deliberately exercises the “adding one later” option rejected in ADR-038 §7 (`docs/adr/ADR-038-refund-reversal-ticket-voiding.md:138-146`). The requirement now exists, so the earlier cost argument no longer applies.\n\nThe new runner remains separate from both existing runners:\n\n- It must not extend `internal/recovery`: checkout recovery has different eligibility and terminal-state semantics (`services/commerce/internal/recovery/runner.go:1-11`).\n- It must not merge into `internal/bulkrefund`: its package explicitly rejects merging state machines (`services/commerce/internal/bulkrefund/runner.go:1-11`), and bulk refund only sees orders in cancellation books, not ordinary staff refunds (`services/commerce/internal/bulkrefund/runner.go:111-139`).\n- An external cron or admin replay endpoint is rejected because it would still need durable claiming, backoff, fencing, and the same commerce database query, while making completion depend on another deployment component.\n- An outbox/event-driven retry is rejected because the failure is a durable row obligation, not a missing event; access outages and issuance lag require revisiting existing rows.\n\nADR-040’s limitation remains true for the bulk runner, but does not apply to this dedicated reconciler: `DriveReversal` accepts the persisted `Refund` directly and does not require the original idempotency key (`services/commerce/internal/bulkrefund/runner.go:44-49`). Amend ADR-040 to clarify that its “reported, not repaired” rule is scoped to cancellation runs.\n\n# Open decisions\n\n- **`ACCESS_URL` readiness — Recommended: readiness failure, liveness preserved.** Split the currently shared handlers (`services/commerce/cmd/commerce/main.go:219-233`) so `/healthz` remains a DB/NATS liveness check while `/readyz` adds an `access_configured` check. The current shared handler would make a readiness failure also fail liveness (`shared/go/httpx/healthz.go:37-57`), so adding the check to the existing handler is rejected. The runner will not claim ticket-voiding work when the URL is absent, but will continue capacity-only work and emit outstanding metrics. Compose already supplies `ACCESS_URL` (`compose.yaml:238-244`).\n\n  The most reversible option is to retain optional startup and use only metrics plus an alert. It is rejected because a deployment with no access URL would otherwise accept refunds while silently accumulating an obligation.\n\n- **Retry policy — Recommended: unlimited transient retries with capped exponential backoff.** A permanent attempt limit would reintroduce the original “money moved, tickets still valid, no driver” failure after a sufficiently long outage. Use environment-tunable interval and batch defaults, with a maximum per-row delay.\n\n- **Partial seated refunds — Recommended: leave the obligation outstanding and exclude it from automatic claims.** Do not stamp `capacity_returned_at`, invent a terminal success state, or repeatedly call inventory. Expose a gauge and oldest-age metric. TKT-164 can later make those rows repairable by adding ticket-to-seat identity.\n\n- **Exchanges — Recommended: out of scope.** The ticket and COS name refunds, and the readiness note concerns the two refund obligations. Exchanges need a separate eligibility/read model and follow-up, referenced by the interrupted-exchange work as TKT-167 (`docs/reviews/2026-08-08-sdlc-board-validation.md:102`). The most reversible option is a shared polymorphic reconciler, but that would prematurely couple refund and exchange state machines.\n\n# Definition of Done\n\n- [ ] **COS 1:** In the composed stack, a refund created while Access is stopped is automatically revisited after Access restarts; both refund obligation timestamps become non-null without replaying the request. Permanently undischargeable partial seated rows remain visibly outstanding and are monitored.\n- [ ] **COS 2:** Repeated claims, worker crashes, lease expiry, and duplicate runner instances converge on one access void, one inventory return, and stable timestamps; no money operation is added or retried.\n- [ ] **COS 3:** A gateway smoke test stops Access, performs a refund, confirms the obligation remains outstanding, restarts Access, and confirms both obligations complete afterward.\n- [ ] **COS 4:** ADR-038 explicitly records the reconciler and the new `ACCESS_URL` readiness decision; `/readyz` fails when `ACCESS_URL` is absent while `/healthz` remains usable.\n- [ ] `make check` passes after regenerated artifacts, if any, are committed.\n- [ ] Commerce contract and PostgreSQL smoke tests pass, including the new claim, lease, join, permanent-row, ordering-guard, and migration tests.\n- [ ] Payments tables, payment facts, payment amounts, and payment calls are unchanged by the reconciler implementation.\n\n# Files\n\nNew files:\n\n- `services/commerce/internal/reversal/runner.go` — dedicated leased runner, retry/backoff policy, shutdown handling, and OTel metrics.\n- `services/commerce/internal/reversal/dbstore.go` — PostgreSQL implementation of the runner’s store port.\n- `services/commerce/internal/reversal/runner_test.go` — fake-port decision-table tests.\n- `services/commerce/internal/store/reversal_smoke_test.go` — real-PostgreSQL tests for selection, joins, leases, fencing, backoff, and permanent seated exclusions.\n- `smoke/refund_reversal_recovery_test.go` — composed-stack outage/restart demonstration using the existing Docker helpers (`smoke/smoke_test.go:344-350`).\n- `services/commerce/internal/store/migrations/0021_refund_reversal_reconciliation.sql` — next migration after the verified current highest migration, `0020_reseller_credentials.sql:1`; adds claim/backoff columns and the refund capacity-after-void constraint.\n- `docs/adr/ADR-062-refund-reversal-reconciliation.md` — durable runner, retry, monitoring, replica, and permanent-obligation decision.\n\nModified files:\n\n- `services/commerce/internal/store/refunds.go` — add the joined outstanding-refund claim/read transitions and require `tickets_voided_at IS NOT NULL` in `MarkRefundCapacityReturned` (`services/commerce/internal/store/refunds.go:384-392`).\n- `services/commerce/cmd/commerce/main.go` — instantiate the runner through `srvHandler.Refunds()` (`services/commerce/internal/api/server.go:146-159`), add interval/batch configuration, register OTel gauges, split liveness/readiness, and place shutdown before dependency workers.\n- `compose.smoke.yaml` — set short reversal interval and batch values so the outage test is bounded.\n- `docs/configuration.md` — document reversal runner settings and the `ACCESS_URL` readiness contract.\n- `docs/development.md` — document operator-visible outstanding, permanently blocked, and retrying states.\n- `docs/adr/ADR-038-refund-reversal-ticket-voiding.md` — amend §7 from “nothing retries on its own” to the TKT-163 reconciler decision and record the readiness change.\n- `docs/adr/ADR-040-event-cancellation-bulk-refund-runs.md` — clarify that its non-repair rule remains a bulk-run limitation, not a system-wide prohibition.\n\nNo OpenAPI or generated-file changes are planned. There is no new HTTP operation; the existing refund operation and response already declare both reversal booleans (`services/commerce/api/openapi.yaml:378-390`, `services/commerce/api/openapi.yaml:1047-1064`). Therefore `services/commerce/internal/api/openapi_gen.go` and frontend generated types remain unchanged.\n\n# Test plan\n\n## Test seam\n\n- The runner’s decisions—claim, skip, requeue, backoff, shutdown release, and fencing—belong in `unit-with-fakes`, following the existing port pattern (`services/commerce/internal/bulkrefund/runner.go:26-49`).\n- SQL eligibility, tenant scoping, `HoldID` population, claim leases, claim tokens, indexes, and the capacity-after-void guard belong in `store smoke` against PostgreSQL. A fake must not prove SQL predicates.\n- The downstream outage and eventual completion belong in `gateway smoke`, because only the composed stack can stop and restart Access and observe the durable result.\n- The existing `DriveReversal` call remains the sole reversal unit; it already enforces void-before-capacity (`services/commerce/internal/refunds/service.go:121-126`).\n\n| Test | Tier | Invariant | What breaks it |\n|---|---|---|---|\n| `TestRunnerRetriesAnIncompleteReversal` | unit-with-fakes | An incomplete reversal is revisited on a later eligible pass and a completed reversal is not revisited. | Finalizing the first incomplete result, omitting the second claim, or treating `TicketsVoided` as the only completion condition. |\n| `TestRunnerBackoffPreventsSamePassSpin` | unit-with-fakes | A transiently failing row is attempted once per scheduled time, not repeatedly in one pass. | Releasing the row with `next_attempt_at = now()` or ignoring the backoff predicate. |\n| `TestRunnerFencesLeaseReleaseByClaimID` | unit-with-fakes | A stale worker cannot clear or reschedule a row claimed by a successor. | Removing the claim-token condition from release or completion. |\n| `TestRunnerReleasesUndrivenClaimsOnShutdown` | unit-with-fakes | Rows claimed but not driven during shutdown become immediately reclaimable and do not consume an attempt. | Reusing the cancelled context for release, or leaving undriven rows leased. |\n| `TestClaimOutstandingRefundJoinsReservation` | store smoke | A selected refund carries the organizer and hold belonging to its reservation. | Reusing `LookupRefundByID`, removing the `orders → reservations` join, or omitting `reservations.hold_id`. |\n| `TestClaimOutstandingRefundIsOrganizerScoped` | store smoke | A caller can claim only its organizer’s refunds. | Removing the organizer predicate from either `order_refunds` or `reservations`. |\n| `TestClaimLeaseExcludesLiveRowsAndReclaimsExpiredRows` | store smoke | A live claim is invisible to another worker, while an expired claim is reclaimable. | Removing lease filtering, `SKIP LOCKED`, or expiry handling. |\n| `TestCapacityMarkerRequiresTicketsVoided` | store smoke | Capacity cannot be recorded as returned while ticket voiding is still outstanding. | Removing `tickets_voided_at IS NOT NULL` from `MarkRefundCapacityReturned` or dropping the database constraint. |\n| `TestPartialSeatedRefundIsVisibleButNotClaimable` | store smoke | A partial seated refund remains outstanding and monitored but is not repeatedly selected for impossible capacity work. | Removing the seated-shape predicate, treating a partial return as complete, or omitting it from the monitored count. |\n| `TestFullSeatedRefundRemainsClaimable` | store smoke | A full seated refund is not mistaken for the permanent partial case. | Comparing only `seat_identities IS NOT NULL` without comparing refund quantity to reservation quantity. |\n| `TestRefundReversalCompletesAfterAccessRestart` | gateway smoke | A refund made while Access is unavailable leaves both obligations open; after Access restarts, both timestamps become non-null without another refund request. | Removing runner wiring, selecting the wrong rows, failing to populate `HoldID`, failing to retry, or changing the worker ordering. |\n| `TestRefundReversalDoesNotReturnCapacityBeforeVoid` | gateway smoke | While Access is stopped, inventory capacity remains unavailable even though the refund’s money leg has completed. | Removing the `DriveReversal` short-circuit, calling inventory independently, or removing the database ordering guard. |\n| `TestMissingAccessURLFailsReadinessButNotLiveness` | gateway smoke or commerce API test | Missing `ACCESS_URL` produces a degraded readiness response while the liveness endpoint remains usable. | Mounting the same handler on both paths, omitting the access readiness check, or treating an absent URL as healthy. |\n\nThe outage test will stop the `access` Compose service, submit the existing internal refund operation, read the commerce row and inventory state, restart `access`, and poll the database until both timestamps are non-null. It will use the existing refund endpoint rather than adding a new documented 2xx operation, so the coverage gate remains unchanged.\n\n# Risks / pre-mortem\n\n- **Two commerce replicas double-drive a refund.** A lease scan without `SKIP LOCKED`, a lease shorter than the batch, or unfenced release lets two workers act concurrently. Use row leases, claim tokens, sequential batches, and a lease sized from the batch’s maximum two downstream calls. Access and inventory idempotency receipts remain the final convergence mechanism.\n- **Capacity is returned before tickets are voided.** A second caller or future refactor bypasses `DriveReversal`. Keep both the application guard and the new SQL `CHECK`/`WHERE` guard; test the outage state before Access restarts.\n- **A permanently partial seated return spins forever.** Inventory refuses it by design (`services/inventory/internal/store/refund_returns.go:130-133`). Exclude the known shape, leave the timestamp null, and expose a gauge/oldest-age metric. TKT-164 owns the eventual repair.\n- **Inventory’s pool lock is overloaded.** Concurrent fan-out from every runner replica turns reconciliation into an on-sale contention source. Process claims sequentially per runner, keep all commerce transactions short, and perform the inventory HTTP call outside database locks. Inventory already takes the pool lock before the claim lock (`services/inventory/internal/store/refund_returns.go:52-81`).\n- **The query never selects the rows that matter.** A query based only on `order_refunds` will omit `HoldID`; a query missing `status='completed'` will process pending money; a query using the wrong organizer column will cross tenants; a query filtering only `tickets_voided_at` will miss capacity-only obligations. Seed each mechanism independently and assert deletion of each seed makes the relevant test fail.\n- **Missing `ACCESS_URL` leaves silent debt.** Readiness must fail, while liveness remains available for diagnostics. Metrics must distinguish missing configuration from a downstream outage.\n- **A lease expires during a downstream call.** The successor may issue a duplicate HTTP request, but the downstream operations are idempotent. The old worker’s metadata write must be claim-token fenced; the design cannot fence an already-issued external request.\n- **A successful test proves only the fake.** Keep runner decisions in fake-port tests and all SQL predicates in PostgreSQL smoke tests, following the existing split (`services/commerce/internal/bulkrefund/runner.go:26-31`).\n\n# Out of scope\n\n- **Money and payments:** no changes to `refundPayment`, payment facts, payment journal, or payment APIs; ADR-003’s compensating-entry rules remain untouched.\n- **Exchanges:** no `order_exchanges` sweep; carry interrupted exchange reconciliation in TKT-167.\n- **TKT-146:** no work is absorbed from that ticket because its scope is not documented in the repository at HEAD.\n- **TKT-164:** no ticket-to-seat association is added; partial seated returns remain monitored but undischargeable.\n- **TKT-165:** no inventory-side caller proof or authorization redesign; the reconciler continues using the existing internal inventory endpoint.\n- **`bulkrefund`:** it continues to report obligations on refunds it did not create; this runner repairs them independently.\n- **New HTTP operations:** no admin retry endpoint, report endpoint, or OpenAPI change.\n- **Offline scanner revocation:** no change to the offline admission limitation recorded for TKT-162 in ADR-038."
    },
    {
      "stage": "plan-review",
      "kind": "plan",
      "body": "<!-- sdlc:stage=plan-review kind=plan -->\n### Adversarial critique of the draft — `main-agent` (config.models.planReview)\n\nDrafter was `gpt-5.6-luna`; reviewer is the session model. The pair differs, so the cross-model\ninvariant holds. Method per `quality-practices.md` §1: Cons-against-Decision first, then every seam\nand wire-level fact verified with `git grep -n` at HEAD, then one named lens.\n\n**Verdict: ACCEPT the approach, REJECT two mechanisms, AMEND four points.** The core — a dedicated\nleased `internal/reversal` runner driving the existing `refunds.Service.DriveReversal` — is right,\nwell-argued against ADR-038 §7 and against both runner precedents, and its \"no new HTTP operation\"\ncall correctly keeps the ADR-030 coverage gate out of play. The two rejections are both about the\nsame failure: **predicting a downstream refusal from state commerce cannot see.**\n\n---\n\n#### Seams verified — all real (no hallucinated seam)\n\nEvery file, symbol and line range the draft cites resolves at HEAD. Spot-checked and confirmed:\n`refunds.Service.DriveReversal` (`internal/refunds/service.go:110-126`), the `BindOrderRefund`\njoin it points to for `hold_id` (`internal/store/refunds.go:120-124`), `LookupRefundByID`'s\ndocumented omission of `HoldID` (`internal/store/refunds.go:247-249`), both runner package docs\n(`recovery/runner.go:1-11`, `bulkrefund/runner.go:1-11`), `srvHandler.Refunds()`\n(`internal/api/server.go:149`), `httpx.Healthz` (`shared/go/httpx/healthz.go:37-58`),\n`ErrPartialSeatedReturn` (`inventory/internal/store/refund_returns.go:130-133`), the pool-lock-first\nordering (`inventory/.../refund_returns.go:52-55, 74`), `ACCESS_URL` at `compose.yaml:244`, and\n`0020_reseller_credentials.sql` as the highest migration — so **0021 is correct**.\n\nWire-level facts also verified, not just symbols: the draft's claim that no OpenAPI change is needed\nholds — `refundOrder` at `services/commerce/api/openapi.yaml:380` already declares the `Refund`\nschema carrying both booleans, and no new operation means `uncovered2xxOps()` never grows\n(`smoke/coverage_test.go:90`, `TestMain` at `:126-135`).\n\n---\n\n#### ❌ REJECT 1 — the seated-exclusion predicate cannot be written in commerce (Approach §5, `TestPartialSeatedRefundIsVisibleButNotClaimable`, `TestFullSeatedRefundRemainsClaimable`)\n\nThis is the draft's most consequential mechanism and it does not work. Three independent reasons,\neach fatal on its own:\n\n1. **Commerce never reads `seat_identities`.** `git grep -n seat_identities services/commerce/internal/store/*.go` excluding tests returns **nothing**. The column exists (`0013_reservation_seats.sql:25`) but no commerce Go code reads it. The draft treats it as an available marker; it is currently write-only from commerce's side.\n\n2. **Inventory's refusal does not key on seat *identities* — it keys on `claim_seats` rows in the *inventory* database.** `refund_returns.go:127-130`: `SELECT count(*) FROM claim_seats WHERE claim_id=$1`, and the comment two lines up is explicit that seatedness is **any** seat row, released or not, and that inferring seatedness from live rows is wrong (it says so as an ai-review pass-2 finding). Commerce cannot query `claim_seats`; it is a different service's database, and ADR-002/ADR-007 forbid reaching into it.\n\n3. **Even with perfect seatedness knowledge the predicate is still wrong, because \"partial\" is not a property commerce can compute.** The refusal fires when `returned != 0 || quantity != quantityTotal` (`refund_returns.go:131-133`). `returned` is `claims.returned_quantity` — inventory-side state that moves when *any other refund of the same order* returns capacity. So a refund for the reservation's full quantity is still a **partial** return if an earlier refund already returned some. The draft's rule (\"compare refund quantity to reservation quantity\") is wrong in both directions: it excludes rows inventory would accept, and admits rows inventory will refuse.\n\n**Amendment — do not predict the refusal, observe it.** Delete the seated predicate entirely. The\ncodebase already has the right mechanism for \"a downstream refusal this row will never survive\":\nbound attempts and **park**, exactly as `ReleaseStuckOrder` does\n(`internal/store/recovery.go:122-133`) — capped exponential backoff\n`least(power(2, least(attempts,8)), interval '5 minutes')` plus\n`parked_at = CASE WHEN attempts >= Max THEN now() END`, with the claim scan filtering\n`parked_at IS NULL`. A permanently-refused seated row then costs ~10 attempts spread over\nincreasing backoff and parks itself, visibly, forever after — no cross-service inference, no\npredicate that can drift from inventory's rule. This also generalises: it parks *any* permanently\nundischargeable row, including shapes nobody has thought of, which a hand-written predicate by\nconstruction cannot.\n\nThe two tests above are replaced by `TestPermanentlyRefusedReversalParksAfterItsAttemptBudget`\n(store smoke) and a unit test that a 409 from inventory is charged an attempt while a 503 from\naccess is too — see the amended test plan below.\n\n---\n\n#### ❌ REJECT 2 — \"unlimited transient retries\" contradicts the parking this plan needs (Open decisions §2)\n\nThe draft recommends *\"unlimited transient retries with capped exponential backoff\"*, reasoning\nthat an attempt limit would reintroduce the original failure after a long outage. **That reasoning\nis sound in isolation and incompatible with REJECT 1's fix**, and it is also the draft's own Con\narguing against its own Decision: §Risks says *\"A permanently partial seated return spins forever\"*\nis a top risk, then §Open decisions removes the only bound that would stop it. With the seated\npredicate deleted, unlimited attempts means the permanently-refused row retries forever — the exact\nincident the draft names.\n\n**Amendment — bound attempts, and make the bound restartable.** Take ADR-040 §5's distinction\nliterally, because it was learned the hard way there (\"both halves of this were wrong in the first\nimplementation and were caught in review\"):\n\n- A **definite refusal** (inventory 409 `ErrPartialSeatedReturn`, access 409 `ErrRefundBatchConflict`) charges an attempt and, on exhaustion, parks.\n- A **\"not yet\"** (access **503** `ErrTicketsNotIssued` — `services/access/internal/api/refunds.go:50-55`, which is *the ticket's primary case*) and any ambiguous/transport failure charges an attempt and backs off.\n- The draft's real concern is answered not by removing the bound but by **the attempt counter resetting on progress**: a pass that discharges *one* of the two obligations has made progress and must reset `attempts` to 0, so a long outage followed by recovery does not arrive with the budget already spent.\n\n`MaxRecoveryAttempts = 10` (`recovery.go:50`) is the house precedent; `bulkrefund` uses 5 for\nambiguous failures only (`runner.go:109`). Pick 10 and say why in the ADR.\n\n---\n\n#### ⚠ AMEND 1 — the `ACCESS_URL` readiness recommendation is right, but its stated rationale is refuted by an existing ADR\n\nThe draft recommends splitting `/healthz` and `/readyz` so readiness fails when `ACCESS_URL` is\nabsent. **The split and the check are accepted**, but the draft does not know that this repo has\nalready litigated readiness-gating-on-a-dependency and decided against it — `compose.yaml:386-391`,\nciting ADR-021 §D6:\n\n> readiness: `/readyz` is what the container healthcheck probes, so gating it on NATS would let a\n> broker blip close every turnstile — the customer-denying failure §D6 chose fail-open to avoid.\n\nThat precedent must be addressed, and it **distinguishes cleanly**: the rejected pattern gates\nreadiness on a *runtime dependency's liveness* (a blip flips it), while this checks a **static\nstartup configuration fact** — is the URL configured — which cannot flap. A misconfigured deployment\nshould never take traffic; a briefly-unreachable access should. State that distinction in the ADR or\nthe next reviewer will read this as contradicting ADR-021 §D6.\n\nTwo corrections to the mechanics:\n\n- Commerce's container healthcheck probes **`/healthz`**, not `/readyz` (`main.go:93-101`, `compose.yaml:511`), and the gateway `depends_on: commerce: service_healthy` (`compose.yaml:504`). So the split is **safe** — a failing `/readyz` will not deadlock compose startup. The draft asserts the split is needed to preserve liveness but never checks which endpoint the healthcheck actually probes; it is right by luck. Verify it in the plan, because if it had been `/readyz` the whole stack would fail to start.\n- **Check configuration, never reachability.** A probe that dials access would reintroduce exactly the flapping ADR-021 §D6 rejected.\n\n**Test-seam correction:** `TestMissingAccessURLFailsReadinessButNotLiveness` is listed as\n*\"gateway smoke or commerce API test\"*. It cannot be a gateway smoke test — the smoke stack always\nsets `ACCESS_URL` (`compose.yaml:244`), and removing it would break every other smoke test in the\nsame stack. It must be a **commerce unit/API test** that builds the handler pair with an empty URL.\nPin the seam now; \"or\" in a test plan is how a seam gets decided badly at 2am.\n\n---\n\n#### ⚠ AMEND 2 — the `MarkRefundCapacityReturned` SQL guard reverses a decision TKT-161 took deliberately\n\nThe draft adds `AND tickets_voided_at IS NOT NULL` to the mark, plus a CHECK constraint. **Accepted**\n— it closes a real asymmetry with the exchange side, which carries the guard in both the WHERE clause\n(`store/exchanges.go:605-611`) and a constraint (`0011_exchange_capacity_return.sql:22-24`) — but the\ndraft presents it as an obvious gap when it is a **reversal of a stated choice**.\n`0009_refund_capacity_return.sql:7-11` says: *\"Independent STORAGE, not independent EXECUTION…\nCommerce enforces that ordering — it will not attempt the return until `tickets_voided_at` is set.\"*\nThat was a decision, not an oversight.\n\nThe argument for reversing it is exactly this ticket: **TKT-163 adds a second call site**, and\n\"one caller enforces it correctly\" stops being a sufficient guarantee once callers multiply. Say\nthat in the ADR — an unargued reversal of a documented decision is how the next ticket re-reverses it.\n\nSafety verified so no migration hazard: `capacity_returned_at` has exactly **one** writer in\ncommerce (`refunds.go:390`; the other hits at `exchanges.go` are a different table), and it fires\nonly downstream of `DriveReversal`'s guard, so no existing row can violate the constraint and the\n`ALTER TABLE … ADD CONSTRAINT` validation pass cannot fail on live data.\n\n---\n\n#### ⚠ AMEND 3 — one factual error: TKT-167 is not a home for exchange reconciliation\n\nOpen decisions §4 defers exchanges to *\"TKT-167\"*, citing\n`docs/reviews/2026-08-08-sdlc-board-validation.md:102`. That line is a board inventory listing\nTKT-167 as *\"interrupted exchange\"* — and **TKT-167 is already Done and merged**:\n`60449fec TKT-167: resume an interrupted exchange from the basis it persisted (#231)`. It is not an\nopen home for anything.\n\n**The scoping decision itself is correct and is accepted** — the ticket and all four COS name\nrefunds; exchanges have a genuinely different eligibility model (their only discharge point is\n`POST /internal/exchanges/{id}/tickets-switched`, `api/exchanges.go:763-818`, and their only retry\nis access's JetStream redelivery driven by a 502 at `:801-807`). But the deferral must name a **new**\nfollow-up ticket, filed at closeout, not a merged one. Fixed in `plan-final`.\n\n---\n\n#### ⚠ AMEND 4 — two smaller corrections\n\n- **`compose.smoke.yaml`** is listed under modified files to shorten the interval. Keep the change but note the constraint it creates: the outage test's polling deadline must be derived from the configured interval, not a hard-coded sleep, or it becomes a flake the moment the default changes.\n- **`TestRunnerReleasesUndrivenClaimsOnShutdown`** is correct and important, and the draft correctly identifies the fresh-context requirement. Name the precedent so the implementer copies rather than reinvents: `recovery.Runner.releaseUndriven` (`recovery/runner.go:266-286`) uses a fresh 5s `context.Background()` *and* conditions on the claim token so a lapsed-and-reclaimed row is left alone.\n\n---\n\n#### ✅ Accepted without change\n\nThe rejections of `internal/recovery` extension, `bulkrefund` merger, an external cron, and an\nevent-driven outbox — each with a stated deciding reason, and the `bulkrefund` one correctly argued\nagainst that package's own doc. The observation that ADR-040's *\"reported, not repaired\"* rule is\nscoped to bulk runs and does not bind a dedicated reconciler (because `DriveReversal` takes a\npersisted `Refund`, not an idempotency key) — correct, and the ADR-040 clarification is the right\nway to record it. The no-new-HTTP-operation call. The three-tier test seam. The `orders → reservations`\njoin requirement. The pre-mortem's \"the query never selects the rows that matter\", which is the\nright paranoia and directly implements the repo's *\"if I delete this seed, what goes red?\"* rule.\n\n---\n\n#### Lens applied — pre-mortem, on the *reviewed* plan\n\n*Assume this shipped and caused an incident.* The three most plausible, after the amendments:\n\n1. **A parked row is invisible and nobody looks.** Parking converts \"retries forever\" into \"stops silently\". The gauge is therefore not optional decoration — it is the mechanism that makes parking honest. Require `..._parked` and `..._oldest_age_seconds` gauges, following `access/internal/lifecyclejob/runner.go:168-194`, and note that commerce registers **zero** metrics today so this is the service's first `otel.Meter` call.\n2. **The reconciler drives a refund whose money leg is still `pending`.** The claim query must filter `status='completed'`. A pending refund has not moved money; voiding its tickets would be a reversal of a sale that has not happened. The draft names this in its pre-mortem — promote it to an explicit predicate with its own store-smoke test.\n3. **Two obligations, one attempt counter.** If voiding succeeds and capacity fails, the reset-on-progress rule (REJECT 2) must fire, or the row burns its budget on the half that already works. This is the interaction the two amendments create between them, and it is exactly the shape AGENTS.md warns about — a round of fixes needs a review of the fixes' *interaction*.\n\n**Introduced at plan-review, reviewed by no second model:** the parking-instead-of-predicting\nmechanism (REJECT 1), the reset-attempts-on-progress rule (REJECT 2), the\nstatic-config-vs-runtime-dependency distinction for readiness (AMEND 1), and the\n`status='completed'` predicate (lens item 2). These reach Gate 2 reviewed by nobody but their\nauthor; they are named again in `plan-final` and are the first things the ai-review pass should\nattack.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan final — the `kind=plan` draft as amended below is the plan\n\nA **delta**, not a restatement: resume reads the draft and this together. Gate 2 taken by the agent\nper `config.gates: autonomous`; every open decision is resolved here with its rationale, per the\nrule that a self-made gate decision must be recorded rather than merely acted on.\n\n---\n\n#### Decisions taken at Gate 2 (agent, no human)\n\nResolution rule applied as written: prefer what the critique recommends; where it makes no\nrecommendation, take the **most reversible** option.\n\n**D1 — Seated/permanent rows: park after a bounded attempt budget, do not predict the refusal.**\nCritique REJECT 1. The draft's predicate cannot be written correctly in commerce — no commerce code\nreads `seat_identities`, inventory decides seatedness from `claim_seats` in *its own* database, and\n\"partial\" depends on `claims.returned_quantity` which commerce cannot see. Adopt the\n`ReleaseStuckOrder` shape instead (`internal/store/recovery.go:122-133`): capped exponential\nbackoff, park at the budget. Also the more reversible option — it adds no cross-service inference\nthat could drift from inventory's rule.\n\n**D2 — Bound attempts at 10, and reset the counter on progress.** Critique REJECT 2. Unlimited\nretries and D1's parking are incompatible, and the draft's own §Risks argues against its §Open\ndecision here. `MaxRecoveryAttempts = 10` is the house precedent (`recovery.go:50`). The draft's\nreal concern — a long outage exhausting the budget — is answered by **resetting `attempts` to 0\nwhenever a pass discharges an obligation it had not discharged before**, not by removing the bound.\n\n**D3 — `ACCESS_URL` becomes a readiness failure; `/healthz` and `/readyz` are split.** COS 4,\nanswered rather than deferred. Check **configuration presence only, never reachability**. The\nexisting precedent against readiness-gating (`compose.yaml:386-391`, ADR-021 §D6) is about a\n*runtime dependency's liveness*, which flaps; a missing env var is a static startup fact that cannot.\nVerified safe: commerce's container healthcheck probes `/healthz` (`main.go:93-101`,\n`compose.yaml:511`) and the gateway waits on `service_healthy` (`compose.yaml:504`), so a failing\n`/readyz` cannot deadlock compose. This is the *least* reversible decision in the plan and is taken\ndeliberately: ADR-038 §7 made absence a silent degradation, and a reconciler that can never run\nbecause the URL is unset converts a visible outstanding obligation into an invisible one. Recorded\nin the ADR as a deployment-contract change.\n\n**D4 — Refund side gains the SQL/CHECK ordering guard.** Critique AMEND 2. Reverses TKT-161's\n\"application enforces it\" choice, and the reversal is argued rather than assumed: this ticket adds a\n**second call site**, and one-caller-enforces-it stops being sufficient once callers multiply.\nVerified there is exactly one writer of `capacity_returned_at` in commerce (`refunds.go:390`) and it\nfires only downstream of `DriveReversal`'s guard, so the constraint cannot fail validation on\nexisting rows.\n\n**D5 — Exchanges are out of scope; the deferral names a NEW ticket.** Critique AMEND 3. The draft\ndeferred to TKT-167, which is **already Done and merged** (`60449fec`). The scoping is right — all\nfour COS name refunds, and exchanges discharge through a different endpoint with JetStream\nredelivery as their retry — but a merged ticket is not a home. **A new Backlog ticket is filed at\ncloseout** for the `order_exchanges` sweep, with the gap documented (their only retry is access's\nredelivery driven by a 502 at `api/exchanges.go:801-807`; if that consumer dead-letters, nothing in\ncommerce revisits those rows).\n\n**D6 — The claim query filters `status='completed'`.** Pre-mortem lens item 2. A pending refund has\nnot moved money; voiding its tickets would reverse a sale that has not happened. Explicit predicate,\nwith its own store-smoke test.\n\n**D7 — Metrics are required, not optional.** Parking converts \"retries forever\" into \"stops\nsilently\", so the gauge is the mechanism that makes parking honest — this is what earns COS 1's\nsecond branch (\"explicitly monitored… part of the deployment contract\"). Follow\n`access/internal/lifecyclejob/runner.go:168-194`: an `ObserveMetrics(meter) error` method registering\n`Int64ObservableGauge`s plus one `RegisterCallback` querying the store. Note this is commerce's\n**first** `otel.Meter` call — the MeterProvider is already live (`main.go:191`), so no new wiring.\nMirror that file's doc: they are observability, not a gate.\n\n---\n\n#### Amendments to the draft\n\n1. **Approach §5 is deleted and replaced** by D1 (park, don't predict). The `seat_identities` marker and the quantity comparison are removed entirely.\n2. **Open decisions §2 replaced** by D2 (bounded at 10 + reset on progress).\n3. **Open decisions §1 kept, rationale corrected** per D3 — the split is right, the reason must be the static-config-vs-runtime-dependency distinction, and the plan must state that `/healthz` is what the healthcheck probes (the draft got this right by luck, without checking).\n4. **Open decisions §4 kept, TKT-167 reference removed** per D5.\n5. **Files:** unchanged except that the migration `0021_refund_reversal_reconciliation.sql` now adds claim/lease/attempts/**next_attempt_at**/**parked_at**/last_error columns (no seated predicate), plus the partial index for the claim scan and the capacity-after-void constraint. Migration number **0021 verified correct** (highest existing is `0020_reseller_credentials.sql`). No OpenAPI or generated-file change — confirmed, and it is what keeps the ADR-030 coverage gate out of play.\n6. **Test plan:** `TestPartialSeatedRefundIsVisibleButNotClaimable` and `TestFullSeatedRefundRemainsClaimable` are **replaced** by:\n   - `TestPermanentlyRefusedReversalParksAfterItsAttemptBudget` (store smoke) — invariant: *a refusal that repeats forever stops being retried and becomes visible instead*. Breaks if: `parked_at` is never set, the claim scan omits `parked_at IS NULL`, or attempts are not charged.\n   - `TestProgressResetsTheAttemptBudget` (unit-with-fakes) — invariant: *discharging an obligation restores the row's full retry budget*. Breaks if: the reset is omitted (a long outage then parks a row that was recovering) or fires when nothing was discharged (a row never parks).\n   - `TestDefiniteRefusalAndNotYetAreBothCharged` (unit-with-fakes) — invariant: *both a definite refusal and a \"not yet\" consume budget, and neither is mistaken for success*. Breaks if a 409 or a 503 is read as discharged. Note **503 is the ticket's primary case** (`access/internal/api/refunds.go:50-55`).\n   - `TestClaimSkipsRefundsWhoseMoneyHasNotMoved` (store smoke, D6) — invariant: *only a refund whose money is durable is eligible for reversal*. Breaks if `status='completed'` is dropped.\n7. **`TestMissingAccessURLFailsReadinessButNotLiveness` seam pinned** to a **commerce unit/API test**, not gateway smoke: the smoke stack always sets `ACCESS_URL` (`compose.yaml:244`) and unsetting it would break every other test in that stack.\n8. **The outage test derives its deadline from the configured interval**, never a hard-coded sleep.\n9. **`releaseUndriven` precedent named** for the implementer: `recovery/runner.go:266-286` — fresh 5s `context.Background()` *and* claim-token-conditional.\n\n#### Test seam (recorded, per the skill's requirement)\n\nThree existing seams, no new ones. **unit-with-fakes** (`internal/reversal/runner_test.go`, port\npattern from `bulkrefund/runner.go:26-49`) for every runner *decision*; **store smoke**\n(`//go:build smoke`, `internal/store/reversal_smoke_test.go`) for every SQL *predicate* — the\nrepo's rule is that a test must live at the tier its mechanism does, and a fake enforcing in Go what\nthe SQL must enforce proves only that the fake and the handler agree; **gateway smoke**\n(`smoke/refund_reversal_recovery_test.go`) for COS 3's real outage, because only the composed stack\ncan stop and restart access.\n\n#### Introduced at plan-review, reviewed by no second model\n\nCarried forward verbatim from the critique, and named again here because these reach Gate 2 with\none author and no second reader — they are the first things the ai-review pass should attack:\n\n- **the parking-instead-of-predicting mechanism** (D1),\n- **the reset-attempts-on-progress rule** (D2),\n- **the static-config-vs-runtime-dependency distinction** used to justify the readiness split against ADR-021 §D6's precedent (D3),\n- **the `status='completed'` predicate** (D6).\n\n#### Lens\n\nPre-mortem, applied to the reviewed plan (not the draft). It produced D6 and D7 and surfaced the\nD1/D2 interaction — voiding succeeding while capacity fails must reset the budget, or the row burns\nit on the half that already works. That interaction exists *only because* both amendments were made,\nwhich is the repo's own warning that a round of fixes needs a review of the fixes' interaction.\n\n#### Risk verdict, restated\n\n`risk:low` not claimed. Touches a data migration, deployment contract (`ACCESS_URL` readiness) and\nservice startup wiring. Money path untouched and demonstrated so: no change to `refundPayment`,\n`order_facts`, the payments journal, or any payments call — the runner composes no money path and\ndrives only the two already-idempotent reversal legs.\n"
    }
  ]
}
