{
  "key": "TKT-226",
  "type": "Story",
  "parent": "TKT-21",
  "classification": "complex",
  "summary": "A mail path — the missing capability three TKT-21 decisions were shaped around",
  "status": "Done",
  "labels": [],
  "assignee": "agent",
  "pr": {
    "number": 182,
    "url": "https://github.com/mathd/ticketing_system/pull/182",
    "state": "merged"
  },
  "links": [],
  "description": "**As** a buyer who forgot their password, **I want** a way back into my account,\n**so that** creating one is not a trap.\n\nFiled after TKT-21 closed, because the epic kept running into the same wall. ADR-049 states it\nplainly: *\"This repo has no mail path. No SMTP, no queue, no provider. Every design that depends on\nsending mail — email verification, password reset, 'we sent the owner a note' on a duplicate\nregistration — is unavailable, not merely unbuilt.\"*\n\n**Three separate decisions were shaped by that absence, and two of them are worse than they need to\nbe:**\n\n1. **Registration answers 409 on a duplicate** — an unauthenticated, unrate-limited membership oracle\n   over the whole customer base (ADR-049 §2). The standard mitigation is *answer 201 and mail the\n   owner*. **TKT-224** exists to rate-limit a disclosure that mail would remove outright.\n2. **There is no password recovery of any kind.** `docs/development.md` § Customer accounts: *\"A\n   customer who forgets their password cannot get back in.\"* That is the largest hole in what TKT-21\n   shipped, and it has no ticket until this one.\n3. **No email verification**, so an account can register any address it does not own.\n\n**This is a prerequisite, not a follow-up.** Two of the epic's three filed follow-ups exist to work\naround it.\n\n**Acceptance Criteria:**\n- [ ] The service can send a transactional message, and **the plan states which service owns that\n  capability and why** (ADR-002 cuts at five services; a mail sender belongs to one of them or the\n  deviation is an ADR). Note that `access` already owns ticket *delivery* — say whether this is the\n  same concern or a different one.\n- [ ] A **local, offline sender** for `make up` and the smoke suite that captures messages instead of\n  sending them — the gate must not depend on a network or a provider account, and a test must be able\n  to assert what would have been sent.\n- [ ] The plan states what happens when sending **fails**. A password reset that silently does not\n  arrive is worse than one that refuses, and this is a queue-vs-inline decision with an ADR-016-shaped\n  recovery question behind it.\n- [ ] **Password reset** end to end: request, a single-use time-bounded token, set a new password,\n  and every existing session for that customer invalidated (ADR-049 §4's session map is in-process —\n  say how, or say it cannot and why that is acceptable).\n- [ ] The reset flow does **not** reintroduce an enumeration oracle: requesting a reset for an\n  unknown address answers the same as for a known one.\n- [ ] The plan says whether TKT-224's **409 disclosure is now narrowed** (ADR-049 §2 becomes\n  revisitable once mail exists) — decided either way, with the reason.\n- [ ] `make check` green (lint + test + build, Go & TS, gateway smoke suite); regenerated code committed BEFORE the gate runs (ADR-009).\n\n**Non-goals:** marketing email, templates or branding · a provider integration beyond one port with\none fake (ADR-032's PSP-port shape is the precedent) · rate limiting (TKT-224) · GDPR retention of\nmessage logs (TKT-33).",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "A buyer who forgets their password can get back in, and the system can send a transactional message without a network."
    },
    "scope": {
      "state": "met",
      "note": "One sending port, one offline fake, one reset flow. Not templates, not marketing, not a provider account."
    },
    "uncertainties": {
      "state": "deferred",
      "note": "(a) which of the five services owns sending — access already owns ticket delivery, which may or may not be the same concern; (b) queue vs inline and what a send failure does; (c) whether reset invalidates in-process sessions or admits it cannot. All three are plan decisions with the constraints written down; none blocks starting."
    },
    "dependencies": {
      "state": "met",
      "note": "None. TKT-220..223 are Done and this depends on nothing they left open."
    },
    "approach": {
      "state": "met",
      "note": "Mirror ADR-032's PSP port: one interface, one fake used by make up and the smoke suite, one real implementation behind config. The fake is what makes the gate hermetic."
    },
    "first_slice": {
      "state": "met",
      "note": "The port plus the offline fake plus password reset. Verification and the 409 narrowing are separable and come after."
    },
    "success": {
      "state": "met",
      "note": "Deterministic: a captured message asserted by a test, a reset that works end to end, and an unknown-address request that is indistinguishable from a known one."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked 2026-08-06 (post-epic)."
    }
  },
  "context": {
    "capturedAt": "2026-08-06",
    "binding": [
      {
        "id": "AGENTS.md",
        "date": "2026-08-06",
        "rule": "Local gate: `make check` (lint + test + build, Go & TS, plus the gateway smoke suite). CI runs the same gate - keep them mirrored. Commit regenerated code before running the gate."
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-049",
        "why": "States the absence this ticket closes, and the three decisions shaped around it: the 409 oracle (§2), no recovery path (Consequences), and the in-process session map (§4) that a reset would have to invalidate."
      },
      {
        "id": "ADR-002",
        "why": "Five services. A mail sender lands inside one or the deviation is an ADR - and `access` already owns ticket DELIVERY, which is the closest existing concern."
      },
      {
        "id": "ADR-032",
        "why": "The shape to copy: a provider behind a port with a fake, so the gate never needs a real account or a network."
      },
      {
        "id": "ADR-016",
        "why": "A send that can fail and be retried is a recovery-state question, and this repo has an opinion about those."
      },
      {
        "id": "ADR-021",
        "why": "Name the adversary. 'The reset link is single-use' means what, against whom - someone reading a mailbox, or someone with database access?"
      }
    ],
    "reference": [
      {
        "summary": "The disclosure this would let us close: ADR-049 §2 accepts registration's 409 as an unauthenticated membership oracle explicitly BECAUSE no mail path exists. TKT-224 is the mitigation built around it.",
        "link": "docs/adr/ADR-049-customer-identity-and-storefront-sessions.md"
      },
      {
        "summary": "The operator-facing statement of the gap, already written: docs/development.md, 'There is no password recovery.'",
        "link": "docs/development.md"
      }
    ]
  },
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Implementation readiness — CONCERNS\n\n| Criterion | Status | Note |\n|---|---|---|\n| Objective | OK | A buyer locked out of their own account can get back in |\n| Scope | OK | One port, one fake, one reset flow |\n| Uncertainties | WARN | Three, all deferred to plan time with their constraints written down |\n| Dependencies | OK | None |\n| AC verifiable | OK | Every criterion is a test or a stated decision |\n| Context-mémo | OK | Baked, with the ADR-049 sections that constrain it |\n\n**Concerns:**\n\n1. **Ownership is genuinely undecided.** `access` owns ticket delivery today, which is either the\n   same concern (one sender, two callers) or a different one (transactional identity mail is not\n   fulfilment). Getting it wrong is a service-boundary mistake that costs a migration.\n2. **This ticket makes TKT-224 partly redundant, and that is the point.** Rate limiting was designed\n   around a disclosure mail removes. Whoever plans this should read TKT-224 first and say whether it\n   shrinks.\n\n**Verdict:** CONCERNS — proceedable. Nothing structural; the uncertainties are decisions, not\nunknowns."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-226\n\n**Selection reason.** Chosen by the repo owner at Gate 1 over TKT-224/225/227. The TKT-21 handoff\nrecommended it and the reason holds: two of the epic's three filed follow-ups exist to work around\nthis absence, and one of them (TKT-224) is a control built on top of a disclosure that mail removes\noutright. The largest product hole TKT-21 shipped — *a customer who forgets their password cannot get\nback in* (`docs/development.md`) — has no other ticket.\n\n**Blockers.** `links: []`. Zero open blockers. TKT-220…223 are Done and this depends on nothing they\nleft open.\n\n**Risk verdict — `risk:low` NOT claimed.** Stating it explicitly even though the answer is obvious,\nbecause the label's carve-outs are exactly the tickets whose risk is obvious to the claimer and\ninvisible in the thread afterwards (TKT-190):\n\n- **Auth path.** A password reset is a credential-change path. Getting the token single-use,\n  time-bounded, and non-enumerating wrong is an account-takeover bug, not a defect.\n- **Data migration.** A reset token needs storage; that is a commerce migration under ADR-022\n  (out-of-band `migrate` subcommand), not an app-level change.\n- **>5 files.** A port, a fake, a real-ish sender, a store, handlers, storefront pages, migration,\n  compose wiring, and their tests. Nowhere near the threshold.\n- **Session invalidation.** ADR-049 §4's session map is in-process. Invalidating every session for a\n  customer crosses into that design, which the ticket names as an open decision.\n\nGate 2 therefore applies in principle. **This run is gateless by the owner's instruction** — I take\nthe gate decisions and every blocking objection I proceed past is recorded in the `overrides:` list\nof the `kind=metrics` closeout. That list is the part an owner cannot reconstruct from the merged\ndiff.\n\n**Branch.** `TKT-226-mail-path`, off `origin/main` @ `72a023d4`, in a worktrunk worktree.\n\n**Deferred uncertainties carried into planning** (from `readiness.uncertainties`, all three are\ndecisions with their constraints written down, none blocks starting):\n(a) which of the five services owns sending — `access` already owns ticket *delivery*;\n(b) queue vs inline, and what a send failure does;\n(c) whether reset invalidates in-process sessions, or admits it cannot and why that is acceptable.\n\nPlus the ticket's own required call: **does TKT-224's 409 disclosure narrow now?** — decided either\nway, with the reason.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Execution plan — draft\n\n**Drafted by `gpt-5.6-luna`** (codex companion, `task`, effort high, read-only — no repo file was modified). Config: `config.models.plan`. Codex session `019fd9ca-6310-7082-adba-3aeab0c324f7`.\n\nBriefed by me from the ticket, the context-mémo, and a **claim-time re-resolution of the governing ADRs** — the mémo listed ADR-049/002/032/016/021; I added **ADR-043** (decides public-contract vs `/internal/` placement), **ADR-042**, **ADR-022** (the migration runs out-of-band) and **ADR-012** (a reset token is a bearer credential) before briefing.\n\n---\n\n# TKT-226 — execution plan\n\n## 1. Decisions\n\n### (a) Commerce owns transactional mail\n\n`commerce` owns the mail port because it owns customer accounts, password credentials, reset tokens, and orders under ADR-002 and ADR-049. No sixth service, gateway-owned store, or cross-service NATS path is introduced.\n\n`access` already owns ticket delivery attempts under ADR-012. That is a different concern: access delivers post-purchase ticket artifacts, while commerce sends account-lifecycle messages. The physical provider could eventually be shared, but ownership and retry state remain separate.\n\nCreate an ADR-050 documenting this boundary.\n\n### (b) Use a durable transactional outbox, not inline sending\n\nPassword-reset token creation and mail-outbox insertion commit in one commerce transaction. A background mail drainer sends frozen message bytes after commit.\n\nThis follows ADR-016 §Decision 6 and the existing `services/commerce/internal/outbox/drainer.go` behavior:\n\n- claim with a lease;\n- send through the port;\n- mark sent only after successful sender return;\n- release with exponential backoff on failure;\n- dead-letter after the existing bounded-attempt policy;\n- never log the recipient, token, or message body.\n\nIf the database transaction or enqueue fails, the API returns a generic `503`; it does not claim that mail was accepted.\n\nIf sending fails after enqueue, the API’s prior `202` remains an honest “accepted for delivery” response. The row remains retryable and failures are operator-visible through persisted error state and logs. Exhaustion is dead-lettered rather than silently discarded.\n\nNo NATS routing is used, so ADR-017, ADR-033, and ADR-034 are not engaged.\n\n### (c) Invalidate all sessions in the active storefront process\n\n`commerce` cannot directly reach the storefront’s in-process `Map` from `web/storefront/src/lib/session.ts`.\n\nThe successful storefront reset route will:\n\n1. receive the reset result, including `customer_id`;\n2. call new `destroyAllSessionsForCustomer`;\n3. delete the browser’s `SESSION_COOKIE`;\n4. redirect to sign-in.\n\nThis invalidates every session for that customer in the current storefront process. Cross-replica invalidation remains impossible because ADR-049 deliberately chose process-local sessions; that limitation is acceptable for the current single-replica Compose topology and must be stated in ADR-050.\n\nA direct caller of commerce’s public reset endpoint cannot invalidate a storefront map; the supported browser flow is the storefront route.\n\n### (d) Narrow TKT-224’s registration disclosure after this ticket\n\nYes. Once mail exists, the duplicate-specific `409` should be removed in a follow-up.\n\nRegistration should return the same generic acceptance status and body for both a new and already-registered address, and should not return a principal. New accounts will require a subsequent sign-in. A duplicate attempt will queue an owner notification.\n\nThis removes the membership oracle that ADR-049 §2 accepted only because mail was unavailable. TKT-224 remains necessary for submission and mail-volume abuse; it is not replaced by this change.\n\n## 2. The adversary, named\n\nThe reset token is a bearer credential.\n\nIt defends against:\n\n- replay after successful use, through an atomic single-use update;\n- replay after expiry, through a server-side expiry check;\n- accidental exposure through gateway request logs, by carrying the token in an email URL fragment and submitting it later in a POST body;\n- password-reset timing and response enumeration, through one generic response and the ADR-049 masked-cost discipline;\n- network or browser observers who can see ordinary request URLs, because the fragment is not sent to the gateway.\n\nIt does not defend against:\n\n- someone who can read the recipient’s mailbox;\n- someone who can read commerce’s database, including the frozen mail-outbox payload;\n- someone who can write commerce’s database or replace password/reset rows;\n- a caller already inside the Compose network;\n- cross-replica session invalidation.\n\nThe plan must not call this tamper-evident. It is an application-level recovery credential with bounded lifetime and use, not an integrity guarantee against a database adversary under ADR-021.\n\n## 3. Definition of done\n\n- Commerce exposes a provider-neutral mail port and deterministic offline fake.\n- `make up` and smoke use the fake by default and perform no network mail calls.\n- The fake captures complete messages for assertions.\n- Migration `0018` creates reset-token and mail-outbox storage through ADR-022’s out-of-band `commerce-migrate` path.\n- Reset request:\n  - accepts an email;\n  - returns the same status/body for known and unknown addresses;\n  - never returns whether an account exists;\n  - transactionally stores a hashed token and frozen mail message for known accounts.\n- Reset completion:\n  - accepts the token and new password in a POST body;\n  - rejects malformed, expired, and previously used tokens;\n  - updates the bcrypt password;\n  - invalidates the token atomically;\n  - returns the customer ID needed by the storefront to clear sessions.\n- Existing storefront sessions for that customer are removed by `destroyAllSessionsForCustomer`.\n- Reset pages and forms pass `gate.ts`’s proxy-aware origin check.\n- Token values never appear in a gateway URL, response cache, application log, or rendered HTML.\n- Existing account and guest-checkout paths remain unchanged.\n- Generated `openapi_gen.go` and `commerce-api-types.gen.ts` are regenerated and committed before `make check`.\n- `make check` is green.\n- A real browser submits both reset forms against `make up`; render-only verification is insufficient.\n\n## 4. Files\n\n### New\n\n- `docs/adr/ADR-050-mail-path-and-password-recovery.md` — records mail ownership, outbox semantics, failure behavior, token threat model, and process-local session invalidation.\n- `services/commerce/internal/mail/port.go` — new `Message` and `Sender` port.\n- `services/commerce/internal/mail/fake.go` — offline capturing sender with deterministic failure injection.\n- `services/commerce/internal/mail/fake_test.go` — proves capture behavior and no network dependency.\n- `services/commerce/internal/mail/drainer.go` — mail outbox worker using the ADR-016 lease/retry/ack-before-mark protocol.\n- `services/commerce/internal/mail/drainer_test.go` — pure drainer tests for success, retry, lease loss, and dead-letter behavior.\n- `services/commerce/internal/store/password_reset.go` — reset-token creation/completion transactions, token hashing, and password update logic.\n- `services/commerce/internal/store/mail_outbox.go` — Postgres claim, release, sent, and retry operations for mail rows.\n- `services/commerce/internal/store/password_reset_test.go` — pure reset-token and validation tests.\n- `services/commerce/internal/store/password_reset_smoke_test.go` — real-Postgres tests for transaction and driver behavior.\n- `web/storefront/src/pages/[locale]/account/forgot-password.astro` — anonymous reset-request form.\n- `web/storefront/src/pages/[locale]/account/reset-password.astro` — anonymous reset-completion form; reads the fragment locally and submits the token in the POST body.\n\n### Modified\n\n- `services/commerce/api/openapi.yaml` — declares public reset request/completion operations and schemas; quote all flow-mapping descriptions containing commas.\n- `services/commerce/internal/api/server.go` — injects the sender, registers public reset routes, and starts the mail dependency wiring.\n- `services/commerce/internal/api/customer.go` — preserves existing customer behavior while sharing generic error/no-store handling with reset handlers.\n- `services/commerce/internal/api/password_reset.go` — new handlers for reset request/completion.\n- `services/commerce/internal/api/password_reset_test.go` — handler mapping, response parity, no-reflection, and no-log tests.\n- `services/commerce/cmd/commerce/main.go` — selects the fake sender by default and starts the mail drainer.\n- `services/commerce/internal/api/openapi_gen.go` — regenerated and committed.\n- `web/storefront/src/lib/commerce-api-types.gen.ts` — regenerated and committed.\n- `web/storefront/src/lib/customer-api.ts` — adds reset request/completion calls using POST bodies and no-store behavior.\n- `web/storefront/src/lib/session.ts` — adds `destroyAllSessionsForCustomer`.\n- `web/storefront/test/session.test.ts` — tests customer-scoped invalidation.\n- `web/storefront/test/gate.test.ts` — treats forgot/reset pages as anonymous and verifies unsafe submissions still require the trusted origin.\n- `web/storefront/src/lib/locales.ts` — adds reset-page strings.\n- `smoke/customer_accounts_test.go` — extends the existing live-commerce customer smoke seam with reset and fake-mail assertions.\n- `compose.yaml` — makes the offline fake sender explicit for local Compose.\n- `compose.smoke.yaml` — makes the offline fake sender explicit for smoke, if the override requires it.\n\nThe existing `services/commerce/internal/outbox/drainer.go` and `services/commerce/internal/store/store.go` remain the reference implementation for delivery semantics; the mail queue must not reuse `completion_outbox`, whose schema is order-event-specific.\n\n## 5. The test seam\n\n### Pure unit tests\n\nUse dependency seams rather than real infrastructure for:\n\n- `mail.Fake.Send`;\n- mail message construction;\n- reset token format and hashing;\n- handler status/body mapping;\n- reset-request response parity;\n- logging and secret-reflection checks;\n- `destroyAllSessionsForCustomer`;\n- `gateRequest` classification and ordering.\n\nThe existing seams to preserve are:\n\n- `customerStoreFn`;\n- `compareCustomerHashAndPassword`;\n- `registerCustomerFn`;\n- `authenticateCustomerFn`;\n- `lookupSession` and `next` instrumentation in `gate.test.ts`.\n\n### Real Postgres tests\n\nThe fake-store learning requires real Postgres for anything involving:\n\n- `INSERT ... RETURNING`;\n- unique token-hash constraints;\n- foreign keys;\n- transaction atomicity;\n- `UPDATE ... RETURNING`;\n- `used_at` and expiry predicates;\n- mail-outbox claim leases and retry columns;\n- migration `0018`.\n\nThese belong in `services/commerce/internal/store/password_reset_smoke_test.go`, using the existing `outboxDB` and `schemaDB` helpers from the `store` package.\n\nThe real-Postgres tests must be written before relying on fake-store tests, because a fake cannot expose SQL driver behavior, constraint behavior, or the actual row scan.\n\n### Browser submission\n\nAgainst `make up`, submit:\n\n1. `/en/account/forgot-password` with a known address;\n2. the same form with an unknown address;\n3. retrieve the captured fake message from the commerce database;\n4. open the reset page using the fragment link;\n5. submit the new password form;\n6. verify the old password fails and the new password succeeds;\n7. verify an existing browser session is redirected to sign-in after reset.\n\nThis must exercise Astro’s real form handling, relative actions, `gate.ts`, forwarded-origin headers, gateway routing, redirects, and cache headers.\n\n## 6. Test plan\n\n### Mail port and drainer\n\n- `TestFakeSenderCapturesMessage` — fails if `Send` drops the message or mutates its recipient/body.\n- `TestFakeSenderReturnsConfiguredFailure` — fails if configured failures are swallowed.\n- `TestMailDrainerMarksSentOnlyAfterSend` — fails if the row is marked before sender success.\n- `TestMailDrainerReleasesFailedMessageWithBackoff` — fails if a failed row disappears or is immediately retried.\n- `TestMailDrainerDeadLettersAfterMaximumAttempts` — fails if poison mail retries forever or starves newer rows.\n- `TestMailDrainerDoesNotLogRecipientOrToken` — fails if sensitive message data enters logs.\n\n### Enumeration and request behavior\n\n- `TestRequestPasswordResetAnswersIdenticallyForKnownAndUnknownAddresses` — fails if status or response bytes differ.\n- `TestRequestPasswordResetComparesAtMaskedCostForKnownAndUnknownAddresses` — uses `compareCustomerHashAndPassword`; fails if the unknown path skips the fixed-cost comparison.\n- `TestRequestPasswordResetDoesNotLogSubmittedAddress` — fails if the handler adds the email to outage logs.\n- `TestResetRequestResponseIsNoStore` — fails if a reset response can be cached.\n\n### Token lifecycle\n\n- `TestPasswordResetTokenIsOpaqueAndNeverReturnedByTheStore` — fails if storage or return values expose the raw token.\n- `TestCompletePasswordResetRejectsMalformedToken` — fails if malformed bearer input reaches a database lookup or produces a successful update.\n- `TestCompletePasswordResetIsSingleUse` — fails if two completions succeed for one token.\n- `TestCompletePasswordResetIsAtomicUnderConcurrentUse` — real Postgres; fails if concurrent requests both change the password.\n- `TestCompletePasswordResetRejectsExpiredToken` — fails if expiry is checked using client time or after password mutation.\n- `TestCompletePasswordResetUpdatesPassword` — fails if the new bcrypt credential is not the one accepted afterward.\n- `TestResetTokenAndMailOutboxCommitTogether` — real Postgres; fails if either the token or its message can commit without the other.\n- `TestPasswordResetMigration0018CreatesRequiredConstraints` — fails if migration numbering, foreign keys, uniqueness, or indexes are missing.\n\n### Session invalidation\n\n- `it('destroys every session for one customer and no other customer')` in `web/storefront/test/session.test.ts` — fails if one customer’s reset removes another customer’s sessions or leaves one of the target’s sessions alive.\n- A storefront route test or browser check must fail if reset succeeds but the old session still reaches `/en/account`.\n\n### Live smoke\n\nExtend `smoke/customer_accounts_test.go` with:\n\n- `TestPasswordResetIsReachableThroughTheGatewayWithoutACredential` — fails if the routes become internal or require a service token.\n- `TestPasswordResetUnknownAndKnownRequestsHaveTheSameAnswer` — fails on response enumeration.\n- `TestPasswordResetUsesTheOfflineCapturedSender` — fails if the outbox is not drained by the fake or if the captured message is absent.\n- `TestPasswordResetCompletesThroughTheGateway` — fails if the message token cannot complete the reset, the old password still works, or the new password fails.\n\nThe smoke test must keep token/message values in memory and must not print them in failure output.\n\n## 7. Risks\n\n- **Token leakage through URLs:** putting the token in a query or path would expose it to gateway logs and traces. Use a fragment and POST body only.\n- **Astro form rejection:** `make check` renders storefront pages but does not submit forms. Browser verification is mandatory.\n- **Origin handling:** all reset POSTs must pass `web/storefront/src/lib/gate.ts`; do not restore Astro `checkOrigin`.\n- **Outbox false success:** marking a mail row before `Sender.Send` returns would make a lost reset look delivered.\n- **Dead-letter invisibility:** a failed sender must leave durable evidence and operator-visible logs without logging recipient or token data.\n- **Fake-only confidence:** SQL constraints, transaction ordering, and scan types require real Postgres tests.\n- **Session scope:** commerce cannot invalidate an arbitrary storefront process. The plan only claims invalidation in the active single-replica storefront process, matching ADR-049’s current session model.\n- **Retention:** message rows may contain recipient data and frozen message content. GDPR retention and deletion remain TKT-33, not this ticket.\n- **Generated drift:** run and commit `make generate` output before `make check`, per ADR-009.\n\n## 8. Slicing\n\nThis should be split into two implementation tickets.\n\n### TKT-226 first slice\n\n- ADR-050;\n- commerce mail port and fake;\n- migration `0018`;\n- durable mail outbox and drainer;\n- password-reset request/completion API;\n- storefront reset pages and session invalidation;\n- real-Postgres, unit, smoke, and browser-submit verification.\n\n### Follow-up ticket\n\n- email verification;\n- generic registration response;\n- removal of the duplicate-registration `409`;\n- owner notification for duplicate registration;\n- storefront registration change from immediate session creation to subsequent sign-in;\n- associated contract regeneration and browser-submit coverage.\n\nNo repository files were modified while preparing this plan.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### Plan — final (delta on the `kind=plan` draft)\n\n**Critique by `main-agent`** (`config.models.planReview`; the draft was `gpt-5.6-luna`, so the\ncross-model invariant holds). **The `kind=plan` draft as amended below is the plan.**\n\n#### Seam verification — the draft names no file or symbol that does not exist\n\nChecked at HEAD (`72a023d4`), because a drafter that could not run the gate will invent seams:\n`smoke/customer_accounts_test.go` ✓ · `web/storefront/src/lib/{locales,session,gate,customer-api,commerce-api-types.gen}.ts` ✓ ·\n`services/commerce/internal/api/openapi_gen.go` ✓ · `compose.smoke.yaml` ✓ ·\n`store.OutboxDB`, `outboxDB(t)`, `schemaDB(t, ctx)` ✓ · `gateRequest`, `requiresSession`,\n`originIsTrusted`, `isUnsafeMethod` ✓ · `customerStoreFn`, `compareCustomerHashAndPassword`,\n`registerCustomerFn`, `authenticateCustomerFn` ✓. Latest migration is `0017`, so `0018` is correct.\nOne clerical error: `services/commerce/internal/api/password_reset.go` and `…_test.go` are listed\nunder **Modified**; they are new.\n\n---\n\n### Accepted as drafted\n\n(a) **Commerce owns the mail port.** Correct, and for the right reason — it owns the accounts, the\ncredentials and the tokens, so no data crosses a boundary. The `access` comparison is also right:\n`access` owns ticket *issuance and lifecycle*, driven by a NATS consumer; it has no sending\ncapability at all today. Nothing to share, so nothing to argue about.\n\n(b) **Durable outbox rather than inline** — accepted, but the draft's *reason* is the weaker one. See\nF2.\n\n(d) **The 409 narrows, in a follow-up; TKT-224 is not replaced.** Accepted with its reasoning: the\nchange forces registration to stop returning a principal, which breaks the register→signed-in flow\nTKT-220 shipped and TKT-221 attaches to. That is a second ticket, not a stretch of this one.\n\nThe outbox schema (`attempts`, `next_attempt_at`, `dead_lettered_at`, partial index on the unclaimed\nset), the lease/ack-before-mark protocol, and the DoD are accepted as drafted.\n\n---\n\n### F1 — [high] The URL fragment is refused. Query parameter + hidden field.\n\n**Amended.** The draft carries the reset token in the mail link's **fragment** so it never reaches a\nserver, then has the page submit it in a POST body. A fragment is not sent to any server *including\nours*, so the page must read `location.hash` in the browser — which makes **password recovery depend\non JavaScript**. That is the one path a locked-out buyer has, and the storefront's only client-side\ncode today is the seat/hold picker on the *purchase* path; register, sign-in and claim are all plain\nserver-rendered forms.\n\nThe disclosure it buys is one this repo does not make. `shared/go/obs/requestlog.go:20` logs\n`\"path\", r.URL.Path` and nothing else — **no query string** — and ADR-049 § *TKT-222 amendment*\nalready relies on exactly that fact to justify putting twenty uuids in a query string.\n\n**Decision:** the link carries `?token=…`; the page renders it into a **hidden field**; the POST\ncarries it in the body. No JavaScript. The draft's real rule survives and is kept: the token must\nnever appear in a **path segment** — that *is* logged, and it is precisely TKT-202's shape.\n\nThe residual the fragment was actually protecting against is **`Referer` leakage**, so close that\ndirectly: serve the reset page with `Referrer-Policy: no-referrer`. No such header exists anywhere in\nthe repo today, so this is new and deliberate rather than a pattern being followed.\n\n### F2 — [high] The outbox's load-bearing justification is missing, and it is not ADR-016.\n\n**Amended (reasoning, not design).** The draft justifies the queue from ADR-016 §Decision 6\nprecedent. The decisive argument is different, and it must be written into ADR-050 or the queue will\nbe deleted later by someone looking at a fake sender that cannot fail:\n\n> **Inline sending cannot satisfy the enumeration-parity criterion.** An unknown address does no send\n> and therefore cannot fail; a known address can. So *inline + honest refusal* makes send-failure an\n> account-existence oracle — the exact thing AC-5 forbids — and *inline + swallowed error* is the\n> \"silently does not arrive\" outcome AC-3 names as worse than refusing. The outbox is the only shape\n> that is both honest and non-enumerating: both paths return the same answer because **neither has\n> attempted delivery yet** when the response is written.\n\n### F3 — [medium] ADR-050 is right and insufficient — ADR-049 needs an amendment too.\n\n**Amended.** Two of ADR-049's statements stop being true: Consequences (\"No password reset… no\nrecovery path at all\") and §2's premise (\"the standard mitigation needs mail, which does not exist\").\nAn ADR whose stated facts are false is worse than no ADR. Repo precedent is unambiguous — TKT-221,\nTKT-222 and TKT-223 each appended an amendment section to ADR-049 rather than superseding it.\n\nDo both: **ADR-050** for the mail path itself (ownership, port shape, outbox semantics, send-failure\nbehaviour, the token's adversary), and a **TKT-226 amendment section on ADR-049** recording what\nchanged about §2 and Consequences, and what did not.\n\n### F4 — [high] The reset token must be SHA-256, not bcrypt — and the plan was silent.\n\n**Introduced.** The draft says \"hashed token\" and never says which hash. The default a reader reaches\nfor in *this file's neighbourhood* is bcrypt, by analogy with `hashCustomerPassword` six lines away —\nand **a bcrypt-stored token cannot be looked up at all**: every bcrypt hash carries its own salt, so\nfinding the row means comparing the candidate against every row in the table at cost 10. That is not\na slow lookup, it is a table scan of KDF operations.\n\n**Decision:** the token is 32 bytes from `crypto/rand`, base64url in the link; the column stores\n**SHA-256 of the raw token** and is `UNIQUE`; lookup is by hash equality. A fast hash is the correct\nchoice precisely because the input is 256 bits of entropy — there is nothing to brute-force, so\nthere is nothing for a work factor to buy. The DB never holds the raw token, which is what bounds a\nread-only database adversary to nothing.\n\n### F5 — [high] The masked-cost test asserts a property this design does not have. Delete it.\n\n**Rejected.** The draft imports ADR-049 §3's equal-cost discipline onto the reset-request path and\nnames `TestRequestPasswordResetComparesAtMaskedCostForKnownAndUnknownAddresses`, using\n`compareCustomerHashAndPassword`. **The mechanism does not transplant.** §3's mask is the KDF, and a\nreset *request* submits no password — there is no bcrypt on that path to hide behind. Worse, the\nasymmetry it would need to mask is larger than sign-in's: a known address does two INSERTs inside a\ntransaction, an unknown address does nothing.\n\nShipping that test means one of two things, and both are the failure this repo keeps paying for: it\nfails, or it is written until it passes and then asserts a property the code does not provide. Eight\ntests in TKT-21 could not fail; this is that arriving in advance, at plan time, where it is cheap.\n\n**Decision:** delete the masked-cost test. Keep\n`TestRequestPasswordResetAnswersIdenticallyForKnownAndUnknownAddresses` (status and response bytes —\nwhich *is* true and *is* what AC-5 asks for) and **state the residual honestly in ADR-050**, naming\nthe adversary per ADR-021:\n\n> The reset-request response is identical in **status and bytes**; it is **not** equal in **cost**. A\n> known address commits two rows, an unknown address commits none. This defends against a caller\n> reading responses; it does not defend against one timing them. Closing it would mean writing rows\n> for addresses that do not exist. It is accepted because the residual is *smaller than what already\n> ships* — registration's 409 (ADR-049 §2) is an explicit, unmasked oracle on the same table — and\n> because volume is what makes timing measurable, which is **TKT-224**'s subject.\n\n### F6 — [medium] AC-4's \"every existing session invalidated\" is true only through the storefront route. Say so.\n\n**Amended.** The draft's flow is correct: the storefront's reset-completion route calls\n`destroyAllSessionsForCustomer`, and because sessions live in that one process this *does* evict an\nattacker's stolen session when the real owner resets. That is the threat that matters and it is\ncovered.\n\nBut completion is a **public commerce contract operation**, so a caller can complete a reset directly\nagainst the gateway and change the password with the session map untouched. Naming it (ADR-021)\nrather than letting \"every existing session is invalidated\" stand unqualified:\n\n- **Closed:** an attacker holding a live storefront session, when the owner resets through the\n  storefront. This is the case the criterion exists for.\n- **Open:** a reset completed by calling commerce directly. Bypassing the UI signs nobody out.\n- **Open and unchanged:** cross-replica and cross-restart, by ADR-049 §4's design.\n\nMaking completion `/internal/` would close the first gap and is refused for ADR-049 §1's reason — the\nstorefront holds no service credential and giving it one puts a credential in a process serving\nanonymous traffic.\n\n### F7 — [low] Two decisions the draft left as \"if required\". Decide them.\n\n- **Compose changes: none.** `compose.yaml` and `compose.smoke.yaml` are dropped from the file list.\n  The fake is the **default in the binary**, exactly as payments selects the fake PSP unless a\n  provider key is configured (ADR-032). A compose file that has to name the fake means the default is\n  wrong.\n- **Do not generalize the two drainers.** `mail/drainer.go` will read as a near-copy of\n  `outbox/drainer.go` and that is deliberate: the store functions it calls (`ClaimOutbox`,\n  `ReleaseOutbox`, `MarkPublished`) are bound to `completion_outbox` by name, and a shared abstraction\n  over two row shapes buys nothing today. Two instances do not earn an abstraction; a third would.\n  Recorded here so the ai-review pass files it as a decision rather than as duplication.\n\n---\n\n### Introduced at plan-review, reviewed by no second model\n\nCross-model review covers the draft and it will cover the code. It does **not** cover what this pass\n*added*. Naming them so Gate 3's reviewer knows where to look:\n\n1. **F4 — SHA-256 token hashing, `UNIQUE` on the hash, 32 bytes from `crypto/rand`.** The draft was\n   silent; this is entirely mine.\n2. **F2 — the enumeration-parity justification for the outbox.** New reasoning that becomes an ADR-050\n   claim, load-bearing for why the queue may not later be deleted.\n3. **F1's `Referrer-Policy: no-referrer`.** No such header exists in this repo; there is no local\n   precedent to have got wrong or right.\n4. **F5's accepted timing residual and its \"smaller than what already ships\" argument.** A security\n   claim written by one model and reviewed by none.\n5. **F3 — that ADR-049 gets a TKT-226 amendment section.**\n\n### Verification this ticket needs beyond `make check`\n\n- **A browser must submit both reset forms.** `make check` renders storefront pages and never submits\n  one; the whole SSR-rejects-the-write class (`gate.ts`, relative form actions, redirects, cache\n  headers) is invisible to it. New forms → mandatory (AGENTS.md, TKT-105).\n- **Real-Postgres tests come first for every new query** — the single-use `UPDATE … RETURNING`, the\n  expiry predicate, the token/mail same-transaction commit, the claim lease. A fake store cannot see\n  the driver (TKT-222 shipped three such defects past a green gate).\n- Every new test gets the mutation check: break what it covers, observe red, restore.\n\n### Gate 2\n\n`risk:low` was not claimed, so Gate 2 applies in principle. **This run is gateless by the owner's\ninstruction and I am taking it.** No blocking objection was raised and overridden at this stage —\nF1 and F5 are the plan-review pass *rejecting the draft*, which is the pass working, not an override.\nThe `overrides:` list in the closeout is the record if that changes.\n"
    },
    {
      "stage": "coding",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=coding kind=plan-final -->\n### Plan amendment — a mail *port* already exists in this repo, and decision (a) moves because of it\n\nFound while wiring, before any code was written. Both the draft and my plan-review pass took\nADR-049's \"this repo has no mail path. No SMTP, no queue, no provider\" at face value. That sentence\nis exactly true about **senders** and misleading about **ports**:\n\n```go\n// services/access/internal/consumer/consumer.go:113\ntype Mailer interface {\n    Send(context.Context, uuid.UUID, string, string) error\n}\ntype LogMailer struct{ log *slog.Logger }\nfunc (m LogMailer) Send(_ context.Context, id uuid.UUID, email, link string) error {\n    sum := sha256.Sum256([]byte(strings.ToLower(strings.TrimSpace(email))))\n    linkSum := sha256.Sum256([]byte(link))\n    m.log.Info(\"ticket delivery accepted\", \"delivery_id\", id,\n        \"recipient_hash\", hex.EncodeToString(sum[:]), \"ticket_link_hash\", hex.EncodeToString(linkSum[:]))\n    return nil\n}\n```\n\n`LogMailer` is the **only** implementation, it is what `main.go:252` wires, and it **sends nothing** —\nit logs a hash of the recipient and returns nil. ADR-012 records this deliberately (\"delivery\nattempts\", \"the log mailer uses a deterministic delivery ID\").\n\n**So the accurate statement is: the repo has one mail port and zero senders.** Ticket delivery is not\na capability `access` has and this ticket lacks — it is the *same* missing capability, already\nstubbed once.\n\nThis corrects the draft's decision (a), which said *\"`access` delivers post-purchase ticket\nartifacts\"*. It does not. Nothing in this repo has ever sent an email.\n\n#### What changes\n\n**The port and the fake go to `shared/go/mail`, not `services/commerce/internal/mail`.**\n\n- `shared/go/fakepsp` is exact precedent: a port's offline fake that more than one module needs lives\n  in `shared/go`, and `shared/go` is already in `go.work` for every service.\n- It costs **zero extra code** — the same files in a different directory — and it is where a real\n  provider has to land, because two callers of the concept already exist. That is rung 4 of the\n  ladder (use what is installed), not scaffolding for a speculative future.\n- Cheapest-to-reverse tiebreak: if `shared/go/mail` turns out to be commerce-only, nothing is harmed;\n  if commerce-local turns out to be wrong, it is a move plus import churn in two modules.\n\n**What does NOT change:**\n\n- **Commerce still owns the reset flow, the tokens, the outbox and the drainer.** The *port* is\n  shared; the *durable delivery* is commerce's, because the rows live beside the accounts. Decision\n  (b) and its enumeration-parity justification are untouched.\n- **`access` is NOT migrated in this PR.** Its `Send(ctx, deliveryID, email, link)` has no subject and\n  no body, so adapting it to a general `Message` means composing a subject and body for ticket\n  delivery — a behaviour change to a path this ticket does not otherwise touch, in the service with\n  the lifecycle-trail invariants. ADR-050 records `access.LogMailer` as the pre-existing instance and\n  names its migration as what a real provider forces, not what this ticket does.\n\n#### Introduced here, reviewed by no second model\n\nAdd to the plan-review list: **the `shared/go/mail` placement and the decision to leave `access`\nalone.** Neither the draft nor any review pass saw this — the draft was briefed on an ADR sentence\nthat is true about senders and was read as true about ports. The ai-review pass gets the diff, so it\nwill see the placement; it will not see the reasoning, which is the point of writing it here.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n### Implementation — TDD, gate green\n\n**Implemented by `main-agent`** (`config.models.implement`). Commit `58b67e06` on\n`TKT-226-mail-path`, **+3336 / −8 across 30 files**.\n\n#### Gate\n\n`make check` **PASS**. Run as the sole command in its shell call from the repo root, redirected to a\nfile with an exit sentinel; judged from the **log body**\n(`grep -E 'Error [0-9]+|^FAIL|--- FAIL|drifted'` → **0 matches**) as well as `EXIT=0`, because those\ntwo have diverged in this repo before (TKT-94, TKT-101).\n\nRegenerated code (`openapi_gen.go`, `commerce-api-types.gen.ts`) was **committed before** the gate ran\n— `check-generate` diffs against HEAD, so an uncommitted regen reads as drift.\n\n**The new tests demonstrably ran rather than skipping**, which matters because every one of them\n`t.Skip`s without a DSN:\n\n| | |\n|---|---|\n| `ticketing/shared/mail` | ok 1.161s (untagged) |\n| `commerce/internal/store` | ok 1.229s untagged, **ok 8.314s under `-tags smoke`** against real Postgres |\n| `commerce/internal/mailer` | no test files untagged (all smoke-tagged), **ok 1.038s under `-tags smoke`** |\n| `TestPasswordResetPathsAreReachableWithoutACredentialAndNotEdgeDenied` | **PASS** |\n| `TestAForgottenPasswordCanBeRecovered` | **PASS** — the acceptance criterion end to end on the live stack |\n\nThat last one is the one worth naming. It registers through the gateway, requests a reset, polls\ncommerce's `mail_outbox` until the row is **retired by the running container's drainer**, extracts the\ntoken from the composed body, redeems it, and asserts the new password signs in, the old one does not,\nand the token cannot be replayed. It fails if the drainer was never wired — a defect every unit test\nin this diff would sail past.\n\n#### What was built\n\n| Area | Files |\n|---|---|\n| Port + offline fake | `shared/go/mail/{port,fake,mail_test}.go` |\n| Schema | `store/migrations/0018_password_reset_and_mail_outbox.sql` |\n| Store | `store/{password_reset,mail_outbox}.go` + `password_reset_smoke_test.go` |\n| Drainer | `internal/mailer/{drainer,drainer_smoke_test}.go` |\n| Contract + handlers | `api/openapi.yaml`, `internal/api/password_reset{,_test}.go`, `server.go` |\n| Wiring | `cmd/commerce/main.go`, `compose.yaml` (`PUBLIC_BASE_URL`) |\n| Storefront | `forgot-password.astro`, `reset-password.astro`, `session.ts`, `gate.ts`, `customer-api.ts`, `locales.ts` |\n| Decisions | `ADR-050`, `ADR-049 § TKT-226 amendment`, `docs/development.md § Password recovery` |\n| Live proof | `smoke/password_reset_test.go` |\n\n#### Deviations from the approved plan, and why\n\n1. **`shared/go/mail` instead of `services/commerce/internal/mail`** — the `access.Mailer` discovery,\n   already posted as a `kind=plan-final` amendment before any code was written.\n2. **`internal/mailer`, not `internal/mail`** — the package would otherwise shadow the imported\n   `ticketing/shared/mail` at every call site in `main.go`.\n3. **`compose.yaml` IS touched**, contrary to F7. F7 refused a compose entry naming the fake sender,\n   and that still holds — the fake is the binary's default and nothing declares it. `PUBLIC_BASE_URL`\n   is different in kind: it is required configuration for the link, it is the variable access already\n   uses for the same purpose, and it cannot be defaulted in code without guessing a hostname.\n   `compose.smoke.yaml` is unchanged; it inherits.\n\n#### Mutation checks — every non-trivial test, broken then restored\n\nThe discipline that lapses under fix-momentum, so it was run as each test was written:\n\n| Mutation | Test that caught it |\n|---|---|\n| Drop the CR/LF recipient guard | `TestValidateRefusesUndeliverableMessages/recipient_with_{LF,CR}` |\n| `Sent()` returns the backing slice | `TestSentReturnsACopy` |\n| Handler answers 404 for an unknown address | `TestRequestPasswordResetAnswersIdenticallyForKnownAndUnknownAddresses` |\n| Build the link from a hard-coded foreign host | `TestTheResetLinkUsesTheConfiguredOriginNotTheRequestHost` |\n| Drop the `customerId` predicate in the session sweep | `leaves other customers alone` (+2 others) |\n\nEach went red on the mutation and green on restore. The real-Postgres tests were **written before**\nthe queries they cover, per the TKT-222 learning — a fake store cannot see the driver.\n\n#### Things a reviewer should attack first\n\nStated plainly rather than defended, because this is the list I would start from:\n\n1. **The enumeration parity is the whole feature's security property** and it is asserted in exactly\n   two places (handler bytes, and gateway bytes). If there is a third path where a known and an\n   unknown address diverge — a validator rejection, a header, a log, a timing-visible outage — it is\n   not covered.\n2. **`RequestPasswordReset` returns `(ok bool, err error)` and the handler discards `ok`.** That is\n   deliberate, and it is also exactly the shape where someone later \"improves\" the handler by using\n   it. There is no test that would stop a future caller from branching on it.\n3. **The cost asymmetry is real and accepted** — two INSERTs versus none. ADR-050 names it. If the\n   reviewer thinks the reasoning (\"smaller than the 409 that already ships\") is wrong, that is a\n   finding worth raising.\n4. **`mail_outbox` holds live reset links in plaintext with nothing pruning it.** Deliberate, TKT-33,\n   and stated in three places — but it is a real exposure and should not pass unremarked.\n\nLabel moves to `agent:ai-review`; `config.models.aiReview` = `gpt-5.6-luna` via the codex companion's\n`adversarial-review`, given the branch diff only and primed for guilt (no plan, no reasoning — a\nreviewer that has read the justification evaluates code against intent instead of against reality).\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n### Adversarial review — two passes, plus a browser check that found the worst defect\n\n**Reviewed by `gpt-5.6-luna`** (`config.models.aiReview`) via the codex companion's\n`adversarial-review`, given the branch diff only and primed for guilt. Implementation was\n`main-agent`, so the cross-model invariant holds.\n\n**Two passes, and the second was mandatory**: pass 1's fixes were non-trivial by several of the\ncriteria at once — blocking findings fixed, new control flow, a concurrency path, new tests. Pass 2\nthen proved pass 1's own fix wrong at the root, which resets the cap rather than stopping at two.\n\n---\n\n#### Pass 1 — `--base origin/main`, 7 findings\n\n| # | Finding | Verdict |\n|---|---|---|\n| 1 | [critical] Production mail sender is an in-memory fake | **REJECTED as a defect, upheld as a risk** |\n| 2 | [high] Reset requests diverge on enqueue outages and timing | **CONFIRMED (narrow), accepted + documented** |\n| 3 | [high] The raw reset credential is stored in the database | **CONFIRMED as a fact; test overclaim FIXED** |\n| 4 | [high] Concurrent reset requests can leave multiple live tokens | **CONFIRMED — real race, FIXED** |\n| 5 | [high] A successful send can be retried forever if retirement fails | **CONFIRMED — FIXED** |\n| 6 | [high] Session invalidation is bypassable and not cross-replica | **REJECTED — documented, deliberate** |\n| 7 | [high] The POST still carries the token in the URL | **CONFIRMED — FIXED** |\n\n**#4 was the sharpest.** `RequestPasswordReset` invalidated outstanding tokens and minted a new one\nwith no lock, so two transactions both saw no live token — neither had committed its INSERT — and\nboth inserted. My \"issuing invalidates the outstanding ones\" rule was silently a rule about\n*sequential* requests, and the test covering it was sequential. Fixed with `SELECT … FOR UPDATE` on\nthe customer row. **Mutation-verified: without the lock, 2 racers leave 2 live tokens.**\n\n**#7** — `action=\"\"` resolves to the DOCUMENT's URL, query included, so copying the token into a\nhidden field added a second copy rather than moving it. My own code comment claimed the opposite.\n\n**#3** — the storage is documented and deliberate, but the reviewer caught that\n`TestTheRawTokenIsNeverStored` only scanned `password_reset_tokens` while the token *is* at rest in\nthe `mail_outbox` body. A test whose name claims more than it checks is the thing someone later\ncites to argue the credential is not at rest. Renamed; a second test now pins the outbox exposure as\na known state.\n\n**#1 rejected, but not ignored.** Fake-by-default is ADR-032's rule and this ticket's stated\nnon-goal. The risk underneath was real and unaddressed: a row reaches `sent_at` and *looks*\ndelivered while nobody received anything. Commerce now WARNs at every boot that it delivers nothing.\n\n**#6 rejected.** The reviewer read ADR-050 (its own tool log shows the grep) and disagreed with the\nacceptance rather than missing it. Shared session storage is a direct reversal of ADR-049 §4 and out\nof scope.\n\n---\n\n#### Pass 2 — `--base 58b67e06`, the fix diff only, 4 findings\n\n| # | Finding | Verdict |\n|---|---|---|\n| 1 | [high] Issuance and redemption deadlock on inverted lock order | **CONFIRMED — I INTRODUCED IT, FIXED** |\n| 2 | [high] Post-send mark failures recorded as ordinary dead letters | **CONFIRMED — FIXED** |\n| 3 | [medium] The concurrency test does not force the race it claims | **CONFIRMED — FIXED** |\n| 4 | [medium] The source scan can pass while the form posts wrongly | **CONFIRMED — FIXED** |\n\n**#1 is why the second pass exists.** My `FOR UPDATE` fix made issuance take customer→tokens while\nredemption still took tokens→customer. A customer with a live token, with a request and a redemption\nin flight, forms a cycle; Postgres aborts one. That is a 500 on a recovery path — and on the request\nside a 500 is *also* the one status that differs from the unknown-address answer, so the deadlock\nfed the enumeration residual I had just documented. **A correct fix composing into a new defect,\nexactly the shape AGENTS.md records.** **Mutation-verified: with the old order, `redemption\ndeadlocked` every run.**\n\n**#3** — the reviewer was right that `close(start)` makes goroutines *eligible*, not overlapped. Both\nconcurrency tests now hold a row lock from the test itself and poll `pg_stat_activity` until the\nbackends are *observed* blocked. Probabilistic → deterministic.\n\n**Found while fixing, by neither review:** `internal/mailer` is the second package calling\n`store.Migrate`, and `go test ./internal/...` runs packages concurrently — two goose runs raced until\nthe loser died on `column \"terminal_outcome\" already exists`. It passed the first two gates by luck.\nIt now has its own database, following the precedent `payments` set for its api package.\n\n---\n\n#### The browser found what nothing else could\n\n**`Referrer-Policy: no-referrer` made Chrome send `Origin: null`, and `gate.ts` refused it — every\nreset completion answered 403 before the handler ran. The feature did not work at all.**\n\n```\nPOST /en/account/forgot-password   origin=\"http://localhost:18099\"  → 200\nPOST /en/account/reset-password    origin=\"null\"                     → 403\n```\n\nThat header was added on pass 1's advice, to close the `Referer` leak the rejected URL-fragment\ndesign would have covered. It is the strictest-looking value and it silently disabled the thing it\nwas protecting. Now `Referrer-Policy: origin`, which keeps `Origin` intact and *still* strips path\nand query from the Referer.\n\n**Three green gate runs, two adversarial passes and 166 unit tests all passed over a dead feature.**\nThird time for this class in this repo (TKT-105, TKT-220). A vitest tripwire now fails if anyone\nhardens it back.\n\n**20/20 browser checks pass** against the real stack through the real gateway: register → request →\nthe drainer retires the message through the offline fake → the mailed link opens → the form submits\nto a URL with no token in it → the old password stops working → the new one works → **a separate\nlive session for that account is signed out** → a replayed link is refused. Known and unknown\naddresses render byte-identical pages.\n\nA defect in my own driver, worth recording: `waitForURL('**/en/account**')` also matches\n`/en/account/sign-in`, so \"the new password signs in\" passed vacuously while the password had not\nchanged. Now an exact URL comparison. Three of that run's four failures were cascade from the 403;\nonly one was independently real.\n\n#### Verification\n\n`make check` green on the final tree, judged from the log body. Every fix mutation-checked: break the\nproduction code, observe red, restore. The stack tooling (`.tkt226-browser/`) is deliberately\n**untracked** — it holds generated dev credentials, and a real harness is **TKT-196**.\n\nLabel moves to `needs:human`. Gate 3 is waived on this run, so I take it and record it.\n"
    },
    {
      "stage": "po-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=po-review kind=summary -->\n### PO validation — every COS, and what is demonstrably true\n\nMerged as `75febfea` (PR [#182](https://github.com/mathd/ticketing_system/pull/182), squash).\n`origin/main` verified by **content**, not by the merge badge — TKT-97 squash-merged a stale SHA and\nonly a content check caught it. Confirmed on `origin/main`: the `Referrer-Policy: origin` fix, the\n`FOR UPDATE` lock, and `shared/go/mail/port.go`.\n\n#### The COS, one by one\n\n| # | Criterion | Verdict |\n|---|---|---|\n| 1 | The service can send a transactional message, and the plan states **which service owns it and why** | **MET** — ADR-050 §1. `access` compared explicitly: it owns ticket *delivery* and has a `Mailer` whose only implementation logs a hash and returns nil. Same missing capability, already stubbed once. |\n| 2 | A **local, offline sender** for `make up` and the smoke suite that captures instead of sending, assertable by a test | **MET** — `shared/go/mail.Fake`. `make check` runs offline; `TestAForgottenPasswordCanBeRecovered` reads the captured message out of `mail_outbox` and completes the reset with it. |\n| 3 | The plan states what happens when sending **fails** | **MET** — ADR-050 §3. Backoff, ten attempts, dead-letter, and a `DELIVERED-BUT-UNCONFIRMED` marker for the one case where the row lies about itself. |\n| 4 | **Password reset** end to end, incl. every session for that customer invalidated | **MET, with a named limit.** Verified in a browser: a *separate* live session was signed out. A reset completed by calling commerce directly signs nobody out — stated, not implied. |\n| 5 | The reset flow does **not** reintroduce an enumeration oracle | **MET in status and bytes; NOT in cost.** Both stated in ADR-050 and proven in a browser (known and unknown render byte-identical pages). Two residuals named: cost, and a partially-broken database. |\n| 6 | The plan says whether TKT-224's **409 narrows** | **MET** — decided: yes, in a follow-up, with the reason (it forces registration to stop returning a principal, breaking the register→signed-in flow TKT-221 attaches to). |\n| 7 | `make check` green; regenerated code committed **before** the gate | **MET** — four gate runs, the last green; regen committed first each time. |\n\n#### What to check yourself, in a browser\n\n`tools/browserstack.sh`-shaped stack (`make up` is still broken — **TKT-227**), then:\n\n1. `/en/account/sign-in` → the \"Forgotten your password?\" link.\n2. Submit a **registered** address, then an **unknown** one. **The two pages must be identical.**\n3. `SELECT body FROM mail_outbox WHERE recipient='…' ORDER BY created_at DESC LIMIT 1;` — the mailbox\n   is a table, because nothing has ever sent an email here.\n4. Open the link, set a new password. The old one must stop working; a second browser signed in to\n   that account must be signed out.\n5. Re-open the same link: refused.\n\n#### What is NOT met, stated plainly\n\n- **No email verification.** An account can still register an address it does not own. Out of scope\n  by the ticket's own non-goals, and it means COS-3 of the original framing is untouched.\n- **Nothing has ever sent an email.** Commerce WARNs this at every boot. The first real provider is\n  the first time any of this meets a network.\n- **`mail_outbox` holds live reset links in plaintext** and nothing prunes it — **TKT-33**.\n\nGate 4 is waived on this run. I am taking it, and the `overrides:` list in the closeout is the record.\n"
    },
    {
      "stage": "done",
      "kind": "metrics",
      "body": "<!-- sdlc:stage=done kind=metrics -->\n### Closeout — TKT-226\n\nMerged `75febfea` · PR [#182](https://github.com/mathd/ticketing_system/pull/182) · branch\n`TKT-226-mail-path` deleted.\n\n#### Durations\n\nFrom the `sdlc-state` branch's commit log (`git log -- .sdlc/tickets/TKT-226.json`), not hand-tracked.\n\n| Stage | Duration |\n|---|---|\n| Backlog (shaped in a prior session) | n/a |\n| Backlog → Ready (**Gate 1, the owner's real decision**) | 15m 41s |\n| Ready (claim) | 31s |\n| `agent:planning` (draft, `gpt-5.6-luna`) | 12m 15s |\n| `agent:plan-review` (critique + finalize, `main-agent`) | 2m 22s |\n| Planning → Building (**Gate 2, waived**) | 0s |\n| `agent:coding` (implementation + gate 1) | 2h 24m 42s |\n| `agent:ai-review` (2 passes, fixes, 3 more gate runs, browser verification) | 1h 7m 16s |\n| Building → PO Review (**Gate 3, waived**) | ~2m |\n| **Total, Ready → merged** | **~3h 47m** |\n\n**human-wait total: 15m 41s** — all of it Gate 1, which was a genuine human decision (the owner chose\nthis ticket over TKT-224/225/227). Gates 2, 3 and 4 were waived: **0 minutes**.\n\n**diff: +3903 / −8 (32 files)**\n\n#### overrides:\n\nGates 2–4 were replaced by agent judgement. The machinery caught the ordinary defects; what it could\nnot settle is where a review stated a **blocking** objection and I proceeded anyway. That list is the\nsmallest thing an owner must read to re-take the decisions taken for them, and it is unrecoverable\nlater — a fixed finding and an overridden one look identical in a merged diff.\n\n**Two `[critical]`/`[high]` objections overridden:**\n\n1. **[critical] \"Production mail sender is an in-memory fake — do not ship.\"** (ai-review pass 1)\n   - *Reviewer's reason:* commerce unconditionally wires `mail.NewFake()`; the drainer marks rows\n     sent, so users receive nothing while the outbox claims delivery succeeded.\n   - *My reason:* this is ADR-032's rule and **this ticket's stated non-goal** (\"a provider\n     integration beyond one port with one fake\"). Shipping a provider was never in scope, and there\n     is no provider account. The reviewer had the diff only and could not know.\n   - *What I did anyway:* the risk underneath was real and unaddressed, so commerce now **WARNs at\n     every boot** that it delivers nothing, and `docs/development.md` says a buyer on a local stack\n     will never receive anything.\n   - *Residual risk carried by:* the follow-up that lands a real provider. **Nothing has ever sent an\n     email from this repo.**\n\n2. **[high] \"Session invalidation is bypassable and not cross-replica.\"** (ai-review pass 1)\n   - *Reviewer's reason:* only the Astro storefront calls `destroyAllSessionsForCustomer`; a caller\n     can redeem the public commerce endpoint directly and leave every session alive. Its\n     recommendation was a session-revocation event or shared session storage.\n   - *My reason:* that is a **direct reversal of ADR-049 §4**, which chose in-process sessions\n     deliberately and priced the consequence. The reviewer read ADR-050 (its own tool log shows the\n     grep) and disagreed with the acceptance rather than missing it — a legitimate disagreement, not\n     a defect. Making completion `/internal/` is refused by ADR-049 §1: the storefront holds no\n     service credential.\n   - *Residual risk carried by:* ADR-050's adversary section and ADR-049's TKT-226 amendment, both of\n     which name it. **No ticket is filed** — see the open question below.\n\n**Two `[high]`s upheld as gaps and accepted as trades** (not overridden, but decided by me alone):\n\n3. **Enumeration parity does not hold on a partially-broken database.** A write failure after the\n   customer lookup succeeds turns a known address into a 500 while an unknown stays 202. Both fixes\n   are worse: answering 202 on a write failure makes an outage silent; writing rows for non-existent\n   accounts is what the cost paragraph already rejects. **Documented in ADR-050.**\n4. **The response is equal in bytes, not in cost.** ADR-049 §3's mask is the KDF and a reset\n   *request* submits no password. Accepted because the residual is smaller than registration's 409,\n   which already ships. **Documented in ADR-050.**\n\n**Process note for the override policy question the TKT-21 closeout raised:** both `[critical]`/\n`[high]` overrides here were the same shape as TKT-21's two — *a reviewer rediscovering a trade-off\nan ADR already records*. That is now four instances of one pattern across two epics.\n\n#### learnings:\n\n1. **`Referrer-Policy: no-referrer` nulls the `Origin` header and breaks any origin-checked form\n   POST.** → `docs/learnings/` (repo-local, written this session) **and cited in ADR-050**. This is\n   the highest-value thing the run produced: the strictest-looking header value silently disabled the\n   feature it was protecting, and three green gate runs, two adversarial passes and 166 unit tests\n   all passed over it.\n2. **A test written to close a review finding is where the mutation check lapses.** Two of mine could\n   not fail — the bounded-resend fixture (steal-the-claim landed after the code path it targeted) and\n   the concurrency test (releasing goroutines makes them *eligible*, not overlapped). → the existing\n   `three-ways-one-test-could-not-fail` learning gains a postscript; the rule is already in AGENTS.md.\n3. **Fixing a race can create a deadlock.** The `FOR UPDATE` that closed the double-token race\n   inverted lock order against redemption. → already covered by\n   `2026-08-03-two-correct-fixes-can-compose-into-a-new-defect.md`; **cite, do not restate.**\n4. **An ADR sentence true of one thing can be read as true of another.** \"No SMTP, no queue, no\n   provider\" is exactly right about senders and was read as true about ports by the plan draft, which\n   got decision (a) wrong. → recorded in ADR-050's Context.\n\n`AGENTS.md` needs **no edit**: every rule that would have caught these is already in it (browser\nsubmit, mutation checks, fixture-can-distinguish, fixes compose). They were not followed closely\nenough, which is a discipline failure, not a missing rule.\n\n#### retro:\n\n**(1) What would have made this ticket faster or better?**\n\n- **The context-mémo's `governingAdrs` was a shaping-time snapshot missing four ADRs** — ADR-043\n  (which *decides* the public-vs-`/internal/` placement), ADR-042, ADR-022 and ADR-012. I re-resolved\n  at claim time because the skill mandates it, and it mattered. **Concrete change:** the mémo bake in\n  `references/shaping.md` should record *which* ADRs existed at bake time, so the claim-time\n  re-resolution is a diff rather than a re-derivation.\n- **The plan draft was briefed on an ADR sentence that was misleading**, and neither the drafter nor\n  the plan-review pass caught it — I found it while wiring. **Concrete change:** the plan brief's\n  \"governing ADRs\" section should ask the drafter to *verify* an ADR's factual claims against the\n  code when the claim is load-bearing, not just read the decision.\n- **`make up` is still broken (TKT-227)** and cost ~20 minutes of adapting the previous run's\n  scratch tooling to this worktree. Second run in a row.\n\n**(2) What should the humans change?**\n\n- **Decide the override policy.** The TKT-21 closeout raised it; this run adds two more instances of\n  the identical shape. Proposed rule, ready to adopt: *\"The agent may proceed past a `[high]` when an\n  ADR already records the trade-off, provided the closeout names the ADR and the reviewer's reason.\"*\n  Four for four so far.\n- **TKT-196 is now the highest-value open ticket, by evidence rather than assertion.** A browser found\n  a total feature failure that the entire automated gate could not. The tooling that found it is\n  untracked scratch in `.tkt226-browser/` and **dies with this session** — for the second time.\n- **File a ticket for the direct-completion session gap** (override #2), or accept it in writing. Right\n  now it lives only in two ADRs, which is where things go to be forgotten.\n- **`.sdlc/server.py`'s `--pr` flag is `N|url|state`, not `N:url:state`** as `tools/comment.py`'s\n  docstring says. One-character fix in the helper's usage line.\n\n*V0 is interactive: I can apply the mémo/brief changes and the `comment.py` docstring now if you want\nthem; the override policy and the two tickets are yours.*\n"
    }
  ]
}
