{
  "key": "TKT-99",
  "type": "Task",
  "status": "Building",
  "labels": [
    "agent:ai-review"
  ],
  "assignee": "agent:claude-code",
  "pr": {
    "number": 112,
    "url": "https://github.com/mathd/ticketing_system/pull/112",
    "state": "open"
  },
  "summary": "access smoke: broker-level proof that durable deletion flips /readyz (or exits) — end-to-end for TKT-97",
  "description": "TKT-97 planning decision #4 (deferred to follow-up, owner-waived gateless run). TKT-97 unit-tested the *reaction* to ConsumeContext.Closed() firing (waitConsume) but not that the nats.go library actually fires Closed() on durable deletion — that needs a live NATS. Add a smoke test in the smoke/ module: start the access consumer against a real JetStream, delete the durable (access-ticket-issuer and/or access-slot-policy) via the NATS API, assert /readyz flips to 503 (or the process exits) within a bound. Larger harness than make check's unit gate (compose orchestration + durable manipulation), which is why it was split out of TKT-97. Confidence today rests on the library's documented Closed() contract (pull.go:803-808) + TKT-97's unit reaction test; this closes the loop end-to-end.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "summary",
      "body": "<!-- sdlc:stage=backlog kind=summary -->\nBorn from TKT-97 planning open-decision #4 (E2E durable-kill test: now vs follow-up → follow-up). Code pointers: smoke/access_consumer_test.go (existing live-NATS access smoke to extend), services/access/internal/consumer/run.go (waitConsume, the reaction under test). Governing ADR: ADR-017 §256-259 (the 'loudly-unready on durable deletion' guarantee this would prove end-to-end)."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\nClaimed from Ready. Branch `TKT-99-access-durable-deletion-smoke` off `origin/main` @ 25b7f6f.\n\n**Selection reason.** Owner-directed run. TKT-99 is the deferred half of TKT-97 (planning\ndecision #4): TKT-97 shipped and unit-tested the *reaction* to `ConsumeContext.Closed()` firing\n(`services/access/internal/consumer/run.go:waitConsume`), but nothing in the repo proves the\nnats.go library actually fires `Closed()` when a durable is deleted out from under a live\nconsumer. Today's confidence is a documented library contract plus a unit test that fakes the\nsignal. This closes it end-to-end against a real JetStream. Zero open blockers (`links: []`).\n\n**⚠️ GATELESS RUN — the four human gates are waived for this ticket by explicit repo-owner\ndirective, as a process experiment.** Recorded here so the board history shows it. Substitutions:\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human moves Backlog → Ready | already given (ticket was in Ready) |\n| 2 — plan approval | human approves the plan | orchestrator resolves open decisions itself: prefer the plan-critique's recommendation; absent one, the most reversible option. Every self-made decision + rationale recorded in `kind=plan-final`. |\n| 3 — review + merge | human reviews the PR and squash-merges | orchestrator squash-merges after the adversarial ai-review converges (same triage / second-pass / churn-cap rules). Review-guide still posted on the PR first — it is the audit record. |\n| 4 — PO acceptance | PO validates COS | orchestrator self-validates each COS against test evidence on the merged code, posts the validation note, then the full `kind=metrics` closeout. |\n\nThis is **not** `risk:low` — the label is absent and stays absent. A waived gate is a recorded\nowner decision; `risk:low` is a claim about the work. Per `references/ab-stages.md` §`plan` rule 6,\na waived Gate 2 does **not** collapse the plan A/B: every arm runs and the orchestrator stands in\nas judge, disclosing the arm mapping in the stage comment.\n\n**Stop conditions still armed** (the run halts and reports instead of proceeding): gate/CI failing\ntwice on one cause with no clear fix; implementation reaching into auth, money paths, data\nmigrations or CI/deploy config beyond what the plan anticipated; a blocking finding surviving the\nchurn cap; a configured model unreachable after the documented escalation ladder; any ambiguous\ndestructive action.\n\n**Model routing for this ticket** (`.claude/sdlc.config.json`, as it stands in the working tree):\n`plan: [\"gpt-5.6-sol\"]` · `planReview: main-agent` · `implement: main-agent` ·\n`aiReview: [\"gpt-5.6-sol\"]`. Both cross-model pairs are distinct (planner ≠ plan-reviewer,\nimplementer ≠ ai-reviewer), so the load-bearing invariant holds. Both A/B arrays are currently\n**single-arm** — the A/B machinery runs but measures nothing; noted so the stage comments are not\nread as a comparison.\n\n**Governing ADR (re-resolved at claim time, not trusted from the mémo):** ADR-017 §5b —\n§236-241 (\"under Compose nothing acts on unhealthy … a visible red dot and no more\"; parking never\nself-heals) and §254-259 (\"treat *recreate the durable consumer* as equivalent to a rollback\"), the\nparagraphs that make durable deletion a first-class operational event rather than an accident. The\nmémo's `governingAdrs: [\"ADR-017\"]` was captured 2026-07-19 and still resolves; re-checked against\n`docs/adr/` at HEAD. One consequence the drafter must not miss: because §236-238 says Compose does\n**not** react to an unhealthy container, the only thing that actually removes a terminated access\nprocess under `restart: unless-stopped` is the **process exit** — so \"the red dot\" and \"the restart\"\nare two different observables with two different lifetimes, and which one a smoke test can see is a\ndesign question, not a detail.\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n**Drafted by `gpt-5.6-sol`** (codex companion, `task --effort high`, read-only — no working-tree writes). `config.models.plan` is a single-arm array, so the A/B machinery ran but measured nothing; this is one draft, not a comparison. Brief: ticket + COS candidates + ADR-017 §5b governing section + the real `run.go` / `consumer.go` / `policy.go` / `main.go` excerpts + the smoke-harness constraints (no access host port, `restart: unless-stopped`, source-order test sequencing, DeliverAll replay) + five named open decisions. Verbatim below; critique follows as `kind=plan-final`.\n\n## 1. Approach\n\n- Add one smoke test, `TestAccessPolicyDurableDeletionRestartsAndRecovers` (new), at the top of [smoke/access_consumer_test.go](/Users/matdupui/Sources/Mathieu/ticketing_system/smoke/access_consumer_test.go), before `TestAccessPoisonEventPolicy`.\n- Establish positive controls: Access is ready, `access-slot-policy` exists with the expected configuration, and the container’s initial `RestartCount`, `StartedAt`, durable `Created`, and relevant log count are recorded.\n- Delete `access-slot-policy` through `stream.DeleteConsumer`; do not simulate `ConsumeContext.Closed()`.\n- Do not try to catch the transient `/readyz` 503. Assert the durable-specific `waitConsume` error and an automatic container restart within 15 seconds.\n- Assert the recreated durable has a later `Created` timestamp and Access returns to ready with no pending policy replay within 20 seconds.\n- Require two consecutive healthy/stable observations before returning.\n- Register recovery cleanup before deletion so even an intentional RED mutation that prevents self-healing cannot poison later smoke tests.\n- Keep the test in `make check`; it should normally add only one fast container restart.\n\n## 2. Decisions\n\n### D1 — Observable\n\n**Recommendation:** use the exact termination diagnostic plus `RestartCount` and `StartedAt`. Use `/app healthcheck` only to prove recovery.\n\nRequired failure observations:\n\n- The Access logs gain one occurrence of:\n\n  ```text\n  access: access-slot-policy: consume context closed (durable deleted or subscription terminated)\n  ```\n\n- `{{.RestartCount}}` becomes greater than its baseline.\n- `{{.State.StartedAt}}` becomes later than its baseline.\n\nA host port should not be added. It would expose another internal service and still would not make the 503 deterministic: the interval between `ready.Store(false)` and process exit is only a few goroutine scheduling turns. An in-network `docker exec ... /app healthcheck` has process-launch latency and cannot distinguish a 503 from connection failure once Access exits. Either probe could miss the entire window.\n\nThe restart is the durable observable ADR-017 expects Compose to act on. The exact error connects that restart to `waitConsume` rather than merely observing container churn.\n\n### D2 — Durable\n\n**Recommendation:** delete only `access-slot-policy`.\n\nReasons:\n\n- It exercises the same `CreateOrUpdateConsumer` → `Consume` → `cc.Closed()` → `waitConsume` path as `access-ticket-issuer`.\n- Replaying policy publications invokes `UpsertSlotPolicy`, which is idempotent by envelope ID in [services/access/internal/store/policy.go](/Users/matdupui/Sources/Mathieu/ticketing_system/services/access/internal/store/policy.go).\n- Placing the test before `TestAccessPoisonEventPolicy` means the isolated smoke stream should not yet contain catalog publications, so the normal gate replay is empty.\n- Deleting `access-ticket-issuer` would replay all retained orders and revisit signed lifecycle issuance. That is materially more blast radius for no additional proof of nats.go’s `Closed()` behavior.\n- Testing both would add a second restart and replay without covering a distinct library mechanism.\n\n### D3 — Restoration\n\n**Recommendation:** make restoration a hard test postcondition and back it with cleanup.\n\nBefore deletion, register cleanup that retains a usable NATS connection until cleanup completes. If either the durable is absent or `/app healthcheck` fails, cleanup must run:\n\n```text\ndocker restart <project>-access-1\n```\n\nThen poll for up to 20 seconds until all are true:\n\n- `stream.Consumer(..., \"access-slot-policy\")` and `Info` succeed.\n- The durable has the expected filter, delivery, and acknowledgment policies.\n- `NumPending == 0` and `NumAckPending == 0`.\n- `docker exec <project>-access-1 /app healthcheck` exits zero.\n- The container is running.\n\nThe normal assertion additionally requires the recreated consumer’s `Created` timestamp to be later than the pre-deletion value. Require the full condition twice, 500 milliseconds apart, with an unchanged `RestartCount`. This avoids returning during a brief healthy interval before another failure.\n\nCleanup must use its own bounded context and report both Access logs and container state if forced recovery fails.\n\n### D4 — Gate placement\n\n**Recommendation:** keep it in the default `make check`.\n\nThis test closes a dependency-level assumption on which Access liveness depends. Making it opt-in would leave ordinary changes to nats.go, consumer configuration, `waitConsume`, or process wiring unprotected.\n\nThe success path contains no fixed multi-second sleep. A 15-second reaction bound and 20-second restoration bound are failure ceilings; the expected warm path should finish in roughly 2–6 seconds. One lower-risk durable and one restart are proportionate to the coverage.\n\n### D5 — Mutation resistance\n\n**Recommendation:** require both the newly emitted exact diagnostic and a post-deletion restart.\n\nA restart alone is insufficient. The discriminator is:\n\n1. Capture the count of the exact `access-slot-policy` termination message in Access logs.\n2. Successfully delete that live durable.\n3. Require the count to increase.\n4. Require `RestartCount` and `StartedAt` to advance afterward.\n5. Require a newly created durable.\n\nAn unrelated crash can increment `RestartCount`, but it cannot produce the durable-specific `waitConsume` message and a replacement durable with a later `Created` timestamp.\n\nC2 should be narrowed slightly: this smoke test pins delivery of `Closed()` and propagation of the distinctive termination error through `main`. The existing `TestWaitConsumeAsyncTerminationLatchesUnreadyAndErrors` in [services/access/internal/consumer/run_test.go](/Users/matdupui/Sources/Mathieu/ticketing_system/services/access/internal/consumer/run_test.go) pins `ready.Store(false)`. Removing only that store is not reliably observable through HTTP before exit; the combined smoke and unit gate pins both halves.\n\n## 3. Definition of done\n\n### C1 — Live broker reaction\n\n- A smoke test obtains the existing live `access-slot-policy` durable from `PLATFORM`.\n- Access passes `/app healthcheck` before deletion.\n- `stream.DeleteConsumer(ctx, \"access-slot-policy\")` succeeds against the real smoke JetStream.\n- Within 15 seconds, the exact termination diagnostic appears and the Access container restarts.\n- No mocked channel or direct invocation of `waitConsume` is used.\n\n### C2 — Mutation-resistant behavior\n\nAmended COS:\n\n- Neutering the `closed` arm so it no longer returns the named termination error makes the new smoke test RED.\n- Removing the arm entirely makes the test RED because neither the diagnostic nor automatic restart occurs.\n- Removing only `ready.Store(false)` makes the existing TKT-97 unit test RED; this limitation is documented rather than papered over with a racy 503 poll.\n- A random Access restart without the exact new diagnostic does not satisfy the smoke test.\n\n### C3 — Stack recovery\n\nBefore the new test returns:\n\n- The replacement `access-slot-policy` durable exists and is newer than the deleted instance.\n- Its configuration still matches `SubjectPerformancePublished`, `DeliverAllPolicy`, `AckExplicitPolicy`, and `MaxDeliver: -1`.\n- Its pending and acknowledgment-pending counts are zero.\n- Access passes `/app healthcheck` in two consecutive stable samples.\n- A cleanup fallback force-restarts Access and verifies the same healthy end-state if the main assertion exits early.\n- `TestAccessPoisonEventPolicy` and all later smoke tests pass in the same package invocation.\n\n### C4 — Gate and cost\n\n- The new test runs without an environment guard under the existing smoke build tag.\n- `make check` passes.\n- A warm before/after measurement shows no more than approximately 10 seconds added to the smoke stage; investigate rather than accepting a larger regression.\n- The test uses bounded polling, not a fixed long sleep.\n\n## 4. Files\n\n- [smoke/access_consumer_test.go](/Users/matdupui/Sources/Mathieu/ticketing_system/smoke/access_consumer_test.go)\n\n  Add the new test before `TestAccessPoisonEventPolicy`. Add imports needed for Docker commands and numeric/time parsing. Any small polling or restoration helper introduced here must be clearly new, local to this file, return errors instead of calling `Fatal` from cleanup, and include command output in failures.\n\nNo changes are planned for:\n\n- `compose.yaml`\n- `compose.smoke.yaml`\n- `scripts/smoke.sh`\n- `smoke/smoke_test.go`\n- Production Access code or ADRs\n\nThe existing `natsURL`, `project`, and `inspect` symbols are sufficient.\n\n## 5. Test plan\n\n### Test seam\n\n1. Create a 45-second overall test context.\n2. Connect to `natsURL`, construct JetStream, and obtain `PLATFORM`.\n3. Resolve `access-slot-policy` and call `Info`.\n4. Assert its exact consumer configuration.\n5. Run `docker exec <project>-access-1 /app healthcheck`; require success.\n6. Record:\n\n   - Consumer `Created`.\n   - Container `RestartCount`.\n   - Container `StartedAt`.\n   - Current count of the exact termination message in `docker compose -p <project> logs --no-color access`.\n\n7. Register connection close and restoration cleanup in an order that keeps NATS usable during restoration.\n8. Call `stream.DeleteConsumer(ctx, \"access-slot-policy\")`.\n9. Poll every 250 milliseconds for at most 15 seconds until:\n\n   - The exact diagnostic count has increased.\n   - `RestartCount` is greater than baseline.\n   - `StartedAt` is later than baseline.\n\n10. Poll restoration for at most 20 seconds and require the stable conditions from C3 twice, 500 milliseconds apart.\n\nOn failure, include the baseline/current restart metadata, consumer lookup error, healthcheck result, and recent Access logs.\n\n### Honest RED\n\nBecause TKT-99 is test-only, RED means demonstrating that the test rejects a broken version of the already-shipped behavior:\n\n1. Add the smoke test.\n2. Temporarily change the `case <-closed:` branch in [services/access/internal/consumer/run.go](/Users/matdupui/Sources/Mathieu/ticketing_system/services/access/internal/consumer/run.go) to return `nil` without the distinctive error.\n3. Rebuild the Access smoke binary and run the new test against a live isolated Compose stack.\n4. Confirm the test fails because the exact diagnostic does not appear, even if `Run` subsequently exits through another error and Docker restarts it.\n5. For the stronger mutation, temporarily make the `closed` arm wait for `ctx.Done()`; confirm the test times out waiting for both the diagnostic and automatic restart, while cleanup force-restarts Access and restores the durable.\n6. Separately remove only `ready.Store(false)` and run `TestWaitConsumeAsyncTerminationLatchesUnreadyAndErrors`; confirm the existing unit test is RED.\n7. Revert every temporary mutation.\n8. Run the Access unit tests, the new smoke test in the real stack, and finally `make check`.\n\nDo not commit any mutation used to establish RED.\n\n## 6. Risks\n\n- **The 503 is too short to observe.**  \n  Mitigation: do not assert it. Assert the exact error and restart; use readiness only after recovery.\n\n- **An unrelated restart creates a false positive.**  \n  Mitigation: require an increase in the exact durable-specific log message, advanced restart metadata, and a newly created durable.\n\n- **Docker logs contain an older matching message.**  \n  Mitigation: compare the exact message count before and after deletion rather than searching for mere presence.\n\n- **The durable is recreated before the test can observe its absence.**  \n  Mitigation: absence is not an assertion. Successful deletion plus a later `ConsumerInfo.Created` proves deletion/recreation without racing the gap.\n\n- **Docker restart backoff delays recovery in CI.**  \n  Mitigation: use a 20-second recovery ceiling, report container state and logs, and retain the cleanup fallback.\n\n- **A RED mutation leaves Access running without its durable.**  \n  Mitigation: cleanup checks both durable existence and readiness, force-restarts the container when either is missing, then waits for quiescence.\n\n- **Replay mutates lifecycle state or slows the suite.**  \n  Mitigation: delete only `access-slot-policy`, keep this test first, and require its replay backlog to drain before proceeding.\n\n- **Later changes add publications before this test.**  \n  Mitigation: retain an explicit comment that source placement is operationally significant. Policy replay remains envelope-idempotent, and the pending/ready postcondition catches failure to converge.\n\n- **Aggressive Docker polling adds overhead.**  \n  Mitigation: use 250-millisecond polling only during the short reaction window, then 500-millisecond recovery sampling; do not invoke the existing two-second `retry` where it would impose unnecessary latency.\n\n## 7. Out of scope\n\n- Exposing Access on a host port or adding `SMOKE_ACCESS_URL`.\n- Making the transient 503 window longer for testability.\n- Deleting `access-ticket-issuer` or both Access durables.\n- Re-testing every other cause of `ConsumeContext.Closed()`.\n- Changing restart policy, readiness semantics, or `waitConsume`.\n- Adding a new ADR; this plan follows ADR-017.\n- Adding an opt-in test mode or altering `scripts/smoke.sh`.\n- Adding separate lifecycle assertions: the existing smoke tail’s `verify-lifecycle` remains the safety net.\n\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n**Critique by `main-agent`** (`config.models.planReview`; planner was `gpt-5.6-sol` — cross-model\ninvariant satisfied). This is a **delta**: the `kind=plan` draft **as amended below is the plan**.\n\n## Verification of the draft (adversarial pass — every named seam checked at HEAD 25b7f6f)\n\nEvery file and symbol the draft names exists. `git grep -n … HEAD`:\n\n| Named | Verdict |\n|---|---|\n| `smoke/access_consumer_test.go`, `TestAccessPoisonEventPolicy` | exists (131 lines, alphabetically first in the package) |\n| `smoke/smoke_test.go:307 inspect(t, container, format)` | exists, wraps `docker inspect -f` |\n| `retry`, `natsURL`, `project`, `gatewayURL` package vars | exist |\n| `services/access/internal/store/policy.go` → `UpsertSlotPolicy` | exists; doc comment says *\"applies one publication event's policy, idempotently by …\"*, and `TestUpsertSlotPolicyIdempotentByEnvelope` (`pass_admission_smoke_test.go:262`) pins it. The draft's idempotency claim is **backed by a test**, not asserted. |\n| `TestWaitConsumeAsyncTerminationLatchesUnreadyAndErrors` (`run_test.go:16`) | exists |\n| durable `access-slot-policy`, `SubjectPerformancePublished`, `DeliverAllPolicy`, `AckExplicitPolicy`, `MaxDeliver:-1` (`policy.go:176-178`) | exact match |\n| container name `<project>-access-1` | matches the convention already used at `smoke_test.go` for the migrate jobs |\n\n**Two load-bearing claims I checked in the dependency and in `main`, because the whole test rests\non them and a drafter that cannot run code would have to guess:**\n\n1. **The 409 path.** `jetstream/pull.go:722` — `if errors.Is(msgErr, ErrConsumerDeleted) || errors.Is(msgErr, ErrBadRequest) { return msgErr, nil }` returns a **terminal** error from `handleStatusMsg`, which stops the subscription and closes `Closed()` (`pull.go:803-816`). The server sends `409 Consumer Deleted` to the outstanding pull request the moment the durable is deleted, so the reaction is **request-driven and prompt**, not bounded by the idle-heartbeat timeout. This is what makes a 15-second reaction ceiling generous rather than tight — **ACCEPTED, and the reason is now on the record so nobody loosens the bound later on a hunch.**\n2. **The exact diagnostic.** `policy.go:212` returns `waitConsume`'s error **unwrapped**; `awaitShutdown` passes it through because `isShutdownConsumerError(ctx, err)` is `ctx.Err() != nil && errors.Is(err, context.Canceled)` — both false here (the root ctx is live, the error is a `fmt.Errorf` string); `main.go:49-51` then prints `fmt.Fprintf(os.Stderr, \"%s: %v\\n\", serviceName, err)` and `os.Exit(1)`. The literal the draft asserts —\n   `access: access-slot-policy: consume context closed (durable deleted or subscription terminated)`\n   — is **byte-correct**. **ACCEPTED.**\n\n## Verdicts on the draft's five decisions\n\n**D1 (observable: exact diagnostic + `RestartCount`/`StartedAt`, no host port) — ACCEPTED.**\nThe reasoning is right for the right reason: ADR-017 §236-241 says Compose does not act on\nunhealthy, so the 503 window is bounded by `ready.Store(false)` → `os.Exit(1)`, a few scheduling\nturns. Polling it from the host would be a race dressed as a test. Not adding a port mapping also\nkeeps the diff inside one file — no `compose.yaml`, no `scripts/smoke.sh`, no new env plumbing.\n\n**D2 (delete `access-slot-policy` only) — ACCEPTED.** Same `CreateOrUpdateConsumer` → `Consume` →\n`cc.Closed()` → `waitConsume` path as the issuer, so nothing about the library mechanism is lost,\nand the replay is an idempotent projection upsert instead of re-running signed lifecycle issuance.\nCorrect blast-radius call.\n\n**D3 (restoration as a hard postcondition + cleanup fallback) — ACCEPTED with amendment A4.**\n\n**D4 (keep it in `make check`, no env guard) — ACCEPTED.** An opt-in test for a dependency\ncontract is a test that runs the day someone remembers to run it, which is never. The success path\nis one container restart with bounded polling and no fixed sleep.\n\n**D5 (discriminator = the exact durable-named diagnostic *count increasing*, plus advanced restart\nmetadata, plus a newer `Created`) — ACCEPTED.** Counting before/after rather than testing presence\nis the right instinct: the message is durable-specific, so an unrelated crash cannot forge it.\nThe draft's honesty about C2's limit is also correct and I am keeping it rather than papering over\nit: **removing only `ready.Store(false)` is not observable from this test**, because the process\nexits before any host-side probe could see the 503. That half stays pinned by the TKT-97 unit test\n(`run_test.go:16`). Two tests, two halves, stated — not one test pretending to cover both.\n\n## Amendments (mine — the draft did not contain these)\n\n**A1 — drop the pre-deletion \"assert its exact consumer configuration\" step.** It proves nothing\nthis ticket is about, and the post-restoration config assertion (which *does* earn its keep: it\nproves `CreateOrUpdateConsumer` recreated the durable *faithfully*, not merely that something with\nthe right name reappeared) already covers the same fields. Shortest diff that still proves the\nclaim. Recording `Created` before deletion stays — that one is load-bearing.\n\n**A2 — keep the 15s reaction ceiling, and comment *why* it is safe** with the `pull.go:722`\ncitation above. A bound with no recorded rationale is a bound the next person doubles \"to fix a\nflake\" without ever learning whether the flake was real.\n\n**A3 — raise the overall test context from 45s to 90s.** 15s reaction + 20s restoration = 35s of\nassertion ceilings before counting the cost of the `docker` calls themselves (`docker compose logs`\non a six-service stack, repeated `inspect`, `docker exec … /app healthcheck`). At 45s the context\ncan expire *during* restoration — which is precisely the C3 nightmare: a poisoned stack, and the\nnext twelve smoke tests failing with the blame pointing at them. The ceilings are what bound the\ntest; the context is only a backstop and must not be the thing that fires first.\n\n**A4 — state the `t.Cleanup` LIFO ordering explicitly.** The draft says to register cleanup \"in an\norder that keeps NATS usable\" without naming the rule. `t.Cleanup` runs **last-registered-first**,\nso: register `conn.Close()` **first** (it runs last), then the restoration cleanup, and only then\ncall `DeleteConsumer`. Get this backwards and the restoration path finds a closed connection at the\nexact moment it matters most. This is a one-line ordering constraint that silently destroys the C3\nguarantee, so it belongs in the plan, not in someone's head.\n\n**A5 — a genuinely RED test is a finding, not a test bug.** If `Closed()` does not fire on durable\ndeletion against the real broker, TKT-99 has falsified the premise TKT-97 shipped on, and the\ncorrect response is to **stop and report** — not to weaken the assertion until it passes. Naming\nthe failure mode in advance is what stops it being rationalized away at 11pm.\n\n**A6 — do not commit the RED mutation, and prove the revert.** The draft says not to commit it;\nI am adding that `git diff origin/main -- services/access/` must be **empty** before the push, and\nthat this is checked, not remembered. This ticket's whole diff is one test file.\n\n## Self-made decisions at the waived Gate 2 — and why\n\nThe owner waived Gate 2 for this run. Per `references/ab-stages.md` §`plan` rule 6 I stand in as\nthe judge. The rule given was: **prefer what the critique recommends; absent a recommendation, take\nthe most reversible option.**\n\n| # | Decision | Resolution | Rationale |\n|---|---|---|---|\n| 1 | D1 observable | restart + exact diagnostic; **no host port** | critique recommends it, and it is the more reversible option — adding a port mapping to `compose.yaml` is a change to shared infrastructure that every future smoke run inherits; a test file is deletable in one commit. Both criteria agree. |\n| 2 | D2 durable | `access-slot-policy` only | critique recommends it; also the lower-blast-radius option. |\n| 3 | D3 restoration | hard postcondition + force-restart fallback | critique recommends it. |\n| 4 | D4 gate placement | in `make check`, no env guard | critique recommends it. Noting the tension honestly: opt-in would be the *more reversible* choice, so the two criteria **disagree** here, and I took the critique's recommendation as instructed. If the smoke stage regresses by more than ~10s the call flips to opt-in — that threshold is C4 and it is measurable, not a matter of taste. |\n| 5 | COS C2 scope | **narrowed** as the critique proposes | C2 as I wrote it at claim time overclaimed: no host-side test can observe the removal of `ready.Store(false)`, because the process exits first. Keeping the original wording would have shipped a COS that the test does not meet and that a validation note would have had to fudge. Narrowed, with the uncovered half explicitly assigned to the existing unit test. |\n| 6 | A1–A6 | applied | mine; see the section immediately below. |\n\n**Amended COS (this is the set Gate 4 validates against):**\n\n- **C1** — a smoke test deletes the live `access-slot-policy` durable through the real JetStream API\n  and observes access reacting with the durable-specific `waitConsume` diagnostic plus an automatic\n  container restart, within 15s. No mocked channel, no direct `waitConsume` call.\n- **C2** — the test is RED if the `closed` arm of `waitConsume` is neutered or removed. *Scope\n  stated:* it pins `Closed()` delivery and the propagation of the named error through `main`; it\n  does **not** pin `ready.Store(false)` (unobservable before process exit) — `run_test.go:16` pins\n  that half.\n- **C3** — before the test returns, the durable is recreated with its configuration intact and zero\n  pending, and access passes `/app healthcheck` on two consecutive samples 500ms apart; a cleanup\n  fallback force-restarts access if the main path exits early. Every later smoke test passes in the\n  same package invocation.\n- **C4** — `make check` green; the smoke stage grows by no more than ~10s warm; bounded polling, no\n  fixed long sleep.\n\n## Introduced at plan-review, reviewed by no second model\n\nPer the skill's required section — these reach the (waived) Gate 2 having been reviewed by nobody\nbut their author, and are called out again in the hand-off for exactly that reason:\n\n- **A1** — removal of the pre-deletion config assertion. *Lowest risk of the six: it deletes an\n  assertion, so the worst case is slightly weaker evidence, not a wrong test.*\n- **A2** — retaining 15s with a recorded rationale. *No behavioural change; a comment.*\n- **A3** — 45s → 90s overall context. *Loosens a backstop. Cannot mask a failure: the 15s and 20s\n  ceilings still fire first and are what the assertions are written against.*\n- **A4** — the `t.Cleanup` LIFO ordering constraint. **The riskiest of the six if I have it\n  backwards** — but it is mechanically checkable at implementation time (`t.Cleanup` is documented\n  LIFO), and the implementer must verify it rather than take my word.\n- **A5** — \"a real RED is a finding, stop and report\". *Process, not code.*\n- **A6** — assert `git diff origin/main -- services/access/` is empty before pushing. *Process, and\n  a strictly additional check.*\n\n## Plan of record\n\nThe `kind=plan` draft, **as amended by A1–A6 and with COS C2 narrowed as above**, is the plan.\nFiles: `smoke/access_consumer_test.go` only. No production code, no `compose.yaml`, no\n`scripts/smoke.sh`, no ADR.\n"
    },
    {
      "stage": "coding",
      "kind": "summary",
      "body": "<!-- sdlc:stage=coding kind=summary -->\n**Implemented by `main-agent`** (`config.models.implement`). PR: https://github.com/mathd/ticketing_system/pull/112 · branch `TKT-99-access-durable-deletion-smoke` · commit `5d46dcb`.\n\n```yaml\nstage: coding\nimplementer: main-agent\nfiles: 1        # smoke/access_consumer_test.go\ndiff: \"+232/-1\"\nlocal_gate: \"make check — green (judged from log body: 0 hits for 'Error N|FAIL|drifted'; 64 PASS; smoke ok 55.8s; sentinel EXIT=0)\"\nnew_test_runtime: \"1.23s in-gate (1.68s on the iteration stack)\"\nproduction_code_changed: none\n```\n\n## TDD — RED observed twice against a live stack, before green\n\nThis is a test-only ticket, so \"red\" means the test rejects a broken version of the\nalready-shipped behaviour. Both mutations were applied to\n`services/access/internal/consumer/run.go`, rebuilt (`make build-gate-linux`), and run against a\nreal Compose stack.\n\n**RED 1 — `case <-closed:` returns `nil` instead of the named error.**\nTest failed: `termination diagnostic count still 0 (want > 0)`.\n\nThe interesting part, and the reason this test is shaped the way it is: **the container restarted\nanyway.** With `waitConsume` returning nil, `policy.go`'s trailing\n`return fmt.Errorf(\"policy consumer stopped: %w\", ctx.Err())` fired against a nil `ctx.Err()` and\nthe process exited — the log shows `access: policy consumer stopped: %!w(<nil>)`. A test that\nasserted only \"access restarted\" would have passed this broken build. Requiring the exact\ndurable-named diagnostic is what caught it. D5's discriminator is now **demonstrated, not argued** —\nthis is the single most valuable thing the RED pass produced.\n\n**RED 2 — `case <-closed:` blocks on `<-ctx.Done()` (no termination at all).**\nTest failed the same way, and access never restarted — which is what exercised the recovery\nfallback. Cleanup force-restarted the container and restored the stack: durable recreated at\n16:42:17, `/app healthcheck` exit 0. **C3's safety net is verified rather than assumed**, which\nmattered: it is the only path that protects the other 63 tests in the package.\n\n**GREEN** — mutation reverted, rebuilt, test passed in 1.68s. Reaction was sub-second, which\nindependently confirms the plan-review's `pull.go:722` reasoning: the 409 is request-driven, not\nheartbeat-bound.\n\n**A6 discharged:** `git diff origin/main -- services/access/` returns **0 lines**. No mutation\nreached the branch. Checked, not remembered.\n\n## Plan adherence\n\nEvery amendment A1–A6 from `kind=plan-final` landed as written:\n\n| | Amendment | Landed |\n|---|---|---|\n| A1 | drop the pre-deletion config assertion | yes — config is asserted once, post-recovery, where it proves `CreateOrUpdateConsumer` rebuilt the durable *faithfully* |\n| A2 | keep 15s, record why | yes — the `pull.go:722` citation is in the test comment, aimed at the next person who wants to raise it after a flake |\n| A3 | 90s overall context | yes, with a comment saying it is a backstop and the 15s/20s ceilings must fire first |\n| A4 | `t.Cleanup` LIFO ordering | yes — `connection.Close` registered first (runs last), recovery cleanup second. **A4 was the amendment I flagged as riskiest because it was mine and unreviewed; it is correct, and RED 2 exercised the exact path it protects.** |\n| A5 | a real RED is a finding | not triggered — `Closed()` fires as documented |\n| A6 | prove the revert | yes, above |\n\nDeviations from the draft: **none** beyond A1–A6.\n\n## Files\n\n`smoke/access_consumer_test.go` (+232/−1). One test\n(`TestAccessDurableDeletionTerminatesAndRecovers`), three local helpers (`dockerRun`,\n`accessLogCount`, `restartState`, `poll`) and three constants. No production code, no\n`compose.yaml`, no `scripts/smoke.sh`, no ADR — exactly the file list the plan committed to.\n\nReused rather than reinvented: `inspect`, `project`, `natsURL` from `smoke_test.go`. `poll` is new\nrather than reusing the package's `retry` because `retry` fixes a 2s interval and calls `Fatal` —\nboth wrong here (2s granularity would eat most of a 15s budget, and the cleanup path cannot call\n`Fatal`). That is a stated trade, not an oversight.\n"
    },
    {
      "stage": "ai-review",
      "kind": "summary",
      "body": "<!-- sdlc:stage=ai-review kind=summary -->\n**Reviewed by `gpt-5.6-sol`** (`config.models.aiReview`, codex `adversarial-review`, branch diff\nvs base, primed for guilt, plan withheld). Implementer was `main-agent` — cross-model invariant\nsatisfied. Single-arm array: the A/B machinery ran, measured nothing; **not** a comparison.\n\n**Three passes, not one.** Each judged the *previous pass's fix diff*, which is the code nobody had\nreviewed. Passes 2 and 3 are convergence, not churn, and the distinction is load-bearing here:\npass 2 did not re-litigate pass 1, it found that **pass 1's fix did not hold**.\n\nA harness note for the record: the first launch wrapped the runner in `timeout`, which does not\nexist on macOS — exit 127, the model never ran. Re-launched via harness backgrounding (the\nalternative `codex-runner.md` names). Not counted as a rung failure, because rung 1 was never\nactually attempted. Worth knowing for the next macOS run.\n\n## Findings and verdicts\n\nEvery finding verified against the revision under review (branch HEAD), never against `origin/main`.\n\n| # | Pass | Sev | Finding | Verdict | Action |\n|---|---|---|---|---|---|\n| R1 | 1 | high | cleanup declared recovery on ONE healthy sample; the main assertion required two | **CONFIRMED** | fixed — shared `settled` helper, both paths |\n| R2 | 1 | high | `dockerRun` used `exec.Command` with no context → every deadline fictitious | **CONFIRMED** | fixed — `exec.CommandContext`, 15s per-command ceiling |\n| R3 | 1 | high | file-ordering comment claimed \"cost optimisation, not a correctness requirement\" | **CONFIRMED — the comment was wrong** | fixed — comment corrected, draining error names the suspect |\n| R4 | 1 | med | the diagnostic can't prove deletion *caused* the termination | **CONFIRMED, not fixed here** | → **TKT-123** (backlog) |\n| R5 | 2 | high | the bracket-less sample let a restart between health and count go undetected — **R1's fix did not hold** | **CONFIRMED** | fixed — sample bracketed by restart counts |\n| R6 | 2 | med | phase deadlines didn't bound the subprocesses inside them — **R2's fix was incomplete** | **CONFIRMED** | fixed — `settled` derives a context bounded by `within` |\n| R7 | 3 | med | bracket compared only `RestartCount`, which is not a process identity | **CONFIRMED (in part)** | cheap half applied; expensive half declined |\n\n**R4 → TKT-123.** Real, and correctly out of scope: `waitConsume`'s message covers every async\ntermination, so the fix is a production diagnostic change. A test-only ticket has no business\nmaking one. Filed with the code pointers and the ADR-017 framing.\n\n**R7, partially declined — stated plainly.** The finding's core is right and the fix was free:\n`restartState` already returned `StartedAt` and the bracket discarded it, so comparing both costs\none comparison. Declined: the container-ID identity struct and the \"deterministic helper tests for\nfirst sample / error reset / mid-sample change / between-sample change / deadline exhaustion\" —\nthat is a test harness for a test helper inside a test file, and the coverage does not justify the\ncode. Recorded as a judgement call, not an oversight.\n\n## Second/third-pass calls\n\n- **Pass 2: earned.** Pass 1's fixes changed control flow, changed two helper signatures, and added\n  context plumbing — non-trivial by every clause of the rule. It found R5 and R6, and R5 in\n  particular meant the crash-loop invariant R1 supposedly restored **was not actually holding**.\n  Had the loop stopped at one pass, the PR would have shipped a comment describing a guarantee the\n  code did not provide. This is the single strongest argument in this ticket for the second-pass rule.\n- **Pass 3: earned by the same rule** — pass 2's fix reset the counter, since it proved the previous\n  fix wrong at the root rather than polishing it.\n- **Stopped at 3.** One finding, medium (down from two highs), strictly narrower than its\n  predecessor: converging, and the next pass would be churn.\n\n## ⚠️ The last fix diff is unreviewed — stated, not buried\n\nCommit `44bb945` (R7's fix) has **not** been through an adversarial pass. It is two comparisons and\na comment. **I verified it myself, against a live stack, and I am labelling that as my own work\nrather than as review.** Also carried in the PR review-guide, per the skill's requirement when the\nloop stops with an unreviewed fix diff.\n\n## Gate\n\n`make check` green on the final diff, judged from the log body (`grep -E 'Error [0-9]+|FAIL|drifted'`\n→ zero hits; 64 PASS; `ok ticketing/smoke 54.5s`; sentinel `EXIT=0`). New test **1.95s** in-gate —\nC4's budget was ~10s. Smoke stage 54.5s vs 55.8s on the pre-fix run, so no regression.\n\nLocal `HEAD` == `origin/TKT-99-access-durable-deletion-smoke` == `44bb945` — verified before the\nmerge, per the TKT-97 trap.\n"
    }
  ],
  "context": {
    "binding": [],
    "governingAdrs": [
      "ADR-017"
    ],
    "reference": [
      {
        "summary": "smoke/access_consumer_test.go; waitConsume",
        "link": "TKT-97 planning decision #4"
      }
    ],
    "capturedAt": "2026-07-19"
  }
}
