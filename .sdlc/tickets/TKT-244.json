{
  "key": "TKT-244",
  "type": "Story",
  "summary": "US-CH2b: Slot channel-allocation editor — and the inventory credential it needs",
  "status": "Building",
  "labels": [
    "agent:coding"
  ],
  "assignee": "agent (Claude Opus 5)",
  "pr": null,
  "parent": "TKT-17",
  "description": "**Carved out of TKT-236 when its `governingTests` bake found the back office cannot reach the\nendpoint at all.** The screen is the small half of this ticket; the credential is the real subject,\nand giving it its own ticket is the point of the split.\n\nChannel allocations shipped in TKT-78: `PUT /internal/slots/{id}/channel-allocations` atomically\nreplaces a slot's whole allocation set under the pool lock. There is no UI, and there cannot be one\nuntil the back office can call it.\n\n**The blocker, verified at HEAD:**\n- `compose.yaml:379-392` gives the back office `GATEWAY_URL`, `CATALOG_STAFF_WRITE_TOKEN`,\n  `COMMERCE_URL`, `COMMERCE_STAFF_WRITE_TOKEN`. There is **no `INVENTORY_URL`** and no internal token.\n- The comment there refuses the shared one outright: *\"that one value opens every service's internal\n  surface, and this is a public-facing SSR process.\"*\n- `web/backoffice/test/api.test.ts:471` pins it as an assertion: `X-Internal-Token` must be\n  `undefined`.\n- **Inventory has no staff-write credential at all** — only `INTERNAL_SERVICE_TOKEN`. Catalog's\n  equivalent exists because TKT-191 created it; inventory's does not exist yet.\n\nSo this ticket creates a **new per-service credential for inventory**, following the precedent\nTKT-194 set for commerce (`COMMERCE_URL` + `COMMERCE_STAFF_WRITE_TOKEN`, reached directly in-network\nbecause the gateway edge-denies `/internal/`).\n\n**Conditions of Success:**\n- An ADR records the inventory staff-write credential: what it opens, why it is not the shared\n  internal token, and how it relates to the three existing identity classes (ADR-042 staff sessions,\n  ADR-043 internal tokens, ADR-049 customer sessions). ADR-043 draws the guard-placement line; this\n  is a new credential, not a new placement.\n- The credential is generated independently in `scripts/stack-env.sh` (a separate `/dev/urandom`\n  read, never a copy — `stack-env.sh:55-61`), added to `web/backoffice/credentials.mjs`, and refused\n  at startup if absent or equal to another (`credentials.test.ts:14-44`).\n- Inventory guards the allocation write with it. **`X-Internal-Token` must still work** — access and\n  other services use that surface — so this is an additional accepted credential on one route, not a\n  replacement.\n- A back-office slot page shows the current allocation set with each channel's cap, `release_at` and\n  **current consumption**, and submits the full set as **one atomic replace**. The shipped PUT is a\n  full-set replace under the pool lock, so per-row saves would round-trip a stale set.\n- The two server-side refusals surface as field errors, not a generic failure: caps summing above\n  pool capacity, and a cap below that channel's current consumption.\n- Allocation rows for channels absent from the registry render with their raw code and a marker —\n  the registry is a lookup, not a constraint (TKT-235), and pre-existing codes stay editable.\n- **Browser-verified:** `make browser` plus a spec in `test/browser/` that *submits* the form.\n  Note `scripts/browser.sh` provisions **no** staff account — the spec must provision its own.\n\nQuality gates: `make check` green; `make browser` green with the new spec; screenshot in the PR;\nOpenAPI/types regenerated and committed before the gate.\n\nParent: TKT-17. Depends on TKT-236 (the back-office channels surface it extends). Non-goals: seated\npools (TKT-176 owns the seated seam); per-channel pricing UI; the reseller API's partner credential\n(TKT-240 — a *machine-to-machine* class, distinct from this staff-operated one, though TKT-240's ADR\nshould cite this one).",
  "readiness": {
    "objective": {
      "state": "met",
      "note": "Channel allocations have shipped since TKT-78 with no UI, and the back office cannot reach the endpoint. Carved out of TKT-236 so the credential decision is the subject rather than an obstacle."
    },
    "scope": {
      "state": "met",
      "note": "IN: an inventory staff-write credential + its ADR + stack-env/credentials wiring, the allocation editor screen, the two refusal paths as field errors, a browser-submit spec. OUT: seated pools (TKT-176), the reseller partner credential (TKT-240), pricing UI."
    },
    "uncertainties": {
      "state": "open",
      "owner": "human",
      "note": "Whether inventory's new credential guards ONLY the allocation route or its whole /internal/ surface. Narrow is more reversible (widening later is additive); whole-surface is more consistent with how catalog's staff-write credential works. An ADR decides it — this is the ticket's real content and should not be settled while building a form."
    },
    "dependencies": {
      "state": "open",
      "note": "Blocked by TKT-236 (the channels surface this extends). TKT-235 is Done."
    },
    "approach": {
      "state": "open",
      "owner": "human",
      "note": "Depends on the uncertainty above. shaping.md forbids approach:deferred on an auth-touching ticket, so this holds at Gate 1 by design — that is the whole reason this was split out of TKT-236."
    },
    "first_slice": {
      "state": "met",
      "note": "Credential + guard + editor in one slice. Splitting further would ship a credential nothing uses, or a screen that cannot save."
    },
    "success": {
      "state": "met",
      "note": "COS above; the browser-submit spec and the two field-error paths are the acceptance signals."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked below, inherited and extended from TKT-236's."
    }
  },
  "context": {
    "binding": [
      {
        "id": "ADR-001",
        "rule": "Money is represented as integer minor units + ISO currency code everywhere; floats are banned on money paths.",
        "date": "2026-07-12"
      }
    ],
    "governingAdrs": [
      {
        "id": "ADR-024",
        "why": "The allocation semantics this screen edits: full-set atomic replace under the pool lock, caps summing <= capacity, each cap >= its channel's current consumption, release_at as a clock_timestamp() predicate. The UI must not offer per-row saves."
      },
      {
        "id": "ADR-043",
        "why": "Draws the contract-operation vs internal-route line for where a guard LIVES. This ticket adds a new credential, not a new placement — say so explicitly in the ADR so the two are not conflated."
      },
      {
        "id": "ADR-042",
        "why": "Staff identity and back-office sessions — the human side of this credential; the operator is a staff session, the credential is how the SSR process proves itself to inventory."
      },
      {
        "id": "ADR-049",
        "why": "Customer identity — the third existing class, named so the new ADR positions itself against all three."
      },
      {
        "id": "ADR-002",
        "why": "The gateway edge-denies /api/<svc>/internal/, which is why the back office reaches commerce directly in-network (TKT-194) rather than through the gateway. Same shape here."
      },
      {
        "id": "ADR-009",
        "why": "Contract-first: inventory's allocation route is declared in its OpenAPI document, so a new security scheme is a contract edit and generated code must be committed before the gate."
      },
      {
        "id": "ADR-021",
        "why": "Name the adversary: a per-service credential limits blast radius against a compromised SSR process. It is not tamper-evidence, and the ADR must not claim it is."
      }
    ],
    "governingTests": [
      {
        "test": "web/backoffice/test/api.test.ts:471",
        "rule": "The back office must NEVER send X-Internal-Token. A new inventory client must use its own credential header, not this one."
      },
      {
        "test": "web/backoffice/test/credentials.test.ts:14-44",
        "rule": "Every back-office credential must be set and must DIFFER from the others; the startup refusal must name the missing variable and never echo its value."
      },
      {
        "test": "web/backoffice/test/credentials.test.ts:53-74",
        "rule": "The entrypoint spawns start.mjs and must exit 1 BEFORE importing dist/ — a new credential joins credentials.mjs, not just the client."
      },
      {
        "test": "scripts/stack-env.sh:55-61",
        "rule": "Each credential is an independent /dev/urandom read. 'A separate read, not a copy' — deriving one from another defeats the separation."
      },
      {
        "test": "web/backoffice/test/authorization.test.ts:63",
        "rule": "Every file under src/pages/** needs a ROUTE_MATRIX row and every row must name a file; a new page with no row fails the suite."
      },
      {
        "test": "web/backoffice/test/gate.test.ts:313",
        "rule": "An unclassified route 403s for every role including admin — a page without a matrix row renders 403, not the page."
      },
      {
        "test": "scripts/browser.sh:73-74",
        "rule": "test/browser/*.mjs is a glob with no allowlist, but browser.sh provisions NO staff account — a back-office spec must provision its own via compose exec catalog /app provision-staff."
      },
      {
        "test": "scripts/browser.sh:38-44",
        "rule": "Preflight checks storefront and scanner dist but NOT web/backoffice/dist, though Dockerfile.smoke COPYs it — a stale dist fails at docker build with a confusing error."
      },
      {
        "test": "smoke/coverage_test.go:92",
        "rule": "Every documented 2xx op of inventory must be driven by an unfiltered smoke run — a new or changed inventory operation needs a driving test."
      }
    ],
    "reference": [
      {
        "summary": "PRD v1 — TKT-17 capability row",
        "link": "docs/product/prd-v1.md"
      },
      {
        "summary": "CREDENTIAL BLAST RADIUS — the back office is deliberately denied INTERNAL_SERVICE_TOKEN; the comment states why, and TKT-190 is the worked example of proposing it anyway",
        "link": "compose.yaml:379-392"
      },
      {
        "summary": "The precedent to follow: COMMERCE_URL + COMMERCE_STAFF_WRITE_TOKEN, reached directly in-network because the gateway edge-denies /internal/",
        "link": "web/backoffice/src/lib/commerce.ts"
      },
      {
        "summary": "The endpoint being wrapped — full-set atomic replace, no If-Match (ADR-024 accepts this while allocation editing is single-operator)",
        "link": "services/inventory/api/openapi.yaml:217"
      }
    ],
    "capturedAt": "2026-08-10"
  },
  "links": [
    {
      "type": "blocked-by",
      "key": "TKT-236"
    }
  ],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### ⚠️ Implementation readiness — CONCERNS (holds at Gate 1 by design)\n\nCarved out of TKT-236 on the owner's decision, after that ticket's `governingTests` bake found the\nback office cannot reach either endpoint it needed.\n\n| Criterion | Status | Note |\n|---|---|---|\n| Objective | ✅ | Shipped allocation machinery with no operator UI |\n| Scope | ✅ | Credential + guard + editor; seated pools stay TKT-176's |\n| Uncertainties | ⚠️ | **Narrow vs whole-surface guard is an ADR decision, `owner: human`** |\n| Dependencies | ⚠️ | Blocked by TKT-236 |\n| Approach | ⚠️ | Follows from the uncertainty; `owner: human` |\n| First slice | ✅ | Splitting further ships a credential nothing uses, or a screen that cannot save |\n| Success | ✅ | Browser-submit spec + the two field-error paths |\n| Context-mémo | ✅ | Baked, incl. 9 governing tests and the blast-radius entry |\n\n**Concerns:**\n1. **This ticket creates a new credential class.** That is its subject, not a side effect — which is\n   exactly why it was split out. `shaping.md` forbids `approach: deferred` on an auth-touching\n   ticket, so this holds at Gate 1 until the owner decides the guard's scope.\n2. Blocked by TKT-236.\n\n**Verdict:** CONCERNS. Correctly shaped and correctly blocked. **Under this run's standing waiver\nthe agent would decide the credential scope itself — the owner may prefer to take this one, as both\nprior closeouts recommended for TKT-240.**"
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — TKT-244\n\n**Branch:** `TKT-244-inventory-staff-credential-allocation-editor` off `origin/main` @ `c28697d1`.\n**Backend:** local tracker (`config.tracker: local`); state on `sdlc-state`.\n\n**⚠️ THIS RUN IS GATELESS — all four human gates waived by the repo owner.**\n\nThe owner, who is the repo owner and the PO for this board, explicitly waived Gates 1–4 for this run\nas a **process experiment**, and asked that the waiver be recorded here so the board history shows\nthe ticket ran without them. Recorded verbatim in effect:\n\n| Gate | Normally | This run |\n|---|---|---|\n| 1 — priority | human moves Backlog → Ready | **waived**; treated as given |\n| 2 — plan approval | human moves Planning → Building | **waived**; agent resolves open decisions, preferring the critique's recommendation, else the most reversible option; every self-made decision recorded in `kind=plan-final` |\n| 3 — review + merge | human reviews and squash-merges | **waived**; agent squash-merges after the ai-review converges. Review-guide still posted first, as the audit record |\n| 4 — PO acceptance | PO validates COS | **waived**; agent self-validates each COS against test evidence on the merged code |\n\nEverything else in the skill applies unchanged: `config.models` routing with no substitutions,\ncross-model review for both plan and code, TDD with tests observed red first, `make check` green\nbefore every push and after the merge, all marker comments, no AI attribution.\n\n**Entry-status correction (the skill's \"read the ticket's actual status\" rule).** The run was handed\nover as \"the ticket is in Ready\". It was **not** — TKT-244 sat in **Backlog** with a `kind=readiness`\nverdict of **CONCERNS**, held there *by design*. So this is not a Gate 1 rubber-stamp; the hold had\ntwo distinct causes and they resolve differently:\n\n1. **`dependencies: open` — blocked by TKT-236.** ✅ **Genuinely resolved, not waived.** TKT-236 is\n   **Done** (PR #194 merged). The link is stale, not live. No waiver needed for this half.\n2. **`uncertainties: open` + `approach: open`, both `owner: human`** — whether inventory's new\n   credential guards **only** the allocation route or its **whole `/internal/` surface**. This is a\n   real open decision and it is the one the waiver actually covers. `shaping.md` forbids\n   `approach: deferred` on an auth-touching ticket, which is precisely why this ticket exists as a\n   separate ticket. **Under the waiver I will decide it at `kind=plan-final`, preferring the\n   critique's recommendation, else the more reversible option — and record the reasoning.**\n\nThe readiness comment anticipated exactly this and flagged that **the owner may prefer to take this\none personally**, as both prior closeouts recommended for TKT-240. The waiver is explicit and\npost-dates that note, so I proceed — but it is called out here so the choice is visible in the\nhistory rather than buried in a plan comment.\n\n**Risk verdict: `risk:low` NOT claimed, and it would be wrong to claim it.** Stated explicitly\nbecause the label's carve-outs are exactly the tickets whose risk looks obvious to the claimer and\ninvisible to everyone afterwards (TKT-190). This ticket hits **three** carve-outs at once:\n\n- **auth** — it creates a new credential class for a service that has none; that is the ticket's\n  entire subject, not a side effect;\n- **CI/deploy config** — `scripts/stack-env.sh`, `compose.yaml`, `web/backoffice/credentials.mjs`;\n- **>5 files** — inventory API + contract + back-office client + page + route matrix + browser spec\n  + ADR, at minimum.\n\nSo the plan gate would have been mandatory here. It is waived by owner decision, not by risk level —\nthose are different things and the distinction matters for reading this history later.\n\n**Why this ticket, now.** TKT-246 (merged 2026-08-13) made `channel_allocations.sold_by`\nload-bearing: inventory now refuses a claim whose seller does not match, under the pool row lock.\nThe authorization primitive shipped, but **no operator can reach it** — `PUT\n/internal/slots/{id}/channel-allocations` has no UI, and the back office holds no credential for\ninventory at all. TKT-246 also inherited a live deploy prerequisite from TKT-240's analysis: a\nchannel with **no active allocation is refused outright**, so every channel in use needs an\nallocation configured before that path is safe. This ticket is what lets an operator do that.\n\n**Facts re-verified at HEAD (`c28697d1`) rather than trusted from the mémo** — the mémo was baked\n2026-08-10 and two ADRs have landed since:\n\n- `compose.yaml:410-444` — the back office holds `GATEWAY_URL`, `CATALOG_STAFF_WRITE_TOKEN`,\n  `COMMERCE_URL`, `COMMERCE_STAFF_WRITE_TOKEN`, `CATALOG_URL`. **Still no `INVENTORY_URL`**, still\n  no inventory credential. ✅ ticket's premise holds.\n- Inventory's guard is `internalOnly` (`services/inventory/internal/api/operational.go:19`), a single\n  wrapper checking the shared `INTERNAL_SERVICE_TOKEN`, applied to all 15 `/internal/` routes\n  including `PUT /internal/slots/{id}/channel-allocations` (`server.go:116`). ✅ no staff-write\n  credential exists in inventory.\n- **New since the mémo: ADR-053 (TKT-236) chose \"NO NEW CREDENTIAL\"** for catalog's operator read,\n  reusing `CATALOG_STAFF_WRITE_TOKEN` on one exact method+path. That is a direct precedent *against*\n  this ticket's presumed shape, and its reasoning does **not** transfer unexamined: ADR-053's case\n  rested on the credential already holding the capability, which is false here — the back office\n  holds nothing for inventory. The planning stage must weigh it rather than inherit the ticket's\n  assumption. Also new: **ADR-056** (partner credential identity, TKT-246).\n- **Registry hygiene flag:** two files claim **ADR-055** —\n  `ADR-055-on-sale-write-rate-limiting.md` and `ADR-055-presale-unlock-codes.md`. A collision, not\n  this ticket's to fix; the new ADR here must not take a colliding number.\n\n**Governing ADRs re-resolved at claim time** (mémo list + what landed since): ADR-024, ADR-043,\nADR-042, ADR-049, ADR-002, ADR-009, ADR-021, **ADR-053**, **ADR-056**.\n\nTransitioning Backlog → Ready → Planning (`agent:planning`).\n"
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### 📋 Execution plan — TKT-244 (draft)\n\n**Drafted by `gpt-5.6-luna`** via the codex companion (`task --model gpt-5.6-luna --effort high`),\nper `config.models.plan`. Read-only run; the drafter touched no file. Codex session\n`01a002b8-5c1e-7fb2-b931-bd633cc53700`, one rung, conclusive verdict, no escalation needed.\n\nThe drafter was briefed with the ticket, the COS, the re-resolved governing ADRs (incl. **ADR-053**\nand **ADR-056**, both of which post-date the mémo bake), the nine governing tests, and ~40 verified\ncode facts at HEAD. It was explicitly told to weigh ADR-053's \"NO NEW CREDENTIAL\" precedent rather\nthan inherit the ticket's presumption.\n\n---\n\n#### 1. Decision — the credential's scope\n\n**Recommends a new `INVENTORY_STAFF_WRITE_TOKEN`, accepted on exactly two operations:**\n\n- `GET  /internal/slots/{id}/availability` (the page's read — it needs consumption)\n- `PUT  /internal/slots/{id}/channel-allocations` (the save)\n\nBoth remain **additionally** authorized by `X-Internal-Token`. The allowance is **method-and-path\nexact, not a `/internal/` prefix** — the other 13 internal routes keep the plain `internalOnly`\nwrapper. Guard stays inline, so this is a new *credential*, not a new *placement* (ADR-043).\n\n**Why ADR-053 does not transfer** (the drafter's argument, which I checked and accept): ADR-053's\ncase rested on the catalog credential *already holding* create/update power over channels, so\naccepting it on one read added amplification but no new capability class. **The back office holds\nnothing for inventory today.** Reusing `CATALOG_STAFF_WRITE_TOKEN` would hand a catalog credential\npower over *inventory capacity* and couple two services' blast radii — a catalog-token compromise\nwould become an inventory-write compromise. Different question, different answer.\n\n**Why not whole-surface:** no page needs it; it would grant a public-facing SSR process every\ninventory operational capability (holds, capacity adjustments, group reservations, transitions,\ncache control), and later narrowing would be a security migration rather than an additive change.\nWidening a route-scoped token later is additive; narrowing a broad one is not.\n\nReversal cost: remove the second header acceptance, the env wiring and the client calls, then rotate.\n\n#### 2. Definition of Done\n\nMaps 1:1 to the COS — ADR-057; independent `/dev/urandom` draw wired to both services; the\ntwo-operation acceptance with `X-Internal-Token` still working; the page showing cap + `release_at` +\ncurrent consumption with unknown-channel markers and one atomic save; the two refusals as field\nerrors; **round-trip preservation of `opens_at`/`closes_at`/`requires_code`/`sold_by`**; a\n`test/browser/` spec that submits; generated artifacts committed before the gate.\n\n#### 3. Files to touch (drafter's list, condensed)\n\n- **Inventory Go** — `internal/api/server.go` (staff credential on `Server`, a second wrapper on the\n  two routes, coded 409s), `internal/api/operational.go` (`internalOnly` unchanged for the other 13),\n  `cmd/inventory/main.go` (`runtimecfg.RequiredCredential`, reject equality with the shared token),\n  `internal/store/store.go` (typed allocation errors), `internal/store/channel_allocations.go`\n  (carry `requires_code` + `sold_by` on the availability read).\n- **Contract** — `api/openapi.yaml` (Error codes + optional `channel`; `ChannelAvailability` gains\n  two fields), `openapi_gen.go`, plus a new `inventory-api-types.gen.ts` wired into\n  `generate:api` and `check-generate`.\n- **Back office** — new `lib/inventory.ts` (direct in-network, `X-Inventory-Staff-Write-Token`, never\n  `X-Internal-Token`), new `lib/allocation-form.ts` (pure mapping/serialization),\n  a slot editor page, `lib/authorization.ts` (ROUTE_MATRIX row, admin-only), `credentials.mjs`\n  (three-way pairwise separation), plus `test/api.test.ts` and `test/credentials.test.ts`.\n- **Scripts/compose** — `stack-env.sh` independent draw; `compose.yaml` injects the credential into\n  inventory and adds `INVENTORY_URL` + the credential to backoffice.\n- **ADR** — `docs/adr/ADR-057-inventory-staff-write-credential.md` (explicitly *not* 055, which is\n  already duplicated).\n- **Tests** — inventory api/store suites, `allocation-form.test.ts`, `test/browser/slot-allocations.mjs`;\n  the five existing smoke drivers must keep working on `X-Internal-Token`.\n\n#### 4. Test seam\n\nPer-tier rather than one fake-backed test, which is the right call under AGENTS.md's \"a test must\nlive at the tier its mechanism does\":\n\n- **`services/inventory/internal/api/server_test.go`** — absent/wrong staff credential → 401; empty\n  configured value fails closed; correct credential reaches the handler; **the staff credential is\n  refused on an unrelated internal route and on the wrong method/path**; `X-Internal-Token` still\n  accepted on both. *Mutations:* delete the staff branch, allow an empty configured value, widen the\n  wrapper to the whole prefix, or swap OR for AND.\n- **`internal/store/channel_allocations_smoke_test.go`** — typed refusal identity (over-capacity vs\n  cap-below-consumption **carrying the offending raw channel**), plus a preseeded allocation with\n  non-null windows, `requires_code=true` and non-null `sold_by`, edited and re-read to prove nothing\n  was dropped. *Mutations:* remove an INSERT column, omit a scan assignment, return the generic\n  sentinel, name the wrong channel.\n- **The `problem()` seam** — exact 409 bodies for both typed errors, `channel` present only on the\n  channel-specific one.\n- **`web/backoffice/test/api.test.ts`** — direct `INVENTORY_URL`, the inventory header, all\n  allocation fields in the body, and **no `X-Internal-Token`**.\n- **`allocation-form.test.ts`** — capacity error → the total field; consumption error → *the cap\n  input of the channel the response names*; unknown codes stay raw; serialization preserves all four\n  unrendered fields. *Mutations:* answer for channel B while asserting A; drop a serialized field;\n  parse `requires_code` with checkbox semantics.\n- **`test/browser/slot-allocations.mjs`** — provisions its own admin, seeds an unregistered code and\n  real consumption, submits the real form, asserts one save = one PUT, checks both field errors, and\n  queries the DB to prove every unrendered field survived.\n\n#### 5. Field preservation\n\nThe availability read must be treated as a **complete allocation snapshot**, not a display read.\nUnrendered fields ride as explicit hidden values — `requires_code` as the literal `true`/`false`,\n**never checkbox semantics** (a hidden input always submits, so `value=\"\"` reads as true — the\nTKT-236 defect). `sold_by` is called out as the dangerous one: omitting it unbinds the allocation and\nis an authorization regression, pinned today by the existing\n`TestAReplaceThatOmitsSoldByUnbinds`.\n\n#### 6. The two field errors\n\nClient-side re-derivation is rejected as non-authoritative because **consumption can change between\nthe GET and the PUT** — a sharper argument than TKT-236's channel-form precedent needed, and correct.\nInstead: `ErrAllocationCapacityExceeded` → `409 {code: \"allocation_caps_exceed_capacity\"}` beside a\nderived total field; `ErrAllocationCapBelowConsumption{Channel}` → `409 {code:\n\"allocation_cap_below_consumption\", channel: \"<raw code>\"}` mapped to that row. Both codes and the\noptional `channel` join the OpenAPI `Error` schema; unrelated capacity/conflict errors keep their\nexisting wire shape.\n\n#### 7. Risks named by the drafter\n\nRoute-scoped guard silently accepting everywhere · the page unbinding resellers or clearing\npresale/window config · the UI guessing the failing channel from stale client state · stale generated\nartifacts or backoffice `dist` in the browser image · credential equality checked only in the back\noffice, leaving inventory misconfigured.\n\n#### 8. Non-goals\n\nNo whole-surface acceptance; no reuse of the three existing credentials; no seated-pool editor\n(TKT-176); no pricing/fees/partner-API changes; no cross-tenant staff identity redesign (TKT-245);\nno tamper-evidence claim (ADR-021).\n\n---\n\n**Full draft:** `plan-draft-raw.md` (212 lines) in the run scratchpad. Critique follows as\n`kind=plan-final`.\n"
    },
    {
      "stage": "planning",
      "kind": "plan-final",
      "body": "<!-- sdlc:stage=planning kind=plan-final -->\n### ✅ Plan-final — TKT-244 (delta on the `kind=plan` draft)\n\n**Critique by `main-agent` (Claude Opus 5)** per `config.models.planReview`; the draft was written by\n`gpt-5.6-luna`. Cross-model invariant satisfied — different model drafted and critiqued, and the\ncritic was not shown the drafter's justification beyond the draft itself.\n\n**The `kind=plan` draft as amended below is the plan.**\n\n---\n\n#### A. Verification of the draft's seams and wire facts\n\nEvery file, symbol and test the draft named was checked at HEAD (`c28697d1`). **The draft is unusually\nwell grounded — no hallucinated seam.** Specifically confirmed to exist:\n\n| Draft claim | Verdict |\n|---|---|\n| `internalOnly` at `operational.go:19`, wrapping 15 routes | ✅ exact |\n| `replaceAllocations` at `operational.go:150`; store at `channel_allocations.go:148` | ✅ exact |\n| `ErrUnavailable` / `ErrConflict` at `store/store.go:22-23`, both collapsing to a bare 409 in `problem()` (`server.go:163-166`) | ✅ exact |\n| `runtimecfg.RequiredCredential` (`shared/go/runtimecfg/runtimecfg.go:123`) | ✅ exists, already used by catalog + access |\n| `TestAReplaceThatOmitsSoldByUnbinds` (`channel_allocation_seller_smoke_test.go:454`) | ✅ **exists** — the drafter cited a real test it had found, not one it invented |\n| `check-generate` must be edited alongside `generate:api` | ✅ exact — the Makefile comment says so explicitly (TKT-220) |\n\n**Wire-level facts verified beyond symbol existence** (the `quality-practices.md` §1 rule that seam\nverification does not prove payload shapes):\n\n- `Error` (`openapi.yaml:400`) is `additionalProperties: false` with a **closed `code` enum** of\n  eight values. So the two new codes and the `channel` field are a **mandatory** contract edit, not\n  an optional nicety — and with response validation on, an un-declared field would fail at runtime,\n  not silently pass. The draft was right for a reason it did not state.\n- `ChannelAvailability` (`openapi.yaml:757`, Go at `channel_allocations.go:128`) carries\n  `channel, cap, release_at, released, opens_at, closes_at, window_open, held, confirmed, available`\n  — and **neither `requires_code` nor `sold_by`**. Confirmed by reading both the schema and the\n  `SELECT` in `channelAvailabilities` (`channel_allocations.go:204`).\n\n#### B. Amendments — accepted with changes\n\n**A1 — [accepted, sharpened] The field-preservation problem is worse than the draft states, and it\nis the ticket's real hazard.**\nThe draft treats extending the availability read as one bullet. It is the load-bearing item: the\neditor **cannot read `sold_by` or `requires_code` at all today**, while `ReplaceChannelAllocations`\nDELETEs every row and re-INSERTs from the submitted set (`channel_allocations.go:187-193`). So the\nnaïve editor does not merely fail to preserve them — it **cannot** preserve them, and the first save\nagainst any reseller-bound allocation silently unbinds it. Post-TKT-246 that is an **authorization\nregression**, not data loss: `sold_by` is judged under the pool row lock and unbinding turns a\nreseller's stock public. Elevated from a bullet to a blocking DoD item; the round-trip test is the\none test that must not be cut.\n\n**A2 — [accepted, strengthened] Narrowness needs a router-walking enumeration test, not just a\n\"wrong path is refused\" test.**\nThe draft proposes an exact method/path test. Insufficient on its own, and ADR-053 §Decision records\nexactly why: there, **widening the allowance to a prefix killed no test**, because the hand-mounted\nhandlers carried their own checks — the guard could stop being narrow while every route-level test\nstayed green. Commerce already solved this: `TestTheEnumerationCoversEveryInternalRouteCommerceServes`\n(`staff_credential_test.go:120`) walks the **real chi router** and fails on *both* an uncovered route\nand a stale entry, plus a floor assertion that catches a broken walk. **Required, adapted to\ninventory's 15 routes**: the staff credential must be probed against every internal route and refused\non all but the two.\nTwo traps carried over from that file, both mandatory: each probe fixture must be **otherwise valid**\n(real UUIDs, required body, `Idempotency-Key`) or the request validator answers 400 *before* the\ncredential check runs — a fixture the validator rejects passes just as happily with the credential\nwired to every route (TKT-191 needed three attempts to get this right); and the refusal assertion\nmust name the status inventory actually returns (**401**, per ADR-043's catalog/commerce split —\ninventory's `internalOnly` writes 401, *not* commerce's 404; do not copy commerce's expectation).\n\n**A3 — [accepted with a correction] The page path and its navigability.**\n`web/backoffice/src/pages/slots/[id].astro` does not exist and neither does `src/pages/slots/`.\nMore importantly the draft never says **how an operator reaches it** — there is no slot list in the\nback office, and `ROUTE_MATRIX` has no slots row. Verified: the domain term in the back office is\n**performance**, not slot (`catalog.ts:307-353`), and inventory's pool `slot_id` **is** the catalog\nperformance id (inventory looks the pool up by that id via\n`GET /internal/performances/{id}`, `consumer/catalog.go:103`). So the mapping is sound and the page is\nimplementable, but **an unreachable page satisfies no COS**. Decision recorded in §C-3.\n\n**A4 — [accepted, with the tier named] `requires_code` must be asserted at the parser tier.**\nThe draft says \"never `checkboxChecked`\". Correct, and it is the TKT-236 defect verbatim (AGENTS.md:\na hidden input always submits, so `value=\"\"` is present-and-empty and reads as **true** — which is\nhow renaming a disabled channel silently re-enabled it). Adding the tier requirement: the assertion\nbelongs on the **pure parser** in `allocation-form.ts`, not on a rendered page, because that is the\nlayer that decides. A page-level test would prove the fixture and the renderer agree and nothing else\n(AGENTS.md, \"a test must live at the tier its mechanism does\").\n\n**A5 — [accepted, scope-trimmed] Do not extend the availability read beyond the two fields needed.**\nThe draft's §3 proposes extending `ChannelAvailability` with `requires_code` and `sold_by`. Accepted —\nbut note `sold_by` is a **reseller identity** now newly visible on a staff read. It is already\noperator configuration the same page will edit, and it is not a secret in the ADR-021 sense, so this\nis acceptable; it must be **stated** in ADR-057 rather than slipped in, because it widens what a\ncompromised back office can enumerate (the exact class of cost ADR-053 got wrong twice).\n\n#### C. Decisions I made myself — Gate 2 is waived, so these are mine\n\nPer the run's standing waiver: prefer the critique's recommendation; where it makes none, take the\n**most reversible** option. Each is recorded with its reasoning.\n\n**C-1 — Credential scope: NEW credential, two operations exactly. [adopting the critique's\nrecommendation]**\n`INVENTORY_STAFF_WRITE_TOKEN`, accepted on `GET /internal/slots/{id}/availability` and\n`PUT /internal/slots/{id}/channel-allocations`, additional to `X-Internal-Token`.\nThis is the decision the ticket was **held at Gate 1 for**, so it gets the fullest statement:\n- ADR-053's precedent genuinely does not transfer. Its \"no new credential\" rested on the catalog\n  token *already* holding create/update power over the same resource, making the read pure\n  amplification. The back office holds **nothing** for inventory, so any reuse grants a new\n  capability class across a service boundary and makes a catalog-token compromise an inventory-write\n  compromise. I checked this claim rather than accepting it: `compose.yaml:410-444` gives the back\n  office no inventory credential of any kind, and inventory's only credential is the shared\n  `INTERNAL_SERVICE_TOKEN` (`cmd/inventory/main.go:104`).\n- Two operations rather than one because the page cannot function with write alone — it must show\n  **current consumption** (a COS), which only the staff availability read provides, and that read is\n  itself behind `internalOnly`. The draft caught this; the ticket text did not.\n- **Reversibility, which is the tie-breaker the waiver names:** widening a route-scoped allowance\n  later is additive and cheap; narrowing a whole-surface one is a security migration. When in doubt\n  the reversible direction wins.\n\n**C-2 — Refusal shape: coded 409s, server-authoritative. [adopting the critique's recommendation\nover the repo's own precedent]**\nWorth flagging because it **diverges from TKT-236's channel-form precedent**, which re-derived field\nattribution client-side from submitted values and explicitly called that presentation-only. The\ndrafter's argument for diverging is correct and is the deciding one: **consumption can change between\nthe GET and the PUT**, so a client-side re-derivation of \"which channel is below consumption\" can name\nthe wrong row — it is not merely non-authoritative, it can be actively wrong in a way TKT-236's\nbounds-check never could (those bounds are static; consumption is live). Server carries the raw\nchannel code; the client never parses the human message and never infers the row from ordering.\n\n**C-3 — Reachability: query-parameter entry, no new list page. [most reversible; the critique made\nno recommendation]**\nThe editor is reached as `/admin/slots/[id]` with the performance id, linked from the existing event\nbuilder, which already holds a `performance` id in its URL (`pages/events/new.astro:35,110`). I am\n**not** building a slot browser: that is unbounded scope for a ticket whose subject is a credential,\nand a link from the page that already knows the id is the smallest thing that makes the editor\nreachable. If it proves awkward, adding a list later is additive. `ROUTE_MATRIX` gains one\n**admin-only** row (`authorization.test.ts:63` and `gate.test.ts:313` fail otherwise — an\nunclassified route 403s for every role including admin).\n\n**C-4 — ADR number: ADR-057. [forced]** ADR-055 is already duplicated across two files\n(`on-sale-write-rate-limiting`, `presale-unlock-codes`); ADR-056 is taken by TKT-246. The existing\ncollision is **not** this ticket's to fix — noted for the backlog instead.\n\n**C-5 — Startup separation is checked in BOTH processes. [most reversible / defence in depth]**\nInventory refuses to start when `INVENTORY_STAFF_WRITE_TOKEN` is absent or equals\n`INTERNAL_SERVICE_TOKEN` (catalog's exact precedent, `cmd/catalog/main.go:127`, including the\nraw-string comparison being sound only because `RequiredCredential` has already refused values HTTP\nwould normalize). `credentials.mjs` extends to **three-way pairwise** separation. Neither value is\never echoed. The back office is the only process holding all three, so it is the only one that can\ncompare them — but inventory can still catch its own misconfiguration, and a credential equal to the\nshared token would silently collapse the whole point of the ticket.\n\n#### D. Pre-mortem (lens applied: **pre-mortem**, per `quality-practices.md` §1)\n\n*Assume this bounced. What went wrong?*\n\n1. **The editor shipped and silently unbound a reseller allocation** — the highest-consequence\n   failure, and invisible in a screenshot. → A1 makes round-trip preservation a blocking DoD item\n   with a non-null `sold_by` in the fixture; the browser spec queries the DB after saving.\n2. **The credential quietly opened the whole internal surface** — precisely ADR-053's recorded\n   mutation blindness. → A2's router-walking enumeration, which fails on stale entries too.\n3. **The gate failed on generated-artifact drift.** A new `inventory-api-types.gen.ts` added to\n   `generate:api` but not to `check-generate`'s diff list is regenerated and never compared (the\n   Makefile comment names this exact failure, TKT-220). → both edited together, and generated output\n   committed **before** the gate runs.\n4. **`make check` was green and the form still 403'd in a browser.** The CSRF/`checkOrigin`,\n   base-path and redirect class is invisible to `make check` (AGENTS.md; TKT-226 caught a\n   `no-referrer` that 403'd every reset). → the browser spec is not optional, and `make browser` runs\n   before the PR.\n5. **A test named the right case and could not fail.** → every new test gets an explicit mutation\n   check, recorded in the coding comment — including tests written while fixing review findings,\n   which is where this discipline actually lapses.\n\n*Changes made by the lens:* elevated field-preservation to blocking (1), added the router-walk\nrequirement (2), and made the `check-generate` edit an explicit DoD line (3).\n\n#### E. Introduced at plan-review, reviewed by no second model\n\nThe cross-model guarantee covers the draft and the code, not the critic's own inventions. These\nreached this point reviewed **only by me** and are named again for the record:\n\n1. **The router-walking enumeration test requirement (A2)**, including the 401-not-404 correction and\n   the otherwise-valid-fixture trap. Adapted from commerce's existing test; the adaptation is mine.\n2. **The reachability decision (C-3)** — query-param entry from the event builder, no slot browser.\n   Entirely mine; the draft was silent on how the page is reached.\n3. **The `sold_by`-on-a-staff-read disclosure note (A5)** and its ADR requirement.\n4. **The parser-tier placement for the `requires_code` assertion (A4).**\n\nThe ai-review stage will see the code these produced, so (1) and (4) get adversarial coverage there.\n(2) and (3) are design choices no second model will judge before merge — **the most likely places\nthis plan is wrong.**\n\n#### F. Rejected from the draft\n\n- **`services/inventory/internal/api/server.go:71-119,133-170` and similar line ranges** — cited with\n  false precision (`server.go` mounts routes at 96-118; `problem()` is 133-176). Harmless, but the\n  implementer should navigate by symbol, not by the draft's line numbers.\n- **\"expose a navigation link where appropriate\"** (draft §3, back office) — too vague to implement\n  against; replaced by the concrete C-3.\n\n---\n\n**Gate 2 is waived.** Proceeding to `Building` / `agent:coding`. Implementation by `main-agent` per\n`config.models.implement`; TDD with every test observed **red first**, `make check` green before the\npush, `make browser` green with the new spec.\n"
    }
  ]
}
