{
  "key": "TKT-147",
  "type": "Task",
  "summary": "Replace the integrity alarm's free-text reason with a fixed reason-code vocabulary (TKT-119 incidental)",
  "status": "Backlog",
  "labels": [],
  "assignee": null,
  "pr": null,
  "links": [
    {
      "type": "relates",
      "key": "TKT-119"
    }
  ],
  "description": "Raised by TKT-119's ai-review and confirmed independently during its plan-review. **Pre-existing** — TKT-119 corrected the ADR clause to describe reality; it did not change the payload.\n\n`alarmData.Reason` (`services/access/internal/store/lifecycle.go:378-386`, field at `:382`) is an\n**unconstrained string**, persisted in the alarm envelope and published to the operator durable. The\ngolden fixture carries `\"reason\":\"chain head mismatch\"`\n(`services/access/internal/store/alarm_envelope_test.go:59`).\n\nIt is the **one unbounded field** in any alarm payload. ADR-025 §D9 (as amended by TKT-119) admits it explicitly as \"a service-produced diagnostic reason string\" and states plainly that nothing enforces its content — the constraint is a *discipline on the producer*, not an enforced property.\n\nThat is honest but weak. A future error path could interpolate scanner-, device- or buyer-derived text into an error and it would flow into an operator-visible alarm payload with nothing to stop it. Every other field in every alarm class is an identifier, an enum, or a scalar.\n\n### The producers — full list, corrected 2026-08-26\n\nSix `oweAlarm` call sites, and they fall into **two kinds**, which is what decides the scope:\n\n*Reason from a Go error (`err.Error()`):*\n- `lifecycle.go:463` and `:475` — both from `reason := cause.Error()` at `lifecycle.go:427`\n  (`degradedScan`), dispositions `DecisionIntegrityQuarantined` / `DecisionIntegrityOperatorControlled`\n- `lifecycle.go:538` — same `reason`, disposition `DecisionAdmittedDegraded`\n- `scan.go:139` — `chainErr.Error()`, disposition **`DecisionExitUnverified`**\n\n*Reason read back OUT OF THE DATABASE (`SELECT reason FROM lifecycle_integrity_quarantine`):*\n- `postgres.go:365-380` — `quarantineReason`, disposition `DecisionIntegrityQuarantined`\n- `scan.go:167-176` — `quarantineReason`, same disposition\n\n**The second kind is the one the original ticket missed, and it changes the work.** A fixed\nvocabulary cannot be applied at the producer alone: existing `lifecycle_integrity_quarantine` rows\nhold free text that is read back and re-emitted, so the change needs a **data migration for existing\nquarantine rows** (or a documented mapping for legacy values), not just a payload change.\n\n### Wanted\n\nA fixed `AlarmReason` vocabulary (like `PolicyConflictRule` in `conflicts.go:32-36`), with the\ndetailed error kept in logs rather than the payload. Then §D9's prohibition becomes enforceable\nrather than aspirational, and the reason field can be pinned by a test the way `rule` already is.\n\n### Scope question to answer before planning\n\n`services/commerce/internal/store/migrations/0019_order_attribution_detachments.sql:42-44` cites\nTKT-147 as *\"already the open ticket for replacing a free-text reason with codes\"* — but that is a\n**commerce** free-text `reason` column, not access's alarm payload. Decide explicitly whether this\nticket owns both surfaces or access only, and say so in the ticket; otherwise the next reader\ninherits a scope nobody chose.\n\n**Why it was not done in TKT-119:** it is a payload change. It fires ADR-017 §3 (a changed field on an existing contract) and ADR-033 (byte-identical wire output), needs a schema decision and a golden migration, and TKT-119 was explicitly a no-behaviour-change ticket. Routing a wire-format decision through a ticket whose COS says \"no behaviour change\" would have been the wrong gate.\n\n**Sites:** `services/access/internal/store/lifecycle.go` (`alarmData.Reason:382`, `oweAlarm:391`, `degradedScan`'s `cause.Error():427`), `services/access/internal/store/scan.go:139` and `:167-176`, `services/access/internal/store/postgres.go:365-380`, goldens in `services/access/internal/store/alarm_envelope_test.go:59`.\n**Governing:** ADR-025 §D9 (amended TKT-119), ADR-017 §3, ADR-033, ADR-003 §D3.",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Gap intact — but three cited coordinates are wrong and the site list is INCOMPLETE\n\nBacklog review 2026-08-26 at HEAD `1aaee7cd`. No open product decision here; it needs **reshaping**, then it is Ready-able.\n\n**Verified intact.** `alarmData.Reason` is still an unconstrained string (`services/access/internal/store/lifecycle.go:378-386`, field at `:382`); `degradedScan` still does `reason := cause.Error()` (`:426-427`); the golden still carries `\"reason\":\"chain head mismatch\"` (`alarm_envelope_test.go:59`); the model to copy is still `PolicyConflictRule` (`conflicts.go:32-36`); ADR-025 §D9 still admits it as a producer discipline rather than an enforced property (`:196-197`, `:207-209`, `:228-230`).\n\n**Corrections the drafter needs:**\n\n1. **`scan.go:114` is wrong** — that line is now `ticketExchanged` (TKT-166/ADR-039). The real `chainErr.Error()` producer is **`scan.go:139`**, and it carries a disposition the ticket does not mention: `DecisionExitUnverified`.\n2. **`lifecycle.go:403` is wrong** — `cause.Error()` is now at `:427`; `oweAlarm` is at `:391`.\n3. **Two producers are missing from the site list**, and they change the shape of the work. Both feed a reason read back **out of the database** rather than from a Go error: `postgres.go:365-380` and `scan.go:167-176`, both `quarantineReason` doing `SELECT reason FROM lifecycle_integrity_quarantine`. The full producer set is five `oweAlarm` call sites: `lifecycle.go:463`, `:475`, `:538`, `postgres.go:380`, `scan.go:139`, `scan.go:176`.\n\n**The consequence of (3) is a scope item the ticket does not carry:** a fixed vocabulary must cover the persisted-quarantine-reason path, which implies a **data migration for existing quarantine rows**, not just a payload change.\n\n**One scope question to answer explicitly before planning.** `services/commerce/internal/store/migrations/0019_order_attribution_detachments.sql:42-44` cites TKT-147 as *\"already the open ticket for replacing a free-text reason with codes\"* — but that is a **commerce** free-text `reason` column, not access's alarm. Decide whether this ticket owns both or only access, and say so; otherwise a future reader inherits a scope nobody chose.\n"
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Description corrected — two wrong line references, and two missing producers that change the scope\n\nRe-verified at HEAD `1aaee7cd` before editing; every site below was read.\n\n**Corrected references:**\n- `scan.go:114` → that line is now `ticketExchanged` (TKT-166/ADR-039). The real `chainErr.Error()`\n  producer is **`scan.go:139`**, carrying disposition `DecisionExitUnverified` — a detail the ticket\n  did not mention and a vocabulary must cover.\n- `lifecycle.go:403` → `cause.Error()` is at **`:427`**; `oweAlarm` is at **`:391`**; the\n  `alarmData` struct is at `:378-386`.\n\n**Added — the finding that changes the work.** The producer list was incomplete. Two `oweAlarm` call\nsites take their reason **out of the database**, not from a Go error:\n`postgres.go:365-380` and `scan.go:167-176`, both `SELECT reason FROM\nlifecycle_integrity_quarantine`. Full set is six sites, in two kinds.\n\n**Consequence, now stated in the ticket:** a fixed vocabulary cannot be applied at the producer alone.\nExisting quarantine rows hold free text that is read back and re-emitted, so this needs a **data\nmigration or a legacy-value mapping** — a scope item that was invisible in the original framing.\n\n**Added a scope question:** `commerce/.../migrations/0019_order_attribution_detachments.sql:42-44`\ncites TKT-147 for a *commerce* free-text reason column. Decide whether that is in scope; do not let\nthe next reader inherit it by accident.\n\n**No open product decision** — the wanted outcome is stated and `PolicyConflictRule` is the model.\nThis is Ready-able once shaped against the corrected site list."
    }
  ],
  "parent": "TKT-19"
}