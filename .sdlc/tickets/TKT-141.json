{
  "key": "TKT-141",
  "type": "Story",
  "status": "Backlog",
  "labels": [],
  "assignee": null,
  "pr": null,
  "summary": "Seat-map list reads: hours tier hides membership changes (TKT-107 follow-up)",
  "description": "### An all-published seat-map list still carries hour-long membership staleness\n\nRaised in TKT-107's plan-review as an internal inconsistency in the draft, resolved there by\n**keeping** the hours tier and documenting the gap (TKT-107 §D3, most-reversible rule). This ticket\nowns closing it.\n\nTKT-107 made the three public seat-map reads emit `no-store` unless the payload is non-empty and\nevery seat map in it is `published` (`cacheControlForSeatMaps`,\n`services/catalog/internal/api/server.go`). That fixes staleness of draft **content**. It does not\nfix staleness of list **membership**:\n\n- `GET /public/venues/{venueId}/seat-maps` — an all-published response is hours-tier. Author a new\n  seat map a minute later and it stays invisible for up to an hour, which is the same \"an authoring\n  write looks lost\" failure TKT-107 was filed for.\n- `GET /public/seat-maps/{id}/versions` — same shape: an all-published family is hours-tier, and an\n  ADR-029 edit inserts a new published version that a cached history cannot show.\n\nTKT-107 deliberately did not close this: demoting the list tier would retire ADR-004's hours tier for\nthe only reads that use it, on a judgement no second model reviewed, and it is not observable today\n(no CDN or shared cache exists anywhere in the stack — see ADR-004 § Amendment TKT-128).\n\n**Decide between** (a) demote both *list* reads to a shorter tier (minutes, or `no-store`) and leave\nonly by-id published geometry hours-tier, since a single published version genuinely is immutable\n(ADR-029); or (b) keep the tier and make it TKT-31's problem — a cache deployment must invalidate the\nvenue and family URLs on every seat-map authoring write. (b) is only credible with an invalidation\nmechanism actually specified.\n\n**Governing:** ADR-004 (rule 1 tier table + the TKT-107 amendment, which states this gap explicitly\nand points here), ADR-029 (published-version immutability), ADR-009 (a tier change is a contract\nchange — the `SeatMapCacheControl` enum would need a third value). **Revisit with:** TKT-31\n(read-path caching & hot-event serving), which owns the shared-cache tier and invalidation.\n\nThis is a decision ticket (which tier, and who owns invalidation), so it goes through the pipeline\nrather than a direct commit.\n",
  "links": [],
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Verified STILL VALID — held at Gate 1 on a human decision\n\nBacklog review 2026-08-26 at HEAD `1aaee7cd`. Every claim checked against the code; nothing has drifted.\n\n- `cacheControlForSeatMaps` unchanged at `services/catalog/internal/api/server.go:79-88` — non-empty + all-`published` → hours tier, else `no-store`. Called from `server_seat_maps.go:151` (`listSeatMapVersions`), `:290` (`getPublicSeatMapGeometry`), `:304` (`listVenueSeatMaps`). An all-published list still gets `max-age=3600`.\n- `SeatMapCacheControl` is a two-value enum (`openapi.yaml:1720-1735`), so the ticket's claim that a third tier needs a new enum value — an ADR-009 contract change — holds exactly.\n- ADR-004 (`docs/adr/ADR-004-cache-first-read-path.md:214-219`) still states the gap verbatim and names TKT-141 as its owner.\n- Still no CDN or shared cache anywhere in the stack, so still unobservable in practice.\n\n**Held on the owner's call**, and the ticket is right to frame it as a decision: (a) demote both list reads to a shorter tier — a fail-closed contract change — or (b) keep the tier and make invalidation TKT-31's problem, which is only credible with an invalidation mechanism that does not exist. Carried into the decision brief.\n\n**Note the interaction with TKT-209**, now in Ready: TKT-209 pins catalog's *existing* tiers into a closed enum. It does not re-decide any tier, so the two do not conflict — but if (a) is ever chosen, it lands as an amendment on top of TKT-209's enum rather than on today's free-form string.\n"
    },
    {
      "stage": "backlog",
      "kind": "decision",
      "body": "<!-- sdlc:stage=backlog kind=decision -->\n### ✅ OWNER DECISION taken 2026-08-26 — demote the two LIST reads; by-id geometry stays hours-tier\n\n**Decision: option (a).** `GET /public/venues/{venueId}/seat-maps` and\n`GET /public/seat-maps/{id}/versions` move off the hours tier. `GET /public/seat-maps/{id}/geometry`\nkeeps it.\n\n**Rationale, as given:** a single published version genuinely is immutable (ADR-029), so caching it\nfor an hour is an honest claim. List **membership** is not immutable — a new seat map or an\nADR-029 edit changes it — so an hours tier on those two reads claims something untrue. Option (b)\nwould have deferred a real bug behind an invalidation mechanism that does not exist.\n\n**SEQUENCING — this matters and is cheap to get wrong.** TKT-209 is in **Ready** and replaces\ncatalog's free-form `CacheControl` with single-valued components. **Do TKT-209 first.** If this lands\nfirst, TKT-209 has to absorb a tier change mid-flight; if TKT-209 lands first, this is a clean\namendment to a closed enum. Wire this as `blocked-by: TKT-209` at shaping.\n\n**Open sub-question for shaping, not for the owner:** minutes or `no-store` for the two list reads.\nMinutes matches what the other public list reads already use and is the smaller claim to defend;\n`no-store` is stricter than the problem requires. Recommend **minutes**, and say why in the ADR.\n\n**Contract consequence (ADR-009):** `SeatMapCacheControl` is a two-value enum\n(`services/catalog/api/openapi.yaml:1720-1735`). A third value is a contract change — regen\n`openapi_gen.go` and `api-types.gen.ts` and **commit before the gate**, since `check-generate` diffs\nagainst HEAD.\n\n**Record in ADR-004**, amending the TKT-107 clause at `:214-219` which currently states this gap as\nopen and points here. The amendment should say which reads changed tier and why the by-id read did\nnot.\n\n**Still unobservable today** — no CDN or shared cache exists anywhere in the stack — so this is\ncorrectness of the claim, not a live defect. Size it accordingly."
    }
  ],
  "parent": "TKT-31"
}