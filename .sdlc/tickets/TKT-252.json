{
  "key": "TKT-252",
  "type": "Task",
  "status": "Planning",
  "labels": [
    "agent:plan-review"
  ],
  "assignee": "agent",
  "pr": null,
  "summary": "No entropy floor on any signing key or credential (runtimecfg)",
  "description": "**Surfaced by TKT-245's ai-review [high], confirmed, and NOT specific to that ticket.**\n\n`runtimecfg.RequiredCredential` (`shared/go/runtimecfg/runtimecfg.go:123-158`) validates **transport\ncharacters** — emptiness, retired literals, edge whitespace, header-safety — and never **entropy or\nlength**. A deployment configured with `x` starts cleanly.\n\nThis guards **every credential in the system**, so the exposure is system-wide rather than catalog's:\n\n| Value | Service | Consequence of a weak value |\n|---|---|---|\n| `COMMERCE_CUSTOMER_ASSERTION_KEY` | commerce | forge a customer assertion → attribute orders to any buyer |\n| `CATALOG_ORGANIZER_ASSERTION_KEY` | catalog | forge an organizer assertion → write into any tenant (TKT-245/ADR-058) |\n| `ACCESS_TICKET_LINK_KEY`, `ACCESS_QR_PRIVATE_KEY` | access | forge ticket links / QR payloads |\n| `INTERNAL_SERVICE_TOKEN`, the three staff-write tokens, `PAYMENTS_INTERNAL_TOKEN` | all | bearer credentials, brute-forceable online |\n\nThe **signing keys** are the sharper half: a captured token gives an offline oracle, so a weak key\nfalls without touching the service at all.\n\n**Conditions of Success:**\n- A `runtimecfg.RequiredSigningKey` (or an entropy floor on `RequiredCredential`) that requires a\n  minimum of 32 random bytes, in a declared encoding, and refuses anything shorter.\n- Adopted by **both** assertion keys at once, and by the ticket-link/QR keys. Adopting it in one\n  service is the failure mode this ticket exists to avoid: it would leave the identical hole one\n  service over while looking closed.\n- Startup tests proving short and malformed keys are refused, and that the error **never echoes the\n  value** (the existing credential tests already assert this and are the pattern to copy).\n- A decision recorded on whether existing deployments need a rotation, since a value that starts\n  today may not start after this lands — that is a deploy-ordering question, not just a code one.\n\nQuality gates: `make check`. Touches `shared/go`, so every service rebuilds.\n\nNon-goals: rotation mechanics or key overlap (no service has them today; ADR-058 § consequences\nrecords that for catalog).\n",
  "comments": [
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Filed from TKT-245's ai-review — confirmed, and deliberately NOT fixed there\n\nai-review raised this as **[high] \"Catalog accepts trivially brute-forceable HMAC signing keys\"**.\nCorrect, and verified: `RequiredCredential` has no length or entropy check.\n\n**Why it was overridden rather than fixed in TKT-245.** The same function guards every credential in\nthe system, including `COMMERCE_CUSTOMER_ASSERTION_KEY` — the **direct precedent** for TKT-245's\nmechanism, with an identical threat model, shipped in TKT-221 with the same absence. So TKT-245\n*inherits* a system-wide property rather than introducing one.\n\nA catalog-only entropy floor would have been worse than none: it would leave the identical hole one\nservice over while **looking** like the class had been addressed — the \"configured-looking and wrong\"\nfailure ADR-042's own credential guard exists to prevent. The fix belongs in the shared package, used\nby both assertion keys at once.\n\n**What already holds, and what does not:** `make up` and `scripts/env-bootstrap.sh` generate 32 bytes\nfrom `/dev/urandom`, compose ships no default and refuses to start without the variable, and no\nsigning key is ever sent to a web process. None of that is *enforcement* — it is a convention a\nproduction deployment can decline to follow, which is exactly the reviewer's point."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Shaping pass — 7 of 8 DoR items met, `approach` held OPEN for a human decision\n\nTicket claim **verified against the code**: `RequiredCredential` (`shared/go/runtimecfg/runtimecfg.go:123-159`)\nchecks emptiness, the retired literal, edge whitespace and `httpguts.ValidHeaderFieldValue`, and\nnothing else. `x` starts cleanly. 14 credential loads across 6 binaries inherit that.\n\n**The ticket's proposed remedy does not survive the read**, and the corrections are worth more to\nthe planner than the original wording:\n\n1. **The floor cannot be uniform.** Three encodings coexist. The two Ed25519 seeds\n   (`ACCESS_QR_PRIVATE_KEY`, `ACCESS_LIFECYCLE_PRIVATE_KEY`) already decode\n   `base64.RawStdEncoding` and refuse anything but exactly `ed25519.SeedSize` — `token.go:37-39`,\n   `keys.go:61-67`. They need **no floor**, only a test pinning that they are covered. So the COS\n   clause *\"adopted by the ticket-link/QR keys\"* is half already-true and half genuinely open:\n   `ACCESS_TICKET_LINK_KEY` is a **raw-string HMAC key with no floor at all** (`server.go:65`).\n2. **`JOURNAL_SIGNING_KEY`'s 16-byte floor is not an oversight.** `ADR-032` line 115 enumerates\n   *\"a secret under 16 bytes\"* as a blessed rejected configuration, and `docs/development.md:448`\n   rests the key-id log-safety argument on the explicit sentence *\"nothing here requires\n   `JOURNAL_SIGNING_KEY` to be high-entropy.\"* Raising 16→32 is an **ADR amendment plus a docs\n   correction**, not a code change — and `main_test.go:145` asserts on the literal substring `\"16\"`.\n3. **A length floor is not an entropy floor.** Thirty-two `a`s passes any `len()` check. The honest\n   claim is a *length* floor that makes online brute force infeasible for values the generators\n   already produce; the word \"entropy\" should not reach the error message or the ADR.\n4. **`ADR-056` lines 61-72 is the in-repo precedent for the number** — *\"a generated 32-byte\n   token… 256 bits\"*. Cite it rather than inventing 32.\n\n**Ordering hazard, pinned as a governing test.** `runtimecfg.go:130-136` carries an explicit\n`ORDER MATTERS` comment. A length case placed anywhere but **last** rewrites the message for the 9\nsub-cases of `TestRequiredCredentialRejectsValuesHTTPWouldChange` and breaks two **exact-string**\nassertions (`runtimecfg_test.go:109`, `:120`). Note the retired literal `local-service-token` is 19\nchars — it must still reach the *retired-default* case, not the new length case.\n\n**Guaranteed fixture breakage, and it is the bulk of the work.**\n`TestAcceptedCredentialSurvivesAnHTTPRoundTripUnchanged` (`:199`) is a **positive** test with\nfixtures of 9, 6 and 10 chars — lengthen them, do not delete the case, it exists to prove interior\nspaces survive an HTTP round trip. And `scripts/smoke.sh:196-197,209` carries 30/31-byte journal\nliterals **co-owned** with `journal_smoke_test.go:45-49` under the `TestSmokeJournalKeyLiteralsMatchScript`\ndrift guard (TKT-117) — edit both or neither. Full radius in the mémo (~10 lines of production code\nagainst 12+ test files and 2 shell scripts).\n\n**No generated value breaks.** `env-bootstrap.sh` and `stack-env.sh` draw 32 bytes from\n`/dev/urandom` (64 hex chars) and `compose.yaml` ships `${VAR:?}` with no defaults. That is the\nargument for a hard refusal over a deprecation window — but it is Q3's to settle, not shaping's.\n\n**A gap the COS asks for that does not exist yet:** there is **no** test asserting the error omits\nthe value on `RequiredCredential` *itself*. The no-echo assertions live on the `InternalTokenFromEnv`\nwrapper and on caller guards. COS clause 3 is new work, not a copy of an existing pattern.\n\n---\n\n### ⛔ Gate 1 is HELD — `approach: open`, `owner: human`\n\nPer `shaping.md`, `approach: deferred` **does not pass** when a ticket touches auth, and this one is\nthe auth substrate for every credential in the system. Three questions, one answer each:\n\n- **Q1 — What does the floor measure?** Raw byte length of the configured string (simple, testable,\n  satisfied by every generator at 64 chars) **or** decoded material per declared encoding\n  (correct-sounding, but teaches `runtimecfg` three encodings and buys nothing for the two seeds\n  that already enforce exactly 32)?\n- **Q2 — Does `JOURNAL_SIGNING_KEY` go 16→32 in this ticket** (carrying the ADR-032 amendment, the\n  `development.md:448` correction and the `main_test.go:145` expectation), **or stay at 16** with the\n  divergence recorded as deliberate?\n- **Q3 — Do existing deployments need a rotation?** Hard refusal at startup (a value that boots today\n  stops booting) or a deprecation window? The ticket's own COS asks for this to be decided.\n\n`risk:low` **not claimed** — auth surface, shared package, all six binaries rebuild, >5 files.\n\n**8 governing ADRs baked into the context-mémo, every one verified to resolve at HEAD**\n(`ADR-012/021/032/042/043/056/057/058`), plus 6 governing tests. Next ADR number: **ADR-059**."
    },
    {
      "stage": "backlog",
      "kind": "readiness",
      "body": "<!-- sdlc:stage=backlog kind=readiness -->\n### Gate 1 — the three open decisions are answered by the owner; `approach` flips to `met`\n\n**Q1 — What does the floor measure? → RAW BYTE LENGTH of the configured string.**\n`len(token)`, not decoded material per declared encoding. The decoded version sounds more correct\nand is the trap: `runtimecfg` currently knows nothing about any encoding *deliberately* — it\nvalidates transport characters, which is encoding-agnostic — so teaching it three encodings means 14\nnew per-caller declarations and a new way to be wrong. It also buys nothing: the two credentials\nwhere decoding matters already refuse anything but exactly `ed25519.SeedSize` **at their own decode\nsites** (`token.go:37-39`, `keys.go:61-67`), which is the tier their mechanism actually lives at.\nThe nine raw-string credentials have no encoding to decode — their string *is* the key material.\n\nADR-059 must say **length floor**, not entropy floor. It defeats online brute force; it certifies\nnothing about entropy, and the error message must not claim otherwise.\n\n**Q2 — `JOURNAL_SIGNING_KEY` 16→32? → NO. It stays at 16, and the divergence is recorded.**\nADR-032 line 115 is a *decision*, not an accident, and the journal key is **absent from this\nticket's own threat table** — its exposure is different in kind: it signs an append-only audit\njournal verified offline by an operator, not a credential an attacker presents. Raising it would\npull in an ADR-032 amendment, the `development.md:448` correction, the `main_test.go:145`\nexpectation and both smoke literals with their drift guard — all as a side effect of a different\nticket, which is exactly the move the plan gate exists to catch. If 16 is wrong it earns its own\nticket. **ADR-059 states plainly why the two numbers differ**: a recorded divergence is a decision,\nan unrecorded one is drift.\n\n**Q3 — Rotation for existing deployments? → NO WINDOW. Hard refusal at startup.**\nNo generator produces a short value: `env-bootstrap.sh` and `stack-env.sh` draw 32 bytes from\n`/dev/urandom` (64 hex chars), `access keygen` emits 43-char base64, `compose.yaml` ships\n`${VAR:?}` with no defaults. Any deployment on the documented path already passes, so a window\nwould protect only a hand-written short key — precisely what this ticket exists to stop, and a\nwarning that lets it keep booting is the \"configured-looking and wrong\" failure ADR-042's guard\nexists to prevent.\n\n**The cost is real and goes in the ADR rather than being assumed away:** an operator who *did*\nhand-write a short key discovers it at **restart**, not at deploy time. That is the right trade —\nthe fix is `openssl rand -hex 32`, the alternative is leaving a forgeable signing key live — but it\nis written down.\n\n---\n\n### Resulting shape (the planner's starting point, not the plan)\n\n- `minCredentialLen = 32`, appended **LAST** in the `RequiredCredential` switch — the `ORDER MATTERS`\n  comment at `runtimecfg.go:130-136` and the two exact-string assertions are why.\n- **One refusal test per credential, at its own call site.** Not one test on the shared helper: a\n  green test one tier above the mechanism proves the fake and the handler agree and nothing else\n  (AGENTS.md).\n- The **missing** direct no-echo assertion on `RequiredCredential` itself — today it exists only on\n  the `InternalTokenFromEnv` wrapper and on caller guards, so COS clause 3 is new work.\n- Lengthened fixtures across ~12 test files, plus `scripts/smoke.sh:196-197,209` **and**\n  `journal_smoke_test.go:45-49` together under their drift guard.\n- **ADR-059** — the floor, the 16-vs-32 divergence and its reason, the no-window call and its\n  operational cost.\n\n**DoR: 8/8 met. No open blockers.** `risk:low` **not claimed** — auth surface, shared package, all\nsix binaries rebuild, >5 files. Gate 1 cleared by the owner → **Ready**."
    },
    {
      "stage": "ready",
      "kind": "claim",
      "body": "<!-- sdlc:stage=ready kind=claim -->\n### Claimed — running **gateless** per `config.gates: \"autonomous\"`\n\nGates 2–4 are taken by agent judgement this run; **Gate 1 was the human's** and was cleared by the\nowner with the three `approach` decisions recorded above. Every other rule is unchanged:\n`config.models` routing as declared, cross-model review for both plan and code, TDD with tests\nobserved red first, `make check` green before every push and after the merge, marker comments at\neach stage, no AI attribution anywhere.\n\n**Selection reason.** Requested by the owner by key. It is also the correct next pick on merit:\nfiled from TKT-245's ai-review as a confirmed **[high]** that was deliberately *overridden* rather\nthan fixed there, on the explicit ground that a catalog-only entropy floor \"would leave the\nidentical hole one service over while looking closed\". That override is the kind of residual the\n`overrides:` list exists to make recoverable — this ticket is the recovery, and it stays open as a\nsystem-wide hole until it lands.\n\n**Zero open blockers** — no `blocked-by` links; `dependencies` verified `met` at shaping.\n\n### ⛔ `risk:low` — NOT claimed\n\nStated explicitly because the label's carve-outs are exactly the tickets whose risk is obvious to\nthe claimer and invisible in the thread afterwards (TKT-190). This ticket hits **four** of them:\n\n- **auth** — it is the credential substrate for all 14 credential loads in the system;\n- **shared package** — `shared/go/runtimecfg`, so all five services *and* the gateway rebuild;\n- **CI/deploy config** — `scripts/smoke.sh` literals move, under a drift guard;\n- **>5 files** — ~12 test files, 2 shell scripts, 1 ADR.\n\nThe plan gate therefore applies in full (taken by the agent this run, per autonomous mode).\n\n**Branch:** `TKT-252-credential-entropy-floor`, off `origin/main` @ `a0a8fbe8`.\n\n**Stage routing for this ticket** (`config.models`, declared not inferred):\n`plan` → `gpt-5.6-luna` · `planReview` → `main-agent` · `implement` → `main-agent` ·\n`aiReview` → `gpt-5.6-luna`. Both cross-model pairs are distinct — `plan ≠ planReview` and\n`implement ≠ aiReview` — so the load-bearing invariant holds."
    },
    {
      "stage": "planning",
      "kind": "plan",
      "body": "<!-- sdlc:stage=planning kind=plan -->\n### Plan draft — by **`gpt-5.6-luna`** via codex (`config.models.plan`), read-only, effort high\n\nJob `task-msvx6v3q-b7wrbb`, session `01a00b0b-c3d8-7913-b742-ddc868419f6d`. Conclusive verdict\nreturned against the intended scope. **It corrected the brief twice** (§Risks) — both corrections\nverified by me and both are real; see the plan-review comment.\n\n---\n\n#### 1. Design decision — a required `minBytes` parameter\n\n```go\nRequiredCredential(envVar, retiredDefault string, minBytes int)\n```\n\nplus `runtimecfg.CredentialMinBytes = 32`. Every ordinary caller passes the constant; payments\ndefines a local `journalCredentialMinBytes = 16` and passes it **only** for `JOURNAL_SIGNING_KEY`\nat `payments/main.go:201`.\n\nThe argument for it: the policy is **visible at every call site**, and an omitted floor is a\n**compile error**. Rejected alternatives, each with a stated reason — a separate exported exception\nfunction (hides the normal 32 inside the implementation), a variadic option (security policy easy\nto omit or bury), an opt-out parameter (makes accidental weakening too easy).\n\nNew arm placed **after** the `httpguts` case at `runtimecfg.go:149-156`, immediately before the\nswitch closes — preserving the empty, retired-default, whitespace and header-value messages.\nError shape `ENV_VAR must be at least 32 bytes`: raw `len(token)`, never \"entropy\", never echoes\nthe value. Ed25519 decode checks stay where they are (`token.go:37-38`, `keys.go:65-66`).\n\n#### 2. Definition of Done\n\n- Rejects transport-valid strings shorter than 32 raw bytes; **exactly 32 succeeds, including 32\n  ASCII `a`s** (the honest statement of D5).\n- A 31-byte UTF-8 value rejected, a 32-byte UTF-8 value accepted — the raw-byte-vs-rune rule.\n- The error names the env var, states bytes, never says entropy, never echoes the value.\n- Both assertion keys (`catalog/main.go:153`, `commerce/main.go:145`), `ACCESS_TICKET_LINK_KEY`\n  (`access/main.go:222`), every other ordinary caller and both wrappers take the 32 floor.\n- `JOURNAL_SIGNING_KEY` stays at **16** (`payments/main.go:201`). Ed25519 seed decoding unchanged.\n- ADR-059 records floor, divergence, adversary, restart behaviour. Smoke journal literals unchanged\n  and `make check` green.\n\n#### 3. Files to change\n\n`shared/go/runtimecfg/runtimecfg.go` — the constant, the required parameter, the new branch, and\n`InternalTokenFromEnv` (`:195-196`) + `PaymentsTokenFromEnv` (`:179-180`) passing it.\n`runtimecfg_test.go` — thread the constant through existing direct calls; **new** tests for 31-byte\nrejection + no echo, 32 `a`s accepted, raw-byte-not-rune counting, and an explicit 16-byte policy;\nlengthen the positive round-trip fixtures at `:201-204` **preserving punctuation, interior-space and\nbase64-padding coverage**; leave the rejection fixtures at `:151-172` untouched so they keep proving\nordering.\nCallers: `catalog/main.go:119,153` · `commerce/main.go:130,145,155` · `inventory/main.go:114` ·\n`access/main.go:207,222` · `access/lifecycle.go:92` · `payments/main.go:201` (local 16 constant, and\nthe existing `>=16` diagnostic at `:194` untouched).\nNew `docs/adr/ADR-059-credential-length-floor.md` per `docs/adr/[template].md`.\n\n**Intentionally unchanged, each with a reason:** `env-bootstrap.sh`/`stack-env.sh` (already 64 hex\nchars), `compose.yaml` (`${VAR:?}`, no defaults), `smoke.sh` + `journal_smoke_test.go` (30/31 bytes,\nboth above the retained 16 floor), `payments/main_test.go:145` (the `\"16\"` assertion **stays\ndeliberate**), `web/backoffice/credentials.mjs` + TS fixtures (**not a `runtimecfg` consumer**; the\nGo services are the authoritative startup boundary, a second JS floor would duplicate policy),\ninternal-API tests with short literals (they bypass startup config and must not get setter-level\nvalidation).\n\n#### 4. Test plan — each with the mutation it catches and how to make it red\n\n- **New length test** — delete the branch → a 31-byte value is accepted. Adding `%s` with the token\n  to the error makes the no-echo assertion red. Including `32×\"a\"` makes `<` → `<=` red.\n- **Raw-byte boundary** — 31 bytes as fifteen `é` + `a`, 32 bytes as sixteen `é`. Catches replacing\n  `len(token)` with rune counting.\n- **Explicit-floor test** — a 16 policy accepts 16 and rejects 15, while a 32 policy rejects that\n  same 16-byte value. Catches ignoring the parameter or hard-coding either number.\n- **Existing `:150` rejection table** — seam is *diagnostic precedence*; catches moving the length\n  case ahead of whitespace/`httpguts` (its fixtures are deliberately <32 and would get the wrong\n  message).\n- **Existing `:104`/`:114` exact-string tests** — catch placing the length case before the empty and\n  retired-default arms; `local-service-token` (19) must still reach the retired-default error.\n- **Existing `:199` round-trip** — catches altering interior-space / punctuation / base64-padding\n  behaviour while lengthening fixtures.\n- **`payments/main_test.go:97-106`** — the 30-byte active journal fixture must stay accepted;\n  catches accidentally passing the 32 policy to journal configuration.\n- **Smoke** — `smoke.sh:196-197,209` unchanged; raising the journal policy would fail packaged\n  `verify-journal` before the drift guard is even reached.\n\n#### 5. Ordering\n\nADR first → signature + branch → **every caller in the same change** (the mandatory argument is what\nmakes a silently-retained old behaviour impossible) → tests, lengthening only the positive fixtures\n→ format, then `make check` once all tracked edits are complete.\n\nSwitch order must remain: empty → retired default → edge whitespace → invalid header value →\n**minimum raw-byte length** → success.\n\n#### 6. ADR-059 outline\n\nContext (ADR-058 explicitly has no minimum) · Decision (32 for ordinary raw-string credentials, 16\nretained for journal) · **Why 32** (ADR-056:61-72 precedent; state plainly it is byte length, *not*\nan entropy guarantee — `32×\"a\"` passes) · **Why the journal differs** (ADR-032:114-123 already\nestablishes its 16-byte contract and effective-key HMAC behaviour, `development.md:394-448`; raising\nit is outside this ticket) · Rotation/deployment (no mechanics introduced; conforming values keep\nworking; a hand-written short value is refused **on restart**, not at edit or deploy time) ·\n**Adversary named** (online guessing of bearer credentials; offline forgery by a holder of captured\nHMAC material) with the explicit non-claim about entropy and human-chosen 32-byte values · Scope\nexclusions.\n\n#### 7. Risks\n\nMost likely first `make check` failure: the **positive round-trip test** — three fixtures at\n`:201-204` are under 32. Next: a missed required argument at a call site, which the signature change\nexposes **at compile time**.\n\n#### Corrections the drafter made to my brief — both verified, both real\n\n- `catalog/cmd/catalog/credentials_test.go:69` is **32 bytes at HEAD, not 31**; it needs no\n  lengthening. Confirmed: `1a2b3c4d5e6f70819293a4b5c6d7e8f9` is 32 chars.\n- The brief's \"14 credential loads\" is **16 actual load instances** — 5 `InternalTokenFromEnv`,\n  10 direct `RequiredCredential`, 1 `PaymentsTokenFromEnv`. Confirmed by `git grep` count = 16. The\n  plan covers every enumerated site rather than trusting the count, which is the right response."
    }
  ],
  "readiness": {
    "objective": {
      "state": "met",
      "note": "runtimecfg.RequiredCredential validates transport characters only (empty, retired literal, edge whitespace, httpguts) and never length or entropy. Verified at shared/go/runtimecfg/runtimecfg.go:123-159. A deployment configured with `x` starts cleanly across 14 credential loads in 6 binaries."
    },
    "scope": {
      "state": "met",
      "note": "IN: an entropy floor in shared/go/runtimecfg, adopted at once by the 9 raw-string credentials (INTERNAL_SERVICE_TOKEN, PAYMENTS_INTERNAL_TOKEN, the 3 staff-write tokens, both assertion keys, ACCESS_TICKET_LINK_KEY) + a decision recorded for the 2 Ed25519 seeds and JOURNAL_SIGNING_KEY; an ADR (ADR-059) stating the floor and the rotation position; fixture/literal migration everywhere the floor now bites. OUT: rotation mechanics, key overlap, mTLS, per-caller credentials, web/backoffice credentials.mjs beyond a stated decision."
    },
    "uncertainties": {
      "state": "met",
      "note": "Resolved by reading rather than by spike. (1) THE FLOOR CANNOT BE UNIFORM: three encodings coexist - raw string (9 vars), base64 RawStdEncoding->ed25519.SeedSize (ACCESS_QR_PRIVATE_KEY token.go:33-39, ACCESS_LIFECYCLE_PRIVATE_KEY keys.go:61-67, both ALREADY exactly 32 decoded bytes and already enforced), and raw bytes with an existing 16-byte floor (JOURNAL_SIGNING_KEY, keyring.go:16 minSecretLen). A single '32 bytes in a declared encoding' rule as the ticket text words it would either double-count the base64 seeds (43 chars pass a 32-char test trivially and prove nothing new) or contradict a shipped ADR. (2) A string-length floor is NOT an entropy floor: 32 repetitions of 'a' passes any len() check. The honest claim is a LENGTH floor that makes online brute force infeasible for values the generators already produce at 64 hex chars; say that, do not write 'entropy' into the error or the ADR. (3) Ordering hazard: the length case must go LAST in the switch - runtimecfg.go:130-136 carries an explicit ORDER MATTERS comment and 9 sub-cases of TestRequiredCredentialRejectsValuesHTTPWouldChange plus 2 exact-string assertions (runtimecfg_test.go:109, :120) expect the existing messages."
    },
    "dependencies": {
      "state": "met",
      "note": "No open blockers, no blocked-by links. Touches shared/go so all 5 services + gateway rebuild. Two co-owned literal sets must move together or a drift guard fails: scripts/smoke.sh:196-197,209 and services/payments/internal/store/journal_smoke_test.go:45-49, pinned by TestSmokeJournalKeyLiteralsMatchScript (journal_smoke_test.go:701, TKT-117)."
    },
    "approach": {
      "state": "met",
      "note": "RESOLVED by the owner at Gate 1 (three decisions, recorded in the kind=readiness comment below). A1: the floor measures RAW BYTE LENGTH of the configured string - len(token) - not decoded material per encoding; runtimecfg stays encoding-agnostic and the two Ed25519 seeds keep enforcing exactly 32 decoded bytes at their own decode sites, which is the tier their mechanism lives at. A2: JOURNAL_SIGNING_KEY STAYS AT 16; ADR-032 line 115 is a shipped decision, the journal key is absent from this ticket's own threat table, and overriding it as a side effect of another ticket is out of scope - ADR-059 records WHY the two numbers differ. A3: HARD REFUSAL at startup, no deprecation window, because no generator produces a short value (env-bootstrap.sh/stack-env.sh draw 32 bytes from /dev/urandom, compose ships ${VAR:?} with no defaults) so a window would protect only the hand-written short key this ticket exists to stop; the operational cost - an operator with a hand-written short key discovers it at restart - is recorded in ADR-059 rather than assumed away. SHAPE: a minCredentialLen=32 case appended LAST in the RequiredCredential switch; one refusal test per credential AT ITS OWN CALL SITE (not one test on the shared helper); the missing direct no-echo assertion on RequiredCredential itself; lengthened fixtures across ~12 test files + scripts/smoke.sh literals with their drift guard; ADR-059."
    },
    "first_slice": {
      "state": "met",
      "note": "One slice, and it must be one: the ticket exists precisely because a per-service adoption 'would leave the identical hole one service over while looking closed'. Slice = the floor in shared/go/runtimecfg + adoption by all 9 raw-string credentials + ACCESS_TICKET_LINK_KEY + the ADR + fixture migration. Sized by the fixture blast radius, not the logic: ~10 lines of production code against 12+ test files and 2 shell scripts."
    },
    "success": {
      "state": "met",
      "note": "COS restated with the read applied: (1) a length floor in runtimecfg refusing values below the declared minimum, placed AFTER the existing cases so every current message survives; (2) adopted by both assertion keys AND ACCESS_TICKET_LINK_KEY in the same change, with a test per credential proving the refusal - not one test for the shared helper (a green test one tier above the mechanism proves the fake and the handler agree, AGENTS.md); (3) the error never echoes the value, asserted directly on RequiredCredential, which today has NO such test - the no-echo assertions live only on the InternalTokenFromEnv wrapper and on caller guards; (4) the rotation/deploy-ordering decision recorded in ADR-059; (5) make check green, including the smoke journal literals and their drift guard."
    },
    "context_memo": {
      "state": "met",
      "note": "Baked below; 8 governing ADRs all verified to resolve at HEAD."
    }
  },
  "context": {
    "governingAdrs": [
      {
        "adr": "ADR-032-stripe-behind-the-psp-port.md",
        "constrains": "Line 115 BLESSES the 16-byte journal floor as a stated rejected-configuration list. Raising JOURNAL_SIGNING_KEY to 32 CONTRADICTS a shipped ADR and requires an amendment here, not a silent bump. Also explains (line ~118+) why HMAC key comparison is on the EFFECTIVE key: RFC 2104 zero-pads short keys and digests long ones, so byte-distinct values can be the same HMAC key - directly relevant to any reasoning about what a short key costs."
      },
      {
        "adr": "ADR-056-partner-credential-identity.md",
        "constrains": "The in-repo precedent for the NUMBER: line 61 'SHA-256 over a generated 32-byte token', line 72 'this token is 256 bits' and bcrypt's cost 'exists to compensate for low-entropy human-chosen secrets'. Cite this for the floor's value rather than inventing 32."
      },
      {
        "adr": "ADR-058-catalog-organizer-assertion.md",
        "constrains": "Defines CATALOG_ORGANIZER_ASSERTION_KEY (HMAC-SHA256, line 63) and states the consequence this ticket bounds - line 189 'anyone holding the signing key mints for any organizer, forever'. States NO minimum length; ADR-059 should say so explicitly rather than implying it always did."
      },
      {
        "adr": "ADR-042-staff-identity-and-backoffice-sessions.md",
        "constrains": "The staff-write credential separation (and the TKT-194 amendment at line 290 adding the money-moving one). Establishes 'generated independently' as the standard but states no length requirement - the gap ADR-059 closes."
      },
      {
        "adr": "ADR-021-ticket-lifecycle-trail-integrity.md",
        "constrains": "NAME THE ADVERSARY before writing any strength claim. Also D4 (line 240-243): lifecycle Ed25519 material is deliberately namespaced apart from QR. Both seeds are already length-correct; the ADR is why they must stay distinct."
      },
      {
        "adr": "ADR-012-ticket-issuance-and-qr-credentials.md",
        "constrains": "ACCESS_QR_PRIVATE_KEY is injected configuration; its seed decode already pins ed25519.SeedSize."
      },
      {
        "adr": "ADR-043-where-a-service-auth-guard-lives.md",
        "constrains": "Where an auth guard belongs - the floor goes in the shared package the 14 loads already call, not in each caller."
      },
      {
        "adr": "ADR-057-inventory-staff-write-credential.md",
        "constrains": "INVENTORY_STAFF_WRITE_TOKEN, the third staff-write credential; line 156 enumerates the four credential classes this floor must cover at once."
      }
    ],
    "governingTests": [
      {
        "test": "TestRequiredCredentialRejectsValuesHTTPWouldChange (shared/go/runtimecfg/runtimecfg_test.go:150)",
        "why": "9 sub-cases whose fixtures are 6-24 chars and which assert on the WHITESPACE and HEADER-VALUE messages. A length check placed before them rewrites the message and fails all 9. The floor case must be LAST."
      },
      {
        "test": "TestInternalTokenRequiresAnExplicitCredential (:104) / TestInternalTokenRejectsTheRetiredDefaultForever (:114)",
        "why": "EXACT-STRING equality on the error text. Any reworded message, including appending a length clause, fails these two. The retired literal 'local-service-token' is 19 chars, so it must still reach the retired-default case, not the new length case."
      },
      {
        "test": "TestAcceptedCredentialSurvivesAnHTTPRoundTripUnchanged (:199)",
        "why": "A POSITIVE test whose 4 fixtures include 'a-b_c.d~e' (9), 'tok en' (6), 'Zm9vYmFy==' (10). GUARANTEED breakage under any floor. It exists to prove interior spaces survive an HTTP round trip - lengthen the fixtures, do not delete the case."
      },
      {
        "test": "TestSmokeJournalKeyLiteralsMatchScript (services/payments/internal/store/journal_smoke_test.go:701)",
        "why": "Reads scripts/smoke.sh and fails if the literal sets drift (TKT-117). smoke.sh:196-197,209 carry 30- and 31-byte journal keys; journal_smoke_test.go:45-49 owns the same four literals. Edit both or neither."
      },
      {
        "test": "services/payments/cmd/payments/main_test.go:145",
        "why": "A table case asserting on the literal substring '16' for the short-secret rejection. If Q2 raises the journal floor, this expectation moves; if Q2 leaves it at 16, it must stay untouched - which is itself the evidence that the divergence was deliberate."
      },
      {
        "test": "shared/go/runtimecfg/runtimecfg_test.go has NO length test and NO direct no-echo test on RequiredCredential",
        "why": "The no-echo assertions are on the InternalTokenFromEnv wrapper (:109, :120) and on caller guards (catalog/credentials_test.go:36,87; commerce:48; payments/main_test.go:162,183-190). COS clause 3 asks for the assertion on RequiredCredential itself - it does not exist yet, so it is new work, not a copy."
      }
    ],
    "callSites": {
      "note": "14 credential loads across 6 binaries - a per-service fix is the failure mode this ticket exists to avoid.",
      "internalToken": [
        "services/catalog/cmd/catalog/main.go:111",
        "services/access/cmd/access/main.go:152",
        "services/inventory/cmd/inventory/main.go:107",
        "services/inventory/cmd/inventory/reconcile_pins.go:183",
        "services/commerce/cmd/commerce/main.go:121"
      ],
      "requiredCredential": [
        "services/catalog/cmd/catalog/main.go:119 CATALOG_STAFF_WRITE_TOKEN",
        "services/catalog/cmd/catalog/main.go:153 CATALOG_ORGANIZER_ASSERTION_KEY",
        "services/commerce/cmd/commerce/main.go:130 COMMERCE_STAFF_WRITE_TOKEN",
        "services/commerce/cmd/commerce/main.go:145 COMMERCE_CUSTOMER_ASSERTION_KEY",
        "services/commerce/cmd/commerce/main.go:155 PAYMENTS_INTERNAL_TOKEN (direct, not via PaymentsTokenFromEnv)",
        "services/inventory/cmd/inventory/main.go:114 INVENTORY_STAFF_WRITE_TOKEN",
        "services/access/cmd/access/main.go:207 ACCESS_QR_PRIVATE_KEY",
        "services/access/cmd/access/main.go:222 ACCESS_TICKET_LINK_KEY",
        "services/access/cmd/access/lifecycle.go:92 ACCESS_LIFECYCLE_PRIVATE_KEY",
        "services/payments/cmd/payments/main.go:201 JOURNAL_SIGNING_KEY"
      ],
      "paymentsToken": [
        "services/payments/cmd/payments/main.go:266"
      ]
    },
    "fixtureBlastRadius": [
      "scripts/smoke.sh:196,197,209 (30/31-byte journal literals, co-owned with the drift guard)",
      "shared/go/runtimecfg/runtimecfg_test.go:151-172 (9 rejection fixtures), :201-205 (3 positive fixtures under 32)",
      "services/catalog/cmd/catalog/credentials_test.go:69 (31 chars - one short)",
      "services/payments/cmd/payments/main_test.go:102 (30), :105-106 (21/21 decoded), :145 (asserts on '16')",
      "services/catalog/internal/api/server_test.go:1596,1601; channels_test.go:47 (19-28 chars, construct servers directly - unaffected by a runtimecfg floor, affected by a setter floor)",
      "services/inventory/internal/api/staff_credential_test.go:36-37; services/commerce/internal/api/staff_credential_test.go:30-31 (25-27 chars, same caveat)",
      "services/commerce/cmd/commerce/credentials_test.go:19,26-35,56 (call credentialsAreDistinct directly)",
      "web/backoffice/test/api.test.ts:50,54,56,316,329,343,356,649,683 (14-25 chars) and web/backoffice/credentials.mjs:36-59 assertCredentialSeparation - presence + pairwise distinctness only, NO length check. The third place a system-wide floor would have to land."
    ],
    "generatorsAlreadySafe": "scripts/env-bootstrap.sh:72-76,97 and scripts/stack-env.sh:47-125 all draw `od -An -tx1 -N32 /dev/urandom` = 64 hex chars; access keygen emits 43-char base64 seeds. compose.yaml ships ${VAR:?} with no defaults. So no GENERATED value breaks under a 32-byte floor - only hand-written fixtures and the smoke literals do. This is what makes the floor cheap to adopt and is the argument for a hard refusal over a deprecation window (Q3).",
    "docsToCorrect": [
      "docs/development.md:448 - 'nothing here requires JOURNAL_SIGNING_KEY to be high-entropy' underpins the key-id log-safety argument; if Q2 raises the journal floor this paragraph is stale.",
      "docs/adr/ADR-032 line 115 - the blessed 'under 16 bytes' rejection."
    ],
    "nextAdrNumber": "ADR-059 (ADR-058 is the highest at HEAD)"
  }
}
