# SDLC state worktree lives in a sibling dir (<repo>.<branch>), not here
__pycache__/
*.pyc

# build artifacts & dependencies
bin/
node_modules/
dist/
gateway/gateway
services/*/catalog
services/*/inventory
services/*/commerce
services/*/payments
services/*/access

# TKT-39 Astro spike — throwaway prototype, not committed
spike/

# Astro generated types (regenerated by astro sync/build)
.astro/
.env

# Codex/agent prompt briefs materialized at run time (sdlc-ticket, references/codex-runner.md).
# They are scratch input to a model, never repo content.
.plan-brief.*
.brief.*
